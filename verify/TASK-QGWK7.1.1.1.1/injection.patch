--- a/crates/orgasmic-cli/src/sequencer_markers.rs
+++ b/crates/orgasmic-cli/src/sequencer_markers.rs
@@ -41,11 +41,13 @@ pub const STOPPED_PICK_RANGE: &str = "revert or cherry-pick sequence";
 pub const SEQUENCER_MARKERS: &[(&str, &str, &str)] = &[
     ("rebase-merge", "rebase", "rebase"),
     ("rebase-apply", "rebase or am", "am"),
     ("MERGE_HEAD", "merge", "merge"),
     ("CHERRY_PICK_HEAD", "cherry-pick", "cherry-pick"),
-    ("REVERT_HEAD", "revert", "revert"),
+    // INJECTED (TASK-QGWK7.1.1.1.1 B-1): the five-marker set TASK-QGWK7.1.1.1
+    // shipped. `git revert` is the same sequencer machinery as cherry-pick,
+    // so a close inside a revert committed the record and reported `ok`.
     ("BISECT_LOG", "bisect", "bisect"),
     (
         "sequencer",
         STOPPED_PICK_RANGE,
         "an interrupted revert/cherry-pick sequence",
