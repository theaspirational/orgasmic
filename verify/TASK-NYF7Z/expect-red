# Pinned from the real red run of 2026-07-29 with the injection applied
# (scratchpad red.log, dispatch task-nyf7z).
#
# The injection removes the guarded `--strict-mcp-config` push from the rmux
# claude arm and nothing else, which is exactly the pre-fix driver.
exit: 101

# THE SIGNATURE IS THE ARGV ITSELF, and it is worth reading literally: the
# failure message prints the complete pre-fix command line, and it is
# character-for-character the one the task body measured off live worker pid
# 37956 -- four flags, no isolation among them. That is the defect, quoted by
# the proof rather than described by it.
contains: default rmux/claude argv must isolate MCP exactly once
contains: "--dangerously-skip-permissions", "--model"
contains: "--session-id"

# Zero, not "some other count". If a future edit pushed the flag twice, or
# pushed a different isolation flag, the count would not be 0 and this artifact
# would have stopped describing the defect it was authored against.
contains:   left: 0
contains:  right: 1

# The DEFAULT half is what falls. The operator-override half of the same test
# is asserted after it and is never reached under the injection -- if the
# panic ever moves to "operator-supplied flag must suppress the guarded push",
# the guard has broken in the opposite direction and this is not that defect.
contains: modes::rmux::tests::claude_argv_is_mcp_isolated_by_default_and_operator_supplied_wins

# Exactly one test falls. A whole-binary collapse is a different failure and
# would mean this proof has not reproduced what it claims to.
contains: test result: FAILED. 0 passed; 1 failed
