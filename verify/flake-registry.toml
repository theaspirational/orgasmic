# orgasmic:TASK-DJ1WV
#
# The flake registry: every failure this project is willing to see and not act
# on, written down where a machine can read it.
#
# Read by `scripts/run-tests.sh`, which reruns each failure in isolation and
# prints a REAL-vs-FLAKE verdict. Before this file existed, that classification
# was folklore — a human recalling which names were 'known', rerunning them by
# hand, and deciding. It was done from memory four times in one day, and once a
# failure was filed against the wrong owning task because one test name carried
# two different failure modes. Red that needs folklore to interpret teaches
# everyone to discount red.
#
# An entry is an excuse with an expiry, not a permanent exemption:
#
#   test       the name cargo prints. A bare function name also matches a
#              module-qualified failure, so an entry survives a test moving.
#   owner      the OPEN task that is fixing it. `--check` fails if that task is
#              done or cancelled, or is not a task at all. A registry that only
#              grows is a graveyard.
#   signature  a literal substring of the panic. This is the load-bearing
#              field: a failure of a registered NAME whose panic does not match
#              the registered SIGNATURE is reported REAL. That is the mislabel
#              detector — one test may carry two entries with two signatures and
#              two owners, and the runner will not confuse them.
#   evidence   how it was measured: what failed, what passed, when.
#   filed      the date the entry was added.
#
# Do not add an entry from memory. Every signature below is either measured in
# this repo or cited to a dated measurement recorded in `.orgasmic/tasks/`, and
# every one of them exists as a literal assertion message in the current source
# — grep it before you trust it.

# --------------------------------------------------------------------------
# TASK-X0PV1 — daemon tests that fail under full-suite parallelism, pass alone
# (owned by TASK-STWVB until 2026-08-07; transferred when that round closed with
# these modes still unfixed, rather than deleting live entries to green --check)
# --------------------------------------------------------------------------

[[flake]]
test = "supervisor::tests::poll_direct_child_pid_prefers_worker_server_over_generic_sibling"
owner = "TASK-X0PV1"
signature = "fake cursor-agent did not start children"
evidence = "Manager measurement 2026-07-28 at HEAD 15997c6, full daemon --lib: 528 passed / 5 failed, this among them. Assertion is crates/orgasmic-daemon/src/supervisor.rs:10321. Green alone here 2026-07-28 (1 passed, 548 filtered out); NOT reproduced in 6 runs in this worktree, including 3 under induced load."
filed = "2026-07-28"

[[flake]]
test = "api::tests::recovery_reattaches_tmux_session_when_handle_exists"
owner = "TASK-X0PV1"
signature = "tmux test session should start"
evidence = "Manager measurement 2026-07-28 at HEAD 15997c6, and identically inside the TASK-1T3FZ dispatched worker, where the daemon error was 'tmux new-session failed (exit 1): server exited unexpectedly'. Assertion is crates/orgasmic-daemon/src/api.rs:23885. Green alone here 2026-07-28; NOT reproduced in 6 runs in this worktree."
filed = "2026-07-28"

# Two failure modes, one name. The env-race mode belonged to TASK-5HBST and is
# FIXED — registering it would re-excuse a defect that no longer exists and
# would file the next failure against a closed task, which is the exact mistake
# this registry was built to make impossible. Only mode 2 is registered.
# The second mode of the same test, and the first thing this registry caught.
# The full-suite run below failed on the registered NAME with an unregistered
# panic, so the runner reported it REAL rather than waving it through as the
# tmux-startup flake — which is what a human classifying from memory would have
# done. Registered afterwards, with the panic it actually produced.
[[flake]]
test = "api::tests::recovery_reattaches_tmux_session_when_handle_exists"
owner = "TASK-X0PV1"
signature = "left: \"interrupted\""
evidence = "Measured here 2026-07-28: full cargo test --workspace under 10 cpu hogs, 1 failure in the whole workspace, crates/orgasmic-daemon/src/api.rs:23908 assertion left == right, left 'interrupted' right 'reattached'. Green alone on the same tree. TASK-STWVB's own body records this mode: 'it is tmux session REATTACH, and it lands on interrupted rather than reattached', pointing at cross-test contention over shared tmux state."
filed = "2026-07-28"

[[flake]]
test = "api::tests::production_resume_native_fork_uses_pinned_claude_not_path_shim"
owner = "TASK-X0PV1"
signature = "resume_native_fork recover: 500"
evidence = "Manager measurement 2026-07-28 on the merged TASK-5HBST tree at load ~10: still failed, with mode 2 (recovery 500) and not the env race. Assertion is crates/orgasmic-daemon/src/api.rs:25580. Green alone here 2026-07-28; NOT reproduced in 6 runs in this worktree."
filed = "2026-07-28"

# --------------------------------------------------------------------------
# TASK-X0PV1 (continued) — two orgasmic-cli dispatch-suite tests in the same
# fail-under-full-suite-parallelism, pass-alone class the task already owns.
# Both measured 2026-08-30 during release certification of b00b48bd:
# `cargo test --workspace --no-fail-fast` red on each, and the classifier's own
# isolation rerun green on each ("isolation: passed" in the run-tests report),
# twice in a row (two certification rounds, 20260830).
# --------------------------------------------------------------------------

[[flake]]
test = "dispatch_close_records_cleanup_failure_and_status_filter_lists_it"
owner = "TASK-X0PV1"
signature = "cleanup failure close should still append tx"
evidence = "Measured 2026-08-30 on b00b48bd (and previously on 9413059a): full-workspace certification suite red at crates/orgasmic-cli/tests/dispatch.rs assertion 'cleanup failure close should still append tx'; run-tests.sh isolation rerun green both rounds. Same-family entries above measure the shared-daemon/tmux contention trigger."
filed = "2026-08-30"

[[flake]]
test = "dispatch_timeout_requests_daemon_cleanup"
owner = "TASK-X0PV1"
signature = "daemon cleanup should remove branch after CLI timeout"
evidence = "Measured 2026-08-30 on b00b48bd (and previously on 9413059a): full-workspace certification suite red at crates/orgasmic-cli/tests/dispatch.rs assertion 'daemon cleanup should remove branch after CLI timeout'; run-tests.sh isolation rerun green both rounds."
filed = "2026-08-30"

# --------------------------------------------------------------------------
# TASK-QCG6J — the reattach-fence regression asserts WHICH refusal fired, so it
# goes red on a safe outcome under load. Two assertions in one test body, so two
# entries: whichever one trips, the owner is named and the other is not blamed.
# --------------------------------------------------------------------------

[[flake]]
test = "api::tests::explicit_reattach_tmux_is_fenced_out_of_a_worktree_a_close_guard_holds"
owner = "TASK-QCG6J"
signature = "the same action is admitted once the guard is released"
evidence = "Measured here 2026-07-28: full daemon --lib at --test-threads=48 under 8 cpu hogs, 548 passed / 1 failed, panic at crates/orgasmic-daemon/src/api.rs:19247 with 'ApiError { status: 400, message: recovery action reattach_tmux is not valid for run run-fence-b73c0c65 }'. Passed in the same suite at default parallelism."
filed = "2026-07-28"

[[flake]]
test = "api::tests::explicit_reattach_tmux_is_fenced_out_of_a_worktree_a_close_guard_holds"
owner = "TASK-QCG6J"
signature = "the refusal must name the cleanup reservation"
evidence = "The mode TASK-QCG6J was filed from: manager gate on TASK-AK6EM, 2026-07-28, panic at crates/orgasmic-daemon/src/api.rs:19225 — the reattach WAS refused, by an earlier validity check rather than by the cleanup fence. Same test, same owner, different assertion than the entry above."
filed = "2026-07-28"

# --------------------------------------------------------------------------
# Deliberately NOT registered. Each of these is a failure somebody could excuse
# from memory today; none of them may be excused by this file, and the runner
# will report every one of them REAL until that changes.
#
#   api::tests::recovery_inventory_waits_for_atomic_claim_commit  (TASK-STWVB)
#   index::tests::git_resolves_quoted_included_origin_from_linked_worktree
#                                                                 (TASK-STWVB)
#     Named as known flakes, and green alone here on 2026-07-28, but no panic
#     text for either has ever been written down and neither reproduced in 6
#     runs here. A signature guessed from the assertions in the test body would
#     excuse whichever one fired — which is the mislabel this registry exists to
#     prevent. Register them the next time one is caught, with its real panic.
#
#   index::tests::live_rebuild_preserves_repo_url_and_schedules_post_bind_refresh
#     Same run: assertion left == right, left "" right
#     "ssh://git@example.com/org/project.git", index.rs:2095.
#     It was load-dependent and had no owning task, so it could not be
#     registered: an excuse with no owner is a permanent exemption.
#
#     FIXED 2026-07-29 by TASK-5FEN5, so it never became an entry. The panic
#     above was reproduced verbatim here first (1 of 11 loaded runs
#     at --test-threads=64 under 10-12 hogs), and both tests turned out to be
#     observing background work on a fixed budget rather than waiting for it to
#     finish. Proof: verify/TASK-5FEN5.
# --------------------------------------------------------------------------

# --------------------------------------------------------------------------
# TASK-TZKAC — DELETED, not registered. The lock-acquisition budget was fixed.
#
# The entry that stood here named
# `api::tests::deleted_or_corrupt_claim_reconstructs_exact_status_and_post`,
# signature "the configured port is 0, so the incumbent address cannot be
# probed", filed 2026-07-29 by TASK-J1XCB and re-owned to TASK-TZKAC the same
# day. It is gone because the defect under it is gone, which is what the rule at
# the top of this file requires.
#
# The mechanism, which J1XCB left unexplained, turned out not to need the
# holder's identity at all. `acquire_daemon_lock` retried the lock only inside
# DAEMON_LOCK_RETRY_BUDGET (125ms, sized by TASK-870YX for a CLI probe that
# holds it for microseconds) and then, finding no shutdown marker, stopped
# trying the lock ENTIRELY and refused on whatever the incumbent probe could not
# establish — and every answer that probe can give short of "a healthy daemon is
# already running" is a condition the holder can grow out of a millisecond
# later. So a transient holder produced a permanent refusal, and this test could
# only ever have been fixed by acquiring the lock: `test_options()` sets
# `port_override: Some(0)`, which makes its probe permanently impossible.
#
# Fixed at the protocol level, not in the test: past the transient budget the
# loop keeps trying the lock, keeps re-reading the marker, and keeps probing, up
# to a ceiling DERIVED from this daemon's own `ShutdownBudgets::total` — so no
# holder, declared or not, can cost a replacement more wall clock than a real
# shutdown can. Proof: verify/TASK-TZKAC, whose injected red reproduces J1XCB's
# second measured refusal verbatim.
#
# The sibling this entry's comment named,
# `tests::daemon_lock_retries_a_transient_probe_hold`, is fixed by the same
# change and was never registered.
#
# Measured, this worktree, 2026-07-29. Pre-fix, carrying the pid probe J1XCB
# proposed: 28 loaded copy-runs (7 rounds x 4 concurrent copies of the daemon
# --lib binary at --test-threads=64) reproduced NEITHER test — J1XCB's own rate
# was 1 in 36, so the holder was never caught in the act and is still
# unidentified. Post-fix, same harness: 7 complete rounds, 28 copy-runs, 0
# failures of either test, and one captured instance of the new wait doing its
# job on the real path — a `Daemon::run` met an undeclared holder, waited 488ms,
# and booted, where pre-fix it would have refused. The refusal now names the pid
# the lock file records and whether it is alive, so the next sighting answers
# the question these runs could not.
# --------------------------------------------------------------------------

# --------------------------------------------------------------------------
# TASK-37WN7 — the SIGTERM handler-installation window, still open despite the
# mitigation built for it.
#
# `serve` registers its SIGTERM handler AFTER `Daemon::run` returns, so a
# SIGTERM sent on the strength of a successful connect can land on the default
# disposition. TASK-Q07Y5 added `wait_until_serve_awaits_signals` for exactly
# this and its comment concedes the window is "invisible on an idle machine …
# under a loaded test binary it is not". This entry exists because the
# mitigation demonstrably does not close it.
#
# Registered 2026-08-05, and only once a signature was in hand. It was
# deliberately left OUT of this registry when TASK-37WN7 was filed earlier the
# same day: no panic had been captured, and a signature-less entry makes every
# future failure of this test name read as this known flake — which is the
# "trains everyone to discount failures" outcome this file exists to prevent.
#
# Measured, this worktree, 2026-08-05, on the TASK-M47E5.2 merge (b20fba5):
# full `scripts/run-tests.sh -p orgasmic-cli` red with this one failure, the
# wrapper's own isolation retry green. The manager then re-ran it alone against
# the already-built binary: 1 failure in the first 14 isolated runs and 12/12
# green immediately after. So it is NOT purely load-dependent — it reproduces
# alone at roughly 7%, which the "passes in isolation" folklore had been hiding.
# The failing assertion is `status.signal()`, i.e. the process really did die on
# the default disposition; it is not the 30s `try_wait` deadline.
#
# The signature pins that one assertion. A failure of this test on the deadline
# branch, or on the exit-code branch, is NOT this flake and must read as REAL.
#
# MECHANISM, traced 2026-08-05 at f60af0a — read this before deciding the entry
# has earned its place. `serve` prints "press Ctrl+C to stop" at main.rs:2299 and
# THEN calls `wait_for_shutdown_signal()` at :2300, which installs the SIGTERM
# handler at :2320. `wait_until_serve_awaits_signals` waits for that printed
# line. So the mitigation observes a point STRICTLY BEFORE registration — the
# wrong side of the boundary. It cannot close this window however it is
# strengthened, because at the moment the only observable signal is emitted, the
# thing it would need to observe has not happened.
#
# That is why this entry is the shortest-lived kind of excuse: the fix is to
# install the handler before announcing readiness, which is cheap and obvious
# once the ordering is seen. TASK-37WN7 owns it and its acceptance now REQUIRES
# deleting this entry in the same change. If you are reading this comment long
# after `filed`, the entry has outlived its justification — that is a finding.
[[flake]]
test = "sigterm_exits_through_graceful_shutdown_rather_than_default_disposition"
owner = "TASK-37WN7"
filed = "2026-08-05"
signature = "serve died on SIGTERM's default disposition"
evidence = "Measured 2026-08-05 on b20fba5: full orgasmic-cli suite red with this sole failure at crates/orgasmic-cli/tests/daemon_lifecycle.rs:319, `assertion left == right failed: serve died on SIGTERM's default disposition, so the graceful shutdown (release-finalization drain, writer shutdown) never ran`. Isolation retry green. Manager re-measurement: 1 failure in 14 isolated runs, then 12/12 green — so it reproduces without load at ~7%, contradicting the load-only reading. The change under test touched crates/orgasmic-cli/src/manager.rs and its tests only; daemon_lifecycle.rs and the serve path are byte-identical to base."
