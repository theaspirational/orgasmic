diff --git a/crates/orgasmic-daemon/tests/dispatch_preflight.rs b/crates/orgasmic-daemon/tests/dispatch_preflight.rs
index 64f449e..a157c07 100644
--- a/crates/orgasmic-daemon/tests/dispatch_preflight.rs
+++ b/crates/orgasmic-daemon/tests/dispatch_preflight.rs
@@ -160,7 +160,6 @@ async fn an_unusable_credential_leaves_no_lease_no_session_and_no_run() {
     std::env::remove_var("ORGASMIC_DRIVER_SIMULATE");
     let harness_dir = tempfile::tempdir().unwrap();
     write_logged_out_claude_stub(harness_dir.path());
-    warm_up_stub(harness_dir.path());
     let saved_path = std::env::var("PATH").unwrap_or_default();
     std::env::set_var(
         "PATH",
@@ -312,6 +311,7 @@ async fn an_unusable_credential_leaves_no_lease_no_session_and_no_run() {
 /// evaluation of a brand-new executable off the probe's budget, and it turns a
 /// stub that cannot answer at all into a failure that says so, instead of one
 /// that surfaces as a mystified 200 much further down.
+#[allow(dead_code)]
 fn warm_up_stub(dir: &Path) {
     let output = std::process::Command::new(dir.join("claude"))
         .args(["auth", "status"])
@@ -338,6 +338,7 @@ fn write_logged_out_claude_stub(dir: &Path) {
         &stub,
         r#"#!/bin/sh
 if [ "$1" = "auth" ] && [ "$2" = "status" ]; then
+  sleep 120
   printf '%s\n' '{"loggedIn":false,"authMethod":"none","apiProvider":"firstParty"}'
   exit 1
 fi
