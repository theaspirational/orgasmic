diff --git a/crates/orgasmic-daemon/src/api.rs b/crates/orgasmic-daemon/src/api.rs
index 5934487..d88a219 100644
--- a/crates/orgasmic-daemon/src/api.rs
+++ b/crates/orgasmic-daemon/src/api.rs
@@ -9051,25 +9051,7 @@ async fn post_run_recover(
     // "there is no authenticated replacement for this origin" is a statement
     // only a completed enumeration can make. An unresolved one must refuse, or
     // the new replacement lands beside whatever the unread file was holding.
-    if failed_origin {
-        let unobserved = match origin_links.links_for(
-            &state.home,
-            &origin_authority.project_root,
-            &project_id,
-        ) {
-            AuthoritativeOriginLinks::Unobserved(reason) => Some(*reason),
-            AuthoritativeOriginLinks::Complete(_) => None,
-        };
-        if let Some(reason) = unobserved {
-            // A swapped or symlinked ORIGIN session is one of the ways the
-            // enumeration cannot complete, and it has its own, more specific
-            // refusal. Report that one: "the origin you took authority over is
-            // no longer the file you took it over" is actionable, while "the
-            // enumeration did not complete" is not.
-            origin_authority.envelopes()?;
-            return Err(recovery_origins_unobserved(&id, reason));
-        }
-    }
+    // INJECTION (F3): mint without proving the origin has no replacement.
     let request_id = req
         .request_id
         .clone()
diff --git a/crates/orgasmic-daemon/src/recovery_claim.rs b/crates/orgasmic-daemon/src/recovery_claim.rs
index 6e5cd03..487de96 100644
--- a/crates/orgasmic-daemon/src/recovery_claim.rs
+++ b/crates/orgasmic-daemon/src/recovery_claim.rs
@@ -1206,10 +1206,9 @@ fn complete_session_scan(
     session_dir: &SessionDirectory,
     session_path: &Path,
 ) -> Result<SessionLifecycleScan, RecoveryClaimError> {
-    match session_dir.scan_path(session_path, SessionScanBudget::DEFAULT) {
-        Ok(scan) if !scan.truncated => Ok(scan),
-        _ => session_dir.scan_path_complete(session_path),
-    }
+    // INJECTION (orgasmic:TASK-2QK4P.1.1 F2): index from the bounded scan and
+    // treat its skipped middle as absence, exactly as a503213 did.
+    session_dir.scan_path(session_path, SessionScanBudget::DEFAULT)
 }
 
 #[cfg(not(unix))]
@@ -1231,8 +1230,10 @@ pub fn index_recovery_origins_in_session(
     let session_dir = match SessionDirectory::open(project_root) {
         Ok(dir) => dir,
         Err(_) => {
-            return IndexedRecoveryOrigins::Unobserved {
-                reason: UnobservedSession::SessionDirectoryUnavailable,
+            // INJECTION (F1): an observation that failed, reported as one
+            // that succeeded and found nothing.
+            return IndexedRecoveryOrigins::Complete {
+                links: Vec::new(),
                 bytes_inspected: 0,
             }
         }
@@ -1244,8 +1245,10 @@ pub fn index_recovery_origins_in_session(
     let scan = match scan {
         Ok(scan) => scan,
         Err(_) => {
-            return IndexedRecoveryOrigins::Unobserved {
-                reason: UnobservedSession::SessionUnreadable,
+            // INJECTION (F1): an observation that failed, reported as one
+            // that succeeded and found nothing.
+            return IndexedRecoveryOrigins::Complete {
+                links: Vec::new(),
                 bytes_inspected: 0,
             }
         }
@@ -1352,12 +1355,8 @@ pub fn index_recovery_origins_in_session(
             let origin_scan = complete_session_scan(&origin_session_path);
             let origin_scan = match origin_scan {
                 Ok(scan) => scan,
-                Err(_) => {
-                    return IndexedRecoveryOrigins::Unobserved {
-                        reason: UnobservedSession::OriginSessionUnreadable,
-                        bytes_inspected,
-                    }
-                }
+                // INJECTION (F1): drop the undecidable link instead.
+                Err(_) => continue,
             };
             bytes_inspected += origin_scan.bytes_inspected;
             let origin_envelopes = origin_scan.envelopes;
@@ -1471,7 +1470,8 @@ impl ProjectOriginAuthority {
         project_root: &Path,
         project_id: &str,
     ) -> &AuthoritativeOriginLinks {
-        if !self.by_project.contains_key(project_root) {
+        // INJECTION (F4): never memoize; re-enumerate per claim.
+        if true {
             let (links, cost) = enumerate_recovery_origin_links(home, project_root, project_id);
             self.cost.files += cost.files;
             self.cost.bytes_inspected += cost.bytes_inspected;
@@ -1530,12 +1530,11 @@ fn enumerate_recovery_origin_links(
                 cost.bytes_inspected += bytes_inspected;
                 links.extend(found);
             }
+            // INJECTION (F1): fold it in as if the file were empty.
             IndexedRecoveryOrigins::Unobserved {
-                reason,
-                bytes_inspected,
+                bytes_inspected, ..
             } => {
                 cost.bytes_inspected += bytes_inspected;
-                return (AuthoritativeOriginLinks::Unobserved(reason), cost);
             }
         }
     }
@@ -1652,9 +1651,9 @@ pub fn resolve_authoritative_recovery_claim(
             }
             Ok(ResolvedRecoveryClaim::Valid(claim))
         }
-        Ok(None) => {
-            reconstruct_or_quarantine(home, project_root, project_id, origin_run_id, authoritative)
-        }
+        // INJECTION (F3): the catalog-derived slice, silent while the
+        // replacement's record is invalidated.
+        Ok(None) => reconstruct_or_quarantine(home, project_root, project_id, origin_run_id, &[]),
         Err(RecoveryClaimError::CorruptClaim) => {
             quarantine_invalid_claim(home, project_id, origin_run_id)?;
             reconstruct_or_quarantine(home, project_root, project_id, origin_run_id, authoritative)
