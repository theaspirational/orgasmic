# Two tests: the durable parser and the safe stdio decline path.
env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-daemon --lib -- boot_reattach_candidate_binds_every_resolved_credential_mode a_credential_resolving_stdio_run_is_declined_without_holding_its_lease
