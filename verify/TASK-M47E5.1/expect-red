# Pinned from a real red run of 2026-08-05 against the narrowed injection.
#
# The injection restores ONLY the `worktree.parent()` fallback in
# `project_root`. Since TASK-M47E5 the managed worktree lives at
# `~/.orgasmic/worktrees/<project-id>/<task>/`, so that parent is the managed
# worktree root — a surface no project owns — and a path allowlist keyed on it
# grants reads under a `.orgasmic/tmp/dispatch` path that is nobody's dispatch
# directory.
exit: 101

# THE DEFECT, and the first failing assertion of its test under the injection.
contains: an unconfigured project root must not be guessed from the worktree

# Exactly one, and no collateral. A binary that collapses wholesale, or a run
# where the worktree grant also broke, is a different failure and has not
# reproduced the defect this artifact claims.
contains: test result: FAILED. 0 passed; 1 failed
