# One line. Run from the repo root by `orgasmic verify TASK-RQ270.5.1`.
# Pins the long-help value-enum hole: shipped prose naming --kind architector
# (the worker kind stage E excised) must fail against clap possible_values.
# Pre-fix that mutation passed green because possible_values_for_flag only
# matched clap's compact one-line layout; post-fix it is the first failure.
env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-cli --test cli_parity shipped_value_enum_arguments_match_clap_possible_values
