# One line. Run from the repo root by `orgasmic verify TASK-HQ970`.
# `env -u ...` because a dispatch leaks its run id and home into subprocesses.
env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-daemon --test tx_ledger_guard
