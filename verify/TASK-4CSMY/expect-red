# The injection removes tmux pane activity and quiet-pane process/status
# evidence. Both owned tests must fail: the driver sees no activity, and the
# daemon releases the healthy provider-bound turn.
exit: nonzero
contains: tmux_pane_activity_publishes_raw_byte_counts_from_a_real_pane
contains: a_network_waiting_tmux_turn_outlives_the_stall_window_and_a_wedge_dies
