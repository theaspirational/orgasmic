# One line. Run from the repo root by `orgasmic verify TASK-XPYRR`.
#
# The production path, not the HTTP contract: the real `orgasmic` binary
# against a real daemon over a scaffolded project, because the defect this
# task was filed about was a missing *CLI surface* over a daemon op that
# already worked. A daemon-level test would have been green the whole time.
env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-cli --test task_title_edit_cli
