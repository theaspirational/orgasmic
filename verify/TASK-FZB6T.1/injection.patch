diff --git a/crates/orgasmic-daemon/src/run_catalog.rs b/crates/orgasmic-daemon/src/run_catalog.rs
index b568056..b488c7e 100644
--- a/crates/orgasmic-daemon/src/run_catalog.rs
+++ b/crates/orgasmic-daemon/src/run_catalog.rs
@@ -994,19 +994,14 @@ impl RunCatalog {
             let probed = probe_authority_path(&planned.previous)
                 .and_then(|probe| reverify_authority(&planned.previous, probe));
             let derived = probed.as_ref().unwrap_or(&planned.previous);
-            let updated = match reconcile_authority_with_ledger(
-                derived,
-                ledger_unusable.is_some(),
-                &ledger,
-                &planned.run_id,
-                project_id,
-            ) {
-                Some(overruled) => {
-                    count_ledger_overrule(&overruled, &mut stats);
-                    Some(overruled)
-                }
-                None => probed,
-            };
+            // INJECTED (TASK-FZB6T.1): the durable tombstone ledger does not
+            // reach the cache-hit path, so the revival above is the only
+            // authority — which is the tree state this defect was observed in.
+            // Without this the ledger (TASK-FZB6T.3 finding 4, wired to this
+            // path by TASK-FZB6T.5 finding 2) re-tombstones the revived entry
+            // on the same pass and masks the defect under test.
+            let _ = derived;
+            let updated = probed;
             let Some(updated) = updated else {
                 continue;
             };
@@ -1232,29 +1227,11 @@ pub fn project_sessions_dir(project_root: &Path) -> PathBuf {
 ///
 /// A refused entry costs one bounded re-scan of a file that is on disk anyway.
 fn snapshot_entry_is_admissible(entry: &RunCatalogEntry, sessions_dir: &Path) -> bool {
-    let path = entry.session_path.as_path();
-    if path.parent() != Some(sessions_dir) {
-        return false;
-    }
-    if path
-        .components()
-        .any(|component| matches!(component, std::path::Component::ParentDir))
-    {
-        return false;
-    }
-    if path.extension().and_then(|ext| ext.to_str()) != Some("jsonl") {
-        return false;
-    }
-    let Ok(metadata) = std::fs::symlink_metadata(path) else {
-        return false;
-    };
-    if !metadata.is_file() || metadata.file_type().is_symlink() {
-        return false;
-    }
-    if SessionFileFingerprint::of(&metadata) != entry.fingerprint {
-        return false;
-    }
-    snapshot_entry_semantics_are_self_consistent(entry)
+    // INJECTED (TASK-FZB6T.1): the pre-fix rule — anything under the project
+    // root is admitted, with no session-directory authority, no traversal
+    // check, and no current file identity check.
+    let project_root = sessions_dir.ancestors().nth(3).unwrap_or(sessions_dir);
+    entry.session_path.starts_with(project_root)
 }
 
 /// Whether an entry's semantic claims are reproduced by its own source
@@ -1572,11 +1549,10 @@ struct AuthorityProbe {
 fn probe_authority_path(authority: &WorktreeAuthority) -> Option<AuthorityProbe> {
     let path = match authority {
         WorktreeAuthority::Verified { worktree, .. } => worktree.as_path(),
-        // `Unprovable` is a statement about the LEDGER, not about the path: no
-        // stat can change it, and re-probing would only re-derive the verdict
-        // that could not be checked in the first place.
-        WorktreeAuthority::Tombstoned { .. }
-        | WorktreeAuthority::Mismatched { .. }
+        // INJECTED (TASK-FZB6T.1): a tombstone is probed again, so a directory
+        // that later appears at the recorded path can revive it.
+        WorktreeAuthority::Tombstoned { recorded, .. } => recorded.as_path(),
+        WorktreeAuthority::Mismatched { .. }
         | WorktreeAuthority::Unrecorded
         | WorktreeAuthority::Unidentified
         | WorktreeAuthority::Unprovable { .. } => return None,
@@ -1608,6 +1584,17 @@ fn reverify_authority(
     previous: &WorktreeAuthority,
     probe: AuthorityProbe,
 ) -> Option<WorktreeAuthority> {
+    // INJECTED (TASK-FZB6T.1): existence at the recorded path is enough to
+    // revive a tombstone, whatever directory now answers to it.
+    if let WorktreeAuthority::Tombstoned { recorded, .. } = previous {
+        if !probe.exists {
+            return None;
+        }
+        return Some(WorktreeAuthority::Verified {
+            worktree: recorded.clone(),
+            identity: probe.current,
+        });
+    }
     let WorktreeAuthority::Verified { worktree, identity } = previous else {
         return None;
     };
