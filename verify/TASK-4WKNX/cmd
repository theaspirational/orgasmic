# One line. Run from the repo root by `orgasmic verify TASK-4WKNX`.
# `env -u ...` because a dispatch leaks its run id and home into subprocesses.
env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-cli --test dispatch dispatch_close_uses_fix_subtask_property_and_abort_backlog
