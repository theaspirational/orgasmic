* WORKER reviewer-claude-rmux
:PROPERTIES:
:ID:                          reviewer-claude-rmux
:KIND:                        reviewer
:DRIVER:                      rmux
:HARNESS:                     claude
:PROVIDERS:                   anthropic
:MODELS:                      claude-sonnet-5 claude-opus-4-8 claude-haiku-4-5
:REASONING_EFFORTS:           low medium high xhigh
:DEFAULT_PROVIDER:            anthropic
:DEFAULT_MODEL:               claude-opus-4-8
:DEFAULT_EFFORT:              xhigh
:LINKED_SKILLS:
:APPLICABLE_STATES:           working, done, blocked, cancelled, approved, requested_changes
:MAX_ITERATIONS:              10
:CONTEXT_BUDGET:              150000
:VERSION:                     1
:END:

** Persona
This reviewer runs inside the interactive Claude TUI through an rmux pane, so
its output is captured from a rendered terminal. Keep findings and verdicts as
plain scannable text; avoid redraw-heavy output that would bloat the captured
session log.

** Operating Rules
- Review the merged diff named in the brief; file findings per the reviewer
  prompt spec and report a verdict (ship / has-issues) with severity counts.
- Completion is `orgasmic dispatch finalize` per the base_worker prompt-spec
  (inherited by every kind). Also print the orgasmic end-of-turn marker
  requested in the runtime prompt on its own final line so the rmux driver
  can release the pane; the driver's scrollback scrape is only a FALLBACK
  for a run that never finalizes.

** Notes
Authored [2026-06-10] for goal-20260610-drain-small-backlog (operator-directed
rmux+claude dispatch batch). Same rmux provisioning caveat as
implementer-claude-rmux: verify the capabilities event on first dispatch.

Model/effort selection: =:DEFAULT_MODEL:= / =:DEFAULT_EFFORT:= pin the model
and reasoning effort for dispatches that pass no explicit override; a custom
template only needs to set those two. For harness-specific flags the schema
has no property for, set =:HARNESS_ARGS:= (whitespace-separated argv passed
verbatim to the harness CLI, e.g. =--model claude-haiku-4-5 --betas
context-1m=); flags there win over the defaults.
