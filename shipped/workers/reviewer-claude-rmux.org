* WORKER reviewer-claude-rmux
:PROPERTIES:
:ID:                          reviewer-claude-rmux
:KIND:                        reviewer
:DRIVER:                      rmux
:HARNESS:                     claude
:PROVIDERS:                   anthropic
:DEFAULT_PROVIDER:            anthropic
:LINKED_SKILLS:
:APPLICABLE_STATES:           working, done, blocked, cancelled, approved, requested_changes
:MAX_ITERATIONS:              10
:CONTEXT_BUDGET:              150000
:VERSION:                     1
:END:

** Persona
This reviewer runs inside the interactive Claude TUI through an rmux pane.
Keep findings and verdicts as plain scannable text; avoid redraw-heavy output.

** Operating Rules
- Review the merged diff named in the brief; file findings per the reviewer
  prompt spec and report a verdict (ship / has-issues) with severity counts.
- Completion is `orgasmic dispatch finalize` per the base_worker prompt-spec
  (inherited by every kind). Pane/process exit without finalize is a failed
  run — there is no end-of-turn marker or scrollback-scrape fallback.

** Notes
Authored [2026-06-10] for goal-20260610-drain-small-backlog (operator-directed
rmux+claude dispatch batch). Same rmux provisioning caveat as
implementer-claude-rmux: verify the capabilities event on first dispatch.

Model/effort selection: pass explicit =--model= and effort flags on each
dispatch via =orgasmic manager dispatch --model … --effort …=, or set
=:HARNESS_ARGS:= (whitespace-separated argv passed verbatim to the harness
CLI, e.g. =--model claude-haiku-4-5 --betas context-1m=).
