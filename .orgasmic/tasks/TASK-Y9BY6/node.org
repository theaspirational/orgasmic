#+title: orgasmic task TASK-Y9BY6
#+orgasmic_version: 2

* IN_PROGRESS TASK-Y9BY6 live_rmux_exit_139_emits_a_fatal_terminal_event times out on every run :testing:drivers:flake:
:PROPERTIES:
:ID: TASK-Y9BY6
:PRIORITY: P2
:END:

`modes::rmux::tests::live_rmux_exit_139_emits_a_fatal_terminal_event` fails
deterministically with `timed out waiting for rmux exit 139 event: Elapsed(())`
(rmux.rs:2871).

Measured 2026-07-26: 3/3 failures on a working tree carrying the TASK-SGRTX
changes, and 2/2 on stashed HEAD without them. So it is pre-existing and
unrelated to that work — recorded here so the next person does not re-derive
that, as happened this morning with the dispatch_endpoint three.

Not investigated. `rmux` IS installed (`~/.local/bin/rmux`) and an rmux-daemon
is running under the orgasmic LaunchAgent config, so "binary missing" is not the
explanation. Candidates worth checking first:
- the test drives the DEFAULT rmux socket, which the running LaunchAgent daemon
  may already own with different config (see the socket-split note in the
  isolated-smoke recipe)
- exit 139 is SIGSEGV; whether the stub actually segfaults on this machine and
  rmux reports it as 139 is unverified

Belongs with TASK-STWVB's theme: the drivers suite cannot answer "did my change
break something?" while one test fails every run for an unrelated reason.
CORRECTION [2026-07-26], same session. This task's opening claim that the test
"fails deterministically" is wrong.

It failed 5/5 when filed (3/3 on the working tree, 2/2 on stashed HEAD). Roughly
ninety minutes later the same test passes, and the full drivers lib suite is
231/231 green on 3 consecutive runs. Nothing in the test or in rmux changed
between those measurements.

What did change is machine state. Between the two, an isolated smoke daemon was
started and stopped twice on port 4899, rmux servers were killed during cleanup,
and a dispatched worker was running full test suites in a worktree. So the
trigger is environmental and shared — most likely the default rmux socket
ownership described in the isolated-smoke recipe — not the test's own logic.

Two other tests were also seen failing exactly once in that window and passing
on every run since:
- adapters::claude::tests::acp_stdio_accepts_a_dispatch_for_a_logged_in_claude
- adapters::claude::tests::acp_stdio_rejects_a_dispatch_for_a_logged_out_claude
- adapters::claude::tests::an_empty_endpoint_still_gets_a_verdict_because_it_still_spawns_claude

Those three manipulate process-global PATH and HOME to simulate a logged-out
harness, which is the documented shared-environment hazard in gotchas
("Tests that set PATH break every other test in the binary"). They are the
drivers-crate instance of TASK-STWVB's disease.

So re-scope: this is not one deterministic rmux failure. It is that the drivers
suite has environment-coupled tests whose result depends on what else is running
on the machine, which is the same reason the daemon suite could not answer "did
my change break something?". Reproduce by running the suite WHILE a second
daemon or an rmux server is live, not by running it alone.

