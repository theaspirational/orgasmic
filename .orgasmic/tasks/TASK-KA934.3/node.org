#+title: orgasmic task TASK-KA934.3
#+orgasmic_version: 2

* IN_REVIEW TASK-KA934.3 Open task-comment routes to members with the :ACTOR: collision guard :e01mc:chain-review:
:PROPERTIES:
:ID: TASK-KA934.3
:IMPLEMENTS:  dec_Q78QN
:PRIORITY: P2
:RELATES_TO: TASK-KA934.2 TASK-KA934.1
:END:

Implement : add the three task-comment routes to MEMBER_ALLOWED_ROUTES (api.rs:~897); make :ACTOR: a guarded identity namespace (refuse or override an admin req.actor that equals a members.org member name - require_comment_body compares raw strings, writer.rs:~1657); document what a member rename does to edit rights on existing comments; re-enable the member-attribution path test; finish with one live HTTP probe as a member session.

** Acceptance
- [ ] Member session can POST comment/edit/delete over HTTP; admin :ACTOR: collision refused or explicitly overridden (test each).
- [ ] task_comments_use_member_session_attribution_and_refresh_activity exercises the real route.
- [ ] Rename semantics stated in the code or docs; daemon lib tests + clippy -D + fmt green.
** Notes
Implementer opencode / zai-coding-plan/glm-5.3 variant max, commit 3181651c, merged 9f6874f0. Manager collision check 2026-09-02: live members.org (~/.orgasmic/user/auth/members.org) has one member, Victor; the live daemon actor is aspirational (212 tx entries this month) - no collision, admin comments keep working after upgrade. Residual disclosed by the implementer: POST /artifacts/:id/comments shares the attribution pattern and is not guarded (candidate follow-up). Guard fails open on a members.org read error (admin-owned file).
Review (opus-5 high, tx-20260901-orgasmic-bfe6e70a): APPROVE WITH FOLLOW-UPS. MEDIUM api.rs:~2991 post_tx: the :ACTOR: guard is one route deep - POST /tx with type=comment + --task + --actor <member-name> (orgasmic tx record --actor) writes the same journal entry unguarded and require_comment_body then treats it as that members editable comment. Admin-only route (not in MEMBER_ALLOWED_ROUTES), so no privilege escalation; consistency gap. Fix direction: call ensure_actor_namespace_free once in prepare_tx_append_request (~:3023) gated on event_routes_to_journal(type), then delete the three handler-level copies - also closes the artifact-comment LOW. LOWs: writer.rs:1683 rename doc covers one direction only (a member re-added/renamed into a retired name inherits the old comments); api.rs:~2349 guard fails open silently on a members.org read error (add warn!); api.rs:~847 POST /artifacts/:id/comments unguarded. Operator decision pending: fix round (KA934.3.1) vs accept.
** Evidence
Worker commit 3181651c merged to main as 9f6874f0 (--no-ff, from 62f986e0). Manager gates on merged main 2026-09-02: cargo test -p orgasmic-daemon --lib -- comment member identity authz allowed_routes = 58 passed 0 failed (/private/tmp/claude-501/-Users-aspirational-Documents-code-tools-orgasmic/25219e20-4403-4221-9eb2-cd596e21be27/scratchpad/gates-KA934.3-daemon.log); clippy -p orgasmic-daemon --all-targets -D warnings rc=0; cargo fmt --all --check rc=0. Worker gates matched. Manager pre-check: allow-list entries match router templates; guard applied to the effective actor in create and to state.actor in edit/delete; member path short-circuits before the guard.
