#+title: orgasmic task TASK-WT09J
#+orgasmic_version: 2

* BACKLOG TASK-WT09J Document (or close) the acp-vs-rmux divergence: acp pushes --safe-mode --strict-mcp-config, rmux now pushes only the latter
:PROPERTIES:
:ID: TASK-WT09J
:PRIORITY: P3
:END:

From TASK-NYF7Z (2026-07-29): the acp claude path (NativeLogin arm, adapters/claude.rs:515-527) pushes --safe-mode --strict-mcp-config; the rmux path now pushes only --strict-mcp-config, deliberately (safe-mode's hooks/plugins/CLAUDE.md suppression was in-scope-refused, and CLAUDE.md discovery is actively wanted for TUI workers). That leaves an UNDOCUMENTED difference between two paths running the same personas. Either argue rmux should take --safe-mode too, or record at both push sites why the TUI path wants what the acp path suppresses. Documentation-plus-decision task; tiny code delta if any.

