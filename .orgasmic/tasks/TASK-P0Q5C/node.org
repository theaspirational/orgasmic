#+title: orgasmic task TASK-P0Q5C
#+orgasmic_version: 2

* IN_REVIEW TASK-P0Q5C No CLI title surface for decision/architecture/glossary/project/handoff nodes despite daemon set_title support
:PROPERTIES:
:ID: TASK-P0Q5C
:PRIORITY: P2
:END:

From TASK-XPYRR's ask-3 sweep (checked against code, not assumed): the daemon's
`set_title` op works for decision, architecture, glossary, project, and handoff
nodes — the UI writes titles for all of them — but no CLI surface exists for
any kind except task (`task update --title`, TASK-XPYRR). Same shape as the
task gap: an operator or manager agent can observe a wrong title and cannot
correct it without the UI.

=== Ask

Add `node title set <ID> --title ... --project ... --kind ...` routing to the
existing `set_title` op through TASK-XPYRR's `edit_heading_line` guard (round
trip or refuse naming the reason; never touches tags/keyword). One surface for
all title-capable kinds rather than per-kind flags.

=== Acceptance

- Retitle round-trips for each supported kind in tests; goal kind is refused
  with a message pointing at TASK-V460X (separate task, no daemon path).
- Refusal semantics identical to task update --title (same guard, same
  messages).
- Verify artifact per TCTTD standard, `git apply`-clean.
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit 153d4607 on sprint/P0Q5C, merged onto local sprint-20260902 as 7af20abb. 'orgasmic node title set <ID> --title …' routes to the existing set_node_title path (edit_heading_line guard); round-trips task/decision/glossary/handoff; goal refused (TASK-V460X); project REFUSED because the daemon rewrite drops the PROJECT heading word and breaks project.org lookup (new P1 task filed from this finding, see RELATES_TO on it); architecture is a retired layer (dec_HBK6A) so the task text was stale there. task-graph.md + ledger.md updated. Agent gates: task_title_edit_cli 2 passed, cli title/node 7 passed, daemon set_title 5 passed, clippy rc=0, fmt rc=0; cli_parity's single failure = pre-existing recall-resume.md slip (fixed by TKR0H on the sprint branch). Operator review/test after the sprint.
- 2026-09-02 sprint review (CLI reviewer): CONFIRMED clean. 'node title set' routes through the daemon's compose, re-parse, compare, refuse guard, so a title with a leading '*', a newline, or a tag-shaped token is refused rather than corrupting the org file.

