#+title: orgasmic task TASK-AP971.8
#+orgasmic_version: 2

* DONE TASK-AP971.8 Derived read views :wf_grilling:
:PROPERTIES:
:ID: TASK-AP971.8
:END:

** Question
Humans lose the one-file glossary/backlog view when nodes become dirs. What compiled read views exist (glossary, board), when are they generated, where do they live, and are they gitignored?
** Resolution
Grilled with owner 2026-08-22; three decisions. Facts: consumers of the one-file views are four prompt-studio context-packs (FILE: .orgasmic/{glossary,decisions,gotchas}.org, tasks/backlog.org), the entry router, two skill refs, and the UI OrgView (GET /org/file?path=); .orgasmic/.gitignore holds only tmp/.
1. Views: exactly the three lost files — board.org (tasks by state), glossary.org, decisions.org — under .orgasmic/views/, same shape as today's files so consumers repoint by path only.
2. Generated by the daemon after each affected refresh, debounced; orgasmic views build for cold clones.
3. Gitignored (.orgasmic/.gitignore gains views/): dirs are truth, views are throwaway; committing them would make every node write a two-file diff and invite hand edits.
Consequence: repoint context-packs, entry router, skill refs and OrgView to views/.

