#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:GOAL_ID:          goal-20260615-ship-the-editable
:LIVENESS:         06d09568396cf31a2734e12271485fb956702cdd
:LIVENESS_AT:      [2026-06-22 Mon]
:UPDATED_AT:       [2026-06-22 Mon]
:END:

Manager-owned snapshot at Graph-page goal completion. Goal goal-20260615-ship-the-editable is REACHED at the code level: all 5 queued tasks DONE (+ a bonus concurrency fix), gates green, parse_errors 0. The one remaining step is operator-dependent: restart/rebuild the :4848 daemon to activate the new endpoints and visually verify the Graph page. Use task Evidence, decisions.org, tx logs, and git for detail.

** Done so far
- TASK-8BRYP (1/5) DONE - superseded flag on /decisions + /graph/nodes; DecisionsView hide-by-default toggle (b9b30e6 + bf204bd).
- TASK-AQMTA (2/5) DONE - /graph/layout overlay endpoint (0e5b21e); concurrency hardened by TASK-3VEM8 (atomic RMW in the writer + node_id validation + curation symmetry, 8-round concurrent test; 4334bbf).
- TASK-JH9DD (3/5) DONE - cross-layer Graph page: new GraphView + generalized NodeGraph, deterministic band layout (superseded gray -> decisions -> architecture -> tasks; glossary left column), curation toggles, backbone-on/dense-off edge layers, route + sidebar nav (31c950d + 63a0bde fail-soft/anchors/toggle).
- TASK-CKRYB (4/5) DONE - three-tier precedence (localStorage > shared overlay > default), persist-on-drag (localStorage only), publish (only write-to-shared), two-level reset, shared-map-updated nudge (d46a18f + 126c5ac int-coords/baseline/tests). 79 UI tests pass incl. 6 buildFlowGraph cases.
- TASK-G7BCJ (5/5) DONE - graph_curator prompt-spec (overlay-scoped, topology read-only) + stripped stale architector layout-hint wording (5e0a357). prompt compile/lint clean.
- Each implementation task: manager diff review + reviewer-claude-rmux (opus-4-8) pass; the AQMTA reviewer caught a real lost-update bug, fixed in 3VEM8.
- dec_B4147 release pipeline (TASK-32R7Z, operator-directed outside the graph goal) DONE: split into two manual lines — runtime/CLI built locally via scripts/publish-runtime.sh to the stable release (darwin x2 signed + linux x2 zigbuild, merged manifest preserving windows), apps to a separate dispatch-only apps release; dropped all v* triggers, deleted release-runtime.yml, reverted v0.0.5 app bumps to 0.0.4. Published runtime 0.0.5 to stable at commit 06d09568. Commits 29723d5/08d2e95/06d0956.
- TASK-38RK6 (cleanup, operator pre-authorized) DONE: deleted git tags v0.0.1-v0.0.5 (local+remote); purged all 50 runtime-* Actions artifacts (~389MB, total now 0); retention-days:1 live in runtime-bundles.yml. stable/nightly releases left intact.

** Next likely actions
- Graph goal goal-20260615-ship-the-editable remains code-complete; dec_B4147 release work is also done. Await operator direction for the next goal.
- Optional follow-ups (not started): (1) dispatch runtime-bundles.yml with only=windows-x86_64 to refresh the Windows runtime on stable via the new merge-not-clobber path; (2) remove the stale app assets (latest.json/orgasmic.app.tar.gz/dmg/apk/android-latest.json) left on the stable release by the old coupled flow now that apps target the apps release; (3) update the local daemon (currently runtime 0.0.3) to the freshly published 0.0.5.

** In flight
- Nothing in flight. todo/in_progress/in_review empty; no open dispatch.

** Divergence / Risks
- RESTART POLICY CHANGED (2026-06-16): the "never restart the daemon from a manager session" restriction was lifted at operator direction and TASK-E18NC landed the recovery-aware replacement. Use shipped/prompt-studio/conventions/daemon-rebuild-restart-policy.org for the correct local-source rebuild/restart flow and post-restart recovery checks.
- PRE-EXISTING RED TEST (candidate follow-up): crates/orgasmic-daemon/tests/integration.rs:799 skill_routes_include_shipped_and_user_markdown_skills fails on clean main; out of scope for the graph goal.
- TOOLING NIT: dispatching a reviewer (kind reviewer) on an IN_REVIEW task flips the heading to IN_PROGRESS; worked around at each close.
- Standing: dec_035A0 babysitter policy unchanged; avoid stashing/replacing live .orgasmic state while the daemon runs.
- The drift scanner now reads shell sources and '# arch:' markers (architecture_drift.rs); arch_WZFAX.3 is the publish-pipeline leaf. Pre-existing drift remains for arch_MK2Q2.4/.5 (ProjectSelector/ManagerPopover components deleted) — unrelated UI debt, separate cleanup.
- Pre-existing fmt drift on main in crates/orgasmic-cli/src/daemon_service.rs + path_env.rs (cargo fmt --check would flag) — not touched here.
- Local daemon still runs the old 0.0.3 runtime; the published stable is 0.0.5.
