#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         c03dc66
:LIVENESS_AT:      [2026-08-06 Thu]
:UPDATED_AT:       [2026-08-06 Thu]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-06, continuing 08-03/05. The architecture layer is GONE (stages
A-E, all reviewed), three chains closed, the daemon log defect fixed, and the
runtime reinstalled TWICE and verified behaviourally both times.

THE FOUR THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE RUNTIME IS NO LONGER BEHIND THE REPO, AND EVERY "INERT UNTIL REINSTALL"
   WARNING IN THE PREVIOUS HANDOFF IS RETIRED. =~/.orgasmic/bin/orgasmic= was
   rebuilt 2026-08-06T11:39Z (=0.0.15-dev.1=; both builds call themselves that,
   so the version string proves nothing). Verified BEHAVIOURALLY, three probes:
   =orgasmic manager drivers= prints =stdio=/=ws=, NOT =acp-stdio=/=acp-ws=;
   =dispatch-close= carries BOTH =--verdict= and =--fix-round-final=; and
   =manager worktree-prune= refuses by construction. So dispatch with =--mode
   stdio= (the old trap said the opposite and is dead), close a reviewer with
   =--verdict approve= rather than =--property VERDICT=ship=, and expect a
   =:FIX_SUBTASK:= close to land =in_review= on its own with no manual flip.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. The safe pattern is =ORGASMIC_DAEMON_URL=
   on the CHILD. =env -u ORGASMIC_HOME= to strip a leaked value is the opposite
   and is fine. TASK-48SFW carries the source chain. Note the asymmetry recorded
   there: BRIEFS keep the blunt blanket ban, because a worker cannot tell which
   verbs open a connection and the failure is total and silent; the CODE fix
   targets the precise rule at =ensure_running=.

3. A PASSING GATE PROVES LESS THAN IT LOOKS, IN TWO SPECIFIC WAYS.
   - =orgasmic verify=: a pass proves the red HAPPENS, not that it is the RIGHT
     red. For a RE-CUT artifact, read the patch and confirm the pinned message is
     the FIRST failing assertion.
   - The daemon suite has NO TRUSTWORTHY FULL-RUN SIGNAL on this host. Under
     load =syspolicyd= bursts (measured 586% CPU, load 11.41, suite 8-15x
     slower); the 2026-08-02 run showed 14 failures that ALL passed alone in 8s
     against the same binary. The cheap control is to re-run each failure alone
     against the ALREADY-BUILT binary before believing any red:
     =env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME target/debug/deps/orgasmic_daemon-<hash> --exact <test> --test-threads=1=
     TEST EXECUTION is the trigger, not compilation. This machine has hard-
     rebooted twice from that chain, so DO NOT loop the full workspace suite.
     TASK-STWVB owns the real fix and is the only task in =todo=.

4. CODEX IS OUT OF CREDITS ON THIS WORKSPACE, AND ITS FAILURE MODE IMPERSONATES A
   TRANSPORT FAULT. Three orphans on 2026-08-06 were root-caused to it: codex
   blocks on an INTERACTIVE QUOTA PROMPT at 0% cpu, the 600s stall release kills
   the pane, the tmux server loses its only session and exits, and =no server
   running= then reads like a transport death. Found by CAPTURING THE PANE EARLY
   instead of waiting out the stall clock — do that before blaming tmux. Until
   the operator restores credits, codex is not a dispatchable harness here, which
   is why this session's implementers ran =cursor-agent= and its reviewers ran
   =claude=.

ALSO STILL TRUE, from the 08-05 measurements: SHARED =CARGO_TARGET_DIR= IS
UNSOUND (two checkouts share one cargo fingerprint dir, so a worker's build
reports =Fresh= without compiling and =CARGO_BIN_EXE_*= hands its integration
tests ANOTHER CHECKOUT'S BINARY — a green suite that never ran your code), and
STABLE WORKTREE SLOTS DO NOT FIX THE TCC PROMPTS (a grant for an ad-hoc-signed
binary is pinned to its CDHASH and a rebuild at the SAME path changes it). Do not
implement either. The surviving fix, TASK-M47E5, shipped.

** Done so far
SESSION 2026-08-06, continuing 08-03/05. Twenty-three tasks reached DONE, the
runtime was reinstalled twice, and the board is now EMPTY of in-flight work:
=in_progress= and =in_review= both have nothing in them, no dispatch is open, and
=git worktree list= shows only the main checkout.

TASK-RQ270 — THE ARCHITECTURE EXCISION (dec_HBK6A), stages A-E, every stage
reviewed to a verdict:
- Stage A doctrine closed; fix round =4d42217= (RQ270.1.1) stopped three
  documents still advertising the retired architect production surface.
- Stage B =37f28c4= (RQ270.2) retired the drift comparator and the =// arch:=
  back-edges. REJECTED — the inventory missed a whole marker prefix — and
  =e56af71= (RQ270.2.1) stripped the =@arch= back-edges and the stale schema
  legend that still named a deleted verb.
- Stage C in two halves, UI first: =f4f2264= (RQ270.3.1) removed the UI
  architecture read surface; =8822f61= (RQ270.3.2) removed the read model. Stage
  D was then BLOCKED by a second parse-error path the carve-out had missed, and
  =7c5b34a= (RQ270.3.2.1) carved retired =arch_= ids out of the IDENTITY lint
  too. Without it, done.org's 231 =:IMPLEMENTS: arch= tokens would have flooded
  the board with parse errors the moment stage D deleted the file.
- Stage D =4ed239a= (RQ270.4) retired the =arch_= node class and deleted
  =architecture.org=. THE SEQUENCING IS THE REUSABLE PART: it was built and
  reviewed on a STAGING BRANCH with main deliberately untouched, then the
  operator chose reinstall-then-merge — the runtime was reinstalled BEFORE the
  merge landed, so the board never went red.
- Stage E =faf8149= (RQ270.5) retired the shipped doctrine; =596be22= (RQ270.5.1)
  fixed a gate that was ALREADY VACUOUS — the value-enum check silently skipped
  every long-help option, so it had never been checking what it claimed.
- dec_32ZHT: the glossary's PRIMARY_LEAF =arch_= refs stay as INERT HISTORY.
  Legacy =arch_= ids remain valid as references; the class is no longer mintable.

TASK-M47E5 FAMILY CLOSED, five rounds, and the shape is worth reading. The
cursor-acp finalize grant that the worktree relocation had broken was narrowed
round by round rather than restored wholesale: =9f7028b= (.1.1) keyed it on the
attempt's OWN artifact dir; =936201f= (.1.1.1) required that dir to be a real stem
child and stopped the CLI minting a degenerate dispatch stem; .1.1.1 was itself
REJECTED because its test did production's arithmetic, and =3732f37= (.1.1.1.1)
pinned the derivation at its real edge.

TASK-4WKNX CHAIN CLOSED. =56a8230= (.1) reserved the manager's own close-tx keys
from the generic =--property= channel; REJECTED — the table was a denylist and the
keys were consumed — and =ca51551= (.1.1) made manager-owned keys win BY
CONSTRUCTION instead of by enumeration.

TASK-ZBYH3 — THE DAEMON LOG DOUBLE-WRITE, and the clearest conflicting-rulings
lesson on this board. =145fb89=: under the LaunchAgent, =StandardOutPath= pointed
at the same file the durable sink opened, so every tracing line went through two
descriptors on ONE INODE — 13MB on the operator's daemon, measured ~half
duplication (400 sampled lines, 201 unique; 995 adjacent identical pairs in 2000)
— and it never rotated. Reviewed REJECT. The fix round =d383ab4= (.1) found that
THE TWO RULINGS CONFLICTED: ruling B suppressed the mirror only when stdout and
the durable log shared an inode, while ruling A moved =StandardOutPath= to a
DIFFERENT inode. After a plist rewrite the mirror SURVIVED and the same total
growth was merely relocated into an unrotated =daemon.stdout.log=. The mirror is
now suppressed whenever stdout is NOT A TERMINAL and a durable sink exists, which
holds under any service manager and on Windows where the inode check could never
fire. Reviewed APPROVE WITH FOLLOW-UPS; manager verified the combined two-file
count 2 before / 1 after and 9 logging tests green.

FILED FROM WHAT THE WORK UNCOVERED
- TASK-G64ZH (P2) — the ZBYH3.1 follow-ups, and the next round: the reopen retry
  is UNBOUNDED (~3 stderr writes plus up to 31 stats per log line in the stuck
  state, into a =daemon.err.log= nothing rotates — the same growth shape this
  chain exists to remove, at ~3x the rate), the =is_terminal()= mirror gate is
  still a proxy with concrete failing cases BOTH directions, and the tty branch
  has no test at all.
- TASK-3HR8R — a task parked in =in_review= with no reviewer dispatch is
  invisible to every status surface.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. There is no
project default and nothing enforces resolution (=--model= > task drawer >
harness default, and the harness default wins SILENTLY — TASK-A55J7). The
2026-08-06 answers, which are NOT a standing setting: implementers =tmux /
cursor-agent / cursor-grok-4.5-high-fast=, reviewers =tmux / claude /
claude-opus-5 / high=. They changed from the 08-05 session precisely because
codex ran out of credits mid-session (lead item 4). Pass =--model= explicitly and
echo all four back after each dispatch.

TRANSPORT: tmux. Both properties read against the INSTALLED daemon: pane liveness
from TASK-4CSMY is confirmed reporting in production, so no =stall_timeout_secs=
override is needed; and rmux fails the survival property (TASK-4DTBZ — it killed
three workers in 24ms). Every dispatch across 08-05 and 08-06 ran on tmux with no
transport trouble. Note the runtime now accepts =--mode stdio= as well, but the
dispatch convention still requires a TUI pane so the operator can watch and
intervene.

SERIALIZE IMPLEMENTER DISPATCHES ON THIS HOST. Each worktree gets its OWN cargo
target dir (shared ones are unsound — see the lead), so two concurrent
implementers mean two workspace builds plus two =run-tests.sh= runs. Test
EXECUTION is what bursts =syspolicyd=, and this machine has hard-rebooted twice
from that chain. One implementer at a time is a deliberate throughput sacrifice,
not an oversight.

THE ORDER FROM HERE
1. TASK-G64ZH (P2) — THE NEXT ROUND, and the only unactioned review follow-up on
   the board. Three findings on =crates/orgasmic-daemon/src/logging.rs=: bound the
   reopen retry (backoff in =SinkState=, one stderr note per window, and SKIP
   =maybe_rotate= entirely while =state.durable.is_none()= so one path owns the
   retry); answer the mirror question BY CONSTRUCTION in the three service
   definitions orgasmic writes itself (launchd =ProgramArguments=, systemd
   =ExecStart=, the Windows wrapper) with =is_terminal()= demoted to the fallback
   for supervisors orgasmic did not write; and pin BOTH branches of the gate,
   since all four new tests drive the non-tty side only.
2. TASK-STWVB (P2) — the suite's full-run signal, and the only task in =todo=.
   Design is on the task; A and B/C are separable rounds. Every other estimate on
   this board is degraded by not having it.
3. TASK-RMA18 (P1) — the worktree-reclamation redesign, carrying all six BLOCK
   SHIP findings from the two prune reviews. It starts from the gated-off code,
   and =worktree-prune= stays refusing until it lands.
4. TASK-A1KJW (P3) — the dispatch record dir is named from the BRIEF FILENAME,
   never the task id (=dispatch_artifact_stem=). Fixing it would also make
   cursor-acp's narrow own-artifacts grant expressible again (TASK-M47E5.1 had to
   delete it).
5. TASK-3HR8R — a task parked in =in_review= with no reviewer dispatch is
   invisible; cheap, and it closes a real hole in the manager's own status read.
6. TASK-37WN7 (P3) — one acceptance item left: strengthen
   =wait_until_serve_awaits_signals= or record why not. The flake itself is
   registered in =verify/flake-registry.toml= with a captured signature.
7. Still P1 and untouched: TASK-4DTBZ, TASK-BX5SR, TASK-A55J7, TASK-48SFW,
   TASK-ADHWZ, TASK-NTNK0, TASK-5M45A.

THE RUNTIME REINSTALL IS NO LONGER A PENDING ITEM — it happened twice this
session. The recipe and its four traps stay worth knowing for next time
(=install.sh= silently skips the unpack if the version dir exists, its trailing
=orgasmic status= auto-spawns a competing daemon — use
=ORGASMIC_INSTALL_SKIP_STATUS=1= — and you MUST codesign or the binary loses its
TCC grant). Always verify BEHAVIOURALLY: both builds call themselves
=0.0.15-dev.1=.

** In flight
ONE DISPATCH RUNNING: TASK-QGWK7 (P1) implementer,
=implementer-cursor-agent-tmux= (tmux / cursor-agent / cursor-grok-4.5-high-fast),
run =run-20260806T151244-7572bac5ee574bd6956fcf560376cf4f=, started_tx
=tx-20260806-orgasmic-4939=, base =c03dc66=. Report lands at
=.orgasmic/tmp/dispatch/task-qgwk7/task-qgwk7-8b3fc1ac3ffb460fa74a4e962c1276da-last.txt=.

WHAT IT IS FIXING: =dispatch-close= deletes the worker's =last.txt= and the
=*.reported= tx carries no body, and =.orgasmic/.gitignore= excludes =tmp/= (zero
commits ever), so a closed dispatch has NO durable record of what the worker
said. The brief survives and the report does not.

THE IRONY IS OPERATIONAL, NOT DECORATIVE: this defect is LIVE right now and will
stay live for this dispatch and its review, because the fix has to merge AND the
runtime has to be rebuilt before =dispatch-close= behaves differently. So EXTRACT
EVERY FINDING FROM THIS WORKER'S REPORT AND ITS REVIEW BEFORE YOU CLOSE THEM, and
write close =REASON=s detailed enough to file tasks from, with =file:line=
anchors. The TASK-G64ZH chain lost three reviews and four implementer reports to
exactly this, and two findings had to be filed from their titles alone.

THE DESIGN IS SETTLED ON THE HEADING — do not re-open it if the worker argues.
The report is NOT inlined into the task node: =done.org= is 26,106 lines and is
reparsed whole on every reindex, a review is ~25KB, and a markdown body with =#=
headings and code fences is a file-corruption surface in an org file (which is
why =node body set= refuses column-0 headings). =last.txt= stays as the run-time
streaming sink. The fix is to PROMOTE the record at close to a TRACKED path keyed
by =started_tx= — per GENERATION, because this chain ran six dispatches across
three tasks — plus =REPORT_PATH= on the completion tx.

THE ONE ANCHOR THE BRIEF DELIBERATELY DID NOT GUESS: the deletion site inside
=crates/orgasmic-cli/src/manager.rs=. There are several =remove_file= /
=remove_dir_all= sites and at least one belongs to the WORKTREE SALVAGE path,
which must not be disturbed — TASK-2BPWM and TASK-D0GA3 exist because it
destroyed worker output twice. The brief tells the worker to find the right one
and says why a guessed anchor would be dangerous here.

CURSOR PROMPT CLEARING NOW POLLS INSTEAD OF SLEEPING, and the numbers justify it:
this dispatch's trust prompt appeared at 1s and its allowlist prompt at 14s. A
fixed sleep between the two is what missed it on TASK-G64ZH.1.1. Poll for
=Workspace Trust Required= then =Not in allowlist=, sending =a= and =BTab=.
TASK-VD0VB is the real fix.

** Divergence / Risks
THE RUNTIME IS CURRENT — this section's largest standing item for three sessions
is CLOSED. Installed =0.0.15-dev.1= rebuilt 2026-08-06T11:39Z, verified by the
three behavioural probes in lead item 1. The four changes that were inert (the
YN5FJ review gate, the M47E5 worktree relocation, the =--verdict= flag, the
4WKNX fix-round default) are all LIVE now. Stop applying the workarounds: no
=--property VERDICT=ship= compat, no manual =done= -> =in_review= flip, no
=--mode acp-stdio=.

CODEX IS OUT OF CREDITS AND FAILS LIKE A TRANSPORT (lead item 4). Until the
operator restores them, do not dispatch codex — and do not read =no server
running= as a tmux defect without capturing the pane first.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item 2).
Unchanged, and it is the single most expensive mistake available. TASK-48SFW.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1). TMUX IS THE
DEFAULT. The TASK-4CSMY pane-liveness channel is confirmed reporting in
production, so the =--governance-json {"stall_timeout_secs":3600}= workaround
stays retired. Read =event.type=, never the serialized line.

=[pid-gone]= IS NOT A DEATH SIGNAL FOR A TMUX DISPATCH. =dispatch-status= prints
it for every tmux run always, because =WORKER_PID=-=. A watcher keyed on it fires
instantly and falsely. Use =[run-gone]=, the tmux session vanishing, or a
non-empty =last.txt=.

=worktree-prune= IS GATED OFF BY CONSTRUCTION and both forms exit 1, including
=--dry-run= (the scan is gated too, because a dry run still walked directories it
had no business walking). Two reviews produced six BLOCK SHIP findings on one
delete path; TASK-M47E5.3 chose to gate rather than patch a third time, and the
implementation is KEPT with its reclamation tests =#[ignore]=d. TASK-RMA18 owns
the redesign and un-ignores them. Do not hand-remove a worktree in the meantime —
=dispatch-close --worktree-remove --branch-delete= is the salvaging path.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). Detection
is =grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention.
That log now rotates by size and no longer double-writes (TASK-ZBYH3), so the
grep is cheaper than it used to be — but the ROLLED generations are separate
files, so grep the set when looking back.

MERGE STILL PRECEDES REVIEW IN PRACTICE. Expect merge -> review -> fix in
=TASK-<id>.N= rounds. When you review a post-merge diff, SAY SO in the brief and
tell the reviewer a REJECT is still actionable, or you will get softened
findings. Across 08-05 and 08-06, roughly half of all reviews returned REJECT —
including reviews OF fix rounds, twice.

DO NOT STASH =.orgasmic= TO GET A CLEAN TREE FOR =orgasmic verify=. The daemon
reads those files, so during the stash window it cannot see its own project and a
concurrent write fails =404 Not Found: node TASK-XXXXX=. Commit the state first;
=verify= only needs the tree clean, and committed state is clean.

REVIEW-BRIEFING LESSONS, all paid for:
- DO NOT ask a reviewer to mutate source. The contract is read-only, so a brief
  asking for a negative probe returns an unrun gap, correctly. The IMPLEMENTER
  runs negative probes; the reviewer judges them.
- A REJECT the manager then fixes personally is a normal, good outcome.
- NAME THE SPECIFIC CLAIM YOU WANT ATTACKED. The TASK-ZBYH3.1 review was told to
  answer two named suspicions and came back with both CONFIRMED and quantified —
  that is where TASK-G64ZH's syscall table came from. A generic "review this
  diff" would not have produced it.
- TELL THE REVIEWER WHAT IS ALREADY ESTABLISHED so it spends effort on what is
  not. Every review this session was given a do-not-re-derive list.
- VERIFY EVERY FINDING AGAINST THE CODE BEFORE ACTING ON IT, including the ones
  you intend to accept.
- WHEN TWO RULINGS ARE RECORDED ON ONE TASK, CHECK THEY COMPOSE. TASK-ZBYH3 took
  both of its candidate rulings and they CONFLICTED — one moved the file the
  other was comparing against — so the fix relocated the defect instead of
  removing it, and only the next review caught it.

STATE-HYGIENE SHAPES TO WATCH FOR:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  Check =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a
  stage.
- A TASK IN =in_review= WITH NO REVIEWER DISPATCH is worse, because nothing
  surfaces it at all — TASK-3HR8R.
- A BLOCKER RECORDED ONLY IN PROSE is invisible; =depends_on= is what the board
  reads.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED misleads every
  skim-reader. Use a =READ THIS FIRST — STATUS= block at the TOP plus a corrected
  TITLE; appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6); it
spawns real billed provider turns.
