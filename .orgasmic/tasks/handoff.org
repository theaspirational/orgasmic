#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         44c7b14
:LIVENESS_AT:      [2026-08-06 Thu]
:UPDATED_AT:       [2026-08-06 Thu]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-06, third block, continuing 08-03/05. Two task chains driven
hard: TASK-QGWK7.1 is DONE and TASK-STWVB is on its third round.

THE FIVE THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE RUNTIME IS BEHIND THE REPO AGAIN, AND IT MATTERS MORE THAN USUAL. The
   installed =~/.orgasmic/bin/orgasmic= was built 2026-08-06T11:39Z; =da88c19=
   (promote the worker report) landed 15:30Z and =955c8d6= (STAGE it) later
   still. So =dispatch-close= RUNS FROM THE INSTALLED CLI AND STILL DELETES
   =last.txt=. Copy the report out by hand into
   =.orgasmic/dispatch-records/<started_tx>/last.txt= BEFORE every close until
   the runtime is rebuilt. The previous handoff's "the runtime is no longer
   behind the repo" was true when written and is NOT true now.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. =ORGASMIC_DAEMON_URL= on the CHILD is
   the safe pattern; =env -u ORGASMIC_HOME= to strip a leaked value is fine.
   TASK-48SFW carries the source chain.

3. =scripts/run-tests.sh= IS CURRENTLY NOT TRUSTWORTHY, AND THAT IS AN OPEN
   TASK, NOT A SURPRISE. Its host-state gate returns *exit 4 INCONCLUSIVE on a
   calm host* for the default no-argument (whole-workspace) invocation — the
   exact command =.orgasmic/project.org:23= prescribes. Cause is measured:
   =SYSPOLICYD_CPU_DEGRADED=100.0= is an absolute bound on a quantity that grows
   with run duration, and ambient accrual alone reaches it in ~22 minutes.
   TASK-STWVB.1.1 owns the fix and is in flight. UNTIL IT LANDS, use plain scoped
   =cargo test= for gates, not the wrapper.

4. A PASSING GATE PROVES LESS THAN IT LOOKS. =orgasmic verify=: a pass proves the
   red HAPPENS, not that it is the RIGHT red — read the red log and confirm the
   pinned message is the FIRST failure. Done four times this session; correct
   four times, but it was worth checking each one. And the daemon suite still has
   no trustworthy full-run signal on this host (that is what the STWVB chain is
   FOR). Re-run any single failure alone against the ALREADY-BUILT binary before
   believing a red. DO NOT loop the full workspace suite: test EXECUTION bursts
   =syspolicyd= and this machine has hard-rebooted twice from that chain.

5. EVERY =tmux/cursor-agent= DISPATCH PARKS ON TWO INTERACTIVE PROMPTS BEFORE IT
   EXECUTES ANYTHING, and each one is a dead worker if unattended (0% CPU, no
   pane output, killed by the 600s stall release, presents as a hung transport).
   Clear workspace trust with =tmux send-keys <session> a= right after dispatch,
   and the =orgasmic= allowlist with =send-keys BTab=. TASK-VD0VB is filed P1 and
   unfixed; it fired on all four cursor-agent dispatches this block. CODEX
   remains out of credits and is not dispatchable here.

ALSO STILL TRUE, from the 08-05 measurements: SHARED =CARGO_TARGET_DIR= IS
UNSOUND (a worker's build reports =Fresh= without compiling and =CARGO_BIN_EXE_*=
hands its integration tests ANOTHER CHECKOUT'S BINARY), and STABLE WORKTREE SLOTS
DO NOT FIX THE TCC PROMPTS (a grant is pinned to the binary's CDHASH and a
rebuild at the same path changes it). Do not implement either.

** Done so far
SESSION 2026-08-06 (third block). FIVE MERGES, FOUR REVIEWS, and the pattern of
the session is that EVERY review earned its keep: three REJECT, one
approve-with-follow-ups, and every finding held up when the manager re-verified
it against the source before acting.

THE TASK-QGWK7.1 ROUND — DONE, and the chain's actual goal is met.
=955c8d6= stages the promoted record (=git add -- <dest>= in both promote paths,
routed through one =promote_and_stage_dispatch_record=), bounds =stdout.log= to a
64KB tail with the original byte count beside it in =stdout.log.bytes=, skips
0-byte stdout entirely, streams the copy instead of buffering a whole artifact,
fences the =--no-worktree-remove= promote with the cleanup lock and close guard,
and makes =REPORT_PATH= project-relative on the finalize fallback. Reviewed
APPROVE WITH FOLLOW-UPS: *a worker report is now durable BY CONSTRUCTION, not by
habit* — the reviewer traced every close path and confirmed there is no third
promote caller that skips staging, and established that staging is STRONGER than
the prior state (a staged path survives =git checkout -- .= and is skipped by
=git clean -fdx=). F-5 was deferred with a stated reason and the reasoning was
checked and accepted: =worktree-prune= refuses by construction, so
promote-on-reclaim would be unreachable code today.

THE TASK-STWVB CHAIN — THREE ROUNDS, and it is the cautionary tale of the session.
- =b94f942= TASK-STWVB landed A (fixture consolidation onto a warmed inode), B
  (host state in the verdict, new exit 4 INCONCLUSIVE) and C (load-sensitivity
  interlock). REJECTED, four BLOCK SHIP.
- =54dae49= TASK-STWVB.1 answered all four IN MECHANISM. REJECTED AGAIN, two
  BLOCK SHIP.
- TASK-STWVB.1.1 is the round in flight.

THE LESSON IS THE SHAPE, NOT THE INSTANCE. Every round fixed the MECHANISM it
was handed and left THE NUMBER uncalibrated, so the same failure returned through
whichever signal had not been examined yet. Round 1: the suite tripped its own
LOAD threshold and INCONCLUSIVE preempted REAL, so exit 0 and exit 1 were both
unreachable for =orgasmic-daemon= — demonstrated by the implementer's own gate,
*exit 4 on 734 passed / 0 failed*. Round 2 fixed that for load and reopened it for
SYSPOLICYD: an absolute 100s bound on a quantity that grows with run duration,
with the cargo build INSIDE the sampled window. Manager-measured independently,
clean 60s window on an idle host: *+4.42s, 0.0737 s/s, so 22.6 minutes to reach
the threshold with NOTHING RUNNING* (reviewer measured 0.0753 s/s separately).
Round 3's acceptance is therefore written as a DEMONSTRATION, not an assertion:
=scripts/run-tests.sh= with no arguments must not return exit 4 on a calm host.

PART A IS SETTLED — DO NOT RE-PROBE IT. The round-2 implementer ran the A-effect
probe and read it as negative; the round-2 REVIEWER OVERTURNED that, and the
argument is right: the probe measures syspolicyd CPU BURN while part A avoids
serialized Gatekeeper QUEUE WAIT — the measured 196ms fresh vs 4ms linked is
BLOCKING, not CPU — so an instrument blind to the effect returned exactly what it
returned. Recorded as *mechanism-justified, effect size unmeasured*. Two rounds
died on that question; a third would be waste.

FILED FROM WHAT THE WORK UNCOVERED
- TASK-STWVB.1.1 (P1) — two block-ship, four follow-ups. In flight.
- TASK-QGWK7.1.1 (P2) — eight follow-ups. M-1 is a real behaviour bug the
  QGWK7.1 change introduced, manager-reproduced in source: a =git add= failure
  sets =worktree_failed=true= even when the worktree WAS removed, so all three
  =--branch-delete= arms fail their =!worktree_failed= guard and the branch is
  SILENTLY retained with =branch_failed=false= and no error naming it. Trigger:
  a concurrent state chore holding =.git/index.lock=.

WHAT WENT RIGHT IN THE PROCESS, worth repeating. Every brief named SPECIFIC
CLAIMS to attack and carried a do-not-re-derive list; every review answered them
by number and came back with measurements. The manager verified every block-ship
finding against the source BEFORE acting on it, and independently reproduced the
one measurement the whole round-3 case rests on. And every =orgasmic verify= pass
was checked by reading the red log to confirm the pinned message was the FIRST
failure — which it was, all four times.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. Still no
project default and nothing enforces resolution. The 2026-08-06 answers, which
are NOT a standing setting: implementers =tmux / cursor-agent /
cursor-grok-4.5-high-fast= (effort omitted DELIBERATELY — the cursor model id
carries the tier, and that choice was stated on each dispatch =--reason= rather
than left as an unnoticed omission); reviewers =tmux / claude / claude-opus-5 /
high=. Every one of those reviewers earned its cost.

TRANSPORT: tmux, unchanged. Six dispatches across this block, zero transport
trouble. =[pid-gone]= is still printed for every tmux run always and is still not
a death signal.

*CLEAR THE TWO CURSOR-AGENT PROMPTS BY HAND, WITHIN TEN MINUTES OF EVERY
DISPATCH.* TASK-VD0VB is filed, P1, and UNFIXED, and it fired on every single
=tmux/cursor-agent= dispatch this block: workspace trust (=send-keys a=), then
the =orgasmic= allowlist (=send-keys BTab= for "Run Everything"). Each parks at
0% CPU, emits nothing, and is killed by the 600s stall release — indistinguishable
from a hung transport. The working recipe is a watcher that captures the pane
every 20s and clears the allowlist prompt automatically; the trust prompt fires
before the watcher is useful, so clear that one by hand right after dispatch.

*THE WATCHER WILL BE KILLED, THE DISPATCH WILL NOT.* Harness-tracked background
watchers died to session-level group-kills twice this block. Both times
=dispatch-status= still showed =[run-live] [unreported]= and the worker was fine
— it is daemon-owned. Just relaunch the watcher; do not read a dead watcher as a
dead worker.

THE ORDER FROM HERE
1. TASK-STWVB.1.1 (P1) — IN FLIGHT. Round 3. Its acceptance is a DEMONSTRATION:
   =scripts/run-tests.sh= with no arguments must not return exit 4 on a calm
   host. Do not accept an assertion in its place; two rounds have already been
   lost to numbers that were never measured against the invocation that matters.
2. TASK-QGWK7.1.1 (P2) — eight follow-ups, M-1 first (the silent branch
   retention). Small and fully specified.
3. TASK-RMA18 (P1) — the worktree-reclamation redesign, six BLOCK SHIP from two
   prune reviews. =worktree-prune= stays refusing until it lands, and TASK-QGWK7.1
   deferred its F-5 onto this.
4. TASK-A1KJW (P3) — the dispatch record dir is named from the BRIEF FILENAME,
   not the task id. Now more visible: the tracked =dispatch-records/= directories
   are keyed by =started_tx= (good), but the =tmp/= staging dir is still
   brief-stem-named.
5. TASK-3HR8R — a task parked in =in_review= with no reviewer dispatch is
   invisible. Cheap, and it closes a hole in the manager's own status read.
6. TASK-37WN7 (P3) — one acceptance item left.
7. Still P1 and untouched: TASK-4DTBZ, TASK-BX5SR, TASK-A55J7, TASK-48SFW,
   TASK-ADHWZ, TASK-NTNK0, TASK-5M45A.

BUDGET DISCIPLINE THAT WORKED, keep it. Every brief carried an explicit execution
budget (a named maximum number of full suite runs) and every worker respected it.
Test EXECUTION is the syspolicyd trigger; this machine has hard-rebooted twice
from that chain. Serializing implementer dispatches one at a time is a deliberate
throughput sacrifice and it held all session.

** In flight
NOTHING IS IN FLIGHT. Both TASK-QGWK7.1.1 dispatches are closed
=manager.dispatch_aborted=; =dispatch-status= is empty and the tree is clean.

*TMUX/CURSOR-AGENT KILLED TWO CONSECUTIVE WORKERS AND I STOPPED RATHER THAN
DISPATCH A THIRD.* Both on TASK-QGWK7.1.1, same harness and mode:
- =tx-...-5099=: ran ~20 min, produced *712 insertions across 8 files plus an
  authored =verify/TASK-QGWK7.1.1/=*, then the tmux server vanished with no
  report. *WIP PRESERVED as =0273e9f= on branch =task-qgwk7.1.1-impl=*, worktree
  kept at =~/.orgasmic/worktrees/orgasmic/task-qgwk7.1.1=.
- =tx-...-5106=: fresh worktree/branch, died producing NOTHING.

RULED OUT both times, so do not re-derive: reboot (=uptime= 4 days), host load
(3.91), the =syspolicyd=->=tccd=->WindowServer chain (no DiagnosticReports
entry), and harness health (=cursor-agent --version= -> =2026.08.04-aaa8809=,
exit 0, AFTER both deaths). =stdout.log= is 0 bytes both times, which is normal
for tmux and carries no information.

*THE SALVAGED WIP IS UNVERIFIED AND MUST NOT BE MERGED AS-IS.* No gates were run,
no summary exists, and it MODIFIES THE PARENT'S ALREADY-PASSING PROOF
(=verify/TASK-QGWK7.1/expect-red= and =injection.patch=). That may be a
legitimate re-cut or a proof bent to fit; it needs judging, not inheriting. Read
it with =git diff main..0273e9f=.

*THE CHEAPEST LEAD IS RECORDED ON TASK-VD0VB (raised to P1).* =cursor-agent=
prints, verbatim, that =--trust=, =--yolo= or =-f= will pre-trust a directory
non-interactively — the flag option 2 of that task speculated about EXISTS, and
orgasmic's driver passes none of them. Prefer =--trust= (it names the property we
need) over =--yolo=/=-f= (which likely relax more), but VERIFY what each does.

THE OPERATOR DECISION THIS NEEDS: implementers ran =cursor-agent= this session
only because codex is out of credits. Reviewers ran =tmux/claude/claude-opus-5/
high= and were *4 for 4 with no transport trouble*. Switching implementers to
claude would fix reliability but COLLIDES WITH THE ROUTER'S INDEPENDENCE RULE —
the reviewer must be a different harness family, and with codex out of credits
and cursor-agent unreliable there is no third option. So the choice is: restore
codex credits, accept same-family review with a stated reason, or fix
TASK-VD0VB/the transport first. That is the operator's call, not the manager's.

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO AGAIN — this section's standing item REOPENED on
2026-08-06. The installed =0.0.15-dev.1= was rebuilt at 11:39Z, which is BEFORE
=da88c19= (promote the worker report, 15:30Z) and =955c8d6= (stage it). So
=dispatch-close= still deletes =last.txt= and the promotion/staging work is inert
until the next rebuild — copy the report out by hand before every close (lead
item 1). What the 11:39Z rebuild DID make live stays live and its workarounds
stay retired: the YN5FJ review gate, the M47E5 worktree relocation, the
=--verdict= flag, and the 4WKNX fix-round default. No =--property VERDICT=ship=
compat, no manual =done= -> =in_review= flip, no =--mode acp-stdio=.

CODEX IS OUT OF CREDITS AND FAILS LIKE A TRANSPORT (lead item 4). Until the
operator restores them, do not dispatch codex — and do not read =no server
running= as a tmux defect without capturing the pane first.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item 2).
Unchanged, and it is the single most expensive mistake available. TASK-48SFW.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1). TMUX IS THE
DEFAULT. The TASK-4CSMY pane-liveness channel is confirmed reporting in
production, so the =--governance-json {"stall_timeout_secs":3600}= workaround
stays retired. Read =event.type=, never the serialized line.

=[pid-gone]= IS NOT A DEATH SIGNAL FOR A TMUX DISPATCH. =dispatch-status= prints
it for every tmux run always, because =WORKER_PID=-=. A watcher keyed on it fires
instantly and falsely. Use =[run-gone]=, the tmux session vanishing, or a
non-empty =last.txt=.

=worktree-prune= IS GATED OFF BY CONSTRUCTION and both forms exit 1, including
=--dry-run= (the scan is gated too, because a dry run still walked directories it
had no business walking). Two reviews produced six BLOCK SHIP findings on one
delete path; TASK-M47E5.3 chose to gate rather than patch a third time, and the
implementation is KEPT with its reclamation tests =#[ignore]=d. TASK-RMA18 owns
the redesign and un-ignores them. Do not hand-remove a worktree in the meantime —
=dispatch-close --worktree-remove --branch-delete= is the salvaging path.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). Detection
is =grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention.
That log now rotates by size and no longer double-writes (TASK-ZBYH3), so the
grep is cheaper than it used to be — but the ROLLED generations are separate
files, so grep the set when looking back.

MERGE STILL PRECEDES REVIEW IN PRACTICE. Expect merge -> review -> fix in
=TASK-<id>.N= rounds. When you review a post-merge diff, SAY SO in the brief and
tell the reviewer a REJECT is still actionable, or you will get softened
findings. Across 08-05 and 08-06, roughly half of all reviews returned REJECT —
including reviews OF fix rounds, twice.

DO NOT STASH =.orgasmic= TO GET A CLEAN TREE FOR =orgasmic verify=. The daemon
reads those files, so during the stash window it cannot see its own project and a
concurrent write fails =404 Not Found: node TASK-XXXXX=. Commit the state first;
=verify= only needs the tree clean, and committed state is clean.

REVIEW-BRIEFING LESSONS, all paid for:
- DO NOT ask a reviewer to mutate source. The contract is read-only, so a brief
  asking for a negative probe returns an unrun gap, correctly. The IMPLEMENTER
  runs negative probes; the reviewer judges them.
- A REJECT the manager then fixes personally is a normal, good outcome.
- NAME THE SPECIFIC CLAIM YOU WANT ATTACKED. The TASK-ZBYH3.1 review was told to
  answer two named suspicions and came back with both CONFIRMED and quantified —
  that is where TASK-G64ZH's syscall table came from. A generic "review this
  diff" would not have produced it.
- TELL THE REVIEWER WHAT IS ALREADY ESTABLISHED so it spends effort on what is
  not. Every review this session was given a do-not-re-derive list.
- VERIFY EVERY FINDING AGAINST THE CODE BEFORE ACTING ON IT, including the ones
  you intend to accept.
- WHEN TWO RULINGS ARE RECORDED ON ONE TASK, CHECK THEY COMPOSE. TASK-ZBYH3 took
  both of its candidate rulings and they CONFLICTED — one moved the file the
  other was comparing against — so the fix relocated the defect instead of
  removing it, and only the next review caught it.

STATE-HYGIENE SHAPES TO WATCH FOR:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  Check =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a
  stage.
- A TASK IN =in_review= WITH NO REVIEWER DISPATCH is worse, because nothing
  surfaces it at all — TASK-3HR8R.
- A BLOCKER RECORDED ONLY IN PROSE is invisible; =depends_on= is what the board
  reads.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED misleads every
  skim-reader. Use a =READ THIS FIRST — STATUS= block at the TOP plus a corrected
  TITLE; appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6); it
spawns real billed provider turns.

NEW THIS BLOCK, 2026-08-06 third block:

THE TEST GATE ITSELF IS THE LARGEST OPEN RISK. =scripts/run-tests.sh= returns
exit 4 on a calm host for its default invocation (lead item 3), and
=.github/workflows/ci.yml:200-206= now DELIBERATELY fails the lane on exit 4 with
a comment justifying it as "host state not attributable to this run" — a premise
that is FALSE until TASK-STWVB.1.1 lands. NOT YET LIVE: nothing is pushed (main
is ~740 commits ahead of =origin/main=), so no CI run has happened. Push would
make it live. Revisit that comment when the fix lands.

A GATE CAN FAIL BY RETURNING NO VERDICT, NOT ONLY BY BEING RED. The STWVB chain's
recurring defect was never a false green — it was INCONCLUSIVE swallowing REAL,
so exit 0 and exit 1 both became unreachable. A gate whose answer is always "no
verdict" gets waved through exactly like one that is always red. When reviewing
any gate change, ask what its answer is on the ORDINARY case, not just the
failure cases.

A SIGNAL THAT CANNOT FIRE READS AS A MEASUREMENT. Twice now in one chain: the
syspolicyd sampler reported a measured =0.0= where it should have reported
UNKNOWN (regex could never match a truncated =ps comm= field), and after that was
fixed the host WORD still printed =calm= for an all-unknown judgment. Whenever a
probe can be unavailable, check that unavailable and zero are DISTINGUISHABLE in
the output — this is the same class as the four-round =logging.rs= proxy chain
from the previous block.

A TEST THAT COULD NOT HAVE FAILED BEFORE THE CHANGE PINS NOTHING. The QGWK7.1
review found two of nine acceptance tests assert on a NEW helper in isolation and
never enter the production call path — they cannot fail on the parent tree because
the function did not exist there. When a brief demands "pinned by a test", check
the test enters the production function, not just that it exists.

A =git add= FAILURE CURRENTLY MIS-CLASSIFIES A CLOSE AND SILENTLY KEEPS THE
BRANCH (TASK-QGWK7.1.1 M-1). Manager-reproduced in source. If a close reports
=CLEANUP_STATUS=worktree_failed= but the worktree is gone and the report is
present, this is why — and the branch was NOT deleted despite =--branch-delete=.
