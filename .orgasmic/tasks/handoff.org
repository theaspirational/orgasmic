#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         eb772a6
:LIVENESS_AT:      [2026-08-07 Fri]
:UPDATED_AT:       [2026-08-07 Fri]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-06/07, third and fourth blocks, continuing 08-03/05. Two chains
driven hard: TASK-QGWK7.1 is DONE, TASK-STWVB went FOUR rounds, and the runtime
was reinstalled at the end.

THE FIVE THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE RUNTIME IS CURRENT AGAIN, as of 2026-08-07T04:42Z. Manifest commit
   =eb772a6= (was =d803d49=, 81 commits behind). *Verified behaviourally, because
   the version string proves nothing — every build calls itself =0.0.15-dev.1=:*
   the running daemon's =txt= inode is =529321123=, which MATCHES the freshly
   installed binary and DIFFERS from the pre-reinstall =527048231=. Codesigned
   =com.theaspirational.orgasmic=, cert root =a70531007cd128ff...=, so TCC
   survived (6 projects, =parse_errors 0=, zero permission errors). Rollback:
   =~/.orgasmic/runtimes/0.0.15-dev.1-darwin-aarch64.bak-20260807a=.

   *So the hand-copy workaround is RETIRED — but confirm it before trusting it.*
   =da88c19= + =955c8d6= are now installed, so =dispatch-close= should promote
   AND STAGE the worker report by itself. No close has exercised that path yet.
   The next one is the behavioural proof; until it passes, still copy =last.txt=
   out by hand first, because the cost of being wrong is a destroyed report.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. =ORGASMIC_DAEMON_URL= on the CHILD is
   the safe pattern; =env -u ORGASMIC_HOME= to strip a leaked value is fine.
   TASK-48SFW carries the source chain.

3. =tmux/cursor-agent= KILLED TWO CONSECUTIVE WORKERS AND THE CAUSE IS UNKNOWN.
   Both on TASK-QGWK7.1.1: one ran ~20min and produced 712 lines before the tmux
   server vanished; the next died producing nothing. RULED OUT both times —
   reboot (=uptime= 4 days), host load (3.91), the syspolicyd->tccd->WindowServer
   chain (no DiagnosticReports entry), and harness health (=cursor-agent
   --version= exit 0 AFTER both deaths). *The operator's standing answer changed
   as a result: implementers are now =tmux / claude / claude-opus-5 / high=*, and
   SAME-FAMILY REVIEW IS ACCEPTED with a stated reason, because codex is out of
   credits and cursor-agent is unreliable so no third family exists. Say that on
   each dispatch rather than routing around the router's rule silently.

4. A PASSING GATE PROVES LESS THAN IT LOOKS. =orgasmic verify=: a pass proves the
   red HAPPENS, not that it is the RIGHT red — read the red log and confirm the
   pinned message is the FIRST failure. Done five times this session, correct
   five times, and still worth checking each one. Same discipline for a reinstall
   probe: this session's =dispatch-close --help= probe returned 0 BOTH before and
   after, so it was NON-DISCRIMINATING and proved nothing — the inode comparison
   is what settled it. A probe you did not pre-test can only mislead you.

5. =scripts/run-tests.sh= HAS AN OPEN FALSE GREEN. A crashed/aborted test binary
   contributes ZERO listed failures, so whenever any other failure classifies
   (i.e. whenever a registered flake fires) the crash arm is skipped and the run
   reports =GREEN modulo N registered flake(s)= at *exit 0* — over a log
   containing =SIGABRT=. TASK-STWVB.1.1.1.1 owns it. PRE-EXISTING, not introduced
   by this session's work. Until it lands, prefer plain scoped =cargo test= for
   gates, and never read a wrapper GREEN as proof nothing crashed.

ALSO STILL TRUE, from the 08-05 measurements: SHARED =CARGO_TARGET_DIR= IS
UNSOUND, and STABLE WORKTREE SLOTS DO NOT FIX THE TCC PROMPTS. Do not implement
either.

** Done so far
SESSION 2026-08-06/07. SEVEN MERGES, SIX REVIEWS (five REJECT, one approve), and
a runtime reinstall. Every review earned its cost: every finding held when the
manager re-verified it against the source before acting.

TASK-QGWK7.1 — DONE, and the QGWK7 chain's goal is met. =955c8d6= stages the
promoted record through a single =promote_and_stage_dispatch_record= on both
close paths, bounds =stdout.log= to a 64KB tail with the original byte count in
=stdout.log.bytes=, skips 0-byte stdout, streams the copy, and fences the
=--no-worktree-remove= promote. Reviewed APPROVE WITH FOLLOW-UPS: *a worker
report is durable BY CONSTRUCTION, not by habit* — the reviewer traced every
close path and confirmed there is no third promote caller, and established that
staging is STRONGER than the prior state (survives =git checkout -- .=, skipped
by =git clean -fdx=).

TASK-STWVB — FOUR ROUNDS, and the chain's own lesson is the deliverable. Merges
=b94f942= (A+B+C), =54dae49= (round 2), =b92199a= (round 3), =1add6fd= (round 4).
EVERY ROUND FIXED THE MECHANISM IT WAS HANDED AND LEFT THE NUMBER OR AN ADJACENT
BRANCH WRONG, so the same failure returned through whichever signal had not been
examined:
- r1: the suite tripped its own LOAD threshold and INCONCLUSIVE preempted REAL,
  so exit 0 and exit 1 were both unreachable — demonstrated by the implementer's
  own gate, *exit 4 on 734 passed / 0 failed*.
- r2: fixed for load, reopened for SYSPOLICYD — an absolute 100s bound on a
  quantity that grows with run duration, with the cargo build INSIDE the sampled
  window. Manager measured ambient accrual independently: *+4.42s over a clean
  60s idle window, 0.0737 s/s, 22.6 minutes to the threshold with NOTHING
  RUNNING* (reviewer got 0.0753 s/s separately).
- r3: made it a RATE (=SYSPOLICYD_RATE_DEGRADED=1.50=, calibrated on a real
  =--workspace= run: wall 356s, delta +215.2s, rate 0.6045) and fixed the calm
  host — but moving =SUITE_EXIT != 0= above the =FAIL_COUNT= arm dropped the
  guard that made its message true, so a registered flake REDDENED the live gate
  and the C interlock became =--classify=-only.
- r4: restored the guard, derived the host word from the rate, added a 10s
  minimum window, and made =LOAD_DEGRADED_THRESHOLD= actually compared. 44
  selftest cases, seven of them LIVE-PATH — the =--classify=-only blindness that
  let r3's defect ship is closed.

*THE GATE'S FIRST HONEST RUN CAUGHT A REGRESSION FROM THE OTHER CHAIN.* That is
the argument for the whole task. Its =--workspace= run surfaced
=reviewer_dispatches_before_reported_implementer_close_and_unlocks_main_merge=
as REAL; manager confirmed it fails ALONE (5.49s) and root-caused it to
=955c8d6='s staging — =git merge= refuses because the staged record would be
overwritten. A gate nobody trusted an hour earlier found a defect that had
already been merged AND reviewed.

PART A IS SETTLED — DO NOT RE-PROBE. The r2 implementer read its A-effect probe
as negative; the r2 REVIEWER overturned that and was right: the probe measures
syspolicyd CPU BURN while A avoids serialized Gatekeeper QUEUE WAIT (196ms fresh
vs 4ms linked is BLOCKING, not CPU), so an instrument blind to the effect
returned what it returned. Recorded as *mechanism-justified, effect size
unmeasured*.

TASK-QGWK7.1.1 — THREE DISPATCHES, TWO DEAD. Attempt 1 died at ~20min leaving 712
uncommitted insertions; the manager COMMITTED them as =0273e9f= before closing,
which is the only reason they survived — the abort close removed the worktree
directory underneath. Attempt 2 died producing nothing. *Manager then STOPPED
rather than dispatch cursor-agent a third time*, and the operator switched
implementers to claude. Attempt 3 (claude) REPORTED cleanly.

MANAGER RULING on the salvage, measured not argued: attempt 1 had invented a
fifth M-0 option — stage into a TEMPORARY index, then =write-tree= /
=commit-tree= / =update-ref refs/orgasmic/dispatch-records/<tx>=. Clever, and it
cannot block a merge. REJECTED: a scratch-repo clone test showed *a fresh clone
does not see the record* (=refs/orgasmic/*= is not in the default fetch refspec),
so it buys merge-compatibility by abandoning the criterion the chain exists for —
and it had rewritten the F-1 test and the parent's verify artifact to match. Its
M-2 verification loop was salvaged; the rest was not.

RUNTIME REINSTALLED 2026-08-07T04:42Z to =eb772a6=. Built while a worker was
still live (compilation is not the syspolicyd trigger), swapped in the
zero-worker window the recipe mandates. All four recorded traps handled.

FILED FROM WHAT THE WORK UNCOVERED: TASK-STWVB.1.1.1.1 (P1, the crashed-binary
false green), TASK-QGWK7.1.1 M-0 (the merge-breaking regression, found by the new
gate), and TASK-VD0VB raised to P1 with a MEASURED lead — =cursor-agent= prints
that =--trust=/=--yolo=/=-f= pre-trust a directory non-interactively, the flag
that task speculated about EXISTS, and orgasmic's driver passes none of them.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. Nothing
enforces resolution and the harness default wins SILENTLY (TASK-A55J7). The
2026-08-07 answers, which are NOT a standing setting:
- implementers =tmux / claude / claude-opus-5 / high= — CHANGED mid-session after
  cursor-agent killed two consecutive workers.
- reviewers =tmux / claude / claude-opus-5 / high=, 5 for 5 with zero transport
  trouble.
- SAME-FAMILY REVIEW IS ACCEPTED, with the reason stated on each dispatch: codex
  is out of credits and cursor-agent is unreliable, so no third harness family
  exists to satisfy the router's independence rule. State it; do not route around
  it quietly. *Restoring codex credits retires this exception* and is the single
  cheapest thing the operator can do for review quality.

THE ORDER FROM HERE
1. INTEGRATE TASK-QGWK7.1.1 (see In flight). Merge, gate, review, close. Watch
   whether the close auto-promotes — that is the reinstall's proof.
2. TASK-STWVB.1.1.1.1 (P1) — the crashed-binary false green. Fix direction is
   fully specified: detect the crash FROM THE LOG (diff the =error: test failed,
   to rerun pass `<target>`= set against the targets that produced a parsed
   failure block; corroborate with =(signal: N, SIG...)=), report it as its own
   =CRASHED (n)= section and its own RED arm above =FAIL_COUNT= and
   =HOST_DEGRADED=. *Do NOT re-drop the =FAIL_COUNT= guard* — that recreates the
   r3 defect exactly. Also stamp the suite exit into the log so =--classify=
   agrees with the live path, mirroring =write_host_stamp=.
3. TASK-VD0VB (P1) — now has a MEASURED fix: =cursor-agent= documents
   =--trust=/=--yolo=/=-f=. Prefer =--trust= (it names the property we need)
   over the other two (which likely relax more), but VERIFY what each does. This
   is also what would make cursor-agent dispatchable again.
4. TASK-RMA18 (P1) — worktree-reclamation redesign, six BLOCK SHIP from two prune
   reviews. =worktree-prune= stays refusing until it lands, and TASK-QGWK7.1
   deferred its F-5 onto it.
5. TASK-3HR8R — a task parked in =in_review= with no reviewer dispatch is
   invisible. FOUR STWVB tasks are sitting in =in_review= right now precisely
   because their chains closed as rejected rounds; this verb would surface that.
6. TASK-A1KJW (P3), TASK-37WN7 (P3).
7. Still P1 and untouched: TASK-4DTBZ, TASK-BX5SR, TASK-A55J7, TASK-48SFW,
   TASK-ADHWZ, TASK-NTNK0, TASK-5M45A.

HOUSEKEEPING WORTH DOING: four TASK-STWVB* tasks sit in =in_review= as closed
rejected rounds whose successors landed. Decide whether a rejected round whose
fix round shipped should move to =done= — right now the board reads as four open
reviews that nobody is going to run.

PRACTICES THAT PAID OFF, keep them:
- Every brief named SPECIFIC CLAIMS to attack and carried a do-not-re-derive
  list. Every review answered by number and came back with measurements.
- Every brief carried an explicit EXECUTION BUDGET (a named maximum number of
  full suite runs). Every worker respected it. Test EXECUTION is the syspolicyd
  trigger and this machine has hard-rebooted twice from that chain.
- The manager verified every BLOCK SHIP against the source before acting, and
  independently reproduced the one measurement a whole round rested on.
- Serializing implementer dispatches one at a time held all session.

** In flight
ONE DISPATCH IS OPEN AND AWAITING INTEGRATION — not orphaned, not running.
TASK-QGWK7.1.1, =tx-20260807-orgasmic-5117=, =[reported]=: the worker finalized
cleanly and its tmux session ended normally. =dispatch-status= shows
=[run-gone] [reported]=, which means "worker finished, manager has not
integrated" — it is NOT a death. The task sits in =in_progress=.

WHAT IS WAITING:
- Branch =task-qgwk7.1.1-impl3=, worktree
  =~/.orgasmic/worktrees/orgasmic/task-qgwk7.1.1-c=, commit =960caea=
  "fix(dispatch): commit the promoted record instead of leaving it staged" — so
  it chose CANDIDATE 1 of the manager's M-0 ruling (commit the record) over
  candidate 2 (keep staging, document close -> commit -> merge). Judge that
  choice at integration; both were live options.
- Report preserved BY HAND at
  =.orgasmic/dispatch-records/tx-20260807-orgasmic-5117/last.txt= (13,495 bytes),
  copied before the reinstall as insurance. NOT yet committed.

THE NEXT CLOSE IS ALSO THE REINSTALL'S BEHAVIOURAL PROOF. The installed CLI now
carries =da88c19= + =955c8d6=, so =dispatch-close= should promote AND STAGE the
record by itself instead of deleting =last.txt=. That path has never run. Copy
=last.txt= out by hand FIRST anyway (already done for this one), then close and
CHECK whether =.orgasmic/dispatch-records/<started_tx>/= appears and is staged.
Report the result — it retires the workaround or reopens it.

SEQUENCE FROM HERE: merge =task-qgwk7.1.1-impl3=, gate it, review it (the
merge-sha close is REFUSED on the default branch until a reviewer verdict exists
for that generation — the gate is live and enforcing), then close reviewer then
implementer. The convention's step 4/5 ordering is STALE: it says close then
review, but the gate demands review first, and the tx log shows =reviewer.done=
preceding =implementer.done= on every task this session.

NINE DURABLE WORKER REPORTS NOW LIVE IN THE REPO under
=.orgasmic/dispatch-records/=, all placed by hand. The reviews are worth reading
before touching either chain — =tx-...-4980= (28,018 bytes), =-5005=, =-5026=,
=-5063=, =-5084= carry measurements and per-site tables no summary preserves.

** Divergence / Risks
THE RUNTIME IS CURRENT AS OF 2026-08-07T04:42Z — reinstalled to =eb772a6=,
verified by inode (=529321123=, differs from the pre-reinstall =527048231= and
matches the installed binary) rather than by version string, which proves
nothing. TCC survived. *The one thing NOT yet verified is the behaviour that
motivated the reinstall*: no =dispatch-close= has run on the new binary, so
"close promotes and stages the report by itself" is INSTALLED but UNPROVEN. Keep
copying =last.txt= out by hand until one close demonstrates it — a wrong
assumption here destroys a worker report, which is exactly what the QGWK7 chain
exists to prevent.

CODEX IS OUT OF CREDITS AND FAILS LIKE A TRANSPORT (lead item 4). Until the
operator restores them, do not dispatch codex — and do not read =no server
running= as a tmux defect without capturing the pane first.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item 2).
Unchanged, and it is the single most expensive mistake available. TASK-48SFW.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1). TMUX IS THE
DEFAULT. The TASK-4CSMY pane-liveness channel is confirmed reporting in
production, so the =--governance-json {"stall_timeout_secs":3600}= workaround
stays retired. Read =event.type=, never the serialized line.

=[pid-gone]= IS NOT A DEATH SIGNAL FOR A TMUX DISPATCH. =dispatch-status= prints
it for every tmux run always, because =WORKER_PID=-=. A watcher keyed on it fires
instantly and falsely. Use =[run-gone]=, the tmux session vanishing, or a
non-empty =last.txt=.

=worktree-prune= IS GATED OFF BY CONSTRUCTION and both forms exit 1, including
=--dry-run= (the scan is gated too, because a dry run still walked directories it
had no business walking). Two reviews produced six BLOCK SHIP findings on one
delete path; TASK-M47E5.3 chose to gate rather than patch a third time, and the
implementation is KEPT with its reclamation tests =#[ignore]=d. TASK-RMA18 owns
the redesign and un-ignores them. Do not hand-remove a worktree in the meantime —
=dispatch-close --worktree-remove --branch-delete= is the salvaging path.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). Detection
is =grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention.
That log now rotates by size and no longer double-writes (TASK-ZBYH3), so the
grep is cheaper than it used to be — but the ROLLED generations are separate
files, so grep the set when looking back.

MERGE STILL PRECEDES REVIEW IN PRACTICE. Expect merge -> review -> fix in
=TASK-<id>.N= rounds. When you review a post-merge diff, SAY SO in the brief and
tell the reviewer a REJECT is still actionable, or you will get softened
findings. Across 08-05 and 08-06, roughly half of all reviews returned REJECT —
including reviews OF fix rounds, twice.

DO NOT STASH =.orgasmic= TO GET A CLEAN TREE FOR =orgasmic verify=. The daemon
reads those files, so during the stash window it cannot see its own project and a
concurrent write fails =404 Not Found: node TASK-XXXXX=. Commit the state first;
=verify= only needs the tree clean, and committed state is clean.

REVIEW-BRIEFING LESSONS, all paid for:
- DO NOT ask a reviewer to mutate source. The contract is read-only, so a brief
  asking for a negative probe returns an unrun gap, correctly. The IMPLEMENTER
  runs negative probes; the reviewer judges them.
- A REJECT the manager then fixes personally is a normal, good outcome.
- NAME THE SPECIFIC CLAIM YOU WANT ATTACKED. The TASK-ZBYH3.1 review was told to
  answer two named suspicions and came back with both CONFIRMED and quantified —
  that is where TASK-G64ZH's syscall table came from. A generic "review this
  diff" would not have produced it.
- TELL THE REVIEWER WHAT IS ALREADY ESTABLISHED so it spends effort on what is
  not. Every review this session was given a do-not-re-derive list.
- VERIFY EVERY FINDING AGAINST THE CODE BEFORE ACTING ON IT, including the ones
  you intend to accept.
- WHEN TWO RULINGS ARE RECORDED ON ONE TASK, CHECK THEY COMPOSE. TASK-ZBYH3 took
  both of its candidate rulings and they CONFLICTED — one moved the file the
  other was comparing against — so the fix relocated the defect instead of
  removing it, and only the next review caught it.

STATE-HYGIENE SHAPES TO WATCH FOR:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  Check =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a
  stage.
- A TASK IN =in_review= WITH NO REVIEWER DISPATCH is worse, because nothing
  surfaces it at all — TASK-3HR8R.
- A BLOCKER RECORDED ONLY IN PROSE is invisible; =depends_on= is what the board
  reads.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED misleads every
  skim-reader. Use a =READ THIS FIRST — STATUS= block at the TOP plus a corrected
  TITLE; appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6); it
spawns real billed provider turns.

NEW THIS BLOCK, 2026-08-06 third block:

THE TEST GATE ITSELF IS THE LARGEST OPEN RISK. =scripts/run-tests.sh= returns
exit 4 on a calm host for its default invocation (lead item 3), and
=.github/workflows/ci.yml:200-206= now DELIBERATELY fails the lane on exit 4 with
a comment justifying it as "host state not attributable to this run" — a premise
that is FALSE until TASK-STWVB.1.1 lands. NOT YET LIVE: nothing is pushed (main
is ~740 commits ahead of =origin/main=), so no CI run has happened. Push would
make it live. Revisit that comment when the fix lands.

A GATE CAN FAIL BY RETURNING NO VERDICT, NOT ONLY BY BEING RED. The STWVB chain's
recurring defect was never a false green — it was INCONCLUSIVE swallowing REAL,
so exit 0 and exit 1 both became unreachable. A gate whose answer is always "no
verdict" gets waved through exactly like one that is always red. When reviewing
any gate change, ask what its answer is on the ORDINARY case, not just the
failure cases.

A SIGNAL THAT CANNOT FIRE READS AS A MEASUREMENT. Twice now in one chain: the
syspolicyd sampler reported a measured =0.0= where it should have reported
UNKNOWN (regex could never match a truncated =ps comm= field), and after that was
fixed the host WORD still printed =calm= for an all-unknown judgment. Whenever a
probe can be unavailable, check that unavailable and zero are DISTINGUISHABLE in
the output — this is the same class as the four-round =logging.rs= proxy chain
from the previous block.

A TEST THAT COULD NOT HAVE FAILED BEFORE THE CHANGE PINS NOTHING. The QGWK7.1
review found two of nine acceptance tests assert on a NEW helper in isolation and
never enter the production call path — they cannot fail on the parent tree because
the function did not exist there. When a brief demands "pinned by a test", check
the test enters the production function, not just that it exists.

A =git add= FAILURE CURRENTLY MIS-CLASSIFIES A CLOSE AND SILENTLY KEEPS THE
BRANCH (TASK-QGWK7.1.1 M-1). Manager-reproduced in source. If a close reports
=CLEANUP_STATUS=worktree_failed= but the worktree is gone and the report is
present, this is why — and the branch was NOT deleted despite =--branch-delete=.

NEW 2026-08-07:

=tmux/cursor-agent= IS NOT DISPATCHABLE UNTIL TASK-VD0VB LANDS. Two consecutive
worker deaths, cause undetermined after ruling out reboot, load, the syspolicyd
chain and harness health. Combined with codex being out of credits, CLAUDE IS THE
ONLY RELIABLE HARNESS ON THIS HOST, which is why same-family review is currently
accepted. That is a stated exception, not a new norm.

A TMUX DISPATCH LEAVES NO EVIDENCE WHEN IT DIES. =stdout.log= is 0 bytes by
design (tmux never persists pane content) and the pane dies with the session, so
there is NO artifact that survives to say why a worker died. Both deaths above
were undiagnosable for exactly this reason. TASK-VD0VB's acceptance asks for a
parked prompt to be distinguishable from a hung transport; today neither is
distinguishable from a crash.

=scripts/run-tests.sh= HAS AN OPEN FALSE GREEN (TASK-STWVB.1.1.1.1). A crashed
binary reads as =GREEN modulo N registered flake(s)= at exit 0 whenever any other
failure classifies. PRE-EXISTING, not introduced here. Prefer scoped
=cargo test= for gates until it lands, and never read a wrapper GREEN as proof
nothing crashed.

A NON-DISCRIMINATING PROBE IS WORSE THAN NO PROBE. The reinstall's
=dispatch-close --help= check returned 0 both before AND after, so it proved
nothing — the change altered behaviour without touching help text. Pre-test the
probe against the OLD binary and confirm it actually differs, or you will read a
null result as a verdict.

COMMIT A DEAD WORKER'S OUTPUT BEFORE CLOSING ITS DISPATCH. =dispatch-close
--status aborted= removed the worktree directory out from under 712 uncommitted
insertions; they survived only because the manager committed them to the branch
first. The salvage-to-=refs/orgasmic/salvage/*= path exists, but a branch commit
is readable and a salvage ref is not.
