#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         0bf80eb0
:LIVENESS_AT:      [2026-09-06 Sun]
:UPDATED_AT:       [2026-09-06 Sun]
:GOAL_ID:          goal-20260826-finalise-the-e01mc-twcp9
:END:

Reconciled 2026-09-06 from live CLI state and source main 0bf80eb0. This is the current digest; do not act on superseded instructions from older handoffs. Historical narrative remains in ledger Git history and the local handoff-before.org backup at /Users/aspirational/.codex/artifacts/orgasmic-status-20260906/.
Operator scope: reconcile completed work, repair malformed IDs through supported daemon operations, and triage P0/P1 backlog. No implementation worker is running. No new feature work or runtime deployment was performed in this pass.

** Done so far
- TASK-D6N77.1 closed DONE: merge 0423ae5 is in main; independent Sol/high PASS already recorded. Current UI typecheck and runDockLabels tests (24/24) close the previous UI execution gap. Existing backend gates remain historical merge-time evidence.
- TASK-TWCP9 closed DONE: owner FINAL decision and chain 543782d7 delivered the orphan ledger branch. Live entry resolves the separate ledger worktree; main tracks no .orgasmic files. D2DM3 messaging remains separately deferred.
- TASK-X0PV1 was already DONE: merge 0bf80eb0 transferred its last flake entry to TASK-STWVB.
- Triage recorded as Status 2026-09-06 on all P0/P1/high backlog items, plus STWVB and ZZ2S4. Seven titles now name remaining work. 099T6 moved P1 to P2 because default billing protection already shipped; QSX56 high normalized to P1.
- Corrected STWVB's stale sampler diagnosis: current run-tests.sh uses pgrep -x plus ps time and rate-based classification; do not rebuild the old comm-truncation fix.
- Source checkout clean at 0bf80eb0. Cached origin/main matches; remote and release channel were not refreshed.

** Next likely actions
1. TASK-ZZ2S4 is the unresolved requested repair. Existing CLI cannot safely rename malformed task identities. Implement and independently review a daemon-owned migration before applying it: inventory current references, preserve journals and historical event references, record old/new identity mapping, and prove interruption/retry safety in an isolated ledger. Do not create duplicate replacement tasks or use doctor collision repair as a substitute.
2. P0 TASK-0Y363: isolate the remaining health-starvation recovery case; original FD leak is fixed. A failed health probe is not authority to kill or replace a live lock owner. Define the safe response from isolated evidence.
3. P1 TASK-48SFW: highest-priority concrete implementation candidate after P0/ID repair. Foreign-home auto-start still rewrites the per-user service. Guard the shared service-start boundary and verify with a temporary adapter.
4. TASK-ADHWZ: audit remaining mixed create/update and restart acceptance; node directories, serialized mutation, and concurrent-create/update regressions already exist.
5. P1 residuals: VD0VB command allowlist/fresh-worktree proof; ZMYEN partial report and report-file support; QSX56 independent review/live workflow proof; P4XKT measured repeated canary; VBSG2 materialization/manual retro.
6. Existing TODO: STWVB must prove classification of the remaining process-spawn flake; TJ9DG needs a superseding decision for retired architecture assumptions. No release or push is scheduled by this handoff.

** In flight
TASK-ZZ2S4 is in_progress in native subagent id_repair_impl, branch codex/zz2s4-id-repair at /Users/aspirational/.orgasmic/worktrees/orgasmic/codex-zz2s4-id-repair. Candidate 7f6deeaf passed 3 CLI fixtures, daemon-fence test, live dry-run (exactly 3 IDs), and release/UI build. Independent native high-effort review found one blocker: orphan temporary files before atomic persist prevent resume. Narrow fix underway; then re-review, rebuild, managed runtime staging, offline CLI apply and post-restart verification. No live ledger repair or daemon restart yet. No orgasmic worker generation exists; do not prune this active native worktree. P0/48SFW remain triaged backlog, with isolated P0 discriminator plan recorded.

** Divergence / Risks
- ID repair remains unfulfilled, with precise blocker and scope on TASK-ZZ2S4. The three nodes contain journals; references also exist in TASK-8DWJP, dec_EWY0K, claims and historical transactions. A text-only rename would strand history.
- Current daemon boot c301d40a-7b8b-43dd-9994-12e3327d9a6c (pid 64646, status version 0.0.24) remains running; ledger sync succeeded and writer queue is empty. Build/deployment parity with main was not re-certified. Prior handoff reported runtime b1c6ca5f behind main; treat that SHA as historical until verified.
- Never hand-edit .orgasmic state. Use CLI/daemon writes; no forced identity property changes or duplicate reconstruction.
- Test/provider calls were limited to UI typecheck and 24 unit tests. No Rust suite, billed harness invocation, live dispatch, service restart, push or release occurred.
- For subsequent Rust tests use scripts/run-tests.sh and its syspolicyd watchdog; never run cargo test --workspace on this host. Default-safe billing guards already exist; never enable ORGASMIC_ALLOW_BILLED_TESTS for this reconciliation.
- The foreign-home/per-user-service startup hazard remains in source. Do not stage ORGASMIC_HOME for daemon-dependent probes; an explicit child ORGASMIC_DAEMON_URL routes to an isolated daemon.
- P0/P1 triage is source inspection, not fresh runtime reproduction. Implemented-looking mechanisms with incomplete acceptance remain open; verify their recorded scope before closing.
