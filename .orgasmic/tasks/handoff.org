#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         db071733bf3bf95e275d6d1314c31b277a5441dd
:LIVENESS_AT:      [2026-07-26 Sun]
:UPDATED_AT:       [2026-07-26 Sun]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-07-28 (manager-direct + nine dispatches). The theme: every merged
fix round in the WGXKD / 6AYEJ / C0XMR / 8W30B family was reviewed for the first
time, and every single one came back BLOCK SHIP — not because the code was
wrong, but because the proof was.

THE ONE THING TO KNOW
Three of the four reviews said, in different words, the same thing: the
implementation is correct and the test that guards it cannot detect its own
regression. TASK-6FNAY's `StalledRmuxReapControl::release` sleeps and then sets
`session_live = false` itself — it never builds an `RmuxControl`, so restoring
any of the three original defects leaves it green. TASK-RRT4T's new sentinel
proves a binary is on PATH, but `run_live_session` turns every SDK startup error
into an inert `Ready`, so four live tests still pass against nothing.
TASK-C0XMR's negative test used `finalized_by_worker: false`, missing the only
case that mattered. When a worker reports "gates green", ask what the test would
do if the fix were reverted. Two rounds now have answered "still pass".

THE LEAK IS TWO LEAKS, AND BOTH ARE THE FLAKE
`LiveSessionGuard` was nine copies of a flock-only guard whose `Drop` unlocked a
file and reaped nothing; session cleanup was a trailing statement a panic skips.
Fixed and merged (TASK-Z3093, 268de73), verified by injection and by an
independent manager rerun (drivers 275/0, session list 3 before → 2 after).
But the same shape survives for *processes*: three orphaned
`cursor-worker-sibling` fixture trees were found holding `/bin/sleep 3600`, two
executing from a worktree `dispatch-close` had already removed. Filed
TASK-BCYMM; the fix must kill the process GROUP, since killing the parent alone
is what orphans the sleep to init. In both classes the load-induced test failure
IS the leak event, so leak rate scales with dispatch volume.

WHAT LANDED
- TASK-Z3093 (268de73) — one shared guard, reaps on Drop. `tokio::spawn` from
  `Drop` is a silent no-op (the runtime dies at the same instant) and
  `Handle::block_on` panics on a runtime worker thread; the answer is a blocking
  reap on a std thread with a bounded join.
- TASK-QSSQH (4c3fd00) — C0XMR fix. The two cases produce a byte-identical
  release, so the boundary is two rules: `RunFail` sticky by KIND regardless of
  position, plus a durable `worker_finalize_admitted` note for driver errors.
- TASK-Q07Y5 (0829c34) — WGXKD.2 fix. `ExitTimeOut` is now DERIVED and lands at
  50 (connection drain 10 + release drain 20 + writer shutdown 10, plus a 10s
  margin), with the CLI stop fuse at 45 in between. On expiry the daemon writes
  a durable loss record outside the writer.

DO THIS FIRST
**The live plist still says 60.** Rebuild and reinstall to pick up the derived
50, using the sanctioned path: `scripts/package-runtime.sh`, then codesign the
staged binary with the `orgasmic dev codesign` identity (the script only signs
when `ORGASMIC_CODESIGN_IDENTITY` is set, and an ad-hoc signature keys TCC to a
per-build cdhash and silently costs the headless daemon its access to
`~/Documents`), then a fresh-inode rename into `~/.orgasmic/bin/orgasmic`, then
`daemon start` from the INSTALLED binary. Never from a worktree build.

STILL OPEN, AND IT MATTERS
The real cause of the historical worker/manager divergence is unknown.
TASK-RRT4T blamed launchd not carrying `~/.local/bin`; that is false on this
machine — the plist embeds the PATH captured at `daemon start`, `ps eww` shows
rmux present, and the reviewer confirmed `command -v rmux` succeeded inside a
dispatched worker. The PATH table in that task is a reproduced simulation, not a
measurement. Net session-count stability cannot distinguish "skipped" from "ran
and released", especially now that Z3093 makes release unconditional. Settling
it needs the sentinel result captured inside a real dispatched worker.

CAPACITY, MEASURED
The `LiveSessionGuard` flock is shared across every test binary in the
workspace, so concurrent worker suites serialize their live sections against each
other: a daemon suite that normally takes ~106s took 378s with three workers
running. Four concurrent rmux workers put load at 28 on 10 cores and cost ~4 GB
of disk each in worktree target dirs. Three is comfortable; four is the ceiling.

A HALF-APPLIED CLOSE, OBSERVED LIVE
`manager dispatch-close` printed "close tx appended but lifecycle update failed"
and then "closed" on the next line. The daemon never died — transient request
failure under load. The ledger now records a `reviewer.done` whose task-heading
transition never happened, and the CLI reported success on the same line as the
failure. Same non-atomic boundary the WGXKD/6AYEJ family is about, reproduced in
the manager path (tx-20260728-orgasmic-3170).

THE INSTALL, unchanged and still true
The operator's daemon is a locally built 0.0.15-dev.1. install.json names the
stable channel and the installer is equality-based, so `orgasmic update` would
see stable advertising 0.0.14, treat it as a change, and revert the machine.
Do not run it. Publishing 0.0.15 needs operator approval.

** Done so far
Session 2026-07-26, in order.

LANDED
- TASK-8W30B (d60c4ed, merge d3fae4d): RmuxControl::release aborted its lifecycle
  task before Session::kill, poisoning the shared ordered SDK transport, so every
  rmux run leaked a session, pty and harness process. Leak +8/run -> 0,
  manager-measured. Reviewer then BLOCKed it: cancellation consumed the only
  Session handle before a cancellable await, so Drop could not retry.
- TASK-6FNAY (83ea420, merge 075e017): the round-2 fix. as_ref() instead of
  take(), 2 s SDK + 2 s CLI reap budgets inside the caller's 5 s, an
  endpoint-exact `-S <endpoint> kill-session -t <name>` fallback, and a detached
  run_complete sender that survives cancellation.
- TASK-STWVB round 2 (67e427c, merge c578fcc): one OnceLock-owned warm executable
  per test binary, per-test variation moved into argv, hard links for
  production-shaped paths. Kept, because the mechanism is sound — but the gate is
  NOT met; see In flight.
- Prompt rules (902b240, 3f7121c): every dispatch report must name driver,
  harness, model and effort; and check `orgasmic <group> <verb> --help` before
  inventing a verb. The second was written because a shipped convention taught
  `orgasmic manager drivers`, which has never existed.

MEASURED, and worth not re-deriving
- macOS charges 150-270 ms of Gatekeeper evaluation on the FIRST exec of each
  newly created executable, through one serialized daemon. Warm exec 4 ms. A hard
  link to a warmed file is also 4 ms (the cache key is the inode); a byte-
  identical copy pays 162 ms. And `/bin/sh script` on a fresh file is 5 ms
  against 270 ms for `./script` — the simplest sidestep, and the one to use next.
- The remaining suite pressure is in the rmux test path, not in the converted
  fixtures: syspolicyd burns +281 s of CPU during a 106 s run with rmux present
  and +32 s with it absent. What it is exec-ing has NOT been identified. The rmux
  binary itself is cheap (9.2 ms, 0 ms syspolicyd).

BOARD REPAIR
In Review went 5 -> 3. TJKFC (P0, partial implementation) and STWVB moved back to
in_progress; Z8WEJ and VB9DQ stay in review with their real blocker recorded —
they need a live claude dispatch on a daemon carrying today's main.

CLI DEFECTS FOUND WHILE WORKING, now understood correctly
`--property KEY=VALUE` writes any key verbatim and the schema reads only a fixed
UPPERCASE set (schema.rs:276-286), matched case-SENSITIVELY. So `write_scope`,
`test_cmd` and `priority` in lowercase are stored where nothing reads them, and
the response echoes them back as changed. The earlier note in this session that
uppercase WRITE_SCOPE was "silently dropped" was wrong — uppercase is the form
that works. `task create` has no --priority flag at all, which is how all seven
tasks filed today came out priority None. Tracked on TASK-HXSW0.

** Next likely actions
1. TASK-RRT4T (P0). Make a skipped test visible in the default cargo test summary
   — panic unless an explicit opt-out is set, or mark them #[ignore] and run them
   through a filtered gate. Nothing else on this crate can be trusted first, and
   it is the cheapest fix on the board. Audit tmux and cursor-agent guards for the
   same `if !available() { return }` shape.
2. Integrate TASK-2BPWM when its watcher reports (in flight).
3. TASK-STWVB round 3. Two tests still red. Use `/bin/sh <script>` rather than
   more warming, find what the rmux test path execs, and treat the regenerate
   test's new 409 "harness busy" symptom as an unestablished cause.
4. TASK-S2KM0 (CI runs the suite). Note the trap recorded on it: Linux has no
   Gatekeeper, so the fixture class of failure would go green in CI while failing
   on every developer Mac. CI is still the right answer to "every green claim is
   one local run", but not a substitute for a local gate.
5. TASK-HXSW0 (CLI self-documentation): reject unknown/miscased property keys
   instead of storing them, give `task create` the flags `task update` has, and
   make help agent-first per the operator's instruction — good errors that name
   the fix, not command inventories pasted into prompts.
6. Publish 0.0.15 to stable. Needs operator approval; it is a public release.
7. Then TASK-Z8WEJ / TASK-VB9DQ end-to-end verification on the published runtime.

** In flight
TASK-2BPWM implementer, LIVE. Worker implementer-codex-acp-stdio
(acp-stdio / codex / gpt-5.6-sol / xhigh), pid 52455, run
run-20260726T133651-60f38adc34c349cd9800c0aaed726807, dispatched from db07173.
Brief at .orgasmic/tmp/dispatch/task-2BPWM/task-2BPWM-brief.md. One watcher
installed with a 60-minute deadline. It fixes `dispatch-close` force-removing
dirty worktrees by default (salvage-commit before removal, both the CLI path at
manager.rs:2007 and the daemon's at api.rs:4438). One question was deliberately
left to the worker to argue: whether --worktree-remove should default false for
--status aborted.

TASK-STWVB is in_progress, NOT closed. Round 2 is merged and kept, and its gate
is not met: 502/2, 501/3, 502/2, 502/2 across four manager runs at ~106-112 s,
against the worker's reported 504/0 at ~26 s. The difference is entirely
TASK-RRT4T — hiding rmux from PATH reproduces the worker's numbers exactly. Its
branch task-stwvb-impl and worktree at .orgasmic/tmp/dispatch/task-stwvb/worktree
are still on disk; leave them until round 3 or clean them deliberately.

Two orphan dispatch records remain from 2026-07-23, both [pid-gone] [run-gone]:
TASK-870YX and TASK-RRVX0. Untouched, and older than this session.

17 dispatch worktrees are on disk, some from 2026-07-23. Do not mass-clean them
until TASK-2BPWM lands: the current `dispatch-close` default is exactly the
data-loss path being fixed.

OPERATOR HOLD [2026-07-26]: DO NOT SPAWN CLAUDE WORKERS. Standing instruction
given after TASK-6AYEJ merged — "do not spawn new claude workers for now". No
`--harness claude` dispatch of any kind, implementer or fixer, until the
operator lifts it explicitly. Read it as a hold on dispatching generally rather
than a narrow one: the reviewer stage is codex and so is not literally covered,
but every dispatch this session was authorized turn by turn and nothing on the
board is urgent enough to launch unasked. Ask first, including for a codex
reviewer. Nothing was running when the hold arrived. This does NOT retract the
standing worker matrix in `manager-dispatch` — that stays the default for when
dispatching resumes. Do not "helpfully" rewrite it to codex-only. For whoever
lifts the hold: the claude lane has two known problems of its own, either of
which may be the reason for it — TASK-RWCRN (P0, every rmux dispatch is killed
at exactly 600s unless `--governance-json '{"stall_timeout_secs": 0}'` is
passed) and TASK-NYF7Z (P1, rmux claude workers get no `--strict-mcp-config`
and hold live handles to nine of the operator's MCP servers).

SUPERSEDED ABOVE: the TASK-2BPWM entry at the top of this section is stale.
TASK-2BPWM is done (its reviewer BLOCK SHIP was cleared by TASK-D0GA3, merged
0daa77c). TASK-D0GA3 is done. TASK-6AYEJ is merged at 5f5670f and in_review,
reviewer pass owed. Nothing is in flight.

THE 6AYEJ FIX IS MERGED BUT DORMANT. Workers resolve `orgasmic` from the
daemon's launchd PATH, which is the installed 0.0.15-dev.1 build, not the source
tree. Until the runtime is rebuilt, workers still emit `implementer.done` and
close their own dispatches, so `manager dispatch-close` stays unreachable and
the manager must remove each worktree and branch by hand after merging. The
rebuild is an operator decision and has not been taken.

The 17 stale dispatch worktrees can now be cleaned safely — the reason to wait
(TASK-2BPWM's force-removal data-loss path) is fixed and merged. Not yet done.

** Divergence / Risks
- WORKER TEST CLAIMS ON THE DAEMON CRATE ARE NOT EVIDENCE until TASK-RRT4T lands.
  Three over-reports today, one mechanism. Re-run the scoped test yourself and
  verify a side effect that proves it executed.
- The daemon suite is not green: post_artifact_regenerate_hot_path (now 409
  "harness busy") and poll_direct_child_pid (30 s deadline) fail in-suite and pass
  alone; recovery_inventory fails about 1 run in 3.
- 0.0.15 is unpublished and the running daemon predates every fix from today.
  Do not run `orgasmic update` — it would revert the machine to 0.0.14.
- There is still no CI, so every green claim is one local run on one machine.
  This is the root of the "chasing issues while fixing issues" feeling the
  operator named: without a shared gate, each fix is verified once, by whoever
  happened to run it, in an environment nobody else can reproduce. TASK-S2KM0.
- A live dispatch remains the only acceptable evidence for driver argv changes.
  Every defect found in the last two days had a passing test standing over it.
- TASK-VB9DQ and TASK-Z8WEJ sit in in_review with Evidence written against
  pre-SGRTX behaviour. VB9DQ's "not verified end to end" is stale in the opposite
  direction: it IS verified, but only after SGRTX. Re-read both before closing.
- Superseded and retained so it is not repeated: an earlier note in this handoff
  claimed `orgasmic run release` does not exist by omission and that `run list`
  reported 0 live. The first is by design (release is the daemon's job; the
  manager's surface is `orgasmic run recover`, and `manager dispatch-close`
  releases the run when the record carries a run_id, manager.rs:627). The second
  was simply false — the run was live for about 80 minutes and was cleared by a
  daemon restart, which cost about a minute of downtime and required
  `rmux kill-server` first because a stale rmux-daemon owned the socket.
