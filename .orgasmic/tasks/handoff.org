#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         9b807444fc55eeed12af7a69137d0597b1b7329f
:LIVENESS_AT:      [2026-07-31 Fri]
:UPDATED_AT:       [2026-07-31 Fri]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-07-30/31: one containment failure, three merges, board clear.
Nothing is running — =dispatch-status= is empty, in_review is empty.

THE ONE THING TO KNOW
A daemon *unit test* spawned real, billed agents that outlived it. TASK-95SGV.2's
test built a =StageWorker { driver: "acp-stdio", harness: "hermes" }= and guarded
the spawn with "unreachable without a hermes binary" — true on CI, false here.
Twelve-plus =hermes acp= sessions survived as PPID-1 orphans and worked =main= for
25+ minutes (repeated cargo tests, =cargo clean=, a probe commit, and one orphan
running an unscoped =kill -9= sweep across the machine). They self-replicated
while the unsafe test sat on main as =f215bb7=, each orphan re-running the test
that spawns orphans, and stopped at the hermetic merge =b510b30=. The structural
fix landed as TASK-3NJ9K; the residual gap is TASK-3TQ0R.

Two rules that came out of it, both cheap and both earned:
- A VANISHED TX ENTRY MEANS REVERTED, NOT LOST. =tx-20260730-orgasmic-3988=
  left =.orgasmic/tx/2026-07.org= and =dispatch-close= refused, because an
  orphan's =git checkout HEAD -- ...= reverted the daemon's *uncommitted* writes.
  It returned verbatim once the orphans were dead. Kill first, re-read, never
  hand-author a replacement.
- OWNERSHIP IS PROVABLE, so prove it. Every ACP session logs its dispatch prompt
  in =~/.hermes/logs/agent.log=. Do not pattern-match =ps=. The standing hermes
  gateway (=hermes_cli.main gateway run=) is NOT task-owned — spare it.

** Done so far
Session 2026-07-30/31. Older sessions compacted out; their durable measurements
already live in =.orgasmic/gotchas.org= and =decisions.org=.

LANDED
- TASK-95SGV.1 (merge =f55cf6e=) — cleanup reservations release on every refusal.
- TASK-95SGV.2 (merge =b510b30=) — the ordinary dispatch endpoint proves its
  cleanup conflict provider-free. The first cut of this test caused the orphan
  incident; the merged version spawns nothing.
- TASK-RKTH1 (merge =5e6b993=) — tmux packs a whole command line into ONE imsg
  capped at 16 KiB, and the compiled prompt rode in that argv, so a large brief
  died as tmux's =command too long= and surfaced as an opaque =500 failed to
  acquire worker run=. Oversized launches now go through a 0600 self-unlinking
  launcher script: tmux sees a short path, the harness still gets the prompt as
  one byte-exact argv element. Measured: 16 000 bytes of argv spawns, 20 000 does
  not.
- TASK-3NJ9K (merge =75fe95f=) — daemon driver resolution goes through
  =crate::driver_resolution=: a pass-through normally, a default-deny fence under
  =cfg(test)=. Two tiers, because resolving and launching differ. Resolution
  keeps a mux allowance for live tmux/rmux coverage that never launches;
  =resolve_launch_driver= (five call sites) refuses a mux address whose harness
  that mode would exec. The second tier came out of manager review: the worker's
  mode-only allowance would have passed =tmux=/=claude=, and the dispatch path
  stages a placeholder both mux modes swap for the real binary.
- TASK-CB6GQ (merge =9b80744=) — =node body set --section= appended when the
  section was missing, so any typo minted a permanent heading, and no CLI verb
  could remove one though =remove_section= existed in core, in =NodeEditOp= and
  on the route. Editing and creating are now distinct ops, =--create= is how a
  new section says so, =node body unset --section= removes one, and an absent
  section is named instead of collapsing to =org file update failed=. A parity
  test asserts every daemon edit op is reachable from some CLI verb.

FILED
- TASK-3TQ0R (P2) — =cfg(test)= does not reach =crates/orgasmic-daemon/tests/=.
  Audited: nothing there spawns today, so it is a structural gap, not a leak.

GATES on =9b80744=: daemon lib 640, drivers 313, core 156, CLI 201, node
round-trip 8; fmt clean, clippy -D warnings clean. verify TASK-3NJ9K / RKTH1 /
KPMFK / 95SGV.2 / CB6GQ all PASS.

Three proofs were strengthened rather than merely kept green: =verify/TASK-3NJ9K=
now injects BOTH fence tiers; =verify/TASK-KPMFK= is pinned in full again after
the stub removed the =codex app-server= race that had forced it to be truncated;
=verify/TASK-CB6GQ= runs its two binaries with OR'd exit codes, because chaining
them with =&&= would have stopped at the first failure and pinned half a proof.

LEDGER REPAIR: =tx-20260731-orgasmic-4025= re-records a manager observation that
never reached the project ledger. It was written from inside a dispatch worktree
in the small hours of 07-30, so it landed in that worktree's frozen =.orgasmic=
snapshot (gotcha =0a054b3=) and survived only in a git stash; the live ledger for
that day starts at 3973 and the entry claimed 3931.

** Next likely actions
1. TASK-4CSMY (P1, backlog) — give tmux a coalesced =pane_activity= and
   generalise TASK-JQ8AV's open-turn consult beyond rmux. Still the SHIP BLOCKER
   for the tmux default flip, and the reason every long tmux dispatch must carry
   =--governance-json '{"stall_timeout_secs":3600}'=.
2. TASK-A55J7 (P1, backlog) — refuse to dispatch when no model resolves, and
   persist resolved model/effort on RunMeta, the dispatch tx, run list and
   dispatch-status. Resolution is exactly =--model= > task drawer > REFUSE; the
   operator has ruled out a project-level default.
3. TASK-0JY7M (P1, todo) — ask once per session for agent selection, and state
   driver/harness/model/effort back after every dispatch.
4. REVIEW POSTURE, the one decision this session deliberately did not take.
   TASK-JQ8AV and TASK-FZB6T are now in IN_REVIEW rather than IN_PROGRESS, which
   is what is actually true: merged (=4fc57fe=, =1b726f6=), no live worker,
   accepted on manager-replayed injection proofs and green gates alone, neither
   author having reported. Either dispatch reviewers over them or review-close
   them deliberately — but the board should not drift back to reading as if the
   work were still being written. FZB6T is the one to review first: its rescued
   refactor changed the run-catalog concurrency model and its author never
   described it.
5. Reinstall the runtime in a zero-worker window (now is one). The installed
   binary is 51+ commits behind and contains none of this session's merges — so
   TASK-RKTH1's fix is not live, and a dispatch with a large brief through the
   INSTALLED daemon still dies on tmux's =command too long=. Not done here
   because replacing the operator's running daemon is theirs to time.
6. TASK-3TQ0R (P2, backlog) — extend the fence past =cfg(test)= to daemon
   integration tests. Not urgent (nothing there spawns today) but it is the last
   place the orphan class can still be written by accident.
7. TASK-STWVB (P2, in_progress) now carries a second axis: the suite's flakes are
   worktree-sensitive, not only load-sensitive. Identical code in two worktrees
   measured 42.9s/clean against 85.9s/red. Any full-suite number quoted anywhere
   needs the worktree held constant to mean anything.

** In flight
Nothing running. =dispatch-status= is empty, no =hermes acp= or harness
processes are alive, no tmux or rmux server is up, and =git worktree list= shows
only the repo root — every dispatch worktree from this session has been removed
and its branch kept, so no worker history was lost.

The board, after this session squared it with reality:
- IN_PROGRESS (2): TASK-TJKFC (P1, partial implementation, no worker) and
  TASK-STWVB (P2, the flake work, no worker).
- IN_REVIEW (2): TASK-JQ8AV and TASK-FZB6T — merged, unreviewed, no live worker.
  See Next likely actions item 4; this is a pending decision, not pending work.
- TASK-95SGV closed done: parent fix =bf555ae= plus both subtasks merged, nothing
  outstanding.

Incident forensics are no longer in an ephemeral scratchpad. They live at
=~/.orgasmic/incidents/2026-07-30-orphaned-acp/= (3.3 MB, 22 files) with a
README that stands alone: pre-kill =ps=, per-PID cwd/fd/descendants, the six
=hermes-snap-*= env captures, the 4,282-line =agent.log= window that proves
orphan ownership, and the repo/ledger state from before the kills.

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO. =~/.orgasmic/bin/orgasmic= is built at =ee1f855=,
51+ commits behind =main=. None of this session's merges are live for the
operator's daemon — including TASK-RKTH1, so a large brief dispatched through the
INSTALLED daemon still dies on tmux's =command too long=. TASK-CB6GQ's
=node body unset= is the exception and works today, because the =remove_section=
op it calls has always existed daemon-side; only the CLI half was missing.
Reinstall in a zero-worker window.

TMUX IS THE DEFAULT AND IS STILL NOT SAFE. =pane_activity= is emitted by
=modes/rmux.rs= and nothing else, and TASK-JQ8AV's stall rescue is gated on
=transport == "rmux"=, so on tmux both pane channels are blind and a
provider-bound think at ~0% cpu is released at 600s. Until TASK-4CSMY lands,
=--governance-json '{"stall_timeout_secs":3600}'= is mandatory on every
long-thinking tmux dispatch. Not optional.

NOTHING ENFORCES THE MODEL. Resolution is =--model= > task drawer > harness
default, and the harness default wins silently — which is how three dispatches
once ran on claude-fable-5 while everyone believed opus-5. Until TASK-A55J7,
pass =--model= explicitly and echo driver/harness/model/effort back.

THE INTEGRATION SUITE IS OUTSIDE THE FENCE. =cfg(test)= covers lib unit tests
only; =tests/dispatch_preflight.rs= really does POST =acp-stdio=/=claude= at a
live daemon. Audited 2026-07-30: every such test engineers a host-independent
credential refusal, so none spawns. The risk is that the safety is supplied per
test rather than structurally — the exact shape of the comment that caused the
incident. TASK-3TQ0R.

TWO CHANGES REACHED MAIN WITH NO WORKER REPORT AND NO INDEPENDENT REVIEW.
TASK-JQ8AV and TASK-FZB6T, now parked in IN_REVIEW so the board says so. Their
injection proofs are evidence about the one defect each names and about nothing
else in the diff. FZB6T is the sharp case — it changed the run-catalog
concurrency model and its author never described it. If run enumeration, boot
reattach or the session writer misbehaves, start there.

THE FLAKE SET HAS TWO AXES, AND A FULL-SUITE NUMBER MEANS NOTHING WITHOUT BOTH
HELD CONSTANT. Load is the known one. The second, measured 2026-07-31 and
recorded on TASK-STWVB, is the WORKTREE: identical code at =75fe95f= ran
42.9s/640-clean in one worktree and 85.9s with a failure in another, on a quiet
machine, sequentially. So never quote a suite count without naming the worktree,
and never attribute a red to a diff without re-running the BASE code in the SAME
worktree — that control costs one checkout and one run, and during TASK-CB6GQ it
was the only thing separating "my change broke the suite" from "this worktree is
slow", after four consecutive reds that looked exactly like a regression.
Cause unidentified; leftover mux state was ruled out. TASK-STWVB owns it.

LATENT, not currently biting: the daemon probes =claude auth status= for
=loggedIn=, and the standalone CLI reads the macOS Keychain by account — so it
answers =authMethod: none= whenever =USER= is absent, however valid the login.
The LaunchAgent plist sets only =ORGASMIC_HOME= and =PATH=. Not a proxy problem:
=ANTHROPIC_BASE_URL= lives only inside shell functions and never reaches the
daemon.
