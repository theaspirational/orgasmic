#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         57e35dedd63d8788ea778b727c72a631e1080ce2
:LIVENESS_AT:      [2026-08-02 Sun]
:UPDATED_AT:       [2026-08-02 Sun]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-02/03: two dispatches closed, two reviewers running, and a
measurement that invalidates how this project has been reading its own test
suite.

THE ONE THING TO KNOW
A red suite here is not evidence about your diff until you have spent 8 seconds
disproving the machine. Twice tonight, on two different trees, a full
=cargo test -p orgasmic-daemon --lib= returned 14 failures and took 660-820s
against a 43-86s comparable — and both times all 14 passed when re-run one at a
time against the SAME already-built binary in the SAME worktree, in 8 seconds
total. The cause was =syspolicyd= at 586% CPU, the scan chain that has
hard-rebooted this Mac twice. Two beliefs recorded in =gotchas.org= one commit
earlier turned out to be wrong: the Developer Tools grant does NOT prevent the
burst (Warp holds it, and the burst happened anyway), and the trigger is test
EXECUTION, not compilation (a full rebuild ran at 0-64%).

The second thing, which cost real work today: A TMUX DISPATCH ON THE INSTALLED
DAEMON DIES ON THE WALL CLOCK NO MATTER HOW MUCH WORK IT IS DOING. The runtime
is =57e35de=, which has JQ8AV's rmux pane-liveness channel but not TASK-4CSMY's
tmux one. TASK-FZB6T.1's worker committed 4,004 good lines at 14:52:39Z and was
killed at 15:00:36Z, eight minutes later, with its entire report unwritten. The
work was salvaged off the branch; the report is gone for good. Dispatch over
rmux until the runtime is rebuilt.

Both of those are the same lesson in different clothes: the thing that looked
like a code problem was the environment, and the cheap control existed in both
cases. Run it first.

** Done so far
Session 2026-08-02/03. Two dispatches closed, one of them salvaged off a dead
worker; two reviewers now running; and one finding that invalidates every
full-suite number this project has quoted tonight.

READ THIS FIRST: THE SUITE CANNOT BE TRUSTED ON THIS HOST, AND THE CONTROL IS 8
SECONDS. Two full =cargo test -p orgasmic-daemon --lib= runs tonight returned 14
failures each (637/651 on =d1a41c1=, 659/674 on =512d636=) and took 659.90s and
820.47s against a 42.9-85.9s comparable. Both times, re-running those same 14
ONE AT A TIME against the SAME already-built binary in the SAME worktree gave
14/14 green in 8 seconds. The discriminator was =syspolicyd= at 586% CPU with
load average 11.41 — the scan chain in =gotchas.org= that hard-rebooted this
machine at 20:01:36 on 2026-08-02. Killing the suite dropped it to 75% within
seconds. Never attribute a red here to a diff without the isolation rerun:

: env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME \
:   target/debug/deps/orgasmic_daemon-<hash> --exact <test> --test-threads=1

Two corrections to the =gotchas.org= entry written in =d1a41c1=, both measured:
- THE DEVELOPER TOOLS GRANT DOES NOT PREVENT THE BURST. =dev.warp.Warp-Stable=
  holds =kTCCServiceDeveloperTool=2=, the run was hosted under Warp, and
  syspolicyd still hit 586%. The grant was added at 21:59 on 08-02, after that
  days reboot, so tonight was the first time it was ever under load.
- THE TRIGGER IS TEST EXECUTION, NOT COMPILATION. A full rebuild of the daemon,
  core and cli test binaries at =jobs=4= ran with syspolicyd at 0-64%; the test
  run that followed pinned it at 586%. The old entry blamed build scripts and
  proc-macro dylibs, which is where the last two investigations looked.

LANDED
- TASK-JQ8AV.1 (merge =7ade8af=, closed =tx-4070=) — the prior session merged it
  and died before closing, leaving the dispatch open with the work already on
  main. Position-aware pane status contract (last 12 non-blank rows, column 0,
  measured marker shapes) with real at-rest tmux AND rmux false-rescue arms;
  post-probe revalidation of lifecycle/identity/precedence/progress; probe
  subprocesses funnelled through an owned process group with a 2s child deadline
  and kill+reap.
- TASK-FZB6T.1 (merge =91ff72c=, closed =tx-4079=) — SALVAGED. The worker
  committed 4,004 lines at 14:52:39Z and was killed eight minutes later at the
  3600s stall ceiling without ever calling =dispatch finalize=; =tx-4069= had
  recorded it orphaned. The branch was intact. Nine reviewer findings closed: a
  confirmed archive/compact/rollback transaction with a plan-digest manifest,
  transport-aware reclaimability, paging before classification, a payload budget
  over the serialized event, tombstones terminal per run IDENTITY (device+inode)
  rather than per path, per-project dirty state, snapshot admission through
  session-directory authority, the catalog mutex released across filesystem
  work, and exact =run show <id>= by exact lookup.

WHY THAT LOSS HAPPENED, because it will happen again otherwise: the installed
daemon is =57e35de=, which carries JQ8AVs *rmux* pane-liveness channel but not
TASK-4CSMYs *tmux* one. A tmux dispatch on this daemon has no progress signal
at all and dies on the wall clock regardless of how much work it is doing. Use
rmux until the runtime is rebuilt.

GATES on =91ff72c=: fmt clean; clippy -D warnings clean; core lib 158/0; cli
green; =run_inventory_wire= 2/0; daemon lib 659/674 with all 14 reds green in
isolation. All FOUR FZB6T injection proofs PASS red-then-green byte-identical
(=TASK-FZB6T=, =TASK-FZB6T.1=, =-redraw=, =-corruption=).

The one manager check worth repeating: =verify/TASK-FZB6T/injection.patch= was
REGENERATED by this diff (82 lines), and a re-cut injection patch is the easiest
place for a proof to be silently weakened. It was read against the original
before the close — it removes the same cache-hit branch, translated into the new
four-phase =refresh_dir=, and =expect-red=, =cmd= and =README= are untouched.

FILED
- TASK-T7KSH (P2) — =inspect_rmux_binary= execs an unbounded child on the
  daemons rmux probe path, defeating the child deadline JQ8AV.1 just installed.
  The author MEASURED it (5.012s sweep against a 2s deadline, 120s test
  teardown) and left it only because it is outside that tasks write scope. The
  shipped regression deliberately answers =-V= normally, so it cannot catch this.
- TASK-WPC6G (P2) — =gotchas.org= is off the node write surface entirely, so
  every entry in it is a hand edit in violation of the write rule. Found by
  trying to record the corrections above through the CLI and finding no verb.

TASK-STWVB re-framed rather than extended: it has now been confounded by three
separate environment axes (89%-full disk, worktree identity, host scan load),
each of which produced a stable-looking failure set that read as a regression.
Its first deliverable is a gate that gives a defensible verdict on a loaded
machine, not a per-test root cause.

** Next likely actions
1. WAIT FOR THE TWO REVIEWERS, then close their dispatches and act on the
   verdicts. This is the only thing genuinely in flight; everything else below
   is ordered behind it because it depends on those verdicts.

2. REBUILD AND REINSTALL THE RUNTIME FROM SOURCE — but only after the verdicts.
   The operator authorized this explicitly 2026-08-03 ("free to rebuild and
   restart the daemon from source when needed"), and it was DELIBERATELY NOT
   DONE tonight. Reason: the installed daemon is =57e35de=, and everything
   merged since is unreviewed. Reinstalling now would put an unreviewed
   run-catalog and session-storage rewrite — deletion code that rewrites the
   daemons own evidence of what workers did — into the daemon that manages the
   operators six real projects. The cost of waiting is that tmux dispatches
   stay unsafe; the cost of not waiting is the operators run history. Waiting
   was the right trade, but it EXPIRES the moment the reviews come back clean,
   because until the rebuild lands, TASK-4CSMYs whole point (tmux safety) is
   unverifiable in practice and every dispatch has to go over rmux.
   Recipe and its four traps: memory =orgasmic-runtime-reinstall=; the sharp one
   is that =install.sh= SKIPS the unpack when =runtimes/<version>-<target>/=
   already exists and the version string does not change between local builds,
   so move the old dir aside first, and pass =ORGASMIC_INSTALL_SKIP_STATUS=1=.

3. TASK-A55J7 (P1, backlog) — refuse to dispatch when no model resolves, and
   persist resolved model/effort on RunMeta, the dispatch tx, run list and
   dispatch-status. Resolution is exactly =--model= > task drawer > REFUSE; the
   operator has ruled out a project-level default. Still the standing reason
   every dispatch must pass =--model= explicitly and echo the four values back.

4. TASK-0JY7M (P1, todo) — ask once per session for agent selection, and state
   driver/harness/model/effort back after every dispatch. Asked and answered
   this session by hand: reviewers on codex/gpt-5.6-sol/high, implementers on
   claude/opus-5/high.

5. TASK-T7KSH (P2, backlog, filed today) — the unbounded =inspect_rmux_binary=
   child. Sharper than its priority suggests right now, because rmux is
   currently the ONLY safe dispatch transport on the installed daemon, and this
   is a hole on the rmux probe path specifically.

6. TASK-STWVB (P2) — now re-framed. Its first deliverable is a gate that returns
   a defensible verdict on a loaded machine; the per-test root cause is second.
   The 8-second isolation control recorded in Done-so-far is most of a design.

7. TASK-WPC6G (P2, filed today) — put =gotchas.org= on the node write surface.
   Small, and it removes a standing rule violation that every session currently
   has to either commit or drop knowledge over.

8. TASK-TJKFC (P1, in_progress) — partial implementation, no worker, untouched
   for several sessions. Worth a deliberate decision: finish it or move it back
   to todo rather than letting it sit in IN_PROGRESS claiming to be underway.

** In flight
TWO REVIEWERS RUNNING, both =rmux= / =codex= / =gpt-5.6-sol= / =high= with
=--governance-json {"stall_timeout_secs":3600}=, read scopes disjoint:

- =tx-20260802-orgasmic-4077= — TASK-4CSMY + TASK-JQ8AV over
  =crates/orgasmic-daemon/src/supervisor.rs= (=9017514..HEAD=, 1,472 insertions).
  One reviewer for two tasks because it is one mechanism: 4CSMY carries the
  unactioned BLOCK SHIP =tx-4055= and JQ8AV.1 is the fix round for both.
  Worktree =.orgasmic/tmp/dispatch/task-4CSMY-review/worktree=.
- =tx-20260802-orgasmic-4081= — TASK-FZB6T over the run catalog
  (3,549 insertions incl. a new 1,158-line =run_history.rs=). Worktree
  =.orgasmic/tmp/dispatch/task-FZB6T-review/worktree=.

Both briefs are preserved at =.orgasmic/tmp/brief-supervisor-review.md= and
=.orgasmic/tmp/brief-runcatalog-review.md=. Both carry the host warning and the
instruction to isolate-rerun any red before believing it; both are told to call
=orgasmic dispatch finalize= explicitly, since that is the exact step whose
absence cost the FZB6T.1 report today.

THE BOARD
- IN_PROGRESS (2): TASK-TJKFC (P1, partial implementation, no worker) and
  TASK-STWVB (P2, the flake work, no worker). Neither has a live worker.
- IN_REVIEW (3): TASK-JQ8AV, TASK-FZB6T, TASK-4CSMY — all three merged, all
  three under a live reviewer as of this handoff.
- Both fix subtasks (TASK-JQ8AV.1, TASK-FZB6T.1) are DONE.
- TODO (7) and BACKLOG (~47) untouched this session, plus the two new filings.

WHEN THE REVIEWERS REPORT, the close sequence is =manager dispatch-close
--started-tx <the tx above> --status done=, then act on the verdict. A SHIP
verdict on all three is what unblocks the runtime rebuild below; a NEEDS FIXES
mints a fix subtask exactly as JQ8AV.1 and FZB6T.1 were minted.

** Divergence / Risks
THE SUITE HAS NO TRUSTWORTHY FULL-RUN SIGNAL ON THIS HOST, and this is the risk
that contaminates every other judgement on the board. Measured twice tonight on
two different trees: 14 failures in-suite, 14/14 green in 8 seconds in
isolation, while =syspolicyd= sat at 586% CPU. Anyone who quotes a suite count
without naming the worktree AND the host state is quoting noise. The control is
cheap and it is in Done-so-far — use it before believing any red. This machine
has hard-rebooted twice from this chain (=WindowServer-2026-07-29-112228.ips=,
=WindowServer-2026-08-02-200136.ips=), so it is not only a measurement problem.
If you run the suite unattended, guard it: sample =ps -o pcpu= -p <syspolicyd>=
every 10s and kill the cargo run if it holds above ~400% for a minute.

THE RUNTIME IS DELIBERATELY BEHIND THE REPO. =~/.orgasmic/bin/orgasmic= runs
=57e35de=; main is well past it. This is a decision, not drift — see Next
action 2. Consequences while it holds:
- TMUX DISPATCHES ARE UNSAFE. =pane_activity= reaches the installed daemon from
  =modes/rmux.rs= only, and JQ8AV's open-turn consult is gated on
  =transport == "rmux"= there, so on tmux both pane channels are blind and the
  run dies on the wall clock. This is not theoretical: it destroyed the
  TASK-FZB6T.1 report today, eight minutes after that worker committed 4,004
  good lines. DISPATCH OVER RMUX until the rebuild lands.
- TASK-4CSMY cannot be validated in practice, only in tests, because the fix is
  not in the running daemon.
- The version string will NOT change across the rebuild — both builds call
  themselves =0.0.15-dev.1= — so version, boot_id and =orgasmic status= cannot
  tell old from new. Discriminate with the =commit= field in
  =current/runtime-manifest.json=, or a new-code endpoint probe. Rollback is
  =runtimes/0.0.15-dev.1-darwin-aarch64.bak-20260802a= (the =ee1f855= build).

THREE MERGED CHANGES HAVE NEVER BEEN INDEPENDENTLY READ, which is what the two
live reviewers exist to fix. Until they report, treat main as unreviewed:
- TASK-FZB6T.1 is the sharp one. Its author was killed before reporting, so
  unlike every other change on this board it carries NO self-assessment and NO
  declared residual risks. It rewrote the run-catalog concurrency model and it
  is deletion code. If run enumeration, boot reattach, =run history compact= or
  the session writer misbehaves, start there.
- TASK-JQ8AV.1 at least declared six residual risks; the first became
  TASK-T7KSH and the rest are carried in the reviewer's brief.

NOTHING ENFORCES THE MODEL. Resolution is =--model= > task drawer > harness
default, and the harness default wins silently — which is how three dispatches
once ran on claude-fable-5 while everyone believed opus-5. Until TASK-A55J7,
pass =--model= explicitly and echo driver/harness/model/effort back.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE. =tests/dispatch_preflight.rs=
really does POST =acp-stdio=/=claude= at a live daemon. Audited 2026-07-30:
every such test engineers a host-independent credential refusal, so none spawns
today. The safety is supplied per test rather than structurally, which is the
exact shape of the comment that caused the 2026-07-30 orphan incident.
TASK-3TQ0R owns it.

LATENT, not currently biting: the daemon probes =claude auth status= for
=loggedIn=, and the standalone CLI reads the macOS Keychain by account — so it
answers =authMethod: none= whenever =USER= is absent, however valid the login.
The LaunchAgent plist sets only =ORGASMIC_HOME= and =PATH=.
