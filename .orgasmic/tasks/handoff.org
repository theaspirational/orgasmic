#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         40f70591a323ebb8fbbeb2a4caa31d81138e106c
:LIVENESS_AT:      [2026-07-24 Fri]
:UPDATED_AT:       [2026-07-24 Fri]
:GOAL_ID:          goal-20260725-land-the-committed
:END:

Session 2026-07-25 (manager-implementer, no workers dispatched — the operator
gated agent dispatch on runtime and dispatch issues being fixed first).

LANDED ON MAIN (merge 5863214)
1. TASK-KWSTJ (done) — bounded GET /api/runs. Was already verified last session;
   this session merged it. 24.7s -> 0.10s TTFB, 28.0 MB inspected of 2.404 GB,
   200 runs classified with zero differences from the pre-fix build.
2. TASK-9P810 (in_review) — the runtime binary is a real file at a stable path,
   replaced by fresh inode, and an install whose code identity differs from the
   incumbent's is refused with both requirements named. Root cause differed from
   the filing: the plist already named bin/orgasmic, but the kernel resolves
   symlinks at exec, so TCC recorded the versioned path.
3. TASK-KKGKM (in_review) — the daemon binds before per-project filesystem work.
   Item 4 was confirmed by reproduction, not assumed: a FIFO planted where a
   session JSONL belongs wedges boot at phase "reattaching runs", 0% CPU, zero
   fds on the file, with a stack sample in read_session_file -> read_to_string
   on the main thread inside block_on. That last detail also explains why the
   heartbeat froze and `status` hung.
4. TASK-TJKFC (in_progress) — the dispatch startup gate exists and rejects
   before any resource is committed. Structural only; see the task for the two
   remaining halves and the open design question about probe cost.

STATE OF THE TREE
main is green: cli 206, drivers+core 403, daemon integration suites pass. Five
driver tests that had been failing on main since TASK-K05MX are fixed. The
daemon lib suite still fails four tests on every full run, pre-existing and now
filed as TASK-STWVB with evidence that they fail identically with unrelated
changes stashed out.

THE ONE THING NOT DONE, DELIBERATELY
None of this is installed. The operator's daemon is still the pre-fix 0.0.14
build, so `GET /api/runs` still takes 35 s on their machine and the boot fix is
not protecting them yet. Installing is outward-facing and previously caused a
30-minute outage plus two System Settings approvals, so it needs the operator's
say-so. TASK-9P810 is precisely what makes that install safe, and its own
migration prints that the first stable-path install still costs one final
permission prompt because the executed path changes.

NEXT
- Install the built runtime (operator decision) and then TASK-Z8WEJ / TASK-VB9DQ
  verify end-to-end, which is also what unblocks dispatching agents again.
- Finish TASK-TJKFC: adapter probes, then the atomic-abort half.
- TASK-7QM8M (bound boot reattach reads) and TASK-STWVB (flaky daemon suite)
  were both filed from evidence found this session.
- The process arc (YGB1R, TW3N3, JQARS, 8ED6V, 3CM0Q, 4YX1K, 0JY7M) is untouched.

** Done so far
- Published runtime 0.0.14 to https://github.com/theaspirational/orgasmic/releases/tag/stable with four refreshed macOS/Linux targets and the existing Windows 0.0.7 target preserved.
- Published apps 0.0.8 to https://github.com/theaspirational/orgasmic/releases/tag/apps-stable with macOS updater/DMG assets and an Android aarch64 APK.
- Attached approved release notes to both releases and verified both tags target 40f70591a323ebb8fbbeb2a4caa31d81138e106c.
- Downloaded and verified runtime-latest.json, latest.json, and android-latest.json versions, commit provenance, target inventory, and hashes.

** Next likely actions
1. Begin the next round from clean synchronized main.
2. Update or launch a fresh runtime 0.0.14 process only when ready to handle the macOS Documents permission prompt.
3. Run the dedicated Windows stable workflow when a Windows runtime newer than retained 0.0.7 is required.

** In flight
None. Runtime and app stable releases are published and verified; no worker dispatch is active.

** Divergence / Risks
- The foreground daemon at PID 15304 / boot d43b38ae-a23d-4b75-91a3-e5ef3066a58a intentionally remains on runtime 0.0.13 and was not restarted. Every newly launched daemon under Documents may prompt for access again.
- The Windows stable runtime remains 0.0.7 because Windows cannot be built locally from macOS.
- The macOS app publisher produced updater signatures but skipped Apple notarization because notarization credentials were not present in the environment.
