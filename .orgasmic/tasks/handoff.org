#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         db071733bf3bf95e275d6d1314c31b277a5441dd
:LIVENESS_AT:      [2026-07-26 Sun]
:UPDATED_AT:       [2026-07-26 Sun]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-07-28 (manager-direct + twenty-one dispatches). Eleven rounds
merged, every one verified by an injection the manager ran personally, not by
reading the worker's account of having run it.

THE ONE THING TO KNOW
A green suite is not evidence on this project. TASK-Q07Y5 shipped with every
gate green — CLI, daemon, and the manager's own — and killed the operator's
daemon 10.00s after every boot, because no test anywhere ran a daemon longer
than ten seconds. The standard that came out of it and now holds for every
round: reinject the defect, show the test red with its output, restore, show it
green. Ask a worker what its test would do if the fix were reverted and expect
an answer with an exit code in it. **Re-run the injection yourself before
merging.** Four times today a worker's fix was correct and my own re-injection
was what confirmed it; once (JQARS) my injection was malformed and I nearly
filed a defect against a working gate — check your probe before blaming the code.

STATE OF THE INSTALL — CURRENT AND VERIFIED
`~/.orgasmic/bin/orgasmic` is 0.0.15-dev.1 built from `dc6db85`, containing
R74E8, S0QRM, H0TYV, ATAXN, JQARS, GT91X and KKBTP. Signed with
`ORGASMIC_CODESIGN_IDENTITY='orgasmic dev codesign'`; designated requirement
byte-identical to the incumbent's (certificate root a70531007c…), so TCC grants
carried. **ExitTimeOut is the derived 50.** Daemon holds one pid and one boot
across the whole post-reinstall window, 0 parse errors.

Reinstall recipe, in order: set the codesign identity or `package-runtime.sh`
silently ships unsigned; verify the DR matches the incumbent; copy to
`orgasmic.new`, `codesign -v`, `mv -f`; `rsync` the `shipped/` payload; restart
from the INSTALLED binary. **Only ever with zero workers running** — a restart
cycles the rmux LaunchAgent and destroys every live session. That is exactly how
the morning's outage killed three dispatches.

WHAT LANDED TODAY
- R2HDN (5d4651a) — a live test now fails when acquisition degrades to inert.
- 1T3FZ (68f3ed6) — destructive close holds a daemon-owned worktree reservation
  installed under the supervisor lock BEFORE the verdict, so the verdict is
  monotone. Under review now.
- 69CW6 (03f4d05) — the self-satisfying reap surrogate replaced by
  StallableRmuxEndpoint driving the real RmuxControl.
- R74E8 (e1b94b2) — the Q07Y5 redo. The drain budget starts at a oneshot fired
  from inside `with_graceful_shutdown` instead of wrapping the server's whole
  life. Verified three ways: injection (dies at 402ms), real release binary at
  the production 10s budget (81s, one pid), and the real LaunchAgent after
  reinstall (181s, one pid). The suite now has
  `daemon_serves_far_beyond_its_connection_drain_budget` — the only thing
  watching that class of defect.
- S0QRM (e1b94b2) — claude credential tier detected, not guessed. A detected
  native login beats an ambient ANTHROPIC_API_KEY.
- H0TYV — dec_Y5MPK item 6: orgasmic-attribution of a vendor transcript is
  *minted* (a NativeRuntime record naming the session id), never inferred from
  path shape or file age.
- ATAXN (688fd7b) — a departing daemon publishes `$ORGASMIC_HOME/daemon.shutdown`
  with its own `budget_ms`; a replacement waits on that budget instead of the
  125ms transient budget. `LockHolder` now says by what the lock is held and
  whether that thing is leaving.
- JQARS — `orgasmic manager drivers` exists, and `cli_parity` now resolves every
  `orgasmic ...` invocation quoted in shipped entry prose against clap.
- GT91X — tmux/rmux stamp `CODEX_INTERNAL_ORIGINATOR_OVERRIDE=orgasmic`, so
  codex reviewer transcripts are findable. **Verified live after the reinstall.**
- KKBTP — the credential decision is resolved once before ownership and pinned
  through acquire, composition and RunMeta. `auth status` is observed once per
  dispatch instead of three times; no subprocess is reachable from
  `compose_request` at all. dec_7P79C records the `Unknown` policy.

THE FINDER WAS BLIND TO ITS OWN REVIEWS
The Z8WEJ reviewer produced a substantial BLOCK and `orgasmic run
native-transcript` said the transcript did not exist. I read it by hand-parsing
`~/.codex/sessions/**` in Python. Cause: the finder required
`session_meta.originator == "orgasmic"`, a value **nothing in the codebase set** —
it appeared only in the finder and its own test fixtures, which is why its unit
tests passed against a production match of nothing. Fixed by GT91X, whose
fixtures now read the value back out of `harness_launch_env("codex")`, so
fixture and launch path cannot diverge again. If a worker's report is missing,
check `orgasmic run native-transcript` first, then the worker's scratchpad
`summary.md`/`report.md`, then the harness store directly.

TWO CLAIMS OF MINE THAT WORKERS DISPROVED — BOTH WORTH THE PATTERN
1. I filed GT91X saying the historical `originator: orgasmic` rollouts were
   editor sessions and that the mechanism had "stopped". Both false. Codex labels
   JSON-RPC app-server clients `source: vscode`; those were orgasmic's own acp-ws
   driver, via `clientInfo.name`, which never stopped and never covered the TUI.
   The gate was never satisfiable on the mux path rather than having regressed.
2. Earlier I reported that workers inherit the daemon's `$TMUX`. False — the pid
   I measured was a claude worker, because `pgrep -f "orgasmic.*daemon"` matches
   its *prompt text*. Get the daemon pid from `orgasmic status`.
Both corrections are recorded on the tasks. A wrong history sends the next
reader hunting a regression that never happened.

KNOWN LOAD FLAKES — CONFIRM, DO NOT ASSUME
Under concurrent load a full `cargo test -p orgasmic-daemon --lib` shows up to
five failures; all pass in isolation:
`poll_direct_child_pid_prefers_worker_server_over_generic_sibling`,
`recovery_reattaches_tmux_session_when_handle_exists`,
`recovery_inventory_waits_for_atomic_claim_commit`,
`production_resume_native_fork_uses_pinned_claude_not_path_shim`,
`git_resolves_quoted_included_origin_from_linked_worktree`.
Four of the five are on the recovery path — TASK-STWVB's strongest structural
clue, and not a "slow machine" story. TASK-5HBST (in flight) is fixing the
PATH-mutation one. **A suite with standing known flakes trains everyone to
discount red**, which is how a real regression gets waved through.

IN FLIGHT
- TASK-1T3FZ (reviewer, codex) — the destructive close path, merged without an
  independent review. Brief asks about reservation leaks on early return,
  `close_guard_id` reuse across daemon restarts, the `owner_pid` sweep under pid
  reuse, and whether a reservation can outlive the daemon that owns it now that
  ATAXN's marker exists.
- TASK-5HBST (implementer) — `PrependPathGuard` mutates process-global PATH with
  no shared lock.
- TASK-QRB8S (implementer) — `START_TIMEOUT` is a literal 20s while ATAXN made a
  40s start legitimate, so the CLI can report a failed start for a daemon that
  comes up fine.

NOT YET DISPATCHED
TASK-4DTBZ and TASK-QSX56 (both high), TASK-0RCRY (P1), TASK-95SGV (P1),
TASK-XPYRR (P1), TASK-BCYMM, TASK-37TAF, TASK-KPMFK, TASK-4PPPE, and five filed
by H0TYV: TASK-F9VEZ, TASK-B6D8W, TASK-2B215 (codex/cursor/hermes record no
NativeRuntime), TASK-ZY259, TASK-E3K1B.

A TASK TITLE CANNOT BE CORRECTED (TASK-XPYRR)
`task update` rejects `--title`; `node body set` only touches prose and named
sections. So TASK-0RCRY's heading still asserts the `$TMUX` diagnosis I retracted
this morning, and hand-editing `.orgasmic` is forbidden. Its body opens with the
correction, so anyone who opens it is set straight — but the board is wrong until
XPYRR lands.

WRITING TASK BODIES: `**` HEADINGS
`node body set` replaces free prose *between the drawer and the first nested
heading*. A body containing `** Section` headings is rejected or truncated —
ATAXN was filed with 300 of its 3959 characters for this reason. Use `===` for
sub-headings in a body you set through the CLI. Existing `**` sections created by
other paths are fine and are NOT returned in the JSON `description` field; read
the raw org node if you think content is missing.

RUN MANAGER COMMANDS FROM THE REPO ROOT
Bash cwd persists. From inside a dispatch worktree, `orgasmic manager ...`
resolves that worktree's frozen `.orgasmic` snapshot and reports stale state
confidently — `dispatch-status` returned **empty** once today and I briefly read
it as all three workers dying. `task update --property` from there returns a tx
id and silently does not apply. Daemon-routed writes with `--project orgasmic`
are safe; anything reading local files or the tx log is not.

CAPACITY, MEASURED
Three concurrent rmux workers is comfortable, four is the ceiling. The
LiveSessionGuard flock is shared across every test binary, so concurrent suites
serialize their live sections — a daemon suite that normally takes ~180s took
517s under three workers plus two parallel gates. It is not hung; check the
output file's mtime before concluding it is.

THE INSTALL, unchanged and still true
Locally built 0.0.15-dev.1. install.json names the stable channel and the
installer is equality-based, so `orgasmic update` would see stable advertising
0.0.14, treat it as a change, and revert the machine. Do not run it. Publishing
0.0.15 needs operator approval.

** Done so far
Session 2026-07-26, in order.

LANDED
- TASK-8W30B (d60c4ed, merge d3fae4d): RmuxControl::release aborted its lifecycle
  task before Session::kill, poisoning the shared ordered SDK transport, so every
  rmux run leaked a session, pty and harness process. Leak +8/run -> 0,
  manager-measured. Reviewer then BLOCKed it: cancellation consumed the only
  Session handle before a cancellable await, so Drop could not retry.
- TASK-6FNAY (83ea420, merge 075e017): the round-2 fix. as_ref() instead of
  take(), 2 s SDK + 2 s CLI reap budgets inside the caller's 5 s, an
  endpoint-exact `-S <endpoint> kill-session -t <name>` fallback, and a detached
  run_complete sender that survives cancellation.
- TASK-STWVB round 2 (67e427c, merge c578fcc): one OnceLock-owned warm executable
  per test binary, per-test variation moved into argv, hard links for
  production-shaped paths. Kept, because the mechanism is sound — but the gate is
  NOT met; see In flight.
- Prompt rules (902b240, 3f7121c): every dispatch report must name driver,
  harness, model and effort; and check `orgasmic <group> <verb> --help` before
  inventing a verb. The second was written because a shipped convention taught
  `orgasmic manager drivers`, which has never existed.

MEASURED, and worth not re-deriving
- macOS charges 150-270 ms of Gatekeeper evaluation on the FIRST exec of each
  newly created executable, through one serialized daemon. Warm exec 4 ms. A hard
  link to a warmed file is also 4 ms (the cache key is the inode); a byte-
  identical copy pays 162 ms. And `/bin/sh script` on a fresh file is 5 ms
  against 270 ms for `./script` — the simplest sidestep, and the one to use next.
- The remaining suite pressure is in the rmux test path, not in the converted
  fixtures: syspolicyd burns +281 s of CPU during a 106 s run with rmux present
  and +32 s with it absent. What it is exec-ing has NOT been identified. The rmux
  binary itself is cheap (9.2 ms, 0 ms syspolicyd).

BOARD REPAIR
In Review went 5 -> 3. TJKFC (P0, partial implementation) and STWVB moved back to
in_progress; Z8WEJ and VB9DQ stay in review with their real blocker recorded —
they need a live claude dispatch on a daemon carrying today's main.

CLI DEFECTS FOUND WHILE WORKING, now understood correctly
`--property KEY=VALUE` writes any key verbatim and the schema reads only a fixed
UPPERCASE set (schema.rs:276-286), matched case-SENSITIVELY. So `write_scope`,
`test_cmd` and `priority` in lowercase are stored where nothing reads them, and
the response echoes them back as changed. The earlier note in this session that
uppercase WRITE_SCOPE was "silently dropped" was wrong — uppercase is the form
that works. `task create` has no --priority flag at all, which is how all seven
tasks filed today came out priority None. Tracked on TASK-HXSW0.

** Next likely actions
1. Read each worker's report. There is no dispatch-report command: try
   `orgasmic run native-transcript <run_id>` first, then the worker's
   scratchpad summary.md/report.md, then the harness store directly.
2. **Re-run each injection yourself before merging.** Check your probe is
   well-formed before concluding a gate is broken — one of mine was not today.
3. On the 1T3FZ review: SHIP closes it; BLOCK means file the fix round and leave
   it open with BLOCKED_BY set. Do not close a task before deciding whether it
   needs a review round — the state machine refuses a reviewer dispatch from
   done, which cost a retarget today.
4. Keep three workers saturated. Remaining: 4DTBZ and QSX56 (high), then 0RCRY,
   95SGV, XPYRR, 4PPPE, and H0TYV's five NativeRuntime tasks.
5. Reinstall the runtime only in a zero-worker window. It is current as of
   dc6db85; there is no pending reason to reinstall until something merges.

** In flight
Three workers, dispatched 2026-07-28 17:18-17:20Z.

- TASK-1T3FZ — reviewer, codex/gpt-5.6-sol/high, tx-20260728-orgasmic-3371.
  The destructive close path (dispatch-close can remove a worktree and a branch)
  was merged and manager-verified but never independently reviewed. The brief
  asks it to attack reservation leaks on early return, close_guard_id reuse
  across daemon restarts, the owner_pid sweep under pid reuse, whether
  'undetermined refuses' can wedge an operator with no way out that is not
  hand-editing state, and whether a reservation can outlive its daemon now that
  ATAXN publishes a shutdown marker.
- TASK-5HBST — implementer, claude/opus-5/high, tx-20260728-orgasmic-3379.
  PrependPathGuard mutates process-global PATH with no shared lock. Acceptance
  is 10 consecutive clean full runs, reported as a count.
- TASK-QRB8S — implementer, claude/opus-5/high, tx-20260728-orgasmic-3382.
  START_TIMEOUT is the last literal on the shutdown-budget axis.

** Divergence / Risks
The destructive-path precaution recorded earlier today is RETIRED: TASK-AK6EM
landed and TASK-1T3FZ is closed. Closing a dispatch no longer depends on the
manager remembering to check [run-gone] first, and a daemon replacement mid-close
now inherits the guard. Both are enforced in code, and the lease barrier is
compiler-enforced.

REMAINING RISK ON THIS PATH, NAMED BY THE IMPLEMENTER RATHER THAN HIDDEN
A future path could still insert a `RunRecord` with no lease. It would break
unrelated invariants immediately (release removes the lease by record;
`blocking_run_for_close` treats a lease with no record as blocking), so it is
not silent — but it is not type-enforced the way the lease itself now is. Worth a
task if anyone adds a third run constructor.

WINDOWS RECLAMATION IS IMPLEMENTED BUT ONE BRANCH IS UNEXERCISED HERE
Non-Unix guards are lease-governed and the reclamation logic is tested directly
on this host; what could not be exercised is the `cfg!(unix)` selection itself
on a Windows daemon. The implementer said so plainly instead of faking a test.
** Description
(superseded — this section was created in error; the handoff is the free prose above)
