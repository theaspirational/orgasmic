#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         db071733bf3bf95e275d6d1314c31b277a5441dd
:LIVENESS_AT:      [2026-07-26 Sun]
:UPDATED_AT:       [2026-07-26 Sun]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-07-28 (manager-direct + sixteen dispatches). Seven rounds merged,
every one verified by injection run by the manager, not taken on the worker's word.

THE ONE THING TO KNOW
On this project a green suite is not evidence. TASK-Q07Y5 shipped with every
gate green — CLI, daemon, and the manager's own — and killed the operator's
daemon 10.00s after every boot, because no test anywhere ran a daemon for
longer than ten seconds. The standard that came out of it and now holds: for
every fix, reinject the defect, show the test going red with its output, restore,
show it green. Ask a worker what its test would do if the fix were reverted and
expect an answer with an exit code in it.

R74E8: THE OUTAGE IS CLOSED, AND VERIFIED WHERE IT HAPPENED
TASK-Q07Y5 wrapped `axum::serve(..).with_graceful_shutdown(..)` in the 10s drain
budget. That future is the server's whole life, not the drain. Reverted `0ac88d7`,
redone correctly as TASK-R74E8 (merged `e1b94b2`): a oneshot fires from inside
`with_graceful_shutdown` the moment shutdown is signalled, the outer task awaits
that unbounded — it is the wait *for* the signal — and only then applies
`timeout(connection_drain, serve_task)`.

Verified three ways, each stronger than the last:
- Manager injection: delete the `drain_started_rx.await` and the new test
  `daemon_serves_far_beyond_its_connection_drain_budget` fails at 402ms of uptime.
- Real release binary, production 10s budget, throwaway home: one pid, one
  boot_id across 81s, zero drain errors. Q07Y5 errored at exactly t+10.00s.
- **The real LaunchAgent**, after reinstall: one pid, one boot_id across 181s =
  18x the budget. This is the check Q07Y5 never got.

STATE OF THE INSTALL — REINSTALLED AND CURRENT
`~/.orgasmic/bin/orgasmic` is 0.0.15-dev.1 built from `b85c3ae`, signed with
`ORGASMIC_CODESIGN_IDENTITY='orgasmic dev codesign'`, designated requirement
byte-identical to the incumbent's (certificate root a70531007c…), so TCC grants
carried. Installed by fresh-inode rename; `shipped/` rsynced. **ExitTimeOut is
the derived 50 again.** Recipe that works, in order: set the codesign identity or
`package-runtime.sh` silently ships unsigned; verify the DR matches the incumbent;
copy to `orgasmic.new`, `codesign -v`, `mv -f`; restart from the INSTALLED binary.
Reinstall only when zero workers are running — a restart cycles the rmux
LaunchAgent and destroys every live session.

WHAT ELSE LANDED TODAY
- R2HDN (5d4651a) — a live test now fails when acquisition degrades to inert;
  the missing per-binary sentinel added.
- 1T3FZ (68f3ed6) — destructive close holds a daemon-owned worktree reservation
  installed under the supervisor lock BEFORE the verdict, so the verdict is monotone.
- 69CW6 (03f4d05) — self-satisfying reap surrogate deleted, replaced by
  StallableRmuxEndpoint driving the real RmuxControl through Supervisor::release.
- R74E8 (e1b94b2) — above.
- S0QRM (e1b94b2) — claude credential tier is now *detected*, not guessed. A
  detected native login beats an ambient ANTHROPIC_API_KEY; explicit
  `credential_mode` wins outright; `--credential-mode` is live on the CLI and
  persisted in RunMeta. Native mode adds `--safe-mode` and blanks an inherited key.
  Manager-verified: injecting the pre-fix precedence turns the regression red
  (`left: BareApiKey, right: NativeLogin`).
- H0TYV (merged) — dec_Y5MPK item 6 rules that orgasmic-attribution of a vendor
  transcript is *minted* (a NativeRuntime record naming the session id), never
  inferred from path shape or file age. The session-id regression now compares
  against `ctx.identity.runtime_id` at both adapter and mode layers — manager
  injection with a valid-but-different UUID turns both red, which the old
  assertions could not see.

THE DIAGNOSIS I GOT WRONG, AND HOW
I reported that workers inherit the daemon's $TMUX. False. The pid I measured was
a claude worker — `pgrep -f "orgasmic.*daemon"` matched its *prompt text*. The
real daemon carries no TMUX. Get the daemon pid from `orgasmic status`, never
from a pgrep pattern that can match a prompt. TASK-0RCRY survives on the one real
fact: no tmux call site passes -L/-S, so every probe and session shares one server.

KNOWN LOAD FLAKES — CONFIRM, DO NOT ASSUME
A full `cargo test -p orgasmic-daemon --lib` under concurrent load shows up to
five failures. All five pass in isolation:
`poll_direct_child_pid_prefers_worker_server_over_generic_sibling`,
`recovery_reattaches_tmux_session_when_handle_exists`,
`recovery_inventory_waits_for_atomic_claim_commit`,
`production_resume_native_fork_uses_pinned_claude_not_path_shim`,
`git_resolves_quoted_included_origin_from_linked_worktree`.
Four of the five are on the recovery path — that concentration is TASK-STWVB's
strongest structural clue and is not a "slow machine" story. The PATH-shim one is
TASK-5HBST.

A KILLED DISPATCH LEAVES ITS BRANCH BEHIND
Re-dispatch then fails with "a branch named task-<id>-<kind> already exists".
`git branch -D` plus `git worktree prune` clears it. Hit again today on
`task-z8wej-review`. Worth its own task.

TWO PROCESS MISTAKES WORTH INHERITING
1. At 16:01 during the incident I saw the restarts, read `runs = 1` / `never
   exited` as "a managed reload, not a crash", and filed it as a benign
   observation. That was the outage already running.
2. I closed TASK-S0QRM as done before deciding whether it needed a review round,
   and the state machine correctly refused a reviewer dispatch from `done`.
   Decide the review question *before* closing. The review went to the parent
   TASK-Z8WEJ instead, which is the right target anyway — the parent is what
   closes on SHIP.

IN FLIGHT
- TASK-Z8WEJ (reviewer, codex) — reviewing S0QRM's credential change as merged.
  The brief flags rule 4 (`Unknown` treated as `Absent`, so detection that times
  out under load makes the mode nondeterministic), the deliberately deleted
  assertion, key neutralisation, and whether `--safe-mode` breaks any worker that
  relies on a skill or plugin.
- TASK-ATAXN (P0, implementer) — lock retry budget is 125ms, sized for a
  transient CLI probe, while a shutting-down daemon can hold the same lock for
  up to the derived 40s. Reproduce first; the analysis was never observed end to
  end. Distinguish the two holders rather than raising one number.
- TASK-JQARS (implementer) — `manager drivers` is documented and does not exist.

NOT YET DISPATCHED
TASK-SGRTX (P0, dec_S18RH), TASK-0RCRY (P1), TASK-5HBST, TASK-95SGV, TASK-BCYMM,
TASK-37TAF, TASK-KPMFK, TASK-4DTBZ (P0), TASK-QSX56 (P0), and five filed by H0TYV:
TASK-F9VEZ, TASK-B6D8W, TASK-2B215 (codex/cursor/hermes record no NativeRuntime),
TASK-ZY259 (codex sends persistExtendedHistory:false, uncommented and untested),
TASK-E3K1B (doctor has no vendor-transcript inventory, which dec_Y5MPK items 5
and 6 name and nothing implements).

STILL OPEN
TASK-TJKFC, TASK-STWVB, TASK-WGXKD and TASK-WGXKD.1 (BLOCKED_BY was R74E8, now
landed — recheck whether they close), TASK-WGXKD.2 (todo).

CAPACITY, MEASURED
Three concurrent rmux workers is comfortable, four is the ceiling (load 28 on 10
cores, ~4 GB of worktree target dir each). The LiveSessionGuard flock is shared
across every test binary, so concurrent suites serialize their live sections — a
daemon suite that normally takes ~180s took 517s under three workers plus two
parallel gates. It is not hung; check the output file's mtime before concluding
it is.

THE INSTALL, unchanged and still true
Locally built 0.0.15-dev.1. install.json names the stable channel and the
installer is equality-based, so `orgasmic update` would see stable advertising
0.0.14, treat it as a change, and revert the machine. Do not run it. Publishing
0.0.15 needs operator approval.

** Done so far
Session 2026-07-26, in order.

LANDED
- TASK-8W30B (d60c4ed, merge d3fae4d): RmuxControl::release aborted its lifecycle
  task before Session::kill, poisoning the shared ordered SDK transport, so every
  rmux run leaked a session, pty and harness process. Leak +8/run -> 0,
  manager-measured. Reviewer then BLOCKed it: cancellation consumed the only
  Session handle before a cancellable await, so Drop could not retry.
- TASK-6FNAY (83ea420, merge 075e017): the round-2 fix. as_ref() instead of
  take(), 2 s SDK + 2 s CLI reap budgets inside the caller's 5 s, an
  endpoint-exact `-S <endpoint> kill-session -t <name>` fallback, and a detached
  run_complete sender that survives cancellation.
- TASK-STWVB round 2 (67e427c, merge c578fcc): one OnceLock-owned warm executable
  per test binary, per-test variation moved into argv, hard links for
  production-shaped paths. Kept, because the mechanism is sound — but the gate is
  NOT met; see In flight.
- Prompt rules (902b240, 3f7121c): every dispatch report must name driver,
  harness, model and effort; and check `orgasmic <group> <verb> --help` before
  inventing a verb. The second was written because a shipped convention taught
  `orgasmic manager drivers`, which has never existed.

MEASURED, and worth not re-deriving
- macOS charges 150-270 ms of Gatekeeper evaluation on the FIRST exec of each
  newly created executable, through one serialized daemon. Warm exec 4 ms. A hard
  link to a warmed file is also 4 ms (the cache key is the inode); a byte-
  identical copy pays 162 ms. And `/bin/sh script` on a fresh file is 5 ms
  against 270 ms for `./script` — the simplest sidestep, and the one to use next.
- The remaining suite pressure is in the rmux test path, not in the converted
  fixtures: syspolicyd burns +281 s of CPU during a 106 s run with rmux present
  and +32 s with it absent. What it is exec-ing has NOT been identified. The rmux
  binary itself is cheap (9.2 ms, 0 ms syspolicyd).

BOARD REPAIR
In Review went 5 -> 3. TJKFC (P0, partial implementation) and STWVB moved back to
in_progress; Z8WEJ and VB9DQ stay in review with their real blocker recorded —
they need a live claude dispatch on a daemon carrying today's main.

CLI DEFECTS FOUND WHILE WORKING, now understood correctly
`--property KEY=VALUE` writes any key verbatim and the schema reads only a fixed
UPPERCASE set (schema.rs:276-286), matched case-SENSITIVELY. So `write_scope`,
`test_cmd` and `priority` in lowercase are stored where nothing reads them, and
the response echoes them back as changed. The earlier note in this session that
uppercase WRITE_SCOPE was "silently dropped" was wrong — uppercase is the form
that works. `task create` has no --priority flag at all, which is how all seven
tasks filed today came out priority None. Tracked on TASK-HXSW0.

** Next likely actions
1. Read each in-flight worker's summary.md via `orgasmic run native-transcript <run_id>`
   (there is no dispatch-report command; the report is a Write to the worker's
   scratchpad summary.md). Verify by injection before merging — reinject the
   defect yourself, do not accept the worker's transcript of having done it.
2. On Z8WEJ SHIP: close Z8WEJ. On BLOCK: file the fix round, do not close.
3. Recheck TASK-WGXKD and TASK-WGXKD.1 — both were BLOCKED_BY=TASK-R74E8, which
   has now landed and is verified live. They may close; confirm against git
   rather than assuming, since an earlier claim that this family was fully
   closed was wrong.
4. Keep three workers saturated. Remaining P0s: SGRTX (dec_S18RH), 4DTBZ, QSX56.
   Then 0RCRY (P1), 5HBST, 95SGV, BCYMM, 37TAF, KPMFK, and H0TYV's five.
5. Do not reinstall the runtime while workers are live — a restart cycles the
   rmux LaunchAgent and destroys every session. The install is current as of
   b85c3ae; there is no pending reason to reinstall.

** In flight
Three workers, dispatched 2026-07-28 15:36-15:38Z, all [run-live].

- TASK-Z8WEJ — reviewer, codex/gpt-5.6-sol/high, tx-20260728-orgasmic-3305.
  Reviewing S0QRM's credential change as merged on main. The brief asks it to
  attack four things specifically: rule 4 treats `Unknown` (detection
  impossible) the same as `Absent`, so a detection that times out under load
  makes the credential mode nondeterministic across dispatches of the same task;
  the deliberately deleted `api_key_mode_keeps_the_light_bare_path` assertion;
  whether a blank ANTHROPIC_API_KEY really reads as 'no key'; and whether
  `--safe-mode` silently breaks any worker that relies on a skill or plugin.
  Z8WEJ closes only on SHIP.
- TASK-ATAXN — implementer, claude/opus-5/high, tx-20260728-orgasmic-3311. P0.
  Reproduce first: the analysis came from a residual-risk section and was never
  observed end to end. If it does not reproduce, that is the result.
- TASK-JQARS — implementer, claude/opus-5/high, tx-20260728-orgasmic-3314.
  `manager drivers` is documented and does not exist. Brief carries ten
  first-hand instances of the same class collected today.

** Divergence / Risks
- WORKER TEST CLAIMS ON THE DAEMON CRATE ARE NOT EVIDENCE until TASK-RRT4T lands.
  Three over-reports today, one mechanism. Re-run the scoped test yourself and
  verify a side effect that proves it executed.
- The daemon suite is not green: post_artifact_regenerate_hot_path (now 409
  "harness busy") and poll_direct_child_pid (30 s deadline) fail in-suite and pass
  alone; recovery_inventory fails about 1 run in 3.
- 0.0.15 is unpublished and the running daemon predates every fix from today.
  Do not run `orgasmic update` — it would revert the machine to 0.0.14.
- There is still no CI, so every green claim is one local run on one machine.
  This is the root of the "chasing issues while fixing issues" feeling the
  operator named: without a shared gate, each fix is verified once, by whoever
  happened to run it, in an environment nobody else can reproduce. TASK-S2KM0.
- A live dispatch remains the only acceptable evidence for driver argv changes.
  Every defect found in the last two days had a passing test standing over it.
- TASK-VB9DQ and TASK-Z8WEJ sit in in_review with Evidence written against
  pre-SGRTX behaviour. VB9DQ's "not verified end to end" is stale in the opposite
  direction: it IS verified, but only after SGRTX. Re-read both before closing.
- Superseded and retained so it is not repeated: an earlier note in this handoff
  claimed `orgasmic run release` does not exist by omission and that `run list`
  reported 0 live. The first is by design (release is the daemon's job; the
  manager's surface is `orgasmic run recover`, and `manager dispatch-close`
  releases the run when the record carries a run_id, manager.rs:627). The second
  was simply false — the run was live for about 80 minutes and was cleared by a
  daemon restart, which cost about a minute of downtime and required
  `rmux kill-server` first because a stale rmux-daemon owned the socket.
** Description
(superseded — this section was created in error; the handoff is the free prose above)
