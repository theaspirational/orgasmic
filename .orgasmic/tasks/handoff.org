#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         4eb3f4679a33ab9aade00a8e78550f3bb47ebd7a
:LIVENESS_AT:      [2026-08-05 Wed]
:UPDATED_AT:       [2026-08-05 Wed]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-03/05, continuing the 08-02 session. Three merges landed and
were reviewed, the architecture-layer excision was decided and started, and two
recorded "fixes" were disproved by measurement before anyone built them.

THE FOUR THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE INSTALLED RUNTIME IS 36 COMMITS BEHIND MAIN, AND THAT IS NOW A TRAP WITH
   TEETH. =~/.orgasmic/bin/orgasmic= runs =05e1813= (built 2026-08-03T03:19Z).
   TASK-XCJYC renamed the transport modes on main — =acp-stdio= -> =stdio=,
   =acp-ws= -> =ws= — but YOUR CLI STILL ONLY ACCEPTS THE OLD NAMES. Verified:
   =orgasmic manager drivers= prints =acp-stdio=/=acp-ws=. So dispatch with
   =--mode acp-stdio=, NOT =--mode stdio=, until the runtime is rebuilt, even
   though the repo says otherwise. =--mode tmux= and =--mode rmux= are
   unaffected and are what this session actually used.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. The safe pattern is =ORGASMIC_DAEMON_URL=
   on the CHILD. =env -u ORGASMIC_HOME= to strip a leaked value is the opposite
   and is fine. TASK-48SFW carries the source chain. Note the asymmetry recorded
   there: BRIEFS keep the blunt blanket ban, because a worker cannot tell which
   verbs open a connection and the failure is total and silent; the CODE fix
   targets the precise rule at =ensure_running=.

3. A PASSING GATE PROVES LESS THAN IT LOOKS, IN TWO SPECIFIC WAYS.
   - =orgasmic verify=: a pass proves the red HAPPENS, not that it is the RIGHT
     red. For a RE-CUT artifact, read the patch and confirm the pinned message is
     the FIRST failing assertion.
   - The daemon suite has NO TRUSTWORTHY FULL-RUN SIGNAL on this host. Under
     load =syspolicyd= bursts (measured 586% CPU, load 11.41, suite 8-15x
     slower); the 2026-08-02 run showed 14 failures that ALL passed alone in 8s
     against the same binary. The cheap control is to re-run each failure alone
     against the ALREADY-BUILT binary before believing any red:
     =env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME target/debug/deps/orgasmic_daemon-<hash> --exact <test> --test-threads=1=
     TEST EXECUTION is the trigger, not compilation. This machine has hard-
     rebooted twice from that chain, so DO NOT loop the full workspace suite.

4. TWO RECORDED DECISIONS ON THIS BOARD WERE DISPROVED BEFORE BEING BUILT, and
   both are on TASK-3X5AQ with reproductions. Do not implement either.
   - SHARED =CARGO_TARGET_DIR= IS UNSOUND: two checkouts share one cargo
     fingerprint dir, so a worker's =cargo build= reports =Fresh= without
     compiling and =CARGO_BIN_EXE_*= hands its integration tests ANOTHER
     CHECKOUT'S BINARY — a green suite that never ran your code.
   - STABLE WORKTREE SLOTS DO NOT FIX THE TCC PROMPTS: a grant for an
     ad-hoc-signed binary is pinned to its CDHASH, and a rebuild at the SAME
     path changes it (measured 16237ab -> 6cdc20d). Slots would move the prompt
     from once-per-dispatch to once-per-BUILD.
   The surviving fix is TASK-M47E5: move worktrees OUT of TCC-guarded dirs.

** Done so far
SESSION 2026-08-05 (this handoff's session), continuing 08-03/05. Two merges, both
reviewed to a verdict; three tasks filed from things the work uncovered; 108 dead
branches pruned.

MERGED
- =bb2fb24= TASK-YN5FJ.1 — =dispatch-close --verdict <approve|approve-with-follow-ups|reject>=.
  The default-branch gate demanded a =VERDICT= that only =--property VERDICT== could
  set, so the refusal named a remedy that did not clear the refusal. Three manager
  rulings, all carried in code comments: ANY verdict clears the gate including
  =reject= (the gate asks whether review happened, not whether it approved — and a
  reject that forced =--no-review-required= would stamp a bypass on a dispatch that
  WAS reviewed); the typed set is a strict SUPERSET, so =clean=/=ship=/=has-issues=
  still work through =--property=; passing both spellings is a hard error, not
  last-wins. Reviewed REJECT, fixed, re-reviewed APPROVE.
- =14b9660= TASK-YN5FJ.1.1 — the fix round for that REJECT. The refusal now prints
  =orgasmic manager dispatch-close --task <real id> …= with the executable and
  every known token as its real value, and names its own placeholder set in prose.
  The test now EARNS the word verbatim: it extracts the command from the refusal
  that run produced, substitutes exactly two documented placeholders, ASSERTS every
  other token is free of =<=/=>= (so a message that leaves a placeholder for
  something it knew fails the MESSAGE), and execs =argv[0]= by name off PATH.
  APPROVE, no findings.

TWO PROOFS SHIP FOR YN5FJ.1, AND THE SECOND IS THE INTERESTING ONE.
=verify/TASK-YN5FJ.1-remedy/= removes ONLY the line that writes the flag's value:
the flag still parses, =--help= still documents it, the refusal still prints the
corrected remedy — and the loop still fails. That is a direct proof that a
prose-only fix would not have been a fix.

THE RE-CUT WAS VERIFIED BEYOND ITS GREEN REPLAY. =TASK-YN5FJ.1/injection.patch=
had to be re-cut (whole-file revert of the region the fix round edits). Manager
checked independently: applies clean, injected =manager.rs= BYTE-IDENTICAL to
=bb2fb24^1=, and the red log carries exactly ONE panic — the pinned label, quoting
the pinned pre-fix text. A green =orgasmic verify= alone could not have told you any
of that.

FILED FROM WHAT THE WORK UNCOVERED
- TASK-4WKNX (P2) — =:FIX_SUBTASK: t= closes straight to =done=, and a reviewer
  dispatch is REFUSED from =done=; but the active goal requires a BLOCK SHIP fix
  round to pass a fresh review. The two rules point opposite ways and the only way
  through today is a manual =done= -> =in_review= flip. Hit live, not theorised.
- TASK-37WN7 (P3) — =sigterm_exits_through_graceful_shutdown…= still flakes under
  load DESPITE the TASK-Q07Y5 mitigation built for exactly that race. So it is not
  an unmitigated race; it is a mitigation that does not hold. Deliberately NOT
  registered in =flake-registry.toml=: no panic signature was captured, and a
  signature-less entry makes every future failure of that test name read as this
  known flake.
- TASK-M47E5 was dispatched, not filed — see In flight.

HOUSEKEEPING: 108 merged =task-*-impl= branches deleted with =git branch -d= (the
safe form; every commit stays reachable from main). =git branch= is useful again.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. There is no
project default and nothing enforces resolution (=--model= > task drawer > harness
default, and the harness default wins SILENTLY — TASK-A55J7). THIS session's
answers, which are NOT a standing setting: implementers =tmux / claude /
claude-opus-5 / high=, reviewers =tmux / codex / gpt-5.6-sol / high=. Pass =--model=
explicitly and echo all four back after each dispatch.

TRANSPORT: tmux, and this is now settled rather than a coin-flip. Both properties
read against the INSTALLED daemon: pane liveness from TASK-4CSMY is confirmed
reporting in production, so no =stall_timeout_secs= override is needed; and rmux
fails the survival property (TASK-4DTBZ — it killed three workers in 24ms last
session). Four dispatches ran on tmux this session with no transport trouble.

TRAP #1 STILL HOLDS: dispatch with =--mode acp-stdio=, NOT =--mode stdio=, until
the runtime is rebuilt. Verified again this session against =orgasmic manager
drivers=. =--mode tmux= is unaffected and is what every dispatch used.

A NOTE ON CLOSING A REVIEWER, learned in anger this session. The installed runtime
predates =--verdict=, and its =reviewer_done_stage= does not know the word
=approve= — it maps only =clean=/=ship= to done. So on TODAY's runtime, close a
reviewer with =--property VERDICT=ship=, not =approve=, or the task lands
=in_progress= and you will not notice. After the reinstall, use the flag. This is
the compat path RULING 2 preserved, being used for real.

AND EXPECT THE FIX-ROUND FLIP until TASK-4WKNX lands: closing a =:FIX_SUBTASK: t=
implementer sends the task to =done=, and the reviewer dispatch the goal requires is
then REFUSED from =done=. Flip =done= -> =in_review= with =task update --state= and
say in =--reason= that it is policy, not a correction.

** In flight
ONE DISPATCH RUNNING: TASK-M47E5 (P1) implementer, =implementer-claude-tmux=
(tmux / claude / claude-opus-5 / high), run
=run-20260805T140235-4e4f74ec8c154ad48115bf43d5bbc97a=, started_tx
=tx-20260805-orgasmic-4458=, base =103ed7e=. Report lands at
=.orgasmic/tmp/dispatch/task-m47e5/task-m47e5-542237f0a9b0458d848ba5162e98c562-last.txt=.
If you are a fresh manager and that file is non-empty, integrate it; if the tmux
pane is gone and it is EMPTY, treat the run as orphaned and read =rescue-policy=
before assuming anything completed.

THE MANAGER RULING THAT DISPATCH CARRIES, so you do not re-open it: reclamation is
an EXPLICIT VERB, detection is AUTOMATIC. Deleting a worktree is the sharpest tool
in this codebase (TASK-2BPWM and TASK-D0GA3 exist because a worker's uncommitted
work was destroyed once), so nothing gains authority to delete unattended — but a
verb nobody runs is exactly how invisible garbage accumulates, which is why Part 2
is a requirement of Part 1 rather than a follow-up. Separate the authority from the
visibility.

AFTER M47E5, THE OBVIOUS ORDER
1. TASK-RQ270 stages B-E — the architecture excision. Stage B (drift comparator,
   ~-550 LOC) is cheapest and independent. Mint =TASK-RQ270.N= per round the way
   stage A was done. ONE LOAD-BEARING CONSTRAINT: the dangling-edge lint carve-out
   lands in stage C and must be on main BEFORE stage D deletes =architecture.org=,
   or done.org's 231 =:IMPLEMENTS: arch= tokens start failing the lint.
2. TASK-4WKNX (P2) — resolve the FIX_SUBTASK/review contradiction. Small, and it
   removes a manual backwards state transition from every fix round.
3. TASK-STWVB (P2) — the suite's full-run signal. Design is on the task; A and B/C
   are separable rounds.
4. TASK-37WN7 (P3) — capture the flake's panic, then fix or register it.
5. Still P1 and untouched: TASK-4DTBZ, TASK-BX5SR, TASK-A55J7, TASK-48SFW,
   TASK-ADHWZ, TASK-NTNK0.

THE RUNTIME REINSTALL IS NOW UNBLOCKED. TASK-YN5FJ.1 and its fix round have landed,
which was the stated precondition. The recipe and its four traps are in the
=orgasmic-runtime-reinstall= memory: a zero-worker window is mandatory, =install.sh=
silently skips the unpack if the version dir exists, its trailing =orgasmic status=
auto-spawns a competing daemon (=ORGASMIC_INSTALL_SKIP_STATUS=1=), and you MUST
codesign or the binary loses its TCC grant. Verify BEHAVIOURALLY — both builds call
themselves =0.0.15-dev.1=. Two good new-code probes now: =--mode stdio= being
accepted, and =dispatch-close --verdict= existing.

BOARD: IN_PROGRESS = TASK-M47E5. TODO = TASK-STWVB. IN_REVIEW = empty.
BACKLOG = 87 (86 + TASK-4WKNX + TASK-37WN7 - M47E5 - the two YN5FJ items done).
=parse_errors= 0. Nothing pushed; main is 500+ commits ahead of origin and pushing
is an operator decision, never a manager one.

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO AGAIN — this REVERSES the previous handoff, which
said "THE RUNTIME IS NOW CURRENT". Installed =05e1813=, main is 36 commits
ahead. See trap #1 in the lead: the mode rename is the part that bites.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item
2). Unchanged, and it is the single most expensive mistake available.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1): three pid
cycles in one session, no log since 2026-07-26, no crash report; it killed three
workers at once. TMUX IS THE BETTER DEFAULT and is what this session used
throughout. The tmux pane-liveness channel from TASK-4CSMY is now CONFIRMED
REPORTING IN PRODUCTION — measured on the TASK-XCJYC run,
=driver_event type "pane_activity"= with real byte counts — so the
=--governance-json {"stall_timeout_secs":3600}= workaround has been RETIRED
deliberately and later dispatches ran without it. Read =event.type=, never the
serialized line: an earlier grep on the same file matched =driver_config= and
produced a false positive.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). One
failing transaction killed the writer task; every write then returned 500 while
=status=, =task get= and =dispatch-status= all answered normally. Detection is
=grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention
— that was a previous manager's first and wrong reading.

MERGE STILL PRECEDES REVIEW IN PRACTICE, AND THE FIX IS MERGED BUT NOT LIVE.
=dispatch-close= refuses to close an implementer as done without =--merge-sha=,
and until the runtime is rebuilt a reviewer still cannot dispatch against an
open generation. So expect to merge, then review, then fix in =TASK-<id>.N=
rounds — which is what this session did three times. When you review a
post-merge diff, say so in the brief and tell the reviewer a REJECT is still
actionable, or you will get softened findings.

TWO REVIEW-BRIEFING LESSONS, both paid for this session.
- DO NOT ask a reviewer to mutate source. The reviewer contract is strictly
  read-only, so a brief asking for a negative probe ("break it and confirm the
  guard fires") returns an unrun gap, correctly. The IMPLEMENTER runs negative
  probes; the reviewer judges them.
- A REJECT that the manager then fixes personally is a normal, good outcome. It
  happened on TASK-XCJYC and cost three comment lines.

STATE-HYGIENE SHAPES TO WATCH FOR, both found this session:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  TASK-STWVB sat that way for days. Check
  =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a stage.
- A BLOCKER RECORDED ONLY IN PROSE is invisible. TASK-STWVB said "GATE IS
  BLOCKED BY TASK-8W30B" in its body while =depends_on= was empty.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED will mislead every
  skim-reader. This cost a worker a full round on TASK-TJKFC. The fix that works
  is a =READ THIS FIRST — STATUS= block at the TOP plus a corrected TITLE;
  appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural — the exact shape of the comment
that caused the 2026-07-30 orphan incident. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6);
it spawns real billed provider turns.
