#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         f60af0a
:LIVENESS_AT:      [2026-08-05 Wed]
:UPDATED_AT:       [2026-08-05 Wed]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-03/05, continuing the 08-02 session. Three merges landed and
were reviewed, the architecture-layer excision was decided and started, and two
recorded "fixes" were disproved by measurement before anyone built them.

THE FOUR THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE INSTALLED RUNTIME IS 36 COMMITS BEHIND MAIN, AND THAT IS NOW A TRAP WITH
   TEETH. =~/.orgasmic/bin/orgasmic= runs =05e1813= (built 2026-08-03T03:19Z).
   TASK-XCJYC renamed the transport modes on main — =acp-stdio= -> =stdio=,
   =acp-ws= -> =ws= — but YOUR CLI STILL ONLY ACCEPTS THE OLD NAMES. Verified:
   =orgasmic manager drivers= prints =acp-stdio=/=acp-ws=. So dispatch with
   =--mode acp-stdio=, NOT =--mode stdio=, until the runtime is rebuilt, even
   though the repo says otherwise. =--mode tmux= and =--mode rmux= are
   unaffected and are what this session actually used.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. The safe pattern is =ORGASMIC_DAEMON_URL=
   on the CHILD. =env -u ORGASMIC_HOME= to strip a leaked value is the opposite
   and is fine. TASK-48SFW carries the source chain. Note the asymmetry recorded
   there: BRIEFS keep the blunt blanket ban, because a worker cannot tell which
   verbs open a connection and the failure is total and silent; the CODE fix
   targets the precise rule at =ensure_running=.

3. A PASSING GATE PROVES LESS THAN IT LOOKS, IN TWO SPECIFIC WAYS.
   - =orgasmic verify=: a pass proves the red HAPPENS, not that it is the RIGHT
     red. For a RE-CUT artifact, read the patch and confirm the pinned message is
     the FIRST failing assertion.
   - The daemon suite has NO TRUSTWORTHY FULL-RUN SIGNAL on this host. Under
     load =syspolicyd= bursts (measured 586% CPU, load 11.41, suite 8-15x
     slower); the 2026-08-02 run showed 14 failures that ALL passed alone in 8s
     against the same binary. The cheap control is to re-run each failure alone
     against the ALREADY-BUILT binary before believing any red:
     =env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME target/debug/deps/orgasmic_daemon-<hash> --exact <test> --test-threads=1=
     TEST EXECUTION is the trigger, not compilation. This machine has hard-
     rebooted twice from that chain, so DO NOT loop the full workspace suite.

4. TWO RECORDED DECISIONS ON THIS BOARD WERE DISPROVED BEFORE BEING BUILT, and
   both are on TASK-3X5AQ with reproductions. Do not implement either.
   - SHARED =CARGO_TARGET_DIR= IS UNSOUND: two checkouts share one cargo
     fingerprint dir, so a worker's =cargo build= reports =Fresh= without
     compiling and =CARGO_BIN_EXE_*= hands its integration tests ANOTHER
     CHECKOUT'S BINARY — a green suite that never ran your code.
   - STABLE WORKTREE SLOTS DO NOT FIX THE TCC PROMPTS: a grant for an
     ad-hoc-signed binary is pinned to its CDHASH, and a rebuild at the SAME
     path changes it (measured 16237ab -> 6cdc20d). Slots would move the prompt
     from once-per-dispatch to once-per-BUILD.
   The surviving fix is TASK-M47E5: move worktrees OUT of TCC-guarded dirs.

** Done so far
SESSION 2026-08-05, continuing 08-02/03. SEVEN merges, every one reviewed to a
verdict; three of those reviews returned REJECT and each got a fix round. Five
tasks filed from what the work uncovered; 108 dead branches pruned.

MERGED, in order
- =bb2fb24= TASK-YN5FJ.1 — =dispatch-close --verdict <approve|approve-with-follow-ups|reject>=.
  The default-branch gate demanded a =VERDICT= that only =--property VERDICT== could
  set, so the refusal named a remedy that did not clear the refusal. Three manager
  rulings, all carried in code comments: ANY verdict clears the gate including
  =reject= (the gate asks whether review happened, not whether it approved — and a
  reject that forced =--no-review-required= would stamp a bypass on a dispatch that
  WAS reviewed); the typed set is a strict SUPERSET, so =clean=/=ship=/=has-issues=
  still work through =--property=; passing both spellings is a hard error, not
  last-wins. Reviewed REJECT, fixed, re-reviewed APPROVE.
- =14b9660= TASK-YN5FJ.1.1 — the fix round for that REJECT. The refusal now prints
  =orgasmic manager dispatch-close --task <real id> …= with the executable and
  every known token as its real value. The test EARNS the word verbatim: it
  extracts the command from the refusal that run produced, substitutes exactly two
  documented placeholders, ASSERTS every other token is free of =<=/=>= (so a
  message that leaves a placeholder for something it knew fails the MESSAGE), and
  execs =argv[0]= by name off PATH. APPROVE, no findings.
- TASK-M47E5 — managed worktrees relocated OUT of the project to
  =~/.orgasmic/worktrees/<project-id>/=, the surviving fix from TASK-3X5AQ's two
  disproved ones. Reviewed, and the review found three P0/P1 defects on the
  RECLAMATION half that shipped alongside it.
- =b20fba5= TASK-M47E5.2 — the fix round for those three. REJECTED AGAIN, with
  three MORE BLOCK SHIP findings. Six confirmed defects across two rounds, all on
  one delete path.
- =a1cd738= TASK-M47E5.3 — the ruling that followed: GATE THE VERB OFF rather than
  patch it a third time. =worktree-prune= now refuses by construction with and
  without =--dry-run=, the SCAN is gated too (a dry run still walked directories it
  had no business walking), and =dispatch-status= no longer advertises reclaimable
  trees. The implementation is KEPT, not deleted, and the reclamation tests are
  =#[ignore]=d rather than removed — that coverage is what TASK-RMA18 un-ignores.
  This decoupled the runtime reinstall from the landmine.
- TASK-M47E5.1 — cursor-acp's own-dispatch-artifacts grant, which the relocation
  had made permanently false (it derived the attempt dir from =worktree.parent()=).
  The honest outcome was that the NARROWER grant no longer exists: it was removed
  and callers routed to the sibling, rather than keeping two names for one
  predicate. TASK-A1KJW would make it expressible again.
- =2d9d8b8= TASK-4WKNX — a =:FIX_SUBTASK:= close now lands =in_review= by default;
  =--fix-round-final= opts out, requires =--reason=, and stamps the ledger.
  REJECTED on review — see In flight.

THE DEFAULT WAS INVERTED ON MEASUREMENT, NOT TASTE. 2026-08-05 ran five fix
rounds and one of them — the fix for three data-loss findings — was itself
rejected with three more BLOCK SHIP findings, two P0. Under the old default that
round would have closed straight to =done= and six confirmed defects would be
sitting on main unreviewed. The two failure modes are not symmetric: a trivial fix
waiting for a cheap review is recoverable; an unreviewed fix for a data-loss
finding is what the goal exists to prevent.

PROOF DISCIPLINE, THE PART WORTH COPYING
- TWO proofs ship for YN5FJ.1, and the second is the interesting one.
  =verify/TASK-YN5FJ.1-remedy/= removes ONLY the line that writes the flag's
  value: the flag still parses, =--help= still documents it, the refusal still
  prints the corrected remedy — and the loop still fails. A direct proof that a
  prose-only fix would not have been a fix.
- The YN5FJ.1 re-cut was verified BEYOND its green replay: applies clean, injected
  =manager.rs= BYTE-IDENTICAL to =bb2fb24^1=, red log carries exactly ONE panic,
  the pinned label quoting the pinned pre-fix text. A green =orgasmic verify=
  alone could not have told you any of that.
- TASK-4WKNX's proof pins the END-TO-END SYMPTOM (=reviewer dispatch is allowed
  only from IN_REVIEW=) rather than the stage assertion. A proof pinned on the
  stage would have passed while the workflow stayed broken.

FILED FROM WHAT THE WORK UNCOVERED
- TASK-RMA18 (P1) — the worktree-reclamation redesign, carrying all six BLOCK SHIP
  findings and starting from the gated-off code.
- TASK-4WKNX (P2, now merged) — hit live, not theorised: the =FIX_SUBTASK= close
  and the reviewer-dispatch gate pointed opposite ways, and the only way through
  was a manual backwards state flip performed three times in one day.
- TASK-4WKNX.1 (P1) — see In flight.
- TASK-A1KJW (P3) — the dispatch record dir is named from the BRIEF FILENAME,
  never the task id.
- TASK-37WN7 (P3) — =sigterm_exits_through_graceful_shutdown…= flakes DESPITE the
  TASK-Q07Y5 mitigation built for exactly that race, so it is a mitigation that
  does not hold, not an unmitigated race. NOW REGISTERED in
  =verify/flake-registry.toml= with a captured signature — and the measurement
  CORRECTED THE TASK'S OWN PREMISE: it reproduces WITHOUT load, at roughly 7% (one
  failure in the first 14 isolated runs, then 12/12 clean), which contradicts the
  "invisible on an idle machine" reading it was filed under.

HOUSEKEEPING: 108 merged =task-*-impl= branches deleted with =git branch -d= (the
safe form; every commit stays reachable from main). =git branch= is useful again.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. There is no
project default and nothing enforces resolution (=--model= > task drawer >
harness default, and the harness default wins SILENTLY — TASK-A55J7). THIS
session's answers, which are NOT a standing setting: implementers =tmux / claude
/ claude-opus-5 / high=, reviewers =tmux / codex / gpt-5.6-sol / high=. Pass
=--model= explicitly and echo all four back after each dispatch.

TRANSPORT: tmux, settled rather than a coin-flip. Both properties read against
the INSTALLED daemon: pane liveness from TASK-4CSMY is confirmed reporting in
production, so no =stall_timeout_secs= override is needed; and rmux fails the
survival property (TASK-4DTBZ — it killed three workers in 24ms). Every dispatch
across 2026-08-05 ran on tmux with no transport trouble.

TRAP #1 STILL HOLDS: dispatch with =--mode acp-stdio=, NOT =--mode stdio=, until
the runtime is rebuilt. =--mode tmux= is unaffected and is what every dispatch
used.

SERIALIZE IMPLEMENTER DISPATCHES ON THIS HOST. Each worktree gets its OWN cargo
target dir (shared ones are unsound — trap #4), so two concurrent implementers
mean two workspace builds plus two =run-tests.sh= runs. Test EXECUTION is what
bursts =syspolicyd=, and this machine has hard-rebooted twice from that chain.
One implementer at a time is a deliberate throughput sacrifice, not an oversight.

A NOTE ON CLOSING A REVIEWER, learned in anger. The installed runtime predates
=--verdict=, and its =reviewer_done_stage= does not know the word =approve= — it
maps only =clean=/=ship= to done. So on TODAY's runtime, close a reviewer with
=--property VERDICT=ship=, not =approve=, or the task lands =in_progress= and you
will not notice. After the reinstall, use the flag. This is the compat path
RULING 2 preserved, being used for real.

AND EXPECT THE FIX-ROUND FLIP TO STILL BE NEEDED. TASK-4WKNX MERGED (=2d9d8b8=),
so the repo now closes a =:FIX_SUBTASK: t= implementer to =in_review= by default
— but the INSTALLED CLI is the one that runs, and it still sends the task to
=done= where the reviewer dispatch is refused. Keep flipping =done= -> =in_review=
with =task update --state= and say in =--reason= that it is policy, not a
correction, until the reinstall. This is the clearest example of why four merged
changes being inert matters operationally.

THE ORDER FROM HERE
1. TASK-4WKNX.1 — in flight. Integrate its report, review it, then close the
   4WKNX chain. A BLOCK SHIP is not closed until its fix round lands and passes
   a FRESH review; that is the goal's own Reached-When, not a preference.
2. TASK-RQ270 stage B (P1) — architecture excision, drift comparator. Cheapest
   and independent: delete =crates/orgasmic-cli/src/architecture_drift.rs= (457
   LOC, arch-only end to end) plus the =Drift= verb (=main.rs:754=, =:3090-3097=,
   =:3138=), and strip the =// arch:= back-edges. MEASURED, not estimated: 51
   markers across the workspace, and every one is on LINE 1 of its file as a
   header comment — so this stage cannot conflict with interior edits in any
   concurrently-dispatched file. Stage A is DONE (TASK-RQ270.1).
   THE LOAD-BEARING CONSTRAINT for later stages: the dangling-edge lint carve-out
   lands in stage C and must be on main BEFORE stage D deletes
   =architecture.org=, or done.org's 231 =:IMPLEMENTS: arch= tokens start failing
   the lint.
3. TASK-RMA18 (P1) — the worktree-reclamation redesign, carrying all six BLOCK
   SHIP findings from the two prune reviews. It starts from the gated-off code.
4. TASK-STWVB (P2) — the suite's full-run signal. Design is on the task; A and
   B/C are separable rounds.
5. TASK-A1KJW (P3) — the dispatch record dir is named from the BRIEF FILENAME,
   never the task id (=dispatch_artifact_stem=, =manager.rs:~5517=). That is why
   TASK-4WKNX.1's worktree sits at =task-4wknx.1/= while its logs sit at
   =task-4wknx-1/=. Fixing it would also make cursor-acp's narrow own-artifacts
   grant expressible again (TASK-M47E5.1 had to delete it).
6. TASK-37WN7 (P3) — one acceptance item left: strengthen
   =wait_until_serve_awaits_signals= or record why not. The flake itself is now
   REGISTERED with a captured signature.
7. Still P1 and untouched: TASK-4DTBZ, TASK-BX5SR, TASK-A55J7, TASK-48SFW,
   TASK-ADHWZ, TASK-NTNK0.

THE RUNTIME REINSTALL IS UNBLOCKED AND IS NOW THE HIGHEST-VALUE SINGLE MOVE, but
it is an OPERATOR-AUTHORIZATION item, not a manager one: it swaps the live daemon
and needs a zero-worker window. The recipe and its four traps are in the
=orgasmic-runtime-reinstall= memory: =install.sh= silently skips the unpack if the
version dir exists, its trailing =orgasmic status= auto-spawns a competing daemon
(=ORGASMIC_INSTALL_SKIP_STATUS=1=), and you MUST codesign or the binary loses its
TCC grant. Verify BEHAVIOURALLY — both builds call themselves =0.0.15-dev.1=.
Three good new-code probes now: =--mode stdio= being accepted,
=dispatch-close --verdict= existing, and =dispatch-close --fix-round-final=
existing.

** In flight
ONE DISPATCH RUNNING: TASK-4WKNX.1 (P1, fix round) implementer,
=implementer-codex-tmux= (tmux / codex / gpt-5.6-sol / high), run
=run-20260805T191948-75931043950a4f588c6c97b4abafd9a6=, started_tx
=tx-20260805-orgasmic-4535=, base =f60af0a=. Report lands at
=.orgasmic/tmp/dispatch/task-4wknx-1/task-4wknx-1-105802ed444541948082cdb68e6ed913-last.txt=.
If that file is non-empty, integrate it; if the tmux pane is gone and it is
EMPTY, treat the run as orphaned and read =rescue-policy= before assuming
anything completed.

WHAT IT IS FIXING, so you do not re-derive it: TASK-4WKNX merged =2d9d8b8= and
its review returned REJECT with one BLOCK SHIP, which the manager confirmed
against the code. =close_done_request= pushes caller =--property= entries BEFORE
its own typed keys, and =parse_close_property= accepts any uppercase key — so
=--property FIX_ROUND_FINAL=true= forges the audit key without the =--reason=
the flag demands, and =--fix-round-final --property FIX_ROUND_FINAL=false=
writes duplicate contradictory keys that =extra()= resolves FIRST-wins to the
caller's value. The manager also found the identical hole on the PRE-EXISTING
=NO_REVIEW_REQUIRED=. The fix is a rule about manager-owned close-tx keys, not a
third copy of the =VERDICT= one-off.

THE ONE CONSTRAINT THAT WILL BITE A FRESH MANAGER READING THAT DIFF:
=--property VERDICT== is a SUPPORTED legacy free-text spelling that the
default-branch gate reads (TASK-YN5FJ.1 RULING 2). "Reserve every manager-owned
key" is therefore wrong as stated — the class needs at least two policies,
reserved and aliased. If the fix flattens them it has either broken =VERDICT= or
left =FIX_ROUND_FINAL= forgeable.

A NOTE ON MONITORING A TMUX DISPATCH, paid for this session: =dispatch-status=
prints =[pid-gone]= for EVERY tmux dispatch, always, because =WORKER_PID=-= —
the manager never tracks the pane pid. It is NOT a death signal and a monitor
keyed on it fires instantly and falsely. The real signals are =[run-gone]=, the
tmux session disappearing, and a non-empty =last.txt=.

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO. Installed =05e1813=; main is far ahead. Trap #1
in the lead (the =acp-stdio= vs =stdio= mode rename) is the part that bites.

FOUR MERGED CHANGES ARE INERT UNTIL THE REINSTALL, and that is now the main
reason to do it: the TASK-YN5FJ review gate, the TASK-M47E5 worktree relocation,
the TASK-YN5FJ.1 =--verdict= flag, and the TASK-4WKNX fix-round default. Observed
DIRECTLY rather than inferred, twice: a reviewer dispatched after M47E5 merged
still got an in-project worktree, and TASK-4WKNX.1's worktree — dispatched AFTER
4WKNX merged — sits at =.orgasmic/tmp/dispatch/task-4wknx.1/worktree=. The CLI
that creates worktrees and closes dispatches is the installed one.

THE REINSTALL IS NOT BLOCKED. It was, because =orgasmic manager worktree-prune=
shipped a delete path that failed TWO reviews with six BLOCK SHIP findings, and
the only thing keeping it harmless was that the installed runtime predated the
verb. TASK-M47E5.3 (=a1cd738=) made the verb REFUSE BY CONSTRUCTION — both forms
exit 1, the scan is gated too, and =dispatch-status= no longer advertises
reclaimable trees. Verified on the real binary. The protection is now in the code
rather than in this document. TASK-RMA18 owns the redesign and un-gates it.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item 2).
Unchanged, and it is the single most expensive mistake available.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1). TMUX IS THE
BETTER DEFAULT. The TASK-4CSMY pane-liveness channel is confirmed reporting in
production, so the =--governance-json {"stall_timeout_secs":3600}= workaround
stays retired. Read =event.type=, never the serialized line.

=[pid-gone]= IS NOT A DEATH SIGNAL FOR A TMUX DISPATCH. =dispatch-status= prints
it for every tmux run always, because =WORKER_PID=-=. A watcher keyed on it fires
instantly and falsely. Use =[run-gone]=, the tmux session vanishing, or a
non-empty =last.txt=.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). Detection
is =grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention.

MERGE STILL PRECEDES REVIEW IN PRACTICE, AND THE FIX IS MERGED BUT NOT LIVE. So
expect merge -> review -> fix in =TASK-<id>.N= rounds. When you review a
post-merge diff, SAY SO in the brief and tell the reviewer a REJECT is still
actionable, or you will get softened findings. 2026-08-05 ran six such rounds and
THREE returned REJECT — including the review of a fix round, and the review of
the very task that made fix rounds reviewable.

DO NOT STASH =.orgasmic= TO GET A CLEAN TREE FOR =orgasmic verify=. The daemon
reads those files, so during the stash window it cannot see its own project and a
concurrent write fails =404 Not Found: node TASK-XXXXX=. Commit the state first;
=verify= only needs the tree clean, and committed state is clean.

REVIEW-BRIEFING LESSONS, all paid for:
- DO NOT ask a reviewer to mutate source. The contract is read-only, so a brief
  asking for a negative probe returns an unrun gap, correctly. The IMPLEMENTER
  runs negative probes; the reviewer judges them.
- A REJECT the manager then fixes personally is a normal, good outcome.
- NAME THE SPECIFIC CLAIM YOU WANT ATTACKED. Asking "is the handle actually the
  reference point everywhere, or does any surviving call re-derive a path?"
  produced the finding that classification re-resolved the pathname while removal
  used the fd. A generic "review this diff" would not have.
- TELL THE REVIEWER WHAT IS ALREADY ESTABLISHED so it spends effort on what is
  not. Every review this session was given a do-not-re-derive list.
- VERIFY EVERY FINDING AGAINST THE CODE BEFORE ACTING ON IT, including the ones
  you intend to accept. Confirming the TASK-4WKNX finding by hand is what turned
  up the THIRD instance of the same hole, on a key the reviewer never looked at.

STATE-HYGIENE SHAPES TO WATCH FOR:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  Check =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a
  stage.
- A BLOCKER RECORDED ONLY IN PROSE is invisible; =depends_on= is what the board
  reads.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED misleads every
  skim-reader. Use a =READ THIS FIRST — STATUS= block at the TOP plus a corrected
  TITLE; appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6); it
spawns real billed provider turns.
