#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:GOAL_ID:          cleared
:LIVENESS:         e0e8b0e
:LIVENESS_AT:      [2026-07-16 Wed]
:UPDATED_AT:       [2026-07-16 Wed]
:END:

Manager-owned snapshot reconciled [2026-07-11 Sat] after the runtime-served entry/workflows redesign and the 0.0.11 stable publish. No active goal or in-flight work remains. Use task Evidence, the tx ledger, and Git history for implementation detail.

** Done so far
- Reconciled operator span 06d0956..ad38043 (44 commits outside manager sessions, 2026-06-22..07-01): dec_B4147 amendments 2-7 land fully local-first publishing (publish-runtime.sh + publish-apps.sh incl. --channel nightly, version-less assets, runtime retain-current+1 pruning, apps-stable tag namespace, unified Android versionCode); runtime 0.0.7 and apps 0.0.6 shipped, 0.0.8/0.0.7 dev lines open. Android remote-daemon connect + in-app update check (dec_PVDP3, 169d60f) with rustls/insets/APK-opener/countdown fixes. Nested decisions M2 :PARENT: tree merged (arch_Q1JVD, TASK-2DFTX; 423b267/dc3a8ad). CLI bearer auth for external daemons + stale-token auto-repair (b66ab8a/b6734d2). acp-ws codex appserver worker dropped, reviewer moved to acp-stdio (9d47627).
- Goal pivot recorded [2026-07-03 Fri]: dec_ZG9B9 (excise the Graph page viewer; supersedes dec_WWAHT); TASK-M6146 filed; goal-20260703-cut-the-graph set (tx-20260703-orgasmic-1167). Committed 15a7443.
- TASK-M6146 (1/6) DONE: Graph page viewer excised, merged 45c6116 (worker 93529c2, implementer-claude-rmux sonnet-4-6 high; -2997/+43). Gates green (ui 73 tests, daemon suite, clippy, fmt). Manager integration: arch_M8JQT reference-pages-only revision, arch_MK2Q2.7 SOURCE_PATHS pruned, drift clean, parse_errors 0. REVIEWER PASS (reviewer-claude-rmux sonnet-4-6 xhigh, run-20260703T185257): VERDICT ship, zero findings. implementer.done tx-1192, reviewer.done tx-1203. State commits 159496c + this one.
- TASK-SJQ9V filed: doctor staleness test fails only under cargo test --workspace concurrency (pre-existing latent race surfaced at integration; passes standalone parallel+serial).
- Worker templates modernized [2026-07-03 Fri]: claude-rmux implementer -> claude-sonnet-5 (high), reviewer -> claude-opus-4-8 (xhigh); MODELS allowlist now sonnet-5/opus-4-8/haiku-4-5. Applied twice: user overrides in ~/.orgasmic/user/workers/ (live daemon resolves them, verified via orgasmic worker show) + checkout shipped/workers/ for the next runtime publish. Transcript audit confirmed no silent fallback on prior runs: all M6146 turns genuinely served by claude-sonnet-4-6.
- TASK-ZEFEY (2/6) implemented and merged ddd6b56 (worker 9a5f043, sonnet-4-6 high, ~24m; +1742, 8 files): full artifact store per arch_ARSPJ — ART-<slug>/ folders, artifact.* tx types, index projection + WS topics, GET reads, submit/feedback(+consume) writes with validate-before-write and atomic files+tx transaction, 22-type block registry, orgasmic artifact CLI verbs, CLI round-trip integration test. Worker completed but rmux stall detector clobbered last.txt post-eot (manager-rescued close tx-1215; tooling bug filed TASK-B05AM). Manager review clean; one design question (hand-rolled parse_org_properties vs canonical Org parser) carried to the reviewer.
- TASK-ZEFEY reviewer pass closed (opus-4-8 xhigh first live run, ~8.5m): VERDICT ship-with-fixes — acceptance conforms to arch_ARSPJ end to end, but 1 HIGH (validate_mdx byte-scan false-rejects/false-accepts, proven via rustc probe) + 3 MEDIUM (hand-rolled Org parser truncation/injection, writer bypass, non-atomic comment writes) + 4 LOW. All 8 filed as reviewer.finding tx (tx-1226..1233 range); reviewer.done tx-1234. Substantially deeper than the sonnet-4-6 M6146 pass — model upgrade validated.
- TASK-Y2ZQJ fix subtask filed (:PARENT_TASK: TASK-ZEFEY, :FIX_SUBTASK: t) with all findings + two manager rulings (zero-block MDX rejected; detail bundle excludes consumed by default with include_consumed param); dispatched to implementer-claude-rmux on claude-sonnet-5 (first sonnet-5 run), run-20260703T195310.
- TASK-Y2ZQJ (ZEFEY fix) LANDED: merge 916a159 (worker commit ab4b5b8, sonnet-5 high, session c86523ed, first sonnet-5 fix run). All 8 findings closed — HIGH validate_mdx rewritten as top-level structural scan (scan_tag_header, opaque registered-block bodies, zero-block reject); MED canonical OrgFile::parse for artifact.org/reviews.org + invertible leading-* comma-escape; MED writer-mediated archive/dir (fs bypass removed); MED atomic add/consume via single writer.transaction; LOWs idempotent artifact.created / typed VersionNotFound 404 / single-line TITLE-PROMPT escape / truthful WS state — plus both manager rulings. Gates: manager INDEPENDENTLY re-ran cargo test -p orgasmic-daemon on the worktree = 201 lib + all integration binaries green; clippy --workspace --all-targets -D warnings on merged tree = clean; worker cli 71/71 (--test-threads=1), fmt clean, live HTTP production-path probe on isolated ORGASMIC_HOME. implementer.done tx-1245. Evidence recorded on the node.
- TASK-B05AM RECURRED (2nd occurrence, now reproducible): the Y2ZQJ implementer finished fully (committed ab4b5b8, gates green) but wrote NO report at all — neither last.txt NOR stdout.log existed (worse than ZEFEY, which at least clobbered last.txt). Manager rescued the full report + gate results from the harness transcript. Report lost on 2 of last 3 rmux dispatches. B05AM priority raised to high; recurrence Evidence recorded.
- TASK-KBHAN filed (LOW, :PARENT_TASK: TASK-Y2ZQJ): self-disclosed residual — Y2ZQJ hardened leading-* but not #+begin_/#+end_ block-marker injection in comment bodies; file-global block-mask means an unmatched #+begin_ in a comment can suppress LATER comments in the same reviews.org. Blast radius = comments hidden within one reviews.org (no artifact.org property/flag injection); scoped out of the one-round fix.
- TASK-Y2ZQJ REVIEWER PASS closed (reviewer-claude-rmux opus-4-8 xhigh, run-20260703T211936, ~10m): VERDICT ship-with-fixes. All 8 ZEFEY findings genuinely closed (8/8), both manager rulings hold, 28 targeted artifact tests pass, validator re-proven via standalone rustc probe (15 inputs). BUT the finding-#4 atomicity refactor introduced a NEW MEDIUM regression: M1 — concurrent comment-add/consume on one artifact silently loses a reviews.org write (swapped race-safe in-lock mutate_file for read-outside-lock + blind FileRewrite). Plus L1 LOW (fold inline state/version scrapers into load_artifact) and L2 LOW note (validator literal-close-string early-termination -> T25XQ handoff). Findings filed as reviewer.finding tx; reviewer.done recorded. last.txt scrollback-truncated again (last.txt existed this time but only held TUI scrollback); VERDICT+top findings recovered from rmux raw capture — B05AM-adjacent, another reason the raw capture / transcript is the reliable source.
- TASK-Y2ZQJ M1 regression FIXED INLINE by manager (operator chose inline over dispatch), commit 1854420: per-artifact async mutex (ApiState::artifact_write_lock) serializes the reviews.org read->transaction window in both feedback handlers, restoring the serialization the atomicity refactor dropped while keeping finding-#4 atomicity. L1 folded in (add handler state/version -> one load_artifact()). L2 recorded as a note on TASK-T25XQ (validator is structural-only; real MDX parser there owns nesting/literal-close-string). New regression test artifact_concurrent_comment_adds_do_not_lose_writes (12 concurrent adds, multi_thread), teeth-verified (12 survivors with lock, 1 without). Gates: daemon 202 lib + integration green, clippy -D warnings clean, fmt clean. INDEPENDENT opus review (fresh in-process agent, routed around the broken rmux path per B05AM): VERDICT ship, no blocking findings — verified full-critical-section coverage, no deadlock/re-entrancy, std Mutex never held across await, L1 equivalence, teeth; also confirmed post_artifact_submit writes reviews.org only in its is_new creation branch (no unlocked third writer). TASK-Y2ZQJ marked DONE; reviewer.done tx recorded.
- ARTIFACT STORE FOUNDATION COMPLETE + HARDENED: TASK-ZEFEY (store) + TASK-Y2ZQJ (8 findings) + M1 concurrency fix, all reviewer-verified. The store the whole artifact queue builds on is now race-safe. TASK-2ZQSB unblocked (todo).
- TASK-B05AM FIXED (clobber acceptance), commit ed6a6fc: rmux/tmux release(reason) synthesizes RunComplete{summary:reason} when a run never emits its own terminal event (eot missed -> stall fires); dispatch_last_summary_from_session ranked that sentinel above the real transcript, so last.txt got 'stall_timeout_exceeded' instead of the report. Root cause pinned from the real ZEFEY session (run_complete then release, both 'stall_timeout_exceeded', 9263 assistant chunks shadowed). Fix drops the release-echo run_complete and falls back to the assistant tail (32 KiB cap); driver/supervisor unchanged. Teeth-verified test + normal-path guard. daemon 204 lib green, clippy/fmt clean. B05AM -> done. SPLIT: the daemon-restart-mid-run 'no last.txt at all' mode (Y2ZQJ reattach kills the watcher) is distinct -> filed TASK-567JG (needs RunMeta artifact-path persistence).
- TASK-2ZQSB (3/6) LANDED: merge 6f8b88b (worker c37a5c0, sonnet-5, +1368/11 files). WorkerKind::Artifactor + artifactor worker record + artifact-generator prompt spec + slots; POST /artifacts/generate + /:id/regenerate launch a real worker run via spawn_worker_run under the artifact.generate:{id} lease (per-artifact regen mutex, LeaseHeld->409); submit-only write path; release-watcher reverts stuck-regenerating (only-if-still-regenerating, reuses Y2ZQJ M1 lock) with artifact.generation.failed tx. Worker self-caught+fixed a lease-ordering bug via live probe. Gates: core green, daemon 216 lib + integration green, cli 100% single-threaded (parallel 1-fail=known SJQ9V), clippy/drift clean, parse_errors 0. Live probe exercised the full acceptance (real run visible, created/submitted tx, 409 on concurrent, 422 no-write on invalid, watcher revert). B05AM FIX VALIDATED: last.txt landed clean after a 56m run (no clobber, first rmux dispatch post-fix). arch_ARSPJ prose updated with artifact.generation.failed. implementer.done tx-1291. Qualified: end-to-end LLM turn not driven (sandbox lacked claude creds); downstream contract proven by direct submit.
- TASK-2ZQSB reviewer pass (opus-4-8 xhigh, run-20260703T233904): VERDICT ship-with-fixes. Verified clean: lease-first ordering, release-watcher revert, submit-only write path, M1 lock reuse (no deadlock), context assembly. Findings M1 (MEDIUM, PRE-EXISTING: post_artifact_submit lacked the per-artifact lock — Y2ZQJ M1 covered feedback add/consume but not submit) + L1/L2 LOW, all fixed inline commit 510d8d9: submit wrapped in artifact_write_lock (teeth-tested artifact_concurrent_resubmits_do_not_lose_version_bumps — 8 concurrent resubmits collapse to 1 version without the lock); consumed_cids predicate matched to consume_all_open_comments; doc lists all 4 writers. daemon 217 lib + integration green, clippy/fmt clean. TASK-2ZQSB -> DONE. The per-artifact write lock now covers ALL four mutating handlers (submit, feedback add, consume, regenerate). last.txt clean on both 2ZQSB dispatches (B05AM fix holding across a 56m impl + a reviewer run).
- RECONCILED same-day continuation span 510d8d9..147b07c (15 commits, [2026-07-04 Sat]) that committed state task-to-task but never updated this handoff. Two threads:
  (a) HOT-SESSION ARTIFACT LOOP side-track (dec_S02E2 hot session as the default artifact loop; dec_1EEYE grilling starts from a bare prompt; arch_045Q0.2): TASK-0J0AV prompt-only generation, dropped the nodes-non-empty guard (merged aa9f9f1); TASK-RCWFF rmux send_input delivers followup turns into a live TUI session (merged 446f8cf); TASK-RH4M5 hot-session artifactor default + live regenerate routing, Parts A/B/C/D1/D2 (merged fd4474c), D3 idle-timeout deferred -> TASK-F9N5F. All three closed via MANAGER INLINE OPUS REVIEW = ship (no dispatched reviewer-rmux pass). Follow-ups filed: TASK-F9N5F (idle-timeout), TASK-X0ZVE (live rmux render_stream flake), TASK-A22TB (renderer fidelity polish).
  (b) TASK-T25XQ (4/6) DONE: Agent-Native block renderer (~22 block types, recursive-descent parser -- no JS eval, same-name nesting, literal </Name>, per-block error isolation), ArtifactView + Artifacts pages + Generate entry points + PlanCommentAnchor comment capture (merged 330c914; worker impl 06887d4, stale-multiselect fix 5473fe2). MANAGER INLINE REVIEW = ship (typecheck clean, vitest 108/108 incl real Mermaid render, vite build ok, security clean at every dangerouslySetInnerHTML/iframe site; worker also ran a live isolated-daemon smoke + forked adversarial review). Fidelity polish -> TASK-A22TB; interactivity -> TASK-EDQPG. NOT given a separate reviewer-claude-rmux pass -- the 07-04 session used manager inline reviews for 0J0AV/RCWFF/RH4M5/T25XQ.
- TASK-ZEFEY heading stale-state corrected to done ([2026-07-04 Sat] 15:12; work was merged ddd6b56 on 07-03, heading had lagged).
- TASK-M6146 (1/6) heading repaired in_progress -> done ([2026-07-04 Sat], this resume): genuinely done since 07-03 (merged 45c6116, reviewer pass zero findings) -- the reviewer-flip nit had left the heading in_progress.
- Artifact queue: M6146 (1/6) done, ZEFEY (2/6) done, 2ZQSB (3/6) done, T25XQ (4/6) done; DJGM5 (5/6) and EDQPG (6/6) remain backlog. The completed hot-session loop now underpins EDQPG's regenerate routing (dec_S02E2).
- TASK-DJGM5 (5/6) DONE + MERGED to main @ 09935b1 [2026-07-05] (member identity, /login, whole-surface capability gating, pinned named comments; --no-ff merge touched only crates/ + ui/, live .orgasmic state untouched). Acceptance two-browser smoke passed on an ISOLATED throwaway daemon (admin/viewer/artifacts-only); a member-shell gap (admin-only global polls 403ing for members -> ErrorPanel) was caught in-smoke + fixed 3d16519 (capability-aware member landing, hide bell/RunDock/admin nav, gate polls on !isMember). tsc clean, 126 vitest, vite build green. Non-blocking follow-ups noted (write-affordance gating audit for read-only viewers; member live-session viewing UI deferred; transient first-connect 401). Leftover task-djgm5-impl worktree removed this session.
- Artifact queue now 5/6 DONE (M6146, ZEFEY, 2ZQSB, T25XQ, DJGM5); TASK-EDQPG (6/6) is the last.
- TASK-EDQPG (6/6) dispatched [2026-07-05] after a manager Explore gap analysis established the regen loop is ~90% already built by 2ZQSB/T25XQ/RH4M5 -- only two real gaps remain (extra-prompt UI; archived-version comment thread) + a small resolved-flag context tightening. See ** In flight. LIVENESS reconciled in place 3376efd -> 62564e4 (13 commits, all DJGM5 work already documented in the top bullet; the stale In-flight/Next-actions were the DJGM5-pre-merge snapshot).
- TASK-EDQPG (6/6, FINAL) DONE + MERGED 45c4546 [2026-07-05] (worker e02307a, implementer-claude-rmux sonnet-5 high, run-20260705T073712, ~26m). Regen loop was ~90% pre-built; manager Explore gap analysis scoped it to two real gaps + a tightening, all landed: extra-prompt RegenerateArtifactDialog (canGenerate-gated), archived-version comment-thread scoping (load_artifact_detail c.version==N for archived views, current-view unchanged), resolved-flag in assemble_regen_context. Gates (manager re-ran on merged main): daemon 255 lib + all integration pass incl 3 new EDQPG tests; clippy -D clean; touched-file fmt clean; ui tsc clean, vitest 133, build green; worker live real-HTTP round-trip proved archived GET returns vN body + its consumed thread while current view stays clean. Inline-reviewed (accepted gate). implementer.done tx-1396. ARTIFACT QUEUE 6/6 DONE.
- [2026-07-06] GOAL goal-20260703-cut-the-graph REACHED + CLEARED (tx-20260706-orgasmic-1406). Final Reached-When (live two-browser two-member LLM smoke) PASSED — manager-driven on an ISOLATED daemon (fresh ORGASMIC_HOME + throwaway git repo + non-4848 port, built from the merged checkout so DJGM5+EDQPG code was under test; production 4848 untouched). alice(editor)+bob(viewer) in separate browser cookie-jars (chrome-devtools isolatedContext): real artifactor (rmux/claude sonnet-5) generated v1 Sprint Board; named comments from both members propagated live over WS with no reload; Regenerate consumed both comments + an extra prompt -> v2 added a Blocked column (alice ask) + assignee names (bob ask), v2 thread clean (criterion-5), archived v1 retained its OWN consumed thread with resolved/consumed badges (EDQPG delta); capability gating held (viewer saw no Regenerate); commenting LOCKED live in both browsers during regen; tx chain in order (created -> submitted v1 -> 2x comment.added -> regenerated{consumed 2 CIDs, extra_prompt} -> submitted v2); parse_errors 0. One cosmetic note: the extra-prompt blue-header ask did not visibly apply (theme kept amber) — LLM/theme variance, not a loop defect. Isolated-daemon harness gotchas saved to auto-memory (CLAUDE_CODE_* env poisons worker claude; claude folder-trust dialog; rmux SDK vs CLI default-socket split). Screenshots delivered to operator.
- [2026-07-06 Mon] TASK-VXXQC DONE (commit 75ddb4e, no active goal -- ad hoc operator-directed fix outside goal-20260703-cut-the-graph which is CLEARED). Root cause: stop_macos_launch_agent (crates/orgasmic-cli/src/daemon_service.rs) discarded launchctl bootout's result; a failed/slow unload left the LaunchAgent loaded+KeepAlive-supervised so killing the pid let launchd respawn it before the restart's own bootstrap, losing the EADDRINUSE race against orgasmic update. Ruled out the SO_REUSEADDR theory from the original filing (tokio's TcpListener::bind already sets it by default on this host, empirically verified). Fix: bootout is now required to succeed and its completion is verified with a bounded 5s poll before returning, failing loudly instead of racing a respawn; not-currently-loaded daemons (bare orgasmic serve) are untouched. 2 new unit tests. Gates green (build, targeted+workspace tests modulo the pre-existing TASK-SJQ9V flake, clippy -D warnings, fmt). LIVE VERIFICATION (operator-approved, real production host): published nightly 0.0.8-nightly.20260706.1783321648 via scripts/publish-runtime.sh --channel nightly (dry-run then live, 4 targets + smoke green), then ran orgasmic update --channel nightly for real -- exit 0, no manual intervention, daemon returned on a new pid/boot_id, tx_count preserved, parse_errors 0, single clean launchd job, port bound once. New gotcha recorded (gotchas.org + memory): overwriting an in-use codesigned macOS binary IN PLACE silently SIGKILLs all later execs of that path (fresh inode via rm+cp fixes it) -- hit only during manual pre-nightly verification, unrelated to the shipped fix.
- [2026-07-06 Mon] NEW GOAL goal-20260706-drain-the-full set (tx-1421) after a relevance audit of all 10 open backlog tasks: every one verified as a real, unfixed gap (P8ZSP Windows-PATH stub still bails; KNENN convention files exist; 567JG/KBHAN/F9N5F/A22TB/SJQ9V/X0ZVE code shapes all confirmed unfixed via Explore; ZNQ8F test RED 20-vs-19; KTRHZ rustfmt RED) — nothing stale, nothing dropped. Committed bbb12c2. Tasks 1-2 (baseline greens) DONE same turn, manager-inline: ZNQ8F (fixtures.rs 19->20, cargo test -p orgasmic-core green) + KTRHZ (cargo fmt --all over the 6 drift files, fmt --check clean + cargo check --workspace green). Backlog 10->8. parse_errors 0 (a transient 6 during the write-burst reindex self-cleared).
- [2026-07-06 Mon] TASK-567JG (3/10) DONE + MERGED 53c354c (worker b2e9654, implementer-claude-rmux sonnet-5 high, run-20260706T120221, ~21m; --no-ff, crates/ only, live .orgasmic state untouched). RunMeta now persists last_path/stdout_path (back-compat Option) + reattach_live_runs_on_boot respawns the dispatch completion watcher (four-Some guard; manager/recovery/pre-upgrade runs skip) so a daemon-restart-mid-run writes last.txt/stdout.log on release — the exact B05AM-adjacent gap SPLIT off from that fix. Boot reorder: reattach moved after ApiState construction in lib.rs (needs grace/auto-commit signal). Manager inline review (accepted per 2026-07-04 ruling) + independent gates: daemon 256 (incl new e2e test that RUNS not tmux-skips, proves last.txt written after simulated restart), core green, clippy -D clean, cli green standalone. Only workspace failure = pre-existing SJQ9V flake (passes standalone). implementer.done tx-1442.
- [2026-07-06 Mon] TASK-KBHAN (4/10) DONE (manager-inline, no dispatch). comment_line_needs_escape in artifacts.rs now comma-escapes #+begin_/#+end_ block markers (not just leading-*) so a member comment can't drive orgasmic_core's file-global block mask and swallow later comments in reviews.org. Matches parser semantics (lowercase + trim_start); invertible (predicate on comma-stripped bare form). Teeth-verified test comment_body_block_marker_does_not_suppress_later_comments (fails with block-check neutered, passes with fix). Gates: daemon 257 lib + integration green, clippy -D clean, fmt --all --check clean.
- [2026-07-06 Mon] TASK-SJQ9V (5/10) DONE (manager-rescue + inline, ce0a483). Aborted a runaway first dispatch (tx-1461; brief over-emphasized 'reproduce first' vs a rare flake -> 112MB session, no fix). Root-caused by reading actual cargo test --workspace failures: a cross-module env-race in orgasmic-cli tests (BRXGG class). Fixed: (a) named test load-invariant (pinned git dates + started_at from fixed instant, run_git git-env isolation, transient-failure retry); (b) new shared test_support module = one poison-resilient env_guard (into_inner, no more PoisonError cascade) + ScopedEnv used by BOTH daemon_client and the 6 daemon-booting doctor tests (which now guard + clear the daemon env). VERIFIED 6x cargo test --workspace daemon-live: ZERO orgasmic-cli env-race failures (was ~1/3). Remaining workspace flakes are a DIFFERENT family = live-tmux/rmux contention (real_tmux_*, live_rmux_render_path, tmux_ws_*) -> TASK-X0ZVE.
- [2026-07-06 Mon] TASK-X0ZVE (6/10) DONE + MERGED d70af6f (worker e4bd43f, implementer-claude-rmux sonnet-5 high, run-20260706T142134, ~1h20m; --no-ff, crates/ test-mods + Cargo.lock only, live .orgasmic untouched). Cross-process advisory flock (fs2::FileExt::lock_exclusive on $TMPDIR/orgasmic-live-session-tests.lock, MSRV-1.87-explicit) as first stmt of every live tmux/rmux test across 4 modules -- orgasmic-drivers modes/tmux.rs (7 real_tmux_*), modes/rmux.rs (9 live_rmux_*), orgasmic-daemon api.rs (boot_reattach/recovery/post_artifact_regenerate x3/artifact_release_watcher/tmux_ws_member_denied) + supervisor.rs (tmux_terminal_event_releases) -- so at most one real mux test runs at a time cross-binary. Worker guarded MORE than the brief named (supervisor.rs + api.rs artifactor tests) on its own initiative, justified by 'any unguarded live test still contends' + a workspace-wide grep for new-session/rmux probes; accepted. fs2 added to drivers dev-deps; .truncate(false) for clippy suspicious_open_options. Manager independent gates on merged main: cargo test -p orgasmic-drivers 125+5+25 pass; clippy --workspace --all-targets -D warnings clean; fmt --all --check clean. Worker ran cargo test --workspace 14x: the X0ZVE flake CLASS (real-tmux/rmux contention) is GONE -- zero failures in all 24 guarded tests every run. implementer.done tx-1473. BUT the goal's 'workspace green 5x consecutive' bar was NOT met as a literal streak: 3 pre-existing OUT-OF-SCOPE orgasmic-cli flakes remain (dispatch_close_prunes_stem_dir_leaving_brief -- worker confirmed reproduces at baseline 88c0e94 with changes stashed; doctor daemon_staleness_warns_for_git_commits_since_boot -- SJQ9V-class, RECURRED so the SJQ9V closure has a residual case; content_lifecycle hub_install_list_remove_uses_git_and_loader_layer). These block the goal's joint workspace-green-3x reached-when and need a follow-up (SJQ9V-adjacent); NOT X0ZVE scope.
- [2026-07-06 Mon] TASK-KNENN (8/10) DONE (manager-inline, docs-only, no dispatch). New 'Convention: dispatching implementation work' in conventions/orgasmic-tooling.org (live .orgasmic + shipped/skills/orgasmic/scaffold copy): implement/dispatch requests route through 'orgasmic manager dispatch --kind implementer --worker implementer-<harness>-<driver> --model --effort --brief' (worker id from 'orgasmic worker list'); raw codex/claude/cursor-agent off-limits for project work; mapping table (codex 5.5 xhigh->implementer-codex-tmux/gpt-5.5/xhigh, claude sonnet high->implementer-claude-rmux/sonnet-5/high, claude opus xhigh->opus-4-8/xhigh) + 'on fast'=harness-config + the WHY (write authority/tx/worktree/leases/reattach). Cross-ref bullet in contributing.org (both copies). Daemon reindex parse_errors 0 after live edits.
- [2026-07-06 Mon] RESIDUAL WORKSPACE-CONCURRENCY FLAKES FIXED (commit 86d6bb6, SJQ9V/X0ZVE line, manager-inline test-only). The 3 pre-existing out-of-scope orgasmic-cli flakes X0ZVE's 14x sweep surfaced (dispatch_close_prunes_stem_dir_leaving_brief in tests/dispatch.rs; doctor daemon_staleness_warns_for_git_commits_since_boot; content_lifecycle hub_install_list_remove_uses_git_and_loader_layer) all share one root: heavy real-subprocess spawning (git init/commit/clone/worktree + a real daemon boot) that transiently fails or races under cargo test --workspace peak concurrency (run_git even panics on a failed spawn via assert!(status.success())). Same class as X0ZVE. Fix: the SAME cross-process advisory flock (shared $TMPDIR/orgasmic-live-session-tests.lock path) added to each — live_session_guard in test_support (lib unit tests: doctor + content_lifecycle) + a local copy in tests/dispatch.rs (integration binary can't reach cfg(test) test_support); fs2 added to cli dev-deps. So at most one heavy real-subprocess test runs at a time across EVERY binary (tmux/rmux + git + daemon-boot). Verified: all 3 pass in isolation; full cargo test -p orgasmic-cli green (80+18+integration); clippy -p orgasmic-cli --all-targets -D warnings clean; fmt --all --check clean. This should clear the goal's workspace-green-3x reached-when — final joint verification deferred until F9N5F+A22TB land + no worker is running (a concurrent worker's own live tests would contend during measurement).
- [2026-07-07 Tue] GOAL SUPERSEDED: goal-20260706-drain-the-full (8/10 drained) -> goal-20260707-harden-the-agent (tx-1504), operator-directed re-prioritization after the 2026-07-06 orsl battle-test friction report. 8 hardening tasks filed via CLI (3K9ZG artifact-id security/JJ9RD config-kind/4T80X ref-validation/GERBB live-registration/DHYXQ bootstrap-smoke/V8WY9 parse-error-observability/MTB56 ergonomics/SPBTA discoverability, tx-1494..1501); 2 inline fixes in working tree only: scaffold dec_NNN text corrected (shipped/skills/orgasmic/scaffold/tasks/backlog.org, F2) + error-message house-style bullet in conventions/contributing.org. F9N5F reverted stale IN_PROGRESS->todo (no live run/lease). NEXT: TASK-3K9ZG (1/10, security). Working tree holds uncommitted scaffold+convention edits awaiting operator commit.
- [2026-07-07 Tue] RESUME reconciliation + TASK-F9N5F INTEGRATED (7/10 by queue slot 6). LIVENESS was 86d6bb6; HEAD had drifted +3: two benign operator commits (098bea0 README install instructions; 40701c5 ConnectGate UI fix holding the Outlet until needsToken clears — both out of hardening scope, folded here not replayed) plus 717c9e1 (last session's own goal-set, already documented). THE REAL FIND: the orphan task-f9n5f worktree held a COMPLETED implementation the prior session missed — worker a05cefc (implementer-claude-rmux sonnet-5, run-20260706T151535) finished the idle-timeout feature (supervisor.rs +261: AcquireRequest.idle_timeout_secs + RunRecord.{last_input_at,idle_timeout}, DEFAULT_IDLE_TIMEOUT_SECS=900, third idle candidate in timed_out_run, send_input resets last_input_at, gated ON only for rmux Artifactor; api.rs +16) with a full 32KB report, all acceptance criteria met, gates green — but was reverted IN_PROGRESS->todo on a "no live run/lease" signal without noticing the commit. Operator chose integrate-not-discard. Merged --no-ff 8bedea7 (worker base cd4f2d4 was stale/pre-86d6bb6 but files DISJOINT from all main changes since — merge-tree zero conflicts). Gates re-run on merged main: daemon 260 lib + all integration binaries green (incl 3 new teeth-verified idle tests), clippy -p orgasmic-daemon --all-targets -D warnings clean, fmt --all --check clean. dispatch-close done tx-1507 -> F9N5F now IN_REVIEW (awaiting reviewer pass per operator choice). Worktree removed + branch deleted. LIVENESS bumped -> 8bedea7. Residual risk (worker-flagged, non-blocking): reattach after daemon restart disables idle detection (no persistence signal reaches rehydration) until next natural release/cold-spawn.
- [2026-07-07 Tue] TASK-F9N5F reviewer pass CLOSED (reviewer-claude-rmux opus-4-8 xhigh, run-20260707T090638, ~8.5m; reviewer.done tx-1520). VERDICT ship-with-fixes — sharp. Verified-correct: min-deadline fold, last_input_at seeded at run_started_at (no instant-idle t0), lock discipline (reset rides the already-held guard, no new lock-across-await; sweep reads under same mutex), gating precision (Artifactor && rmux the only + correct site). Findings: HIGH-1 the persistent artifactor still carries its 600s stall so the 900s idle window is DEAD CODE and hot sessions still die at 600s (feature inert as merged — no production regression vs pre-F9N5F, but does not achieve arch_045Q0.2 intent); MEDIUM-1 idle resets only on send_input not driver output, so a >900s continuous-stream generation is killed mid-run (real: 56m artifactor runs on record); LOW-1 no both-clocks-aged production test. The reviewer ran READ-ONLY (filed no tx itself) so manager recorded the close + findings. Fix subtask TASK-NZ3C9 filed (tx-1522, :PARENT_TASK: TASK-F9N5F :FIX_SUBTASK: t, high) with TWO manager rulings baked in: (1) disable stall for Artifactor && rmux (stall_timeout_secs Some(0)) so idle is the sole gate; (2) idle reference = max(last_input_at, last_driver_event_at) — deliberately broadens the original reset-on-input-only wording so a live streaming turn is never killed and idle means genuinely-abandoned. F9N5F held IN_REVIEW until NZ3C9 lands. NZ3C9 write scope (supervisor.rs + api.rs gate) OVERLAPS 3K9ZG api.rs — must NOT dispatch NZ3C9 until 3K9ZG merges (disjoint-scope discipline).
- [2026-07-07 Tue] TASK-3K9ZG (1/10, security) DONE + MERGED e44d946 (worker d7d3337, implementer-claude-rmux sonnet-5 high, run-20260707T090625, ~17.5m; --no-ff, crates-only, live .orgasmic untouched). Shared validate_art_id() in orgasmic-core (NodeIdClass::Artifact + ART-<5-char-Crockford> grammar, minter/validator consistency test) + require_valid_art_id() guarding ALL 5 Path(art_id) routes (get_artifact/submit/add_comment/comment_resolve/regenerate; generate excluded — mints server-side) before artifact_dir() join; house-style errors (shape + mint cmd). id mint --class artifact added; artifact submit --help documents the shape. Worker also repaired 12 pre-existing non-grammar fixture ids that would now 400. Manager INDEPENDENT gates on merged main: core 136 lib, daemon 262 (incl artifact_routes_reject_traversal_art_id_and_write_nothing driving all 5 routes), clippy --workspace --all-targets -D warnings clean, fmt --all --check clean. implementer.done tx-1526. Reviewer flagged residual: NodeIdClass::Artifact new variant -> any non-exhaustive match over it elsewhere would silently misroute (worker grepped, only 2 exhaustive matches exist, both updated). LIVENESS -> e44d946. Reviewer pass dispatched (run-20260707T093750, opus-4-8 xhigh, security-focused: sink completeness + validator false-accepts).
- [2026-07-07 Tue] TASK-3K9ZG (1/10) reviewer pass CLOSED (reviewer-claude-rmux opus-4-8 xhigh, run-20260707T093750, ~4m; reviewer.done tx-1535) VERDICT SHIP zero blocking findings — all 6 security vectors CLEARED (sink completeness across 25 artifact_dir refs, validator false-accepts via is_minted_stem len==5+Crockford+letter, decode ordering %2e%2e%2f rejected, write-nothing precedes all side effects, 12 fixture repairs intent-preserving, no non-exhaustive match swallows Artifact; cid safe). LOW-1 unreachable (node_class_for_id id_repair.rs:45 lacks ART- arm — no fix warranted, noted). 3K9ZG -> DONE (had to task update --state done: reviewer dispatch flipped IN_REVIEW->IN_PROGRESS, known nit). BACKLOG-HYGIENE note (pre-existing, out of scope): CLI /feedback endpoints (artifact.rs:177,197) point at renamed daemon /comments route -> 404; artifact feedback via CLI is dead until repointed — candidate follow-up.
- [2026-07-07 Tue] TASK-F9N5F now fully DONE — fix subtask TASK-NZ3C9 landed + merged a4b2e90 (worker eb4f9e4, implementer-claude-rmux sonnet-5 high, run-20260707T094408, ~6.5m). Both manager rulings implemented exactly: RULING 1 stall_timeout_secs=(Artifactor&&rmux).then_some(0).or(worker.stall_timeout_secs) at spawn_worker_run (covers launch_artifact_generation via shared gate); RULING 2 idle keyed off last_input_at.max(last_driver_event_at) in timed_out_run. 3 new teeth-tests (stall_disabled_survives_old_stall_point, idle_release_requires_both_clocks_stale, idle_release_frees_lease_production_shaped) + 2 pre-existing F9N5F tests updated to both-clocks semantics. MANAGER INLINE REVIEW (one-round fix subtask): diff matches rulings, Some(0)=disabled established sentinel (post_manager_launch:2113). Gates on merged main: daemon 265 lib+integration green, clippy --workspace -D warnings clean, fmt clean. NZ3C9 implementer.done tx-1543; both F9N5F + NZ3C9 -> DONE. Residual (accepted): RULING 1 api.rs gate lacks an api.rs-level test harness (same gap as original idle condition; verified by inspection). The idle-timeout feature is now FUNCTIONAL (was inert on first merge). LIVENESS -> a4b2e90.
- [2026-07-07 Tue] TASK-JJ9RD (2/10) DONE + MERGED e43f9d2 (worker 6835fe6, implementer-claude-rmux sonnet-5 high, run-20260707T095945; --no-ff, crates-only). Fixes the worst battle-test bug (silent wrong-file config write). NodeKind single-source registry in orgasmic-core (new node_kind.rs: ALL/as_str/parse + round-trip/reject/distinct unit tests); bidirectional NodeKind<->NodeLayer From impls; NodeLayer::Config (kind-scoped -> config.org, for_id cannot infer it); reject_misrouted_config_key in post_org_node_edit pre-write op loop (layer!=Config && key in {TEST_CMD,LINT_CMD,BUILD_CMD,PIPELINE} -> 400 naming --kind config, writes nothing) covering BOTH prop-set and body-set via the single node-edit write path; CLI --kind clap enum + parity test cli_kind_arg_matches_daemon_registry. MANAGER INLINE REVIEW no findings (guard is pre-write on the sole write path, anti-leak teeth-tested). Gates on merged main: core node_kind green, daemon 267 (org_node_config_layer_round_trip + reserved_key_rejected_on_project_layer), cli parity green, clippy --workspace -D clean, fmt clean. implementer.done tx-1554. LIVENESS -> e43f9d2. WORKER REPORT LOST (no last.txt AND no stdout.log; [run-gone] abnormal after commit) — B05AM/567JG-class persistence RECURRENCE despite both prior fixes; verified everything independently. Adaptation: keep verifying gates manually + prefer manager-inline review for correctness-critical tasks so a lost reviewer report cannot drop findings.
- [2026-07-07 Tue] TASK-4T80X (3/10) DONE + MERGED 0d7baf1 (worker impl d957f3e, implementer-claude-rmux sonnet-5 high, run-20260707T102649). Write-time ref-prop validation (index-poisoning class, battle-test F4). Shared core helper orgasmic_core::unresolved_reference_tokens (REFERENCE_PROPERTY_KEYS vocab + tokenize_property, reused by index lint so no drift); reject_unresolved_reference_token called PRE-WRITE in BOTH create_graph_heading + post_org_node_edit; house-style error names property+token+space-separated-ids+--force; generic --force escape hatch added to CLI create/prop-set (none existed). 4 teeth tests incl the battle-test regression (glossary_create_rejects_prose_relates_to_at_write_time). MANAGER INLINE REVIEW (report present but instability): guard pre-write both sinks, write-nothing, no PARENT-contract regression. Gates on merged main: core 139+29+29, daemon 271, cli green, clippy --workspace -D clean, fmt clean. NOTE: worker finished gates-green but its rmux session STALLED at the commit prompt -> manager-committed d957f3e (new failure mode: 3rd dispatch this session with a report/commit anomaly). RESIDUAL (worker-flagged, FILED as TASK-KSD6D high): mutate_graph_heading (decision/arch/glossary revise --property) + task-create (DEPENDS_ON/IMPLEMENTS) sinks still bypass the guard — reuse the same helper. LIVENESS -> 0d7baf1.
- [2026-07-07 Tue] TASK-GERBB (4/10) DONE + MERGED 8be740b (worker 5abe7c3, implementer-claude-rmux sonnet-5 high, run-20260707T114437). Battle-test F1 (fresh project 404s until restart) fixed: watch-registration threaded via a WatcherCommand CHANNEL drained by a separate command_loop (RE-ENTRANCY-SAFE — refresh path never re-enters the watch event loop); refresh diffs loaded_before vs refreshed_board -> watch_project + load for new ids; diagnostic 404 on the 4 task-CRUD not-found sites naming the reindex/restart fix. End-to-end integration test board_refresh_live_loads_freshly_registered_project (boots real daemon, simulates project init file-write, proves task update works with no restart + a 2nd edit proves the watch registered) + 2 diagnostic-404 unit tests. MANAGER INLINE REVIEW (channel pattern verified no-deadlock). Gates on merged main: daemon 273 lib+integration, clippy --workspace -D clean, fmt clean. Report + commit BOTH clean this dispatch (commit-first not yet needed). Residual (minor, candidate follow-up): diagnostic 404 on 4 of ~11 sites; add_project POST route doesnt call watch_project for API-added projects. LIVENESS -> 8be740b. QUEUE: 4/10 done (3K9ZG,JJ9RD,4T80X,GERBB) + F9N5F/NZ3C9; DHYXQ (5/10) now UNBLOCKED (deps JJ9RD+GERBB both done).
- [2026-07-07 Tue] TASK-DHYXQ (5/10, capstone) DONE + MERGED ef2b213 (worker 5531b72, implementer-claude-rmux sonnet-5 high, run-20260707T123437). One CLI-only e2e test greenfield_bootstrap_e2e_through_first_artifact (+501 lines bootstrap_smoke.rs, flock-guarded isolated daemon): project init -> daemon-minted decisions -> glossary/arch resolving refs (4T80X) -> config.org via --kind config (JJ9RD) -> post-init task create/update no-restart (GERBB) -> artifact mint+submit (3K9ZG); asserts parse_errors==0 + lawful CLI write path per scaffolded file; F1/F7/F2 each have a fail-on-reintroduction assertion. No real bootstrap bug found (one test-harness placeholder issue fixed via real shipped tree, +2 lines common/mod.rs, no production src touched). MANAGER INLINE (test-only). Gates on merged main: bootstrap_smoke pass standalone 3.12s, full cli suite green (flock holds), clippy -p orgasmic-cli -D clean, fmt clean. COMMIT-FIRST PROTOCOL VALIDATED: worker committed 5531b72 FIRST then stalled at the report-print step -> commit durable, summary recovered from session transcript. Report last.txt lost again but the stall moved from unrecoverable(commit)->recoverable(report), proving the Tier-1 guard. LIVENESS -> ef2b213. QUEUE 5/10 done (3K9ZG,JJ9RD,4T80X,GERBB,DHYXQ) + F9N5F/NZ3C9.
- [2026-07-07 Tue] FILED (backlog, NOT in active queue): dec_3M7M0 (worker-driven dispatch finalization arch decision) + TASK-WFW1N (implement orgasmic dispatch finalize verb, MOTIVATED_BY dec_3M7M0) — the rmux-finalization rearchitecture from the operator discussion. Also earlier: TASK-KSD6D (4T80X residual sinks). GOAL parse_errors NOTE: global count shows 2 but both are PRE-EXISTING comma-separated RELATES_TO danglers in the ORSL project glossary (/lynx/orsl/orsl), NOT orgasmic — orgasmic project is clean. The goal reached-when parse_errors-0 must be read per-project (orgasmic=0); V8WY9 will make that measurable.
- [2026-07-07 Tue] TASK-V8WY9 (6/10) DONE + MERGED 92795fe (worker e0af7e6, implementer-claude-rmux sonnet-5 high, run-20260707T125819). Parse-error observability (battle-test F5): status --errors groups by project/file/node/property/reason off the existing /graph/parse-errors; reindex + reindex --project verbs (POST /reindex, /reindex/:project -> rebuild()/refresh_project(), 404 unknown, returns fresh per-project counts, NO restart); WARN dedup keyed (path,message) per pass. doctor --project skipped (and/or; status --errors covers all criteria). Full CLI e2e test reindex.rs (status --errors->fix->reindex->status --errors). MANAGER INLINE: dedup collapses only true exact-dup double-counts (clean project stays 0, goal check safe). Gates on merged main: daemon 280, reindex e2e pass, clippy --workspace -D clean, fmt clean. COMMIT-FIRST held. LIVENESS -> 92795fe. QUEUE 6/10 done (3K9ZG,JJ9RD,4T80X,GERBB,DHYXQ,V8WY9). NOTE: status --errors now exists to attribute the orsl comma-ref parse_errors per-project once deployed.
- [2026-07-07 Tue] TASK-MTB56 (7/10) DONE + MERGED d63828a (worker f43c3cc, implementer-claude-rmux sonnet-5 high, run-20260707T? ). 5-part ergonomics batch all landed: F3 word-boundary glossary marker guard + --allow-marker; F12 CompactMutationResponse default (compact_or_full_response, ?json/--json full) on task update + node body/prop set; F13 found+fixed a GENUINE repro (print_decision_outline empty-print) + no-empty-silent guarantee; F14 --ids porcelain on task/glossary/decision/architecture list; F11 post_goal_set -> sync_handoff_goal_id. Tests: list_output.rs flock-guarded + integration + units. MANAGER INLINE (F12 escape hatch verified, existing tests updated). Gates on merged main: daemon 283, cli green, clippy --workspace -D clean, fmt clean. COMMIT-FIRST held. LIVENESS -> d63828a. QUEUE 7/10 done (3K9ZG,JJ9RD,4T80X,GERBB,DHYXQ,V8WY9,MTB56).
- [2026-07-07 Tue] TASK-SPBTA (8/10) DONE + MERGED 39015c6 (worker c07e096, implementer-claude-rmux sonnet-5 high, run-20260707T140347). Contract discoverability (F9+F10): artifact blocks --full points at the block contract + artifact submit --help names it (pragmatic pointer path); architecture|decision|glossary schema verbs (no daemon) print a canonical node from new core/schema_examples.rs. SYNC INVARIANT enforced at COMPILE TIME (schema change without matching example fails to compile) + 3 parity round-trip tests. MANAGER INLINE. Gates on merged main: core 142 lib incl 3 parity, cli green, clippy --workspace -D clean, fmt clean. COMMIT-FIRST held. LIVENESS -> 39015c6. QUEUE 8/10 done (3K9ZG,JJ9RD,4T80X,GERBB,DHYXQ,V8WY9,MTB56,SPBTA). ONLY A22TB (9/10, LOW/LARGE UI renderer polish) remains.
- [2026-07-07] TASK-A22TB (9/10, FINAL) — artifact renderer fidelity polish, merged 9ee034f (impl 41388ef). Canvas x/y board + Connector/Annotation wrappers; Wireframe icons via lucide-react (sanitize.ts, security-preserved: post-sanitize, fixed allowlist, trusted markup); AnnotatedCode margin bubbles; Mermaid from --* tokens both themes; rough.js data-rough coverage. Gates on merged main: tsc clean, vitest 154/154 (20 files), build ok. Report lost (rmux); verified independently. QUEUE NOW DRAINED (todo/in_progress/in_review empty; 10/10 done or cancelled).
- [2026-07-07] TASK-MS6N0 (manager verification-fix, committed 22efacc directly on main) — goal reached-when 3x-workspace run surfaced a residual SJQ9V-class flake: daemon_lifecycle.rs test mod had a PRIVATE env_guard/ScopedEnv guarding process-global ORGASMIC_DAEMON_URL, not excluding daemon_client/doctor reads → run 2/3 flaked daemon_client::explicit_daemon_url_prefers_token_env_over_home_token (home-token vs env-token). Fixed by using shared crate::test_support::{env_guard, ScopedEnv}. fmt/clippy clean; both modules green. Re-running workspace x3.
- [2026-07-07] TASK-WFW1N DONE — worker-driven dispatch finalize (orgasmic dispatch finalize), implements dec_3M7M0. First run STALL-KILLED pre-commit; manager rescued the compiling production surface as WIP cd6a211 (tag wfw1n-wip-cd6a211), re-dispatched continuation which finished tests + 7 prompt-spec files (4aed8c2). Merged 679da43. Adversarial review (in-process agent, not rmux) = fix-then-ship: 3 findings FIXED in 016d01b — #1 HIGH orphan branch keyed only on stall string, now covers all 3 timeout reasons (idle/max-duration/stall; artifactor-rmux idle path was silently synthesizing last.txt); #2 emit terminal tx LAST (after last.txt+release) so a mid-sequence failure can't leave a done-lie; #3 --run-id now guarded to matching task/project. New tests: idle/max-duration orphan + is_timeout_release_reason unit. Gates green (fmt/clippy/daemon/cli; dispatch.rs 21/21). Residuals filed TASK-DWJVH (live rmux prompt-spec smoke at publish time, #4 caller-identity auth LOW, #5 finalize/stall race LOW, --status blocked test). NOTE: not deployed — running daemon still 0.0.8-nightly.20260706; finalize verb goes live only on a runtime publish.
- [2026-07-09 Thu] TASK-Z4D3Q DONE (manager-inline, UI-only): operator-directed UX psychology pass. Actionable empty states with CLI next-step hints (Decisions no-data vs no-match split + Show-all reset; Architecture/Glossary query-aware; Tasks create/goal hints); GenerateArtifactDialog subject-shaped prompt suggestions + draft preserved across close (clears only on successful submit, teeth-tested); RegenerateArtifactDialog same draft protection (onSubmit now Promise<boolean>); Artifacts page prompt-only Generate header action + dialog; BoardRouteView first-run card is a 2-of-3-done setup checklist (goal gradient); done-progress bar+count on Tasks toolbar and ProjectView header; update surfaces lead with release notes (Settings available-panel + AppShell toast first note line). Contrast-effect principle skipped deliberately (no pricing surface; conflicts with product baseline). Gates: ui tsc clean, vitest 160/160 (21 files, 6 new tests), vite build green. SOURCE UNCOMMITTED: working tree also carries pre-existing readable-art-id WIP (validate_art_id_readable daemon+UI artifact-not-found state) sharing ArtifactView.tsx/api.ts — one mixed tree, commit split is operator call. Leftover clean task-qkq3r worktree also prunable.
- [2026-07-11 Sat] Reconciled `595c642..d290da8` (19 commits). Landed the runtime-served entry/workflows redesign (TASK-74TA6, TASK-CZTWD, TASK-3YMVC, TASK-A8559, TASK-PCDA2, TASK-KWD80), the Codex tmux/rmux placeholder fix `a7480ee`, the repo migration `c47a855`, the bounded Docker publish probe, stable runtime publishes through 0.0.11, the archived cockpit report, and the committed UI/artifact WIP snapshots. Re-verified and closed stale TASK-70VV1 and TASK-VBZ24 with current acceptance evidence. Repo is now 0.0.12-dev at `d290da8`.
- [2026-07-16 Wed] Reconciled liveness drift d290da8..04a130a (20 commits, outside tracked manager sessions): TASK-VKP5T external-manager self-registration via orgasmic CLI (driver 'external' run, ORGASMIC_RUN_ID export, register/release verbs, external-run UI guards through the fix/rereview rounds; d517587..04a130a) — now DONE in done.org. RunDock taskbar redesign (3ec334c: per-terminal leases, tab close, agents-only menu). Five dispatch tasks landed and merged: WYSMM (mobile selection-comment quote), TJF7G (interactive artifact Q&A threaded answers), VJNJ2 (stale-401 first-login fix), H5X52 (nightly->newer-stable adoption), E7W7Q. TASK-P0FAQ filed (adopt Vercel AI Elements for the run transcript chat UI; RELATES_TO VKP5T, dec_FBBT2, dec_RDHX7). Housekeeping this session: pruned 4 orphan dispatch worktrees (all completions on main), committed daemon-written project state. LIVENESS bumped -> 04a130a. Still no active goal.
[2026-07-16 Wed] TASK-P0FAQ DONE (adopt Vercel AI Elements for the run transcript chat UI) — operator-directed resume, dispatched via codex gpt-5.6-sol xhigh. Added user-override worker allowlists ~/.orgasmic/user/workers/{implementer,reviewer}-codex-acp.org (gpt-5.5 -> gpt-5.6-sol default, gpt-5.5 fallback) so the live daemon accepts the operator's configured codex model; repo shipped/workers/ copies STILL pin gpt-5.5 (flagged for next runtime publish). Implementer merged 7864370 (vendored AI Elements conversation/message/tool/reasoning/code-block; pure ui/src/lib/transcriptParts.ts normalizer; ManagerChatTranscript rewired; RunSurface untouched; AI Elements 1.9.0 dropped standalone 'response' -> MessageResponse from 'message'). reviewer-codex-acp (gpt-5.6-sol xhigh) VERDICT NEEDS-CHANGES, 6 findings (guards + XSS + gates all PASS) — sharpest: 64/131 tool cards stuck 'running' on the REAL implementer session because synthetic tests masked the true Codex exec/command_execution event shape. Fix subtask TASK-K7P24 (impl c6518a8, merged) resolved all 6 per manager rulings (terminal-close to completed/error on run_complete/run_fail/release; collision-safe full-content code-block cache + LRU; Streamdown @source + KaTeX CSS + semantic-token fallbacks; out-of-order call_id pairing; reduced-motion scroll; empty tool-error output) with a fixture SLICED FROM THE REAL session jsonl; MANAGER INLINE REVIEW (one-round fix) = ship (verified terminal-state logic + cache key in the diff, not just green tests). Final gates on merged main: tsc clean, vitest 253/253 (35 files, +11), VKP5T guard suite 45/45 (guard sources byte-unchanged), build ok (5694 modules). First codex-acp dispatches this session; worker-driven finalize (dec_3M7M0) held clean on all 3 (last.txt intact each time). NOT deployed — UI ships with the next runtime publish. Backlog now EMPTY. LIVENESS -> e0e8b0e.

** Next likely actions
PAUSED — no active goal; awaiting operator direction. Backlog is now EMPTY (TASK-P0FAQ drained this session); todo/in-progress/in-review empty.
- Optional: bump the checkout shipped/workers/{implementer,reviewer}-codex-acp.org gpt-5.5 -> gpt-5.6-sol so the codex model modernization ships with the next runtime publish. Currently only the ~/.orgasmic/user/workers/ overrides carry it (live-daemon only); a runtime publish from the checkout would otherwise revert the live daemon to gpt-5.5.
- Optional deeper cleanup: .orgasmic/tmp/dispatch/ holds many stale brief files + stem dirs from long-closed dispatches (incl this session's task-p0faq/, task-p0faq-review/, task-k7p24/).

** In flight
(none) — no live runs, open/orphan dispatches, or extra worktrees (all 3 session dispatches closed; worktrees removed). backlog/todo/in-progress/in-review all empty.

** Divergence / Risks
- No active goal since [2026-07-11]; manager paused.
- Codex worker model bump (gpt-5.6-sol) lives ONLY in ~/.orgasmic/user/workers/ overrides; the repo shipped/workers/ copies still pin gpt-5.5 — a runtime publish from the checkout would revert the live daemon to gpt-5.5 unless the shipped copies are bumped first.
- Runtime: daemon live on 4848 at 0.0.12-nightly.20260713; repo HEAD e0e8b0e. The P0FAQ transcript UI change is NOT deployed (ships with the next runtime publish). parse_errors 2 are the external ORSL comma-refs (orgasmic project itself clean).
- User-owned untracked paths ui/.design-sync/ and ui/.gitignore remain untouched.
