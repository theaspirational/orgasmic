#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         3abc21497edfeced66e380f89679f5e55e9f010a
:LIVENESS_AT:      [2026-08-05 Wed]
:UPDATED_AT:       [2026-08-05 Wed]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-08-03/05, continuing the 08-02 session. Three merges landed and
were reviewed, the architecture-layer excision was decided and started, and two
recorded "fixes" were disproved by measurement before anyone built them.

THE FOUR THINGS TO KNOW BEFORE YOU DO ANYTHING

1. THE INSTALLED RUNTIME IS 36 COMMITS BEHIND MAIN, AND THAT IS NOW A TRAP WITH
   TEETH. =~/.orgasmic/bin/orgasmic= runs =05e1813= (built 2026-08-03T03:19Z).
   TASK-XCJYC renamed the transport modes on main — =acp-stdio= -> =stdio=,
   =acp-ws= -> =ws= — but YOUR CLI STILL ONLY ACCEPTS THE OLD NAMES. Verified:
   =orgasmic manager drivers= prints =acp-stdio=/=acp-ws=. So dispatch with
   =--mode acp-stdio=, NOT =--mode stdio=, until the runtime is rebuilt, even
   though the repo says otherwise. =--mode tmux= and =--mode rmux= are
   unaffected and are what this session actually used.

2. NEVER SET =ORGASMIC_HOME= INSIDE A DISPATCH, FOR ANY =orgasmic= INVOCATION.
   =ensure_running= -> =start_via_selected_adapter= -> =daemon_service::start=
   writes and loads the PER-USER LaunchAgent plist, which =ORGASMIC_HOME= does
   not scope; a staged home restarts the real daemon and kills every live
   dispatch. Five workers died this way. The safe pattern is =ORGASMIC_DAEMON_URL=
   on the CHILD. =env -u ORGASMIC_HOME= to strip a leaked value is the opposite
   and is fine. TASK-48SFW carries the source chain. Note the asymmetry recorded
   there: BRIEFS keep the blunt blanket ban, because a worker cannot tell which
   verbs open a connection and the failure is total and silent; the CODE fix
   targets the precise rule at =ensure_running=.

3. A PASSING GATE PROVES LESS THAN IT LOOKS, IN TWO SPECIFIC WAYS.
   - =orgasmic verify=: a pass proves the red HAPPENS, not that it is the RIGHT
     red. For a RE-CUT artifact, read the patch and confirm the pinned message is
     the FIRST failing assertion.
   - The daemon suite has NO TRUSTWORTHY FULL-RUN SIGNAL on this host. Under
     load =syspolicyd= bursts (measured 586% CPU, load 11.41, suite 8-15x
     slower); the 2026-08-02 run showed 14 failures that ALL passed alone in 8s
     against the same binary. The cheap control is to re-run each failure alone
     against the ALREADY-BUILT binary before believing any red:
     =env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME target/debug/deps/orgasmic_daemon-<hash> --exact <test> --test-threads=1=
     TEST EXECUTION is the trigger, not compilation. This machine has hard-
     rebooted twice from that chain, so DO NOT loop the full workspace suite.

4. TWO RECORDED DECISIONS ON THIS BOARD WERE DISPROVED BEFORE BEING BUILT, and
   both are on TASK-3X5AQ with reproductions. Do not implement either.
   - SHARED =CARGO_TARGET_DIR= IS UNSOUND: two checkouts share one cargo
     fingerprint dir, so a worker's =cargo build= reports =Fresh= without
     compiling and =CARGO_BIN_EXE_*= hands its integration tests ANOTHER
     CHECKOUT'S BINARY — a green suite that never ran your code.
   - STABLE WORKTREE SLOTS DO NOT FIX THE TCC PROMPTS: a grant for an
     ad-hoc-signed binary is pinned to its CDHASH, and a rebuild at the SAME
     path changes it (measured 16237ab -> 6cdc20d). Slots would move the prompt
     from once-per-dispatch to once-per-BUILD.
   The surviving fix is TASK-M47E5: move worktrees OUT of TCC-guarded dirs.

** Done so far
SESSION 2026-08-03/05 (this handoff's session). Three merges, one decision, five
filings, two disproofs.

MERGED
- =d54fba5= TASK-XCJYC — the acp-* rename cutover. Modes name the WIRE
  (=stdio=/=ws=), the harness decides the protocol. =acp-stdio=/=acp-ws=/=acp=
  are RESERVED and a test fails if any reappears in =MODES=, =SUPPORTED= or
  =TRANSPORTS=. Fixed one real bug (=protocol_version "acp/1"= on the claude
  adapter's simulated path). Added glossary term =term_YX8AG= distinguishing
  ACP, MCP and orgasmic's modes. History is not rewritten: 70 persisted runs
  keep =transport: "acp-stdio"= and still resolve, pinned by three tests plus a
  reviewer's LIVE probe that resolved a real pre-rename record to its Codex
  rollout. Reviewed by codex/gpt-5.6-sol: REJECT on one doc-comment criterion,
  fixed in =dd494ab=.
- =eb2ba3b= TASK-RQ270.1 — architecture excision STAGE A. Retired =POST
  /architect=, =POST /architecture*=, =stage_spec("architect")=, the =architect=
  and =architecture create= verbs, =DispatchEndpointKind::Architector= and
  =DispatchKind::Architector=, plus the architector prompt spec, context pack
  and decomposition convention. -603/+319. Persisted "architector" ledger rows
  still parse (two tests). =GraphLayer::Architecture= went too — forced, not
  chosen: with the POSTs gone it was dead code and =-D warnings= rejected it.
- =6ead5c5= TASK-YN5FJ — review can precede merge. A reviewer may now dispatch
  against a REPORTED-but-open implementer generation (same-kind and unreported
  overlaps still refuse), and =--merge-sha= must resolve, be a merge commit, and
  contain the worker commit. A default-branch close without a linked reviewer
  verdict is refused, escapable by =--no-review-required --reason= which records
  =NO_REVIEW_REQUIRED=true=. Manager-reviewed personally: APPROVE WITH
  FOLLOW-UPS, one confirmed P1 filed as TASK-YN5FJ.1.

DECIDED
- =dec_HBK6A= — excise the architecture layer; =decisions.org= is the single
  durable rationale model, code is the authority on structure, tasks carry the
  work graph. Passes TASK-XYYBT's operator gate, whose measured report
  (=archive/TASK-XYYBT-architecture-excision-estimate.md=) remains the authority
  on the numbers.

FILED
- TASK-RQ270 (P1) architecture excision, stages A-E; TASK-RQ270.1 done.
- TASK-YN5FJ.1 (P1) the review gate's refusal names a remedy that does not
  satisfy it.
- TASK-M47E5 (P1) move worktrees out of TCC-guarded dirs, plus a prune.
- TASK-YN5FJ (P1, now done) the forcing chain itself.

DISPROVED BY MEASUREMENT, BEFORE ANYONE BUILT THEM — the highest-value work of
the session, recorded on TASK-3X5AQ with reproductions.
- Shared =CARGO_TARGET_DIR=: cargo's =-C metadata= omits the checkout path, so
  two checkouts share ONE fingerprint dir; a worker's build reports =Fresh=
  without compiling and its integration tests run ANOTHER checkout's binary.
- Stable worktree slots: TCC grants for ad-hoc-signed binaries are pinned by
  CDHASH, and a rebuild at the same path changes it. Slots would make the
  prompt fire once per BUILD instead of once per dispatch.

CORRECTED
- TASK-STWVB: not blocked (TASK-8W30B is done), never dispatched, title
  disproved three times. Retitled, banner added, moved =in_progress= -> =todo=,
  and given a concrete fix design.
- TASK-3X5AQ: retitled and bannered so its disproved decision cannot be
  implemented by a skim-reader.

** Next likely actions
ASK THE OPERATOR FOR DRIVER/HARNESS/MODEL/EFFORT AT SESSION START. There is no
project default and nothing enforces resolution (=--model= > task drawer >
harness default, and the harness default wins SILENTLY — TASK-A55J7). This
session ran implementers on =claude / claude-opus-5 / high= and reviewers on
=codex / gpt-5.6-sol / high=; that was the operator's choice for THAT session,
not a standing setting. Pass =--model= explicitly and echo
driver/harness/model/effort back after each dispatch.

REMEMBER TRAP #1 IN THE LEAD: dispatch with =--mode acp-stdio=, not =--mode
stdio=, until the runtime is rebuilt. =--mode tmux= is what this session used
throughout and is unaffected.

THE OBVIOUS NEXT MOVES, roughly in order

1. TASK-YN5FJ.1 (P1) — small, well-specified, and it unblocks the reinstall.
   Promote VERDICT to a real =--verdict= flag and make the refusal's remedy
   self-contained. Do this BEFORE rebuilding the runtime.
2. TASK-M47E5 (P1) — worktree relocation + prune. Fully designed on the task
   with the measurements behind every choice; it needs an implementer, not more
   analysis.
3. TASK-RQ270 stages B-E — the excision. Stage B (drift comparator, ~-550 LOC)
   is the cheapest and is independent. Mint =TASK-RQ270.N= subtasks per round,
   the way stage A was done.
4. TASK-STWVB — the only todo. Its design section splits into (A) stop the mux
   suites writing-and-exec'ing a fresh shim script per test, which is what
   bursts =syspolicyd=, and (B/C) teach =scripts/run-tests.sh= that host load is
   an input so a degraded run returns INCONCLUSIVE rather than red — and so a
   degraded GREEN stops being silently trusted. A and B/C are separable rounds.

WHEN YOU DO REBUILD THE RUNTIME — and after YN5FJ.1 lands, you should, because
the repo is 36 commits ahead — the recipe and its four traps are in the
=orgasmic-runtime-reinstall= memory. In short: a zero-worker window is
mandatory, =install.sh= silently skips the unpack if the version dir already
exists, its trailing =orgasmic status= auto-spawns a competing daemon
(=ORGASMIC_INSTALL_SKIP_STATUS=1=), and you MUST codesign
(=ORGASMIC_CODESIGN_IDENTITY="orgasmic dev codesign"=) or the binary loses its
TCC grant. Verify behaviourally, not by version string: both builds call
themselves =0.0.15-dev.1=. A good new-code probe this time is =--mode stdio=
being accepted, which is exactly what fails today.

HOUSEKEEPING WORTH A FEW MINUTES
There are ~40 merged =task-*-impl= branches still on the repo. They cost
nothing but they make =git branch= useless for seeing what is live. Pruning
merged ones is safe and unclaimed by any task.

** In flight
NOTHING RUNNING. =dispatch-status= is empty, =git worktree list= shows only the
repo root, no tmux server is up, the tree is clean at =3abc214=, and nothing has
been pushed (main is 541+ commits ahead of origin — pushing is an operator
decision, never a manager one).

THE BOARD IS SMALL AND HONEST NOW
- TODO (1): TASK-STWVB (P2) — the suite's full-run signal. Design is recorded ON
  the task; see below.
- IN_PROGRESS: empty. IN_REVIEW: empty.
- BACKLOG: 86.

TASK-STWVB WAS CORRECTED THIS SESSION AND ITS OLD FRAMING IS DEAD. It sat in
=in_progress= for days having NEVER been dispatched (no =manager.dispatch_started=
exists for it), and it was NOT blocked — its blocker TASK-8W30B is done. Its
title claimed "four daemon tests fail on every full-suite run", which three
successive re-measurements disproved. It now carries a READ-THIS-FIRST banner and
a design section. Note the dependency on 8W30B was only ever written in PROSE —
=depends_on= was empty — so the board could never have shown it blocked. Worth
watching for that shape elsewhere.

THE HIGHEST-PRIORITY BACKLOG ITEMS, and two have ordering constraints
- TASK-YN5FJ.1 (P1) MUST LAND BEFORE THE NEXT RUNTIME REINSTALL. TASK-YN5FJ
  added a gate refusing an implementer close on the default branch without a
  reviewer verdict — but the verdict is only settable via the generic
  =--property VERDICT=...=, no flag exists, and NO reviewer close in this
  project's history has ever written one. So the refusal's stated remedy does
  not clear the refusal. It is harmless today ONLY because the gate is not live
  until the runtime is rebuilt.
- TASK-M47E5 (P1) — move dispatch worktrees to
  =~/.orgasmic/worktrees/<project-id>/<task-id>/= AND give them a prune. The
  prune is part of the same task on purpose: today a stray worktree sits in the
  repo where =git status= shows it, so after the move the same leak becomes
  silent multi-GB accumulation.
- TASK-RQ270 (P1) — architecture-layer excision, stages B-E remain (stage A
  landed). dec_HBK6A records the ruling; the measured plan is in
  =archive/TASK-XYYBT-architecture-excision-estimate.md=. ONE ORDERING
  CONSTRAINT IS LOAD-BEARING: the dangling-edge lint carve-out lands in stage C
  and must be on main BEFORE stage D deletes =architecture.org=, or done.org's
  231 =:IMPLEMENTS: arch= tokens start failing the lint.
- TASK-3X5AQ (P1) is now a MEASUREMENT RECORD ONLY. Its work moved to M47E5.
  Keep it for the two disproofs; do not implement from it.
- Also P1 and untouched: TASK-4DTBZ (rmux dies silently), TASK-BX5SR (one bad
  mutation kills the daemon writer while reads stay healthy), TASK-A55J7
  (nothing persists model/effort), TASK-48SFW, TASK-ADHWZ, TASK-NTNK0.

WHAT LANDED THIS SESSION
- =d54fba5= TASK-XCJYC — transport modes name the wire; =acp-*= reserved and
  ENFORCED by a test; one real bug fixed (=protocol_version "acp/1"=); glossary
  term =term_YX8AG=. Reviewed (REJECT on one doc-comment criterion, fixed in
  =dd494ab=). =cursor-acp= KEEPS its name by manager ruling — it really does
  speak ACP, and =cursor-agent= is already a live legacy transport id.
- =eb2ba3b= TASK-RQ270.1 — excision stage A. No route, verb or dispatch kind can
  create architecture content or spawn an architector; persisted "architector"
  ledger rows still parse, with two tests.
- =6ead5c5= TASK-YN5FJ — a reviewer can now dispatch against a REPORTED-but-open
  implementer generation, so review can precede merge; =--merge-sha= is verified
  evidence rather than a free string. NOT LIVE until reinstall.

EVERY BRIEF is under =.orgasmic/tmp/= and they are the best templates available.
=brief-xcjyc.md= (a cutover whose old names are persisted in history),
=brief-rq270-1.md= (a subtraction with an explicit not-in-scope list),
=brief-yn5fj.md= (naming a concurrent adjacent-hunk collision so the worker
avoids it), =brief-fzb6t5-fix.md= (constraining a fix round that grows its own
surface), =brief-vegrv-continue.md= (stating a prohibition beside a task that
needs what it forbids).

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO AGAIN — this REVERSES the previous handoff, which
said "THE RUNTIME IS NOW CURRENT". Installed =05e1813=, main is 36 commits
ahead. See trap #1 in the lead: the mode rename is the part that bites.

A DISPATCH CAN STILL KILL EVERY OTHER DISPATCH via =ORGASMIC_HOME= (lead item
2). Unchanged, and it is the single most expensive mistake available.

RMUX DIES SILENTLY AND TAKES EVERY PANE WITH IT (TASK-4DTBZ, P1): three pid
cycles in one session, no log since 2026-07-26, no crash report; it killed three
workers at once. TMUX IS THE BETTER DEFAULT and is what this session used
throughout. The tmux pane-liveness channel from TASK-4CSMY is now CONFIRMED
REPORTING IN PRODUCTION — measured on the TASK-XCJYC run,
=driver_event type "pane_activity"= with real byte counts — so the
=--governance-json {"stall_timeout_secs":3600}= workaround has been RETIRED
deliberately and later dispatches ran without it. Read =event.type=, never the
serialized line: an earlier grep on the same file matched =driver_config= and
produced a false positive.

THE DAEMON CAN STOP PERSISTING WHILE LOOKING HEALTHY (TASK-BX5SR, P1). One
failing transaction killed the writer task; every write then returned 500 while
=status=, =task get= and =dispatch-status= all answered normally. Detection is
=grep "writer task is gone" ~/.orgasmic/logs/daemon.out.log=. If a write fails
with "failed to apply changes", check the writer BEFORE assuming OCC contention
— that was a previous manager's first and wrong reading.

MERGE STILL PRECEDES REVIEW IN PRACTICE, AND THE FIX IS MERGED BUT NOT LIVE.
=dispatch-close= refuses to close an implementer as done without =--merge-sha=,
and until the runtime is rebuilt a reviewer still cannot dispatch against an
open generation. So expect to merge, then review, then fix in =TASK-<id>.N=
rounds — which is what this session did three times. When you review a
post-merge diff, say so in the brief and tell the reviewer a REJECT is still
actionable, or you will get softened findings.

TWO REVIEW-BRIEFING LESSONS, both paid for this session.
- DO NOT ask a reviewer to mutate source. The reviewer contract is strictly
  read-only, so a brief asking for a negative probe ("break it and confirm the
  guard fires") returns an unrun gap, correctly. The IMPLEMENTER runs negative
  probes; the reviewer judges them.
- A REJECT that the manager then fixes personally is a normal, good outcome. It
  happened on TASK-XCJYC and cost three comment lines.

STATE-HYGIENE SHAPES TO WATCH FOR, both found this session:
- A TASK IN =in_progress= WITH NO DISPATCH is claiming work nobody is doing.
  TASK-STWVB sat that way for days. Check
  =grep "manager.dispatch_started" .orgasmic/tx/*.org= before believing a stage.
- A BLOCKER RECORDED ONLY IN PROSE is invisible. TASK-STWVB said "GATE IS
  BLOCKED BY TASK-8W30B" in its body while =depends_on= was empty.
- A LONG TASK BODY WHOSE OPENING PARAGRAPH IS DISPROVED will mislead every
  skim-reader. This cost a worker a full round on TASK-TJKFC. The fix that works
  is a =READ THIS FIRST — STATUS= block at the TOP plus a corrected TITLE;
  appending the correction at the bottom does not work.

THE INTEGRATION SUITE IS OUTSIDE THE cfg(test) FENCE (TASK-3TQ0R). Every such
test engineers a host-independent credential refusal, so none spawns today, but
the safety is per-test rather than structural — the exact shape of the comment
that caused the 2026-07-30 orphan incident. NEVER run
=legacy_drivers_and_explicit_pairs_emit_equivalent_start_events= (TASK-099T6);
it spawns real billed provider turns.
