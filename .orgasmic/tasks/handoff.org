#+title: orgasmic manager handoff
#+orgasmic_version: 1
#+scope: project

* HANDOFF current :handoff:
:PROPERTIES:
:ID:               handoff-current
:LIVENESS:         f434e7d5d21abc008acfd2219da785254601441c
:LIVENESS_AT:      [2026-07-29 Wed]
:UPDATED_AT:       [2026-07-29 Wed]
:GOAL_ID:          goal-20260728-drive-the-whole
:END:

Session 2026-07-29 (post-reboot rescue + operator direction change). A macOS
reboot killed three live implementer dispatches mid-flight. All three were
rescued, verified and merged; nothing was lost.

THE ONE THING TO KNOW
A dead worker is not lost work. All three worktrees survived the reboot with
their commits intact, and the most valuable single piece — TASK-FZB6T's
RunCatalog async->sync refactor — was sitting UNCOMMITTED in a worktree and
passed its own injection proof once committed. Check =git -C <worktree> status=
and =git log main..HEAD= on every orphaned dispatch before writing anything off.
=orgasmic verify <TASK>= needs a clean tree, so committing salvaged work on the
task branch is the way to put it through the gate; the proof accepts it, not the
fact that it compiles.

WHAT LANDED, all three manager-verified by replayed injection proof (never by
reading the worker's account), each replayed TWICE — once on the task branch
and again on fully-merged main, because all three touched overlapping files:
- TASK-95SGV (bf555ae) — dispatch-cleanup reservations release by a minted
  opaque guard id instead of a worktree key recomputed after cleanup deleted
  the directory. Clean tree, had reported.
- TASK-JQ8AV (4fc57fe) — the stall clock gains a third evidence channel: the
  pane's open-turn statusline, read at the deadline when cpu is below
  threshold. This is the fix for the morning's three false-positive kills.
  Worker never reported; its report.md stops mid-sentence at "results below".
- TASK-FZB6T (1b726f6) — compact run catalog + bounded session retention, plus
  the rescued uncommitted refactor (bf1f8cb) removing a
  =Handle::current().block_on= re-entry from inside =spawn_blocking=. THREE
  verify artifacts, all PASS.

Gates on merged main: fmt clean, clippy -D warnings clean, all five injection
proofs PASS. =cargo test -p orgasmic-daemon --lib= was 617 passed / 7 failed
under a 4-job concurrent load (1m avg 18.8); ALL SEVEN pass in isolation in
under 0.4s each. The known-flake set has grown from the handoff's five to
seven — the two new ones are
=explicit_reattach_{rmux,tmux}_is_fenced_out_of_a_worktree_a_close_guard_holds=,
which sit in TASK-95SGV's blast radius and were checked deliberately for that
reason. Still TASK-STWVB's territory, and still a suite that trains readers to
discount red.

THE MODEL WAS NEVER WHAT ANYONE THOUGHT
All three ran on claude-fable-5, not the intended claude-opus-5, because no
=--model= was passed and the harness default won. The ONLY surviving proof is
the =Co-Authored-By: Claude Fable 5= commit trailer — an accident of the
harness, not a record orgasmic kept. The standing matrix in manager-dispatch.org
already said opus-5/high; nothing enforced it. TASK-A55J7 is promoted to P1 and
widened from reporting to ENFORCEMENT: refuse to dispatch when no model
resolves. The operator explicitly ruled out a project-level default model
(models and preferences both move faster than a checked-in key), so resolution
is exactly --model > task drawer > REFUSE, and the session-start ask
(TASK-0JY7M) is the ergonomic half.

TMUX IS THE NEW DEFAULT, AND IT IS NOT YET SAFE
Operator directive: ship tmux as the default pane mode, because a first-time
user may have only tmux, and dogfooding is the only thing that hardens it. The
standing matrix moved rmux -> tmux (f434e7d). But measurement at integration
found tmux is where rmux was BEFORE TASK-RWCRN: =pane_activity= is emitted by
=modes/rmux.rs= and by nothing else, and TASK-JQ8AV's new rescue is gated on
=transport == "rmux"=. So on tmux BOTH pane channels are blind and a
provider-bound think at ~0% cpu is released at 600s — the original incident,
reproduced on the new default. TASK-4CSMY (P1) covers both gaps and is a SHIP
BLOCKER for the default flip. Until it lands, every long-thinking tmux dispatch
MUST carry =--governance-json '{"stall_timeout_secs":3600}'=; the convention
now says so.

** Done so far
Session 2026-07-26, in order.

LANDED
- TASK-8W30B (d60c4ed, merge d3fae4d): RmuxControl::release aborted its lifecycle
  task before Session::kill, poisoning the shared ordered SDK transport, so every
  rmux run leaked a session, pty and harness process. Leak +8/run -> 0,
  manager-measured. Reviewer then BLOCKed it: cancellation consumed the only
  Session handle before a cancellable await, so Drop could not retry.
- TASK-6FNAY (83ea420, merge 075e017): the round-2 fix. as_ref() instead of
  take(), 2 s SDK + 2 s CLI reap budgets inside the caller's 5 s, an
  endpoint-exact `-S <endpoint> kill-session -t <name>` fallback, and a detached
  run_complete sender that survives cancellation.
- TASK-STWVB round 2 (67e427c, merge c578fcc): one OnceLock-owned warm executable
  per test binary, per-test variation moved into argv, hard links for
  production-shaped paths. Kept, because the mechanism is sound — but the gate is
  NOT met; see In flight.
- Prompt rules (902b240, 3f7121c): every dispatch report must name driver,
  harness, model and effort; and check `orgasmic <group> <verb> --help` before
  inventing a verb. The second was written because a shipped convention taught
  `orgasmic manager drivers`, which has never existed.

MEASURED, and worth not re-deriving
- macOS charges 150-270 ms of Gatekeeper evaluation on the FIRST exec of each
  newly created executable, through one serialized daemon. Warm exec 4 ms. A hard
  link to a warmed file is also 4 ms (the cache key is the inode); a byte-
  identical copy pays 162 ms. And `/bin/sh script` on a fresh file is 5 ms
  against 270 ms for `./script` — the simplest sidestep, and the one to use next.
- The remaining suite pressure is in the rmux test path, not in the converted
  fixtures: syspolicyd burns +281 s of CPU during a 106 s run with rmux present
  and +32 s with it absent. What it is exec-ing has NOT been identified. The rmux
  binary itself is cheap (9.2 ms, 0 ms syspolicyd).

BOARD REPAIR
In Review went 5 -> 3. TJKFC (P0, partial implementation) and STWVB moved back to
in_progress; Z8WEJ and VB9DQ stay in review with their real blocker recorded —
they need a live claude dispatch on a daemon carrying today's main.

CLI DEFECTS FOUND WHILE WORKING, now understood correctly
`--property KEY=VALUE` writes any key verbatim and the schema reads only a fixed
UPPERCASE set (schema.rs:276-286), matched case-SENSITIVELY. So `write_scope`,
`test_cmd` and `priority` in lowercase are stored where nothing reads them, and
the response echoes them back as changed. The earlier note in this session that
uppercase WRITE_SCOPE was "silently dropped" was wrong — uppercase is the form
that works. `task create` has no --priority flag at all, which is how all seven
tasks filed today came out priority None. Tracked on TASK-HXSW0.

** Next likely actions
1. Decide the review posture on the three merged-but-unreviewed tasks
   (TASK-95SGV, TASK-JQ8AV, TASK-FZB6T). Dispatching reviewers over them is the
   efficient move because it also serves item 2 — it puts real work on tmux.
2. TASK-4CSMY (P1) — give tmux a coalesced =pane_activity= and generalise
   TASK-JQ8AV's open-turn consult beyond rmux. SHIP BLOCKER for the tmux
   default flip; until it lands every long tmux dispatch needs the stall
   override.
3. TASK-A55J7 (P1, widened) — refuse to dispatch with an unresolved model, and
   persist resolved model/effort on RunMeta, the dispatch tx, run list and
   dispatch-status.
4. TASK-0JY7M (P1) — ask once per session for agent selection. The operator now
   wants this asked at session start every run, and wants driver/harness/model/
   effort stated back after every dispatch.
5. The runtime on disk is still 0.0.15-dev.1 from dc6db85 and does NOT contain
   any of today's merges. TASK-JQ8AV's stall fix is therefore NOT live for the
   operator's own daemon yet — the governance override remains necessary on
   rmux too until a reinstall. Reinstall only in a zero-worker window.
6. Unrelated and unowned: TASK-1SVST, an app crash to the top-level error
   boundary (=undefined is not an object (evaluating 't.sections.map')=),
   filed through the daemon during this session. TASK-F9A34 was cancelled as its
   duplicate.

** In flight
Nothing running. All three dispatches closed done, all three worktrees removed,
=dispatch-status= is empty and =git worktree list= shows only the repo root.

Three tasks sit in IN_REVIEW, having been merged on manager verification alone:
TASK-95SGV, TASK-JQ8AV, TASK-FZB6T. Each carries an Evidence section saying
plainly "NOT independently reviewed: manager integration review + injection
replay only." Two of them reached main with their author never having reported
at all. That is the open decision for the next session: either dispatch
reviewers over them, or review-close them deliberately — but do not let them sit
in review by default and later read as reviewed.

** Divergence / Risks
THE RUNTIME IS BEHIND THE REPO. =~/.orgasmic/bin/orgasmic= is 0.0.15-dev.1 from
dc6db85 and carries none of today's merges. Everything below about the stall
clock is true of the repo, not of the daemon the operator is running.

THE STALL FIX IS NOT LIVE, ON EITHER TRANSPORT. On rmux it is merged but not
installed; on tmux it does not apply at all (TASK-4CSMY). Treat
=--governance-json '{"stall_timeout_secs":3600}'= as required on every
long-thinking dispatch of any pane mode until BOTH a reinstall and TASK-4CSMY
have happened. The reinstall recipe and its zero-worker precondition are
unchanged from the previous handoff.

TWO SUBSTANTIAL CHANGES REACHED MAIN WITH NO WORKER REPORT AND NO INDEPENDENT
REVIEW. TASK-JQ8AV and TASK-FZB6T were accepted on replayed injection proofs and
green gates alone. The proofs are strong evidence about the specific defect each
one names; they are not evidence about anything else those diffs touch. FZB6T's
rescued refactor is the sharpest case — it changes the run-catalog concurrency
model and its author never described it. If anything in run enumeration, boot
reattach or the session writer misbehaves, start there.

THE KNOWN-FLAKE SET GREW FROM FIVE TO SEVEN. All seven pass in isolation, so the
merges are not implicated, but the standing warning holds and is now worse: a
suite with seven standing flakes trains everyone to discount red, which is how a
real regression gets waved through. TASK-STWVB owns this and is in_progress.

THE PREVIOUS HANDOFF WAS STALE AND ITS LIVENESS IS SUPERSEDED. It described
TASK-1T3FZ, TASK-5HBST and TASK-QRB8S as in flight; none were, and the three
tasks that actually were in flight went unmentioned. Its =:LIVENESS:= pointed at
db071733 while HEAD had moved well past it. Both are corrected here. If a
handoff ever again names workers that =dispatch-status= does not, trust
=dispatch-status=.
** Description
(superseded — this section was created in error; the handoff is the free prose above)
