#+title: orgasmic task TASK-BX5SR.2
#+orgasmic_version: 2

* IN_PROGRESS TASK-BX5SR.2 Make cross-API writer idempotency identity collisions actionable instead of permanent 500s :daemon:writer:
:PROPERTIES:
:ID: TASK-BX5SR.2
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/**,crates/orgasmic-cli/**
:LIVE_DAEMON_PROBE: retained-sync-live-20260813T013019
:END:

** Description
Independent Claude review of TASK-BX5SR traced the historic `cached mutation lacks its recorded identity` error: `WriterHandle::transaction()` caches a matching response with `mutation_id: None`; a later `transaction_mutation()` using the same request id rejects it before enqueueing, maps it to a generic 500, and retries are permanently poisoned for the boot. The previous idempotency task landed on main but does not cover this transaction-to-transaction_mutation domain collision.

Design and implement a correct cross-API idempotency rule. Either preserve enough identity for every cached transaction so the mismatch cannot occur, or return a typed actionable conflict that identifies the colliding request id and operations and tells the caller to mint a new key. Do not silently return an invented mutation id and do not weaken idempotency collision checks.

Trace whether current `manager dispatch-close` can actually share this key path; test the production call chain, not a private writer-command imitation.

** Acceptance
- The historical transaction → transaction_mutation request-id sequence is deterministically covered at its public handle/API boundary.
- Repeating the same request either returns the correct original identity or produces a stable actionable non-500 conflict.
- A rejected collision is request-local; a subsequent independent writer write succeeds.
- Dispatch-close is either proven to avoid this domain collision or corrected atomically without introducing duplicate tx/lifecycle effects.
- Focused writer/API/CLI regressions and independent review pass.
** Evidence
- sprint 2026-09-02 (no dispatch): fix on sprint/BX5SR2 commit 0ea11b09 (cross-API writer idempotency keeps its identity or returns a typed conflict instead of a permanent 500; writer.rs, api.rs, manager.rs). Agent gates rc=0 (cli, clippy, daemon, fmt) in a private target dir. Merged into sprint-20260902 as 4a428912 (--no-ff, clean); final full integration gate running.
- agent root cause (differs from the review framing): the writer LOOP ran plain transaction() commands through the mutation-id lookup; a same-request-id retry queued before its first attempt cached hit 'cached mutation lacks its recorded identity' -> 500 (the 2026-08-02 shape, same-API). Fix: (a) mutation_id=None arm replays the original tx id on identity alone; (b) residual cross-API replay is a typed MutationIdentityConflict mapped to 409 naming the request id. Dispatch-close keys (dispatch-close-*, dispatch-close-state-*) never enter the /tx mutation-key domain; pinned by tests (writer 2 new + 1 updated, api 2, cli 1). Follow-up candidate: the sibling 'reused with a different operation' error is still a 500 via the handle path.
- 14:33 follow-up landed: sprint/BX5SR2B commit 828cde7b (RequestIdReuseConflict thiserror struct raised at all three identity-mismatch sites; writer_transaction_error maps it to 409 naming the request id; test api::tests::writer_request_id_reused_with_different_payload_is_a_409_not_a_500). Merged into local main as 0fec5cb6 (--no-ff), not yet pushed; gate running. Still untyped: the 'already used by a different mutation type' bail (two-line follow-up, same struct).
- 14:36 manager commit b1c6ca5f on local main: the three 'already used by a different mutation type' bails now raise RequestIdReuseConflict too; gate green (clippy daemon, 48 filtered writer/idempotency tests, fmt). Every request-id reuse shape is now a 409. Local main is 2 commits ahead of origin (0fec5cb6 merge + this), not pushed.

