#+title: orgasmic task TASK-ZEFEY
#+orgasmic_version: 2

* DONE TASK-ZEFEY Artifact store: schema, tx types, index projection, and CLI verbs :artifacts:daemon:cli:
:PROPERTIES:
:ID: TASK-ZEFEY
:IMPLEMENTS: arch_ARSPJ
:END:

Build the on-disk artifact store and its agent surface: .orgasmic/artifacts/ART-<slug>/ with artifact.mdx (opaque), artifact.org index heading (:ID: :TITLE: :SUBJECT_NODES: :PROMPT: :VERSION: :STATE:), reviews.org comment headings (:CID: :AUTHOR: :VERSION: :ANCHOR: :RESOLUTION_TARGET: :RESOLVED: :CONSUMED:), and versions/ archives. Wire tx types (artifact.created/.submitted/.comment.added/.comment.resolved/.regenerated), writer paths, watcher coverage, index projection (list, per-node rollups, open-comment counts, state), WS topics, and REST reads (GET /artifacts, GET /artifacts/:id[?version=N]). CLI verbs: orgasmic artifact blocks (block-vocabulary reference), orgasmic artifact submit <id> --file <mdx> (validates against the block registry, rejects invalid), orgasmic artifact feedback <id> [--consume <cid>].

Acceptance: on a throwaway project, create -> submit -> feedback -> consume round-trips entirely through the CLI: files and tx entries land as specified, invalid MDX is rejected without any write, the index serves the bundle with correct comment filtering (consumed excluded), and a comment appended while the artifact is in regenerating state is refused.
** Evidence
- Merged ddd6b56 (worker commit 9a5f043, implementer-claude-rmux claude-sonnet-4-6 high, run-20260703T185733, ~24m): +1742 across 8 files. Store layout .orgasmic/artifacts/ART-<slug>/ (artifact.org + opaque artifact.mdx + reviews.org + versions/), artifact.* tx types in shipped/schema/tx.org (regenerated declared, no write path yet per scope boundary), ProjectIndex.artifacts projection with open-comment counts + per-node rollups, Topic::Artifact WS payloads, GET /artifacts + GET /artifacts/:id[?version=N], POST submit/feedback/feedback-consume, orgasmic artifact blocks|submit|feedback CLI verbs, 22-type block registry as shared const data.
- Dispatch note: worker completed fully (its own gates: 301 daemon tests, clippy, fmt; committed with exact brief message; emitted orgasmic-eot marker) but the rmux stall detector killed the idle pane afterwards and last.txt captured only stall_timeout_exceeded — manager-rescued close (implementer.done tx-1215). Filed the tooling bug separately.
- Manager diff review: validate-before-write honored (submit rejects invalid MDX with 422 + block_errors before touching disk); files + artifact.submitted tx written in one atomic writer.transaction; artifact.created via writer.append_tx; no generate/regenerate routes (scope boundary respected); regenerating-state comment refusal at api.rs with test coverage; fs writes in new code confined to #[cfg(test)] modules.
- REVIEW QUESTION carried to reviewer: artifacts.rs uses a hand-rolled parse_org_properties() for artifact.org/reviews.org reads instead of the canonical OrgFile parser — assess second-parser drift risk (dec_QVSM0 spirit) vs pragmatism.
- Post-merge manager gates: recorded after run (see Reviewer pass / worklog).
** Reviewer pass
REVIEWER PASS (reviewer-claude-rmux, claude-opus-4-8 xhigh, run-20260703T194200, ~8.5m): VERDICT ship-with-fixes, no blocker. Acceptance round-trip traced end to end (api.rs:12321 test exercises every clause); folder layout, heading properties, tx types, WS topics, index rebuild, watcher coverage all conform to arch_ARSPJ; scope boundary clean. Findings: 1 HIGH (validate_mdx byte-scan false-rejects generics-heavy Code/DataModel, false-accepts prose/empty - proven via standalone rustc probe), 3 MEDIUM (hand-rolled Org parser truncation/injection - confirms the manager-flagged second-parser risk; fs writes bypassing writer; non-atomic comment add/consume), 4 LOW (duplicate created-tx on retry, headerless reviews.org + 404 mislabel, unescaped PROMPT/TITLE drawer break, hardcoded WS state). All 8 filed as reviewer.finding tx. Fixes tracked in fix subtask; foundation not to be built on until it lands.

