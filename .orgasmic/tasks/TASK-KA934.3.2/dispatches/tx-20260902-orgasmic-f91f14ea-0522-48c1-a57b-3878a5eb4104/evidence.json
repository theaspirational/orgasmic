{
  "event_count": 18020,
  "tool_call_count": 94,
  "unparsed_events": 23,
  "bounded_events": 27,
  "session": {
    "status": "found",
    "filename": "dispatch-TASK-KA934.3.2-implementer-20260902T073308.jsonl",
    "size_bytes": 16544086
  },
  "native_transcript": {
    "status": "unsupported",
    "harness": "opencode",
    "reason": "no transcript finder adapter for harness 'opencode'"
  },
  "narrative": [
    {
      "stream": "assistant",
      "text": "Let me start by understanding the task. I'm the orgasmic implementer for TASK-KA934.3.2. The task involves:\n\n1. Narrow the `:ACTOR:` forward guard to comment-type journal writes\n2. Inverse guard in `member add`: refuse a name equal to the daemon actor\n3. Doctor warns when any members.org name equals the live daemon actor or manager_actor\n4. Fix dead assertion in `admin_post_tx_journal_actor_colliding_with_member_name_refused`\n\nLet me first read the task record and explore the codebase. The project is at /Users/aspirational/.orgasmic/ledgers/orgasmic, and I'm working in a worktree at /Users/aspirational/.orgasmic/worktrees/orgasmic/task-ka934.3.2.\n\nLet me start by getting the task record and exploring the relevant files.Let me look at the relevant files. The key files:\n- `crates/orgasmic-daemon/src/api.rs` (~:2340 ensure_actor_namespace_free, ~:3077 prepare_tx_append_request, ~:8662 prepare_api_tx_as, ~:39085 test)\n- `crates/orgasmic-cli/src/member.rs` (~:78)\n- `crates/orgasmic-core/src/members.rs` (~:165/~:186)\n- `crates/orgasmic-cli/src/doctor.rs` (~:253 push_tracked_views_findings, check_daemon_for_status)\n- `crates/orgasmic-daemon/src/lib.rs` (~:268 daemon default actor)\n- writer.rs ~:1691 require_comment_body\n\nLet me explore these.Now let me look at:\n1. `event_routes_to_journal` (api.rs ~:8867)\n2. `require_comment_body` in writer.rs ~:1691\n3. The test at ~:39085\n4. `choose_actor` at ~:9009Let me look at the writer.rs require_comment_body and understand what \"editable comment\" means. Also let me check the StatusResponse struct and the test at 39085.Now I understand the structures. `StatusResponse` doesn't currently expose `actor` or `manager_actor`. Per the brief, I need to add `actor` and `manager_actor` to `/status` (small, additive).\n\nNow let me look at the test at ~39085 and the member add code, doctor.rs, and the daemon lib.rs.Now let me look at:\n1. `member.rs` in the CLI\n2. `members.rs` in core (add_member)\n3. How the CLI resolves config (manager_actor)\n4. The daemon lib.rs ~:268 (default actor)Now I need to understand:\n1. How `manager_actor` is configured. Let me look at how cfg.manager_actor is loaded (lib.rs:1126) and how the CLI resolves config.\n2. How the CLI's `doctor` uses `DaemonStatus` and where the CLI loads daemon config.\n\nLet me look at:\n- daemon config loading (`manager_actor`)\n- CLI config loading (`crates/orgasmic-cli/src/config.rs`?)Now I understand the daemon config. `DaemonConfig::load(home)` reads `$ORGASMIC_HOME/config.yaml`. The CLI is `crates/orgasmic-cli`. Does the CLI depend on `orgasmic-daemon`? Let me check the CLI's Cargo.toml. The `DaemonConfig` is in `orgasmic-daemon` crate. Let me check if the CLI can access it.The CLI depends on `orgasmic-daemon`, so it can use `orgasmic_daemon::config::DaemonConfig::load(home)` to read `manager_actor`. Is `config` module public in lib.rs of daemon? Let me check. Also, how does the CLI start the daemon with `--actor`? Let me check main.rs for the actor flag.Now I understand the doctor structure. The `DaemonStatus` struct in doctor.rs deserializes from `/daemon/status` response (the `StatusResponse` in api.rs). I need to add `actor` and `manager_actor` to `StatusResponse` (additive) and to the CLI's `DaemonStatus` struct.\n\nLet me now look at the rest of the test at api.rs ~39125 (the end of the dead-assertion test) and then look at members.rs add_member. Also let me check where the `member add` flow could reach a daemon status — the brief says: \"If a daemon IS reachable (the CLI has a status client — see doctor's check_daemon_for_status), also compare against its reported actor when the status payload exposes it\".\n\nLet me read members.rs add_member first.Now let me look at `add_member` in members.rs (around 165/186):Now I understand the structure. Let me now plan the implementation:\n\n### Move 1: Narrow the forward guard\nIn `prepare_tx_append_request` (api.rs:3076) and `prepare_api_tx_as` (api.rs:8661), narrow `event_routes_to_journal(&req.r#type)` to also require `ty == \"comment\"`.\n\nLooking at `require_comment_body` in writer.rs: `entry.ty != \"comment\"` → \"not an editable comment\". So the single predicate for \"editable comment\" is `ty == \"comment\"`. Note `worker.comment` is a separate type. Let me check what `worker.comment` is — the guard covers types where `:ACTOR:` grants rights. The authorship check compares raw stored `:ACTOR:`. Let me check which types carry an editable `:ACTOR:` — from writer.rs `require_comment_body`, only `ty == \"comment\"` entries are editable comments. But what about `worker.comment`? Let me check how worker.comment is written and whether it has an `:ACTOR:` stamp.\n\nActually, the brief says: \"fire only when the type is `comment` (keep the `event_routes_to_journal` gate AND add `ty == \"comment\"`, or whatever single predicate the code already has for 'editable comment' — see `require_comment_body`, writer.rs ~:1691)\".\n\nSo the guard should fire only for `ty == \"comment\"`. But wait — I should check how worker.comment is stamped. Let me search for worker.comment in writer.rs.\n\n### Move 2: Inverse guard in `member add`\nIn `cmd_add` (member.rs:76), before calling `add_member`:\n- Compute daemon actor candidates:\n  - `$USER` (env var) — daemon default\n  - `manager_actor` from `DaemonConfig::load(home)` (CLI has orgasmic-daemon dep)\n  - If daemon reachable, the status-reported `actor` and `manager_actor`\n- If `name` equals any, bail with a message naming the collision and telling the user to pick another member name or start the daemon with `--actor`.\n\nThe message: \"refuses a name equal to the daemon actor name\". Message names the collision and says to pick another member name or start the daemon with `--actor`.\n\nFor the daemon status: the CLI has a status client in doctor.rs — `daemon_status_async`. That's a private fn in doctor.rs. I need a way for member.rs to query status. Options:\n- Make a shared helper. The doctor's `daemon_status` is private. I could add a small pub(crate) function in doctor.rs or better in daemon_client.rs. Let me check daemon_client.rs.\n\nAlso need to add `actor` and `manager_actor` to:\n- `StatusResponse` in api.rs (additive serialization)\n- CLI's `DaemonStatus` in doctor.rs (with serde default so old daemons still parse)\n\nNote: the CLI's DaemonStatus derives Deserialize with `#[serde(default)]` fields where needed. I'll add:\n```rust\n#[serde(default)]\nactor: Option<String>,\n#[serde(default)]\nmanager_actor: Option<String>,\n```\nBut DaemonStatus is compared with PartialEq/Eq and constructed in tests at line 693. Adding fields means updating test constructors. Let me check test at doctor.rs:692.\n\nAlso there's `StatusResponse` used in daemon api.rs get_status — add fields `actor: state.actor.clone()` and `manager_actor: state.manager_actor.clone()`.\n\nWait, but the status endpoint is `/daemon/status`. Let me confirm the CLI's DaemonStatus parses `/daemon/status` response — yes, `daemon_url(&base_url, \"/daemon/status\")`.\n\n### Move 3: Doctor warning\nAdd a new findings fn `push_member_actor_collision_findings(out, home)` next to `push_tracked_views_findings` (do NOT edit push_tracked_views_findings). When daemon is Running (reuse daemon_status(home) — it returns DaemonLiveness::Running(DaemonStatus)), compare each members.org name against status.actor and status.manager_actor (and possibly the config manager_actor?). The brief says: \"warn when any `members.org` name equals the live daemon actor or manager_actor (reuse the status client + `read_members`)\".\n\nSo: if daemon liveness is Running(status), read members, and for each member whose name == status.actor or status.manager_actor (Some), push Finding::Warn with a message naming the collision.\n\nNote that doctor's `diagnose` already calls `push_daemon_findings` which calls `daemon_status(home)`. Calling daemon_status twice means two HTTP requests... but doctor runs are rare; still, I could restructure to compute once. `push_daemon_findings` calls `daemon_status(home)`. To avoid double network calls, I could change `diagnose` to compute `let liveness = daemon_status(home);` once and pass to both `push_daemon_findings` and the new fn. That changes push_daemon_findings' signature — allowed since it's not in the off-limits list (off-limits is `push_tracked_views_findings` and project_migrate.rs). Let me keep it simple but clean: compute liveness once in `diagnose`, pass `&DaemonLiveness` to both functions.\n\nWait — off limits: \"`doctor.rs` `push_tracked_views_findings` (add a NEW findings fn next to it, do not edit that one)\". So I can edit other parts of doctor.rs.\n\n### Move 4: Dead assertion\nReplace the `if journal.exists() { ... }` block with `assert!(!journal.exists())`.\n\nWait — but hold on. After narrowing the guard... in the test `admin_post_tx_journal_actor_colliding_with_member_name_refused`, the POST /tx with type \"comment\" and actor \"alice\" is refused → nothing written → journal does not exist (it may not exist at all since no comment was ever written... actually wait, does anything else create the journal? The test seeds projects via seed_two_projects; TASK-001 journal wouldn't exist). Then the allowed tx (actor \"daemon-admin\", type comment) passes → creates journal. But the assertion `assert!(!journal.exists())` runs BEFORE the allowed tx. So after narrowing, the refusal still fires for type \"comment\" (guard still covers comment). So journal still doesn't exist at that point. \n\nBut wait — the allowed POST /tx with actor \"daemon-admin\" — does that write to the journal? It's type \"comment\", task TASK-001, project proj-a. `node_journal_path` → journal at root_a/.orgasmic/tasks/TASK-001/journal.org. That happens after the assertion, so fine.\n\nHmm, but does the writer actually write the tx to journal asynchronously? The refusal happens before queueing, so no.\n\n### Tests to add/update\n\nAcceptance: \n1. \"member add <daemon-actor-name> is refused with a message naming the collision\" (test)\n2. \"doctor warns on an existing collision\" (test)\n3. \"guard fires only for comment-type journal writes\" (test)\n4. Dead assertion fixed.\n\nGates:\n- `cargo test -p orgasmic-daemon --lib -- comment member identity authz post_tx status`\n- `cargo test -p orgasmic-cli --bin orgasmic -- member doctor`\n- `cargo clippy -p orgasmic-daemon -p orgasmic-cli -p orgasmic-core --all-targets -- -D warnings`\n- `cargo fmt --all --check`\n\nNow, where do existing tests live for the guard? Let me search for tests referencing the guard in api.rs — e.g., tests named like `*actor*`. Also there's a test for comment mutation actor. Let me search for tests that exercise the wide guard so I can update them (e.g., tests asserting non-comment journal tx with member actor is refused — those would now pass through).\n\nLet me search.Now let me look at the existing tests around the guard and how `worker.comment` is stamped, to understand the narrow scope:Now let me check the daemon `test_options()` used in the api tests — what actor does it use? Let me look for `fn test_options` in api.rs and lib.rs. This matters because `admin_comment_actor_colliding_with_member_name_refused` uses admin comment mutations, which still go through `comment_mutation_actor` — that path stays guarded (comment mutations are not touched by the narrowing).\n\nAlso I should find where the daemon's own `--actor` flag is in the CLI (main.rs serve). Let me check main.rs:904 and 4328.The test daemon uses `DaemonOptions::default()` → actor = `$USER`. So the running test daemon actor is `$USER` (the test process user).\n\nNow, the `cmd_serve` at main.rs:2430. Let me check whether `serve` has an `--actor` flag. The brief says \"start the daemon with `--actor`\". Let me look at cmd_serve and the Serve variant in the CLI enum.Interesting — the CLI `serve` does NOT have an `--actor` flag; `DaemonOptions::default()` reads `$USER`. Wait — the brief says \"start the daemon with `--actor`\". Let me check where `--actor` is a daemon option... main.rs:4328 mentioned `--actor`. Let me check that context. Also check `daemon_service.rs` maybe writes `--actor`. Actually main.rs:4328 is `.split(\"--actor\")` — let me look.OK — `serve` does NOT have an `--actor` flag. The daemon actor is `DaemonOptions::default()` = `$USER`, and there's no `--actor` CLI flag for serve. The brief says the message should say \"pick another member name or start the daemon with `--actor`\". Hmm, but there's no `--actor` flag on serve. Let me check `manager.actor` in config.yaml — that's the way to change the daemon actor (`manager_actor` in DaemonConfig overrides... wait let me re-check choose_actor: `req.actor.or(manager_actor).unwrap_or(state.actor)`. So if `manager.actor` is set in config.yaml, the effective daemon actor is manager_actor.\n\nWait, actually let me check how opts.actor and manager_actor interact in lib.rs 1126-1130:\n- `manager_actor: cfg.manager_actor.clone()` \n- `actor: opts.actor.clone()`\n\nAnd `choose_actor`: `req.actor.clone().and_then(non_empty).or_else(|| state.manager_actor.clone()).unwrap_or_else(|| state.actor.clone())`.\n\nSo the effective actor for journal stamps is: manager_actor if configured, else $USER.\n\nThe task description says \"start the daemon with `--actor`\" — hmm, the task description literally says \"Message names the collision and says to pick another member name or start the daemon with `--actor`.\" But there's no `--actor` serve flag. There is `orgasmic tx record --actor`. Hmm. The task description also says \"and to the configured manager_actor / --actor if discoverable from the daemon config\". \n\nSince `serve` has no `--actor` flag, the message should probably say \"set `manager.actor` in config.yaml\". Let me not add a new flag (that would widen scope). The task says don't redesign product behavior; the message should mention how to change the daemon actor. I'll phrase the message to reference the actual mechanism: pick another member name, or set `manager.actor` in the daemon config / start the daemon with a different actor. Hmm — but the task explicitly says \"says to pick another member name or start the daemon with `--actor`\".\n\nWait, maybe I should check if the daemon service definition or another path passes --actor. Let me search daemon_service.rs for how it launches. Actually let me search for \"--actor\" more broadly.Confirmed: there is NO `--actor` flag on `serve`. The way to change the daemon actor is `manager.actor` in config.yaml. The task says the message should say \"pick another member name or start the daemon with `--actor`\" — but `--actor` doesn't exist for serve. The brief says message \"names the collision and says to pick another member name or start the daemon with `--actor`\". Since `--actor` doesn't exist on the serve surface, I should phrase it accurately with the actual mechanism: pick another member name, or set `manager.actor` in the daemon config (and restart). Hmm.\n\nActually wait — the task assignment says \"and to the configured manager_actor / --actor if discoverable from the daemon config\". I think the manager (who wrote the task) misremembered the flag. The acceptance criterion says \"refused with a message naming the collision\". I'll write the message naming the collision and the actual remedy: pick another member name, or set `manager.actor` in config.yaml / restart the daemon with a different actor. This is faithful to the intent (tell the user how to avoid the collision) without inventing a flag. I'll note it in the report as a small deviation with evidence (no `--actor` flag on serve).\n\nHmm, but wait. Adding an `--actor` flag to serve could be considered part of the task... no — \"Do not redesign product behavior\" and \"Stop and escalate if the task requires new decisions\". The narrow fix is the message content; the acceptance criterion only requires \"refused with a message naming the collision\". I'll go with the real mechanism in the message.\n\nNow let me plan the implementation:\n\n## 1. api.rs — narrow the guard\n\nIn `prepare_tx_append_request` (line 3076):\n```rust\nif event_routes_to_journal(&req.r#type) {\n    ensure_actor_namespace_free(state, &actor)?;\n}\n```\nbecomes:\n```rust\nif journal_actor_guard_applies(&req.r#type) {\n    ensure_actor_namespace_free(state, &actor)?;\n}\n```\n\nAnd add a helper:\n```rust\n/// dec_Q78QN — journal-routed types stamp `:ACTOR:`, but only `comment`\n/// rows carry an actor the member can then edit or delete by raw string\n/// match (`require_comment_body`); the other journal producers pass\n/// `actor: None` and the daemon-fallback actor never grants member rights.\nfn journal_actor_guard_applies(ty: &str) -> bool {\n    ty == \"comment\"\n}\n```\n\nHmm wait, should the predicate be `ty == \"comment\"` alone, or `event_routes_to_journal(ty) && ty == \"comment\"`? Since \"comment\" itself routes to journal (it's in the list), `ty == \"comment\"` implies `event_routes_to_journal(ty)` is true. So `ty == \"comment\"` suffices. The brief says \"keep the `event_routes_to_journal` gate AND add `ty == \"comment\"`, or whatever single predicate the code already has for 'editable comment'\". A single predicate `ty == \"comment\"` is cleaner. But hmm — `comment.deleted`? `event_routes_to_journal(\"comment.deleted\")` is false (ends_with .deleted → false). And comment edit/delete journal rows are written by the writer internally, not through prepare_tx_append_request. The tombstone rows stamp `:EDITED_BY:`/`:DELETED_BY:` via `comment_mutation_actor` which already guards. So the guard at the tx-append boundary only needs to fire for type `comment` (the create path that stamps `:ACTOR:`).\n\nWait — what about \"worker.comment\"? Let me check what worker.comment rows look like — do they stamp `:ACTOR:`? And can a member edit/delete a worker.comment? `require_comment_body` bails if `entry.ty != \"comment\"` — \"not an editable comment\". So worker.comment rows are not editable by members → an actor collision on worker.comment grants no rights. Good — narrow to `ty == \"comment\"`.\n\nBut wait, let me double check where worker.comment is written and if there's an authorship path for it. Let me search writer.rs for how comments are appended and what types. Also the activity route might show them. The authorship check is in require_comment_body — only \"comment\" ty. OK.\n\nSo the narrow predicate: `fn journal_write_grants_actor_rights(ty: &str) -> bool { ty == \"comment\" }`.\n\nHmm, but hold on. Let me think about the member-session comment path in `prepare_api_tx_as`. Member sessions reach a journal through comment handlers; for a member, actor == member name (legitimate). For an admin (member_name None) posting a `comment`-type api tx, the guard fires (actor = manager_actor/state.actor or admin-supplied). That's exactly what stays.\n\nIn `prepare_api_tx_as` (line 8661):\n```rust\nif identity.member_name().is_none() && event_routes_to_journal(&req.ty) {\n    ensure_actor_namespace_free(state, &actor)?;\n}\n```\nbecomes:\n```rust\nif identity.member_name().is_none() && journal_actor_guard_applies(&req.ty) {\n    ensure_actor_namespace_free(state, &actor)?;\n}\n```\n\nUpdate the comments accordingly.\n\nAlso `ensure_actor_namespace_free`'s doc comment may need a small update (it describes the general guard — fine as-is).\n\n## 2. StatusResponse — expose actor + manager_actor\n\nAdd to `StatusResponse`:\n```rust\npub actor: String,\npub manager_actor: Option<String>,\n```\nAnd in `get_status`, set `actor: state.actor.clone(), manager_actor: state.manager_actor.clone(),`.\n\nAre there other places constructing StatusResponse? Let me search.\n\n## 3. CLI — member add inverse guard\n\nIn `member.rs` `cmd_add`:\n```rust\nfn cmd_add(home: &Home, name: &str, role: Option<RoleArg>, grant: &[String]) -> Result<()> {\n    let grants = build_grants(role, grant)?;\n    refuse_daemon_actor_collision(home, name)?;\n    let token = orgasmic_core::add_member(home, name, &grants)?;\n    ...\n}\n```\n\n`refuse_daemon_actor_collision`:\n- collect candidate actor names:\n  - `$USER` env (daemon default) — with label \"the daemon default actor ($USER)\"\n  - `orgasmic_daemon::config::DaemonConfig::load(home)` → manager_actor (ignore errors — if config is unreadable, skip? Hmm. DaemonConfig::load can fail on parse error. member add shouldn't hard-fail because config.yaml is broken? Actually a broken config.yaml breaks the daemon boot too... but member add is host-local. Fail-open with the $USER check still applying seems right; but silently skipping... The forward guard fails open with a warn log. For the CLI I'll fail open (skip manager_actor check) — but maybe mention nothing. Hmm, actually, let me think: DaemonConfig::load error → context \"parse <path>\". If config.yaml is broken, daemon won't start at all, so daemon actor = nothing running... but $USER default applies when daemon starts later. The $USER check remains the core protection. I'll skip unreadable config silently (best-effort) — or should the CLI error? If config.yaml is unparseable, `member add` should probably still work (it's a different subsystem). I'll fail open.\n  - daemon status (if reachable): status.actor, status.manager_actor.\n\nFor reaching the daemon from member.rs, I need a status client. doctor.rs has `daemon_status_async` (private) using `read_daemon_token`, `daemon_base_url`, `daemon_url`, `api_path`. Those are all in doctor.rs. I could make a shared `pub(crate)` helper. Options:\n- Add `pub(crate) fn daemon_status(home) -> Option<DaemonStatus>` in doctor.rs and reuse from member.rs.\n- Move to daemon_client.rs.\n\nLet me look at daemon_client.rs to see what's there.\n\nAlso the DaemonStatus struct lives in doctor.rs. For member.rs to use status.actor, the struct fields need pub(crate) visibility. And to parse actor/manager_actor, add serde-default fields.\n\nDesign: in doctor.rs, make `DaemonStatus` fields `pub(crate)` with the new fields `#[serde(default)] pub(crate) actor: Option<String>` etc. Hmm wait, but doctor tests construct DaemonStatus directly (line 692). Adding fields updates those constructors.\n\nActually — cleaner: move the status-probing into daemon_client.rs? Let me look at daemon_client.rs first.\n\n## 4. Doctor collision findings\n\nNew fn `push_member_actor_collision_findings(out, home)`:\n- liveness = daemon_status(home) — reuse. But push_daemon_findings already computes it. To avoid two probes, restructure `diagnose`:\n\n```rust\nlet liveness = daemon_status(home);\npush_daemon_findings_with(&mut out, &liveness);\npush_member_actor_collision_findings(&mut out, home, &liveness);\n```\n\nWait — but `push_daemon_findings` is called in `diagnose` at line 118. I can change `push_daemon_findings` to take `&DaemonLiveness`. That's editing doctor.rs but NOT `push_tracked_views_findings` — allowed.\n\nHmm, but `daemon_status` creates a tokio runtime and blocks — fine, once.\n\nNew findings fn:\n```rust\n// orgasmic:dec_Q78QN, TASK-KA934.3.2\n/// Inverse half of the `:ACTOR:` namespace guard: a member whose name equals\n/// the live daemon actor makes every admin journal write ... warn.\nfn push_member_actor_collision_findings(out: &mut Vec<Finding>, home: &Home, liveness: &DaemonLiveness) {\n    let DaemonLiveness::Running(status) = liveness else { return; };\n    let members = match orgasmic_core::read_members(home) { Ok(m) => m, Err(_) => return };\n    let mut collisions = ...\n    for member in members {\n        if Some(member.name == status.actor) || status.manager_actor == Some(name) { ... }\n    }\n}\n```\n\nWarning message naming the collision:\n\"member `alice` collides with the daemon actor (…): admin journal writes stamping that actor are refused (dec_Q78QN); pick another member name or change the daemon actor (`manager.actor` in config.yaml)\".\n\nSomething like that.\n\nAlso — should doctor warn when daemon is NOT running but members collide with $USER/manager_actor from config? The brief says: \"doctor warns when any members.org name equals the live daemon actor or manager_actor\". \"Live daemon actor\" implies the daemon is running; if not running, the daemon default would be $USER when it starts. The task description: \"(3) doctor warns when any members.org name equals the live daemon actor or manager_actor (reuse the status client + read_members)\". Status client → needs a reachable daemon. I'll warn only when the daemon is reachable (Running). That also keeps it deterministic. Hmm, but consider: daemon down + member named $USER → when daemon auto-starts, actor = $USER → collisions. But the member add guard already refuses new ones at add time; existing ones... Actually, if `member add` refuses daemon-actor names, how could an existing collision exist? Pre-existing installs before this fix. The doctor warning catches those. When daemon is down, we can still compare against $USER + config manager_actor without the status client. Hmm — the brief says \"reuse the status client + read_members\", implying the Running branch. But warning also when down (using the same defaults the daemon would boot with) is strictly more useful and cheap. \n\nLet me keep scope tight: follow the brief — warn based on the live daemon status when Running; when Unavailable, fall back to the same candidates `member add` uses ($USER + config manager_actor)? That could produce a false warning if the operator always starts the daemon with a different actor... but there's no --actor flag, so daemon actor = manager_actor or $USER always. The \"live\" wording suggests only-when-running. I'll do: only when daemon reachable (Running + status exposes actor). For older daemons without actor in status payload, actor is None → nothing to compare → skip (or fall back to config?). Keep it simple: Running requires non-None actor to compare... Actually if status.actor is None (old daemon), we can still use config manager_actor + $USER? No — keep to what the brief says: reuse status client + read_members; compare against live daemon actor or manager_actor. If the daemon predates the status fields, skip (the remedy is `orgasmic restart`, which staleness checks likely already flag).\n\nHmm, wait. Actually let me reconsider: when daemon is down, doctor could warn using $USER + config manager_actor, since those are exactly the actors a locally-started daemon would use. This makes doctor useful for catching pre-existing collisions even when the daemon is down. But the brief explicitly shapes it as \"live daemon actor ... reuse the status client\". I'll implement the Running-only version. Tight scope, matches brief.\n\n## 5. member add + status\n\nmember.rs `cmd_add` needs daemon status. Since doctor.rs's `daemon_status` is sync (creates runtime) — I can reuse it. Make it `pub(crate) fn daemon_status` and `DaemonLiveness` pub(crate)? Or simpler: expose a helper in doctor.rs:\n\n```rust\n/// Daemon status for other CLI verbs (member add inverse guard). Returns the\n/// parsed status when the daemon is reachable and authorized.\npub(crate) fn live_daemon_status(home: &Home) -> Option<DaemonStatus> {\n    match daemon_status(home) {\n        DaemonLiveness::Running(status) => Some(status),\n        _ => None,\n    }\n}\n```\n\nAnd make DaemonStatus fields pub(crate). Member.rs then:\n\n```rust\nlet daemon = crate::doctor::live_daemon_status(home);\n```\n\nWait — is doctor.rs a module of the CLI bin? Yes: `crate::doctor`. Let me check main.rs module declarations. And daemon_client.\n\nActually, wait: there's a subtlety — `daemon_status` in doctor is `fn daemon_status(home: &Home) -> DaemonLiveness` (sync, builds a runtime). member add calling it adds a ~10s worst case timeout on an unreachable daemon... The reqwest client timeout is 10s; connect to a dead port fails fast (connection refused). Fine.\n\nNow, what about `member add` comparing against `$USER`: the daemon default actor is `std::env::var(\"USER\").unwrap_or(\"unknown\")`. member.rs should use the same: `std::env::var(\"USER\").ok()`. On Windows, USER isn't set... but this codebase uses `USER` for the daemon too, so mirror it. Note \"unknown\" fallback: if USER is unset, daemon actor = \"unknown\". Should member add refuse \"unknown\"? Member names can be \"unknown\". Edge case — mirror the daemon: candidate = env USER or \"unknown\". Hmm, refusing \"unknown\" seems wrong-ish but consistent with the daemon default. Let me include it: the candidate is the daemon default actor value, i.e. `$USER` else `\"unknown\"`. Actually let me keep it faithful: compute `default_daemon_actor()` = `std::env::var(\"USER\").unwrap_or_else(|_| \"unknown\".into())` and refuse name == that. It's the real daemon default, so refusing it is correct.\n\nWait, but also the daemon might run with `manager.actor` set → effective actor is manager_actor (choose_actor prefers manager_actor). So candidates: daemon-default ($USER/\"unknown\"), config manager_actor, live status actor, live status manager_actor. Dedup, case-sensitive comparison (member names are [A-Za-z0-9_.-], actors are raw strings; compare trimmed? member names have no whitespace).\n\nError message: names the collision:\n```\nrefusing member name `{name}`: it collides with the daemon actor `{actor}` ({source}); admin journal writes stamping that actor would be refused. Pick another member name, or change the daemon actor (`manager.actor` in config.yaml).\n```\n\nHmm the brief says message \"says to pick another member name or start the daemon with `--actor`\". Since --actor doesn't exist, use the real mechanism. I'll note this in the report.\n\n## 6. Dead assertion fix\n\nReplace:\n```rust\n        let journal = root_a.join(\".orgasmic/tasks/TASK-001/journal.org\");\n        if journal.exists() {\n            let contents = std::fs::read_to_string(&journal).unwrap();\n            assert!(\n                !contents.contains(\":ACTOR: alice\"),\n                \"forged actor landed in the journal\"\n            );\n        }\n```\nwith:\n```rust\n        let journal = root_a.join(\".orgasmic/tasks/TASK-001/journal.org\");\n        assert!(\n            !journal.exists(),\n            \"forged `:ACTOR: alice` entry landed in the task journal\"\n        );\n```\n\nWait — but there's a wrinkle: this test, after narrowing, does the refused tx still get refused? The refused tx is type \"comment\" → guard applies → 403. Yes. And nothing wrote the journal before → journal doesn't exist. The allowed tx later creates it. Good.\n\nBut also — the second part of the test: \"The same tx with a non-member actor passes the guard\" — type comment, actor daemon-admin → OK, and the writer then... creates the journal. Fine (after the assert).\n\nAlso the member comment path stays OK.\n\nAdditionally, I should add a test for the narrowing: a journal-routed NON-comment admin tx with a member-named actor now passes (e.g. `task.property_updated` with actor \"alice\" → OK), and still `comment` refused. And maybe `worker.comment` passes. Where to add? Right in `admin_post_tx_journal_actor_colliding_with_member_name_refused` — extend it, or add a new test. The brief gate runs `cargo test -p orgasmic-daemon --lib -- comment member identity authz post_tx status`. A test name containing those keywords... Let me add assertions to the existing test (it already covers refusal + allowed) plus a new test for the narrowing:\n\n`admin_post_tx_non_comment_journal_actor_member_name_allowed` — hmm, must match the filter words: comment member identity authz post_tx status. Something like `admin_post_tx_non_comment_journal_actor_colliding_member_name_allowed`. Contains \"comment\", \"member\", \"post_tx\". Good.\n\nActually wait — do I even need a full daemon-boot test for that? It's an HTTP-level behavior; the existing test boots a daemon. A second daemon boot ~fine. Or extend the existing test with a couple more requests (same daemon): after the allowed tx, post a `task.property_updated` with actor \"alice\" → expect OK now (narrowed). That's cheapest and keeps one boot. I'll extend the existing test with the narrowed-scope assertions and update its doc comment.\n\nAlso `prepare_api_tx_as` narrowing — is there an existing test for admin api tx with member actor? The admin api tx path (`prepare_api_tx_as`) is reached via `POST /tasks/...` handlers etc. Comment handlers route through `prepare_api_tx`... hmm, actually where is prepare_api_tx_as called? Let me check. The comment create handler → `prepare_api_tx`? Let me search. There's also a direct `api_tx` route? Let me look at how routes call prepare_api_tx_as.\n\nFor the member add test: CLI test in member.rs `#[cfg(test)] mod tests` — cmd_add is private but tests are in the same module. Test: create Home (temp), set... hmm, cmd_add prints. Tests can call `cmd_add(&home, \"alice\", ...)` where daemon not running → status unreachable → only $USER + config manager_actor checks apply. To test the refusal deterministically, set `manager.actor` in home config.yaml to \"alice\" and assert cmd_add errors with message naming collision. And another test: name == $USER refused. $USER in test env = the test runner user (e.g. \"aspirational\")... `std::env::var(\"USER\")` — in CI could be unset → \"unknown\". For a deterministic test, use manager_actor in config (deterministic) for the collision test. For the $USER path: read the same env var in the test and use that name. That's deterministic relative to the same env.\n\nNote: cmd_add with an UNREACHABLE daemon — the status probe happens (connect refused fast). Fine.\n\nBut careful: in the test environment, `live_daemon_status` probes the daemon base URL from `home` config... With a temp home and no config, base = 127.0.0.1:4848. If the developer has a REAL daemon running on 4848 (very possible on this machine!), the probe would hit the real daemon and could return a real status... but for temp-home tests, the token file `home.auth_token()` for the temp home doesn't exist → `read_daemon_token` → None → Unauthorized → not Running. Good — no cross-talk: token is read from the temp home. \n\nWait, check `read_daemon_token`: `daemon_client::read_bearer_token(home)`. Reads `$ORGASMIC_HOME/user/auth/token` of the temp home. Temp home → no token → None → Unauthorized → live_daemon_status returns None. Good.\n\nBut also `ORGASMIC_DAEMON_URL` env var could leak in — daemon_base_url checks `std::env::var(\"ORGASMIC_DAEMON_URL\")` FIRST, before the token. But token check comes first in daemon_status_async: `let Some(token) = read_daemon_token(home) else { return Unauthorized }`. Temp home → no token → Unauthorized regardless of URL env. Good. But never set ORGASMIC_HOME (rule) — we use Home::at temp paths directly.\n\nFor the doctor test: `push_member_actor_collision_findings` — test with a fabricated DaemonLiveness::Running(DaemonStatus { actor: Some(\"alice\"), manager_actor: Some(\"bob\"), ... }) + home with members alice → expect a Warn naming alice. There are existing doctor tests constructing DaemonStatus (line 692 `status_started_at`). Let me look at doctor tests around 692-760 to match the pattern.\n\nLet me now check:\n1. Where StatusResponse is constructed (other than get_status).\n2. daemon_client.rs contents.\n3. doctor tests.Now let me check the remaining pieces: `StatusResponse` constructions, `daemon_client.rs`, and the doctor tests:Now I have a good picture. Plan:\n\n**doctor.rs** — I'll expose a `pub(crate)` helper `live_daemon_status(home) -> Option<DaemonStatus>` and make `DaemonStatus` fields pub(crate) + add `actor`/`manager_actor` fields with `#[serde(default)]`. And restructure `diagnose` to compute daemon liveness once and pass it to `push_daemon_findings` + new `push_member_actor_collision_findings`.\n\nWait, careful: `diagnose` is `pub fn` used elsewhere? Let me check callers of doctor::diagnose. Only in main.rs presumably. Changing internal structure is fine.\n\n**member.rs** — add collision check using:\n1. daemon default actor: `std::env::var(\"USER\").unwrap_or_else(|_| \"unknown\".into())`\n2. config manager_actor: `orgasmic_daemon::config::DaemonConfig::load(home)` — note: DaemonConfig::load requires home.config(); error → skip. Hmm wait, does DaemonConfig::load have side effects? No, just reads config.yaml. Note the CLI already loads config in several places (daemon_base_url uses read_bind_port with raw yaml). Using `DaemonConfig::load` — the brief says \"manager_actor when readable from the daemon config the CLI already loads (look at how the CLI resolves config; do not add a new config file)\". The CLI doesn't currently parse manager_actor anywhere — it uses raw serde_yaml value reads (read_bind_port). Option A: use `orgasmic_daemon::config::DaemonConfig::load(home)` — reuse the daemon's own parser (CLI depends on the daemon crate). That's the cleanest \"config the CLI already loads\" — the same file `$ORGASMIC_HOME/config.yaml`. I'll use that.\n\n3. live status: `crate::doctor::live_daemon_status(home)` → status.actor / status.manager_actor.\n\n**api.rs**:\n- Narrow guard: `ty == \"comment\"` predicate helper + update comments.\n- StatusResponse: add `actor: String`, `manager_actor: Option<String>`; fill in get_status.\n- Dead assertion fix.\n- Extend test with narrowed-scope assertions.\n\nCheck: does anything else serialize StatusResponse — no, only get_status (line 9106).\n\nAlso — is there a test asserting the full status payload shape that would break with new fields? Additive serde Serialize fields don't break deserialization elsewhere. The CLI's DaemonStatus derives only Deserialize with defaults — but wait, `DaemonStatus` in doctor has non-default fields (started_at, boot_id, pid). Adding two `#[serde(default)]` fields is fine for old payloads.\n\nWait, actually there might be a `status` CLI handler in main.rs that parses /daemon/status too? Let me check how `orgasmic status` parses. Search for \"daemon/status\" in main.rs.\n\n**Doctor warning message**: shape like `push_tracked_views_findings` — a `Finding::Warn(format!(...))`.\n\nLet me now check the doctor tests region (lines 560-800) to match test style, and check main.rs status handler + module list.Let me check main.rs:2539 — `orgasmic status` reads /daemon/status as raw Value, so adding fields is safe.\n\nNow I have all the pieces. Let me set up a todo list and start implementing.\n\nImplementation plan:\n\n### A. api.rs (daemon)\n1. Narrow guard: add helper predicate + change two call sites + update comments.\n2. `StatusResponse`: add `actor: String` and `manager_actor: Option<String>`; populate in `get_status`.\n3. Fix dead assertion in test + extend test to cover narrowing (non-comment journal tx with member actor passes).\n\n### B. member.rs (CLI)\n1. `cmd_add`: refuse daemon-actor collisions before minting.\n   - candidates: default daemon actor (`$USER` else \"unknown\"), config manager_actor (via `orgasmic_daemon::config::DaemonConfig::load`, skip on error), live daemon status actor/manager_actor (via `crate::doctor::live_daemon_status`).\n2. Tests: collision via config manager_actor refused with message naming it; `$USER` collision refused; non-colliding name still works (add_member mints).\n\nWait — cmd_add prints to stdout. Tests calling cmd_add will print token noise; that's fine in tests (captured).\n\nActually — for the \"allowed\" test, cmd_add calls add_member → writes members.org in temp home. Fine.\n\n### C. doctor.rs (CLI)\n1. `DaemonStatus`: make fields pub(crate), add `actor: Option<String>`, `manager_actor: Option<String>` with `#[serde(default)]`.\n2. Add `pub(crate) fn live_daemon_status(home) -> Option<DaemonStatus>`.\n3. Restructure `diagnose` to compute liveness once: `let daemon_liveness = daemon_status(home); push_daemon_findings(&mut out, home, &daemon_liveness); push_member_actor_collision_findings(&mut out, home, &daemon_liveness);`\n4. New `push_member_actor_collision_findings` (place right after `push_tracked_views_findings`... the brief says \"add a NEW findings fn next to it\"). Read members; for each member whose name == status.actor or status.manager_actor → Warn naming the collision + remedy.\n5. Update `status_started_at` test helper to fill new fields.\n6. Tests for the new findings fn: member colliding with status.actor warns; colliding with manager_actor warns; non-colliding → no warn; daemon not running → no warn.\n\nThe doctor test filter: `cargo test -p orgasmic-cli --bin orgasmic -- member doctor` — test names must contain \"member\" or \"doctor\"? Actually the filter is a substring filter — tests matching ANY of the words (cargo test with multiple filters matches if name contains any of them... Actually multiple filter args = OR? For libtest, passing multiple filters matches tests whose name contains ANY of the filters. Yes — since Rust 1.51ish, multiple filters are OR'd). So my new tests should have names containing \"member\" or \"doctor\" to be run by the gate. E.g. `member_actor_collision_doctor_warn` — contains both.\n\nSimilarly member.rs tests contain \"member\" naturally.\n\nAnd for the daemon gate: `comment member identity authz post_tx status` — my api.rs test changes: the existing test name `admin_post_tx_journal_actor_colliding_with_member_name_refused` contains \"post_tx\" and \"member\". Any new test should contain one of those filters. I'll extend the existing test rather than add new ones (it covers refusal + pass-through + member path). I'll add narrowing assertions inside it.\n\nAlso there may be a status test... the gate includes \"status\" — maybe existing status tests exist. Let me add assertion in an existing status test? Not needed.\n\n### D. members.org — no changes in core.\n\nNow — one more consideration for `prepare_api_tx_as`: the comment says \"admin-supplied and daemon-fallback actors are refused on journal-routed types\". Update to comment-type.\n\nAlso `prepare_tx_append_request` comment at 3070-3075 needs updating.\n\nLet me also double-check `comment_mutation_actor` — unchanged (comment edit/delete stamps EDITED_BY/DELETED_BY; still guarded for admin).\n\nOne question: does the writer itself stamp `:ACTOR:` for other journal-routed types where a member-name actor would grant rights? E.g. `reviewer.finding` / `review.verdict` — do they stamp ACTOR and does any authorship check key on them? The authorship check is comment-only (`require_comment_body` checks ty == \"comment\"). Activity route shows edited_by/deleted_by for comment rows. So only \"comment\" grants rights. Good.\n\nWait — but what about `worker.comment`? Let me check how worker.comment rows are created and read. Search writer.rs.I have the full picture now. Let me set up a todo list and start implementing:`worker.comment` doesn't appear in writer.rs or core — it's only in the routing table in api.rs. So `worker.comment` rows come from external producers via POST /tx. Do they carry an editable-by-member `:ACTOR:`? `require_comment_body` only allows ty == \"comment\" for edit/delete, so worker.comment rows don't grant member rights. Narrow to `ty == \"comment\"`.\n\nNow implement the api.rs changes.`worker.comment` appears only in the routing table — edit/delete authorship (`require_comment_body`, writer.rs:1707) accepts only `ty == \"comment\"`, so that's the right narrow predicate. Now the api.rs changes:Now the StatusResponse — add actor and manager_actor fields. Then update get_status to populate them.Now expose `actor`/`manager_actor` on `/status`:Now fix the dead assertion and extend the test in api.rs (~39085). Replace the `if journal.exists()` block and add narrowing assertions.Now fix the dead assertion and extend the test for the narrowed scope:Wait — I need to double-check the ordering issue: the non_comment tx writes to the TASK-001 journal (`task.property_updated`, task TASK-001 → node_journal_path → journal.org created). That happens BEFORE the assert? Let me re-check the order in the edited test:\n\n1. refused tx (comment, actor alice) → 403\n2. assert !journal.exists()  ← comes right after the refusal, before non_comment ✓ (I placed the assert before the non_comment block — yes, my edit has assert first, then non_comment tx)\n3. non_comment tx → OK, may create journal — fine, after the assert.\n4. allowed tx (comment, daemon-admin) → OK\n5. member comment → OK\n\nGood, the assert is still before any successful write to that journal.\n\nNow todo 1 and the daemon-side test are done. Next: CLI side. First doctor.rs DaemonStatus + live_daemon_status + collision findings.Guard narrowed, status exposed, dead assertion fixed. Now the CLI side — first doctor.rs:Wait — `DaemonStatus` was `pub struct` — is it used outside the crate? It's in `orgasmic-cli` which is a bin crate (well, it has a lib? Cargo.toml shows only `[[bin]]`). A bin crate's `pub` items are still crate-internal for practical purposes, but `pub(crate)` is more accurate. But making it pub(crate) when it was pub — check if `DaemonStatus` is re-exported or used in doc... it's fine within the bin. Actually wait — clippy might complain about... no. `check_daemon_for_status_with_status` is `pub fn` taking `&DaemonStatus` — with a private-ish type that's fine in a bin crate.\n\nHmm, but is orgasmic-cli also a lib? Cargo.toml only has `[[bin]] name = \"orgasmic\" path = \"src/main.rs\"`. So everything is a binary crate; `pub` vs `pub(crate)` doesn't matter functionally, but pub(crate) avoids \"private type in public interface\" edge concerns. Actually since check_daemon_for_status_with_status is pub and takes DaemonStatus... in a bin crate this is all fine.\n\nWait, actually — I changed it to pub(crate); is DaemonStatus referenced from outside doctor.rs? Let me check. It was `pub struct` in doctor.rs; used in doctor.rs itself (daemon_status_async, diagnose_daemon_staleness, etc.) and in tests. And maybe main.rs? Let me grep.There's a SEPARATE `DaemonStatus` in `daemon_lifecycle.rs` (line 89). The doctor.rs one is distinct. Neither is used outside doctor.rs for the doctor's DaemonStatus (daemon_lifecycle has its own). Fine.\n\nNow add `live_daemon_status` helper and the collision findings fn in doctor.rs, and restructure `diagnose` to compute liveness once.\n\nCurrent diagnose:\n```rust\n    push_cli_path_findings(&mut out, home);\n    push_retired_content_findings(&mut out, home);\n    push_tracked_views_findings(&mut out, home);\n    push_daemon_findings(&mut out, home);\n    push_daemon_path_findings(&mut out);\n```\n\nI'll change to:\n```rust\n    push_cli_path_findings(&mut out, home);\n    push_retired_content_findings(&mut out, home);\n    push_tracked_views_findings(&mut out, home);\n    let daemon_liveness = daemon_status(home);\n    push_daemon_findings(&mut out, &daemon_liveness);\n    push_member_actor_collision_findings(&mut out, home, &daemon_liveness);\n    push_daemon_path_findings(&mut out);\n```\n\nAnd push_daemon_findings signature change:\n```rust\nfn push_daemon_findings(out: &mut Vec<Finding>, liveness: &DaemonLiveness) {\n    match liveness {\n        DaemonLiveness::Running(status) => {\n            if let Some(finding) = diagnose_daemon_staleness(home, &status) {\n```\n— wait it uses `home` for staleness: `diagnose_daemon_staleness(home, &status)`. So push_daemon_findings still needs home. Keep `home` param and add liveness param.\n\nNew findings fn placed right after push_tracked_views_findings:\n\n```rust\n// orgasmic:dec_Q78QN,TASK-KA934.3.2\n/// Inverse half of the `:ACTOR:` namespace guard. `member add` now refuses\n/// daemon-actor names, but a member added before that guard (or before the\n/// daemon actor was changed) poisons every admin journal comment write: the\n/// daemon refuses to stamp an actor that shares a member name. The finding\n/// names the colliding member and both remedies.\npub(crate) fn push_member_actor_collision_findings(\n    out: &mut Vec<Finding>,\n    home: &Home,\n    liveness: &DaemonLiveness,\n) {\n    let DaemonLiveness::Running(status) = liveness else {\n        return;\n    };\n    let members = match orgasmic_core::read_members(home) {\n        Ok(members) => members,\n        // A parse error already fails login and the forward guard logs it;\n        // doctor has no better remedy to offer than those paths.\n        Err(_) => return,\n    };\n    for member in members {\n        let which = [\n            (\"daemon actor\", status.actor.as_deref()),\n            (\"manager_actor\", status.manager_actor.as_deref()),\n        ]\n        .into_iter()\n        .find(|(_, actor)| actor == Some(member.name.as_str()));\n        if let Some((label, actor)) = which {\n            out.push(Finding::Warn(format!(\n                ...\n            )));\n        }\n    }\n}\n```\n\nMessage content: name collision + remedy. Shape like push_tracked_views_findings (multi-line with remedy). Something:\n\n```\nmember `alice` collides with the live {label} `{actor}` (members.org vs daemon): every admin journal comment write is refused with 403 because it would stamp `:ACTOR: alice` into a member-editable namespace (dec_Q78QN)\n  fix: pick another member name (`orgasmic member revoke alice`, re-add), or change the daemon actor (`manager.actor` in $ORGASMIC_HOME/config.yaml, then `orgasmic restart`)\n```\n\nHmm — length. Let me keep it informative but tighter:\n\n```rust\nout.push(Finding::Warn(format!(\n    \"members.org name `{}` collides with the live daemon {} (`{}`) — \\\n     admin journal comment writes stamping that actor are refused, so comment \\\n     moderation 403s until resolved\\n  \\\n     fix: rename the member (revoke + re-add) or change the daemon actor \\\n     (`manager.actor` in config.yaml) and restart\",\n    member.name, label, actor,\n)));\n```\n\nGood enough. Now `live_daemon_status`:\n\n```rust\n/// The parsed daemon status when a daemon is running and authorized, for CLI\n/// verbs that need the live daemon actor without doctor's full finding list\n/// (`member add` inverse guard, TASK-KA934.3.2).\npub(crate) fn live_daemon_status(home: &Home) -> Option<DaemonStatus> {\n    match daemon_status(home) {\n        DaemonLiveness::Running(status) => Some(status),\n        _ => None,\n    }\n}\n```\n\nPlace near check_daemon_for_status.There's a separate `DaemonStatus` in `daemon_lifecycle.rs`, so no clash. Now add the collision findings fn and the status helper:Now update `push_daemon_findings` to take the shared liveness, and add `live_daemon_status`:Now update the test helper `status_started_at` in doctor.rs tests to include the new fields, then add tests for the collision findings. Then move to member.rs.Update the doctor test helper and add collision tests:Now add doctor tests for push_member_actor_collision_findings. I'll add them near the ledger_sync test. The tests need:\n1. Member name == status.actor → Warn naming it.\n2. Member name == manager_actor → Warn naming it.\n3. No collision → no warn.\n4. Old daemon (actor None, manager None) → no warn even with members.\n\nThese tests use `Home::at(temp)` + `orgasmic_core::add_member`. Note add_member requires grants. And `push_member_actor_collision_findings` takes `&DaemonLiveness` — construct `DaemonLiveness::Running(status)` directly (no network).\n\nTest names must match the gate filter `member doctor` → include \"member\" and/or \"doctor\": `doctor_warns_when_member_name_equals_daemon_actor`, `doctor_distinguishes_manager_actor_collision`, `doctor_member_actor_no_collision_no_warn`, `doctor_skips_member_actor_collision_when_daemon_predates_status_fields`.\n\nDoes add_member need home.ensure()? Home::at(...).ensure() creates dirs. doctor.rs tests use Home::at — let me check an existing usage... In tests at line 1092ish: `check_daemon_for_status_none_when_unauthorized` — let me look at how they build home.Good. Now add the collision findings tests after `ledger_sync_failures_are_doctor_warnings` (around line 822). I'll insert them there.Now the member.rs inverse guard. Implementation:\n\n```rust\nuse orgasmic_daemon::config::DaemonConfig;\n\nfn cmd_add(home: &Home, name: &str, role: Option<RoleArg>, grant: &[String]) -> Result<()> {\n    let grants = build_grants(role, grant)?;\n    refuse_daemon_actor_collision(home, name)?;\n    let token = orgasmic_core::add_member(home, name, &grants)?;\n    ...\n}\n```\n\nThe collision function:\n\n```rust\n// orgasmic:dec_Q78QN,TASK-KA934.3.2\n/// Inverse half of the `:ACTOR:` namespace guard. A member name is the raw\n/// string the daemon compares stored `:ACTOR:` values against, so a member\n/// named like the daemon actor would make every admin journal comment write\n/// 403 (the daemon refuses to stamp a member-named actor). The CLI cannot\n/// know the actor a future daemon boot will use, so it refuses the default\n/// (`$USER`), the configured `manager.actor`, and — when a daemon is\n/// reachable — the actors its live status reports.\nfn refuse_daemon_actor_collision(home: &Home, name: &str) -> Result<()> {\n    let mut candidates: Vec<(&'static str, String)> = vec![(\n        \"the daemon default actor\",\n        std::env::var(\"USER\").unwrap_or_else(|_| \"unknown\".into()),\n    )];\n    if let Ok(config) = DaemonConfig::load(home) {\n        if let Some(manager_actor) = config.manager_actor {\n            candidates.push((\"manager.actor in config.yaml\", manager_actor));\n        }\n    }\n    if let Some(status) = crate::doctor::live_daemon_status(home) {\n        if let Some(actor) = status.actor {\n            candidates.push((\"the live daemon actor\", actor));\n        }\n        if let Some(actor) = status.manager_actor {\n            candidates.push((\"the live daemon manager_actor\", actor));\n        }\n    }\n    for (source, actor) in &candidates {\n        if *actor == name {\n            bail!(\n                \"refusing member name `{name}`: it collides with {source} `{actor}` — journal \\\n                 writes stamping that `:ACTOR:` would be refused for colliding with a member. \\\n                 Pick another member name, or change the daemon actor \\\n                 (`manager.actor` in config.yaml).\"\n            );\n        }\n    }\n    Ok(())\n}\n```\n\nHmm wait, message per acceptance: \"member add <daemon-actor-name> is refused with a message naming the collision\". Good — names both.\n\nNote about the source labels: \"the daemon default actor ($USER)\" — include $USER hint. Let me write candidates as (\"the daemon default actor ($USER)\", ...) and for the live status the label \"the running daemon actor\". For dedup — not needed for correctness.\n\nOne subtlety: `DaemonConfig::load` — validates LAN binding etc. (mutates cfg.bind but no side effects on disk). Returns Result; on error (parse failure) skip. Good.\n\nAlso: `crate::doctor::live_daemon_status(home)` makes an HTTP probe. In the common case (daemon down) that's a fast connection-refused. OK.\n\nTest for member.rs:\n\n```rust\n#[test]\nfn member_add_refuses_name_matching_configured_manager_actor() {\n    let tmp = tempfile::tempdir().unwrap();\n    let home = Home::at(tmp.path().join(\"home\"));\n    home.ensure().unwrap();\n    std::fs::write(home.config(), \"manager.actor: alice\\n\").unwrap();\n    let err = cmd_add(&home, \"alice\", Some(RoleArg::Viewer), &[]).unwrap_err();\n    let msg = format!(\"{err:#}\");\n    assert!(msg.contains(\"alice\"), ...);\n    assert!(msg.contains(\"collides\"), ...);\n    assert!(msg.contains(\"manager.actor\"), ...);\n    // nothing minted\n    assert!(orgasmic_core::read_members(&home).unwrap().is_empty());\n}\n```\n\nWait — but `cmd_add` probes `live_daemon_status` — temp home has token? home.ensure() creates dirs but no token file. read_bearer_token → read_home_token fails → Err → in daemon_status_async: `let Some(token) = read_daemon_token(home) else { return Unauthorized }` — read_daemon_token returns Result; `.ok()` → None on missing file. → Unauthorized → live_daemon_status → None. Good, no network in that path? daemon_status_async checks token BEFORE building client/base_url. So no HTTP at all. \n\nBut careful — `ORGASMIC_DAEMON_URL` env: daemon_status_async checks token first regardless. Good.\n\nAlso note the config file I write: `manager.actor: alice\\n` — DaemonConfig::load parses YamlConfig. manager is a nested struct `manager: { actor }`. The yaml key is `manager:` with `actor:` — check config.rs test: `manager:\\n  actor: dev@example.com\\n` → cfg.manager_actor = Some(\"dev@example.com\"). So write \"manager:\\n  actor: alice\\n\".\n\nAnother test: `$USER` collision:\n```rust\n#[test]\nfn member_add_refuses_daemon_default_actor_name() {\n    let tmp = ...; home.ensure();\n    let default_actor = std::env::var(\"USER\").unwrap_or_else(|_| \"unknown\".into());\n    let err = cmd_add(&home, &default_actor, Some(RoleArg::Viewer), &[]).unwrap_err();\n    assert!(msg.contains(&default_actor) && msg.contains(\"collides\") && msg.contains(\"daemon default actor\"));\n    assert!(read_members empty);\n}\n```\n\nAnd a pass-through test:\n```rust\n#[test]\nfn member_add_allows_name_disjoint_from_daemon_actors() {\n    // config manager.actor + default $USER, member \"carol\" minted\n    ...\n    let token = cmd_add(...).unwrap(); — wait cmd_add returns Result<()> and prints.\n    assert!(read_members names contains \"carol\");\n}\n```\n\nBut wait — in the pass-through test, the member name must differ from $USER which varies by environment; \"carol\" could theoretically equal $USER... test envs won't have USER=carol. But to be robust: pick a name from [\"carol\", \"carol-2\", ...] not equal to default actor and not equal to manager actor. Simple: choose `format!(\"carol{suffix}\")` if equal. Eh — just filter: `let name = [\"carol\", \"carol2\", \"carol3\"].into_iter().find(|n| n != &default_actor && n != \"alice\").unwrap();`. Keep it simple but safe.\n\nAlso cmd_add prints — fine.\n\nTest names for gate filter `member`: these are in member.rs tests, names start with e.g. `member_add_refuses...` — wait, existing test names in member.rs don't include \"member\" prefix (role_seeds_wildcard_grant etc.). The gate filter `member doctor` will run tests whose names contain \"member\" OR \"doctor\" — existing member.rs tests like `role_seeds_wildcard_grant` won't match \"member\"... Hmm! The existing tests in member.rs would NOT be selected by `cargo test -- member doctor` filters! Let me check: libtest with multiple filter args — since Rust 1.51, \"When multiple filters are passed, tests matching ANY filter run\"? Actually let me recall: `cargo test -- foo bar` passes both as filters; libtest matches if the name contains ANY of the filters? \n\nHistorically, libtest accepted multiple filters and matched ANY (OR). Yes — multiple --skip are OR'd (skip if any matches), and multiple positional filters are OR'd too. Confirmed: libtest treats multiple filters as OR.\n\nBut existing member.rs test names: `role_seeds_wildcard_grant`, `grant_without_equals_errors`... none contain \"member\" or \"doctor\". So the gate `cargo test -p orgasmic-cli --bin orgasmic -- member doctor` runs only tests with \"member\"/\"doctor\" in the name — that's the manager's chosen gate (targeted; NEVER unfiltered). My new tests must include those substrings. I'll name them with `member_add_...` prefix. Good.\n\nNow write the member.rs changes.Now the member.rs inverse guard:Now the doc comment at the top of member.rs says \"no daemon poke or restart\" — member add now probes the daemon status best-effort. Should update the module doc slightly for accuracy. The doc says:\n\n```\n//! and the daemon re-reads that file fresh on every request, so a write here is\n//! immediately effective — no daemon poke or restart. Member management is\n//! intentionally host-local (admin by virtue of filesystem access) and is not\n//! an HTTP route.\n```\n\nThe write itself is still daemon-free; the guard does a best-effort read-only status probe. Update the doc line minimally:\n\n\"...immediately effective — no daemon poke or restart.\" — I'll add one sentence: \"`member add` does consult the daemon (best-effort, read-only) to refuse names colliding with the daemon actor (dec_Q78QN).\" Keep it small.Now the member.rs tests:Wait — `member.rs` already has `use crate::home::Home;` at the top of the file (line 13). In the tests module I added `use crate::home::Home;` again — that's within the `mod tests` scope which is fine (the outer use is at file scope, tests module needs its own or `use super::*` brings it in — actually `use super::*` already imports `Home` from the parent module since it's `use crate::home::Home;` in the parent... `use super::*` imports all items in scope of parent including its `use` statements? No — `use super::*` imports public items of the parent module... in Rust, `use` declarations are private by default, but within the same crate, glob imports DO bring in private-use items? No: glob import `use super::*` imports all *visible* items of super, and since child modules can see private items of ancestors, `use super::*` brings in the parent's `use`-imported names too (imports are items with visibility; private use items are visible to child modules... actually within the same module tree, a `use` in the parent is accessible from the child via `super::Home` only if the use is `pub(crate)` or the child accesses it... Hmm.\n\nActually, the standard pattern: in the parent, `use crate::home::Home;` creates a private import in the parent module. A child module CAN refer to `super::Home`? Private items of a parent module ARE visible to child modules. Imported names are items with default private visibility, and child modules can access parent's private items. And `use super::*;` glob-imports all items nameable in super, including its private imports. So `Home` would already be in scope via `use super::*`, and adding `use crate::home::Home;` again creates... a duplicate import error? No — a direct `use` of the same name shadows glob imports (explicit imports take precedence over globs). So `use crate::home::Home;` + `use super::*` which also provides Home → the explicit one wins; no error. It's redundant but not an error. Clippy/rustc might warn about unused import? No, it IS used. Actually it may raise no warning at all. But to be clean, remove the redundant use since `use super::*` already covers it — wait, does it? TempHome test: existing tests use `RoleArg`, `build_grants` via `use super::*`. I'll just drop my extra `use crate::home::Home;` line — `use super::*` should bring Home in (parent has `use crate::home::Home;`).\n\nHmm, but if the glob doesn't import it (private imports are visible to child modules only via path, and glob imports only import *public* items? Let me think carefully. Rust reference: glob imports bring in all *importable* items — for a child module importing from parent via `use super::*`, private items ARE importable because visibility from child to parent's private items is allowed. Yes: \"use super::*;\" in a child module imports everything the child can name in super, which includes private items of super. This is the standard pattern in every Rust test module (e.g. they access private fns via `use super::*`). And `use` declarations themselves are items — private `use` items are accessible from child modules. So Home comes in. My explicit `use crate::home::Home;` is redundant but harmless (explicit beats glob, no ambiguity error).\n\nActually wait — is it harmless? If glob import also provides `Home`, an explicit `use crate::home::Home;` in the same module is allowed — explicit imports shadow glob imports. No error. But cleaner to remove. Let me remove it.Now check whether `daemon_lifecycle.rs` has its own DaemonStatus used by doctor... no, doctor imports `crate::daemon_lifecycle::LedgerSyncStatus` only.\n\nAlso check whether DaemonStatus's `started_at` etc. fields' visibility — I made struct pub(crate) but fields `started_at`, `boot_id`, `pid`, `ledger_sync` are private (no pub). They're used only within doctor.rs — fine. The new `actor`/`manager_actor` are pub(crate) for member.rs.\n\nNow check main.rs `Cmd::Status` handler at 2539 — it uses raw serde_json::Value, unaffected.\n\nNow — does anything else construct doctor::DaemonStatus? The `status_started_at` helper (updated) and `status_with_actor` (new). Also maybe other tests construct it via serde_json::from_value? `ledger_sync_failures_are_doctor_warnings` deserializes LedgerSyncStatus only. OK.\n\nNext: check compile. Let me build the three crates quickly (cargo check) before running tests.Now let me compile-check everything before running the gates:Two errors:\n1. doctor.rs:344 — `actor` is `Option<&str>` from `.find()` — wait, the `.find()` on the array iterator returns the whole tuple `(&str, Option<&str>)`. The collision found item IS the tuple where actor == Some(name), so `actor` there is `Option<&str>` = Some(name). I should unwrap or format as the inner. Since find matched on `actor == Some(member.name.as_str())`, I can `.map()` to unwrap. Change:\n\n```rust\n        .find(|(_, actor)| actor == Some(member.name.as_str()))\n        .and_then(|(label, actor)| actor.map(|actor| (label, actor)));\n```\n\n2. member.rs test — `candidate != &default_actor`: candidates are `&&str` in find closure (iter over array of &str gives &&str). Fix: `*candidate != default_actor && *candidate != \"alice\"`. Actually `candidate` is `&&str`; `candidate != &default_actor` compares `&&str` with `&String`? No — default_actor is String; `&default_actor` is &String; `&&str != &String` — the first error message says line 255 candidate != \"alice\" fails. Simplest: \n\n```rust\nlet name = [\"carol\", \"carol-2\", \"carol-3\"]\n    .into_iter()\n    .find(|candidate| **candidate != default_actor && **candidate != \"alice\")\n```\nHmm messy. Better:\n\n```rust\n.find(|candidate| **candidate != default_actor && **candidate != \"alice\")\n```\nWait `.into_iter()` on `[&str; 3]` yields `&str` items, and the closure receives `&&str`. So `**candidate` is str? `candidate: &&str`, `*candidate: &str`, `**candidate: str`. Compare `str != String`? No PartialEq<str> for String... the"
    }
  ],
  "narrative_truncated": true
}
