#+title: orgasmic task TASK-T09M7
#+orgasmic_version: 2

* BACKLOG TASK-T09M7 Driver-side audit: a driver's producer handle must own every event sender it created (stray-sender invariant)
:PROPERTIES:
:ID: TASK-T09M7
:PRIORITY: P3
:END:

From TASK-HAREX (2026-07-29): the 82-minute phantom run was caused by a stray event-sender clone in a driver-internal task the supervisor does not own — the drain's recv() never returned None. HAREX bounded the CONSEQUENCE (DrainGate on requested release); nothing yet asserts the invariant that a driver's producer handle owns every sender it created, so the next stray sender still costs a full RELEASE_DRAIN_BUDGET per release and drops post-budget events. Add a per-mode DriverSession invariant test turning this liveness bug into a test-time failure. acp_stdio and rmux are the two to read first (the persistent-socket rmux design holds shared state the tmux CLI design cannot — the 0RCRY task body's structural-asymmetry note). Injection per TCTTD standard: natural red = a mode with a deliberately-planted stray sender passing the invariant test pre-fix.

