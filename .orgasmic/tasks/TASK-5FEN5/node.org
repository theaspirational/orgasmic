#+title: orgasmic task TASK-5FEN5
#+orgasmic_version: 2

* DONE TASK-5FEN5 Two unowned load flakes found by the registry verification: babysitter flush and index repo_url rebuild :daemon:testing:flake:
:PROPERTIES:
:ID: TASK-5FEN5
:PRIORITY: P1
:END:

Discovered 2026-07-28 by the TASK-DJ1WV worker while verifying flake-registry
seed signatures under induced load (8-12 cpu hogs, elevated --test-threads).
Both fail under load, pass green in isolation, and had never been written down:

1. `supervisor::tests::live_babysitter_summary_flushes_on_event_threshold`
   (crates/orgasmic-daemon/src/supervisor.rs:9727) — failed 2 of 3 runs at 64
   test threads.
2. `index::tests::live_rebuild_preserves_repo_url_and_schedules_post_bind_refresh`
   (crates/orgasmic-daemon/src/index.rs:2095) — assertion left "" vs right
   "ssh://git@example.com/..." — the rebuilt index observed before the repo_url
   refresh landed.

The flake registry (verify/flake-registry.toml) deliberately refuses entries
without an owning open task, so both currently classify REAL and a loaded
full-suite run exits nonzero. This task exists to own them; its number should
be added to registry entries ONLY once each panic signature is captured
verbatim from a real failure (the DJ1WV rule: never guess a signature from the
test body).

=== Ask

1. Reproduce each under load, capture the exact panic text, register both with
   this task as owner (or fix them outright if the cause is shallow —
   registering is the fallback, fixing is the goal).
2. Diagnose: both look like timing assumptions about background work
   (babysitter flush threshold; post-bind refresh scheduling) observed from a
   test that does not wait on the completion signal. Fix the synchronization,
   not the timeout size, per the R74E8/ATAXN standard.
3. On fix, delete the registry entries (the registry check enforces this once
   this task is done).

=== Acceptance

- Injection per project standard, shipped as a verify/TASK-<id>/ artifact that
  `git apply` accepts.
- Either both tests survive 5 consecutive loaded runs (fixed), or both are
  registered with measured signatures and this task documents the diagnosis
  for the eventual fix.
THIRD CANDIDATE [2026-07-29], from the TASK-HQ970 worker's gate classification: `dispatch_system_only_session_without_finalize_orphans_not_scrapes` — 'timed out waiting for session ready' under full-suite load, passes in isolation and 34/34 when its binary runs alone. Same timeout shape as the others. If you reproduce it under the load recipe, capture its verbatim panic and treat it under this task's ownership too; if not, leave it unregistered per the DJ1WV rule.

FOURTH CANDIDATE [2026-07-29], from the TASK-XPYRR worker's gate run: `supervisor::tests::dead_pid_aborts_joins_hung_producer_then_receiver_releases` — "run run-… did not release within 12s" (crates/orgasmic-daemon/src/supervisor.rs:8065), one RED under full-suite load, green in isolation. Same shape: a timeout-bounded wait on background work observed under contention. Same rule applies: capture the verbatim panic under the load recipe before registering; ownership under this task, not a new entry.

