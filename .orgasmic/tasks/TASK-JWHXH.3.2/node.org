#+title: orgasmic task TASK-JWHXH.3.2
#+orgasmic_version: 2

* IN_PROGRESS TASK-JWHXH.3.2 project migrate on a non-git root: views cleanup only; refuse the v1->v2 rewrite and --to-branch up front :e01mc:chain-review:
:PROPERTIES:
:ID: TASK-JWHXH.3.2
:IMPLEMENTS: dec_XH2XY
:PRIORITY: P2
:RELATES_TO: TASK-JWHXH.3.1
:FIX_SUBTASK: t
:END:

Fix round for the JWHXH.3.1 review (opus-5, tx-21182657; merged e290d7fb). MEDIUM: the non-git early return in refuse_dirty_tree lets run_at fall through to apply_with_recovery (destructive v1->v2 rewrite) on a non-git root, where the partial-apply recovery hint prints inert git commands; --to-branch dies late inside create_orphan_branch after views were already deleted. Fix in run_at: detect the work tree once; on a non-git root run only the views cleanup, then refuse the v1->v2 rewrite and --to-branch up front with a plain message (no VCS to recover from; init git or back up first). Keep the summary helper; cover the println path once in the real-apply test if cheap.

** Acceptance
- [ ] Non-git v1 fixture: migrate deletes views, refuses the rewrite with the plain message, leaves the v1 files untouched; non-git --to-branch refused before any git call (tests).
- [ ] Git fixtures unchanged. cargo test -p orgasmic-cli --bin orgasmic -- migrate doctor; clippy -D; fmt.
** Evidence
Worker commit 8c460413 (opencode zai-coding-plan/glm-5.3-flash variant max) merged to main as 82ed5a9d (--no-ff, on top of 2be9f0a0). Manager gates on merged main 2026-09-02: cargo test -p orgasmic-cli --bin orgasmic -- migrate doctor = 33 passed 0 failed (/private/tmp/claude-501/-Users-aspirational-Documents-code-tools-orgasmic/25219e20-4403-4221-9eb2-cd596e21be27/scratchpad/gates-JWHXH.3.2-cli.log); clippy -p orgasmic-cli --all-targets -D warnings rc=0; fmt rc=0. Worker gates matched. Manager pre-check: single work-tree probe, non-git branch = views cleanup then plain bail when the plan has old_files or --to-branch, refuse_dirty_tree early return deleted, summary shared via print_summary.
** Notes
Implementer opencode glm-5.3-flash max, commit 8c460413, merged 82ed5a9d. Review (opus-5 high, tx-20260902-orgasmic-c7fb41cd): APPROVE WITH FOLLOW-UPS, LOW-only; MEDIUM closed. Accepted LOWs (operator may carry or drop): (1) :169-176 on a non-git v1 root the run deletes .orgasmic/views then bails, so the "views directory removed" line is never printed and the error is silent about it - append the views lines to the bail context or print the summary first; (2) :170 the message says "init git or back up .orgasmic first" but a bare git init then trips refuse_dirty_tree (everything untracked) - say git init && git add -A && git commit, or drop the git hint; (3) --dry-run on a non-git v1 root now exits non-zero with no plan instead of the old DRY RUN summary - print the summary and a warning line instead of Err; (4) :906-910 the summary assertion exercises views_summary_lines on a hand-built plan, not the call site; non_git_to_branch_refused_before_any_repo_change is misnamed (views deletion happens first). Confirmed: git-path parity byte-for-byte, single probe, refuse_dirty_tree has one gated caller.
