{
  "event_count": 16373,
  "tool_call_count": 34,
  "unparsed_events": 8,
  "bounded_events": 10,
  "session": {
    "status": "found",
    "filename": "dispatch-TASK-JWHXH.3.2-implementer-20260902T073308.jsonl",
    "size_bytes": 14402794
  },
  "native_transcript": {
    "status": "unsupported",
    "harness": "opencode",
    "reason": "no transcript finder adapter for harness 'opencode'"
  },
  "narrative": [
    {
      "stream": "assistant",
      "text": "Let me start by understanding the task. I'm the orgasmic implementer for TASK-JWHXH.3.2. The task is about `project migrate` on a non-git root: views cleanup only; refuse the v1->v2 rewrite and `--to-branch` up front.\n\nLet me first read the task record, then `project.org` and `gotchas.org`, then the relevant files.\n\nThe project is at `/Users/aspirational/.orgasmic/ledgers/orgasmic`. Let me start by getting the task record and reading the relevant files.\n\nLet me plan:\n1. Read the task record: `orgasmic task get --project orgasmic TASK-JWHXH.3.2`\n2. Read `dec_XH2XY` (a decision record)\n3. Read `project.org` and `gotchas.org` in the project\n4. Read `crates/orgasmic-cli/src/project_migrate.rs`\n5. Implement the fix in `run_at`\n6. Add tests\n7. Run gates: targeted cargo test, clippy, fmt\n8. Commit and finalize\n\nI'm working in a worktree at `/Users/aspirational/.orgasmic/worktrees/orgasmic/task-jwhxh.3.2`. Let me verify the environment first.I'll start by reading the task record, decision record, and project docs, then the target file.The `ls` on the ledger returned nothing? Let me check with the right path. Also read `dec_XH2XY`, `project.org`, `gotchas.org`.Let me continue reading the gotchas (probably not all needed) and now read the target file `crates/orgasmic-cli/src/project_migrate.rs` in my worktree. Let me check the worktree state first.Now let me check the worktree and read the target file.Now I understand the code. Let me analyze the current flow in `run_at` (line 154):\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    let views = ViewsMigration::plan(root)?;\n    if to_branch\n        && migration.old_files.is_empty()\n        && views.is_clean()\n        && is_ledger_root(home, root, &migration.project_source)?\n    {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n    if to_branch && !dry_run && !is_ledger_root(home, root, &migration.project_source)? {\n        migrate_to_branch(home, root, &migration)?;\n    } else if migration.old_files.is_empty() {\n        if views.is_clean() {\n            println!(\"already migrated\");\n            return Ok(());\n        }\n    } else if !dry_run {\n        apply_with_recovery(root, &migration)?;\n    }\n    ...\n}\n```\n\nThe problem:\n1. `refuse_dirty_tree` early-returns `Ok(())` on a non-git root (line 219-221). So on a non-git root, it falls through.\n2. `views.apply(root)` runs — deletes `.orgasmic/views/`. Fine.\n3. Then if `migration.old_files` is non-empty (v1 project), `apply_with_recovery(root, &migration)` runs — the destructive v1→v2 rewrite with no VCS. Its partial-apply context prints inert `git checkout`/`git clean` commands.\n4. If `--to-branch` was passed, `migrate_to_branch` runs, which calls `create_orphan_branch` and dies with \"failed before this run changed the repository\" — after views were already deleted.\n\nThe fix, per the brief:\n- Detect the work tree ONCE at the top of `run_at` (`git_ok(root, [\"rev-parse\", \"--is-inside-work-tree\"])` — same probe `ViewsMigration::plan` uses; share it).\n- On a non-git root: run the views cleanup (plan/apply/summary as today), then if the migration plan has anything to rewrite (i.e., `!migration.old_files.is_empty()`) or `--to-branch` was passed, `bail!` with a plain message: \"<root> is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\", leaving the v1 files untouched.\n- `refuse_dirty_tree`'s early return can then go back to being unreachable — prefer deleting it if the new gate makes it dead.\n- Git-repo behaviour must not change.\n\nWait, but there's an ordering subtlety. The brief says: \"run the views cleanup (plan/apply/summary as today), then if the migration plan has anything to rewrite or `--to-branch` was passed, bail! with a plain message... leaving the v1 files untouched.\"\n\nHmm, but \"leaving the v1 files untouched\" — the views cleanup deletes `.orgasmic/views/`, and the v1 files (tasks/*.org, decisions.org, etc.) stay untouched. That matches the acceptance criteria: \"migrate deletes views, refuses the rewrite with the plain message, leaves the v1 files untouched\".\n\nSo on non-git root with a v1 project:\n1. `plan(root)` — plans the migration.\n2. `ViewsMigration::plan(root)` — plans views.\n3. Views apply — delete views dir.\n4. Then check: if `!migration.old_files.is_empty() || to_branch` → bail with the plain message.\n\nWait — but should views cleanup happen before the bail? The brief says yes: \"run the views cleanup (plan/apply/summary as today), then if the migration plan has anything to rewrite or --to-branch was passed, bail!\". Hmm, \"plan/apply/summary as today\" — but the summary printing happens after the bail point... Actually the bail happens, so no summary prints. Wait, let me re-read.\n\n\"On a non-git root: run the views cleanup (plan/apply/summary as today), then if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!` with a plain message ..., leaving the v1 files untouched.\"\n\nHmm — this is a bit ambiguous: does the bail happen before or after the views cleanup? The acceptance criteria say: \"Non-git v1 fixture: migrate deletes views, refuses the rewrite with the plain message, leaves the v1 files untouched\". So: views are deleted, then the refusal happens. So the order is: views cleanup happens (deletes views), THEN the bail.\n\nBut wait — should the bail print the views summary first? In the error case, the command fails with the bail error, so no \"MIGRATED\" summary prints. Hmm, but \"plan/apply/summary as today\" then \"bail\" — maybe the summary prints before the bail? That seems odd for an error path.\n\nLet me think about what's cleanest and matches the acceptance criteria:\n\nOption A: bail right after views.apply (before summary). The error message is the plain message. Views are deleted, v1 files untouched.\n\nOption B: bail after printing the summary. The command prints MIGRATED + views line, then errors out. Weird.\n\nHmm, actually let me reconsider. What if the v1 rewrite is refused but views cleanup succeeded — the operator would want to know the views were cleaned. But an error path that also prints summary to stdout is unusual. Actually, printing the summary before bailing would be informative: \"MIGRATED ... views directory removed\" then error to stderr. Hmm.\n\nActually wait. Let me re-read once more: \"On a non-git root: run the views cleanup (plan/apply/summary as today), then if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!` with a plain message\".\n\nHmm, I think this phrasing means: the views-cleanup portion behaves as today (plan → apply → summary), and then the gate refuses the rewrite. But if we bail before printing the summary, the summary never prints, so \"summary as today\" wouldn't hold in the refusal case.\n\nHmm wait, but for the refusal case there IS no \"as today\" — today it destructively rewrites. So \"as today\" refers to the cleanup behavior itself.\n\nLet me think about which is best for the acceptance test: \"migrate deletes views, refuses the rewrite with the plain message, leaves the v1 files untouched\". Either ordering satisfies this if we check stdout for the message (bail prints to stderr via anyhow).\n\nHmm, actually, there's another consideration: what about `--to-branch` on a non-git root when everything is already clean? E.g., a ledger root on a non-git... wait, can a ledger be a non-git root? The first early-return at line 157: `to_branch && migration.old_files.is_empty() && views.is_clean() && is_ledger_root(...)` → \"already migrated\". That runs before the git detection. If we put the non-git gate before that... hmm, we need to be careful about \"Git-repo behaviour must not change\" and also not break the \"already migrated\" path.\n\nWait, actually, for the `already migrated` early return with `to_branch`: on a non-git root, `is_ledger_root` checks if root canonicalized == home.project_ledger(id). A ledger at `~/.orgasmic/ledgers/<project>` — could that be a non-git directory? The ledger is created via `git worktree add` on the orgasmic branch, so it's always a git worktree. But hypothetically, if someone's root is a non-git dir that IS the registered ledger and everything is clean, then `to_branch && clean && is_ledger_root` → \"already migrated\". That's sensible and should keep working — the new gate must not fire there.\n\nHmm wait, but the brief says \"non-git `--to-branch`: refused before any git call\". Hmm! \"refused before any git call\" — that means the --to-branch refusal should happen before ANY git call? But detecting non-git requires a git call (`git rev-parse --is-inside-work-tree` fails on a non-git root — that IS a git call, but a probe one).\n\nLet me re-read: \"Non-git `--to-branch`: refused before any git call.\" I think this means refused before any *mutating* git call (like `git rm`, `create_orphan_branch`'s `read-tree`/`add`/`write-tree`/`commit-tree`/`update-ref`, `git worktree add`). The probe itself (`rev-parse --is-inside-work-tree`) is obviously needed to detect non-git-ness. So the test should assert that no repo state changed / or that the error comes from the gate, not from create_orphan_branch's \"failed before this run changed the repository\" message.\n\nHow do I test \"refused before any git call\"? On a non-git root, any actual git invocation would fail. The old behavior: `migrate_to_branch` → ... → `create_orphan_branch(root, stage)` → `git_ok(repo, [\"rev-parse\", \"--verify\", \"refs/heads/orgasmic\"])` fails (non-git) → tries to create orphan → `git_env` fails with \"git read-tree --empty failed: ... fatal: not a git repository\". And the error message would be \"branch cutover failed before this run changed the repository\". Also views would already be deleted.\n\nThe new behavior: bail with the plain message. Test: assert error message contains \"is not a git work tree\" and does NOT contain \"branch cutover\", and views... hmm wait — for --to-branch, does views cleanup still happen before the refusal?\n\nPer the brief: \"on a non-git root run only the views cleanup, then if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!`\". So yes: views cleanup happens first, then the refusal for both the rewrite and --to-branch.\n\nHmm, wait, but there's a subtlety with the \"already migrated\" early return for to_branch: on a non-git root, if `migration.old_files.is_empty() && views.is_clean() && is_ledger_root(...)` — the early return happens before our gate. Should the gate be before or after that early return?\n\nConsider a non-git ledger root: ledgers are git worktrees in practice. But a registered project on a plain dir where `is_ledger_root` is true (registered path == root, e.g., the test `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet` registers root itself with `projects::register_project(&home, &root, \"plaindir\", \"main\")` — hmm, does register_project copy to a ledger or register in place? Let me check. In that test, after registration, root has views etc. and `is_ledger_root` presumably compares canonicalized root to `home.project_ledger(\"plaindir\")`.\n\nLet me look at `projects::register_project` to understand.\n\nActually, let me think about the flow I want for `run_at`:\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    let views = ViewsMigration::plan(root)?;\n    let is_git = git_ok(root, &[\"rev-parse\", \"--is-inside-work-tree\"]);\n    if to_branch\n        && migration.old_files.is_empty()\n        && views.is_clean()\n        && is_ledger_root(home, root, &migration.project_source)?\n    {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    if !is_git && (!dry_run && ...) hmm\n```\n\nWait — what about `--dry-run` on a non-git root with a v1 project? Today: `refuse_dirty_tree` returns Ok (non-git), views apply skipped (dry_run), then `migration.old_files.is_empty()` false, `!dry_run` false → falls to the summary print: \"DRY RUN\" + headings + nodes + bytes + views plan lines. So dry-run works today on non-git and just prints the plan. Should the new gate fire on dry-run? The brief: \"if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!`\". Hmm, it doesn't mention dry_run. But refusing a dry-run would break `migration_is_verbatim_dry_run_and_idempotent`... no wait, that test calls `plan`/`apply` directly, not `run_at`. Is there a test that dry-runs a v1 project on non-git root via run_at? `branch_cutover_is_orphan_dry_run_idempotent_and_worker_discoverable` does `run_at(&home, &repo, true, true)` — git repo though.\n\nHmm. The gate placement: if we bail on dry-run too, then `--to-branch --dry-run` on a non-git root refuses — that seems right actually (the operator asked for something impossible; telling them up front is better than printing a plan that can't run). And a plain v1 `--dry-run` on non-git: printing a plan for a rewrite that will be refused... also better refused? Hmm, but the brief says \"run the views cleanup (plan/apply/summary as today), then ... bail!\". For dry-run, views apply is skipped (dry_run), summary prints the plan lines... then bail? That would make the \"views directory on disk (untracked)\" dry-run plan line print followed by an error. Weird.\n\nLet me think about what's minimal and sane: the gate should refuse *destructive* paths. A dry run changes nothing. But refusing dry-run too is simpler and arguably more correct: the operator asked for an operation this root cannot support; the error explains why. Hmm.\n\nBut wait — \"Git-repo behaviour must not change\" and minimal scope. The brief literally says: \"if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!`\". No dry_run exception mentioned. Simplest faithful implementation: gate fires regardless of dry_run. Let me check: would that break any existing test? Existing tests that call run_at on non-git roots:\n- `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet`: run_at(&home, &root, false, false), v2 project (version 2, no old_files), views present. Gate: `!is_git && (migration.old_files.is_empty()? ...` — old_files empty (already v2, no collections present... wait, project.org version 2, no tasks/decisions/glossary files → `present == 0 && already_v2` → default Migration, old_files empty) and to_branch false → gate passes → views cleanup runs as today. \n\n- Other non-git run_at calls? `branch_cutover...` uses git repos. The final `run_at(&home, &ledger, false, true)` — ledger is a git worktree. OK.\n\nSo no existing test breaks either way on dry_run. Decision: should the gate fire on dry_run? Consider the acceptance test \"non-git --to-branch: refused before any git call\" — no dry-run mentioned. I'll include dry-run in the gate (i.e., refuse regardless of dry_run) — no wait. Hmm. Let me think again.\n\nActually, hmm: `--dry-run` on a non-git v1 root today prints a plan and changes nothing. If I make it bail, I'm changing dry-run behavior (arguably fine, arguably an improvement — you can't run this plan here). If I keep dry-run working, the gate is `if !is_git && !dry_run && (!migration.old_files.is_empty() || to_branch)`.\n\nThe brief: \"on a non-git root run only the views cleanup, then if the migration plan has anything to rewrite or `--to-branch` was passed, `bail!` with a plain message\". The literal reading has no dry-run carve-out. And for `--to-branch --dry-run` on non-git, bailing is clearly right (the summary would print \"target orphan branch orgasmic\" which is impossible). For plain v1 `--dry-run` on non-git... the plan can never be applied here. I'll apply the gate unconditionally (no dry_run carve-out) — simplest and matches the brief text. Views cleanup still runs first (or in dry-run, plan lines only), then bail.\n\nHmm wait, but then in the dry-run + v1 + non-git case, the bail comes before the summary prints — meaning the operator never sees the plan. Is that OK? The error tells them to init git or back up first. Fine.\n\nBut hold on — one more ordering consideration: the first early return (`to_branch && ... && is_ledger_root` → \"already migrated\"). This is checked BEFORE the views cleanup and BEFORE the gate. On a non-git root with to_branch where everything is clean and root IS the ledger: \"already migrated\" prints. That's today's behavior too (refuse_dirty_tree non-git early return → ... wait, today: to_branch=true, dry_run=false, then `!is_ledger_root` is false → goes to `else if migration.old_files.is_empty()` → views.is_clean() true → \"already migrated\" returns Ok. So today this path prints \"already migrated\" for the non-git ledger root. With my gate placed AFTER this early return, behavior is preserved. Good — the gate must be after the early return.\n\nWait, actually let me re-check: with to_branch=true, dry_run=false, is_ledger_root=true: first early return fires (all four conditions) → \"already migrated\". Yes preserved.\n\nNow the flow after the early return:\n\n```rust\n    if !is_git {\n        // Views-only cleanup; the rewrite and --to-branch need a repo.\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if !migration.old_files.is_empty() || to_branch {\n            bail!(\n                \"{} is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\",\n                root.display()\n            );\n        }\n        // summary print as today\n        ...\n        return Ok(());\n    }\n```\n\nHmm wait, but then the summary code is duplicated or restructured. The brief says \"Keep the summary helper\". The summary helper is `views_summary_lines`. The summary PRINTING in run_at prints migration headings etc. On the non-git views-only path, migration.headings would be empty (if old_files is empty... wait — if old_files is empty but migration isn't default? When old_files is empty: either already_v2 clean (default Migration: headings empty, nodes 0, bytes 0) — or present==0 && already_v2 && !has_legacy_artifacts. Hmm, could old_files be empty but headings non-empty? old_files gets pushed per collection source that exists. If no collection sources exist, no headings. plan_artifacts adds to old_files too. So old_files empty ⟺ no sources and no artifacts ⟺ headings empty, nodes 0. But bytes/headings only in the rewrite branch.\n\nHmm wait, one more case: v2 project WITH legacy artifacts → `present == 0 && already_v2 && !has_legacy_artifacts` fails → proceeds to build migration; collections all bail with \"migration source is missing\"! Because for each collection, every source must be a file: `if !source_path.is_file() { bail!(\"migration source is missing\") }`. So a v2 project with artifacts but no tasks/*.org files → hard error \"migration source is missing: .../tasks/backlog.org\". That's pre-existing behavior, not my scope.\n\nOK so on the non-git path with old_files empty and !to_branch: the summary prints \"MIGRATED\" (or DRY RUN), no headings, \"  nodes 0\", \"  bytes 0\", \"  heading_round_trip byte-for-byte\", then views lines. That's exactly today's behavior for `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet`. Let me verify today's path for that test: to_branch=false, refuse_dirty_tree → Ok (non-git). views_applied = views.apply → true. Then `to_branch && ...` false → `else if migration.old_files.is_empty()` → true → views.is_clean()? After apply, views is the PLAN (tracked empty, dir_present true) — is_clean() is `tracked.is_empty() && !dir_present` → false (dir_present true) → so no early return → falls through to print summary. Yes. So \"MIGRATED\" + nodes 0 + bytes 0 + views directory removed.\n\nSo the cleanest minimal restructure: keep the single flow, but replace `refuse_dirty_tree(root)?` + downstream with:\n\n```rust\n    let is_git = git_ok(root, &[\"rev-parse\", \"--is-inside-work-tree\"]);\n    if !is_git {\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if !migration.old_files.is_empty() || to_branch {\n            bail!(...);\n        }\n        print_summary(&migration, &views, views_applied, dry_run, to_branch, home)?;\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;  // now unreachable early return? No — is_git true here, so the early return IS dead.\n```\n\nHmm wait, if I compute is_git once and only call refuse_dirty_tree when is_git, the early return inside refuse_dirty_tree becomes dead. The brief: \"prefer deleting it if the new gate makes it dead\". So: delete the early return from refuse_dirty_tree, making it require a git tree. But careful: refuse_dirty_tree would then run git status on a tree known to be git — fine.\n\nBut wait, there's also ViewsMigration::plan's internal probe (line 67). \"same probe ViewsMigration::plan uses — share it\". So I should restructure so the probe happens once in run_at and is passed to ViewsMigration::plan. Let me change `ViewsMigration::plan(root)` → `ViewsMigration::plan(root, is_git)`:\n\n```rust\n    fn plan(root: &Path, is_git_work_tree: bool) -> Result<Self> {\n        let tracked = if is_git_work_tree {\n            git_capture(root, &[\"ls-files\", \"--\", \".orgasmic/views\"])\n                .unwrap_or_default()\n                .lines()\n                .map(str::to_string)\n                .collect()\n        } else {\n            Vec::new()\n        };\n        Ok(Self {\n            tracked,\n            dir_present: root.join(\".orgasmic/views\").is_dir(),\n        })\n    }\n```\n\nAnd run_at:\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    let is_git = git_ok(root, &[\"rev-parse\", \"--is-inside-work-tree\"]);\n    let views = ViewsMigration::plan(root, is_git)?;\n    if to_branch\n        && migration.old_files.is_empty()\n        && views.is_clean()\n        && is_ledger_root(home, root, &migration.project_source)?\n    {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    if !is_git {\n        // No VCS to recover from: only the dec_XH2XY views cleanup is safe\n        // here; the v1→v2 rewrite and --to-branch are refused up front.\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if !migration.old_files.is_empty() || to_branch {\n            bail!(\n                \"{} is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\",\n                root.display()\n            );\n        }\n        print_run_summary(home, &migration, &views, views_applied, dry_run, to_branch);\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;\n    ...\n}\n```\n\nHmm, wait. Should I extract the summary printing into a helper (`print_run_summary`)? The summary block is lines 177-211. It's used by both the non-git path and the git path. Extracting it avoids duplication. The brief says \"Keep the summary helper\" (referring to `views_summary_lines`). Extracting a print helper is reasonable and keeps the diff small-ish. Alternatively, restructure with the gate inline:\n\n```rust\n    refuse... no wait\n```\n\nAlternative structure that avoids the early-return branch duplication:\n\n```rust\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n    if to_branch && !dry_run && !is_git {\n        bail!(...);\n    }\n    if !is_git && !migration.old_files.is_empty() {\n        bail!(...);\n    }\n    if is_git { refuse_dirty_tree(root)?; }\n    if to_branch && !dry_run && is_git && !is_ledger_root(...) { migrate_to_branch } ...\n```\n\nHmm wait but this changes the git-path ordering: views.apply would run BEFORE refuse_dirty_tree on git trees. Today: refuse_dirty_tree first, then views.apply. Does order matter for git? refuse_dirty_tree excludes .orgasmic/views from the dirty check (idempotency). If views.apply runs first (untracks + deletes views), then refuse_dirty_tree — a tree that was clean-except-views is still clean after. But a tree dirty for OTHER reasons: today refuses BEFORE deleting views. If I reorder, views get deleted then refuse → destructive before refusal! Bad. Must keep refuse_dirty_tree before views.apply on the git path.\n\nSo for the non-git branch, order is views.apply then (maybe) bail; for git, refuse then views.apply. The `if !is_git { ... return Ok(()); }` early structure is cleanest. Then dedupe the summary printing via a helper since both paths need it.\n\nWait, actually — do I even need the summary on the non-git path to go through the same code? Yes, to keep \"plan/apply/summary as today\". Extract:\n\n```rust\nfn print_summary(home: &Home, migration: &Migration, views: &ViewsMigration, views_applied: bool, dry_run: bool, to_branch: bool) -> Result<()> {\n    println!(\"{}\", if dry_run { \"DRY RUN\" } else { \"MIGRATED\" });\n    for (collection, count) in &migration.headings {\n        println!(\"  {collection}.nodes {count}\");\n    }\n    println!(\"  nodes {}\", migration.nodes.len() + migration.in_place_nodes);\n    println!(\"  bytes {}\", migration.bytes);\n    println!(\"  heading_round_trip byte-for-byte\");\n    for line in views_summary_lines(views, views_applied, dry_run) {\n        println!(\"{line}\");\n    }\n    if to_branch {\n        ... same as today ...\n    }\n    Ok(())\n}\n```\n\nHmm, but this is a bigger refactor. Minimal alternative: in the non-git branch, only views matters (migration.headings empty, nodes 0, bytes 0, to_branch... wait to_branch bails, so the to_branch tail never prints on the non-git path). So the non-git path prints: \"MIGRATED\"/\"DRY RUN\", nodes 0, bytes 0, heading_round_trip, views lines. If I DON'T extract a helper, I'd duplicate ~10 lines. Extraction is cleaner. Let me extract — the gate ordering with the ledger-root \"already migrated\" stays put.\n\nHmm, wait. Actually, let me reconsider: could I instead place the non-git gate WITHOUT restructuring run_at's main flow? Like:\n\n```rust\n    if !is_git && (to_branch || !migration.old_files.is_empty()) {\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        bail!(\"...\");\n    }\n```\n\nHmm, but then in the non-git views-only case (old_files empty, !to_branch), flow continues: refuse_dirty_tree early return (deleted → would now fail git status on non-git!). So refuse_dirty_tree still needs its guard or must be skipped. If I delete refuse_dirty_tree's early return, the main flow's refuse_dirty_tree call must be gated on is_git. And views.apply at line 166 runs for both. Then `to_branch && !dry_run && !is_ledger_root` — on non-git with to_branch... already bailed above. `migration.old_files.is_empty()` → non-git v1 already bailed. So after the up-front bail, the remaining non-git flow is exactly the views-only case, which flows through the existing code IF refuse_dirty_tree is skipped:\n\n```rust\n    if is_git {\n        refuse_dirty_tree(root)?;\n    }\n```\n\nWait but that's exactly \"delete the early return inside refuse_dirty_tree, gate the call site\". Then the rest of run_at (lines 166-212) is untouched! That's the minimal diff:\n\n1. `run_at`: compute `is_git` once at top; pass to `ViewsMigration::plan`.\n2. After the \"already migrated\" early return: add\n   ```rust\n   if !is_git && (to_branch || !migration.old_files.is_empty()) {\n       let views_applied = if dry_run { false } else { views.apply(root)? };\n       bail!(\n           \"{} is not a git work tree; ...\",\n           root.display()\n       );\n   }\n   ```\n   Hmm wait — but here views_applied is unused (we bail). `let views_applied = ...` then bail → unused variable warning. Actually the cleanup must still happen (acceptance: \"migrate deletes views, refuses the rewrite\"). So just `if dry_run { ... }`:\n   ```rust\n   if !is_git {\n       if !dry_run {\n           views.apply(root)?;\n       }\n       if to_branch || !migration.old_files.is_empty() {\n           bail!(\"...\");\n       }\n   }\n   ```\n   \n   Hmm wait, but if !is_git and NOT (to_branch || rewrite): fall through to the rest. refuse_dirty_tree gated on is_git — skipped. views.apply at 166 runs (or not, dry_run). Then line 167: `to_branch && ...` false. Line 169: old_files empty → views.is_clean()? → if clean \"already migrated\" else fall to summary. Then summary prints. That's today's behavior exactly (dry-run and real). \n\n3. refuse_dirty_tree: delete the non-git early return (now dead: call site gated on is_git; non-git non-refused flows skip the call).\n\nHold on, wait. There's a subtle bug: if `!is_git` and `!dry_run`, we call `views.apply(root)?` in the gate, then fall... no wait — if we bail, we don't fall through. If `to_branch || !old_files.is_empty()` we bail after cleanup. If neither, we skip the bail, and then line 166 calls `views.apply(root)` AGAIN. Is apply idempotent? Yes: \"Idempotent: a second run on a clean tree is a no-op\" — after the first apply, `views.tracked` is from the plan (non-git → empty) and dir_present... the plan's `dir_present` was captured BEFORE apply, so `views.is_clean()` uses stale dir_present=true → apply runs remove_dir_all again → NotFound → Ok(()) → returns... wait:\n\napply: `if self.is_clean() { return Ok(false) }` — stale plan says dir_present=true → not clean → tracked empty → skip git rm → remove_dir_all → NotFound (already deleted) → Ok → return Ok(true). So the second call returns Ok(true), harmless (dir already gone). But views_applied=true then prints \"  views directory removed\" — correct summary anyway.\n\nHmm, but wait — in the bail case, cleanup runs once; in the fall-through case, cleanup runs once (line 166) — unless I put the cleanup only inside the bail condition. Let me restructure to avoid double-apply:\n\n```rust\n    if !is_git {\n        if !dry_run {\n            views.apply(root)?;\n        }\n        if to_branch || !migration.old_files.is_empty() {\n            bail!(\n                \"{} is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\",\n                root.display()\n            );\n        }\n    } else {\n        refuse_dirty_tree(root)?;\n    }\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n```\n\nThe double-apply is harmless (idempotent) but ugly. Alternatively for the non-git bail branch, do cleanup then bail without falling through:\n\n```rust\n    if !is_git && (to_branch || !migration.old_files.is_empty()) {\n        if !dry_run {\n            views.apply(root)?;\n        }\n        bail!(\"...\");\n    }\n    if !is_git {\n        // views-only: main flow handles it, minus refuse_dirty_tree\n    }\n```\n\nHmm, that leaves the main flow needing is_git-gated refuse:\n\n```rust\n    if is_git {\n        refuse_dirty_tree(root)?;\n    }\n```\n\nBut then in the non-git views-only path, line 166 `views.apply` runs once. And in the non-git refused path, we bailed already. No double apply. \n\nWait, one problem: in the refused path with dry_run=false — views.apply's Result is discarded (just `views.apply(root)?;`). Fine.\n\nBut hmm, there's still something: the bail path in dry_run: `--to-branch --dry-run` non-git → no cleanup (dry run) → bail. Good: dry run changes nothing, refuses up front.\n\nAnd non-git v1 --dry-run → bail without any changes. Good.\n\nNow — the message. Brief: `\"<root> is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\"`. I'll use exactly that.\n\n4. ViewsMigration::plan: take `is_git: bool` param. Tests call `ViewsMigration::plan`? Let me grep... The test `views_summary_reports_post_apply_state_not_pre_apply_counts` constructs ViewsMigration directly. No test calls ViewsMigration::plan directly? Let me check the file... I only see `views_summary_lines` calls in tests. Let me grep the crate for other callers of `ViewsMigration::plan` and `run_at`.\n\nAlso need to check: is `run_at` called from elsewhere (e.g., project.rs command handler)? Let me grep.\n\nNow the tests to add:\n\nTest 1: non-git v1 fixture — migrate deletes views, refuses the rewrite with the plain message, leaves v1 files untouched.\n\nBuild a non-git root with a v1 project (version 1, tasks/decisions/glossary files) AND views dir. Register project. run_at(home, root, false, false) → expect Err with message containing \"is not a git work tree\". Assert:\n- `.orgasmic/views` gone (deleted).\n- v1 files intact: `tasks/todo.org` still exists with original content, `project.org` still has version 1, `decisions.org` exists, no `tasks/TASK-A/` dir created.\n\nTest 2: non-git --to-branch refused before any git call.\n\nNon-git root, v1 (or v2?) with to_branch=true. \"refused before any git call\" — with a v1 fixture the gate order: cleanup then bail — message is the plain one, not \"branch cutover failed before this run changed the repository\". Assert error contains \"is not a git work tree\" and not \"branch cutover\". For \"before any git call\": on a non-git root every real git call fails, so if it reached create_orphan_branch we'd get a different error. Also assert views deleted (cleanup ran first) and v1 files intact.\n\nHmm — should test 2 use a v2+views fixture (like the existing non-git test) so the only difference is to_branch=true? Either works. The brief: \"Non-git `--to-branch`: refused before any git call.\" I'll use a v2+views fixture similar to the existing one — cleanup deletes views, then to_branch bails. Actually wait — for a v2 project with old_files empty and to_branch=true: gate fires (to_branch true) → cleanup (deletes views) → bail. Hmm, but is that the RIGHT behavior for a v2 non-git root with to_branch? Today: refuse_dirty_tree Ok → views.apply → to_branch && !dry_run && !is_ledger_root → if registered root is NOT the ledger → migrate_to_branch → dies in create_orphan_branch. If root IS the ledger (registered in place)... `register_project(&home, &root, ...)` — does it register with the ledger at home or the root path? In the existing test `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet`, after register_project(home, root, \"plaindir\", \"main\"), is_ledger_root(home, root, ...) — if register copies the project to a ledger under home, root != ledger. Let me check `projects::register_project`.\n\nI need to look at orgasmic-core projects.rs register_project to understand. Actually the first early return requires `is_ledger_root` — for the plain-dir test, if register_project registers the root as its own ledger... let me just read the code.\n\nAlso the third test consideration: \"cover the println path once in the real-apply test if cheap\" — i.e., assert one summary line by capturing `views_summary_lines` output at the call site rather than stdout. Hmm: \"cover the println path once in the real-apply test if cheap.\" The println path = the `println!(\"{line}\")` loop at line 187-189. Capturing stdout is awkward; the brief suggests \"assert one real-apply summary line by capturing the lines from `views_summary_lines` at the call site\". Meaning in the real-apply test (e.g., `plain_branch_views_doctor_warns_migrate_untracks_then_doctor_quiet`), call `views_summary_lines(&views, views_applied, false)` — but views/views_applied are internal to run_at... \"at the call site\" — hmm. Maybe: after run_at, recompute `ViewsMigration::plan` and call views_summary_lines with views_applied=true to assert what WOULD have printed? That doesn't cover the println path itself.\n\nActually \"cover the println path once in the real-apply test if cheap\" — the println path in views_summary_lines' caller. Since we can't capture stdout easily in tests (well, we could with gag crates, not available), the \"if cheap\" suggests: restructure so run_at returns or the summary is computable. Simplest cheap approach: in the real-apply git test, after run_at, re-plan views and assert `views_summary_lines(&replanned, true, false) == vec![\"  views untracked and directory removed\"]`. Hmm, but the summary function with post-apply plan: tracked empty, dir_present false → `(true, false) => return Vec::new()` → empty. Not useful.\n\nAlternatively assert with the PRE-apply plan (captured before run_at) and views_applied=true. In the test, seed gives tracked=1 file + dir present → views_summary_lines(&pre_plan, true, false) == [\"  views untracked and directory removed\"]. That validates the real-apply summary line content. It's a bit synthetic but \"if cheap\" — it is cheap. Where's the \"real-apply test\"? `plain_branch_views_doctor_warns_migrate_untracks_then_doctor_quiet` (real apply on git repo) or the non-git one. The brief says \"in the real-apply test\". I'll add to `plain_branch_views_doctor_warns_migrate_untracks_then_doctor_quiet`: capture plan before run_at, assert the line after.\n\nHmm, is that worth it? It's cheap: \n```rust\nlet views_plan = ViewsMigration::plan(&repo, true).unwrap();\n...run_at...\nassert_eq!(views_summary_lines(&views_plan, true, false), vec![\"  views untracked and directory removed\".to_string()]);\n```\nWait, ViewsMigration::plan now takes is_git param. In the test, repo is git → true. OK.\n\nHmm, actually — maybe better: make this assertion in the NEW non-git v1 test (a real-apply test): pre-plan views (non-git → tracked empty, dir_present true), then views_summary_lines(&views_plan, true, false) == [\"  views directory removed\"]. Both cheap. I'll do it in the non-git v1 test since that's the new path. Actually the brief says \"the real-apply test\" — ambiguous which. The existing git real-apply test already exercises views_summary_lines indirectly. Let me just add it to the new non-git v1 test (which IS a real apply — views get deleted).\n\nNow, let me also double check the gate doesn't break `ledger_without_remote_views_doctor_warns_migrate_untracks_then_doctor_quiet`: ledger is a git repo (seed_views_repo inits git). is_git=true → git path unchanged. Good.\n\nAnd `branch_cutover_is_orphan_dry_run_idempotent_and_worker_discoverable`: all git. Good.\n\nNow `run` (line 149) calls run_at — the CLI surface. Also check other callers of run_at / ViewsMigration::plan / refuse_dirty_tree across the crate.\n\nLet me also check doctor.rs references? The brief says in doctor.rs touch nothing. `push_tracked_views_findings` is called in tests here — that's doctor.rs's function, unchanged.\n\nNow about `--to-branch` refusal placement vs the first early-return: For the non-git ledger-root + to_branch + everything clean case, early return fires first → \"already migrated\" — preserved. For non-git + to_branch + NOT clean/ledger → gate bails. Good.\n\nWait, one more: what about `is_ledger_root` when root is non-git v1 and to_branch=false? Gate: `to_branch || !old_files.is_empty()` → rewrite pending → cleanup then bail. Right.\n\nWhat about non-git, v2, views clean, no to_branch: gate skipped (old_files empty, no to_branch) → line 167 false → old_files empty → views.is_clean() true → \"already migrated\". Today: same. Good.\n\nNow, one design question: where exactly to put the gate relative to `refuse_dirty_tree`. Order in run_at after early return:\n\n```rust\n    if !is_git {\n        if !dry_run {\n            views.apply(root)?;\n        }\n        if to_branch || !migration.old_files.is_empty() {\n            bail!(\n                \"{} is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\",\n                root.display()\n            );\n        }\n    } else {\n        refuse_dirty_tree(root)?;\n    }\n```\n\nWait, but if we bail we did the cleanup; if we don't bail we fall through and line 166 does views.apply again (double). As analyzed, second apply is a harmless no-op returning Ok(true)... wait no: after first apply deleted the dir, second apply: is_clean()? plan dir_present=true (stale) → not clean → remove_dir_all → NotFound → ignored → Ok(true). Harmless but runs remove_dir_all twice. Ugly. Clean it up: only apply once:\n\n```rust\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n    if !is_git && (to_branch || !migration.old_files.is_empty()) {\n        bail!(\"...\");\n    }\n    if is_git {\n        refuse_dirty_tree(root)?;\n    }\n```\n\nBut this reorders views.apply BEFORE refuse_dirty_tree on git trees → dirty trees get views deleted before refusal. BAD (destructive before refusal). Unless... refuse_dirty_tree excludes views; a dirty-for-other-reasons tree: today views are NOT deleted (refuse first). If reordered, views deleted THEN refuse. That changes git behavior (must not).\n\nSo keep order: refuse (git) → apply. For non-git: apply → bail. The `if !is_git { ... } else { refuse }` block with fall-through double-apply is the issue. Fix: structure as:\n\n```rust\n    if !is_git {\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if to_branch || !migration.old_files.is_empty() {\n            bail!(\"...\");\n        }\n        println! ... summary ...\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n    ... rest unchanged ...\n```\n\nThis duplicates the summary print (lines 177-189 partially). The to_branch tail (190-211) never runs on the non-git path (to_branch bails). So the non-git summary is: DRY RUN/MIGRATED, headings (empty), nodes, bytes, heading_round_trip, views lines. I could extract a helper `print_summary(migration, views, views_applied, dry_run)` for the shared part (everything except the to_branch tail), used by both paths. That keeps it DRY:\n\n```rust\nfn print_summary(migration: &Migration, views: &ViewsMigration, views_applied: bool, dry_run: bool) {\n    println!(\"{}\", if dry_run { \"DRY RUN\" } else { \"MIGRATED\" });\n    for (collection, count) in &migration.headings {\n        println!(\"  {collection}.nodes {count}\");\n    }\n    println!(\"  nodes {}\", migration.nodes.len() + migration.in_place_nodes);\n    println!(\"  bytes {}\", migration.bytes);\n    println!(\"  heading_round_trip byte-for-byte\");\n    for line in views_summary_lines(views, views_applied, dry_run) {\n        println!(\"{line}\");\n    }\n}\n```\n\nThen git path: print_summary(...); if to_branch { ...tail... }. Non-git path: print_summary(...); return Ok(()).\n\nThis is clean and minimal. Let me now also handle: on the non-git path, does the early \"already migrated\" for views-only need to stay? Line 169-173: `else if migration.old_files.is_empty() { if views.is_clean() { println!(\"already migrated\"); return Ok(()); } }`. In my restructure, the non-git path with views clean (dir absent, tracked empty): gate skipped → falls to `views.apply` → is_clean() → Ok(false) → views_applied=false → summary prints \"MIGRATED nodes 0 bytes 0...\" with NO views line (views_summary_lines returns empty when !views_applied). Hmm wait — today, for non-git v2 + views CLEAN + no to_branch: line 170 `views.is_clean()` → true → \"already migrated\" → return. With my restructure returning early via the non-git branch, I'd print the full \"MIGRATED / nodes 0 / bytes 0\" summary instead of \"already migrated\"! Behavior change on a non-git root. Hmm!\n\nWait let me recheck: non-git path in my restructure skips the `else if migration.old_files.is_empty() { if views.is_clean() ... }` check. Today that check applies to BOTH git and non-git. To preserve exactly: the non-git branch must also do the already-migrated check. Include it:\n\n```rust\n    if !is_git {\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if to_branch || !migration.old_files.is_empty() {\n            bail!(\"...\");\n        }\n        if !dry_run && views.is_clean() {\n            println!(\"already migrated\");\n            return Ok(());\n        }\n        print_summary(&migration, &views, views_applied, dry_run);\n        return Ok(());\n    }\n```\n\nWait, today: `else if migration.old_files.is_empty()` — reached when NOT (to_branch && !dry_run && !is_ledger_root). For non-git, to_branch already bailed in my gate, and old_files empty in the fall-through. The condition in the original: `if views.is_clean()` — no dry_run condition! So today, non-git v2 views-clean DRY RUN also prints \"already migrated\" and returns. Hmm wait: dry_run + old_files empty + views clean → \"already migrated\" (not DRY RUN). Let me preserve: `if views.is_clean() { println!(\"already migrated\"); return Ok(()); }` without dry_run condition. Since in the non-git fall-through old_files is always empty, this matches line 169-173 exactly (for non-git inputs).\n\nHmm, wait: does views.apply run before that check today? Yes (line 166 before 169). But if views.is_clean() (plan says clean), apply returns Ok(false) without touching anything. Fine.\n\nSo non-git branch:\n\n```rust\n    if !is_git {\n        // No VCS to recover from: only the dec_XH2XY views cleanup runs here;\n        // the v1→v2 rewrite and --to-branch are refused up front.\n        let views_applied = if dry_run { false } else { views.apply(root)? };\n        if to_branch || !migration.old_files.is_empty() {\n            bail!(\n                \"{} is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\",\n                root.display()\n            );\n        }\n        if views.is_clean() {\n            println!(\"already migrated\");\n            return Ok(());\n        }\n        print_summary(&migration, &views, views_applied, dry_run);\n        return Ok(());\n    }\n```\n\nHmm wait, order: should the bail come before or after cleanup? Acceptance: \"migrate deletes views, refuses the rewrite with the plain message\". So cleanup first, then bail. Yes, as above.\n\nAnd the \"already migrated\" check after the bail gate: for non-git, old_files empty (else bailed), so `views.is_clean()` alone ≡ original `else if old_files.is_empty() { if views.is_clean() }`. \n\nNow the git path stays byte-identical except: refuse_dirty_tree loses its early return, ViewsMigration::plan signature, and the summary extraction.\n\nGit path after restructure:\n\n```rust\n    refuse_dirty_tree(root)?;\n    let views_applied = if dry_run { false } else { views.apply(root)? };\n    if to_branch && !dry_run && !is_ledger_root(home, root, &migration.project_source)? {\n        migrate_to_branch(home, root, &migration)?;\n    } else if migration.old_files.is_empty() {\n        if views.is_clean() {\n            println!(\"already migrated\");\n            return Ok(());\n        }\n    } else if !dry_run {\n        apply_with_recovery(root, &migration)?;\n    }\n    print_summary(&migration, &views, views_applied, dry_run);\n    if to_branch {\n        ... tail unchanged ...\n    }\n    Ok(())\n```\n\nGood — git behavior preserved.\n\nrefuse_dirty_tree after removing the early return: the comment about the views exemption stays; the function now assumes a git tree (call sites: only run_at, now gated). Let me check for other callers of refuse_dirty_tree... I'll grep.\n\nNow the tests:\n\n```rust\n#[test]\nfn non_git_v1_root_deletes_views_then_refuses_rewrite_keeping_v1_files() {\n    let tmp = tempfile::tempdir().unwrap();\n    let home = Home::at(tmp.path().join(\"home\"));\n    let root = tmp.path().join(\"plain\");\n    let dotorg = root.join(\".orgasmic\");\n    std::fs::create_dir_all(dotorg.join(\"tasks\")).unwrap();\n    std::fs::create_dir_all(dotorg.join(\"views\")).unwrap();\n    std::fs::write(\n        dotorg.join(\"project.org\"),\n        \"#+orgasmic_version: 1\\n\\n* PROJECT plainv1\\n:PROPERTIES:\\n:ID: plainv1\\n:END:\\n\",\n    ).unwrap();\n    std::fs::write(\n        dotorg.join(\"tasks/todo.org\"),\n        \"* TODO TASK-A title\\n:PROPERTIES:\\n:ID: TASK-A\\n:END:\\n\",\n    ).unwrap();\n    std::fs::write(\n        dotorg.join(\"decisions.org\"),\n        \"* dec_A choice\\n:PROPERTIES:\\n:ID: dec_A\\n:END:\\n\",\n    ).unwrap();\n    std::fs::write(dotorg.join(\"glossary.org\"), \"#+title: empty\\n\").unwrap();\n    std::fs::write(dotorg.join(\"views/board.org\"), \"#+title: derived\\n\").unwrap();\n    projects::register_project(&home, &root, \"plainv1\", \"main\").unwrap();\n    assert!(!git_ok(&root, &[\"rev-parse\", \"--is-inside-work-tree\"]));\n\n    let views_plan = ViewsMigration::plan(&root, false).unwrap();\n\n    let error = run_at(&home, &root, false, false).unwrap_err();\n    let message = format!(\"{error:#}\");\n    assert!(message.contains(\"is not a git work tree\"), \"{message}\");\n    assert!(message.contains(\"init git or back up .orgasmic first\"), \"{message}\");\n    // Views cleanup still ran.\n    assert!(!dotorg.join(\"views\").exists());\n    assert_eq!(\n        views_summary_lines(&views_plan, true, false),\n        vec![\"  views directory removed\".to_string()]\n    );\n    // The v1 rewrite never ran.\n    assert!(dotorg.join(\"tasks/todo.org\").is_file());\n    assert_eq!(\n        std::fs::read_to_string(dotorg.join(\"tasks/todo.org\")).unwrap(),\n        \"* TODO TASK-A title\\n:PROPERTIES:\\n:ID: TASK-A\\n:END:\\n\"\n    );\n    assert!(!dotorg.join(\"tasks/TASK-A\").exists());\n    assert!(dotorg.join(\"decisions.org\").is_file());\n    assert_eq!(\n        std::fs::read_to_string(dotorg.join(\"project.org\")).unwrap(),\n        \"#+orgasmic_version: 1\\n\\n* PROJECT plainv1\\n:PROPERTIES:\\n:ID: plainv1\\n:END:\\n\"\n    );\n}\n\n#[test]\nfn non_git_to_branch_refused_before_any_repo_change() {\n    let tmp = tempfile::tempdir().unwrap();\n    let home = Home::at(tmp.path().join(\"home\"));\n    let root = tmp.path().join(\"plain\");\n    let dotorg = root.join(\".orgasmic\");\n    std::fs::create_dir_all(dotorg.join(\"views\")).unwrap();\n    std::fs::write(\n        dotorg.join(\"project.org\"),\n        \"#+orgasmic_version: 2\\n\\n* PROJECT plainbr\\n:PROPERTIES:\\n:ID: plainbr\\n:END:\\n\",\n    ).unwrap();\n    std::fs::write(dotorg.join(\"views/board.org\"), \"#+title: derived\\n\").unwrap();\n    projects::register_project(&home, &root, \"plainbr\", \"main\").unwrap();\n\n    let error = run_at(&home, &root, false, true).unwrap_err();\n    let message = format!(\"{error:#}\");\n    assert!(message.contains(\"is not a git work tree\"), \"{message}\");\n    assert!(!message.contains(\"branch cutover\"), \"{message}\");\n    // Views cleanup ran; nothing else changed.\n    assert!(!dotorg.join(\"views\").exists());\n    assert!(dotorg.join(\"project.org\").is_file());\n}\n```\n\nWait — for the to_branch test with v2 and views: register_project(&home, &root, ...) — need to check whether \"already migrated\" early return could fire: requires `is_ledger_root(home, root, ...)`. If register_project registers root itself as the ledger path... Let me check projects::register_project. In `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet` (existing test), register happens with the root, and then run_at(.., false, false) proceeds past the early return (to_branch=false so early return can't fire). For to_branch=true in my new test: early return requires old_files empty (yes, v2) && views.is_clean() (views present → NOT clean) → early return skipped. Good, no is_ledger_root issue.\n\nHmm wait, but also — for to_branch + v1 fixture: old_files non-empty → early return skipped → gate fires. Either fixture works. I'll use v2+views to differentiate from test 1 (v1+views). Both cases covered.\n\nNow \"refused before any git call\" — how to assert? On a non-git root, `git` (the helper) would fail with a non-success status (git prints \"fatal: not a git repository\"). migrate_to_branch → create_orphan_branch → git_env read-tree → git() → bail \"git read-tree --empty failed: fatal: not a git repository\" wrapped in \"branch cutover failed before this run changed the repository\". So asserting the message contains the plain gate text and NOT \"branch cutover\"/\"git read-tree\" proves no mutating git call ran (the only git calls before the gate: the probe itself, which is expected). I'll assert `!message.contains(\"git \")` maybe too strong... the plain message has no \"git \" except... \"init git or back up\" — contains \"git\". OK just assert contains plain text and not \"branch cutover\" and not \"not a git repository\" (git's own fatal). Good.\n\nNow, register_project — let me look at its signature/behavior to make sure the fixture is right. Also `Home::at`. The existing tests show usage; I'll mirror them.\n\nOne concern: `run` uses `find_project_root` — tests use run_at directly. Fine.\n\nAlso: does anything else call `ViewsMigration::plan` or `refuse_dirty_tree` elsewhere in the crate? Let me grep. And check doctor.rs's push_tracked_views_findings for its own probe (untouched).\n\nLet me also review the println-coverage request once more: \"Keep the summary helper; cover the println path once in the real-apply test if cheap.\" — I'll add the views_summary_lines assertion in the non-git v1 real-apply test (cheap, done above). Good enough. Hmm, \"the println path\" — the loop `for line in views_summary_lines(...) { println!(\"{line}\") }`. I can't capture stdout without extra deps. The brief itself offers the alternative: \"assert one real-apply summary line by capturing the lines from views_summary_lines at the call site rather than stdout\". My approach matches.\n\nWait, \"at the call site\" — maybe they mean: inside run_at, the call site... no, in the test. OK.\n\nNow let me check clippy: `print_summary` takes several refs — fine. Unused `Result` — print_summary returns () (println! can panic, fine).\n\nLet me also double check the bail message uses `root.display()`. Brief's message: \"<root> is not a git work tree; the v1→v2 rewrite and --to-branch need a repository to recover from — init git or back up .orgasmic first\". Note the em-dash. Keep ASCII? The codebase uses unicode in messages? E.g., line 493 uses plain text. The brief literally includes \"→\" and \"—\". I'll use the brief's exact text.\n\nNow implement. Steps:\n1. Edit run_at.\n2. Edit refuse_dirty_tree (remove early return).\n3. Edit ViewsMigration::plan signature.\n4. Add print_summary helper.\n5. Add two tests + summary-line assertion.\n6. Update existing test call sites of ViewsMigration::plan (only in run_at; tests construct directly).\n7. Gates.\n\nLet me first grep for other callers.The worktree is clean and on the right branch. Now let me check for other callers of the functions I'll touch, and look at `projects::register_project` to build correct fixtures.Good — all callers are in `project_migrate.rs` only. Let me quickly check `register_project` to confirm fixture behavior (does it register the root path itself or copy to a ledger?).All callers live in `project_migrate.rs`. Let me check `register_project` behavior briefly:`register_project` registers the project root itself on the board. So in the non-git fixture, `is_ledger_root(home, root, ...)` — `home.project_ledger(id)` is a different path under home, so `is_ledger_root` is false. Fine.\n\nNow I have everything I need. Let me make the edits:\n\n1. `run_at`: compute `is_git` once, pass to `ViewsMigration::plan`, add the non-git branch.\n2. `ViewsMigration::plan(root, is_git)`.\n3. `refuse_dirty_tree`: delete the non-git early return.\n4. Extract `print_summary` helper.\n5. Add the two tests.\n\nLet me write the edits.`register_project` registers the root itself on the board. I have the full picture now. Plan:\n\n- `run_at` detects the work tree once (shared probe with `ViewsMigration::plan`), and on a non-git root runs only the views cleanup, then bails on the rewrite/`--to-branch` with the plain message.\n- Delete the now-dead non-git early return in `refuse_dirty_tree`.\n- Two new tests.Now the edits. First, `ViewsMigration::plan` shares the probe:Now `run_at` — single probe, non-git branch, and the extracted summary helper:Now add the `print_summary` helper. I'll place it right before `run_at` or after it — let me put it right after `views_summary_lines` (before `run`), or between run_at and refuse_dirty_tree. Let me add it right before `refuse_dirty_tree` (after run_at ends). Actually, let me look at the current state around run_at's end to place the helper cleanly.Now add the `print_summary` helper after `run_at` and delete the dead early return in `refuse_dirty_tree`:Now add the two new tests. Let me place them after `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet` (line ~840 in the new numbering). Let me read the tests section around there to place them.Now the two new tests. Let me insert them after the existing non-git views test:Now let me double-check the full `run_at` and make sure everything is consistent. Let me read the current state of run_at.Let me review the final state of the modified section:The code looks good. One check: the \"already migrated\" in the non-git branch — the original condition (line 169-173 old) was `else if migration.old_files.is_empty() { if views.is_clean() ... }` with no dry_run condition. In my non-git branch, `views.is_clean()` with no dry_run condition matches. But note: on a non-git views-only dry-run where views is clean, it prints \"already migrated\" — same as before. And where views is NOT clean and dry_run: prints DRY RUN summary — same as before. Good.\n\nOne subtle thing: in the non-git branch, `views.is_clean()` after `views.apply` — apply doesn't mutate the plan (it's `&self`), so is_clean reflects the pre-apply plan state. Same as today (today line 170 checks the same stale plan after apply). Identical behavior.\n\nWait, one more check on the git-path \"already migrated\": today at line 169-173 the `else if migration.old_files.is_empty()` branch is reached in the git path when NOT(to_branch && !dry_run && !is_ledger_root). That's unchanged.\n\nAlso verify: does any existing test rely on refuse_dirty_tree's behavior on non-git roots? `non_git_project_views_doctor_warns_migrate_deletes_then_doctor_quiet` — non-git, v2, views present, to_branch=false → non-git branch: apply (deletes views), gate skipped (no to_branch, old_files empty), views.is_clean()? plan dir_present=true → not clean → print_summary → \"MIGRATED / nodes 0 / bytes 0 / heading_round_trip / views directory removed\". Previously: same output (the shared code). Test only asserts views gone + project.org present + doctor quiet. Passes.\n\n`ledger_without_remote_views...` and `plain_branch_views...` — git repos, unchanged path.\n\n`migration_is_verbatim_dry_run_and_idempotent` — calls plan/apply directly, unaffected.\n\n`partial_apply_error_names_exact_recovery_commands` — apply_with_recovery directly, unchanged.\n\n`branch_cutover_is_orphan_dry_run_idempotent_and_worker_discoverable` — git repos throughout; the first run_at is dry-run to_branch on a git repo → git path, refuse_dirty_tree (clean) → ... unchanged. Good.\n\nNow also check: `git_ok` is still used — yes, in run_at, tests, create_orphan_branch. No unused warnings.\n\nMSRV 1.87 gotcha — I'm not using any new std APIs. Fine.\n\nComments: the codebase uses comments liberally (existing file has many), so my comments fit conventions. The system prompt says no comments unless asked — but AGENTS.md-style repo conventions here clearly use explanatory comments on non-obvious logic, and the brief's fix intent benefits from the dec_XH2XY reference, mirroring the existing comment style in this file (lines 56-58, 216-218). I'll keep them minimal — they match existing file style.\n\nNow run the gates. First the targeted test. Note the gotchas: never pipe cargo output — redirect to a log file. Also the targeted filter: `cargo test -p orgasmic-cli --bin orgasmic -- migrate doctor`. Hmm — that filter runs tests with \"migrate\" or \"doctor\" in the name? libtest treats the filter as substring match, one filter = matches names containing \"migrate\"... wait, `-- migrate doctor` passes two filters \"migrate\" and \"doctor\"; libtest matches ANY of the filters (OR). Good, that's the intended targeted set.\n\nAlso, worker environment: tests that spawn git are fine. ORGASMIC_HOME must NOT be set — check env. Also ORGASMIC_RUN_ID might be set in worker env? The gotcha about re-running: `env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME` was for daemon tests. For cli bin tests, let me just run as-is first, redirected to a log file.\n\nLet me run the three gates sequentially (they can't run in parallel — cargo locks the target dir). Start with the test gate.Code is in place. Now the gates — each to a log file, never piped. First the targeted test:My v1 fixture is incomplete: `plan()` requires ALL six task-state files (`backlog.org` etc.) to exist for a v1 project (line ~303: `if !source_path.is_file() { bail!(\"migration source is missing\") }`). I need all six `tasks/<state>.org` files. Let me mirror the fixture shape from `migration_is_verbatim_dry_run_and_idempotent` which writes all TASK_STATES.\n\nLet me fix the fixture: write all six state files.My v1 fixture is incomplete — `plan` requires all six task-state files. Fixing:Test gate green: 33 passed, 0 failed. Let me confirm my two new tests actually ran (they contain \"migrate\"? `non_git_v1_root_deletes_views_then_refuses_rewrite_keeping_v1_files` — hmm, filter is \"migrate\" and \"doctor\" substrings. \"non_git_v1_root_deletes_views_then_refuses_rewrite_keeping_v1_files\" does NOT contain \"migrate\" or \"doctor\"! Neither does \"non_git_to_branch_refused_before_any_repo_change\". So they were filtered out! Let me check: 277 filtered out — were my new tests in the 33 or filtered?\n\nThe filter matches substring in the full test path `project_migrate::tests::...` — \"project_migrate\" contains \"migrate\"! So all project_migrate tests match. Let me verify by grepping the log for my test names.Green — but let me confirm my new tests actually ran (filters are `migrate`/`doctor`; the module path `project_migrate::` should match):Both new tests ran and passed (the module path `project_migrate` matches the \"migrate\" filter). Now clippy and fmt gates.Both new tests ran and passed. Now clippy and fmt:Formatting fixes needed. Run `cargo fmt` to apply, then re-verify with `--check`.Two formatting nits — apply fmt and re-check:fmt is green now. Since fmt changed the file after the test run, re-run the targeted test + clippy to be safe (fmt only reformats, but the gates should be on the final tree). Quick re-run of the test gate and clippy.fmt changed the file after the gates ran — re-run test and clippy on the final tree:All three gates green on the final tree:\n- test: 33 passed, 0 failed (includes 2 new tests)\n- clippy -D warnings: exit 0\n- fmt --check: exit 0\n\nLet me do a final review of the diff, then commit and finalize. The brief says: one commit `TASK-JWHXH.3.2: fix(cli): <one line>`, then `orgasmic dispatch finalize --summary-file <path>` with `--commit` since this is a small known-scope fix (per Completion section: \"for a small known-scope fix pass --commit so the change lands in the same finalize call\").\n\nWait — re-reading the Rules: \"Report: ... Finish with `orgasmic dispatch finalize --summary-file <path>` (report only, no `--commit`).\" But the Completion section says: \"Same contract as base_worker; for a small known-scope fix pass --commit so the change lands in the same finalize call.\"\n\nHmm, conflicting. The Rules say \"(report only, no --commit)\" while Completion says \"for a small known-scope fix pass --commit\". The Completion section is specifically about the commit step: it says for a small known-scope fix pass --commit so the change lands in the same finalize call. This IS a small known-scope fix round. The Completion section appears to override/refine the generic rule for this dispatch kind. So: make the commit locally, then `orgasmic dispatch finalize --summary-file <path> --commit`.\n\nWait, \"pass --commit so the change lands in the same finalize call\" — does --commit make finalize itself create the commit? Or does it record the commit I already made? I think finalize --commit records/lands my pre-made commit as part of the dispatch record. Let me check `orgasmic dispatch finalize --help` first.\n\nLet me review the final diff before committing.All gates green on the final tree. Let me review the full diff before committing:The diff is exactly scoped: one file, the gate, the shared probe, the dead early-return deletion, the summary helper, and the two tests. Git-repo path is byte-identical in behavior.\n\nNow commit. The commit message format: `TASK-JWHXH.3.2: fix(cli): <one line>`. E.g., `TASK-JWHXH.3.2: fix(cli): refuse migrate rewrite and --to-branch on non-git roots after views cleanup`.\n\nThen write the summary file and finalize. Let me check `orgasmic dispatch finalize --help` to get flags right.Diff is exactly scoped. Commit and check the finalize contract:The commit is made (8c460413). Now write the summary file and finalize with `--commit` (worktree is already clean, so `--commit` will just record HEAD sha — per the help: \"if the worktree was already clean, the current HEAD\").\n\nWait — the rules said \"Finish with `orgasmic dispatch finalize --summary-file <path>` (report only, no `--commit`)\" but Completion said \"for a small known-scope fix pass --commit so the change lands in the same finalize call\". I already made the commit myself. Since the worktree is clean, `--commit` records HEAD. The Completion instruction is the more specific one for this dispatch; and since I committed already, passing `--commit` is harmless (clean tree → records current HEAD). Actually, to follow the brief's Completion contract (\"pass --commit so the change lands in the same finalize call\"), I'll pass --comm"
    }
  ],
  "narrative_truncated": true
}
