#+title: orgasmic task TASK-RCWFF
#+orgasmic_version: 2

* DONE TASK-RCWFF rmux send_input: deliver followup turns into a live TUI session :runtime:drivers:
:PROPERTIES:
:ID: TASK-RCWFF
:IMPLEMENTS: arch_A53QX.4 arch_045Q0.2
:END:

Implement WorkerDriver::send_input for the rmux mode. The pane-level primitive already exists — paste_text_and_submit (paste-buffer -p -b + send-keys Enter, modes/rmux.rs:940) is used once at dispatch to paste the brief; wire the trait method to the same verb set so POST /runs/:id/input -> supervisor.send_input (which already records composer_send) lands a new user turn in the live harness pane.

Requirements:
- Re-append the end-of-turn-marker instruction to every followup (same contract as the dispatch paste) so the babysitter's per-round stall detection keeps working.
- Define mid-turn behavior deliberately (queue vs reject with a clear ack message) and test it; a paste landing while the harness is streaming must not corrupt the turn.
- No supervisor/API changes expected — the plumbing exists; this is driver-mode work plus tests.
- Rendered-screen tests mirroring the existing dispatch-paste tests: input lands as a user turn, EOT marker fires at round end, composer_send tx recorded.

