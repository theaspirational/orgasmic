#+title: orgasmic task TASK-EKF7T
#+orgasmic_version: 2

* DONE TASK-EKF7T Whole-file session readers remain where lifecycle facts suffice: the completion watcher re-reads the growing transcript on every poll
:PROPERTIES:
:ID: TASK-EKF7T
:PRIORITY: P2
:END:

From TASK-7QM8M (2026-07-29), the boot path's siblings: (1) spawn_dispatch_completion_watcher (api.rs:5875 at its HEAD) calls read_session_file on EVERY POLL while waiting for release — the whole growing transcript, repeatedly; strictly worse than the boot path 7QM8M fixed, which paid it once. It needs only terminal lifecycle events (the scanner's tail window). (2) stage_outcome_from_session (api.rs:6394) reads whole for a stage's terminal outcome — one read, lifecycle-only consumption, same substitution. (3) The strictness decision 7QM8M surfaced: a live run with one torn transcript line is now boot-reattachable and inventory-visible but still UNREADABLE to these whole-file callers (read_session_file is all-or-nothing) — decide whether that strictness is right for any caller that only wants lifecycle facts, and state it where the callers live. Apply 7QM8M's scan_session_lifecycle substitution with its final_envelope_retained discipline; measure the watcher's per-poll cost before/after on a large live transcript.
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit 8e9b5129 on sprint/EKF7T, merged onto local sprint-20260902 as 034daaed. api.rs: SessionTail{offset,partial,envelopes}.poll() next to spawn_dispatch_completion_watcher — stat; len<offset resets to 0; len==offset reads nothing; else seek+read_to_end, parse only complete new lines, carry the partial; retains only Lifecycle envelopes + run_complete/run_fail/run_error driver events. Supervisor snapshot check stays first. Malformed terminated line -> Err without advancing (whole-file strictness preserved). stage_outcome_from_session left whole-file (one read at terminal time; needs driver_error/Note ordering). Tests: session_tail_incremental_reads_each_line_once_and_only_new_bytes (bytes_read per poll == appended bytes), _restarts_from_zero_on_truncation, _malformed_terminated_line_errors_without_advancing. Gates (private target): daemon 26 + 18 passed, clippy rc=0, fmt rc=0. Operator review/test after the sprint.
- 2026-09-02 sprint review (daemon reviewer): CONFIRMED correct. SessionTail offset/partial accounting is right including the error-restore path; truncation restarts from 0; malformed terminated lines keep whole-file strictness. Its retained-envelope filter (Lifecycle + run_complete/run_fail/run_error) covers exactly what every watcher predicate consults, and stage_outcome_from_session deliberately keeps the whole-file read.

