#+title: orgasmic task TASK-RQ270.2.1
#+orgasmic_version: 2

* DONE TASK-RQ270.2.1 Strip the @arch back-edges stage B missed, and stop the schema legend naming a deleted verb
:PROPERTIES:
:ID: TASK-RQ270.2.1
:PRIORITY: P1
:FIX_SUBTASK: t
:WRITE_SCOPE: crates/orgasmic-core/,ui/
:END:

** Description

Fix round for the two BLOCK SHIP findings on TASK-RQ270.2's review. Both are
confirmed by the manager. The full record is on that task under "Review round 1 —
REJECT (finding 1 is a manager inventory error)". Read it there.

** Finding 1 — 26 =// @arch= back-edges survive in =ui/=

The deleted comparator accepted FOUR prefixes, not two
(=architecture_drift.rs:263-277= at base =f59ce41=): =// arch:=, =# arch:=,
=// @arch=, =# @arch=. Stage B stripped the first two. *The manager's inventory
only ever counted those two*, so the acceptance grep false-greened.

Nothing reads the survivors — the generic scanner (=marker.rs:169=) indexes only
=orgasmic:= payloads — so these are dead back-edges of exactly the kind stage B
exists to remove.

*Manager ruling on scope*: these amend stage B rather than becoming a follow-on.
=ui/**= enters scope *for comment stripping only*; stage C still owns every UI
behaviour, view and route.

** Finding 2 — a live command advertises the deleted one

=crates/orgasmic-core/src/schema_examples.rs:86='s =SOURCE_PATHS= note reads
"repo paths =architecture drift= checks against =// orgasmic:<id>= markers".
=architecture schema= is deliberately still live (stage C owns it) and
=print_node_schema= emits that legend verbatim.

Reword rather than delete: the note is also WRONG in a second way that predates
this stage — drift checked =// arch:=/=@arch=, never =orgasmic:=. Say what
=SOURCE_PATHS= means now, without naming a deleted verb.

=cli_parity= cannot catch this class: it extracts invocations from =shipped/=
only, not from generated schema text.

** Acceptance

- Zero matches for =^\s*(//|#)\s+@arch(\s|$)= outside =.orgasmic/= and
  =archive/=.
- Zero matches for the =arch:= forms as well; do not regress stage B's work.
- All 60 =orgasmic:arch_= markers unchanged, proven with a *per-path* comparison
  (path + marker content), not a content-only hash. The content-only form is
  blind to a marker moving between files, and marker PATHS are observable through
  =scan_project_markers= / =/graph/markers/:node_id=.
- No UI behaviour, view, route, type or import changes. Line-1 comment removal
  only.
- The schema legend names no deleted verb.
- =cli_parity= still passes; the CLI still offers =architecture {list,get,schema}=
  and not =drift=.

** Not in scope

Everything stage C and D own: the read model, =GET /architecture*=, the CLI reads,
the UI views themselves, the node class, the dangling-edge lint carve-out, and
=architecture.org=.
** Landed and verified by the manager
Merged =3536be8= (worker =e56af71=, base =1f14c35=). 27 files, +1/-27.

=== It derived the patterns from the reader, which was the point

The report states the patterns and their source: =f59ce41:architecture_drift.rs:263-277=,
whose =strip_prefix= chain accepted all four forms. That is the correction the
manager's own inventory failed to make, and the implementer applied it rather
than copying the manager's narrower grep.

=== Manager's independent verification on the merged tree

*All four retired prefixes, one regex, zero matches* outside =.orgasmic/= and
=archive/=:

: git grep -n -I -E '^[[:space:]]*(//|#)[[:space:]]+(@arch([[:space:]]|$)|arch:)' \
:   -- . ':(exclude).orgasmic/**' ':(exclude)archive/**'   ->  0

*The UI diff is comment-only, proven not asserted.* Every added/removed line in
=ui/= across =1f14c35..3536be8=, with =-\s*// @arch= filtered out, is EMPTY. 26
files, one line each. No behaviour, view, route, type or import moved.

*Per-path marker comparison, run by the manager across THREE revisions* —
pre-stage-B base, stage B, and this fix — normalizing to =path|marker-content=:

: f59ce41  1a0cec73b545a6127172233f22d7aad902f099206937daacc473f2ca1651e578  60 records
: bb06bc4  1a0cec73b545a6127172233f22d7aad902f099206937daacc473f2ca1651e578  60 records
: 3536be8  1a0cec73b545a6127172233f22d7aad902f099206937daacc473f2ca1651e578  60 records

Constant across all three. The 60 =orgasmic:arch_= markers survived BOTH rounds
in the same files with byte-identical content. (This hash differs from the
reviewer's =187fb552…= because the normalization differs; what matters is that it
is invariant across revisions, which is the property being claimed.)

*Real-binary probe of the reworded legend:*

: ./target/debug/orgasmic architecture schema
:   :SOURCE_PATHS: — repo paths that implement this architecture node

No deleted verb named, and the pre-existing conflation of =// arch:= with
=orgasmic:= markers is gone too.

=== Gates on the merged tree

- =scripts/run-tests.sh -p orgasmic-cli= — GREEN, 0 failures.
- =scripts/run-tests.sh -p orgasmic-core= — GREEN, 0 failures.
- =cargo test -p orgasmic-cli --test cli_parity= — 3 passed.
- =cargo clippy --workspace --all-targets -- -D warnings= — exit 0.
- =cargo fmt --all --check= — exit 0.

*The UI gates were not re-run by the manager, with a stated basis.* The
implementer ran =npm run typecheck= and =npm test= (38 files, 264 tests) green.
The manager's independent check that the UI diff contains NO line other than 26
=// @arch= removals is the stronger argument: a removed leading comment cannot
change typecheck or test behaviour unless it carried a directive, and the diff
filter would have surfaced any non-=@arch= removal. Re-running =npm ci= for a
provably comment-only diff would buy nothing.

=== No injection proof, agreed again

Dead-text removal and a prose correction — no red to pin. The authoritative-reader
greps, the per-path inventory, the emitted schema output and =cli_parity= are the
instruments. The previous reviewer endorsed this reasoning for stage B and it
applies unchanged.
** Review round 1 — REJECT (docs); fixed directly by the manager
Reviewer =reviewer-codex-tmux= (tmux/codex/gpt-5.6-sol/high) on merge =3536be8=
vs base =1f14c35=. REJECT, one BLOCK SHIP (docs). *Fixed directly by the manager
in =5491ef1=* — a two-line prose deletion did not warrant a dispatch round.

=== The finding, confirmed

=ui/README.md:23= said "Source modules carry =@arch arch_MK2Q2.*= annotations
linking them back to the architecture graph". This round removed all 26 of those
annotations, so the sentence became false BECAUSE of this change, and it was the
last live =@arch= reference outside =.orgasmic/= and =archive/=. Left alone it
would tell a contributor to recreate dead back-edges nothing indexes.

Same failure class as the schema legend this very task existed to fix.

=== THE THIRD NARROW CHECK IN ONE STAGE — the real lesson

Round 1: the manager grepped =arch:= and missed the =@arch= prefix entirely.
Round 2: the manager's acceptance criterion was
=^\s*(//|#)\s+@arch(\s|$)= — *anchored to the comment prefix, therefore blind to
prose by construction*. It could confirm the edit happened; it could not confirm
the goal was met.

The pattern is not carelessness about greps. It is *writing checks shaped like the
EDIT instead of shaped like the CLAIM.* The claim was "no live reference to the
retired syntax survives". The instrument for that is a bare =@arch= search across
the tree.

Adopted, and applied on the fix commit:

: @arch anywhere                       -> 0
: (//|#) arch: prefixes                -> 0
: architecture drift / architecture_drift -> 0
: orgasmic:arch_ markers               -> 60 (unchanged)

*Verify the claim with the broadest instrument, then narrow only to explain
failures.*

=== It answered the question that mattered most: no fifth prefix

The brief asked whether four prefixes was complete or complete-by-luck. The
reviewer read the deleted comparator in full: =run= has ONE marker collection
path (=collect_arch_markers= -> =arch_markers_at_file_top=), lines 264-277 contain
exactly four literal prefixes, and the other =starts_with= calls only skip a
shebang or unrelated comment lines. *No fallback scanner and no fifth prefix.*

It also caught a precision point the manager missed: the old =@arch= branches did
NOT require a whitespace boundary, so the manager's regex is not byte-equivalent
to the reader for an adjacent payload like =// @archarch_ABCDE.1=. It grepped for
such survivors: zero. That is the difference between "my pattern found nothing"
and "nothing is there".

=== It validated the manager's UI-gate skip on evidence, not deference

The brief asked for that judgement to be checked. The reviewer inspected every
changed UI hunk: each removes exactly one first-line =// @arch= comment, and no
removed line carried =@ts-expect-error=, an ESLint directive, a bundler pragma or
any other directive. On that evidence it agreed not repeating =npm ci=/typecheck/
tests was sound.

=== And it checked the legend was CORRECT, not merely verb-free

=ArchitectureNode::source_paths= is populated directly from =:SOURCE_PATHS:=
(=schema.rs:371,558=), and =.orgasmic/architecture.org:25-36= treats those paths
as the repository sources a node owns, coupled to its minimum tests. So "repo
paths that implement this architecture node" accurately describes the live
meaning until stage C removes the surface.
** Review round 2 — stage B COMPLETE; one stage-A finding carried
Reviewer =reviewer-codex-tmux= (tmux/codex/gpt-5.6-sol/high) on =5491ef1= and the
=3536be8..HEAD= delta. *"Excision stage B is COMPLETE."* The REJECT is for a
stage-A-facing doctrine defect, explicitly NOT for residual stage-B code or
marker syntax.

=== The manager's own fix was checked, not deferred to

The brief said a manager fixing a finding personally still owes a fresh review
and told the reviewer not to go easy on authorship. It checked: =ui/README.md=
loses only the false =@arch= paragraph, with no replacement claim, no dangling
prose and no broken Markdown.

=== It reproduced every broad claim, and improved the marker proof again

Bare =@arch= = 0; =(//|#) arch:= prefixes = 0; =architecture drift= /
=architecture_drift= = 0; tracked =architecture_drift= paths = 0;
=orgasmic:arch_= = 60.

And it rebuilt the per-path inventory across *four* revisions — =f59ce41=,
=bb06bc4=, =3536be8=, =HEAD= — 60 records each, one constant SHA-256, with the
=f59ce41= vs =HEAD= exact diff EMPTY. That is the strongest form of the claim yet:
no marker changed file or content across either stage-B round.

=== Stage B completeness — the verdict the previous round withheld

: "Stage B completeness verdict: YES. Its comparator is deleted, every syntax
:  spelling only that comparator read is absent, and the generic marker inventory
:  is byte-identical per path. Nothing remains for stage B."

It also swept =verify/README.md=, =.github/= and =shipped/project-scaffold/= and
found no retired syntax and no current architect-production claim there.

=== The finding, confirmed by the manager — and it belongs to STAGE A

Three live files still advertise the architect production surface that stage A
(TASK-RQ270.1) retired:

- =README.md:31= — "architector authors the architecture graph" as a live
  pipeline stage.
- =ui/README.md:49= — "Pipeline stages post to =/grill=, =/architect=, and
  =/plan=".
- =shipped/entry/router.org:8= — "worker role: implementer, *architect*, planner,
  reviewer, or griller".

Against the authoritative current surfaces the reviewer cites: =manager.rs:28-39=
accepts only implementer/reviewer dispatches, =api.rs:3603-3610= treats
=architect= as retired and exposes only grill/plan stage specs,
=integration.rs:2261= pins =/api/architect= as a RETIRED route, and no
architect/architector prompt spec exists under =shipped/prompt-studio/prompt-specs/=.

*The third is the one with teeth.* =shipped/entry/router.org= is compiled into
every worker prompt, so each dispatched worker currently receives a role list
naming a producer the product cannot launch.

=== Manager ruling on disposition

*Stage B closes; the finding is CARRIED, not dropped.* The reviewer's own verdict
is that stage B's objective is met and independently verified; the defect is
stage A's deliverable being incompletely retired. Blocking stage B on it would
conflate two stages whose ordering this excision deliberately separates.

Precedent, and the same shape: TASK-M47E5.2's six surviving findings closed with
that task and are carried by TASK-RMA18, which owns the redesign. Filed here as
*TASK-RQ270.1.1*.

*Dispatched rather than fixed by the manager*, unlike the README deletion. The
router change is not mechanical: the reviewer asks whether =architect= is
required as a LEGACY RECOGNITION path, and if so that it be described as legacy
with the consuming compiler/reattach path proven. That is investigation, not
prose deletion.

