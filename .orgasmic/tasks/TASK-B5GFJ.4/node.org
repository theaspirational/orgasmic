#+title: orgasmic task TASK-B5GFJ.4
#+orgasmic_version: 2

* DONE TASK-B5GFJ.4 fix-subtask: mock-peer lifecycle test + gated real-claude smoke :drivers:tmux:claude:fix:test:medium:
:PROPERTIES:
:ID:               TASK-B5GFJ.4
:PRIORITY:         P2
:WORKER:           implementer-codex-stdio
:IMPLEMENTS:       arch_A53QX
:PRODUCES:         crates/orgasmic-drivers/src/modes/tmux.rs
:WRITE_SCOPE:      crates/orgasmic-drivers/src/**
:READ_SCOPE:       crates/orgasmic-drivers/src/**
:TEST_CMD:         cargo test --workspace
:DEPENDS_ON:       TASK-B5GFJ.1
:END:

** Description
Reviewer F-4 (MEDIUM): Existing tests cover helper-level delta/EOT predicates and shell proxies (=sleep=, =cat=) but NOT the full lifecycle (acquire → Ready → TextChunk → EOT → release) and NOT real claude. Wouldn't catch F-1 or F-2.

Fix:
1. Add a mock-peer-style lifecycle test using a fake TUI command (a shell script printing text + the EOT marker on its own line + exiting). Asserts: Ready emitted, TextChunk envelopes for the script's output, RunComplete on EOT detection, supervisor run released, tmux session gone.
2. Add a real-wire test gated on =which tmux && which claude=. Acquires implementer-claude-tmux against a tiny test prompt, asserts the same lifecycle, releases cleanly. Mirrors the acp-stdio gated tests pattern.

** Acceptance Criteria
- [ ] Full-lifecycle mock-peer test added (acquire→Ready→TextChunk→EOT→release; uses fake TUI script).
- [ ] Real-wire smoke test added, gated on tmux + claude binaries present.
- [ ] Both tests pass under =cargo test --workspace= (real-wire test no-ops when binaries absent).
- [ ] Tests would have caught F-1 and F-2 had they existed pre-fix.

** Evidence
(populated at task close)

