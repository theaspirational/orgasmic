#+title: orgasmic task TASK-VT9FG
#+orgasmic_version: 2

* BACKLOG TASK-VT9FG harden supervisor probe deadline invariant and rmux recovery fixture proof    :supervisor:testing:
:PROPERTIES:
:ID: TASK-VT9FG
:PRIORITY: P2
:END:

** Description
Follow-up from independent review of TASK-GPY3H. Keep the approved spawn-bound repair intact while closing its test-only gaps:
- mechanically assert =PROBE_CHILD_DEADLINE < WORK_PROBE_TIMEOUT=;
- restore the rmux recovery test's proof that its fixture is attached to the owned endpoint before cleanup;
- tighten the additive spawn test's intentionally broad upper tolerance if a stable threshold is measured.

Do not fold TASK-T7KSH's unbounded =rmux -V= probe into this work. Treat a shared deadline budget across the three chained probes as a separately scoped design decision, not a quick patch.

** Acceptance
- A constant change that makes the child deadline exceed the outer timeout fails a deterministic test or compile-time assertion.
- The recovery fixture proves session containment at its owned rmux endpoint.
- Focused supervisor and API tests pass serially under load.

