#+title: In Progress
#+orgasmic_version: 1

* IN_PROGRESS TASK-TJKFC Preflight harness readiness and atomically abort fatal startup dispatches :P1:
:PROPERTIES:
:DEPENDS_ON: TASK-KWSTJ
:ID: TASK-TJKFC
:PRIORITY: P0
:END:

Problem: dispatch can create a worktree and enter the daemon before provider or harness authentication is known to be usable. A fatal startup DriverError can then leave an open dispatch record and resources that block later work. Add a bounded non-interactive preflight at the provider-local driver boundary after configuration is resolved but before lease, session, or manager.dispatch_started ownership is committed. The default capability must be explicit unsupported, not false success; supported adapters return ready or a typed fatal reason. Claude and ACP implementations must test the same execution context used by the worker and must never prompt. A definitive preflight failure returns a normal rejected dispatch so the existing CLI rollback restores lifecycle state and removes the newly created worktree and branch. Also make dispatch attempt creation atomic with the earliest driver events: a fatal event during the startup gate must record an aborted attempt, close any open dispatch, release the lease, and safely clean only attempt-owned resources. Once startup has been accepted and meaningful work has occurred, later failures retain normal failed-run history and inspection artifacts. Acceptance: fake-provider tests prove auth failure creates no lease, session, or open dispatch and CLI rollback removes attempt resources; a race test emits fatal before dispatch bookkeeping and proves no orphan; supported-preflight success reaches running; unsupported preflight preserves current drivers; a post-start fatal remains a failed historical run and does not erase work. Provider-specific parsing stays inside the driver adapter; manager and supervisor consume only typed readiness/startup outcomes.

PARTIAL IMPLEMENTATION (2026-07-25, manager-implementer). The startup gate
exists and is wired; the concrete probes and the atomic-abort half do not.

LANDED
- =Preflight= on =WorkerDriver=: three-valued Ready / Fatal{reason} /
  Unsupported, defaulting to Unsupported. The default is the point: a driver
  that never implemented a probe must not be able to report readiness it never
  checked. Only Fatal rejects a dispatch, so an inconclusive probe leaves the
  pre-existing behaviour intact.
- Called in =spawn_worker_run= immediately after =driver.validate=, which is the
  last point at which rejecting is free. Verified by reading the ordering rather
  than assuming it: the session path, babysitter, lease and acquire all come
  after, and =record_dispatch_started= runs after =spawn_worker_run= returns.
- A Fatal returns =400 Bad Request=. Verified this routes correctly:
  =dispatch_failure_needs_daemon_cleanup= treats any "daemon returned ..."
  message as an unambiguous rejection, so the CLI runs
  =cleanup_created_resources= (worktree + branch + artifact paths) and
  =restore_task_lifecycle_stages=. Pinned by a test using the actual
  preflight message shape, because routing it as ambiguous would leave behind
  exactly the litter the 2026-07-25 dispatch left.
- The probe receives placement (worktree, task, run kind, project) and an empty
  =RuntimeIdentity=. A probe must not depend on a run id it exists to avoid
  creating.
- Driver-level tests: the default is Unsupported and does not reject; only Fatal
  rejects; a harness answering "Please run /login" is classified Fatal rather
  than surfaced into a worker that would wait on a prompt nobody will answer.

NOT LANDED, and this matters for reading the task's status honestly
1. No adapter implements =preflight= yet, so nothing changes behaviourally
   today. The gate is structural.
2. The claude probe has an unresolved design question worth deciding before
   writing it. Auth failure is observable in ~39 ms because it happens before
   any model call, but a probe that exercises "the same execution context the
   worker will use" and succeeds will have made a real (small) model call on
   every dispatch. Cheaper probes exist and are all weaker: =claude auth status=
   is exactly the check measured to give a false pass (loggedIn: true while the
   dispatch argv answered "Not logged in"). Pick deliberately.
3. The atomic-abort half is untouched: a fatal driver event *after* startup has
   been accepted still leaves an open dispatch record, and the race test the
   task asks for (fatal emitted before dispatch bookkeeping completes) does not
   exist.
4. Consequently the acceptance clause "fake-provider tests prove auth failure
   creates no lease, session, or open dispatch and CLI rollback removes attempt
   resources" is only half covered: the reject-before-commit ordering is proven
   by construction and by the routing test, but there is no end-to-end test
   driving a fake provider through the daemon and asserting the absence of a
   lease and session.

Note on the original incident: its specific cause was fixed separately in
61b9b7f — orgasmic's own =--bare= argv, not an operator credential problem.
This task remains worth finishing for the class, not that instance.

PROBES LANDED (2026-07-25, manager-implementer). All three installed harnesses
now rule on their own credentials before a dispatch commits anything. The
atomic-abort half is still open; see NOT LANDED below.

The design question this task flagged is decided, and the answer is the opposite
of what the original evidence implied. Both halves were measured, not reasoned:

1. What a truthful probe costs. Making the harness itself rule on a credential
   means submitting a turn. Measured on claude 2.1.220, a ONE-CHARACTER prompt
   cost $0.0994 and 24.5k tokens, because the request writes the system prompt,
   tools and skills to cache before any answer returns. Cancelling early does
   not refund it: the failure verdict lands at 0.390 s and the outbound request
   at 0.326 s, so no deadline separates them. A check that silently bills a
   tenth of a dollar per dispatch is not a check worth having. Rejected.

2. Why `claude auth status` was actually wrong. The task recorded it as an
   ambient self-report that lied. It is not. Measured: `auth status` reports
   ONLY the claude.ai/keychain login and is blind to ANTHROPIC_API_KEY, while
   the failing dispatch carried `--bare`, whose contract is that OAuth and the
   keychain are never read. The two were answering about DIFFERENT CREDENTIALS,
   so they could disagree without either being wrong.

So the rule on `WorkerDriver::preflight` is rewritten. It was "exercise the
worker's exact argv"; it is now "resolve the same credential mode the worker
will resolve, then check the credential THAT MODE consumes". Once the mode is
resolved first, `auth status` is exactly the right question for native login
and exactly the wrong one for `--bare`. Cost: $0 and 0.28 s.

The guarantee behind it is structural, not documentary: `resolve_credentials`
was extracted out of `compose_request`, so the probe and the launch cannot
resolve different modes. A probe that reasoned its way to a mode independently
could confidently interrogate a credential the worker was never going to use,
which is precisely the failure above.

WHAT EACH ADAPTER NOW DOES
- claude: native login -> `claude auth status`, reads the `loggedIn` boolean.
  `--bare` -> the key it will present; an empty one is fatal, a present one is
  reported as unchecked (only the API can accept a key, and asking is the
  billed turn above).
- cursor-agent: a configured `api_key_env` is judged on the key; otherwise
  `cursor-agent status`.
- codex: `codex login status`.
- everything else: unchanged default `Unsupported`.

All five mode drivers delegate through one `preflight_via_adapter`, since
readiness is the harness's property and not the transport's.

VERIFIED LIVE, BOTH DIRECTIONS
- Healthy machine: Ready from claude, cursor-agent and codex.
- Credential-less machine (HOME/CODEX_HOME pointed at an empty directory, so no
  operator was logged out): Fatal from all three, each naming its remedy.

TWO BUGS THE LIVE TEST CAUGHT THAT UNIT TESTS COULD NOT
Both are recorded in gotchas.org, because both are the kind of thing a
reasonable person assumes.
1. codex writes its status answer to STDERR and leaves stdout empty. Reading
   stdout classified a logged-in codex as "no probe".
2. `claude auth status` EXITS 1 when logged out. Gating the verdict on a zero
   exit turned the one answer this mechanism exists to catch into
   "inconclusive" - the same defect this task is about, reproduced inside its
   own fix. Exit status is now ignored entirely; what decides whether a verdict
   was reached is whether the harness said something recognisable.
This is why `installed_harnesses_answer_their_own_readiness_probe` asserts
against real binaries: a unit test supplies the answer it then classifies, so it
can never find either.

DELIBERATE ASYMMETRIES, so a later reader does not "fix" them
- cursor and codex answer in prose, so only an exactly-observed logged-out
  phrase may reject. Anything unrecognised is `Unsupported`. A harness that
  rewords its message costs us the safeguard, never a working dispatch - the
  only direction worth being wrong in.
- An empty endpoint does NOT skip the probe. acp-stdio upgrades that config into
  a real subprocess whenever the binary is present, so the credential is just as
  real; skipping would have exempted the most common dispatch shape there is.
- The probe does not call `claude_available()`. That is a blocking
  `claude --version` on the async path - the TASK-KKGKM hazard - and is
  unnecessary, because a missing binary makes the status command unspawnable,
  which already reads as inconclusive.
- Fatal reasons are constants, never echoes of harness output. `auth status`
  answers with the operator's email, org and plan, and these reasons reach tx
  records and task evidence.

STILL NOT LANDED
- The atomic-abort half is untouched. A fatal driver event AFTER startup has
  been accepted still leaves an open dispatch record, and the race test this
  task asks for (fatal emitted before dispatch bookkeeping completes) does not
  exist.
- No end-to-end test drives a fake provider through the daemon and asserts the
  absence of a lease and session. The reject-before-commit ordering is proven by
  construction and by the routing test; the absence assertion is not written.
- hermes has no probe. Its auth surface was not measured, and inventing one
  would mean guessing a logged-out string - the one guess that can block a
  dispatch rather than merely fail to save one.
Reading order: the earlier NOT LANDED list is superseded by the block above.
Its items 1 (no adapter implements preflight) and 2 (the claude probe's design
question is open) are both now false; item 3 (atomic abort) and item 4 (the
end-to-end absence test) remain open and are restated under STILL NOT LANDED.

ATOMIC ABORT, PART 1 (2026-07-25, manager-implementer). The seventy-minute
orphan is fixed. The dispatch record is deliberately NOT auto-closed, and the
reason is below - it contradicts this task's own acceptance wording.

The incident's session JSONL was still on disk
(.orgasmic/tmp/sessions/dispatch-TASK-KWSTJ-reviewer-20260725T073841.jsonl) and
it settles what actually happened. Read in order:

  07:38:44.754  lifecycle acquire
  07:38:45.166  driver_error  fatal: true   "claude authentication_failed"
  07:38:45.175  driver_event  run_fail      "claude_result_error"
  ...           nothing at all, for seventy minutes
  08:49:23.536  driver_error  "claude exited with status exit status: 143"
  08:49:23.542  lifecycle release  outcome failed, finalized_by_worker false

The 143 is SIGTERM - the operator killing it. So the run did not end when it
failed; it ended when a human intervened, and everything in between was the
system waiting.

ROOT CAUSE, and it is one line. `terminal_event_releases_transport`
(supervisor.rs) allow-listed `tmux | tmux-tui | rmux`. On every other transport
a terminal event set `rec.terminal_outcome` and released nothing, leaving the
release to stream end. That is a bet that the harness will exit once it has
nothing left to do - sound on the success path, where exiting is part of
completing normally, and unsound on the failure path, because
`claude -p --input-format stream-json` holds stdin open waiting for a turn that
is never coming. The stream never ended, so the lease stayed held and the pid
stayed alive, which `dispatch-status` reported as `[pid-alive]`: a fatal startup
that leaves a live process is indistinguishable from work in progress.

FIX. A terminal event whose outcome is Failed now releases on every transport
(`terminal_failure_releases_any_transport`). Releasing reaps the process group -
`reap_process_group` sends TERM then KILL - so the failure now costs 0.4 s
instead of running until someone notices. Artifact draining is untouched:
`artifactor_lifecycle_in_flight` still defers through `pending_terminal_drain`.

The test is `a_declared_failure_releases_without_waiting_for_the_harness_to_exit`
and its shape is the point. Every pre-existing fatal-path test drops the driver
sender as soon as it has emitted, so stream end rescues it and none of them can
see this defect. The new stub keeps the sender in its *control* and drops it
only on release, which is what a real transport does - the reap is what closes
the channel. Its stream therefore never ends on its own, so the two-second
bound can only be met by the fix. Verified by mutation: with the new clause
disabled the test times out; restored, it passes in 0.14 s.

WHY THE DISPATCH RECORD IS STILL NOT AUTO-CLOSED
This task's acceptance says a fatal startup must "close any open dispatch". That
is the wrong instruction for the daemon to follow, and it took reading the CLI
to see why.

A dispatch is closed by `implementer.done` / `reviewer.done` /
`architector.done` / `manager.dispatch_aborted` (`scan_open_dispatches`);
`manager.dispatch_orphaned`, which the watcher does write, is not in that set.
So the obvious fix is to have the daemon write `dispatch_aborted` instead. But
closing is not a bookkeeping act: `cmd_dispatch_close` also runs
`cleanup_dispatch` - it removes the worktree, deletes the branch, and records
`CLEANUP_STATUS` on the closing tx so `close_tx_ran_cleanup` can tell that the
resources are gone. The daemon cannot do that half. It is a git operation on the
operator's repository.

A daemon-written close would therefore mark the dispatch closed with no
`CLEANUP_STATUS`, and `dispatch-status` would stop listing it - while the 15 MB
worktree and the branch stayed on disk with nothing left pointing at them. That
converts a visible orphan into an invisible one, which is strictly worse than
the state this task set out to fix.

So the open-until-acknowledged record is correct, and the thing that was
genuinely broken about it is already fixed elsewhere: `dispatch-close --status
aborted` used to die at 10.03 s against the pre-TASK-KWSTJ `/api/runs`, so the
documented remedy did not work. It does now.

If auto-close is still wanted, it is a different and larger piece of work -
the daemon would have to own worktree and branch removal - and it should be
argued on its own merits rather than inherited from this task's wording.

STILL NOT LANDED
- The race test this task asks for (a fatal event emitted before dispatch
  bookkeeping completes) does not exist. Note the preflight gate makes the
  common case unreachable rather than handled, which is not the same thing.
- No end-to-end test drives a fake provider through the daemon and asserts the
  absence of a lease and session.
- hermes still has no readiness probe.
COMPLETE (2026-07-25). The three items left open above are done.

RACE TEST. `a_failure_declared_before_the_run_is_recorded_still_ends_the_run`
(supervisor.rs). A driver that declares its fatal synchronously inside
`acquire`, before returning the session, so the queue is guaranteed non-empty
when the supervisor inserts the run record. What protects this is an ordering,
not a lock: the record goes in before the drain task spawns, so no event can be
applied through `runs.get_mut` while the record is missing. Verified by mutation
- deferring the insert past the drain's first events produces no release at all.
Worth noting how it fails: not by timing out, but instantly, because a run
absent from the map reads as already released. That is the bug's own shape, an
orphan indistinguishable from a finished run.

END-TO-END ABSENCE. `an_unusable_credential_leaves_no_lease_no_session_and_no_run`
(tests/dispatch_preflight.rs), a real dispatch through the daemon with an empty
`ANTHROPIC_API_KEY`: 400, no session file, no lease, no run.created, and a
second dispatch still refused with 400 rather than 409. Its own test binary
because it mutates process-global env. Non-vacuity checked by running it with a
non-empty key - the dispatch then succeeds and acquires, so the absence really
is the preflight's doing.

HERMES: NO PROBE, and now for a measured reason rather than an unmeasured one.
`hermes auth status <provider>` is the cleanest status command of the four
(0.25 s, exit 0, both directions). The problem is that orgasmic has no provider
to ask about. Measured on hermes 0.16.0 by driving `hermes acp` directly: a
`session/new` carrying provider "nonexistent-provider" succeeded on
`ollama-cloud:glm-5.2`, and so did the same call carrying "anthropic". Hermes
ignores the provider orgasmic sends. So a probe on `cfg.provider` would
interrogate a credential the worker never presents - the `claude auth status`
mistake again, and here in the direction that blocks working dispatches. The
reasoning is recorded on `HermesAdapter` itself. The underlying defect is
TASK-5M45A.

Tests: drivers 261/0. supervisor module 97 passed, with
`stall_detector_revalidates_after_driver_event_race` failing intermittently -
2 of 3 runs on HEAD without these changes and 2 of 3 with them, so pre-existing
(TASK-STWVB). dispatch_endpoint has 3 failures, identical on HEAD, from stale
worker fixtures ("legacy user/workers directory ignored").
** Evidence
Live reproduction 2026-07-25, dispatching a reviewer for TASK-KWSTJ. This is the abstract problem statement happening end to end, with timings.

Dispatch `tx-20260725-orgasmic-2741`, run `run-20260725T073841-ef292c73d0e344edb7d475148a3f0c56`, worker `reviewer-claude-acp-stdio`, mode `acp-stdio`, harness `claude`. From the run's own session JSONL:

- 07:38:44 acquire, then run_meta — worktree, lease, and dispatch record all committed
- 07:38:45.158 driver_event text_chunk, stream system: "claude status: requesting"
- 07:38:45.162 driver_event text_chunk, stream assistant: "Not logged in - Please run /login"
- 07:38:45.166 driver_event driver_error, fatal: true, "claude authentication_failed"
- 07:38:45.175 driver_event run_fail, error_code claude_result_error

The run was fatally unusable 1.2 seconds after acquire, for a reason knowable before any resource was committed. CORRECTION (2026-07-25, after root-cause analysis): the harness WAS authenticated. `claude auth status` reports `loggedIn: true, authMethod: claude.ai, subscriptionType: max`. Orgasmic's own argv caused the failure — the claude adapter hardcodes `--bare`, whose contract is that OAuth and keychain are never read (see TASK-Z8WEJ). Do not read this evidence as an operator credential problem. What the failure left behind:

- an open dispatch record, still listed by `manager dispatch-status` 70 minutes later
- a 15 MB worktree at `.orgasmic/tmp/dispatch/task-kwstj-review/`
- a branch `task-kwstj-review`
- zero-byte `last.txt` and `stdout.log`
- the harness process (pid 52741, `claude --bare -p --input-format stream-json ...`) still alive 70 minutes after emitting a fatal error, reported by dispatch-status as `[pid-alive] [run-gone]` — supervisor had dropped the run while the process persisted

The manager treated `[pid-alive]` as "review in progress" and reported it as running to the operator. A fatal startup error that leaves a live process is worse than a crash: it is indistinguishable from work.

Second finding, and the reason this is not merely cosmetic: cleanup was blocked by the very defect TASK-KWSTJ fixes. `orgasmic manager dispatch-close --status aborted` on the installed 0.0.14 CLI fails after exactly 10.03 s with "daemon request failed ... /api/runs — is the daemon reachable?". The pre-fix CLI takes the legacy liveness fence through `GET /api/runs`, which on this board needs 35.2 s to first byte, against a 10 s client deadline. So a dispatch that failed at startup cannot be closed by the documented verb until the bounded-inventory fix ships. The two defects compound: startup leaves the mess, and enumeration prevents cleaning it.

Preflight design note this supports, sharpened by the root cause: the check must exercise the exact argv the worker will run, not merely the harness's ambient credential state. Measured here: `claude auth status` returns `loggedIn: true` while the very same binary invoked with the dispatch's own flags returns "Not logged in" in 39 ms. A preflight that asked the harness whether it was logged in would have passed and still produced this failure. The 39 ms cost of the real probe is also the argument for doing it: it is cheaper than the worktree it prevents.

Also relevant to the preflight's non-interactive requirement: the harness's own answer here was "Please run /login", i.e. an instruction to start an interactive flow. A preflight must classify that as a definitive fatal reason and reject the dispatch, never surface it into a worker that then waits on a prompt nobody will answer.
STATE CORRECTED [2026-07-26]: in_review -> in_progress. This task was never
awaiting a reviewer. Its own body says PARTIAL IMPLEMENTATION and "No adapter
implements preflight yet, so nothing changes behaviourally today. The gate is
structural." A P0 sitting in in_review reads as finished work pending a second
opinion, which is the opposite of true here.

Remaining, per the body: the concrete adapter probes, the atomic-abort half, and
the unresolved design question about probe cost. Two things landed since this
task was last touched that bear on that question and should be read before the
probes are written:

- The gotcha "Harness status commands disagree about stream and exit code" —
  claude writes JSON to stdout and exits 1 when logged out; codex answers on
  stderr and exits 0 either way; cursor-agent writes prose and exits 0 either
  way. Neither stdout-only nor exit-code gating classifies all three correctly.
- The gotcha "Asking a harness to prove it can work costs real money" — a
  one-character claude turn cost $0.0994 and 24.5k tokens, and cancelling early
  does not refund it. That is the probe-cost question this task recorded, already
  measured.

This task also still carries the sharpest constraint from its own Evidence: the
probe must exercise the exact argv the worker will run. `claude auth status`
returned loggedIn:true while the same binary with the dispatch argv returned
"Not logged in" in 39 ms.

* IN_PROGRESS TASK-STWVB Four daemon tests fail on every full-suite run and pass alone :testing:daemon:flake:
:PROPERTIES:
:ID: TASK-STWVB
:PRIORITY: P2
:END:

Every full `cargo test -p orgasmic-daemon --lib` run fails the same four tests,
and each passes alone:

- api::tests::post_artifact_regenerate_hot_path_reuses_live_run_id
- api::tests::recovery_inventory_waits_for_atomic_claim_commit
- api::tests::recovery_reattaches_tmux_session_when_handle_exists
- supervisor::tests::poll_direct_child_pid_prefers_worker_server_over_generic_sibling

Measured 2026-07-25: 496 passed / 4 failed, and the identical four with an
unrelated change stashed out. They only fail under full-suite parallelism, not
in a filtered subset, so the trigger is cross-test interference rather than any
one test's logic.

Why this is worth fixing rather than tolerating: a suite that always fails four
tests cannot answer "did my change break something?". Establishing that these
four were pre-existing cost two full 13-minute runs during TASK-KKGKM — a
stash-and-compare that every future change to this crate has to repeat. A red
suite is also indistinguishable from a red suite with one new real failure in
it.

Leads, not conclusions:
- These tests share process-global state: `live_session_guard()` and static
  hooks such as `RECOVERY_POST_ORIGIN_AUTHORITY_HOOK`, which serialize some
  tests against each other but not against everything that observes the same
  state.
- The same class of bug was found and fixed in the CLI crate on the same day:
  `path_env`'s tests set process-global PATH to a bare tempdir, so any
  concurrently running test spawning a PATH-resolved tool failed with a bare
  `NotFound` (~3 runs in 4). Worth checking whether these four have an analogous
  shared-environment dependency before assuming a timing race.
- Two of the four are tmux/mux-dependent, so a real external session leaking
  between tests is also plausible.

Acceptance:
- `cargo test -p orgasmic-daemon --lib` is green, repeatedly, at full
  parallelism
- the cause is named per test, not worked around by serializing the whole suite
- if any test genuinely requires exclusive global state, it says so and enforces
  it, rather than depending on scheduling luck
RE-MEASURED [2026-07-26], and the original inventory does not hold.

The "four tests fail on every full run" claim was made while the build
environment was badly degraded: the disk was 89% full, target/ was 57 GB with
164,619 retained .o files, and a full `cargo test -p orgasmic-daemon --lib` run
took 13 minutes. After clearing that (dead worktree targets, 31 GB of
incremental, a stale Time Machine snapshot, and switching the dev profile to
line-tables-only) the same suite runs in ~45 seconds and target/debug is 3.4 GB.

Five consecutive full runs at default parallelism on the repaired environment:

| test                                                          | failed |
|---------------------------------------------------------------|--------|
| api::post_artifact_regenerate_hot_path_reuses_live_run_id       | 3/5    |
| recovery_claim::reconcile_pending_rejects_rename_swap_after_open| 2/5    |
| api::post_artifact_regenerate_cold_changed_address_replaces_... | 1/5    |
| api::recovery_inventory_waits_for_atomic_claim_commit           | 0/5    |
| api::recovery_reattaches_tmux_session_when_handle_exists        | 0/5    |
| supervisor::poll_direct_child_pid_prefers_worker_server_over_...| 0/5    |

So three of the four originally named tests did not fail once, and two tests
that were never on the list do fail. The suite is green outright in 1 run of 5.

What this means: a 13-minute run on a thrashing disk stretches every await long
enough to tip deadline-bounded waits, which is why the failure set looked stable
and looked like those four. The environment was a confound. The remaining three
are real flakes but a smaller and different problem than recorded.

Also fixed and split out of this task:
- stall_detector_revalidates_after_driver_event_race — root-caused to
  Supervisor::new's own 50ms timeout monitor releasing the aged run out from
  under the test, not to load. Fixed in a629a51 with Supervisor::unmonitored.
  wait_for_event_count no longer reports a released run as "event_count 0".
- The 3 dispatch_endpoint failures were a separate, deterministic defect
  (TASK-5T575, fixed in 05d029b).

Remaining scope for this task is the three flakes in the table, not the original
four. The regenerate pair are the priority — both touch the same hot/cold
artifact-regenerate launch-record path, so they are likely one cause.
GATE IS BLOCKED BY TASK-8W30B [2026-07-26], and this is a measurement problem
rather than a remaining-work problem.

The rmux session leak adds 8 leaked sessions per full-suite run (manager-
confirmed: 23 -> 31 across one run, from a known-empty daemon). Saturation
degrades the rmux driver to inert, at which point
`post_artifact_regenerate_hot_path_reuses_live_run_id` fails on all retries with
`409 capability_unsupported: send_input`.

So '5 consecutive green full-suite runs' cannot be satisfied on a machine where
each run makes the next one worse. The gate should be re-run only after
TASK-8W30B lands, and the two fixed tests should be judged on the controlled
before/after they already have rather than on an absolute streak.

Post-merge state, 3 runs on a quiet machine (499-500 passed):
- reconcile_pending_rejects_rename_swap_after_open: 0/3 — FIXED, was 4/6
- post_artifact_regenerate_hot_path_reuses_live_run_id: 3/3 — TASK-8W30B
- poll_direct_child_pid_prefers_worker_server_over_generic_sibling: 3/3 in-suite
  but PASSES ALONE on both the pre-merge and post-merge commits, so it is
  in-suite interference and specifically NOT a regression from the merge
  (checked deliberately, since the implementer touched its sibling)
- recovery_inventory_waits_for_atomic_claim_commit: 1/3

Remaining scope after the merge: the interference behind poll_direct_child_pid
and recovery_inventory, both of which pass in isolation.
STATE CORRECTED [2026-07-26]: in_review -> in_progress, and the root-cause
attribution above is DISCONFIRMED.

The block is lifted: TASK-8W30B merged (d60c4ed / d3fae4d) and release growth
went from +8 per suite run to 0, manager-measured independently. So the
"measurement problem" framing above no longer applies — the environment no
longer degrades run over run.

But the line "post_artifact_regenerate_hot_path_reuses_live_run_id: 3/3 —
TASK-8W30B" attributed that failure to the leak, and that is now wrong. On the
merged tree, with release growth at zero, the test still fails:

    assertion `left == right` failed: two consecutive rounds must reuse one run_id
      left:  run-20260726T105347-034ff6ca59844f43a18a9a0ae8a3ba2c
      right: run-20260726T105337-c9b87801bc334547a747b5b8ebf2277a

That is a run-id reuse failure, not a `409 capability_unsupported: send_input`
saturation failure. The leak may have aggravated its environment; it was not the
cause. The reviewer on TASK-8W30B reached the same conclusion independently.

Remaining scope is therefore three tests, not two:
- post_artifact_regenerate_hot_path_reuses_live_run_id — needs its own root
  cause, in the hot/cold artifact-regenerate launch-record path
- poll_direct_child_pid_prefers_worker_server_over_generic_sibling — in-suite
  interference, passes alone
- recovery_inventory_waits_for_atomic_claim_commit — same shape, passes alone

Do not re-run a "5 consecutive green" gate as the closing condition until the
first of those has a named cause. An absolute streak was the wrong instrument
while the environment degraded, and it is still the wrong instrument for a
deterministic assertion failure.
ROOT CAUSE MEASURED [2026-07-26], manager-direct. The "cross-test shared global
state" hypothesis at the top of this task is WRONG for poll_direct_child_pid.
The cause is process-creation latency, and the test is simply asserting a
deadline the machine cannot meet while the suite runs.

WHAT WAS MEASURED

Both remaining tests pass alone and fast: poll_direct_child_pid in 0.42 s,
post_artifact_regenerate_hot_path in 0.80 s. In-suite they now fail 3/3, not
intermittently — that is an improvement, because deterministic is debuggable.

Running each module alone at full parallelism is green:
- supervisor::tests  99 passed, 12.0 s
- api::tests        199 passed, 37.5 s
So the interference is cross-module, not within either.

Live capture of the fake cursor-agent during a full run (three separate runs,
same result). The test writes a bash script that backgrounds two sleeps, touches
`children-ready`, then sleeps; it waits up to 30 s for that file:

- the process IS alive the whole time — it is not killed, not starved of a slot
- it has ZERO children, so it never reached even its first `&`
- state is S (blocked), never R (runnable), so it is not losing a CPU race
- CPU time stays at 0:00.00 across 28+ seconds of sampling — it never executed
  a single instruction

A process that is alive, blocked, and has consumed no CPU is wedged BEFORE
execution, not running slowly.

THE DISCRIMINATOR

An unrelated trivial bash script (`#!/bin/bash` + one `touch`), spawned from
outside the test binary while the suite ran, timed at:

    6205 ms, 2993 ms, 63771 ms, 64 ms, 354 ms

Sixty-three seconds to spawn a two-line shell script, then 64 ms a few seconds
later. Process creation on this machine intermittently stalls for seconds to a
minute while this suite runs. Nothing about the test is special; it is just the
one test that asserts a 30 s deadline on a freshly spawned external process.

Caveat on that measurement, stated so nobody over-reads it: the timestamps were
taken by spawning python3, so each figure includes one additional process
spawn. That does not change the conclusion — spawning anything is what is slow —
but the per-call attribution is not exact.

Peak process count during a run was only ~720 against a 5333 maxprocperuid, and
fd limit is 1048576, so this is not simple resource exhaustion. The likely
mechanism is serialization in macOS exec (code-signing / syspolicy validation)
under many concurrent spawns, which is worth confirming before designing around
it.

CONSEQUENCE FOR THE FIX

Do not fix this by raising the 30 s deadline. Sixty seconds was observed, so any
constant is a guess. Either remove the dependence on a freshly spawned external
process reaching a checkpoint, or serialize the process-spawning tests against
each other so the suite stops competing with itself for exec.

The same mechanism is a plausible cause for post_artifact_regenerate_hot_path:
its two run_ids are 10 s apart, consistent with round one exceeding a deadline
and its run being released before round two. Not yet confirmed — do not record
it as established.
MECHANISM CONFIRMED [2026-07-26]. It is macOS first-exec evaluation, and it is
measurable in three lines on a completely idle machine:

    fresh script #1 first exec: 0.18 s    second exec: 0.00 s
    fresh script #2 first exec: 0.15 s    second exec: 0.00 s
    fresh script #3 first exec: 0.16 s    second exec: 0.00 s

macOS charges roughly 150-180 ms of syspolicy/Gatekeeper evaluation the first
time a newly created executable is exec-ed, and nothing thereafter. Idle spawns
of an already-evaluated script are 0.00 s, which is why every one of these tests
passes alone.

That per-file cost is paid through a single system daemon, so concurrent first
execs queue. This suite writes many fresh executable fixtures into tempdirs and
execs them in parallel, which is how a 150 ms cost becomes the 3 s / 6 s / 63 s
spawns measured earlier. The failing test is not special; it is the one that
asserts a 30 s deadline against that queue.

So the chain is: fresh fixture -> per-file Gatekeeper evaluation -> serialized
daemon -> queue under suite parallelism -> a spawned helper that never reaches
its first instruction inside the deadline.

FIX DIRECTION

Stop paying the per-file cost per test. The fixture script should be created and
exec-ed ONCE per test binary and then reused, with the per-test variation moved
out of the file contents and into argv, so the second and subsequent uses hit
the warm path. Pre-warming a shared fixture is preferable to serializing the
tests, because it removes the cost rather than queueing it, and it also speeds up
every other spawn-based test in the crate.

Raising the deadline is not a fix. 63 s was observed; any constant is a guess.

NOTE FOR CI (TASK-S2KM0): this mechanism is macOS-specific. A Linux runner has
no Gatekeeper and will very likely not reproduce it, so this test could pass in
CI while failing on every developer Mac. That is an argument for fixing the
fixture rather than tolerating the failure locally, and for not concluding "green
in CI" means "green".

ROUND 2 MERGED, GATE NOT MET [2026-07-26]. The fix is real and kept; the green
claim is not reproducible.

Merged 67e427c (shared warm fixture in crates/orgasmic-daemon/src/test_fixtures.rs,
converted spawn fixtures, gotchas entry). Worker: implementer-codex-acp-stdio
(acp-stdio / codex / gpt-5.6-sol / xhigh), run
run-20260726T125001-9e991bce9f75485a84e2f4be1d127800. It reported 504 passed /
0 failed on three consecutive runs at 24.9 / 27.5 / 26.1 s.

Manager measurement on the merged tree, four runs:

    502 passed / 2 failed   106.5 s
    501 passed / 3 failed   112.1 s   (+ recovery_inventory, known flake)
    502 passed / 2 failed   108.2 s
    502 passed / 2 failed   106.5 s

Both target tests still fail:
- post_artifact_regenerate_hot_path_reuses_live_run_id — now 409 "harness busy"
  rather than the earlier run_id mismatch
- poll_direct_child_pid_prefers_worker_server_over_generic_sibling — still
  "fake cursor-agent did not start children" at its 30 s deadline

WHY THE WORKER SAW GREEN — see TASK-RRT4T. Removing rmux from PATH, changing
nothing else, turns the manager's own run into 504 passed / 0 failed in 22.3 s,
reproducing the worker's numbers exactly. Eight rmux-dependent tests silently
return when the rmux binary is not found. The worker's own 7-before / 7-after
session count was the tell: the test never ran.

WHAT THE ROUND-2 WORK DID ESTABLISH, verified independently:
- Hard-linking a warmed executable does dodge first-exec evaluation. Measured:
  fresh direct exec 196 ms, second exec 4 ms, hard link to a new path 4 ms, a
  byte-identical COPY 162 ms. So link_shared_test_executable is sound and the
  inode, not the path, is the cache key.
- The pressure is inside the rmux test path, not in the converted fixtures.
  syspolicyd CPU during one suite run: +281 s with rmux present, +32 s with
  rmux absent, against ~106 s and ~22 s of wall time. The rmux binary itself is
  cheap (9.2 ms/exec, 0.0 ms of syspolicyd), so the cost is something the rmux
  session path execs, and it has not been identified.
- A strictly simpler primitive exists and was not used: invoking a fresh script
  through a signed interpreter skips evaluation entirely. Measured on the same
  fresh file — `./script` 270 ms, `/bin/sh script` 5 ms, `/bin/bash script`
  3 ms. Round 3 should prefer this over warm-and-hard-link wherever the callee
  is a script, because it needs no shared state, no OnceLock, and no argv
  multiplexing.

REMAINING SCOPE
1. Land TASK-RRT4T first. Until a skipped test is visible in the default
   summary, no gate on this crate can be trusted, including this task's.
2. Identify what the rmux test path execs that costs syspolicyd ~250 s per
   suite run. That is the actual remaining cause for poll_direct_child_pid,
   which passes when that pressure is absent.
3. post_artifact_regenerate_hot_path now fails as 409 "harness busy", a
   different symptom than before; treat its cause as unestablished.

A note on the fixture module for round 3: shared_test_executable() asserts the
on-disk fixture equals FIXTURE_SCRIPT and panics otherwise, while its doc
comment claims a changed script gets a new path because the path is derived from
the hashed test binary name. That reasoning does not hold — cargo's test-binary
hash is not content-derived — so editing FIXTURE_SCRIPT can leave a stale file
at the same path and fail every spawn test until it is deleted by hand. Make it
overwrite, or key the filename on a content hash.

Also weakened, worth restoring: the old pinned-exec wrapper asserted
[ "$1" = __exec-pinned ] || exit 97, so a production regression that stopped
passing that argv failed loudly. The shared fixture falls through to exit 0
instead.

CORRECTION TO THE ABOVE [2026-07-26], same session: the sentence "the pressure is
inside the rmux test path" states a real correlation but the mechanism it implies
is DISPROVEN. rmux does not trip Gatekeeper.

Measured directly, rmux 0.9.0 (crates.io, pinned =0.9.0) against tmux 3.x on the
same machine:

| operation                                | tmux    | rmux    | syspolicyd |
|------------------------------------------|---------|---------|------------|
| 15x create session + kill session         | 13.1 ms | 12.1 ms | +0.01 / +0.00 s |
| 20x pane runs an already-warm script      |  6.9 ms | 12.8 ms | +0.00 / +0.01 s |
| leftover sessions after all kills         | none    | none    | — |

So rmux is leak-free and Gatekeeper-neutral at this level, and roughly 2x slower
than tmux per pane spawn — which at 6 ms of absolute difference explains nothing.

What survives: with rmux on PATH the suite is 106 s and syspolicyd burns +281 s;
with rmux hidden it is 22 s and +32 s, one variable changed. The correlation is
solid. The attribution is not. Whatever spends that CPU has not been identified,
and it is NOT rmux exec-ing things.

A better lead, found while checking: the hot-path test polls for the harness
prompt `for _ in 0..40` at 250 ms — a 10 s ceiling, which is exactly the 10 s gap
between the two run_ids in the original failure. The eight rmux tests also all
take `live_session_guard()`, so they are serialized against each other while the
other ~496 tests run in parallel around them. A serialized 10 s deadline under
full-suite load is a plausible mechanism that needs no Gatekeeper story at all.

THE EXPERIMENT THAT WOULD SETTLE IT
These tests exist only in the rmux mode. Parameterize post_artifact_regenerate_*
over rmux AND tmux. Red under rmux and green under tmux indicts rmux; red under
both is ours. Right now the question cannot be answered, which is the actual
problem.

THIRD MEMBER OF THE FAMILY [2026-07-26, found integrating TASK-D0GA3]. The
full-suite run of `cargo test -p orgasmic-daemon --lib` at merge 0daa77c failed
THREE tests, not two:

- post_artifact_regenerate_hot_path_reuses_live_run_id
  (api.rs:25772 — "second regenerate should route via send_input (hot path)",
  got 409 "harness busy")
- poll_direct_child_pid_prefers_worker_server_over_generic_sibling
  (supervisor.rs:8788 — "fake cursor-agent did not start children")
- recovery_reattaches_tmux_session_when_handle_exists  <-- NEW
  (api.rs:21866 — left "interrupted", right "reattached")

All three pass together in isolation: `--exact` on the three names gives
3 passed / 0 failed in 0.54s, 505 filtered out. Whole-suite time was 182.9s with
rmux present on PATH.

The new one is informative. It is not a spawn-fixture test — it is tmux session
REATTACH, and it lands on "interrupted" rather than "reattached", i.e. something
else in the suite killed or claimed the tmux session it expected to find. That
points at cross-test contention over shared external mux state (session names,
sockets), not at the executable-fixture path that round 2 addressed. Round 3
should start from "which tests share a tmux/rmux namespace" rather than from the
fixture.

Note also that the TASK-D0GA3 worker reported "508 passed, 0 failed" for this
same command with rmux present. That is a fourth over-report, and unlike the
first three it is NOT explained by silent tooling skips — TASK-RRT4T's sentinel
was already in place and would have fired. Either the worker's run genuinely
came up green (making this load- or order-dependent, which fits) or the number
was not measured. Do not treat any single green run of this suite as evidence.

=== Measured again 2026-07-28, and the set is larger than four

Full `cargo test -p orgasmic-daemon --lib` in the manager environment at HEAD
`15997c6`: **528 passed, 5 failed, 451 s**.

| test | symptom |
|------|---------|
| `api::tests::production_resume_native_fork_uses_pinned_claude_not_path_shim` | `recover: 500 Internal Server Error` |
| `api::tests::two_recovery_chain_second_resume_uses_first_fork_session_id` | `first recover: 500 Internal Server Error` |
| `api::tests::recovery_reattaches_tmux_session_when_handle_exists` | `tmux test session should start` |
| `api::tests::equal_run_id_across_projects_requires_canonical_project` | `project-b recover should succeed independently: 500` |
| `supervisor::tests::poll_direct_child_pid_prefers_worker_server_over_generic_sibling` | `fake cursor-agent did not start children` |

The first three also failed, identically, inside the TASK-1T3FZ dispatched
worker, which additionally lost
`boot_reattach_respawns_dispatch_completion_watcher_without_artifacts`,
`failed_terminal_with_live_tmux_never_offers_reattach`, and the tmux sentinel.
The underlying daemon error in the worker was
`tmux new-session failed (exit 1): server exited unexpectedly`.

Three things this rules out (tx-20260728-orgasmic-3226):

- Not environment. Each of these passes in isolation, in both the manager
  environment and a replicated dispatched-worker environment.
- Not a worker-only blindspot. Three reproduce in the manager environment with
  no rmux shim and no `$TMUX`.
- Not a missing or broken tmux. Under the exact worker `PATH` + `TMUX`,
  `tmux new-session -d` exits 0; the tmux the worker resolves is an rmux shim
  pointing at a live server.

Four of the five are a **recovery** path — three `recover` calls returning 500
and one tmux session failing to start for a reattach test. That concentration is
the strongest structural clue on this task so far, and it is not a generic
"slow machine" story.

TASK-0RCRY carries the one concrete candidate mechanism: no tmux call site
passes `-L`/`-S`, so every probe, test session and production call shares a
single tmux server. If isolating that server does not fix these, the result
belongs back here.


=== MANAGER MEASUREMENT 2026-07-28: production_resume_native_fork has TWO failure modes

Recorded after TASK-5HBST merged, because the manager had been mislabelling this
test as purely 5HBST's.

`api::tests::production_resume_native_fork_uses_pinned_claude_not_path_shim`
fails in two distinct ways:

1. **Env race (5HBST's, now fixed).** A concurrent test clobbers process-global
   `HOME`, so `pin_trusted_claude_binary` (api.rs:9906) resolves the wrong
   `~/.local/bin/claude`. Presents as a wrong path or found/not_found mismatch.
2. **Recovery 500 (this task's, still open).** `resume_native_fork recover:
   500 Internal Server Error` at api.rs:25171. Nothing to do with the
   environment.

Measured on the merged 5HBST tree, full `cargo test -p orgasmic-daemon --lib`
at load ~10: it still failed, with **mode 2**. So 5HBST removed one mode and the
test remains on the known-flake list until this task lands.

That makes the recovery cluster on this axis larger than previously recorded.
In the same run: `production_resume_native_fork...` (500 on recover),
`recovery_inventory_waits_for_atomic_claim_commit`, and
`recovery_reattaches_tmux_session_when_handle_exists` — three of the four
failures were recovery-path, and the fourth
(`poll_direct_child_pid_prefers_worker_server_over_generic_sibling`) is the
fixture-process one, a different class.

The 'recover returns 500 under load' signature is now observed on three separate
tests. Whatever this task finds should explain all three, not one.
PRODUCTION-SIDE OBSERVATION [2026-07-29 00:1x]: during a 3-worker compile window, one manager dispatch-status poll reported ALL THREE live runs as [run-gone] simultaneously; the next poll (60s later) and rmux ls showed every session alive and attached, daemon on the same pid/boot throughout. A transient inventory read under load flips run-liveness for every run at once — same family as the 'recover: 500 under load' signature already on this task, observed on the live daemon rather than in a test.
PRODUCTION-SIDE OBSERVATION [2026-07-29 00:1x]: during a 3-worker compile window, one manager dispatch-status poll reported ALL THREE live runs as [run-gone] simultaneously; the next poll (60s later) and rmux ls showed every session alive and attached, daemon on the same pid/boot throughout. A transient inventory read under load flips run-liveness for every run at once. Two minutes later, a node body append to THIS task failed once with 'daemon request failed: error sending request ... is the daemon reachable?' and succeeded on retry — the HTTP surface itself goes briefly unreachable under load. Same family as the 'recover: 500 under load' signature already recorded here; both observed on the live daemon rather than in a test.
PRODUCTION-SIDE OBSERVATION [2026-07-29 00:1x]: with load average 164 (a worker deliberately inducing load per TASK-5FEN5's repro recipe), one dispatch-status poll flipped ALL THREE live runs to [run-gone] at once, and two manager CLI calls failed with 'daemon request failed ... is the daemon reachable?'. Direct diagnosis DURING the failures: daemon pid alive, port 4848 LISTEN, raw HTTP answered 401 in 0.4s. So the daemon was healthy throughout — the CLI's client-side request path times out under scheduler starvation and reports it as daemon unreachability, and the inventory read degrades the same way. Relevant to this task because the same mechanism plausibly explains the 'recover: 500 under load' family: not a daemon defect expressing under load, but load starving some request leg. The error text 'is the daemon reachable?' is also a misdiagnosis-generator: the truthful message would be 'request timed out after Ns (system load may be the cause)'.
PRODUCTION-SIDE OBSERVATION [2026-07-29 00:1x]: with load average 164 (a worker deliberately inducing load per TASK-5FEN5's repro recipe), one dispatch-status poll flipped ALL THREE live runs to [run-gone] at once, and repeated manager CLI calls failed with 'daemon request failed ... is the daemon reachable?'. Direct diagnosis DURING the failures: daemon pid alive, port 4848 LISTEN, raw HTTP answered 401 in 0.4s. So the daemon was healthy throughout — the CLI's client-side request path times out under scheduler starvation and reports it as daemon unreachability, and the run-inventory read degrades the same way. Relevant to this task: the same mechanism plausibly explains the 'recover: 500 under load' family — load starving a request leg rather than a daemon defect. Also: the error text 'is the daemon reachable?' is a misdiagnosis-generator; the truthful message is 'request timed out after Ns (system load may be the cause)'.

FIRST CAPTURED PANIC for the registry's quoted-origin entry [2026-07-29, from TASK-5FEN5's load runs]: `index::tests::git_resolves_quoted_included_origin_from_linked_worktree` — index.rs:2247, `assertion left == right failed, left: None, right: Some("ssh://git@example.com/org/quoted.git")`. The registry names this test as a known STWVB flake awaiting its first verbatim panic; this is it. Registering the row (with this task as owner) is now unblocked and is this task's call, per the DJ1WV rule that only the owning task writes its own entries.

CANDIDATE PANIC [2026-07-29, TASK-0RCRY full-workspace run]: `task_010_recovery_routes_classify_and_continue_runs` (crates/orgasmic-daemon/tests/integration.rs:2637) — `recover: 409 Conflict` under load, green in isolation. Distinct shape from the quoted-origin flake; first time this panic has been written down. Route or register under this task's diagnosis as appropriate.

ATTRIBUTION CONTRADICTED [2026-07-29, TASK-K4G1D differential]: the `left: "interrupted"` reattach flake is now named for the first time — `driver tmux/claude attach proof exceeded inventory deadline`, the Err(_) timeout branch of finish_driver_attach (api.rs:9858). Measured with real tmux 3.6a on a 0RCRY owned server, with the rmux arm GREEN in the same process under the same load. This contradicts this task's standing attribution (cross-test contention over shared external mux state) — that would be the third withdrawn attribution. The live question is why a one-process-spawn tmux attach probe cannot finish inside the inventory deadline under load, and whether that deadline is sized for process-spawn starvation. Also note: K4G1D found the seven tmux_on_path-gated tests execute rmux in worker environments (owned by its follow-up task), so any worker-vs-manager comparison recorded here that relied on "the worker ran these under tmux" needs re-reading.

CANDIDATE PANIC [2026-07-29, TASK-GEZHQ gate run 3 of 5]: `api::tests::recovery_inventory_waits_for_atomic_claim_commit` (crates/orgasmic-daemon/src/api.rs:25017) — `recover POST completes: Elapsed(())` under load, green in isolation. Another recover-route timeout shape for this task's diagnosis; first time written down.

RE-READING FLAGS [2026-07-29, TASK-JGHNC, merged 724dd6b] — four recorded comparisons need correction:
1. The 2026-07-28 five-failure section: the TASK-1T3FZ worker's two extra failures (boot_reattach_respawns…, failed_terminal_with_live_tmux…) plus its tmux sentinel were RMUX failures wearing tmux names, on the shared rmux default server; they say nothing about tmux. Post-JGHNC that comparison cannot be reproduced in a worker at all (strict-gated, NOT RUN).
2. recovery_reattaches_tmux_session_when_handle_exists: the manager symptom ('tmux test session should start') and the worker symptom ('server exited unexpectedly') were read as one signature; the worker half was an rmux-server symptom. Superseded by K4G1D's tmux-arm and JGHNC's rmux-arm measurements.
3. The D0GA3 worker's '508 passed, 0 failed' anomaly has a third explanation: in a worker, seven tests ran a different multiplexer than the manager's run of the same names — never a like-for-like comparison.
4. THE DIFFERENTIAL NOW READS BOTH WAYS: JGHNC's worker run caught recovery_reattaches_RMUX_session_when_handle_exists red with the IDENTICAL sentence K4G1D measured on the tmux arm ('driver rmux/claude attach proof exceeded inventory deadline', api.rs:24464). Red under BOTH multiplexers = the defect is ours (the inventory deadline / attach-proof path), NOT rmux. Any future worker-vs-manager comparison on this family needs the real-tmux PATH recipe (K4G1D report §2); the five new rmux arms are the worker's honest column.

* IN_PROGRESS TASK-J1XCB Three unowned load flakes from 5FEN5's runs: dead-pid release wait, port-0 instance lock, shutdown-budget replacement wait
:PROPERTIES:
:ID: TASK-J1XCB
:PRIORITY: P1
:END:

Successor to TASK-5FEN5 (which fixed its three owned flakes and closed).
These load flakes were seen red during 5FEN5's 2026-07-29 load runs, panics
captured VERBATIM per the DJ1WV rule, and are currently unowned — so the flake
registry rightly classifies any recurrence as REAL and a loaded full-suite run
exits nonzero. This task exists to own them.

1. `supervisor::tests::dead_pid_aborts_joins_hung_producer_then_receiver_releases`
   — 3 sightings (both pre- and post-fix binaries):
   `run <id> did not release within 12s` at supervisor.rs:8077 (8065 in older
   trees). First flagged by the TASK-XPYRR worker's gate, confirmed by 5FEN5.
2. `api::tests::deleted_or_corrupt_claim_reconstructs_exact_status_and_post` —
   api.rs:24818: `called Result::unwrap() on an Err value: daemon instance
   lock … the configured port is 0, so the incumbent address cannot be probed`.
3. `tests::replacement_start_waits_out_a_shutting_down_predecessor` —
   lib.rs:1796, one sighting: `waited 1.50062225s, its own whole shutdown
   budget`.

NOT in scope: `index::tests::git_resolves_quoted_included_origin_from_linked_worktree`
(registry names it a TASK-STWVB flake; its first captured panic is now recorded
on STWVB). NOT in scope: the dispatch_endpoint.rs Gatekeeper family (owned by
its own fixture-rework task).

=== Ask

Per the 5FEN5 standard: reproduce each under the DJ1WV load recipe (10-12 cpu
hogs + --test-threads=64; add suite-concurrent binaries if hogs alone don't
bite), then FIX at the synchronization rather than the budget size
(R74E8/ATAXN). Registering with this task as owner is the fallback, fixing is
the goal; on fix, no registry entry may remain owned by this task.

Note from 5FEN5's diagnosis style: (1) looks like a fixed-budget wait on
background release work; (2) looks like environment/config bleed (port 0)
rather than timing — diagnose before assuming it's the same class.

=== Acceptance

- Either each test survives >=5 consecutive loaded runs post-fix (with pre-fix
  red counts pinned for A/B), or it is registered with its measured signature
  and the diagnosis documented here.
- Injection per TCTTD standard where patch-expressible; deterministic trigger
  preferred over load replay, per the 5FEN5 artifact's precedent.
- No production behaviour change without naming it; test-side-only fixes state
  so explicitly.

FOURTH ENTRY [2026-07-29, from TASK-EP3H1's gate run]: `recovery_faults_kill_real_daemon_and_preserve_original_pane` (crates/orgasmic-daemon/tests/recovery_fault_restart.rs:308) — `called Result::unwrap() on an Err value: AddrParseError(Socket)`. The test harness reads the child daemon's address file and parse().unwrap()s it; under load it reads mid-write and parses a truncated address. Fix is in the harness (retry on parse failure inside the existing 20s deadline loop), not product code. Green in isolation.

* IN_PROGRESS TASK-4YC8E hermes reasoning_effort alias collides with the daemon's dual-key driver_config :drivers:bug:
:PROPERTIES:
:ID: TASK-4YC8E
:PRIORITY: P1
:END:

`adapters/hermes.rs` declares

    #[serde(default, alias = "effort")]
    reasoning_effort: Option<String>

while the daemon writes the value under BOTH keys at once (`api.rs`:
`"effort": effort` and `"reasoning_effort": reasoning_effort`, lines 3573-3574;
also 2329-2330). Serde then sees one field populated twice and fails
deserialization, so `validate_config` returns `InvalidConfig` and the dispatch
dies with

    400 Bad Request: {"error":"driver configuration is invalid"}

Measured on claude 2026-07-26: adding that exact alias to `ClaudeAcpConfig`
reproduced this immediately, and removing it fixed it. Hermes carries the alias
today, so a hermes dispatch that sets an effort should fail the same way.

NOT VERIFIED for hermes itself — inferred from identical code and a reproduced
failure in a sibling adapter, not from a hermes dispatch. Confirm before fixing:
run a hermes acp-stdio dispatch with `--effort high` and check for the 400.

If confirmed the fix is one line — drop `alias = "effort"`, since the daemon
already writes `reasoning_effort`. Check codex too; it also carries
`reasoning_effort` and needs the same read.

Worth fixing beyond the crash: the error text names neither the field nor the
adapter. `InvalidConfig(e.to_string())` has serde's message in hand and drops it
on the floor at the API boundary. Surfacing it would have made this a ten-second
diagnosis instead of a grep through api.rs.

The dual-key write is itself questionable — one value under two names is what
made an ordinary alias a landmine. Consider emitting only `reasoning_effort`
once every adapter reads that key.

