#+title: orgasmic task TASK-XQCNA
#+orgasmic_version: 2

* IN_REVIEW TASK-XQCNA Terminal run states carry a classified reason and the evidence path in dispatch-status; API 500s carry the inner cause :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-XQCNA
:PRIORITY: P1
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 1.
Four failures looked identical on the surface (`[pid-gone] [run-gone] [unreported]`, empty `-last.txt`) while the truth sat in the session jsonl or daemon log: fd exhaustion (`failed to acquire worker run` 500 hid `Too many open files`), codex quota exhausted, hermes transport crash after the review was written, stall kill while waiting on tests. The manager re-dispatched blind twice and reads an empty report as an approval.
** Expected
- Every terminal state has a classified `exit` reason: reported, provider_quota (with retry-after), provider_auth, transport_error, stall_killed, spawn_failed(<os error>), worktree_setup_failed, operator_aborted.
- `dispatch-status` prints the reason and the evidence path (session jsonl) under the flags line.
- API 500s carry the inner error verbatim: `{"error":"failed to acquire worker run","cause":"spawn: Too many open files (os error 24)"}` and name the next command (`orgasmic daemon status`).
** Acceptance
- A fixture per reason class; dispatch-status output shows `exit:` + `evidence:` lines; the 500 body includes `cause`.
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commits 8a0de9e0 d4b457c4 238bd1d0 6355a76d on sprint/XQCNA, merged onto local sprint-20260902 as e796cb72. (1) orgasmic_core::ExitReason (serde tag 'reason') on Lifecycle::Release{exit} (serde default); supervisor freezes the first fatal DriverError/RunFail line onto RunRecord.first_error and classifies at both Release-writing sites. Classes and their backing: reported=finalized_by_worker; stall_killed=STALL_TIMEOUT_REASON prefix; operator_aborted=ReleaseOutcome::Cancelled; provider_auth='not logged in'/'/login'/'authentication'/401/unauthorized/'invalid api key' (existing preflight/adapter strings); provider_quota{retry_after}=429/'rate limit'/quota/'usage limit'/'too many requests' (GENERIC tokens — no driver emits a quota string today; unit-tested only); spawn_failed{os_error}=the drivers' '<bin> spawn: … (os error N)' format; transport_error=other fatal DriverError; unknown{detail}=rest. worktree_setup_failed exists but is never produced (worktree setup is CLI-side before a run exists). (2) dispatch-status prints '  exit: <reason> [<detail>]' + '  evidence: <session jsonl>' for open generations whose run is gone; pre-upgrade tombstones print unknown [<release reason>]. (3) ApiError::internal_with_cause -> {error, cause (SupervisorError verbatim), next:'orgasmic daemon status'} on all 5 acquire sites + the recover catch-all; CLI prints the cause. NOT on RunSummary (released runs leave the snapshot; the Release tombstone is the durable copy). Tests: core 3, supervisor 5 fixtures extended, api acquire_error_cause, cli 2. Gates (private target): daemon 56, cli 15, clippy 4 crates rc=0, fmt rc=0. Operator review/test after the sprint. Unblocks TASK-40ZMJ's quota part once a provider actually emits a quota string.
- 2026-09-02 sprint review (daemon reviewer): serde compatibility CONFIRMED. Lifecycle has no deny_unknown_fields; RunMeta.preflight and Release.exit are Option + serde(default) + skip_serializing_if, so old JSONL deserializes to None and new JSONL is readable by old code (unknown fields ignored). The ExitReason internal 'reason' tag round-trips (tested in core). LOW noted: from_error_line uses substring heuristics ('401'/'authentication' match anywhere in the first error line) and checks AUTH before QUOTA, so a line containing both classifies as provider_auth; exit is advisory metadata only. LOW noted: internal_with_cause puts the inner driver error verbatim in the 500 body - deliberate, token-authed local API, but new information exposure.

