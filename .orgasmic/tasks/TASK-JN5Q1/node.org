#+title: orgasmic task TASK-JN5Q1
#+orgasmic_version: 2

* DONE TASK-JN5Q1 Bound run enumeration when recovery records reference dead workers :daemon:recovery:cli:
:PROPERTIES:
:ID: TASK-JN5Q1
:END:

Reproduce and fix GET /api/runs exceeding the CLI HTTP deadline when durable failed/open run sessions trigger serial driver attach probes.

Acceptance:
- failed terminal tombstones do not perform meaningless liveness attach probes
- genuine attach proofs have a bounded deadline and multiple probes cannot stack beyond the run-list client budget
- GET /api/runs returns a complete classified response when one driver probe is unresponsive
- manager dispatch-close releases its recorded run id directly and no longer requires GET /runs
- focused regressions plus daemon/CLI gates pass

Out of scope: boot transaction replay duration and concurrent daemon restart bind races.

