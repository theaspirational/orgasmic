#+title: Cancelled
#+orgasmic_version: 1

* CANCELLED TASK-M5JTN.2 cursor-agent driver synthetic run_complete on subprocess exit (path-a refactor): clean last.txt summaries on live implementer dispatches :daemon:drivers:cursor:dispatch:design:superseded:
:PROPERTIES:
:ID:               TASK-M5JTN.2
:SUPERSEDED_BY:    TASK-J95T2
:PRIORITY:         P3
:WORKER:           implementer-cursor
:IMPLEMENTS:
:PRODUCES:         crates/orgasmic-drivers/src/adapters/cursor.rs
:WRITE_SCOPE:      crates/orgasmic-drivers/src/adapters/cursor.rs crates/orgasmic-drivers/src/modes/subprocess_stream_json.rs crates/orgasmic-daemon/src/api.rs crates/orgasmic-daemon/tests/dispatch_endpoint.rs
:TEST_CMD:         cargo test --workspace
:DEPENDS_ON:       TASK-M5JTN
:END:

** Description
Closes TASK-M5JTN reviewer F-2: Item 1's path (c) (accumulate all system "thinking delta" chunks into the summary) produces noisy last.txt on live cursor-agent implementer dispatches (composer-2.5-fast emits 500+ raw thinking chunks with no terminal event). Path (a) — driver synthesizes a =DriverEvent::RunComplete { summary }= on clean subprocess exit when no terminal was emitted — is the correct long-term fix. The reviewer confirmed feasibility: =cursor.rs= already has =terminal_emitted=, =emit_run_complete_once=, =translate_result=; =subprocess_stream_json.rs= currently emits nothing on successful exit when =!terminal_emitted()= (lines 276-286).

After path (a) ships, the system-text fallback in =dispatch_last_summary_from_session= becomes redundant for cursor-agent dispatches (the synthetic =run_complete= will win precedence). Decision: keep the system fallback in place as a defense-in-depth for other drivers that might exhibit the same emission pattern, OR remove it once path (a) is verified comprehensive — defer that judgment to this implementer.

** Acceptance Criteria
- [ ] In =crates/orgasmic-drivers/src/modes/subprocess_stream_json.rs= (or =crates/orgasmic-drivers/src/adapters/cursor.rs=, depending on architectural fit), synthesize a =DriverEvent::RunComplete { summary }= when: (a) subprocess exits with status 0, (b) no terminal session marker (=RunComplete=, =Lifecycle::Release=) has been emitted, and (c) some content was accumulated during the run. Use the accumulated assistant text if available, otherwise the system text (consistent with =dispatch_last_summary_from_session= precedence).
- [ ] Verify NO regression on cursor-agent dispatches that DO emit a natural terminal event (e.g., reviewer-cursor dispatches that emit their own =run_complete=) — the synthetic emission must NOT fire when a real one has already arrived. Use the existing =terminal_emitted= flag.
- [ ] Live dispatch (or test that emulates the live shape) of an implementer-cursor run with no real terminal event produces a non-empty =last.txt= where the content is the SUMMARY (distilled assistant or system text), not raw concatenated thinking deltas.
- [ ] Add a focused integration test in =crates/orgasmic-daemon/tests/dispatch_endpoint.rs= OR =crates/orgasmic-drivers/tests/= that emulates the cursor-agent "system-only, no terminal" shape, runs through the full driver-mode pipeline, and asserts the synthetic =RunComplete= is emitted with the expected summary.
- [ ] Optional: with path (a) shipped, the system-text fallback in =dispatch_last_summary_from_session= becomes potentially-removable. If the implementer determines it's safely removable (no other driver depends on it), remove and document. Otherwise leave in place as defense-in-depth and note why.
- [ ] =cargo test --workspace + clippy + fmt + diff= all green. MSRV 1.78.

** Notes
NOT :FIX_SUBTASK: t — this is an architectural refactor (not a one-round fix on a prior reviewer's findings list), so full IMPLEMENT → REVIEW → DONE lifecycle applies. Reviewer pass after merge per persona rule 2. P3 because the system-fallback path SHIPPED in TASK-M5JTN covers the immediate gap; this is the clean-up. Could be safely deferred behind higher-priority work (TASK-NGDNP hung endpoint, install-path hardening, etc.) unless the implementer dogfood velocity matters more than the cleanup.


* CANCELLED TASK-K3Y04 Make Settings reachable for a projectless visitor (ConnectGate "Advanced →") :ui:auth:onboarding:routing:
:PROPERTIES:
:ID:               TASK-K3Y04
:PRIORITY:         P3
:WORKER:           manager-direct
:WRITE_SCOPE:      ui/src/app/router.tsx ui/src/components/AppShell.tsx
:READ_SCOPE:       ui/src/components/SettingsView.tsx ui/src/lib/backend.ts
:TEST_CMD:         npm --prefix ui run build
:DEPENDS_ON:       TASK-EBVHJ
:END:

** Description
Follow-up to TASK-EBVHJ. The =ConnectGate= "Advanced →" button calls =goView('settings')=, but =pathForView= (=AppShell.tsx= ~136) falls back to =/board= whenever =projectId= is null, and Settings is only project-scoped (=/projects/$projectId/settings=). So for the exact target user — a fresh tunnel visitor with no project selected — "Advanced →" lands on =/board= instead of Settings, where they can't adjust baseUrl/profiles before connecting.

Pick the smallest coherent fix:
1. Add a global, project-independent Settings route (e.g. =/settings=) the gate can always reach (the connection/profile section needs no project); OR
2. If a global route is too invasive pre-MVP, hide/disable "Advanced →" in =ConnectGate= when there is no active project (so the button never dead-ends), and surface profile/baseUrl editing inline in the gate instead.

** Acceptance Criteria
- [ ] From a projectless origin, the visitor can reach connection/profile settings (baseUrl + token) without first selecting a project — OR the dead-end affordance is removed and equivalent control lives in the gate.
- [X] No regression to TASK-EBVHJ's blocking token flow; =npm --prefix ui run build= + =tsc --noEmit= green.

** Evidence
Chose option 1 (global route — cleaner than hiding the affordance, and =SettingsView= already handles =projectId: null= by disabling the project-config section). Implemented directly by the manager (trivial ~16-line scope), landed on main in =cbe7c6e=: new root-level =globalSettingsRoute= (path =settings=) → =<SettingsView projectId={null}/>= in =ui/src/app/router.tsx= (added to =routeTree=); =pathForView= in =AppShell.tsx= now returns =/settings= for =goView('settings')= when =projectId= is null (else the existing =/projects/$projectId/settings=). So "Advanced →" and the header settings button both reach Settings projectless. Gates: =tsc --noEmit= + =npm run build= green. Browser click-through unverified this session (no browser-automation tool).

*Reverted (cancelled).* On operator review (screenshot), the =ConnectGate= "Advanced →" button was removed entirely (=9d688fe=): it was added scope beyond the original ask AND subtly broken — =ConnectGate= is a blocking Radix modal, so navigating to settings left the overlay on top, blocking the page it opened; and a tunnel visitor's =baseUrl= defaults to same-origin, so only the token is needed. With the button gone, this task's premise (make its target reachable) no longer applies, so the global =/settings= route + =pathForView= branch were reverted in the same commit. The modal is now just token input + Connect. Net effect of TASK-K3Y04 on the tree: nil.

* CANCELLED TASK-Y8TVF Fix stale shipped-worker count assertion in parses_every_shipped_worker (16 vs 18) :core:tests:fixtures:polish:
:PROPERTIES:
:ID:               TASK-Y8TVF
:PRIORITY:         P4
:WORKER:           implementer-claude-rmux
:WRITE_SCOPE:      crates/orgasmic-core/tests/fixtures.rs
:READ_SCOPE:       crates/orgasmic-core/tests/fixtures.rs shipped/workers/
:TEST_CMD:         cargo test -p orgasmic-core --test fixtures
:END:

** Description
=parses_every_shipped_worker= (crates/orgasmic-core/tests/fixtures.rs:495)
asserts =count == 16=, but =e94d8fa= added two worker templates
(implementer-claude-rmux, reviewer-claude-rmux), making 18. The test fails on
every =cargo test --workspace= since then; worker worktrees missed it because
their workspace runs died earlier on the missing-ui-node_modules TypeScript
issue. Bump the count — or better, replace the hardcoded tripwire with an
assertion that every =shipped/workers/*.org= parses plus a minimum-count floor
(e.g. >= 16), so template additions stop breaking it. Keep the
unsupported-pair check unchanged.

** Acceptance Criteria
- [ ] =cargo test -p orgasmic-core --test fixtures= green with 18 templates.
- [ ] Adding a future template does not require touching this test (or the
  test states clearly why a manual bump is intended).
- [ ] Gates: clippy -D warnings, fmt --check, MSRV 1.78.

** Evidence
CANCELLED [2026-06-10]: resolved as housekeeping inside TASK-K5RG2 (worker
commit =4a6a020=, merged =1ba44b8=) — count bumped 16→18 plus the
selector-determinism template list updated. The structural improvement
(derive count from the directory) was NOT done; reopen only if template
churn makes the manual bump painful.

* CANCELLED TASK-E29XC EXFTV dogfood probe (throwaway): confirm claude-acp real-wire :dogfood:throwaway:
:PROPERTIES:
:ID: TASK-E29XC
:PRIORITY: P5
:WRITE_SCOPE: crates/orgasmic-drivers/src/adapters/claude.rs
:END:

Add a single trailing doc-comment line at the very top of crates/orgasmic-drivers/src/adapters/claude.rs noting the adapter was dogfood-verified. This is a throwaway probe to validate the real-wire claude-acp stdio path (TASK-EXFTV).

* CANCELLED TASK-PGFPM Make companion babysitting functional, then restore default worker attach :daemon:supervisor:babysitter:wiring:reenable:low:
:PROPERTIES:
:ID:               TASK-PGFPM
:PRIORITY:         P5
:WORKER:           implementer-claude-rmux
:WRITE_SCOPE:      crates/orgasmic-daemon/src/supervisor.rs crates/orgasmic-daemon/src/api.rs crates/orgasmic-daemon/tests/ shipped/workers/**
:READ_SCOPE:       crates/orgasmic-daemon/src/supervisor.rs crates/orgasmic-core/src/session.rs shipped/workers/babysitter-stall-detector.org
:TEST_CMD:         cargo test -p orgasmic-daemon
:END:

** Description
The babysitter (=babysitter-stall-detector=) is a companion run attached by
explicit manager dispatch when the resolved worker template declares
=:BABYSITTER_WORKER:=. It is not the deleted task-state auto-spawn watcher from
dec_WC1PW, and this task must not reintroduce implicit worker spawns from task
lifecycle transitions.

The companion babysitter has never actually done its job: in practice it either
died at boot (=Not logged in= under the acp-stdio claude harness) or sat idle
until the stall detector reaped it. Investigation [2026-06-11] found the
intervention path is also unwired independent of auth:

- =flush_babysitter_summary= in =supervisor.rs= -- the function that packages
  the implementer's coalesced events into a =BabysitterSummaryChunk= every 50
  events / 60s -- is only ever called from tests. No live daemon loop forwards
  summaries to the babysitter run.
- Its four tools (=poke_implementer=, =escalate_to_human=,
  =restart_implementer=, =record_finding=) are an acp-stdio/ACP concept with no
  delivery channel on the TUI drivers.

As an interim, companion attach was disabled by removing =:BABYSITTER_WORKER:=
from all 17 default worker configs, and =babysitter-stall-detector.org= was
repointed from acp-stdio/sonnet to rmux/claude/=claude-haiku-4-5= (an
authenticated path) so it at least boots and is watchable. This task makes the
explicit-dispatch companion babysitter real, then restores default worker
=:BABYSITTER_WORKER:= declarations without reviving the removed task-state
watcher.

** Acceptance Criteria
- [ ] A live supervisor loop forwards =BabysitterSummaryChunk= envelopes to the
      explicit-dispatch companion babysitter run on the documented cadence
      (about 50 events / 60s), driven from real implementer activity rather than
      a test-only call.
- [ ] The babysitter's closed tool set is reachable on its chosen driver
      (=poke_implementer=, =escalate_to_human=, =restart_implementer=,
      =record_finding=), and a tool call produces the expected supervisor
      effect.
- [ ] An integration test drives an explicit dispatch or supervisor acquire with
      a worker declaring =:BABYSITTER_WORKER:=, asserts the babysitter receives
      at least one forwarded summary chunk, and asserts =record_finding= lands
      on the task's Evidence.
- [ ] Once the above pass, restore =:BABYSITTER_WORKER: babysitter-stall-detector=
      on default explicit-dispatch worker templates where companion babysitting
      is intended. Do not restore or recreate the deleted task-state auto-spawn
      watcher path from dec_WC1PW.
- [ ] Re-confirm the UI watcher dot (TaskAgentBadges) opens the companion
      babysitter's live transcript for a real intervening babysitter run, or
      document the exact blocker if live UI verification is impossible in this
      dispatch.

** Evidence
- [2026-06-13 Sat] Manager-implementer commit 504ccf3 implemented live companion babysitter summary forwarding: worker driver events now auto-flush BabysitterSummary chunks to the companion JSONL at 50 events / 60s; BabysitterTool accepts the shipped prompt's long tool names (poke_implementer, restart_implementer, escalate_to_human, record_finding) while keeping short aliases; :BABYSITTER_WORKER: babysitter-stall-detector restored on non-rmux shipped workers only. Gates: cargo test -p orgasmic-core babysitter_tool_set_is_closed; cargo test -p orgasmic-drivers babysitter_tool_set_is_closed; cargo test -p orgasmic-daemon live_babysitter_summary_flushes_on_event_threshold; cargo test -p orgasmic-daemon babysitter_can_invoke_allowed_tool; cargo test -p orgasmic-daemon; cargo fmt --check; cargo clippy --workspace --all-targets -- -D warnings; git diff --check. Task remains TODO: real record_finding -> task Evidence and live UI watcher-dot verification are not proven in this manager session; current Supervisor has no project/task Evidence writer hook for babysitter-origin tool calls.

* CANCELLED TASK-GFKFW.2 fix-subtask: live remote-Linux daemon smoke (systemd --user + SSH tunnel) :install:remote:daemon:smoke:fix:
:PROPERTIES:
:ID:               TASK-GFKFW.2
:PRIORITY:         P3
:WRITE_SCOPE:      .orgasmic/backlog.org README.md
:TEST_CMD:         git diff --check
:END:

** Description
Close TASK-GFKFW's one open acceptance criterion: stand up a =systemd --user=
daemon on a Linux host with linger, SSH-tunnel =4848:127.0.0.1:4848= from
macOS, add a remote backend profile with token, init a project on the server,
and confirm a worker run executes on that host. Ops smoke + Evidence entry; no
code expected unless gaps surface.

** Evidence
- Verified premise: TASK-GFKFW.2 is the live remote-Linux ops smoke left open from TASK-GFKFW, not a code implementation task. It requires a real Linux host with =systemd --user=/linger, an SSH tunnel, a remote backend profile/token, a project initialized on that host, and a worker run executed there.
- Existing repo documentation already describes the remote Linux path: install the daemon on the Linux host, supervise with =systemd --user=, keep daemon bind on =127.0.0.1=, connect from macOS by SSH tunnel, and treat remote project paths as owned by the remote daemon host. Prior TASK-GFKFW evidence also records this as documented-path-only and explicitly says the live Linux smoke was not run.
- Current environment does not provide a designated remote Linux smoke target, token, or operator approval for one of the available SSH aliases. Running the smoke against an arbitrary host would be unsafe and outside the task evidence contract.
- No code/doc gap surfaced inside the task scope; the missing ingredient is operator-provided remote infrastructure. Closing this queued item as CANCELLED rather than leaving an unactionable in-progress task.

* CANCELLED TASK-P8ZSP Native Windows PATH support for the CLI (PowerShell/cmd, %USERPROFILE%) :install:path:windows:platform:
:PROPERTIES:
:ID: TASK-P8ZSP
:END:

Extends dec_CKMW3 and dec_YKFZX. The current PATH-hardening is a POSIX-shell mechanism (env file sourced from ~/.*rc); native Windows shells do not read it. Make the CLI reachable on native Windows.

Work required:
- Home resolution: orgasmic-core Home::from_env uses $HOME; add %USERPROFILE% (and/or %ORGASMIC_HOME%) fallback for Windows.
- Installer: install.sh is bash-only; provide a Windows install path (install.ps1 or equivalent) that downloads/places the runtime bundle.
- PATH wiring: instead of a sourced sh env file, set the user PATH via PowerShell $PROFILE and/or setx / the User-PATH registry value; make it idempotent and reversible.
- Binary linking: Windows symlinks need Developer Mode/privilege — fall back to copying the binary or a shim .cmd/.exe launcher into a bin dir on PATH.
- CLI surfaces: path_env::relink_source_binary currently bails on non-unix; add Windows variants for `orgasmic path ensure`, `orgasmic doctor --fix`, and the doctor 'cli on PATH' finding.
- Verification: confirm `orgasmic` resolves in a fresh PowerShell and cmd session.

Note: WSL and Git-Bash/MSYS2 already work via the unix path; this task is specifically native PowerShell/cmd.
** Evidence
[2026-07-06 Mon] CANCELLED by operator decision during goal-20260706-drain-the-full. The task is technically valid (path_env::relink_source_binary still bails on non-unix; Home::from_env uses $HOME only), but native Windows (PowerShell/cmd) is NOT a supported target right now — WSL and Git-Bash/MSYS2 already work via the unix path. Re-file if/when native Windows becomes a target. No code shipped.

* CANCELLED TASK-FN18B base_worker.org still tells stage kinds to call finalize they cannot use
:PROPERTIES:
:ID: TASK-FN18B
:IMPLEMENTS: dec_WDR5K
:END:

Post-TASK-8PXDP residual (dec_WDR5K item 6 fifth amendment). The daemon now correctly keeps protocol-end = Completed for /grill, /plan stage runs, but base_worker.org (which those kinds inherit) still carries the universal 'call orgasmic dispatch finalize' instruction. The CLI rejects finalize for non-dispatch kinds, so a grill/plan worker may waste a turn attempting it. Fix: make the inherited finalize instruction conditional on being a dispatched worker (implementer/reviewer/architector), or move it out of base_worker into the dispatch-kind specs. Cosmetic/efficiency, not corrupting — daemon behavior is already correct.

* CANCELLED TASK-6ZTFM TASK-QPKCD fix round 3: early-exit ownership and daemon recovery authority
:PROPERTIES:
:ID: TASK-6ZTFM
:IMPLEMENTS: dec_WDR5K
:PARENT: TASK-QPKCD
:END:

Resolve final review findings after e8df69f: close no-PID/drain-before-insert early-exit races; move claims to daemon-owned project-scoped authority; make Pending/Committed crash-atomic and reconcilable; pin trusted Claude binary with structured resume mode; clear delta clippy.

* CANCELLED TASK-NW4WV TASK-AFE5Q fix round 7: atomic cleanup and startup ownership
:PROPERTIES:
:ID: TASK-NW4WV
:IMPLEMENTS: dec_WDR5K
:END:

Resolve all seven HIGH findings from TASK-4W8XH review of de94d4d: atomic reservation installation; fail-closed branch/worktree/artifact identity; cancellation-atomic tmux/rmux child ownership; truthful finalize endpoint tests and warnings; mux-bound Cursor trust send; full ambiguous POST ownership and rollback handling.

* CANCELLED TASK-3TEDA TASK-QPKCD fix round 6: single-authority recovery and proven native fork
:PROPERTIES:
:ID: TASK-3TEDA
:IMPLEMENTS: dec_WDR5K
:END:

Resolve all five HIGH findings from TASK-Y8HCP review of f80d971: receiver-only quiescence including timeout drain; complete immutable pre-spawn plan and lifecycle backfill; handle-bound claim authority; explicit symlink-capable pinned Claude install identity with PATH-shim proof; unique fail-closed fork discovery and two-recovery chain.

* CANCELLED TASK-RRVX0 TASK-35TK4 fix: eliminate CLI lock-probe acquisition race :fix:daemon:
:PROPERTIES:
:ID: TASK-RRVX0
:PRIORITY: high
:READ_SCOPE: .orgasmic/gotchas.org,crates/orgasmic-daemon,crates/orgasmic-cli
:TEST_CMD: cargo test -p orgasmic-daemon && cargo test -p orgasmic-cli
:WRITE_SCOPE: crates/orgasmic-daemon,crates/orgasmic-cli
:END:

** Description
Claude Opus review of b18808c..bd3d7bd returned SHIP but identified one medium new regression: the CLI lock-state probe briefly acquires daemon.lock exclusively, while daemon startup makes a single nonblocking exclusive-lock attempt. A microsecond overlap can make a legitimate booting daemon misclassify the CLI probe as an unhealthy incumbent and exit.

** Acceptance Criteria
- Daemon lock acquisition tolerates the CLI probe transient without weakening single-instance exclusion or spawning work before lock ownership.
- Retry behavior is tightly bounded (roughly 100-150 ms total), handles only WouldBlock, and preserves immediate propagation of other filesystem/lock errors.
- Deterministic regression forces the overlap and proves the daemon eventually acquires after the probe releases; a continuously held incumbent still fails closed.
- Preserve Rust 1.87 compatibility by calling fs2::FileExt methods explicitly.
- Run focused lock/autostart/double-serve tests, cargo fmt --all --check, cargo test -p orgasmic-daemon, cargo test -p orgasmic-cli, and cargo clippy --workspace --all-targets -- -D warnings; classify known baseline watcher/bootstrap flakes separately.
** Notes
The two originally assigned Codex gpt-5.6-terra/high attempts were interrupted by shared daemon restarts and produced no edits. A concurrently dispatched equivalent fix completed under TASK-870YX: commit 4c0a151, merged to main as e4b6b7b. Manager independently reran the focused daemon-lock, CLI boot-owner, and double-serve regressions; all passed. This duplicate record is cancelled as superseded, not left as an unfixed defect.

* CANCELLED TASK-Q07Y5 WGXKD.2 fix round: bound writer shutdown and derive ExitTimeOut from the real budget :dispatch:daemon:cli:fix:
:PROPERTIES:
:ID: TASK-Q07Y5
:PRIORITY: P0
:test_cmd: cargo test -p orgasmic-cli
:write_scope: crates/orgasmic-cli/src/daemon_service.rs crates/orgasmic-cli/src/daemon_lifecycle.rs crates/orgasmic-cli/tests/daemon_lifecycle.rs crates/orgasmic-daemon/src/lib.rs crates/orgasmic-daemon/src/writer.rs
:END:

Fix round for the reviewer BLOCK SHIP on TASK-WGXKD.2 (merged `6a518f3`).
Reviewer: reviewer-codex-rmux (rmux / codex / gpt-5.6-sol / high),
run-20260728T103240, reported tx-20260728-orgasmic-3136, closed
tx-20260728-orgasmic-3137.

The merged change is NOT reverted. Its central claim held under review: `try_admit`
checks `closed` and inserts the admission under the same `watch::Sender` write
lock, `post_run_release` acquires it before its first await, the admission moves
into the detached task so cancellation retains ownership through `Drop`, and no
sole handle is taken before a cancellable await. `daemon restart` does parse the
real `/daemon/restart` response with a 40s client ceiling, SIGTERM and Ctrl+C both
route through `RunningDaemon.shutdown`, and the macOS plist is rewritten on every
service start (`start_macos_launch_agent` writes it before bootstrap), not only on
install. All independent gates exited 0 at `ca3f9df`.

What follows is what the round claimed and did not establish.

** 1. HIGH — `ExitTimeOut 60` is not larger than the worst-case shutdown, because
   half of that shutdown has no budget at all

`crates/orgasmic-cli/src/daemon_service.rs:707-727`,
`crates/orgasmic-daemon/src/lib.rs:665-688`,
`crates/orgasmic-daemon/src/writer.rs:513-522`.

After SIGTERM, `RunningDaemon` waits up to 20s for release finalizations and then
calls `WriterHandle::shutdown().await`. That call queues behind prior writer
commands and awaits its reply **with no timeout**. `RESTART_WRITER_DRAIN_TIMEOUT`
bounds only the pre-stop marker, not `WriterHandle::shutdown`.

Concrete failure: a terminal-tx append is still blocked in writer I/O or fsync
when the 20s release drain expires; shutdown then waits behind that append, and
launchd sends SIGKILL at 60s while the append is still non-durable. That
recreates the released-but-unreported loss the whole WGXKD family exists to
prevent.

The comment at daemon_service.rs:707 reasons "drain (20s) + writer shutdown < 60",
but the second term is unbounded, so the inequality is unprovable rather than
generous. The budget test added in this round checks 40 > 20 + 5 and 35 > 20 — it
never proves the plist timeout covers writer shutdown.

**This is live on the operator's machine right now.** The plist was rewritten with
`ExitTimeOut 60` on 2026-07-28 (tx-20260728-orgasmic-3118); the window is wider
than it was, and still not closed.

Required: give writer shutdown an explicit finite budget and a durable timeout
outcome, then **derive** the service-manager timeout from release drain + writer
shutdown + margin rather than hard-coding 60. If the derived number differs from
60, the plist must carry the derived one, and a test must assert the plist value
is the derived value — not a literal.

** 2. LOW — the production lifecycle composition still has no end-to-end regression

`crates/orgasmic-cli/src/daemon_lifecycle.rs:1498-1563`,
`crates/orgasmic-cli/tests/daemon_lifecycle.rs:99-181`.

The >5s test uses a stand-in HTTP server and stops after parsing its response; the
SIGTERM test has no in-flight release. Both pass even if the real `daemon restart`
sequence parses the drain response and then has launchd cut off the old daemon
during writer shutdown — which is exactly finding 1.

Required: drive the actual lifecycle restart path with a release lasting beyond
five seconds, including service stop and replacement bind. Testing the HTTP wait
and the SIGTERM routing in isolation is what let finding 1 through.

** Acceptance

- `WriterHandle::shutdown` has a finite budget and a defined outcome on timeout;
  the outcome is durable or recoverable, not silent.
- The macOS `ExitTimeOut` is derived from the drain and writer budgets, and a test
  asserts the rendered plist carries the derived value.
- A SIGTERM test holds a terminal-tx append in flight through the release timeout
  and proves either durability before exit, or a durable run-specific recovery
  record written before the watchdog can kill the daemon.
- An end-to-end restart regression covers service stop and replacement bind with a
  release that outlasts five seconds.

** Non-goals

- Do not revert `6a518f3`. Its atomic-admission fix is sound and reviewed.
- Reviewer finding 2 (old CLI → new daemon terminal-tx race) is **out of scope**
  and filed separately. It is pre-existing from TASK-WGXKD.1 and the reviewer
  states it does not block this round.

* CANCELLED TASK-F9A34 App crashes with "undefined is not an object (evaluating 't.sections.map')" error boundary
:PROPERTIES:
:ID: TASK-F9A34
:PRIORITY: P2
:READ_SCOPE: ui/src/**
:TEST_CMD: cd ui && npm test
:WRITE_SCOPE: ui/src/**
:END:

Reported by the user 2026-07-29 from a running app build: the desktop window renders the top-level error boundary ("Something went wrong!") with the minified message `undefined is not an object (evaluating 't.sections.map')`, blank behind the banner — so a top-level view crashes during render, not an isolated panel. `t` is a bundler-renamed local; the real signal is `.sections.map` on undefined. Unguarded call sites of that shape: NodeDocEditor.tsx:41 (`toDraft`, `doc.sections.map`) and :63 (`effectiveFields`), and PromptStudioView.tsx:423 (`layered.sections.map`); the same pass should check `doc.properties.map` (NodeDocEditor.tsx:42) and `parseOrgSourceNodes(...).sections.find` (node-views/orgNodes.ts:89,111). Wanted: which payload actually produces the undefined and why (daemon response shape, render-before-load, or version skew), a fix at that source rather than a `?.` at the call site, a regression test for the payload shape that crashed, and enough context in the error boundary (view/component name) that the next minified crash is diagnosable without guessing.

** Description
See the task body above.

** Evidence


** Suspects


** Acceptance

* CANCELLED TASK-15MJN RESTART_DRAIN_TIMEOUT carries 15s of narrative slack; name it as a constant on the restart-endpoint axis :daemon:hardening:
:PROPERTIES:
:ID: TASK-15MJN
:END:

From TASK-G7E4R's axis sweep (its report, section 3): daemon_lifecycle::RESTART_DRAIN_TIMEOUT (40s) waits on the restart endpoint's own budgets (RELEASE_FINALIZATION_DRAIN_TIMEOUT 20s + RESTART_WRITER_DRAIN_TIMEOUT 5s = 25s) and is already guarded by the relationship test restart_drain_fuse_outlasts_the_endpoint_budgets_it_waits_on — so it is NOT the unguarded-literal shape G7E4R removed. But its remaining 15s of slack ('rest of the handler') is documented in prose rather than composed from a named margin constant. Ask: express it as endpoint_budgets_total() + RESTART_HANDLER_MARGIN with the existing relationship test asserting the composition, the G7E4R/QRB8S pattern. Small; ship with a verify artifact.

* CANCELLED TASK-XHP43 tmux pane runs have no liveness signal either, so they are unprotected past 600s :tmux:drivers:defect:
:PROPERTIES:
:ID: TASK-XHP43
:PRIORITY: P2
:END:

Raised by the TASK-RWCRN reviewer as an open question. tmux.rs:1789-1824 has the same pane-is-not-an-event-source shape that TASK-RWCRN fixed for rmux, and only polls for session exit, so a tmux-mode TUI run emits no driver events after ready and is released by the 600s stall detector exactly as rmux was. The reviewer AGREED with leaving tmux out of the rmux-scoped RWCRN fix — tmux has no existing output subscription to reuse, so adding capture polling there would widen both risk and retention scope, and the standing worker matrix is rmux — but flagged that tmux/claude, tmux/codex, tmux/cursor-agent and tmux/hermes remain in orgasmic_drivers::SUPPORTED and are documented as usable. Either give tmux an equivalent liveness signal, or stop advertising tmux pairs as safe for runs longer than 600s. Depends on the shape TASK-RWCRN.1 settles: if pane observation moves from lines to raw bytes, tmux should adopt the same contract rather than inventing a second one.

* CANCELLED TASK-TH906 manager dispatch --effort is silently dropped for every non-claude harness, and the ledger records it as fact anyway
:PROPERTIES:
:ID: TASK-TH906
:PRIORITY: P1
:END:

** Description
=orgasmic manager dispatch --harness codex --effort high= records =:EFFORT: high=
on the =manager.dispatch_started= tx and =:EFFORT_OVERRIDE: high= on the
=run.created= tx, *and never passes the value to the harness.*

=crates/orgasmic-drivers/src/modes/tmux.rs:989-994= pushes =--effort= only inside
the =if is_claude= block. The non-claude branch at =:1004-1011= pushes =--model=
and nothing else:

: if matches!(harness, "codex" | "cursor-agent" | "hermes") {
:     if let Some(model) = cfg.model.as_ref() { … push "--model" … }
: }

=modes/rmux.rs:1817-1828= has the identical shape — model for everyone, effort
for claude only.

*And =--effort= is not even the right flag for codex.* =codex --help=
(codex-cli 0.144.5) exposes =-m/--model= and =-c/--config <key=value>=; there is
no =--effort=. Reasoning effort is =-c model_reasoning_effort="high"=. So the
push would not have worked had it been reached.

** Measured, not inferred
Dispatch =tx-20260808-orgasmic-5485= (TASK-2QK4P.1.1.1 reviewer, codex, tmux) was
given =--model gpt-5.6-sol --effort high=. Both tx entries record =high=. The
worker's own pane footer read:

: gpt-5.6-sol xhigh · ~/.orgasmic/worktrees/orgasmic/task-2qk4p.1.1.1-review

=~/.codex/config.toml= on this machine carries =model_reasoning_effort = "xhigh"=,
which is what the run actually used. *The flag was dropped and the ledger asserts
otherwise.*

** Why this is P1
1. *The ledger is the product's evidence surface.* An effort recorded but not
   applied makes every dispatch comparison across efforts meaningless, and there
   is no way to tell after the fact which runs honoured the flag.
2. *The manager protocol echoes driver/harness/model/effort after each dispatch.*
   That echo is read by the operator as a statement of what ran. It was false
   here and the manager had no way to know.
3. It is silent. No warning, no refusal, no =unsupported=.

** Acceptance
1. Either PASS the effort to every harness that accepts one — for codex that is
   =-c model_reasoning_effort=<value>=, not =--effort= — or REFUSE the dispatch
   by name when the harness cannot take it. Do not record a value that was not
   applied.
2. If a harness genuinely has no effort control, =dispatch= must say so at
   dispatch time and the tx must not carry =EFFORT=/=EFFORT_OVERRIDE=.
3. Same treatment for =--model= on any harness that ignores it, and the same
   audit for the =rmux= path, which has the identical claude-only gate.
4. A regression per supported harness asserting the launch argv actually carries
   the requested effort, or that the dispatch was refused. =tmux.rs:3238-3255=
   already does this for claude; it has no codex counterpart, which is why the
   gap survived.
5. Check whether =cursor-agent= and =hermes= have effort controls too — they are
   in the same =matches!= arm.

** Notes
Observed on runtime =ed0ebbc=, codex-cli 0.144.5, 2026-08-08.

* CANCELLED TASK-QK8PT Temporary daemon refresh benchmark 2026-08-12 :perf-probe:
:PROPERTIES:
:ID: TASK-QK8PT
:PRIORITY: P3
:BASELINE_16: 16
:UPGRADED_16: 16
:END:

* CANCELLED TASK-4WSZV Live concurrent writer verification 2026-08-12
:PROPERTIES:
:ID: TASK-4WSZV
:LIVE_CHECK_04: 04
:LIVE_CHECK_09: 09
:LIVE_CHECK_11: 11
:LIVE_CHECK_14: 14
:LIVE_CHECK_03: 03
:LIVE_CHECK_07: 07
:LIVE_CHECK_05: 05
:LIVE_CHECK_02: 02
:LIVE_CHECK_06: 06
:LIVE_CHECK_13: 13
:LIVE_CHECK_16: 16
:LIVE_CHECK_01: 01
:LIVE_CHECK_12: 12
:LIVE_CHECK_15: 15
:LIVE_CHECK_08: 08
:LIVE_CHECK_10: 10
:END:

* CANCELLED TASK-79VKP.6 Bound dispatch build caches without cross-worktree false greens :framework:worktree:build:performance:
:PROPERTIES:
:DEPENDS_ON: TASK-3X5AQ TASK-GRCWC
:ID: TASK-79VKP.6
:PRIORITY: P1
:END:

** Description
The TASK-K9WWM final test worker created a separate 1.7 GiB worktree cache and spent 50.71 seconds compiling before one exact assertion test; the warm manager target ran all three exact tests in 7 seconds. TASK-3X5AQ proves that sharing CARGO_TARGET_DIR across checkouts can silently execute another checkout binary, and TASK-GRCWC guards destructive pruning. The remaining build-cache duplication has no implementation owner.

Measure and choose a sound cache strategy that keeps checkout-specific fingerprints and final binaries isolated. Candidate mechanisms must be proved rather than assumed: compiler-object caches such as sccache, immutable dependency seeding followed by private fingerprints, filesystem cloning or hardlinks with copy-on-write validation, bounded worktree slots for disk only, or explicit no-reuse with automatic quota pruning. Never share a mutable Cargo target directory across different checkout paths.

** Acceptance
- Baseline disk, cold build, warm build, and concurrent-build costs are recorded for manager plus at least two worktrees.
- The selected design cannot make an edited worktree report Fresh or run a binary built from another checkout; reproduce the TASK-3X5AQ negative control.
- Cache storage is bounded and attributable per project, and cleanup observes TASK-GRCWC safety.
- A TASK-K9WWM-shaped one-file test correction materially reduces duplicate compile time and disk without weakening test identity.

