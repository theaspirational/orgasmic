#+title: Backlog
#+orgasmic_version: 1

* BACKLOG TASK-P19ZM Daemon boot follow-ups from TASK-35TK4 review    :daemon:hardening:
:PROPERTIES:
:ID: TASK-P19ZM
:PRIORITY: medium
:END:

Independent review of b18808c..bd3d7bd found additional non-blocking follow-ups outside the shipped slice: pre-bind project filesystem work remains unbounded; timed-out Git lookup should surface an actionable macOS Files-and-Folders hint; live reindex currently drops a project when its scan times out; daemon KeepAlive/restart semantics deserve an end-to-end regression; daemon-home comparison should avoid raw path-string equality; and repo_url should clear when a remote origin is removed.

Acceptance: split or address these findings with focused regressions, preserving the lock-first and no-pre-bind-Git-spawn invariants. Keep TASK-RRVX0 separate because it is the new CLI lock-probe overlap risk.

* BACKLOG TASK-4DTBZ rmux review transport must survive auth-shadow and daemon disconnects    :drivers:rmux:claude:hardening:
:PROPERTIES:
:ID: TASK-4DTBZ
:PRIORITY: P1
:RELATES_TO: TASK-QSX56
:END:

Two consecutive TASK-QSX56 reviewer dispatches blocked the required Opus verdict for different transport reasons. Attempt 1 inherited a stale ANTHROPIC_AUTH_TOKEN from the foreground daemon and native Claude returned 401 even though the stored claude.ai login was healthy. After restarting the daemon with provider auth overrides removed, attempt 2 reached real Claude Opus 4.8/high and performed substantive analysis, then the rmux daemon closed the pane-output-cursor transport after 4m25s; the worker released protocol_end_without_finalize and no report was preserved. Define explicit provider-auth environment precedence/scrubbing for rmux launches, and make a daemon-side rmux disconnect preserve or reattach the native Claude session/report rather than losing the review. Add deterministic regressions for stale auth shadowing and rmux daemon disconnect during an active reviewer session.
HARD EVIDENCE [2026-08-03], and it is worse than "survive auth-shadow and daemon
disconnects": THE RMUX DAEMON DIED UNDER THREE CONCURRENT DISPATCHES AND KILLED
ALL OF THEM AT ONCE.

Measured. At =2026-08-03T00:03:10Z= the orgasmic daemon logged, for all three
live runs within 24 milliseconds:

: ERROR supervisor: driver release failed; continuing unconditional producer and
:       lifecycle cleanup  run_id="run-...a0f17bb2..." / "...f6cae37c..." / "...a642ea86..."
: (hint: verify the rmux daemon is running and the endpoint is reachable) and CLI
:  fallback (driver transport unavailable: rmux send child exited with exit status: 1)
: WARN api: dispatch completion watcher: release without worker finalize, flagging orphan  x3

Then the orgasmic daemon restarted at =00:04:11Z= — TWICE, two different boot_ids
one after the other, which is the launchd KeepAlive race. No machine reboot:
uptime was continuous at 7h01m and no new panic reports exist. =rmux ls= returned
EMPTY afterwards while a NEW =rmux-daemon= pid was already up.

Casualties: TASK-VEGRV and TASK-3CM0Q implementers, both with uncommitted work
(preserved by the manager as =ad368c6= and =a409197=), and the TASK-FZB6T round-3
reviewer, which lost its entire run. Three workers, one transport failure.

RMUX CYCLED ITS PID AT LEAST THREE TIMES during the 2026-08-02/03 session
(=6049= at 20:06, =99375= later, =3150= after this incident), so this is not a
one-off. It leaves NO trace: =rmux.err.log= has not been written since
2026-07-26, and there is no crash report in either DiagnosticReports directory.

WHY THIS IS SHARPER THAN ITS PRIORITY SUGGESTS. rmux was chosen for every
dispatch that session precisely BECAUSE it is the only pane transport with a
liveness channel in the installed runtime (=57e35de= predates TASK-4CSMYs tmux
channel). So the safe-looking choice is the one that lost three workers, and the
alternative — tmux — is only unsafe because the runtime has not been rebuilt.
Whichever way a manager turns today, one of the two failure modes is live.

WHAT A FIX HAS TO COVER, beyond the original scope:
- rmux dying must not be silent. It currently writes nothing anywhere.
- a driver-release failure across MULTIPLE runs at once should be recognisable as
  a transport-level event rather than reported three times as unrelated run
  failures.
- the orphan flagging is correct but the recovery is not: three workers with live
  work were left with no route to finalize, and the manager had to salvage two
  worktrees by hand.
- consider whether the daemon should refuse to keep dispatching onto a transport
  whose endpoint it has just failed to reach.

* BACKLOG TASK-QSX56 Cursor ACP must authorize scoped project workflow commands and finalize :drivers:cursor:acp:hardening:
:PROPERTIES:
:ID: TASK-QSX56
:PRIORITY: high
:RELATES_TO: TASK-2YZDJ
:END:

A real non-simulated cursor-agent/acp-stdio TASK-2YZDJ run reached native grok-4.5[effort=high,fast=true], read project files, and emitted tool envelopes, but the adapter returned reject_once for the worker shell probe `which orgasmic; orgasmic entry; echo "EXIT_CODE=$?"`. The request payload had nested toolCall.kind=execute and only title/content, with no structured input; current shell_command_allowed accepts only a narrow git/grep/find grammar and cannot authorize the mandatory `orgasmic entry` / `orgasmic dispatch finalize` workflow. Implement a bounded, injection-resistant authorization path for the exact project workflow commands needed by a Cursor worker, using the nested ACP payload actually emitted. Preserve worktree/path confinement and fail closed for unsafe shell controls/unknown commands. Add fixture regressions for the captured payload, permitted entry/finalize commands, and unsafe compound/foreign commands. Prove a real Cursor canary can run entry and finalize. Do not widen into TASK-2YZDJ feature implementation.
** Evidence
Codex gpt-5.6-terra/high implemented the bounded title-only Cursor ACP permission path in 8e3854e. The captured nested payload remains rejected as a compound command; standalone `orgasmic entry` and worker-bound `orgasmic dispatch finalize` are authorized, while shell control, foreign/destructive commands, task/run mismatches, and path escapes fail closed. Worker gates: drivers lib 208 pass, driver clippy/fmt/diff-check green, live legacy Cursor bridge green. Manager independently reran both new focused regressions, fmt, and diff-check before landing. Merged as aa4bb55; rebuilt source daemon boot 43ee051a-2ee7-476c-86aa-98d0dcbb484b is serving the patch. Live ACP workflow proof and independent Claude review remain.
Independent review is not yet complete. First rmux/Opus attempt never reached code because a stale inherited ANTHROPIC_AUTH_TOKEN caused 401; stored claude.ai auth was healthy. After restarting without provider auth overrides, the single retry reached real Claude Opus 4.8/high and inspected the security-sensitive routing, but rmux closed the pane-output-cursor transport before a verdict. Partial transcript identified no bypass before interruption, but it is not a SHIP verdict. TASK-4DTBZ tracks the rmux reliability defect; TASK-QSX56 stays IN_REVIEW.

* BACKLOG TASK-CQM2X Crash-safe run inventory, bounded history, and manual retro foundation    :daemon:recovery:retro:plan:
:PROPERTIES:
:ID: TASK-CQM2X
:IMPLEMENTS: arch_RN73Z
:DEPENDS_ON: TASK-KWSTJ TASK-FZB6T TASK-VBSG2 TASK-5P60H TASK-3R3EZ TASK-TJKFC
:END:

Umbrella plan for the 2026-07-23/25 incident family. The live 0.0.14 daemon reproduces authenticated `GET /api/runs` with no headers or body before a 12-second timeout; unauthenticated 401 and other routes return immediately. The purported complete output file in the follow-up report is not evidence of response framing because curl reported zero downloaded bytes and a fresh output path remained absent. Source inspection shows inventory reconstructs classifications from every historical session JSONL, performs driver attach work, revalidates missing worktrees, and historically rebuilt a whole-board recovery-origin index per failed tombstone.

Measured production evidence: 197 orgasmic session files occupy 2,404,385,576 bytes (2.239 GiB); the largest is 114.7 MB. Legacy Claude/rmux files contain thousands of rendered TUI redraw chunks. `/api/runs` polling also causes repeated missing `ddd/.orgasmic/project.org` warnings. Current source already contains uncommitted TASK-JN5Q1 work for bounded attach probes and exact-run close/finalize; preserve and verify it rather than replacing it.

Execute the child tasks independently in priority order. P0 restores and proves the control plane. P1 separates current lifecycle inventory from historical evidence and bounds retained event volume. Service and registry hardening follow without blocking P0. Manual retro materialization is a separate explicit feature built on the bounded evidence architecture; it is not required for `/api/runs` to work and must not become an automatic startup/completion hook.

Done only when the production-shaped wire regression completes inside its deadline, exact-run close/finalize do not enumerate history, run enumeration cost is independent of historical transcript bytes, default storage cannot recreate rendered-TUI growth, stale/missing records stop producing recurring warnings, and an explicit multi-run retro can analyze selected Claude sessions through derived bounded evidence.

* BACKLOG TASK-VBSG2 Add explicit Claude evidence materialization and manual multi-run retro dispatch    :retro:claude:evidence:
:PROPERTIES:
:DEPENDS_ON: TASK-FZB6T
:ID: TASK-VBSG2
:IMPLEMENTS: arch_RN73Z
:PRIORITY: P1
:END:

Implement the smallest end-to-end slice of arch_RN73Z after operational run storage is bounded. This is an explicit diagnostic feature, not part of normal dispatch, completion, startup, recovery, or run listing.

Slice A — materializer:
- expose `orgasmic run evidence materialize --run <id>` and one equivalent daemon/agent-callable function
- reuse verified NativeRuntime provider/session/path metadata and the existing native-transcript resolver; fail closed when correlation is missing or ambiguous
- parse Claude native JSONL through a Claude-local adapter into the existing DriverEvent/activity vocabulary, including tool_use, tool_result, timestamps, token/context fields when present, and terminal signals
- write an idempotent, versioned `derived_native` cache keyed by source digest + converter version under `.orgasmic/tmp/retro/evidence`; enforce per-event/per-run limits and return a compact summary + reference, never the full transcript
- prove derived events cannot enter supervisor recovery/liveness/finalization paths

Slice B — manual retro:
- add `orgasmic manager retro` accepting repeated run ids, task ids, or an explicit ordered task sequence
- resolve those selectors once into an immutable scope manifest with run ordering, task/dispatch links, source digests, missing evidence, and requested questions
- dispatch a read-only retrospector that starts from catalog summaries, lazily calls the materializer for only needed runs, and reports evidence-linked bottlenecks: repeated equivalent tool calls, retries/failures, tool latency, idle/auth/permission waits, redundant reads, context resets, circular reasoning, output volume, token use, and progress per wall time/token
- restrict writes to the derived evidence directory and report artifact; no task transitions, cleanup, lease/recovery action, dispatch closure, source/vendor mutation, or code edits

Use a backlog-to-done task with multiple implementer/reviewer/recovery runs as the integration fixture. Assert a second materialization is a cache hit, a source change creates a new digest entry, an ambiguous transcript is refused, and a retro spanning several runs produces cross-run findings without injecting raw transcript volume. Routine scheduling is explicitly deferred: a future routine must call this same manager path and be user configured; do not add an automatic trigger here.
** Notes
Blocked in practice for the acp-stdio transport by TASK-VB9DQ. Measured 2026-07-25: the acp-stdio claude adapter passes `--no-session-persistence` and writes zero native transcripts (0 new files under `~/.claude/projects` versus 1 for an otherwise identical run), never passes `--session-id`, and never emits a `NativeRuntime` lifecycle event. Slice A's materializer resolves verified NativeRuntime metadata and parses Claude native JSONL, so for acp-stdio runs there is nothing to resolve and nothing to parse. The rmux/tmux transport is unaffected — it mints an explicit session id and records launch/resume argv. Land TASK-VB9DQ first, or scope this task to TUI-transport runs and say so explicitly.

* BACKLOG TASK-3R3EZ Quarantine missing project/worktree records, warn once, and add project remove    :daemon:project:recovery:
:PROPERTIES:
:DEPENDS_ON: TASK-FZB6T
:ID: TASK-3R3EZ
:PRIORITY: P2
:END:

Fix the repeated missing `ddd/.orgasmic/project.org` warning at its actual source: run-inventory polling revalidates historical RunMeta worktrees and logs on every request. Also complete board lifecycle so an operator can intentionally deregister a project.

Behavior:
1. The run catalog stores worktree/project authority as verified, missing, invalid, or unavailable with the observation time and file identity. Missing/pruned worktrees for pid-gone terminal history become stable tombstoned metadata and are not attach candidates. Inventory reads do not touch or warn about the same missing path repeatedly.
2. A registered project whose root or `.orgasmic/project.org` disappears transitions to `unavailable`; emit one structured warning/event per transition, retain last-good read state where safe, stop project refresh/repo-url/session scans, and unwatch paths that still have a watch. Reappearance triggers revalidation and a recovery event. Do not silently deregister on one ENOENT because removable volumes and temporary renames are valid.
3. Add `orgasmic project remove <id>` through the daemon writer. It removes only the board registration/watch/index entry, never the repository or its `.orgasmic` files. Default output previews the target identity/path; support idempotency and require an explicit confirmation or force flag in noninteractive use. Refuse when live runs/leases still belong to the project unless an explicit safe cleanup flow resolves them first.
4. Board refresh must diff added and removed entries: start watches/load added projects, unwatch/drop removed projects, and never leave stale index entries.

Regression: poll `/api/runs` repeatedly with several records pointing at one pruned worktree and assert zero repeated filesystem reads/warnings after the first classification; remove a board project and assert its watch/index vanish while files remain; simulate temporary disappearance/reappearance and assert one unavailable and one recovered event. Measure warning volume, not only return values.

* BACKLOG TASK-CM5KV Evaluate real ACP as an orgasmic transport    :drivers:research:
:PROPERTIES:
:ID: TASK-CM5KV
:PRIORITY: P2
:DEPENDS_ON: TASK-XCJYC
:END:

Evaluate adding the Agent Client Protocol as a real orgasmic transport, once TASK-XCJYC frees the name.

ACP is an open standard from Zed Industries (August 2025, Apache-licensed): JSON-RPC 2.0 over stdio for local agents, HTTP/WebSocket for remote (work in progress). It standardises the editor-to-agent boundary; MCP standardises tool access beneath it. Adopted by JetBrains, Google and GitHub, with 25+ agents.

The draw is consolidation. Zed's ACP Registry already lists Claude Agent, Codex, Gemini CLI, OpenCode, Copilot, Cursor and Pi Coding Agent. Orgasmic currently maintains a bespoke adapter per harness — `adapters/claude.rs` (stream-json), `adapters/codex.rs` (app-server), `adapters/cursor_acp.rs`, `adapters/hermes.rs` — each with its own event mapping, session identity story and transcript-finder strategy. One ACP transport could in principle serve several of them through a single event mapping.

Open questions to answer before committing to it:
1. Does ACP expose what orgasmic's supervisor actually needs — terminal declaration, tool-call visibility, stall signals, and a resumable native session identity? A protocol built for editor UX may not carry the lifecycle facts recovery depends on.
2. Does an ACP-launched agent still write its vendor-native transcript, or does the adapter layer hide it? The retro contract depends on vendor JSONL existing and being resumable (see dec on transcript ownership).
3. Is the ACP adapter an extra process between orgasmic and the harness, and what does that mean for the startup preflight (TASK-TJKFC) and for credential handling (TASK-Z8WEJ)?
4. Would it replace the bespoke adapters or sit alongside them? A fifth transport that duplicates four existing ones is a cost, not a saving.

This is deliberately a research-and-decide task, not an implementation commitment. If the answer is that ACP does not carry orgasmic's lifecycle needs, recording that is a complete and useful result.

Depends on TASK-XCJYC only for the name; the evaluation can start earlier.

* BACKLOG TASK-5M45A Hermes ignores the provider and model a dispatch asks for    :P1:
:PROPERTIES:
:ID: TASK-5M45A
:END:

Measured 2026-07-25 on hermes 0.16.0, driving `hermes acp` directly with the
same JSON-RPC handshake `acp_stdio` performs.

On the hermes stdio path orgasmic transmits the dispatch's provider and model
only inside `thread_start._meta.orgasmic` (`spawn_payload`, hermes.rs), and the
argv is a bare `hermes acp`. Hermes does not read them. Two `session/new`
calls, identical but for the provider:

  provider = "anthropic"             -> currentModelId ollama-cloud:glm-5.2
  provider = "nonexistent-provider"  -> currentModelId ollama-cloud:glm-5.2

Neither errored. Both landed on hermes's own configured default. An unknown
provider id is not even rejected, so there is no signal that the request was
dropped.

Consequences, in order of how much they cost:
1. A dispatch that specifies a provider silently runs on a different one. The
   operator is billed to the wrong account and gets a different model's output,
   with nothing in the session to say so.
2. It is worse than invisible, it is actively misreported: the `Ready` event's
   capabilities echo `cfg.provider` and `cfg.model` back
   (`on_ws_thread_started`), so the session JSONL records the provider that was
   ASKED FOR as though it were the one in use. Reading the session is the normal
   way to find out what a run did, and here it lies.
3. It blocks a hermes readiness probe. Checking a credential requires knowing
   which credential the launch consumes; while the provider is ignored, orgasmic
   cannot know. See the comment on `HermesAdapter` (TASK-TJKFC decided against a
   probe on exactly this ground).

Not yet established: whether hermes exposes any supported way to select a
provider per ACP session (a `session/set_config_option` equivalent, an argv
flag, or an env var). The adapter already builds a `/model ... --provider ...`
slash command in `transition_state`, so mid-run switching works; whether that
same path can be driven at startup was not tested. The fix might be as small as
issuing that command once after `session/new`.

Minimum acceptable outcome if selection turns out to be impossible: stop echoing
a provider and model the run is not using, so the session records what actually
happened rather than what was requested.

* BACKLOG TASK-Y9BY6 live_rmux_exit_139_emits_a_fatal_terminal_event times out on every run    :testing:drivers:flake:
:PROPERTIES:
:ID: TASK-Y9BY6
:PRIORITY: P2
:END:

`modes::rmux::tests::live_rmux_exit_139_emits_a_fatal_terminal_event` fails
deterministically with `timed out waiting for rmux exit 139 event: Elapsed(())`
(rmux.rs:2871).

Measured 2026-07-26: 3/3 failures on a working tree carrying the TASK-SGRTX
changes, and 2/2 on stashed HEAD without them. So it is pre-existing and
unrelated to that work — recorded here so the next person does not re-derive
that, as happened this morning with the dispatch_endpoint three.

Not investigated. `rmux` IS installed (`~/.local/bin/rmux`) and an rmux-daemon
is running under the orgasmic LaunchAgent config, so "binary missing" is not the
explanation. Candidates worth checking first:
- the test drives the DEFAULT rmux socket, which the running LaunchAgent daemon
  may already own with different config (see the socket-split note in the
  isolated-smoke recipe)
- exit 139 is SIGSEGV; whether the stub actually segfaults on this machine and
  rmux reports it as 139 is unverified

Belongs with TASK-STWVB's theme: the drivers suite cannot answer "did my change
break something?" while one test fails every run for an unrelated reason.
CORRECTION [2026-07-26], same session. This task's opening claim that the test
"fails deterministically" is wrong.

It failed 5/5 when filed (3/3 on the working tree, 2/2 on stashed HEAD). Roughly
ninety minutes later the same test passes, and the full drivers lib suite is
231/231 green on 3 consecutive runs. Nothing in the test or in rmux changed
between those measurements.

What did change is machine state. Between the two, an isolated smoke daemon was
started and stopped twice on port 4899, rmux servers were killed during cleanup,
and a dispatched worker was running full test suites in a worktree. So the
trigger is environmental and shared — most likely the default rmux socket
ownership described in the isolated-smoke recipe — not the test's own logic.

Two other tests were also seen failing exactly once in that window and passing
on every run since:
- adapters::claude::tests::acp_stdio_accepts_a_dispatch_for_a_logged_in_claude
- adapters::claude::tests::acp_stdio_rejects_a_dispatch_for_a_logged_out_claude
- adapters::claude::tests::an_empty_endpoint_still_gets_a_verdict_because_it_still_spawns_claude

Those three manipulate process-global PATH and HOME to simulate a logged-out
harness, which is the documented shared-environment hazard in gotchas
("Tests that set PATH break every other test in the binary"). They are the
drivers-crate instance of TASK-STWVB's disease.

So re-scope: this is not one deterministic rmux failure. It is that the drivers
suite has environment-coupled tests whose result depends on what else is running
on the machine, which is the same reason the daemon suite could not answer "did
my change break something?". Reproduce by running the suite WHILE a second
daemon or an rmux server is live, not by running it alone.

* BACKLOG TASK-099T6 driver_modes equivalence test spawns real claude twice on one session id and now collides    :drivers:test:claude:bug:
:PROPERTIES:
:ID: TASK-099T6
:priority: P1
:PRIORITY: P1
:END:

Found 2026-07-26 running the TASK-8W30B merge gates on main (433fef1 + merge).

SYMPTOM

`cargo test -p orgasmic-drivers --test driver_modes` fails:

    ---- legacy_drivers_and_explicit_pairs_emit_equivalent_start_events ----
    assertion `left == right` failed: legacy=claude-acp mode=acp-stdio harness=claude
      left:  Ready { protocol_version: "claude-code-stream-json/1", ... }
      right: TextChunk { stream: Stderr,
             chunk: "Error: Session ID 6fc90d23-2093-4957-9fa9-ccb9488e0bb1 is already in use." }

CAUSE

The test deliberately shares one identity across the two spawns it compares
(driver_modes.rs:1665-1684, comment: "Share one identity so harness-aware native
metadata (e.g. the tmux claude --session-id pinned to runtime_id) is identical
across the legacy and explicit drivers being compared"). That was harmless while
`--session-id` was computed and then dropped. TASK-SGRTX (f5c6913) and 0f65de5
made the flag actually reach the claude process, so the second spawn of each pair
now asks claude for a session id the first spawn just claimed.

This is fallout from making the claude driver real, not from TASK-8W30B. The
8W30B implementer saw this suite green (28/28) in its worktree and the manager
saw 27/1 on the merged tree an hour later, so it presents as intermittent — the
collision depends on whether the first spawn has registered the session by the
time the second starts. Treat it as a race that is always latent, not as a test
that sometimes breaks.

TWO SEPARATE PROBLEMS, BOTH WORTH FIXING

1. The equivalence assertion needs identical *metadata* across the pair, not an
   identical live session. Give each spawn its own session id and normalize the
   id out of the compared Ready event, the way `normalize_volatile_ready_event`
   already removes `session_id` at line 1724 — or make the case inert.

2. This test spawns a REAL `claude` process, twice per case. Per the gotcha
   "Asking a harness to prove it can work costs real money", a claude turn cost
   $0.0994 and 24.5k tokens when measured 2026-07-25. A default-suite test that
   bills the operator on every run is a problem independent of the collision, and
   it got worse the moment the driver stopped being inert. Decide whether this
   case belongs behind an opt-in gate.

Acceptance:
- `cargo test -p orgasmic-drivers --test driver_modes` is green on repeated
  consecutive runs, not once
- the claude case either does not spawn a billable real turn by default, or its
  cost is a deliberate, documented choice
- the equivalence property being asserted is still actually asserted
2026-07-27, TASK-RWCRN dispatch: this test is now actively costing money on ordinary gate runs, not just on deliberate invocation. The brief explicitly told the worker not to run it; the worker ran it once anyway and was right to flag why — `cargo test -p orgasmic-drivers` is a declared verification gate and this test is not #[ignore]d, so the gate as written cannot avoid it. It failed on a real claude spawn with "Error: Session ID 377794d3-... is already in use", the documented TASK-099T6 collision, made more likely because the suite ran inside a live claude dispatch. Until this is #[ignore]d, every brief must carry a --skip instruction and every gate run must use `-- --skip legacy_drivers_and_explicit_pairs_emit_equivalent_start_events`, which is exactly the kind of tribal knowledge that does not survive. The manager's own gate run used the skip and was green: 239/4/28/1.

* BACKLOG TASK-A55J7 Persist model and effort on the run record and the dispatch tx    :dispatch:runtime:observability:
:PROPERTIES:
:ID: TASK-A55J7
:priority: P1
:PRIORITY: P1
:END:

Filed 2026-07-26 alongside a manager prompt change requiring every dispatch to
state driver, harness, model and effort. That rule is currently unenforceable
after the fact, because two of the four are never written down.

WHAT IS RECORDED TODAY

`manager.dispatch_started` carries `:WORKER:`, `:DRIVER:`, `:HARNESS:` and no
model or effort (verified on tx-20260726-orgasmic-2861 and -2869).
`orgasmic run list` reports `driver`, `harness`, `worker_id`, `kind`, `role`,
`sub_state` — and no model or effort field on the live run object.

`orgasmic manager dispatch` accepts `--model` and `--effort` and passes them
through to the adapter, so the values exist at dispatch time. They are simply
dropped before anything durable is written. A manager who sets them cannot prove
it afterwards, and a manager who omits them cannot discover what the harness
chose instead.

WHY IT MATTERS

Two failures on 2026-07-26 came from exactly this blind spot. A reviewer was
dispatched over a P1 concurrency fix and the manager could not afterwards say
what model was reviewing it. Separately, a TASK-STWVB implementer ran on
claude-opus-5 when nothing had selected it, discovered only by reading argv on a
live process (TASK-SGRTX).

The cost is not tidiness. Worker output is treated as a claim to be weighed, and
weighing a claim requires knowing its author. An evidence section that says "the
implementer measured +5 sessions per run" means something different depending on
what produced it, and today that is unrecoverable the moment the process exits.

WORK

1. Thread the resolved model and effort — the values actually sent to the
   adapter, not the ones requested — onto RunMeta, so they survive boot reattach
   the way last_path/stdout_path/worktree do.
2. Emit them on `manager.dispatch_started` as `:MODEL:` / `:EFFORT:`, and on
   `run.created`.
3. Surface them in `orgasmic run list` and `orgasmic manager dispatch-status`.
4. Record what the harness actually resolved when the manager passed nothing.
   The default is the case most worth capturing — it is the one nobody chose.
   Where the harness does not report its own resolved model, record the request
   as absent rather than inventing a value, and say so in the field.
5. Show them wherever a run is displayed in the UI Runs surface.

Note the task node already carries `model` and `reasoning_effort` properties
(both null on most headings). Decide whether those are the requested values and
the new run fields are the resolved ones, or whether the task properties should
be retired in favour of run-level truth. Two half-populated surfaces for the same
fact is how this became invisible in the first place.

Acceptance:
- a completed dispatch can be asked what model and effort ran it, from state
  alone, after the process is gone
- a dispatch where the manager passed neither still records what was resolved,
  or records explicitly that nothing was requested and the harness default
  applied
- `orgasmic run list` and `dispatch-status` both show all four of driver,
  harness, model, effort

=== WIDENED 2026-07-29 (operator directive) — enforcement, not reporting

The 2026-07-28/29 session dispatched FZB6T, JQ8AV and 95SGV believing they ran
claude-opus-5; they ran claude-fable-5, inherited from the ambient harness
default because no --model was passed. The commit trailers
(=Co-Authored-By: Claude Fable 5=) are the only surviving proof, and they are an
accident of the harness, not a record orgasmic kept. This is the reporting gap
above, but the fix is to make the state impossible rather than merely visible.

ADD to the work items:

6. REFUSE TO DISPATCH WITH AN UNRESOLVED MODEL. If neither --model nor the task
   drawer supplies one, the dispatch fails loudly and writes nothing. Silent
   inheritance of whatever the operator shell/settings happen to say is the
   defect; a hard failure at dispatch time is the fix. Same for --effort.

7. NO PROJECT-LEVEL DEFAULT MODEL. Explicitly ruled out by the operator
   2026-07-29: models and operator preferences both change faster than a
   checked-in config key would track, and a stale default is the same silent
   inheritance wearing a different hat. The resolution order is exactly
   --model > task drawer > REFUSE. Do not add a config key for this; if a
   future reader is tempted, this paragraph is the rationale for why not.

8. The session-start ask is the ergonomic counterpart, and it belongs to
   TASK-0JY7M ("ask once per session for agent selection"), not here. A55J7
   makes the unresolved state impossible; 0JY7M makes supplying it convenient.
   Neither substitutes for the other — ship A55J7 even if 0JY7M slips.

Acceptance ADDS:
- a dispatch with no --model and no task-drawer model exits nonzero, names the
  missing field, and leaves no run record, no worktree and no lease behind
- the refusal is proven by a shipped injection artifact under verify/, like any
  other correctness claim on this project

* BACKLOG TASK-Z9C73 a released rmux run leaves one zombie harness child under the rmux daemon    :rmux:drivers:defect:
:PROPERTIES:
:ID: TASK-Z9C73
:PRIORITY: P2
:DEPENDS_ON: 
:END:

Split out of TASK-RWCRN, whose acceptance included "a released rmux run does not leave a zombie harness process or an orphaned rmux session behind". The session half is met — `rmux ls` is empty after release. The process half is NOT, and the TASK-RWCRN worker measured it with a proper baseline control rather than asserting it.

MEASURED. One live acquire + release leaves exactly one <defunct> child under the rmux-daemon (pid 8920 at time of measurement). Zombie count under that ppid went 3 -> 4 across one release_reaps_live_rmux_session run on the TASK-RWCRN tree, and 4 -> 5 across the same test on the stashed baseline tree (git stash -> run -> git stash pop). Equal deltas on both trees, so the leak is pre-existing and TASK-RWCRN neither caused nor fixed it.

MECHANISM, named at crates/orgasmic-drivers/src/modes/rmux.rs:1094: panes are spawned with keep_alive_on_exit(true) deliberately, because RMUX 0.9 retains a real exit code/signal only for a dead pane it keeps around. The exited child therefore stays unreaped by the rmux daemon, a process orgasmic does not own. So this is not a missing waitpid in orgasmic — any fix has to either negotiate reaping with the rmux daemon, drop keep_alive_on_exit and give up the exit code/signal fidelity that TASK-6FNAY and TASK-8W30B depend on, or accept and bound the leak.

WHY IT MATTERS. One zombie per dispatch is small, but it accumulates across the lifetime of a long-lived rmux daemon, and the standing worker matrix puts every dispatch in an rmux pane. Bounded growth needs to be either proven acceptable or fixed.

ACCEPTANCE:
- name which of the three options is taken, with the tradeoff argued
- if the leak is retained, bound it and prove the bound
- the exit code/signal fidelity relied on by TASK-6FNAY and TASK-8W30B is preserved or its loss is explicitly accepted

* BACKLOG TASK-Y5M0H rmux pane-liveness stream source has no fast regression guard    :rmux:drivers:test-gap:
:PROPERTIES:
:ID: TASK-Y5M0H
:PRIORITY: P2
:END:

Reviewer residual on TASK-RWCRN.1 (run-20260728T045548-5e2ae042c2844e039a98edc26e05a739, verification note 6, SHIP with no blocking findings).

The throttle unit tests at crates/orgasmic-drivers/src/modes/rmux.rs:2788-2880 call PaneActivityThrottle directly, so they would all still pass if the production wiring regressed from PaneOutputStream back to PaneLineStream. The only test that catches that regression is the #[ignore]d live smoke live_rmux_pane_publishes_pane_activity_for_newline_free_redraws at rmux.rs:3381-3409, which does not run in CI.

So the exact defect TASK-RWCRN.1 fixed could silently return while every non-ignored test stays green.

WANTED: a fast, non-ignored test that pins the stream source itself - either asserting spawn_pane_exit_watch subscribes via output_stream_starting_at rather than line_stream_starting_at, or a seam that lets a fake pane transport feed newline-free bytes through the real wiring without a live rmux server.

Not a ship blocker and not urgent: it is insurance on a fix that is already validated live (the TASK-WGXKD dispatch ran claude-on-rmux 1675s with 55 PaneActivity bytes events).

* BACKLOG TASK-F9VEZ codex records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:codex:sessions:evidence:
:PROPERTIES:
:ID: TASK-F9VEZ
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. See that matrix for the measurements this rests on.

`CodexAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`). Verified 2026-07-28: `native_runtime` is implemented in exactly two places in the tree — `adapters/claude.rs:168` and the delegating `modes/acp_stdio.rs:111`.

Codex mints its own identity server-side: `thread.id` is read out of the `thread/start` response in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/codex.rs:198-206`) and surfaced only inside `Ready.capabilities` as `thread_id` (`:225`). `thread_start_params` (`:864-885`) has no id field, so orgasmic cannot pre-mint one the way `--session-id` lets it for claude.

Two consequences, both live:
1. `resolve_recovery_actions` cannot offer `resume_native_fork` for a codex run — there is no validated NativeRuntime session id to resume.
2. dec_Y5MPK item 6 (worker/operator attribution) is not computable for codex. 1457 rollout JSONL files sit under `~/.codex/sessions` on the operator machine (measured 2026-07-28) and none of them can be attributed to an orgasmic dispatch, because attribution is defined as "its session id appears in a NativeRuntime record".

Work:
1. Emit `NativeRuntimeMeta` from the handshake response rather than from launch: provider `codex`, session id = `thread.id`, `session_path` = the rollout file the transcript finder already knows how to locate, and a `resume_argv`/resume params that codex actually accepts. Establish what codex resume looks like before designing the shape — `thread_id` currently flows only into turn params (`:326-410`), and there is no resume call in the adapter today.
2. Confirm the mode carries a NativeRuntime that arrives *after* compose. `modes/acp_stdio.rs` reads `adapter.native_runtime()` immediately after composing the request, which is too early for a value the handshake produces. This is the real blocker and it is shared with TASK-B6D8W (cursor-agent) and TASK-2B215 (hermes).

See also TASK-ZY259: `thread_start_params` also sends `persistExtendedHistory: false`, which is a separate dec_Y5MPK item 4 question and should be settled first since it touches the same call.

Blocked on a contract decision recorded against TASK-SGRTX: whether the cross-harness session contract permits post-launch handshake capture followed by a `NativeRuntime` event, or requires pre-launch pinning. Do not answer it here with a codex-specific patch.

* BACKLOG TASK-B6D8W cursor-agent records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:cursor:sessions:evidence:
:PROPERTIES:
:ID: TASK-B6D8W
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX TASK-F9VEZ
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. Sibling of TASK-F9VEZ (codex); same shape, different protocol.

`CursorAcpAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`).

The session id is server-minted and post-launch: `sessionId` is read out of the `session/new` result in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/cursor_acp.rs:288-292`) and surfaced only inside `Ready.capabilities` (`:320`). Unlike codex this is not an implementation choice orgasmic could lobby to change: ACP defines `session/new` as the agent minting the session id, so a pre-launch pin in the claude `--session-id` sense has no protocol slot at all.

Consequences: no `resume_native_fork` for cursor-agent runs, and dec_Y5MPK item 6 attribution is not computable — 909 JSONL files under `~/.cursor` on the operator machine (measured 2026-07-28), none attributable to an orgasmic dispatch.

The transcript finder already handles the fallback: recorded path confined to `~/.cursor/projects`, else Ready `session_id` + worktree at **high** confidence, else cwd+launch-time at **medium** (`crates/orgasmic-drivers/src/transcript_finder.rs`). So locating a transcript mostly works; what is missing is the recorded NativeRuntime that makes it deterministic and makes attribution possible.

Work: emit `NativeRuntimeMeta` from the `session/new` result — provider `cursor-agent`, session id, resolved session path, and whatever ACP `session/load` (or the cursor equivalent) needs as resume argv. Verify against the binary rather than the spec; `session/new` already returns an undocumented structured models block (`:295`), so the shipped surface is wider than the published one.

Blocked on the same contract decision as TASK-F9VEZ, recorded against TASK-SGRTX: post-launch handshake capture versus mandatory pre-launch pinning. Cursor is the strongest evidence that pre-launch pinning cannot be made universal.

* BACKLOG TASK-2B215 hermes records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:hermes:sessions:evidence:
:PROPERTIES:
:ID: TASK-2B215
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX TASK-F9VEZ TASK-B6D8W
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. Sibling of TASK-F9VEZ (codex) and TASK-B6D8W (cursor-agent).

`HermesAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`).

The session id is server-minted and post-launch: `sessionId` is read out of the `session/new` result in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/hermes.rs:287-292`, method name at `:327`) and surfaced only inside `Ready.capabilities`. As with cursor-agent, ACP gives no slot for a client-supplied session id.

Consequences: no `resume_native_fork` for hermes runs, and dec_Y5MPK item 6 attribution is not computable — 82 JSONL files under `~/.hermes/sessions` on the operator machine (measured 2026-07-28), none attributable to an orgasmic dispatch. The absolute number is small; the property is the same.

The transcript finder already resolves `~/.hermes/sessions/<id>.jsonl` from the Ready `session_id` at **high** confidence, and notes the paired `session_<id>.json` is metadata only (`crates/orgasmic-drivers/src/transcript_finder.rs`). Emitting NativeRuntime turns that into a recorded path rather than a recomputed one.

Work: emit `NativeRuntimeMeta` from the `session/new` result — provider `hermes`, session id, `~/.hermes/sessions/<id>.jsonl`, and a resume argv verified against the binary. Hermes is the cheapest of the three to prove because its path rule is the simplest.

Blocked on the same contract decision as its siblings, recorded against TASK-SGRTX.

* BACKLOG TASK-ZY259 codex thread/start sends persistExtendedHistory:false — decide whether that is a dec_Y5MPK item 4 violation    :drivers:codex:sessions:evidence:
:PROPERTIES:
:ID: TASK-ZY259
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-H0TYV TASK-F9VEZ
:END:

Filed by TASK-H0TYV (VB9DQ fix round). Small, bounded, and worth doing before TASK-F9VEZ.

dec_Y5MPK item 4: "Orgasmic never suppresses vendor persistence. A transport that disables the harness's own session recording is a defect, not a configuration choice." TASK-VB9DQ removed claude's `--no-session-persistence` on exactly that ground.

`thread_start_params` sends `persistExtendedHistory: false` to codex app-server on every run (`crates/orgasmic-drivers/src/adapters/codex.rs:883`). It is the only occurrence in the tree, it carries no comment, and no test asserts anything about it. It is set to `false` — the suppressing value — and nothing records why.

What is known: codex still writes rollout JSONL with this flag set. A codex run that died on a provider 503 wrote `~/.codex/sessions/2026/07/25/rollout-....jsonl` (TASK-VB9DQ evidence), and 1457 rollout files exist under `~/.codex/sessions` on the operator machine (measured 2026-07-28). So it is not the blanket kill switch `--no-session-persistence` was.

What is not known: what "extended history" actually is, and whether anything retro or resume would need is missing from those rollouts because of it.

Work:
1. Determine what codex app-server does with `persistExtendedHistory` — read the shipped app-server, do not infer from the name.
2. If it removes content a retro run or a resume would want, remove the flag and say so under dec_Y5MPK item 4, exactly as claude's flag was removed.
3. If it is genuinely orthogonal to session persistence, leave it and add the one-line comment that its absence is why this task had to be filed.

Either way the outcome is a recorded answer. The defect being fixed here is that a persistence-shaped flag sits in the launch path with no stated reason.

* BACKLOG TASK-E3K1B doctor reports no vendor-transcript inventory, so dec_Y5MPK's operator-owned retention has no numbers to act on    :doctor:sessions:evidence:
:PROPERTIES:
:ID: TASK-E3K1B
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-H0TYV TASK-F9VEZ TASK-B6D8W TASK-2B215
:END:

Filed by TASK-H0TYV (VB9DQ fix round). dec_Y5MPK names `doctor` as the owner of two obligations and neither exists in code.

Item 5 (2026-07-25): orgasmic never prunes a vendor transcript, and "its obligation is visibility: report per-harness counts, total size, and age distribution through `doctor` so the operator can act with full information."

Item 6 (added 2026-07-28 by TASK-H0TYV): that per-harness report splits into orgasmic-attributed and unattributed, where a transcript is orgasmic-attributed exactly when its native session id appears in a `NativeRuntime` lifecycle record in orgasmic's own session JSONL.

Neither has been filed until now, and `doctor` reports nothing about vendor stores. The decision assigns retention to the operator on the explicit premise that orgasmic supplies the numbers; without this the operator owns a duty they have no instrument for. Measured on the operator machine 2026-07-28: 652 JSONL under `~/.claude/projects`, 1457 under `~/.codex/sessions`, 909 under `~/.cursor`, 82 under `~/.hermes/sessions`.

Work:
1. Per-harness inventory in `doctor`: file count, total size, age distribution. Roots are already defined and test-overrideable — reuse `TranscriptRoots::from_env_home` (`crates/orgasmic-drivers/src/transcript_finder.rs`) rather than hardcoding a second copy of the path rules.
2. Split each harness's numbers into orgasmic-attributed and unattributed by the item 6 rule. Read attribution from recorded `NativeRuntime` events; never from path shape, file age, or launch-time proximity — those are the discovery heuristics dec_Y5MPK item 3 forbids.
3. Report honestly where the split is not computable. Today only claude mints its own session id, so codex, cursor-agent and hermes are wholly unattributable until TASK-F9VEZ, TASK-B6D8W and TASK-2B215 land. `doctor` must say "attribution unavailable for this harness" and name the blocking task, not silently report 0 attributed — a zero that means "cannot tell" reads as "orgasmic wrote none of these", which is false and would misdirect a prune.
4. Report only. `doctor` never deletes, moves, or offers to delete a vendor file (item 5).

* BACKLOG TASK-4PPPE serve announces readiness before it can handle SIGTERM, so the graceful-shutdown test races itself
:PROPERTIES:
:ID: TASK-4PPPE
:PRIORITY: P2
:TAGS: cli,lifecycle,flake
:END:
** Description
Found by the TASK-JQARS worker while classifying its own gate failures, and
correctly excluded from that task's scope. It is a **test defect, not a
production defect** — the production path was probed directly and is sound.

=== Measured

`crates/orgasmic-cli/tests/daemon_lifecycle.rs`,
`sigterm_exits_through_graceful_shutdown...`.

- On the JQARS branch: **3 pass / 6 runs**.
- On the pristine baseline (`git stash -u`, same command): **2 pass / 3 runs**.

It fails on both, so it is pre-existing and unrelated to that change.

=== Mechanism

The test sends SIGTERM the instant `serve` prints "press Ctrl+C to stop".
`main.rs` prints that line **before** `wait_for_shutdown_signal()` registers the
handler, so a SIGTERM that arrives inside that window hits the default
disposition and kills the process instead of running the graceful path. The
test's own comment describes the window.

Direct probe of the built binary using the test's exact home, config and
process-group setup: `rc=0` in **3/3 with SIGTERM at 0 ms delay**, and **3/3
with a 300 ms delay**. The production sequence is fine; the test races its own
readiness signal.

=== Why it matters beyond the flake

The readiness line is a **lie about readiness** for one specific consumer: a
supervisor that treats "press Ctrl+C to stop" as "safe to signal" has the same
race. Nothing in the product does that today, but the plist and any future
service adapter are exactly the kind of consumer that would. Decide whether the
line should move after handler registration, or whether a separate, explicit
readiness signal is owed — and record which, because "the test is racy" and
"the readiness announcement precedes readiness" are different bugs with the same
symptom.

=== Ask

1. Make the readiness announcement follow handler registration, or introduce a
   readiness signal that does, and say which you chose and why.
2. Remove the race from the test rather than papering it with a sleep — a sleep
   restores green without restoring the guarantee.
3. Check the same ordering question for Ctrl+C and for the service-stop path
   ATAXN's marker protocol now depends on.

=== Acceptance

- The test passes 10/10 consecutive runs, and a run with SIGTERM at 0 ms delay
  still exits through graceful shutdown.
- Prove it by injection: restore the announcement to its current position, watch
  the test fail, move it back, watch it pass — with output.
- State whether any shipped consumer (plist, installer, docs) tells an operator
  or supervisor to signal on that line.

=== Non-goals

- Do not change the shutdown budgets or the ATAXN marker protocol.

* BACKLOG TASK-QCG6J the reattach fence regression asserts which refusal fired, so it goes red on a safe outcome under load
:PROPERTIES:
:ID: TASK-QCG6J
:PRIORITY: P2
:TAGS: daemon,tests,flake
:END:

Found by the manager's independent gate on TASK-AK6EM, 2026-07-28. **The fence
itself is correct** — this is about what its test asserts.

=== Observed

In a full `cargo test -p orgasmic-daemon --lib` under concurrent load:

```
---- api::tests::explicit_reattach_tmux_is_fenced_out_of_a_worktree_a_close_guard_holds ----
panicked at crates/orgasmic-daemon/src/api.rs:19229:
the refusal must name the cleanup reservation:
  recovery action reattach_tmux is not valid for run run-fence-d825d586...
```

The reattach **was refused**. It was refused by an earlier validity check rather
than by the cleanup fence, because under load the test's tmux session was not
live by the time the recovery action was evaluated. The test asserts *which*
refusal fired, so it goes red on a safe outcome.

Its sibling `boot_rehydration_is_fenced_out_of_a_worktree_a_close_guard_holds`
passed in the same run and its log shows the fence doing exactly its job:

```
boot reattach: skipped (mux session not live / not reattachable)
  run_id=run-fence-cc02bfb7... task_id=TASK-BOOT-FENCE
  error=dispatch cleanup in progress for task=TASK-BOOT-FENCE kind=Worker
```

So the P0 fix is sound and merged. This task is the test's precision.

=== Why it is worth fixing rather than tolerating

A P0 destructive-path fence whose regression goes red for a reason unrelated to
the fence trains the next reader to wave it through — which is precisely how a
real fence regression would get discounted. It also lands on the standing
known-flake list, which this project is actively trying to shrink (TASK-5HBST,
TASK-STWVB).

Note the same full run showed two more load-induced failures on the same axis —
`post_artifact_regenerate_hot_path_reuses_live_run_id` ("harness busy", 409) and
`poll_direct_child_pid_prefers_worker_server_over_generic_sibling` ("fake
cursor-agent did not start children"). AK6EM added three new tmux-shaped tests,
and the `LiveSessionGuard` flock serializes live sections across every test
binary, so the tmux contention window widened. That is context for TASK-STWVB,
not a separate defect.

=== Ask

1. Make the assertion robust to *which* refusal fires: the property under test
   is "a reattach into a guarded worktree does not become live". Assert that,
   and assert the reservation-named refusal only when the run is actually still
   reattachable — or make the fixture's tmux session reliably live for the
   duration, if that can be done without a sleep.
2. Do not weaken it into "some error happened". The test must still fail if the
   fence is removed; verify that by injection (`worktree: None` in the reattach
   admission was AK6EM's own injection and turned four tests red).
3. Say whether the sibling boot-rehydration and pending-plan fence tests have
   the same exposure and fix them together if so.

=== Acceptance

- 10 consecutive full `cargo test -p orgasmic-daemon --lib` runs with no failure
  from any of the three fence tests. Report the count.
- Removing the fence still turns them red, shown by injection with output.
- State plainly whether the fixture still depends on a live tmux session and, if
  so, what now guarantees it.

=== Non-goals

- Do not change the fence, the admission module, the close-guard persistence, or
  dec_NFZY2. All are merged and manager-verified.

RMUX TWIN [2026-07-29, manager salvage gate for TASK-MRJRK]: `api::tests::explicit_reattach_rmux_is_fenced_out_of_a_worktree_a_close_guard_holds` (api.rs:19622) — the rmux arm TASK-JGHNC added of this task's registered tmux test — failed with the same mode (reattach refused by an earlier validity check rather than the cleanup fence), green in isolation. When this task fixes the assertion, fix both arms (they share one body post-JGHNC); the registry entry for the tmux arm should either cover the twin or gain a sibling row owned here, per the DJ1WV only-the-owner-writes rule.

** Description
Found by the manager's independent gate on TASK-AK6EM, 2026-07-28. **The fence
itself is correct** — this is about what its test asserts.

=== Observed

In a full `cargo test -p orgasmic-daemon --lib` under concurrent load:

```
---- api::tests::explicit_reattach_tmux_is_fenced_out_of_a_worktree_a_close_guard_holds ----
panicked at crates/orgasmic-daemon/src/api.rs:19229:
the refusal must name the cleanup reservation:
  recovery action reattach_tmux is not valid for run run-fence-d825d586...
```

The reattach **was refused**. It was refused by an earlier validity check rather
than by the cleanup fence, because under load the test's tmux session was not
live by the time the recovery action was evaluated. The test asserts *which*
refusal fired, so it goes red on a safe outcome.

Its sibling `boot_rehydration_is_fenced_out_of_a_worktree_a_close_guard_holds`
passed in the same run and its log shows the fence doing exactly its job:

```
boot reattach: skipped (mux session not live / not reattachable)
  run_id=run-fence-cc02bfb7... task_id=TASK-BOOT-FENCE
  error=dispatch cleanup in progress for task=TASK-BOOT-FENCE kind=Worker
```

So the P0 fix is sound and merged. This task is the test's precision.

=== Why it is worth fixing rather than tolerating

A P0 destructive-path fence whose regression goes red for a reason unrelated to
the fence trains the next reader to wave it through — which is precisely how a
real fence regression would get discounted. It also lands on the standing
known-flake list, which this project is actively trying to shrink (TASK-5HBST,
TASK-STWVB).

Note the same full run showed two more load-induced failures on the same axis —
`post_artifact_regenerate_hot_path_reuses_live_run_id` ("harness busy", 409) and
`poll_direct_child_pid_prefers_worker_server_over_generic_sibling` ("fake
cursor-agent did not start children"). AK6EM added three new tmux-shaped tests,
and the `LiveSessionGuard` flock serializes live sections across every test
binary, so the tmux contention window widened. That is context for TASK-STWVB,
not a separate defect.

=== Ask

1. Make the assertion robust to *which* refusal fires: the property under test
   is "a reattach into a guarded worktree does not become live". Assert that,
   and assert the reservation-named refusal only when the run is actually still
   reattachable — or make the fixture's tmux session reliably live for the
   duration, if that can be done without a sleep.
2. Do not weaken it into "some error happened". The test must still fail if the
   fence is removed; verify that by injection (`worktree: None` in the reattach
   admission was AK6EM's own injection and turned four tests red).
3. Say whether the sibling boot-rehydration and pending-plan fence tests have
   the same exposure and fix them together if so.

=== Acceptance

- 10 consecutive full `cargo test -p orgasmic-daemon --lib` runs with no failure
  from any of the three fence tests. Report the count.
- Removing the fence still turns them red, shown by injection with output.
- State plainly whether the fixture still depends on a live tmux session and, if
  so, what now guarantees it.

=== Non-goals

- Do not change the fence, the admission module, the close-guard persistence, or
  dec_NFZY2. All are merged and manager-verified.

* BACKLOG TASK-DTCBP orgasmic verify: --all, the verify_artifact drawer property, and the dispatch-close gating decision    :cli:verification:mechanism:
:PROPERTIES:
:ID: TASK-DTCBP
:PRIORITY: P2
:END:

Follow-ups from TASK-TCTTD (merged 539d2af), as listed by its implementer and
confirmed by the manager's replay. The verb works and is proven; these are the
integration steps that make it bind by default instead of on request.

=== 1. `orgasmic verify --all`

Replay every artifact under `verify/`, machine-readable summary, nonzero exit
if any artifact fails. This is the invocation items 2 and 3 actually want.
Expect ~2x a normal suite per artifact plus rebuild churn from patch cycling
(measured on TASK-R74E8: each arm recompiles orgasmic-daemon, 10-35s).

=== 2. `verify_artifact` drawer property (the task body's stated preference,
deferred by the implementer for write scope — correctly)

Add the schema field in orgasmic-core, let implementers set it, have
`orgasmic verify` prefer it over the path convention, and validate at
`dispatch-close` that a task claiming an artifact has one that loads.

=== 3. DECIDE: gate `manager dispatch-close --status done` on a passing replay

TASK-TCTTD's own non-goal said "once the verb has been used in anger". It now
has been (three replays by the manager on merge day, including a false-green
catch of a manager-authored no-op probe). Record the decision either way; if
gating, an escape hatch (--no-verify with a recorded reason) is required for
tasks whose defects cannot be patch-expressed.

=== Acceptance

- `verify --all` replays both shipped artifacts green and a deliberately broken
  one red, with one summary line each.
- The drawer property round-trips create -> get and `verify` resolves it.
- The gating decision is recorded as a dec_ node or in the task, with reasons.

=== Non-goals

- No CI wiring here (TASK-S2KM0 owns the lane; it should call `verify --all`).

NIGHTLY REPLAY [2026-07-29, deferred from TASK-S2KM0]: the per-PR CI lane deliberately excludes `orgasmic verify` replays (cost grows with history; some artifacts are load-dependent reproductions or 60s soaks that would flake on shared runners and teach people to ignore the lane). The right home is the nightly: replay every verify/TASK-*/ artifact on a schedule. That is this task's `verify --all` ask with a concrete consumer — wire it into nightly-soak.yml or a sibling scheduled workflow.

* BACKLOG TASK-0DANB Billing-capable daemon tests are one lock from a charge and their ignore reasons hide the spend    :daemon:testing:mechanism:
:PROPERTIES:
:ID: TASK-0DANB
:PRIORITY: P2
:END:

Follow-up to TASK-MFJZ7 (merged), which gated the one ungated billing-capable
test and audited the rest. Its audit left four daemon-side items, all
default-safe today via bare `#[ignore]` but out of that dispatch's write scope:

- `real_cursor_agent_stream_json_bridge_emits_events_and_releases`
  (`crates/orgasmic-drivers/tests/cursor_agent.rs:136`) — wait: drivers crate,
  but was left as-is because already ignored; include for wording only.
- `dispatch_endpoint_routes_cursor_agent_through_supervisor_and_emits_run_created`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:994`)
- `dispatch_endpoint_routes_acp_stdio_codex_through_supervisor`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:1060`)
- `dispatch_grace_path_writes_artifacts_without_terminal_session_marker`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:2129`)

Three carry ignore reasons phrased as availability ("requires X installed")
when the real hazard is spend: someone installing codex and running
`--include-ignored` to "check the codex path" gets a charge. `#[ignore]` is a
control, but a single-lock one, and the wording invites exactly the mistake.

=== Ask

1. Extend the MFJZ7 mechanism (`BILLED_TESTS` + `skip_unless_billing_allowed`
   + `report_billed_tests`, in `orgasmic_drivers::modes::rmux::test_tooling`)
   to the daemon test binaries above, so `--include-ignored` alone cannot bill
   without `ORGASMIC_ALLOW_BILLED_TESTS=1`.
2. Reword every billing-capable test's ignore reason to name the spend.
3. Re-run MFJZ7's audit method on `crates/orgasmic-daemon/**` (it audited
   drivers thoroughly; daemon got only the grep-obvious cases): every
   `.acquire(`/harness spawn in daemon tests, verdict per test.

=== Acceptance

- Collection proof both arms (never execution), per the MFJZ7 standard: without
  the env, `--include-ignored --list` collects but the guard's runtime skip is
  shown on a safe representative; with the env, collection shows it armed.
- Audit table recorded on this task.
- A `verify/TASK-<this>/` artifact per the TASK-TCTTD standard — with a patch
  that `git apply` ACCEPTS (MFJZ7's shipped patch was a documentation diff that
  git apply rejects; do not repeat that).

* BACKLOG TASK-A2YEM RESTART_DRAIN_TIMEOUT carries 15s of narrative slack; name it as a constant on the restart-endpoint axis    :daemon:hardening:
:PROPERTIES:
:ID: TASK-A2YEM
:PRIORITY: P2
:END:

From TASK-G7E4R's axis sweep (its report, section 3): daemon_lifecycle::RESTART_DRAIN_TIMEOUT (40s) waits on the restart endpoint's own budgets (RELEASE_FINALIZATION_DRAIN_TIMEOUT 20s + RESTART_WRITER_DRAIN_TIMEOUT 5s = 25s) and is already guarded by the relationship test restart_drain_fuse_outlasts_the_endpoint_budgets_it_waits_on — so it is NOT the unguarded-literal shape G7E4R removed. But its remaining 15s of slack (rest of the handler) is documented in prose rather than composed from a named margin constant. Ask: express it as endpoint_budgets_total() + RESTART_HANDLER_MARGIN with the existing relationship test asserting the composition, the G7E4R/QRB8S pattern. Small; ship with a verify artifact.

* BACKLOG TASK-P0Q5C No CLI title surface for decision/architecture/glossary/project/handoff nodes despite daemon set_title support
:PROPERTIES:
:ID: TASK-P0Q5C
:PRIORITY: P2
:END:

From TASK-XPYRR's ask-3 sweep (checked against code, not assumed): the daemon's
`set_title` op works for decision, architecture, glossary, project, and handoff
nodes — the UI writes titles for all of them — but no CLI surface exists for
any kind except task (`task update --title`, TASK-XPYRR). Same shape as the
task gap: an operator or manager agent can observe a wrong title and cannot
correct it without the UI.

=== Ask

Add `node title set <ID> --title ... --project ... --kind ...` routing to the
existing `set_title` op through TASK-XPYRR's `edit_heading_line` guard (round
trip or refuse naming the reason; never touches tags/keyword). One surface for
all title-capable kinds rather than per-kind flags.

=== Acceptance

- Retitle round-trips for each supported kind in tests; goal kind is refused
  with a message pointing at TASK-V460X (separate task, no daemon path).
- Refusal semantics identical to task update --title (same guard, same
  messages).
- Verify artifact per TCTTD standard, `git apply`-clean.

* BACKLOG TASK-V460X Goal titles have no write path: a typo fix requires a false SUPERSEDED record — decide edit-vs-amend semantics, then implement
:PROPERTIES:
:ID: TASK-V460X
:PRIORITY: P2
:END:

From TASK-XPYRR's ask-3 sweep: goal nodes have NO title-write path anywhere —
`/org/node/{id}/edit` rejects goal nodes outright (api.rs:12119), and
`goal set` always supersedes (stamps the active goal SUPERSEDED and prepends a
new heading, api.rs:13440-13447). Correcting a typo in a goal title is
impossible; the nearest available act CHANGES THE RECORD'S MEANING (a
supersession where none happened). This is the TASK-XPYRR complaint, unfixed,
for the node kind where wording arguably matters most — the goal is the
standing instruction every agent reads.

=== Ask

1. Decide the semantics first, then implement: is a goal title correction an
   EDIT (heading rewrite, history in the tx) or an amendment record? The
   supersede-only design was presumably deliberate (goals as append-only
   record); a correction surface must not quietly break that property. Record
   the decision as a dec_ node.
2. Whatever the decision, the operation must preserve the goal's state keyword
   and any metadata, per the edit_heading_line guard pattern.

=== Acceptance

- A typo'd goal title is correctable without a false SUPERSEDED record, proven
  end-to-end.
- The append-only/history property that goal set's supersede flow protects is
  explicitly preserved or explicitly amended, with the dec_ node recorded.
- Verify artifact per TCTTD standard.

* BACKLOG TASK-P9T4N Multi-tx writer transaction so dispatch-close is one commit; reconciliation becomes backstop
:PROPERTIES:
:ID: TASK-P9T4N
:PRIORITY: P2
:END:

From TASK-EP3H1 (2026-07-29): `WriterHandle::transaction(rewrites, tx)` takes
exactly ONE tx, which is the only reason `dispatch-close` cannot be a single
commit — a close needs two ledger entries (the terminal `implementer.done` and
the `task.state_transitioned` from `write_task_lifecycle_and_record`), so the
CLI issues N+1 requests and the pair can tear.

EP3H1 shipped the backstop: close txs record LIFECYCLE_FROM/TO and
`reconcile_torn_closes` finishes a torn close on the next manager command, and
labelled idempotency (`already_applied` vs `already_in_state`) makes replays
safe. What remains is making the tear impossible rather than repairable.

=== Ask

Add a multi-tx writer transaction — `transaction_multi(rewrites, Vec<TxAppend>)`
or equivalent — so the close tx and `task.state_transitioned` land under one
flock, then route dispatch-close through it. Reconciliation stays as the
backstop for pre-existing ledgers and for client-side response loss (which no
server-side atomicity can fix — EP3H1's measured loss was a client timeout on a
request that SUCCEEDED server-side).

=== Acceptance

- A close is one commit: an injected crash between the two legs can no longer
  produce a ledger with the close tx but not the transition.
- Reconciler still passes its torn-close tests against old-format ledgers.
- The writer core change is proven with its own tests (concurrent writers, one
  flock, ordering) — this is the risky layer; EP3H1 deliberately did not touch
  it.
- Injection per TCTTD standard.

* BACKLOG TASK-SJNNA Daemon should scrub TMUX from its environment before spawning tmux (latent: $TMUX selects the socket)
:PROPERTIES:
:ID: TASK-SJNNA
:PRIORITY: P2
:END:

From TASK-0RCRY (2026-07-29), latent-exposure hardening, NOT a live bug report.

Measured mechanism (gotchas.org has the entry): tmux takes its socket path from
$TMUX (up to the first comma) whenever neither -L nor -S is given — $TMUX is
not merely a nesting marker. A daemon that ever inherited TMUX would therefore
aim every unpinned tmux call at the enclosing server; inside a worker's
environment that is an rmux socket, which does not speak tmux's protocol and
fails deterministically with `server exited unexpectedly`:

  TMUX=/private/tmp/rmux-501/default,… tmux new-session -d -s x -- sleep 5
  → exit 1, "server exited unexpectedly" (100% of attempts)

Not live today: the daemon carries no TMUX (measured on the real daemon,
recorded in TASK-0RCRY's premise history). 0RCRY also decided — deliberately,
stated at TMUX_SOCKET_ENV in tmux.rs — that production must NOT pin -L, because
a daemon-created session must remain reachable by the operator's own tmux
client. So the correct hardening is scrubbing, not pinning.

=== Ask

The daemon scrubs TMUX from its own environment (and from child environments
it constructs for tmux invocations) before spawning tmux, so an operator
launching the daemon from inside a tmux/rmux session cannot aim daemon tmux
calls at the enclosing server. Keep the operator-visibility property: no -L/-S
pinning in production.

=== Acceptance

- A daemon started with TMUX set behaves identically to one started without:
  proven by test (spawn path receives a scrubbed env).
- Sessions the daemon creates remain reachable by an unpinned operator client.
- Injection per TCTTD standard, git apply-clean, self-tested with
  `orgasmic verify --artifact` before reporting.

* BACKLOG TASK-5CCZM Empty-git-remote-under-load flake class: three observers of refresh_repo_url's give-up path, no owner
:PROPERTIES:
:ID: TASK-5CCZM
:PRIORITY: P2
:END:

The empty-git-remote-under-load flake class: three test names, one shape, no
owner. An unowned flake cannot be registered (registry rule), so the class
stays invisible and any recurrence classifies REAL in a loaded gate.

Sightings, all green in isolation:

1. `post_projects_registers_existing_orgasmic_project_and_refreshes_index`
   (crates/orgasmic-daemon/tests/integration.rs:357) — `assertion left == right
   failed, left: String(""), right: "git@example.com:org/existing.git"` —
   TASK-0RCRY full-workspace run, 2026-07-29.
2. `post_projects_scaffolds_and_registers_uninitialized_project`
   (integration.rs:437) — same shape, right: "git@example.com:org/fresh.git" —
   same run.
3. `index::tests::live_rebuild_preserves_repo_url_and_schedules_post_bind_refresh`
   — the already-documented candidate in verify/flake-registry.toml's notes;
   TASK-5FEN5 fixed its test-side wait (drive the probe until the URL lands),
   so THIS sighting class is about the remaining callers of the same
   under-load-git-probe behaviour, not that fixed test.

Shape: `refresh_repo_url` bounds its `git config --get remote.origin.url`
probe (3s) and returns WITHOUT WRITING when the bound is spent — deliberate
production behaviour with its own test. Under load the probe loses the race and
observers see "". 5FEN5 measured a passing probe at 2.24s against the 3s bound
on a loaded machine — the failures are that distribution's tail.

=== Ask

Per the 5FEN5 standard: reproduce under load, then fix each observer at the
synchronization (wait on the probe's completion, or drive until present with a
hang-guard bound), not the budget size. integration.rs tests are out-of-process
observers — check what completion signal they CAN see before designing.
Registering with this task as owner is the fallback; fixing is the goal.

=== Acceptance

- Either the named tests survive >=5 consecutive loaded runs post-fix (pre-fix
  counts pinned), or they are registered with measured signatures and the
  diagnosis documented.
- Injection per TCTTD standard where patch-expressible (the 5FEN5 artifact's
  first-probe-returns-empty injection is the template).

* BACKLOG TASK-HVM6H tmux interactive follow-up (send_input + tmux/custom pair): decide feature-or-close, then artifact hot-path differential
:PROPERTIES:
:ID: TASK-HVM6H
:PRIORITY: P2
:END:

From TASK-K4G1D (2026-07-29): the artifact-regenerate hot path cannot be
measured under tmux today, for two independent structural reasons, both
measured:

1. `("tmux", "custom")` is not in `orgasmic_drivers::SUPPORTED` — and it is the
   repo's canonical UNSUPPORTED pair, used by two api.rs tests precisely
   because it must fail. Registering it needs tmux's `build_spawn_plan` to grow
   a custom arm mirroring rmux's (harness_args[0] as command) AND an equivalent
   of rmux's `custom_dispatch_misconfig` guard (which stops a dispatch prompt
   being pasted into a bare shell). Product decision, not test plumbing.
2. `TmuxTuiControl` implements only transition_state/babysitter_action/release;
   `send_input` falls to the trait default → `Unsupported("send_input")`,
   measured against live tmux 3.6a on an owned server. The hot path IS
   send_input (api.rs:15793), so `post_artifact_regenerate_hot_path_reuses_
   live_run_id` cannot hold under tmux by construction.

K4G1D's caveat binds any implementation: a differential whose tmux arm is
brand-new code cannot answer "is rmux at fault" — red under new tmux code
indicts the new code. So this task is only worth doing if the tmux driver's
send_input is wanted AS A FEATURE (interactive follow-up under tmux), with the
differential as a byproduct once the code has matured.

=== Ask

1. Decide whether tmux interactive follow-up is wanted at all. If not, close
   this task with the decision recorded — the K4G1D attach-proof differential
   already provides the rmux-vs-tmux instrument.
2. If wanted: implement `TmuxTuiControl::send_input`, register tmux/custom with
   the misconfig guard, extend the two canonical-unsupported-pair tests to a
   new canonical pair, and only then parameterize the artifact hot-path test.

=== Acceptance

- Either a recorded decision closing the gap as not-wanted, or send_input under
  tmux proven live plus the parameterized hot-path test naming its mode.
- Injection per TCTTD standard if code ships.

* BACKLOG TASK-FZFGJ reattach_tmux is the recovery action id emitted for rmux runs too — rename to name the real mode
:PROPERTIES:
:ID: TASK-FZFGJ
:PRIORITY: P3
:END:

From TASK-K4G1D (2026-07-29): the recovery action id 'reattach_tmux' is emitted for rmux runs as well (api.rs:10295 at K4G1D's HEAD; re-locate before acting). Harmless today, actively misleading in exactly the mux-differential investigations K4G1D built the instrument for (TASK-STWVB, TASK-JGHNC). Rename so the action id names the actual mode, keeping any external consumers of the id in mind — grep the UI and shipped conventions for the string before changing it. Injection per TCTTD standard (natural red: an rmux recovery emitting an id that claims tmux).

* BACKLOG TASK-ZKZBF Audit the remaining --property surfaces (node prop, glossary/decision/architecture create, dispatch-close) for the HXSW0 silent-drop shape
:PROPERTIES:
:ID: TASK-ZKZBF
:PRIORITY: P2
:END:

From TASK-HXSW0 (2026-07-29), which fixed key validation on task create/update
— the surface all six measured silent-drop instances lived on. The same
untyped-passthrough shape exists on:

- `node prop set` / `node prop unset`
- `glossary create --property`
- `decision create --property`
- `architecture create --property`
- `manager dispatch-close --property`

For each surface: determine whether a miscased key is stored-and-never-read
(the `Heading::property` byte comparison in crates/orgasmic-core/src/org.rs
makes this the DEFAULT outcome for any drawer-backed key), whether any per-key
filter drops silently, and whether the key set diverges from a sibling verb.

Reuse `validate_task_property_keys` (api.rs, TASK-HXSW0) where the target is a
schema-read drawer. NOTE: `dispatch-close --property` writes tx metadata, not a
node drawer — the same rule may not apply unchanged; decide and state the rule
for it rather than assuming.

=== Acceptance

- A production-path test per surface asserting a miscased key is refused BY
  NAME (real binary, drawer read off disk afterwards — the HXSW0 test shape in
  crates/orgasmic-cli/tests/task_property_silent_drop_cli.rs is the template).
- Injection per TCTTD standard, self-tested with `orgasmic verify --artifact`
  before reporting.

* BACKLOG TASK-TKR0H Guard shipped prose that DENIES a real verb: negative claims about command names must fail when the verb resolves
:PROPERTIES:
:ID: TASK-TKR0H
:PRIORITY: P2
:END:

From TASK-HXSW0 (2026-07-29). Its shipped-prose guard
(crates/orgasmic-cli/tests/cli_parity.rs, 128 invocations across shipped/**)
proves every QUOTED command resolves against the real clap surface. It cannot
catch the inverse: prose asserting a real verb is ABSENT. That inverse is
measured, not hypothetical — `shipped/prompt-studio/conventions/manager-dispatch.org`
said "there is no listing command … that verb has never existed in any command
group" and `prompt-specs/manager.org` listed `manager drivers` among "commands
that sound obvious and do not exist" — for MONTHS after TASK-JQARS landed
`orgasmic manager drivers`. The prose instructed managers to discover transport
pairs by submitting a bogus pair and reading the rejection. Both were corrected
by hand in HXSW0; nothing prevents recurrence.

=== Ask

A guard that detects negative claims about command names in shipped prose —
"does not exist", "has never existed", "there is no … command/verb" within N
tokens of an `orgasmic …` span — and FAILS when the named verb resolves against
the real clap tree. Precision matters: the corpus legitimately says "X does not
exist" about verbs that genuinely don't (that is the guard passing); tune the
extraction so real negatives survive.

=== Acceptance

- An injection that re-adds a "verb X does not exist" sentence about a verb
  that DOES resolve is caught by the test (this is the natural verify artifact).
- The guard runs in the same test binary as the existing shipped-prose check
  and shares its extraction, so the two directions cannot drift apart.
- Zero false failures on the corpus at HEAD.

* BACKLOG TASK-C4BJR Decide whether task drawer keys are a closed set; document or enumerate the ~15 free-form annotation keys
:PROPERTIES:
:ID: TASK-C4BJR
:PRIORITY: P3
:END:

From TASK-HXSW0 (2026-07-29): --property accepts any UPPERCASE key. Live task headings carry ~15 keys no reader consumes (:MERGE_SHA: x23, :CODEX_COMMIT: x23, :SET_BY:, :CLOSED_AT:, :SEVERITY:, :RESUME_SKILL:, ...), some plainly deliberate annotation. Either (a) declare the drawer open and document the annotation convention, or (b) declare it closed, enumerate the legal set, and migrate existing keys. HXSW0 deliberately did not choose: rejecting unknown keys would have broken working manager flows for a defect nobody measured. Product decision — record it as a dec_ node, then implement whichever guard follows.

* BACKLOG TASK-AP298 An admitted-but-unchecked preflight leaves no trace in the dispatch's own evidence (RunMeta/tx)
:PROPERTIES:
:ID: TASK-AP298
:PRIORITY: P2
:END:

From TASK-GEZHQ (2026-07-29): when the credential probe cannot answer (timeout/spawn failure), the dispatch is admitted per dec_7P79C and GEZHQ's WARNs reach the daemon log — but RunMeta and the tx record still say only that the plan was pinned, not that the probe never answered. A run that dies at startup should be able to say 'admitted without a credential check' from its own record, without the operator correlating daemon-log timestamps. Add the unchecked-admission fact to the dispatch's own evidence surface (RunMeta field or tx annotation), so post-mortems read it where they already look. Keep dec_7P79C intact: this is observability, not a new rejection path. Injection per TCTTD standard: natural red = a probe-timeout dispatch whose run record contains no trace of the unchecked admission.

* BACKLOG TASK-VJ633 Promote the strict tmux rule to one public copy in orgasmic_drivers; drivers and cli gates still accept the shim (K4G1D artifact re-authoring required)
:PROPERTIES:
:ID: TASK-VJ633
:PRIORITY: P2
:END:

From TASK-JGHNC (2026-07-29): after JGHNC fixed the daemon test gates, two
surfaces still accept the rmux shim as tmux:

1. `crates/orgasmic-drivers/src/modes/tmux.rs` — `ToolRequirement("tmux", 9,
   tmux_spawn_usable())` plus a `tmux_available` gate.
2. `crates/orgasmic-cli/tests/dispatch.rs` — `ToolRequirement("tmux", 1, …)`.

The clean fix is ONE public strict rule in `orgasmic_drivers::modes::tmux`
(promote `tmux_mode_availability_for` from its current home in
orgasmic-daemon's api.rs tests), consumed by daemon, drivers, and cli gates so
the rule cannot drift per-crate.

THE CONSTRAINT THAT MAKES THIS A DECISION, NOT A CHORE (JGHNC's stated reason
for leaving it): TASK-K4G1D's shipped injection patch pins the current BODY of
`tmux_mode_availability_for` in api.rs — promoting/moving the canonical copy
means RE-AUTHORING verify/TASK-K4G1D (and re-measuring its red) as part of the
change. A move that silently breaks a shipped proof is a stale-artifact
failure; the artifact update must ship in the same change with its own
red-then-green evidence. verify/TASK-JGHNC pins the gate WIRING in api.rs tests
and may need the same treatment — check both with `orgasmic verify` before and
after.

=== Acceptance

- One canonical strict rule, public in orgasmic_drivers; all three crates' tmux
  gates consume it; a shimmed tmux is ABSENT everywhere, loudly.
- verify/TASK-K4G1D and verify/TASK-JGHNC both PASS after the move (re-authored
  if needed, with the re-measurement shown).
- Injection per TCTTD standard for the new wiring surface.

* BACKLOG TASK-T09M7 Driver-side audit: a driver's producer handle must own every event sender it created (stray-sender invariant)
:PROPERTIES:
:ID: TASK-T09M7
:PRIORITY: P3
:END:

From TASK-HAREX (2026-07-29): the 82-minute phantom run was caused by a stray event-sender clone in a driver-internal task the supervisor does not own — the drain's recv() never returned None. HAREX bounded the CONSEQUENCE (DrainGate on requested release); nothing yet asserts the invariant that a driver's producer handle owns every sender it created, so the next stray sender still costs a full RELEASE_DRAIN_BUDGET per release and drops post-budget events. Add a per-mode DriverSession invariant test turning this liveness bug into a test-time failure. acp_stdio and rmux are the two to read first (the persistent-socket rmux design holds shared state the tmux CLI design cannot — the 0RCRY task body's structural-asymmetry note). Injection per TCTTD standard: natural red = a mode with a deliberately-planted stray sender passing the invariant test pre-fix.

* BACKLOG TASK-6ADDB Unowned load flake: post_artifact_regenerate_cold_changed_address_replaces_whole_launch_record keeps the old launch address
:PROPERTIES:
:ID: TASK-6ADDB
:PRIORITY: P2
:END:

Observed 2026-07-29 in TASK-6HJYT's gate run (workspace suite under load), green in isolation, no registry entry: api::tests::post_artifact_regenerate_cold_changed_address_replaces_whole_launch_record (crates/orgasmic-daemon/src/api.rs:29094 at 6HJYT's HEAD) — 'launch address kept model=old-model/effort=high instead of model=new-model/effort=None'. The regenerated launch address did not replace the persisted one under suite concurrency. Spawns a real rmux session. Per the DJ1WV rule an unowned flake cannot be registered, so recurrence classifies REAL and taxes gates. Diagnose per the 5FEN5 standard: reproduce under suite-concurrent load, fix at the synchronization if the cause is an observer racing background persistence, or fix the product if regenerate genuinely can lose the replacement; register with this task as owner only as the fallback, with the measured signature. Injection per TCTTD standard, self-tested.

* BACKLOG TASK-ZFCQN Collapse the manager.launch: classifier — CLI re-implements is_interactive_manager_task as a string prefix
:PROPERTIES:
:ID: TASK-ZFCQN
:PRIORITY: P3
:END:

From TASK-6HJYT (2026-07-29): the CLI filters manager runs with a literal starts_with("manager.launch:") while the daemon owns supervisor::is_interactive_manager_task. Duplicated across the process boundary; drift means the fences and the daemon disagree about what a manager run is. One pub export and one call site; deferred by 6HJYT to stay disjoint from HAREX's then-live supervisor.rs work (now merged, 47be4fb). Note the boundary: the CLI cannot call daemon code in-process — the collapse is either exporting the predicate into a shared crate both link, or the /runs/live response carrying an is_manager flag computed daemon-side (preferred: the daemon already knows). Small; verify artifact optional if the change is pure refactor with an equivalence test.

* BACKLOG TASK-Z4M4D /recovery/status is a second whole-board scan reachable from the UI with no bounded-scan metrics
:PROPERTIES:
:ID: TASK-Z4M4D
:PRIORITY: P3
:END:

From TASK-6HJYT (2026-07-29): StatusView reads GET /recovery/status for the recovery panel — legitimate consumer — but the endpoint performs a second whole-board scan with no bounded-scan metrics surfaced (GET /runs got InventoryStageMetrics under KWSTJ; this endpoint did not). Now that /runs is split (6HJYT: /runs/live supervisor-local, /runs the inventory), give /recovery/status the same treatment: either consume the inventory's metrics/serialization (recovery_status_lock) explicitly, or document why its scan is bounded. Look only after 6HJYT's shape settles.

* BACKLOG TASK-5NBTZ api.rs real_tmux_on_path claims the owned server BEFORE the strictness check — worker suites start a keepalive through the rmux shim
:PROPERTIES:
:ID: TASK-5NBTZ
:PRIORITY: P2
:END:

From TASK-FJCE9 (2026-07-29), same class as FJCE9/JGHNC, one line above JGHNC's fix: in crates/orgasmic-daemon/src/api.rs tests, real_tmux_on_path calls own_tmux_server_for_tests() AHEAD of tmux_mode_availability_for(...), so inside a worker the daemon lib suite still claims an owned server THROUGH THE RMUX SHIM. Observed directly at 2026-07-29 03:01:49: /private/tmp/rmux-501/ gained orgasmic-test-56699 + .startup-lock (own_tmux_server_for_tests's name format, rmux's server), gone by 03:04. The claim-order attribution is a code reading, not a captured pid — verify at HEAD first. Nothing is killed (smaller than FJCE9's defect) but the class is identical: a tmux-labelled mechanism operating on the rmux server. Fix: check strictness first, claim second — supervisor.rs already has the correct order; recovery_fault_restart.rs (FJCE9) is the third example of the right shape. Injection per TCTTD standard (natural red: in a shim environment the claim happens — assert the rmux socket dir gains no orgasmic-test-* entry when availability fails).

* BACKLOG TASK-15TMP /private/tmp/tmux-501 accumulates dead orgasmic-test-<pid> socket inodes, one per suite run
:PROPERTIES:
:ID: TASK-15TMP
:PRIORITY: P3
:END:

From TASK-FJCE9 (2026-07-29): the 0RCRY owned-server mechanism leaves the socket INODE behind after tmux tears the server down (24 present, none with a live server, oldest 01:39). Harmless but unbounded, and it makes 'did this run create a server?' harder to read from a listing. Options: the keepalive's exit path unlinks its own socket after the server dies (careful: only its own, by exact name, never a sweep of the directory — a sweep could race a live run); or a claim-time cleanup of sockets whose server is provably dead AND whose pid-embedded owner is gone. State the chosen rule and why it cannot remove a live run's socket. Small; verify artifact optional if covered by a direct test.

* BACKLOG TASK-AJP4A Index rebuild drops a project whose scan exceeds the 5s bound and never retries — decide the convergence policy
:PROPERTIES:
:ID: TASK-AJP4A
:PRIORITY: P2
:END:

From TASK-MRJRK (2026-07-29): rebuild_with_timeout (index.rs:495-514 at MRJRK's HEAD) is the one path that removes a key from snap.projects — on scan timeout or join error it warns and moves on, the project is omitted from the published snapshot, and nothing reschedules it. MRJRK made the condition VISIBLE (unindexed_projects in status, warn at publish, the 404 names the index) and deliberately shipped NO convergence, because the 2026-07-25 incident's cause is still open (scan give-up vs short board read — the new status field discriminates them on recurrence) and a silent heal would erase the evidence. This task decides the policy once evidence exists: carry the prior snapshot's ProjectIndex forward past a failed scan (the last-good pattern load_project already uses), or a bounded retry — either WITH a staleness signal so the fix does not re-hide what MRJRK surfaced. Blocked on nothing technically; prefer waiting for one captured recurrence. Also note the accidental convergence: watcher.rs:365-397 reloads missing projects on any board-file event (touch board.org fixes the state) — make that deliberate or remove it as part of the same decision. Injection per TCTTD standard.

* BACKLOG TASK-1TY4M RuntimeOptionsRequest still carries alias=effort — the 4YC8E landmine one client change away
:PROPERTIES:
:ID: TASK-1TY4M
:PRIORITY: P3
:END:

From TASK-4YC8E (2026-07-29): runtime_options.rs:12 declares #[serde(default, alias = "effort")] on RuntimeOptionsRequest — the exact shape that killed hermes dispatches, safe today only because no client sends both key names (UI sends reasoning_effort only, RuntimeOptionsBar.tsx:52). No test holds it. Either pin 'both names in one body is rejected loudly' with a test, or drop the alias once no client sends effort (client-facing API change — check external consumers first). Also from the same report, P4-grade: TmuxConfig.effort / RmuxConfig.effort are now write-only-by-history (no live writer emits effort; kept for driver configs persisted by older daemons) — remove after the dual-key-writing runtime ages out; fold into this task when touched.

* BACKLOG TASK-22ED4 Babysitter cascade-release family fails under extreme concurrency (RunNotFound unwraps + implementer-remains-live)
:PROPERTIES:
:ID: TASK-22ED4
:PRIORITY: P3
:END:

From TASK-J1XCB (2026-07-29), one 4x64-thread round, one sighting each, all green in isolation: supervisor::tests::implementer_release_cascades_to_auto_spawned_babysitter and implementer_release_twice_is_idempotent_for_cascade (RunNotFound unwraps at supervisor.rs:10425/10614) and babysitter_release_before_implementer_release_is_idempotent ('implementer remains live after babysitter release', :10519). Looks like one cascade-release family racing under starvation harsher than a real gate — J1XCB flagged its harness (4 copies x 64 threads on 10 cores) as possibly not gate-realistic. Diagnose with that caveat: reproduce at gate-realistic concurrency first; if only the extreme harness bites, say so and bound the scope accordingly. Fix at the synchronization if real (R74E8).

* BACKLOG TASK-S9GYG Restate the one-probe-per-dispatch property: STATUS_ATTEMPTS means a late harness is legitimately asked twice
:PROPERTIES:
:ID: TASK-S9GYG
:PRIORITY: P3
:END:

From TASK-D1Z87 (2026-07-29): GEZHQ's STATUS_ATTEMPTS=2 means a genuinely late harness is asked auth status TWICE in production, both invocations real — while the count assertions in claude.rs and dispatch_credential_plan.rs say 'exactly one'. In tests the warm-up removes the lateness so 1 is honest, but the property AS WORDED is now narrower than production: it holds for re-probing after admission (what it was written for), not for retrying before it. Decide the semantics: restate as 'one probe DECISION per dispatch, before ownership' and let the count admit STATUS_ATTEMPTS, or pin the current wording with a rationale. Product-semantics decision — record it where the assertions live. Related residual (same report): the __orgasmic_warm_up argv exemption rests on it never becoming a real claude subcommand; the ledger split catches it if it does — note that invariant wherever the decision lands.

* BACKLOG TASK-Y62VV dispatch_cleanup_releases_worker_and_deletes_worktree_branch drives whatever harness the machine has (not hermetic)
:PROPERTIES:
:ID: TASK-Y62VV
:PRIORITY: P3
:END:

From TASK-Z7VQK (2026-07-29): the test installs a codex stub it never puts on the dispatch's PATH, so it dispatches the machine's real harness. Left as found and named in a code comment by Z7VQK — making it hermetic changes what the test dispatches, which needed its own decision. Decide and fix; ensure hermeticity does not create a billed path.

* BACKLOG TASK-80W0C test_fixtures.rs shared file is keyed by test-binary name, which survives rebuilds — silent stale-fixture hazard
:PROPERTIES:
:ID: TASK-80W0C
:PRIORITY: P3
:END:

From TASK-Z7VQK (2026-07-29): src/test_fixtures.rs keys its shared fixture by the test binary's name assuming the name carries a build hash; measured false (dispatch_endpoint-1edf7bef84349123 survived source edits). Z7VQK's own shared file is keyed by content with an assert that caught a stale fixture in flight; give test_fixtures.rs the same content-keying or content-assert so a stale fixture fails loudly instead of silently.

* BACKLOG TASK-1MEW6 live_rebuild git probe retry can exhaust under extreme load: 'every attempt returned without writing a URL' — 5FEN5's fix has a ceiling
:PROPERTIES:
:ID: TASK-1MEW6
:PRIORITY: P2
:END:

From TASK-TZKAC's harness rounds (2026-07-29): index::tests::live_rebuild_preserves_repo_url_and_schedules_post_bind_refresh went red with a NEW panic — TASK-5FEN5's fix (drive the probe until the URL lands, 60s bound) EXHAUSTED rather than bypassed: 'the live Git probe for project never resolved ssh://git@example.com/org/project.git: every attempt returned without writing a URL' (index.rs:2154 at TZKAC's HEAD). 4 of 4 copies in the single slowest post-fix round (201s/copy vs 102s baseline), 0 of 24 in every other round — an extreme-starvation profile (4x64 threads on 10 cores), possibly not gate-realistic (the J1XCB caveat applies). Cannot be registered: 5FEN5 is closed and the registry forbids ownerless excuses. Related: TASK-5CCZM owns the give-up-path OBSERVER class for the same refresh_repo_url behaviour — consider folding this in there or coordinating; the underlying production behaviour (a 3s-bounded probe that returns without writing under starvation) is the shared root. Diagnose whether the gate-realistic fix is observer-side (5CCZM's ask) or the probe's bound/retry policy itself (derived, not guessed).

* BACKLOG TASK-TJ9DG Reconcile arch_EAVPP with the shipped evidence clock: its R2/R11 assumptions are now false in code, with measured cause
:PROPERTIES:
:ID: TASK-TJ9DG
:PRIORITY: P2
:END:

From TASK-JK66P (2026-07-29): arch_EAVPP ('Provider-agnostic progress detection contract', TASK-41B0T, accepted, no implementation existed) assumed stall_timeout stays a pure transport-liveness clock (R2) and excluded pane transports from progress enforcement because 'the yielded child PID cannot be observed' (R11/§2.2). JK66P's merge redefines the stall clock as an evidence-of-work clock for every transport, and its §4.3 measurement disproves the observability premise: the pane's child process tree IS observable from the daemon via the mux (rmux display-message '#{pane_pid}' → ps subtree), and it separated the two measured incidents by three orders of magnitude. MANAGER'S PROVISIONAL VERDICT (to be ratified by whoever holds architecture authority on this task): supersede EAVPP §2.1-2.4 in part — the measured incidents outrank the design-time assumption; IF the opt-in structured-transport progress_stall_timeout_secs is still wanted, rebase it on last_progress_at rather than a parallel clock. Record the outcome as a dec_ node and update the arch record's status/body accordingly. Include TASK-JK66P report §6 as the evidence base.

* BACKLOG TASK-PAZQ2 RunSummary should expose the work clock: last_progress_age + last evidence observation
:PROPERTIES:
:ID: TASK-PAZQ2
:PRIORITY: P3
:END:

From TASK-JK66P (2026-07-29): the supervisor now holds last_progress_at and work-evidence observations, but RunSummary does not expose them, so 'quiet for 8 minutes, work confirmed' is invisible to orgasmic manager status / the UI. arch_EAVPP §5 wants the same surface. Add last_progress_age_secs and the last observation (Working/Idle/Unknown + detail) to RunSummary and thread to the CLI status output. Coordinate with TASK-6HJYT's /runs/live shape (supervisor-local — this is exactly the kind of field it should carry).

* BACKLOG TASK-GJ50T Release-reason strings are structured data parsed by two hand-rolled consumers — extract a shared release_reason module on the third
:PROPERTIES:
:ID: TASK-GJ50T
:PRIORITY: P3
:END:

From TASK-JK66P (2026-07-29): the stall reason now carries structured detail in a String; is_stall_sweep_release_reason (CLI) and anomalous_without_finalize_release_reason (api) both match its leading token by hand, each with a regression test. Rule: on the THIRD consumer, extract the shared release_reason module in core that arch_EAVPP §4.3 already specifies. Also from the same report: no test pins the rmux pane-pid probe against a real session (proven by hand in report §4.3) — a real-rmux-gated test spawning a disposable session on an owned endpoint and asserting '#{pane_pid}' resolves would close it; fold into this task or the EAVPP reconcile, whichever lands first.

* BACKLOG TASK-FBFDF recovery_fault_restart.rs defers to an ambient ORGASMIC_TMUX_SOCKET — result depends on the operator's environment
:PROPERTIES:
:ID: TASK-FBFDF
:PRIORITY: P3
:END:

From TASK-K5NDR's audit (2026-07-29): crates/orgasmic-daemon/tests/recovery_fault_restart.rs:283 sets TMUX_SOCKET_ENV only if the operator has not — so an operator with the variable exported runs a DIFFERENT assertion than one without, the same result-depends-on-who-runs-it class K5NDR fixed in the CLI harness. Possibly an intended operator override for pinning a server (FJCE9-era). Owner's call: keep the override with a loud log line naming the inherited value, or scrub unconditionally like the CLI fixture. Small; decide and pin with a test either way.

* BACKLOG TASK-FDFVP Reconcile the env -u prose and SCRUB layers now that the CLI fixture scrubs by default
:PROPERTIES:
:ID: TASK-FDFVP
:PRIORITY: P3
:END:

From TASK-K5NDR (2026-07-29): with the deny-by-default fixture merged, scripts/run-tests.sh's SCRUB and verify.rs's SCRUBBED_ENV are belt-and-braces for the CLI suite (still load-bearing for OTHER crates' suites — do not remove blindly). Two reconciliations: (1) VERIFY_AFTER_HELP (src/verify.rs:49) still instructs artifact authors to prefix cargo runs with env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME, while verify/TASK-K5NDR's own cmd must do the OPPOSITE (set the variable) — update the prose to describe the scrub-by-default world and the set-it-deliberately exception; (2) audit which other crates' test harnesses still rely on the wrapper's SCRUB (daemon/drivers build Home::at explicitly per K5NDR's audit, so likely none for resolution — but confirm before weakening anything). Manager briefs can drop the env -u incantation for the CLI crate once this lands.

* BACKLOG TASK-45F0N acp_ws.rs breaks its select without draining the socket — same shape as SVKPN's fixed defect, different lifecycle
:PROPERTIES:
:ID: TASK-45F0N
:PRIORITY: P3
:END:

From TASK-SVKPN's driver audit (2026-07-29): acp_ws.rs:277 (commands / ws.next() select) breaks on channel close and on release-done without draining the socket — the same break-without-drain shape SVKPN fixed in subprocess_stream_json and acp_stdio. Deliberately NOT fixed there: no child process, no SubprocessExitSummary, no empty-run_complete synthesis (the product harm SVKPN was about), and the release path closes the socket first — a fix would drain a just-closed socket on an unmeasured path. The loss, if any, is transcript events only. Measure before fixing: construct a fast-closing ws peer and check whether produced frames are lost; if yes, apply the SVKPN bounded-drain pattern (RELEASE_DRAIN_BUDGET derivation, drain after close-initiation, biased select inside the drain); if no, record the negative result and close.

* BACKLOG TASK-EDPFN Abandon has no --reason: the tombstone names the judgment but cannot carry the operator's evidence
:PROPERTIES:
:ID: TASK-EDPFN
:PRIORITY: P3
:END:

From TASK-VSWZT (2026-07-29): rescue-policy's inspect-before-choosing section wants the operator's evidence recorded ('superseded by TASK-X', 'worktree empty'), but RunRecoverRequest has no reason field, so the abandon tombstone carries only the fixed judgment string. Add optional reason threaded into the Release tombstone's reason suffix; CLI --reason flag on run recover. Small; keep the fixed prefix stable (JK66P's leading-token consumers).

* BACKLOG TASK-CS4B3 tmux_server_selection() never names $TMUX even when $TMUX chose the socket
:PROPERTIES:
:ID: TASK-CS4B3
:PRIORITY: P3
:END:

From TASK-2GS7V (2026-07-29): the diagnostic 0RCRY added says 'the default shared server (no -L/-S and no TMUX_TMPDIR)' while the socket in the measured failure actually came from $TMUX — which the message never mentions, though 0RCRY's own gotcha records that $TMUX selects the socket. One line in crates/orgasmic-drivers/src/modes/tmux.rs: when $TMUX is set and no -L/-S was passed, the selection description names $TMUX and its socket path. Keep the existing wording for the genuinely-default case.

* BACKLOG TASK-3J1YB NativeRuntimeMeta.credential_mode agrees across the recovery-claim round trip by coincidence of matching None literals
:PROPERTIES:
:ID: TASK-3J1YB
:PRIORITY: P3
:END:

From TASK-QRTT8's audit (2026-07-29): Lifecycle::NativeRuntime has no credential_mode field (session.rs:422-431), so recovery_claim.rs:1730 and :2043 hard-code None when rebuilding NativeRuntimeMeta from a session, and api.rs:8303 hard-codes the same None into planned_native_runtime. The two sides of the claim-verification != agree BY COINCIDENCE of matching literals, with no test pinning that they must. If either side ever starts carrying the real mode (the claude adapter already writes Some into the meta at launch), claim verification silently starts rejecting valid claims. Ask: either persist the mode in Lifecycle::NativeRuntime so both sides carry the truth, or pin the current both-None normalization with a test naming the invariant and a comment at each literal pointing at the other. Small; the value is that the coincidence stops being silent.

* BACKLOG TASK-7A5GH Cross-process slow-start matrix: two real orgasmic serve processes racing one temp home
:PROPERTIES:
:ID: TASK-7A5GH
:PRIORITY: P2
:END:

From TASK-5P60H (2026-07-29): its in-process concurrent-start matrix proves one lock owner / one listener / classify-not-refuse, but its actors share one process, so pid divergence cannot be reproduced — single ownership is asserted where recorded (lock file, boot record, incumbent status, one bound socket) rather than by pid inequality. A spawned-binary version (two real orgasmic serve processes on a temp home, compressed budgets via env seams) closes that stated limitation. Bigger, slower harness — worth doing when the boot protocol is next touched, or as the integration half of the boot-audit program. Also fold in 5P60H candidate 4: a nightly macOS launchd smoke proving ThrottleInterval against real launchd (not its documentation) belongs with the WE0Q7/nightly infra once the operator pushes the workflows.

* BACKLOG TASK-FWB5H The reviewer stage (rmux/codex) is un-isolated from the operator's MCP servers, and no codex flag can fix it
:PROPERTIES:
:ID: TASK-FWB5H
:PRIORITY: P2
:END:

From TASK-NYF7Z (2026-07-29), a controlled negative result, not a closure: codex-cli 0.144.5 has NO MCP-suppression flag (full --help enumerated; --strict-config is schema strictness, a false friend), and -c 'mcp_servers={}' deep-merges to a no-op — proven with a control showing -c CAN mutate and add servers, so the non-effect is real merge semantics. The only working route (CODEX_HOME redirect) strips stored auth, the same trade that disqualified claude's --bare. Per-server enabled=false is a fails-open blocklist. So rmux/codex reviewers keep live handles to the operator's 9 configured codex MCP servers (linear, mem, plan, playwright, arcadedb, node_repl, svelte enabled) under bypassed approvals. The finding is recorded at the codex arm of build_spawn_plan and pinned by codex_argv_composes_no_mcp_isolation_flag_because_none_exists. Ask: track the asymmetry; re-check on every codex-cli bump (this is a 0.144.5 measurement); consider upstream feature request or a documented operational rule (reviewer personas + MCP exposure). Do not ship the fails-open blocklist or the auth-stripping redirect without explicit decision.

* BACKLOG TASK-WT09J Document (or close) the acp-vs-rmux divergence: acp pushes --safe-mode --strict-mcp-config, rmux now pushes only the latter
:PROPERTIES:
:ID: TASK-WT09J
:PRIORITY: P3
:END:

From TASK-NYF7Z (2026-07-29): the acp claude path (NativeLogin arm, adapters/claude.rs:515-527) pushes --safe-mode --strict-mcp-config; the rmux path now pushes only --strict-mcp-config, deliberately (safe-mode's hooks/plugins/CLAUDE.md suppression was in-scope-refused, and CLAUDE.md discovery is actively wanted for TUI workers). That leaves an UNDOCUMENTED difference between two paths running the same personas. Either argue rmux should take --safe-mode too, or record at both push sites why the TUI path wants what the acp path suppresses. Documentation-plus-decision task; tiny code delta if any.

* BACKLOG TASK-KVRZ9 worker-server survives a group TERM: measure whether production's reap grace can return early with descendants still up
:PROPERTIES:
:ID: TASK-KVRZ9
:PRIORITY: P3
:END:

From TASK-BCYMM (2026-07-29): the pre-suite sighting showed the exec -a worker-server child OUTLIVING a group TERM that took its siblings — BCYMM's fixture reap is immune (unconditional KILL), but production's subprocess_stream_json::reap_process_group TERMs a group containing exactly this process shape (cursor-agent's node worker-server) and waits on the LEADER as its liveness proxy. Measure whether the 2s grace can return early while a worker-server descendant is still alive; if yes, the wait should cover the group (poll the pgid member count, or KILL unconditionally after grace like the test twin). Small, measurement-first.

* BACKLOG TASK-EKF7T Whole-file session readers remain where lifecycle facts suffice: the completion watcher re-reads the growing transcript on every poll
:PROPERTIES:
:ID: TASK-EKF7T
:PRIORITY: P2
:END:

From TASK-7QM8M (2026-07-29), the boot path's siblings: (1) spawn_dispatch_completion_watcher (api.rs:5875 at its HEAD) calls read_session_file on EVERY POLL while waiting for release — the whole growing transcript, repeatedly; strictly worse than the boot path 7QM8M fixed, which paid it once. It needs only terminal lifecycle events (the scanner's tail window). (2) stage_outcome_from_session (api.rs:6394) reads whole for a stage's terminal outcome — one read, lifecycle-only consumption, same substitution. (3) The strictness decision 7QM8M surfaced: a live run with one torn transcript line is now boot-reattachable and inventory-visible but still UNREADABLE to these whole-file callers (read_session_file is all-or-nothing) — decide whether that strictness is right for any caller that only wants lifecycle facts, and state it where the callers live. Apply 7QM8M's scan_session_lifecycle substitution with its final_envelope_retained discipline; measure the watcher's per-poll cost before/after on a large live transcript.

* BACKLOG TASK-5NJR4 Bounded boot reattach declines a truncated file whose live acquire sits in the unread gap — decide the recovery
:PROPERTIES:
:ID: TASK-5NJR4
:PRIORITY: P3
:END:

From TASK-7QM8M (2026-07-29), the deliberate rule-1 boundary: a truncated session file whose newest retained segment does not own the file's end yields NO candidate — including the shape where the live run's own Acquire/RunMeta sit in the unread middle. The whole-file read would have reattached it. Only legacy residue files can have that shape today (manager_launch_ids gives external/terminal launches their own files), and the run stays visible as recoverable through the inventory path. Options 7QM8M named: a larger tail window for manager-shaped files, or a bounded backwards seek for the newest Acquire when rule 1 fails. Decide with a constructed legacy-shaped fixture; do not widen silently.

* BACKLOG TASK-19K68 daemon_client renders every 409 with the node-write CONFLICT sentence — wrong subject for artifact/lease/recovery conflicts
:PROPERTIES:
:ID: TASK-19K68
:PRIORITY: P3
:SOURCE: TASK-RB1ZN
:END:

* BACKLOG TASK-ED54V UI isRunGoneError is 404-only: a 409 release conflict lands as a raw-JSON error toast instead of 'run is shutting down; retry'
:PROPERTIES:
:ID: TASK-ED54V
:PRIORITY: P3
:SOURCE: TASK-RB1ZN
:END:

* BACKLOG TASK-RRETJ StallableRmuxEndpoint pins RMUX_TMPDIR only: SDK half isolated, driver-spawned rmux CLI half reaches the live server inside a worker pane ($RMUX outranks RMUX_TMPDIR)
:PROPERTIES:
:ID: TASK-RRETJ
:PRIORITY: P3
:SOURCE: TASK-SZJ2B
:END:

* BACKLOG TASK-4H33V Pin every rmux-gated test workspace-wide to the owned rmux endpoint (supervisor::tests inherit isolation only when an artifact test ran first in-process)
:PROPERTIES:
:ID: TASK-4H33V
:PRIORITY: P3
:SOURCE: TASK-SZJ2B
:END:

* BACKLOG TASK-1SVST App crashes to the top-level error boundary: undefined is not an object (evaluating 't.sections.map')
:PROPERTIES:
:ID: TASK-1SVST
:PRIORITY: P2
:READ_SCOPE: ui/src/**
:TEST_CMD: cd ui && npm test
:WRITE_SCOPE: ui/src/**
:END:

Reported by the user 2026-07-29 from a running app build: the desktop window renders the top-level error boundary ("Something went wrong!" / "Hide Error") with the minified message `undefined is not an object (evaluating 't.sections.map')` and nothing behind the banner — so a top-level view dies during render, not an isolated panel. `t` is a bundler-renamed local; the real signal is `.sections.map` reached on undefined. Unguarded call sites of that shape today: ui/src/components/orgdoc/NodeDocEditor.tsx:41 (`toDraft`, `doc.sections.map`) and :63 (`effectiveFields`), and ui/src/components/PromptStudioView.tsx:423 (`layered.sections.map`); the same pass should look at `doc.properties.map` (NodeDocEditor.tsx:42) and `parseOrgSourceNodes(...).sections.find` (node-views/orgNodes.ts:89,111). Wanted: which payload actually produces the undefined and why (daemon response shape, render-before-load, or version skew between app and daemon), a fix at that source rather than a `?.` at the call site, a regression test for the payload shape that crashed, and enough context in the error boundary (view or component name) that the next minified crash is diagnosable without guessing. Reproduction is unknown — first ask the user which screen they were on.

* BACKLOG TASK-ADHWZ Concurrent structured-state writes can return success while losing sibling task headings
:PROPERTIES:
:ID: TASK-ADHWZ
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-core/**, crates/orgasmic-daemon/**, crates/orgasmic-cli/**
:END:

** Description

Three distinct =orgasmic task create= calls were issued concurrently against
the same project and =backlog.org= on 2026-07-29. All three returned success:

- TASK-95SGV.1 → =tx-20260729-orgasmic-3946=
- TASK-JQ8AV.1 → =tx-20260729-orgasmic-3948=
- TASK-FZB6T.1 → =tx-20260729-orgasmic-3947=

Immediately afterwards, only TASK-JQ8AV.1 existed in =backlog.org= and the
daemon returned 404 for updates to the other two. The missing headings had to
be recreated sequentially with new idempotency keys. This is silent successful
write loss at the daemon's structured state authority boundary.

** Acceptance

- Reproduce with deterministic concurrent creates of distinct task ids that
  target the same state file.
- Every successful response has exactly one durable heading and matching tx
  after the requests finish and after reindex/restart; no last-writer-wins loss.
- Serialize or compare-and-swap the entire read/transform/write transaction at
  the central writer boundary, not at individual CLI callers.
- Cover concurrent create/create, create/update and update/update operations
  against the same Org file, including response loss and idempotent retry.
- A stale transform must retry from the new file version or fail explicitly;
  it must never report success for content absent from the committed file.
- Preserve the existing sanctioned git/human concurrent-writer rules and tx
  inode-swap protection.
- Add an injection proof and run focused writer/API tests, fmt, and clippy with
  warnings denied. Do not run any billed harness test.

** Incident Evidence

The manager observed the three successful CLI responses directly, then verified
the raw task files with =rg=. Sequential restore txs are
=tx-20260729-orgasmic-3950= and =tx-20260729-orgasmic-3952=.

* BACKLOG TASK-3TQ0R Extend the driver-resolution fence to daemon integration tests
:PROPERTIES:
:ID: TASK-3TQ0R
:IMPLEMENTS: TASK-3NJ9K
:PRIORITY: P2
:TEST_CMD: cargo test -p orgasmic-daemon
:WRITE_SCOPE: crates/orgasmic-daemon/**
:END:

** Description

TASK-3NJ9K routed daemon driver resolution through =crate::driver_resolution=
and fenced it under =cfg(test)=: =resolve_driver= default-denies harness-exec
modes, and =resolve_launch_driver= additionally refuses a mux address whose
harness that mode would exec.

=cfg(test)= is enabled only while compiling the lib's own test harness. An
integration test in =crates/orgasmic-daemon/tests/= links the lib built
*without* it, so both functions there are the production pass-through. The
entire fence — default-deny, the panic, the launch seam — is absent for that
whole body of tests.

This is not hypothetical reach. =tests/dispatch_preflight.rs= boots a real
daemon and =POST=s =/dispatch= with ={"mode": "acp-stdio", "harness":
"claude"}=, i.e. the ordinary dispatch surface with a provider address, over
HTTP. Others carry provider harnesses too: =tests/integration.rs=
(=acp-stdio=/=codex=), =tests/dispatch_credential_plan.rs=,
=tests/recovery_fault_restart.rs= (=tmux=/=claude= session fixtures driving boot
reattach).

Audited 2026-07-30: those dispatches are refused before any spawn, because each
test engineers an unusable credential and asserts the 400. That is genuinely
host-independent — =classify_bare_api_key= refuses an empty key without reading
the Keychain — so **no integration test is known to spawn a provider today**.

The defect is structural, not a live leak: the safety is a property each test
supplies for itself, exactly the shape of the ="unreachable without a hermes
binary"= comment that caused the original incident. Nothing stops the next
integration test from posting the same address without engineering a refusal,
and the fence that exists to catch precisely that cannot see it.

** Acceptance

- A daemon integration test that resolves or launches a real harness-exec
  address is refused the same way a lib unit test is, with the same named,
  host-independent message pointing at the stub.
- The mechanism cannot be =cfg(test)=. Options to weigh and decide in writing:
  a runtime test-profile flag the daemon sets when booted by
  =Daemon::run(.., test_options())=; a cargo feature the =[dev-dependencies]=
  self-dependency enables; or moving the launch seam behind a trait the test
  harness substitutes. Whichever is chosen must not be settable in a production
  build.
- Production behaviour is byte-for-byte unchanged, and the guard costs nothing
  in a release build.
- The existing integration coverage keeps working: the preflight tests must
  still reach their 400 refusals, and the recovery/reattach tests must still
  resolve mux drivers for sessions they created themselves.
- A regression test proves the guard from inside =tests/=: an integration test
  that asks for =acp-stdio=/=claude= is refused, on any host, with and without
  the provider binary installed.
- Audit every file under =crates/orgasmic-daemon/tests/= for provider
  addresses; migrate those whose address is incidental to the stub, and list in
  the worklog the ones that legitimately need a real address and why.

** Prior Evidence

TASK-3NJ9K fence: =crates/orgasmic-daemon/src/driver_resolution.rs=, merged
=75fe95f=. Launch-seam gap found in manager review and closed in =32c3f8c=.
Original incident: =tx-20260730-orgasmic-3992=. Integration reach:
=crates/orgasmic-daemon/tests/dispatch_preflight.rs= around line 200.

* BACKLOG TASK-GGWW2 serve's SIGTERM handler races Daemon::run's return, so the graceful-shutdown test flakes    :daemon:flake:
:PROPERTIES:
:ID: TASK-GGWW2
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-cli/src/daemon_lifecycle.rs
:END:

** Description
`sigterm_exits_through_graceful_shutdown_rather_than_default_disposition`
(`crates/orgasmic-cli/tests/daemon_lifecycle.rs:319`) fails intermittently with
"serve died on SIGTERM's default disposition".

Root cause is documented in the test's own comment and is TASK-Q07Y5's shape:
`serve` registers its SIGTERM handler AFTER `Daemon::run` returns, so a signal
delivered in that window hits the default disposition and kills the process.

MEASURED [2026-08-02] by the TASK-4CSMY implementer, as the base-commit control
the flake convention requires — isolated runs, one worktree held constant:

| tree | isolated runs | failures |
|------|---------------|----------|
| base 9017514 | 8 | 3 |
| with TASK-4CSMY | 5 | 1 |

So it is pre-existing and load/timing sensitive, not a regression. It was NOT
added to `verify/flake-registry.toml` at the time, correctly: that file requires
an OPEN owning task, and an entry with no owner is a permanent exemption.

** Work
1. Register the panic in `verify/flake-registry.toml` against this task id, so
   the suite classifies it rather than reporting an unexplained REAL red.
2. Fix the race: install the SIGTERM handler before `Daemon::run` can return,
   so there is no window in which the default disposition applies.
3. Remove the registry entry in the same change that fixes it — a registered
   flake outliving its fix is the next person's false signal.

** Acceptance
- The handler is installed before the window opens; no ordering-dependent gap
  remains between `Daemon::run` returning and the handler being live.
- The test passes 20 consecutive isolated runs in one worktree.
- `verify/flake-registry.toml` carries no entry for this test once fixed.

* BACKLOG TASK-8XPRR orgasmic-pane-* FIFO directories accumulate in the temp root across test runs    :testing:drivers:
:PROPERTIES:
:ID: TASK-8XPRR
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-drivers/src/modes/tmux.rs
:END:

** Description
TASK-4CSMY gave the tmux driver a pane-liveness channel backed by a private FIFO
in a `tempfile::TempDir` (`pane_output_fifo`, `orgasmic-pane-*` prefix). The
directory is dropped when the watch task ends or is aborted — release, Drop, and
task abort all covered.

The reviewer of that change measured 112 INACTIVE `orgasmic-pane-*` directories
already present in the temp root, in five clusters spanning 15:33-16:28, with no
open `lsof` handles and no surviving `cat` processes. Its own controlled
acquire/release smoke and its explicit Drop-path test each added ZERO entries.

So the production paths clean up correctly and this is NOT a per-run daemon leak.
The residue is from test processes that die without running Drop — the timing
matches the three full-suite runs done during the TASK-4CSMY integration. A
`TempDir` cannot clean up after SIGKILL, and the suite kills process groups.

** Why it is worth a task anyway
It accumulates without bound in `$TMPDIR`, and a directory named after our
driver is the first thing someone will blame when the temp root fills. Two
things are worth deciding rather than leaving implicit: whether the driver
should sweep stale `orgasmic-pane-*` dirs on startup (cheap, bounded, and it
makes the leak self-healing), and whether the tests that spawn panes should reap
their own residue.

** Work
1. Reproduce: run the daemon suite and count `orgasmic-pane-*` before/after.
   Identify which test path leaves them (Drop skipped vs process killed).
2. Decide sweep-on-startup vs test-side cleanup, and say why in the commit.
3. If sweeping: only remove directories with no open handles and an mtime older
   than a conservative floor — never one that could belong to a live run.

** Acceptance
- The mechanism is named with evidence, not inferred.
- A full suite run leaves no unbounded growth in the temp root.
- Nothing can remove a FIFO directory belonging to a live pane.

* BACKLOG TASK-T7KSH inspect_rmux_binary execs an unbounded child on the daemon's probe path, defeating the JQ8AV.1 child deadline    :drivers:rmux:hardening:
:PROPERTIES:
:ID: TASK-T7KSH
:IMPLEMENTS: TASK-JQ8AV
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-drivers/**
:END:

CONFIRMED BY AN INDEPENDENT REVIEWER [2026-08-03], =tx-20260802-orgasmic-4104=
(rmux / codex / gpt-5.6-sol / high), reviewing =9017514..7ade8af=:

#+begin_quote
MEDIUM correctness — not an independent tmux-default blocker:
=crates/orgasmic-drivers/src/modes/rmux.rs:207-237= and
=crates/orgasmic-daemon/src/supervisor.rs:4881-4888=. rmux pane probing still
runs =probe_rmux_binary()=, whose =inspect_rmux_binary= executes =rmux -V=
through unbounded =Command::output()=. The outer five-second =spawn_blocking=
timeout cannot cancel that child or reap it, so a wedged rmux executable leaves
the blocking task and process alive. This confirms the declared residual as a
live hole in the same ownership defect class and warrants its own task.
#+end_quote

So this is no longer one author self-report — two independent readers, one of
whom measured it, reached the same conclusion from the source.

PRIORITY ARGUMENT, unchanged in substance but sharper in context: rmux is
currently the ONLY pane transport with a liveness channel in the installed
daemon (=57e35de= has JQ8AVs rmux channel, not TASK-4CSMYs tmux one), so every
dispatch on this host goes over rmux and therefore over this probe path. The
exposure is not hypothetical while that remains true.

The reviewers fix direction matches the acceptance already recorded here: owned
child process group, absolute deadline, group kill, reap, bounded stdout drain,
and a wedged-=-V= concrete-PID regression.

** Description

TASK-JQ8AV.1 funnelled all four supervisor probe subprocesses through
=probe_command_stdout=, which gives each child its own process group, enforces
=PROBE_CHILD_DEADLINE= (2s) against the CHILD, and SIGKILLs the group then reaps.

One probe subprocess escapes that funnel, and it is on the production rmux path.
=orgasmic-drivers/src/modes/rmux.rs::inspect_rmux_binary= runs
=StdCommand::new(&path).arg("-V").output()= with no deadline. The daemon reaches
it through =pane_probe_command= -> =probe_rmux_binary=.

** Evidence

Measured by the JQ8AV.1 implementer while building the hung-mux regression, not
inferred. With a shim that wedged on EVERY invocation rather than only on the
server-contacting verb:

- the supervisor sweep took *5.012s* — the outer =WORK_PROBE_TIMEOUT=, NOT the
  2s child deadline, proving the child was outside the funnel;
- the test binary then took *120s to exit*, i.e. the tokio runtime was waiting on
  a blocking task stuck in a child the funnel never saw.

The shipped test was then deliberately weakened to answer =-V= normally, which
keeps =inspect_rmux_binary= out of its timed section. That is realistic — =rmux -V=
is answered locally and never contacts a server, so a wedged rmux SERVER does not
hang it — but it means *the shipped regression cannot catch this*.

** Why it matters

A wedged or hung rmux BINARY (as opposed to a wedged server) reintroduces exactly
what JQ8AV.1 set out to eliminate: a blocking-pool thread and a live subprocess
leaked per sweep, at one sweep every =RUN_TIMEOUT_CHECK_INTERVAL= (50ms), for as
long as the condition lasts. rmux is currently the transport of choice for
dispatches on this daemon, because it is the only pane transport with a liveness
channel in the installed runtime.

The tmux path has no equivalent exposure: =real_tmux_on_path()= only stats PATH.

** Acceptance

- =inspect_rmux_binary= (and any sibling in orgasmic-drivers that the daemon
  execs on a probe path) runs under an explicit deadline with kill-and-reap,
  owning its child the way =probe_command_stdout= does.
- A deterministic regression proves it, with a shim that wedges on EVERY
  invocation including =-V= — the case the current test deliberately avoids.
  Assert supervisor latency strictly below =WORK_PROBE_TIMEOUT= and that the
  recorded child pid disappears.
- The test binary exits promptly afterwards; a 120s teardown is itself the
  symptom and should be asserted against.
- fmt, clippy -D warnings, and the focused drivers + daemon tests green. Do not
  run the billed harness test (TASK-099T6).

* BACKLOG TASK-WPC6G gotchas.org is off the node write surface, so every entry in it is a rule-violating hand edit    :runtime:cli:process:
:PROPERTIES:
:ID: TASK-WPC6G
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-core/**, crates/orgasmic-daemon/**, crates/orgasmic-cli/**
:END:

** Description

=.orgasmic/entry.org= states the write rule plainly: when =orgasmic status=
succeeds, =.orgasmic/= state is written through =orgasmic ...= CLI commands and
never by hand. The active goal repeats it ("never hand-edit .orgasmic state").

=gotchas.org= cannot obey that rule, because it is not on the surface at all:

- =orgasmic node body set|append|unset= takes =--kind= from
  {decision, architecture, glossary, project, task, goal, handoff}. There is no
  gotcha kind, and the file has no node =:ID:= — its headings are bare =**=
  entries under a single =* Gotchas= parent.
- Nothing in =crates/orgasmic-cli/src/= addresses it. The only core reference is
  the scaffold template in =projects.rs:525=.

So the only way to add a gotcha is to edit the file directly, which is exactly
what the previous manager session did in =d1a41c1= (66 added lines, no
=graph.gotchas.edited= tx — the tx entries in that commit are unrelated daemon
writes that happened to be uncommitted).

** Why this matters more than the usual missing-verb gap

=AGENTS.md= routes every agent through =orgasmic entry=, and the entry router
names =gotchas.org= as required reading before source edits. The
manager-dispatch and manager-implementer paths both say to read it first. It is
one of the highest-traffic documents in the project, and it is the one document
no agent can write through the sanctioned path.

The practical consequences are already visible:
- gotcha edits carry no tx, so they are invisible in the ledger and cannot be
  attributed or reverted through the normal surface;
- they bypass the OCC/base-version check, so two concurrent writers silently
  clobber;
- an agent that obeys the write rule literally is forced to DROP the knowledge,
  which is the worst outcome of the three.

** Related

Same class as TASK-P0Q5C (no CLI title surface for
decision/architecture/glossary/project/handoff) and TASK-J2RZT (handoff/goal
brought onto the node surface). This is the remaining unaddressed file.

** Acceptance

- =gotchas.org= is addressable through the daemon node surface with the same
  OCC + structural guard every other node gets, and a =tx= is recorded per edit.
- A CLI verb can append an entry and remove one by name, mirroring
  =node body append= / =node body unset --section=.
- The parity test that asserts every daemon edit op is reachable from some CLI
  verb (added by TASK-CB6GQ) covers the new op.
- Decide and record whether existing bare =**= entries get stable ids, or
  whether entries stay addressed by heading text; a heading-text address needs a
  duplicate-title refusal.
- fmt, clippy -D warnings, focused daemon + cli tests green.

* BACKLOG TASK-NTNK0 The rmux shim's tmux shadowing is documented but not defaulted, and two workers lost tmux coverage to it in one session    :drivers:rmux:testing:dispatch:
:PROPERTIES:
:ID: TASK-NTNK0
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-drivers/**, crates/orgasmic-daemon/**
:END:

CORRECTION [2026-08-03], recorded before this task is worked so nobody
rediscovers it as new: *this trap is already documented.*
=gotchas.org:549= — "Suites run from inside an rmux worker pane need
=ORGASMIC_ALLOW_MISSING_TOOLS=tmux=" — states that the worker PATH =tmux= is the
rmux shim and counts as ABSENT, that the resulting red "is the honesty
mechanism, not a defect", and that a genuine tmux arm needs the real binary
resolved ahead of the shim via K4G1Ds scratch-symlink recipe. It even notes
that =tmux -V= through the shim lies, which is exactly what the FZB6T reviewer
saw when it read 3.4 against a real 3.6a.

So the description above overstates the novelty. What is actually new from
2026-08-02/03, and what this task should be scoped to:

1. IT BIT TWO WORKERS IN ONE SESSION AND ONLY ONE HANDLED IT. The TASK-4CSMY.1
   implementer diagnosed it and stripped the shim before running every tmux
   gate. The TASK-FZB6T reviewer did not: it recorded "10 mux-gated tests not
   run" as *environment-blocked* and moved on, so its verdict rests on a smaller
   test set than it appears to. Neither worker had been told; the gotcha did not
   reach either of them, because a dispatch brief does not carry it and the
   entry router only points at =gotchas.org= generally.
2. THE COUNT DRIFTED, 9 -> 10 gated tests, so the entry is also stale in detail.
3. THE UNDERLYING DESIGN QUESTION IS STILL OPEN and the gotcha does not settle
   it — it documents a workaround. Should the rmux client shim occupy the name
   =tmux= on the worker PATH at all? A shim that shadows a real binary of a
   DIFFERENT transport is a trap that has now cost coverage twice.

REVISED ACCEPTANCE — pick one of these deliberately rather than all of them:
- (a) Make the workaround the default for worker-run gates, so a worker cannot
  lose tmux coverage by not having read a gotcha; or
- (b) Stop shadowing: scope the shim to the rmux client path, or name it
  something other than =tmux=, or append rather than prepend; or
- (c) Decide it is correct as-is and make the SENTINEL louder in a worker
  context — e.g. have it name the shim explicitly and print the fix — so the
  next reviewer classifies it as waived coverage rather than as an environment
  block.
In all three cases: refresh the 9 -> 10 count, and do NOT weaken
=required_test_tooling_is_present=. It is the only reason any of this is
visible.

** Description

A worker dispatched over the rmux driver gets a PATH whose FIRST entry is the
rmux shim directory, e.g.
=/var/folders/.../rmux-shim-2d4a7c8363aa8a2b/=. That directory contains a file
named =tmux= which is a SYMLINK TO =~/.local/bin/rmux=.

So inside any rmux dispatch, =tmux= on PATH is not tmux. =real_tmux_on_path()=
correctly refuses it, =tmux_spawn_usable_for_test()= returns false, and the ten
tmux-gated tests in the daemon lib DO NOT RUN. A real tmux exists at
=/opt/homebrew/bin/tmux= (3.6a) and is shadowed.

** How it surfaced, twice, independently

- TASK-FZB6T reviewer (rmux/codex): "The daemon tooling sentinel failed at exit
  101 because =tmux_spawn_usable_for_test= returned false and reported 10
  mux-gated tests not run. =tmux -V= itself reports 3.4, so this is classified
  environment-blocked at live-session creation, not a source regression."
- TASK-4CSMY.1 implementer (rmux/claude): measured the cause, stripped the shim
  directory from PATH and unset =TMUX=/=TMUX_PANE=, after which "the sentinel
  passes and all ten tmux-gated tests run". It ran every tmux-touching gate that
  way, and noted that without it "the tmux arms would have reported =ok= while
  skipping".

Neither worker was looking for this. Both found it because
=required_test_tooling_is_present= exists.

** Why this is P1 rather than a curiosity

The standing dispatch transport on this host is rmux — it is the only pane
transport with a liveness channel in the installed daemon — so this applies to
essentially every worker we run right now.

The failure mode is SILENT-BY-DEFAULT. The sentinel is the only thing standing
between us and a worker truthfully reporting "N/N focused tests green" while ten
tmux tests never executed. That is precisely the shape of evidence failure this
project has been chasing all week: a green that means less than it appears to.

It is also NOT the PATH theory TASK-R2HDN measured false. That one was about
=~/.local/bin= being ABSENT. This is the opposite: =~/.local/bin/rmux= PRESENT
under the name =tmux=, ahead of the real binary.

** Acceptance

- An rmux dispatch does not shadow a real =tmux= binary for the worker process.
  Decide the mechanism deliberately: the shim exists for a reason, so either
  scope it so it is only consulted by the rmux client path, name it something
  that is not =tmux=, or append rather than prepend it and let a real tmux win.
- Whatever the fix, a test proves that a worker environment produced by the rmux
  driver resolves =tmux= to a binary that =real_tmux_on_path()= accepts when one
  exists on the system PATH.
- =required_test_tooling_is_present= keeps failing loudly when tooling really is
  missing — do not weaken the sentinel to make this go away. It is the only
  reason we know.
- Record the trap in =gotchas.org= (blocked on TASK-WPC6G for a write verb; a
  hand edit is currently the only route).

** Related

TASK-WPC6G (no write surface for gotchas.org). TASK-R2HDN (the different, false
PATH theory). TASK-SJNNA (daemon should scrub =TMUX= before spawning tmux) is
adjacent: same family of environment leakage between mux transports.

* BACKLOG TASK-BX5SR One bad mutation kills the daemon writer task, and every write in the process fails until restart    :daemon:writer:availability:
:PROPERTIES:
:ID: TASK-BX5SR
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/**
:END:

** Description

MEASURED DURING A LIVE OUTAGE, 2026-08-02, daemon boot =f8f67c0c=.

Timeline from =~/.orgasmic/logs/daemon.out.log=:

: 22:20:22.139  ERROR api: writer transaction failed
:                     error=cached mutation lacks its recorded identity
: 22:20:22.770  WARN  supervisor: session append failed
:                     error=writer task is gone  run_id=run-20260802T215608-...
: 22:21:45.489  ERROR api: writer transaction failed error=writer task is gone
: 22:21:52.686  ERROR api: writer transaction failed error=writer task is gone

One transaction failed on a cache-identity check, and 0.63s later the WRITER
TASK ITSELF WAS GONE. From that moment every write in the daemon failed:
=task update --state= returned 500 =failed to apply changes=, =manager
dispatch-close= appended its tx but could not update the lifecycle, and the live
TASK-FZB6T.2 workers session appends were dropped on the floor.

Reads stayed healthy throughout — =orgasmic status=, =task get= and
=dispatch-status= all answered normally — so from the outside the daemon looked
FINE. That is the worst property of this failure: a manager can keep issuing
commands and reading plausible state while nothing is being persisted.

** What it cost

The in-flight implementer for TASK-FZB6T.2 was guaranteed to lose its report,
because =orgasmic dispatch finalize= is a WRITE. Recovering the writer required a
daemon restart; the restart released the run and the rmux pane died with it, so
the worker was killed mid-task with 2,452 insertions uncommitted. The manager
preserved them as =d982af1= on =task-fzb6t.2-impl=.

Note the shape: this is the SECOND report lost on this task in one session, by
two completely different mechanisms (the first was the tmux stall ceiling). The
=dispatch finalize= contract has no durable fallback when the daemon cannot
write.

** Two defects, and they should probably be separated

1. *A failing transaction must not kill the writer.* Whatever
   "cached mutation lacks its recorded identity" is, it is a per-request error
   and belongs in a 500 for THAT request. Taking the writer down converts one
   bad request into a total write outage. Same class as TASK-KKGKM, where one
   unreadable project killed the whole runtime.
2. *A dead writer must be loud and must be recoverable without a restart.*
   =orgasmic status= reported =parse_errors: 0= and looked healthy while every
   write was failing. At minimum status should report writer liveness; better,
   the writer should be supervised and restarted in place.

Note =orgasmic restart= itself degraded: it reported
=daemon pre-stop drain returned 500: failed to drain writer before restart=
and warned that mid-flight releases may be unreported — correct behaviour given
the state, but it means the recovery path is also compromised by the same fault.

** Acceptance

- A writer transaction that fails for any reason returns an error for that
  request and leaves the writer task running. A regression injects a failing
  mutation and proves the NEXT write succeeds.
- =orgasmic status= (and =doctor=) report writer liveness, so a dead writer is
  visible without reading daemon logs.
- The writer is supervised: if the task does exit, it is restarted in place, or
  the daemon fails loudly rather than serving reads as if healthy.
- Root-cause =cached mutation lacks its recorded identity= specifically: what
  produces a cached mutation with no recorded identity, and why is it reachable
  from =manager dispatch-close=? The outage began on that exact call.
- Consider a durable fallback for =dispatch finalize= so a worker report is not
  lost when the daemon cannot write. Related: TASK-P9T4N (multi-tx writer
  transaction so dispatch-close is one commit).

* BACKLOG TASK-Q8JEV The installed-harness spinner guard goes silent if Claude ships a sub-1-MiB launcher    :testing:daemon:
:PROPERTIES:
:DEPENDS_ON: TASK-4CSMY
:ID: TASK-Q8JEV
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-daemon/src/supervisor.rs
:END:

** Description

=status_spinner_glyphs_cover_the_installed_claude_frame_set= ignores candidate
binaries smaller than 1 MiB (=supervisor.rs:10540-10576=) and skips entirely
when no qualifying binary exists (=:10718-10724=).

Both choices are correct today and were deliberate: the floor stops a tiny
=claude= PATH shim — which the rmux dispatch environment really does put ahead of
the real binary (TASK-NTNK0) — from tripping the needle-absent FAILURE, and the
skip avoids reddening every claude-less host including CI.

The residual, in the round-4 reviewers words: if Anthropic later ships a real
sub-1-MiB launcher rather than a symlink to the bundled executable, THIS AUDIT
BECOMES SILENT. The guard would skip, the table would stop being checked against
the harness, and the failure mode is the one TASK-4CSMY.2 exists to prevent — a
stale allowlist that kills healthy workers — with nothing noticing.

Measured on this host at review time: =~/.local/bin/claude= canonicalizes to a
256,908,272-byte executable, so the floor does not skip it today.

** Options, per the reviewer

- a hermetic fixture: pin a captured copy of the frame arrays as test data so the
  audit runs everywhere and the installed-binary check becomes a bonus;
- a dedicated claude-present canary that fails loudly when the guard skips on a
  host where claude IS installed but no candidate qualified;
- revisit the floor: identify the product by something other than size.

** Acceptance

- The audit cannot go silent on a host where Claude Code is installed.
- A tiny PATH shim still does not trip a false failure (do not regress
  TASK-NTNK0s interaction).
- Claude-less hosts, including CI, stay green without a ToolRequirement.

* BACKLOG TASK-KF223 Two allowlisted status glyphs are capture-derived and unguarded against role drift    :daemon:drivers:
:PROPERTIES:
:DEPENDS_ON: TASK-4CSMY
:ID: TASK-KF223
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-daemon/src/supervisor.rs
:END:

** Description

=STATUS_SPINNER_GLYPHS= now holds eight glyphs. Six are extracted from the
installed claude binary and are covered by
=status_spinner_glyphs_cover_the_installed_claude_frame_set=. Two are NOT:

- =U+25CF= — claude, from a live pane capture. The binarys frame array does not
  contain it, so the guard cannot check it. The TASK-4CSMY.2 author noted ~10
  other =U+25CF= sites in the binary and judged that chasing which code path
  paints it was out of scope for a false-kill fix.
- =U+25E6= — codex, from the round-3 reviewers live pane capture. Never
  extracted from any binary.

Neither is a defect today: both have real measured live-pane provenance, and
they are in the table for good reason. The gap is that if either harness changes
the ROLE of that glyph — from a status row to a transcript bullet — nothing
notices, and the failure direction flips to a false RESCUE: an at-rest pane whose
transcript starts with that glyph would be classified Working and survive to the
four-hour ceiling.

That is exactly the defect class TASK-JQ8AV.1 and TASK-4CSMY.1 were both raised
to close, surviving in the two places the new audit cannot reach.

** Work

- Trace the =U+25CF= paint path in the claude binary and, if it is extractable,
  fold it into the same guard.
- Decide how the codex glyph gets refreshed — a periodic re-capture under a named
  task, or extraction from the codex binary if its status row is reachable the
  same way. Note the round-3 reviewer captured it live from a real codex pane, so
  a repeatable capture recipe is feasible; measuring it costs a codex run, not a
  billed claude run.
- If neither is extractable, say so in the table comment next to those two
  entries, so the next reader knows which glyphs are audited and which are
  trusted.

** Acceptance

- Every entry in =STATUS_SPINNER_GLYPHS= is either machine-audited against its
  harness or explicitly annotated as capture-only with its capture date.
- No regression to the false-kill direction: a real live status row must still
  be recognised.

* BACKLOG TASK-GCTMA manager dispatch should refuse or loudly flag a dirty worktree the worker will not see    :dispatch:cli:workflow:
:PROPERTIES:
:ID: TASK-GCTMA
:IMPLEMENTS: TASK-TW3N3
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-cli/**, crates/orgasmic-daemon/**
:END:

** Description

TASK-TW3N3 landed the PROSE half: =shipped/entry/router.org= now states that
=manager dispatch --from= takes a ref, so uncommitted source is invisible to the
worker, and that a workers frozen =.orgasmic= snapshot cannot see the daemons
uncommitted writes either.

Prose is necessary and not sufficient. TW3N3s own acceptance says "a dispatch
that would silently review the wrong tree is either blocked or loudly flagged",
and nothing enforces that today. The most expensive version of the mistake is a
REVIEWER dispatched against uncommitted work: it returns a confident, clean
verdict on code it never read, and the verdict looks exactly like a real one.

** Work

- On =orgasmic manager dispatch=, detect a dirty worktree in the project root and
  name the uncommitted paths the worker will NOT see. Warn at minimum; consider
  refusing for =--kind reviewer= specifically, since a review is the case where
  the wrong answer is confidently wrong.
- Decide the override shape (=--allow-dirty=?) and make the message say what to
  do instead: commit to a branch and pass it as =--from=.
- Cover BOTH halves in the check, not just source: uncommitted =.orgasmic/=
  writes are the half that bites managers most often, because the daemon writes
  there continuously and a manager who has not committed since the last CLI
  mutation is dirty without having typed anything.

** Evidence this is real

Every one of the eight dispatches in the 2026-08-02/03 session required
committing =.orgasmic= state first; the manager did it by habit, not because
anything checked. The session also hit the adjacent trap twice — a continuation
or second review round cannot use the derived branch name and fails with
=a branch named ... already exists=, which is now documented in the same router
section.

** Acceptance

- A dispatch from a dirty project root either refuses or emits a warning that
  names the specific uncommitted paths.
- A reviewer dispatch is treated at least as strictly as an implementer one.
- The guard does not fire on paths the worker legitimately does not need.
- A regression proves both the fire and the no-fire case.

* BACKLOG TASK-48SFW Any orgasmic verb run against a staged ORGASMIC_HOME restarts the real daemon and kills every live dispatch    :dispatch:daemon:runtime:
:PROPERTIES:
:ID: TASK-48SFW
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-cli/**, crates/orgasmic-daemon/**
:END:

SECOND HYPOTHESIS, and it implicates the MANAGER rather than the workers. Added
before the evidence is in, so the record is not shaped by which answer turns out
to be more comfortable.

Both incidents happened while a manager MONITOR was polling =orgasmic status= and
=orgasmic manager dispatch-status= every 90 seconds. There is a documented
behaviour — the memory =orgasmic-runtime-reinstall=, and =install.sh='s trailing
=orgasmic status= — that =orgasmic status= AUTO-SPAWNS a daemon when it does not
find a reachable one. An auto-spawn writes the LaunchAgent, and that would
produce exactly the measured signature: plist mtime equal to the restart instant,
no shutdown lines in the log, and =launchctl runs = 1= because the job was freshly
loaded rather than restarted by KeepAlive.

Under this reading the causal chain inverts. Something makes the daemon briefly
unreachable — a slow request, a drain, load — the polling =status= call misses it,
auto-spawns, and the spawn restarts the daemon and kills every live run. The
workers are victims rather than the cause, and the manager's own monitoring is
the trigger.

THE TWO HYPOTHESES ARE DISTINGUISHABLE, AND THE EXPERIMENT IS RUNNING. As of
=00:16Z= no workers are dispatched, and a watcher samples boot_id and plist mtime
every 75 seconds — polling =orgasmic status= at the same cadence that was live
during both incidents, with the workers removed.

- Daemon restarts during that watch => the workers are exonerated and the polling
  path, or something else ambient, is the trigger.
- Daemon stays stable across a window longer than the ~9 minute incident interval
  => polling alone is not sufficient, and the worker hypothesis stands.

WHICHEVER WAY IT LANDS, ONE FIX IS COMMON TO BOTH and should be built regardless:
no routine, read-only status call should ever be able to start a daemon as a side
effect. A read that mutates the LaunchAgent is a trap for every caller, not only
this one, and it is the same defect shape as the =install.sh= trailing-status
trap already recorded in the runtime-reinstall memory.
EXPERIMENT RESULT [2026-08-03 00:26Z] — the second hypothesis is REFUTED as a
sole cause, and the first stands.

Ten consecutive minutes of stability, =03:16:33= to =03:26:33= local, sampling
=boot_id= and the LaunchAgent plist mtime every 75 seconds with NO workers
dispatched. Every sample identical: boot =2f2fcd57=, plist mtime =1785715985=.

That watch polled =orgasmic status= at essentially the cadence that was live
during both incidents (90s then, 75s here) and ran LONGER than the ~9 minute
interval between them. So polling =orgasmic status= does not, by itself, restart
the daemon. The managers monitoring is exonerated as a sole cause.

What survives: A DISPATCHED WORKER IS A NECESSARY INGREDIENT. That is consistent
with the primary hypothesis — a worker invoking a daemon lifecycle verb, which
rewrites the one real LaunchAgent regardless of =ORGASMIC_HOME= — and it is now
the only reading still standing.

Not yet distinguished, and worth naming rather than glossing: whether the trigger
is the WORKERS OWN command, or something the daemon does on the dispatch path
(spawning a mux, writing a run record) that briefly makes it unreachable to a
concurrent caller which then spawns. The next dispatch is deliberately a
READ-ONLY reviewer carrying an explicit prohibition on daemon lifecycle verbs and
no render-check instruction at all: if it survives, the worker-command reading is
supported; if it dies, the dispatch PATH is implicated rather than anything the
worker chose to run.

Still worth building either way, unchanged: a routine read-only status call must
never be able to start a daemon as a side effect.
DISCRIMINATOR RESULT [2026-08-03 00:4xZ] — THE WORKER-COMMAND READING IS
SUPPORTED, and the dispatch path is exonerated.

The next dispatch after the experiment was deliberately built to separate the two
survivors: ONE worker, read-only reviewer, no render-check instruction, carrying
an explicit prohibition on every daemon lifecycle verb.

IT COMPLETED NORMALLY. Full review delivered, =dispatch finalize= called, and the
LaunchAgent-mtime watch fired no rewrite event for its entire run. Its own report
states: "No daemon start/stop/restart/serve command, no launchctl, and no
forbidden billed test was run."

So a dispatch does NOT restart the daemon by existing. Something a worker CHOSE
to run did. Combined with the earlier ten-minute polling watch, both alternative
explanations are now eliminated:

| hypothesis | verdict | evidence |
|------------+---------+----------|
| manager polling =orgasmic status= auto-spawns | REFUTED as sole cause | 10 min stable, no workers, same cadence |
| the dispatch PATH restarts the daemon | REFUTED | lone read-only worker completed untouched |
| a worker INVOKES a lifecycle verb | STANDS | only reading left; prohibition worked |

WHAT THIS MEANS OPERATIONALLY, and it should go in every brief until the code
fix lands: the prohibition is currently the only thing standing between a
concurrent dispatch fleet and a total loss. It worked on first use. It is also
one sentence in a brief that a worker under momentum can skip, which is exactly
the argument for the refusal being enforced in the CLI rather than requested in
prose — the same reasoning TASK-3CM0Q makes about tier declarations.

MANAGERS OWN CONTRIBUTION, restated now that the cause is known: the briefs
that told workers to "stage an ORGASMIC_HOME from the edited files" were the
likely invitation. That instruction is still CORRECT and still necessary — the
live daemon serves installed content, not the worktree — but it must always
travel with the prohibition and with the safe recipe (run the worktrees own
=target/debug/orgasmic entry= against the staged home; never start a daemon).
ROOT CAUSE, FOUND IN SOURCE [2026-08-03 01:53Z]. Everything above this line was
inference; this is the code path, and it corrects the earlier guidance.

: ensure_running(home)                       daemon_lifecycle.rs:152
:   -> ensure_running_local(home, _)                          :159
:     -> start_via_selected_adapter(home)                     :419
:       -> daemon_service::start(home) => ServiceStart::Persistent

On macOS the persistent adapter WRITES AND LOADS
=~/Library/LaunchAgents/orgasmic.daemon.plist=. That path is PER-USER. It is not
scoped by =ORGASMIC_HOME=, and nothing in the call chain notices that the home it
was handed is not the home the LaunchAgent already serves.

So the rule is much wider than "do not run daemon lifecycle verbs":

*ANY =orgasmic= VERB THAT NEEDS A DAEMON, RUN AGAINST A STAGED =ORGASMIC_HOME=,
RESTARTS THE REAL DAEMON.* The staged home has no daemon listening, so
=ensure_running= fires, the adapter rewrites the one real plist to point at the
staged home, launchd reloads it, and every live run on the machine dies. The
worker does not have to type =daemon start=. It only has to run a normal command
with a different home.

THE EVIDENCE THAT ISOLATED IT. TASK-VEGRV was live in ALL THREE outages and
absent from both dispatches that survived. It died 4-6 minutes in each time, and
its pane capture before the first one reads: "All four gates are passing. Now I
need to test the render output by staging the edited files into a temporary
ORGASMIC_HOME and running the entry command." The concurrency theory does not
survive the data either — four concurrent workers ran from 23:09 to 23:46 with no
outage.

THE MANAGER CAUSED THIS THREE TIMES. The render check was a manager instruction,
in a manager-authored brief, and the "safe" rewrite after the second outage was
also a manager instruction — it still ran an =orgasmic= verb against a staged
home, so it was not safe at all. The prohibition added after outage two forbade
lifecycle verbs and therefore did not cover the actual trigger. Two dispatches
survived it and I read that as the prohibition working; they survived because
they had no render check.

CORRECTED GUIDANCE, until the code refuses:
- NEVER set =ORGASMIC_HOME= to anything other than the ambient one inside a
  dispatch, for ANY =orgasmic= invocation.
- A shipped-content change does NOT need a render check to be verified. The
  parse is covered by =cargo test -p orgasmic-core --test fixtures=
  (=parses_shipped_schema_files=), the verb and flag claims by
  =cargo test -p orgasmic-cli --test cli_parity=, and the injection point by
  reading =inject_default_workflow=. Ask for those instead.
- If a rendered artifact genuinely must be inspected, it has to be produced
  without invoking the CLI against a foreign home — read the shipped files
  directly, or add a unit test that exercises the injector in-process.

THE FIX THIS IMPLIES IS BIGGER THAN THE ORIGINAL TASK. =ensure_running= should
refuse, or at minimum warn loudly, when the home it is asked to start differs
from the home the installed LaunchAgent already serves. A per-user service
mutated as a side effect of a per-home read is a trap for every caller, and the
=install.sh= trailing-status trap already recorded in the runtime-reinstall
memory is the same defect wearing a different hat.
THE SAFE PATTERN, found by the TASK-3CM0Q worker and CONFIRMED BY MEASUREMENT
[2026-08-03]. This is what every test and probe that needs a real daemon should
have been doing all along.

Set =ORGASMIC_DAEMON_URL= on the CHILD process. =ensure_running= returns before
=start_via_selected_adapter= is ever reached, so no adapter starts, no
LaunchAgent is written, and the operator daemon is never touched. The existing
=crates/orgasmic-cli/tests/manager_register.rs= already relies on this; it was
simply never written down as the rule.

=crates/orgasmic-cli/tests/manager_tier_cli.rs= spawns the built binary against a
real booted daemon and walks a full undeclared-read -> declare -> read-back ->
refuse -> re-declare sequence. The manager ran it and then checked
=~/Library/LaunchAgents/orgasmic.daemon.plist=: mtime UNCHANGED. So the pattern
is verified, not merely argued.

Contrast with what the three outages did: stage an =ORGASMIC_HOME= and invoke a
verb, which has nothing listening, so =ensure_running= proceeds to the adapter
and rewrites the one real per-user plist.

*The rule to write into briefs and tests from now on:*
- need a real daemon in a test => point the CHILD at one with
  =ORGASMIC_DAEMON_URL=;
- never stage =ORGASMIC_HOME= to get isolation, because it does not isolate the
  LaunchAgent;
- =env -u ORGASMIC_HOME= to strip a LEAKED value is the opposite of staging one
  and remains correct.

This also sharpens the code fix this task asks for. =ensure_running= already has
a cheap correct path when it is told where the daemon is; the defect is that
absent that, it silently escalates to mutating a per-user service. Refusing —
or at minimum warning — when the home it is asked to start differs from the one
the installed LaunchAgent serves would have turned all three outages into an
error message.
RULE SHARPENED BY MEASUREMENT [2026-08-03]. The blanket "never set
=ORGASMIC_HOME=" is the right thing to put in a brief, but it is not the precise
rule, and the code fix should be built against the precise one.

Measured: =ORGASMIC_HOME=<staged> orgasmic doctor= with residue staged in the
fake home printed the expected warning AND left the LaunchAgent plist mtime
unchanged (=04:53:32=, from hours earlier) with the daemons boot_id and pid
untouched. So =doctor= genuinely does not reach =ensure_running=.

The precise rule is therefore *never set =ORGASMIC_HOME= for a verb that NEEDS
THE DAEMON* — because that is what triggers =ensure_running= ->
=start_via_selected_adapter= -> the per-user LaunchAgent write. Read-only local
verbs that never open a connection are unaffected.

TWO CONSEQUENCES, and they point in opposite directions on purpose:

1. *Keep the blanket ban in briefs.* A worker cannot reliably know which verbs
   open a daemon connection, the failure is total and silent when it guesses
   wrong, and the cost of the blanket version is nearly zero because the
   daemon-free verification recipe covers what workers actually need. Precision
   here would buy nothing and risk everything.
2. *Build the code fix against the precise rule.* =ensure_running= is the single
   choke point and already has a cheap correct path when it is told where the
   daemon is (=ORGASMIC_DAEMON_URL=). Refusing — or warning — when the home it is
   asked to start differs from the one the installed LaunchAgent serves would fix
   the class without touching verbs like =doctor= that are already safe.

That asymmetry is the useful part: the guidance humans and agents read should be
blunter than the invariant the code enforces.

** Description

INFERENCE, LABELLED AS SUCH — the evidence is strong and circumstantial, and no
worker artifact survived to prove it directly.

Two dispatch-wide losses on 2026-08-03, ten minutes apart:

| time (UTC) | killed | transport |
|------------+--------+-----------|
| 00:03-00:04 | TASK-VEGRV, TASK-3CM0Q, TASK-FZB6T reviewer | rmux |
| 00:13:05    | TASK-VEGRV, TASK-FZB6T reviewer            | tmux |

The second event rules out the transport as the cause: it happened on tmux, after
the manager switched away from rmux precisely because rmux had died in the first.

WHAT IS MEASURED
- =~/Library/LaunchAgents/orgasmic.daemon.plist= has mtime *03:13:05 local*,
  which is exactly the =00:13:05Z= daemon restart instant. The first event shows
  the same pattern, with TWO boot_ids logged one after the other at =00:04:11Z=
  (a plist reload race).
- The daemon logged NO shutdown lines before either restart. It goes straight
  from steady-state to =starting pre-bind boot work=, so it was killed hard.
- =launchctl print gui/501/orgasmic.daemon= reports =runs = 1= and
  =last exit code = (never exited)=, so this is NOT launchd KeepAlive restarting
  a crashed process.
- No orgasmic crash report exists for 2026-08-03 (latest is 2026-07-31). Memory
  was 61% free with zero swap, so it is not an OOM kill.
- The plist still points at =~/.orgasmic/bin/orgasmic=, so the BINARY was not
  hijacked — only the plist was rewritten and reloaded.

WHAT THAT LEAVES. Something rewrote the LaunchAgent plist, twice, at the exact
instants the daemon restarted. The memory =orgasmic-daemon-start-hijacks-launchagent=
and the existing gotcha both record that =orgasmic daemon start= rewrites THE ONE
REAL LAUNCHAGENT PLIST regardless of =ORGASMIC_HOME= — a per-user path, not a
per-home one. A worker running any daemon lifecycle verb therefore restarts the
operator daemon and kills every sibling dispatch, including itself.

THE MANAGER CONTRIBUTED TO THIS. Several briefs this session instructed workers
to "stage an =ORGASMIC_HOME= from the edited files" for a render check, because
the live daemon serves INSTALLED shipped content rather than the worktree. That
instruction is correct and necessary, and it is one short step from
=orgasmic daemon start= against the staged home — which is not isolated at all.
The instruction was given without the accompanying prohibition.

** Why this is P1

It makes concurrent dispatch unsafe outright. Five workers died in ten minutes,
three with live uncommitted work that had to be salvaged by hand. Nothing warns
the worker, nothing warns the manager, and the failure looks like a transport
problem — the manager initially and wrongly blamed rmux, switched transports, and
lost the next two workers the same way.

** Work

- A daemon lifecycle verb invoked from inside a dispatch (=ORGASMIC_RUN_ID= is
  set in the worker environment) should REFUSE by name, and say why: it would
  restart the operator daemon and kill every live run including your own.
- Consider whether =ORGASMIC_HOME= should scope the LaunchAgent path at all, or
  whether the honest answer is that daemon lifecycle is never per-home and the
  verb should say so.
- Give workers a SAFE render path that needs no daemon: the 2026-08-03
  TASK-0JY7M worker did it correctly by running the worktrees own
  =target/debug/orgasmic entry= against a staged home. Document that as the
  supported recipe so the dangerous one is not reinvented.
- Until this lands, every dispatch brief must carry an explicit prohibition.

** Acceptance

- A daemon lifecycle verb from inside a dispatch refuses, with a message naming
  the consequence.
- The supported no-daemon render recipe is documented where brief authors will
  find it.
- A regression proves the refusal fires when the dispatch environment is present
  and does not fire for an ordinary operator invocation.

* BACKLOG TASK-QNCQ5 Format-1 rollback re-derives with the current compactor, so legacy rollback is unavailable on classifier drift    :daemon:storage:
:PROPERTIES:
:ID: TASK-QNCQ5
:IMPLEMENTS: TASK-FZB6T
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-daemon/src/run_history.rs
:END:

** Description

Round-4 reviewer finding 4 (P2, =BLOCKS_SHIP: no=), split out on the reviewers
recommendation so TASK-FZB6T.5 stays scoped to the three blockers.

For a format-1 record, rollback hashes a replacement rebuilt by the CURRENT
=build_compacted= (=run_history.rs:1756-1766=). That reconstruction is only
usually identical to what the format-1 implementation actually wrote: at
=9bee827= the writer classified by the first textual ="type"= match, while
current code uses a validating structural scanner. The current regressions
themselves document shapes whose classification changed — a nested =text_chunk=
before an outer =tool_result= (=run_catalog.rs:4375-4392=) — and historical
raw-control-byte records followed the old classifier while the current scanner
refuses them.

The compatibility test at =run_history.rs:2936-3018= writes an old manifest
SHAPE, but line 2943 creates the compacted live bytes by calling the CURRENT
=apply=. So it proves field-shape compatibility, not that a replacement actually
emitted by the format-1 implementation is recognised.

** Why this is P2 rather than blocking

It is safe-failing. On a real classifier-drift case rollback REFUSES rather than
overwriting, so the archive is not destroyed and no evidence is lost. What is
lost is the promise: legacy rollback is unavailable for exactly the records whose
classification drifted.

** Work

- Decide whether legacy rollback should be supported at all, or whether refusing
  with a clear reason is the honest contract. Record the decision.
- If supported: authorize the live-file decision against bytes the format-1
  implementation actually emitted, rather than against a reconstruction by the
  current compactor.
- If not: make the refusal explicit and named, so an operator sees "this archive
  predates the current classifier" rather than a generic mismatch.
- Either way the compatibility test must build its old compacted bytes with the
  OLD algorithm, not by calling the current =apply=.

* BACKLOG TASK-YPK0D Operator-initiated recovery reattach does not consult blocks_attach(), unlike boot and inventory    :daemon:recovery:
:PROPERTIES:
:ID: TASK-YPK0D
:IMPLEMENTS: TASK-FZB6T
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-daemon/src/api.rs
:END:

** Description

After TASK-FZB6T.5, two of the three reattach paths consult
=WorktreeAuthority::blocks_attach()=:

- the polled inventory (=api.rs=, =classify_session_files=);
- the boot path (=boot_reattach_candidate=), fixed in TASK-FZB6T.5.

The two OPERATOR-INITIATED routes do not: =execute_run_recover_action= at
=api.rs= ~9180 and ~9481.

The implementer flagged this rather than widening scope on its own, and its
reasoning is worth preserving: those routes take an explicit worktree from the
request or the pending recovery plan rather than from the catalogs recorded
authority, so refusing there is *a product decision about whether an operator may
force a recovery of a run the daemon considers dead* — not the same
consistency-wiring fix the boot path needed.

** The decision to take

Should an explicit operator recovery be able to override a durable tombstone or
an =Unprovable= ledger verdict?

- *If yes* — say so in the code and in the routes docs, so the asymmetry is
  intentional and legible rather than looking like the same oversight that was
  just fixed twice. Consider surfacing the authority verdict in the response so
  the operator is overriding something they can see.
- *If no* — consult =blocks_attach()= there too and return a named refusal that
  distinguishes "this run is durably dead" from an ordinary failure.

Either way the asymmetry should be deliberate. Right now a reader comparing the
three paths cannot tell whether the difference is designed or missed.

** Not urgent

An operator forcing a recovery is an explicit act with a human behind it, which
is materially different from boot auto-reattach or a polled inventory offering a
candidate. That is why this is P2 and was correctly kept out of the blocking
round.

* BACKLOG TASK-3X5AQ Dispatch worktree builds prompt for Documents and leave dead TCC rows — chosen shared-target-dir fix is measurably unsound, needs a new decision    :dispatch:runtime:macos:
:PROPERTIES:
:ID: TASK-3X5AQ
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/**, crates/orgasmic-cli/**, shipped/**
:END:

=== READ THIS FIRST — STATUS ===
The DECISION recorded below (option 2, shared =CARGO_TARGET_DIR=) was DISPROVED
by measurement later the same day. DO NOT IMPLEMENT IT. See the section
"Disproof of the shared target dir (2026-08-03)" at the end of this task: a
shared target dir makes a worker's =cargo build= a silent no-op and greens its
integration suite against another checkout's binary. This task needs a NEW
decision; option 5 (stable worktree slots) is the recommendation.
=== END STATUS ===

DECISION [2026-08-03]: take option 2, the shared =CARGO_TARGET_DIR=. The
operator chose it after the cost of option 1 was measured (it disables the whole
orgasmic-cli integration suite, including the =cli_parity= false-verb guard).

CONCURRENCY IS HANDLED BY CARGO, PROVEN NOT ASSUMED. Two =cargo build= runs
against one target dir were started 2s apart; the second printed:

: Blocking waiting for file lock on artifact directory
: Finished `dev` profile ... in 0.13s

So workers need no coordination protocol and cannot corrupt each other — the
second simply waits. That answers the "how does a worker know" question: it does
not have to.

THREE THINGS THE LOCK DOES NOT COVER, and they are the real design work:

1. *The lock covers BUILDING, not TESTING.* Once artifacts exist, =cargo test=
   just runs binaries, and two workers can run suites concurrently against the
   same target dir with no lock at all. That is exactly the load condition
   measured FIVE times this session to redden the live-mux and process-probe
   tests spuriously. A shared target dir does not cause that, but it removes the
   incidental staggering that per-worktree builds provided.

2. *Flag divergence causes rebuild thrashing.* =cargo clippy --all-targets=,
   =cargo test -p orgasmic-daemon --lib= and =cargo build --release= select
   different artifact sets; workers alternating between them invalidate each
   other and each waits for a rebuild of what the other just discarded. With a
   25 GB target and multi-minute daemon builds this is the dominant cost, and it
   is worse than the lock wait. MITIGATION: briefs should name ONE gate command
   set and workers should not improvise flags — largely already true, and now
   load-bearing rather than stylistic.

3. *Shared blast radius.* One worker running =cargo clean=, or a disk-pressure
   cleanup, destroys every concurrent workers artifacts mid-run. Worth an
   explicit "never cargo clean in a dispatch" line alongside the other
   prohibitions.

ONE CLEAR WIN, measured: each worktree currently carries its own target —
=task-8ed6v= alone is 3.0 GB against the shared 25 GB. Sharing removes that
duplication per concurrent worker, which also reduces the =syspolicyd= scan
surface, since the gotcha attributes the bursts to executing freshly written
binaries.

IMPLEMENTATION NOTE: the daemon composes the worker environment, so
=CARGO_TARGET_DIR= belongs there rather than in a per-brief instruction — a
prose instruction is skippable and this needs to hold for every dispatch. Point
it at the repo-root =target/=, which already holds the TCC grant; that is what
makes the prompts stop.

macOS TCC keys Documents-folder access by BINARY PATH. Every dispatch worktree
lives at a fresh path under
=~/Documents/code/tools/orgasmic/.orgasmic/tmp/dispatch/<task>/worktree/=, so a
worker that builds =target/debug/orgasmic= there produces a program macOS has
never seen. It prompts the operator, and the grant it earns dies with the
worktree.

*Measured 2026-08-03.* The user TCC db has five
=kTCCServiceSystemPolicyDocumentsFolder= rows under =dispatch/*/worktree=, and
ALL FIVE point at worktrees that no longer exist:

: .../dispatch/task-vegrv/worktree/target/debug/orgasmic    granted 03:41:23, worktree GONE
: .../dispatch/task-2dftx/worktree/target/release/orgasmic  GONE
: .../dispatch/task-CZTWD/worktree/target/debug/orgasmic    GONE
: .../dispatch/task-Y2ZQJ/worktree/target/debug/orgasmic    GONE
: .../dispatch/task-jj9rd/worktree/target/debug/orgasmic    GONE

So the prompt is not a grant that failed to stick. It is a NEW grant request per
dispatch, for a path created and destroyed within the hour, and approving it does
nothing for the next worker.

*NOT the runtime reinstall.* Checked at the same time and stated here because the
coincidence is misleading: =~/.orgasmic/bin/orgasmic= holds
=kTCCServiceSystemPolicyAllFiles= = 2, last modified 2026-08-02 18:35:46 — BEFORE
the 2026-08-03 reinstall — with zero permission errors and all 6 projects
indexed. The codesign preserved TCC exactly as intended.

** Description

macOS TCC keys Documents-folder access by BINARY PATH. Every dispatch worktree
lives at a fresh path under
=~/Documents/code/tools/orgasmic/.orgasmic/tmp/dispatch/<task>/worktree/=, so a
worker that builds =target/debug/orgasmic= there produces a program macOS has
never seen. It prompts the operator, and the grant it earns dies with the
worktree.

*Measured 2026-08-03.* The user TCC db has five
=kTCCServiceSystemPolicyDocumentsFolder= rows under =dispatch/*/worktree=, and
ALL FIVE point at worktrees that no longer exist:

: .../dispatch/task-vegrv/worktree/target/debug/orgasmic    granted 03:41:23, worktree GONE
: .../dispatch/task-2dftx/worktree/target/release/orgasmic  GONE
: .../dispatch/task-CZTWD/worktree/target/debug/orgasmic    GONE
: .../dispatch/task-Y2ZQJ/worktree/target/debug/orgasmic    GONE
: .../dispatch/task-jj9rd/worktree/target/debug/orgasmic    GONE

So the prompt is not a grant that failed to stick. It is a NEW grant request per
dispatch, for a path created and destroyed within the hour, and approving it does
nothing for the next worker.

*NOT the runtime reinstall.* Checked at the same time and stated here because the
coincidence is misleading: =~/.orgasmic/bin/orgasmic= holds
=kTCCServiceSystemPolicyAllFiles= = 2, last modified 2026-08-02 18:35:46 — BEFORE
the 2026-08-03 reinstall — with zero permission errors and all 6 projects
indexed. The codesign preserved TCC exactly as intended.

** Why it has not broken anything yet

A worker denied Documents access still reaches =.orgasmic/= state through the
daemon over HTTP, so no run has failed on this. The costs are operator
interruption, a TCC db slowly filling with dead paths, and the risk that an
operator conditioned to dismiss orgasmic prompts dismisses one that matters.

** Same family as TASK-48SFW

Per-user OS state that per-dispatch isolation does not scope. There it was the
LaunchAgent plist; here it is the TCC path grant. Worth fixing with that in mind
rather than as a one-off.

** Options, and the trade is real

1. *Tell workers never to build the CLI.* Simplest, and the operator asked for
   this first. COST, measured: =cargo test -p orgasmic-cli= builds the binary
   automatically for integration tests (=CARGO_BIN_EXE_orgasmic=), so this
   disables essentially the whole CLI integration suite —
   =cli_parity= (the TASK-TKR0H false-verb guard, load-bearing for every shipped
   prose change), =entry=, =doctor=, =status=, =reindex=, =list_output=,
   =node_body_roundtrip_cli=, =task_property_silent_drop_cli=, =bootstrap_smoke=,
   =pinned_exec=, =manager_tier_cli=, =manager_register=. Several of those caught
   real defects in the 2026-08-02/03 session.
2. *Shared =CARGO_TARGET_DIR=.* Point worktree builds at one stable path that
   already holds the grant. Every test keeps working and no new prompt is ever
   raised. Cost: concurrent workers serialize on the cargo lock, and a shared
   target dir is a known contention source.
3. *Move dispatch worktrees outside =~/Documents=.* Removes the whole class —
   Documents is the only guarded ancestor — but changes a path a lot of tooling
   and several gotchas assume, and the daemons own Documents grant exists
   precisely because the project lives there.
4. *Prompt-free by construction:* have the dispatch env set =CARGO_TARGET_DIR=
   only for the CLI crate, or have workers run the INSTALLED
   =~/.orgasmic/bin/orgasmic= (already granted) for anything that just needs a
   working CLI rather than the one they are changing.

Options 2 and 4 keep the suite; 1 and 3 change what workers can verify. Pick
deliberately.

** Acceptance

- A dispatch that builds and runs the CLI raises no new Documents prompt.
- Whatever is chosen, the CLI integration suite still runs for a worker that is
  CHANGING the CLI — that is the case where the tests matter most.
- Existing dead worktree rows in the TCC db are documented as safe to remove (it
  is SIP-protected, so removal is operator-only through Settings).
** Disproof of the shared target dir (2026-08-03)
STOP — THE 2026-08-03 DECISION IS DISPROVED. Do NOT implement the shared
=CARGO_TARGET_DIR=. It is not merely contended, it is UNSOUND: a dispatch
worker's =cargo build= becomes a silent no-op and its integration suite greens
against a DIFFERENT checkout's binary.

*Reproduced 2026-08-03 in the exact production shape* — a git worktree of a
cargo workspace, both pointed at the repo-root target dir (cargo 1.94.1):

: repo root:        main.rs prints MAIN_BRANCH   -> cargo build -> target/debug/probe-cli = MAIN_BRANCH
: git worktree A:   main.rs prints TASK_A_BRANCH -> cargo build -> "Fresh probe-cli", NO compile
:                   target/debug/probe-cli still = MAIN_BRANCH
:                   cargo test in worktree A: 1 passed, BIN_SAYS=MAIN_BRANCH

The worker edited a source file, built, and tested. Nothing errored. The suite
was green. It ran the MANAGER's binary.

*Mechanism, and why the earlier lock measurement missed it.* The file lock is
real and does serialize concurrent builds — that measurement was correct and is
not what fails. What fails is freshness. Cargo's =-C metadata= hash for a path
package does not include the checkout path, so BOTH checkouts share ONE
fingerprint directory (=target/debug/.fingerprint/probe-cli-<hash>=, verified: a
single dir, not one per worktree). Freshness is then decided by comparing the
mtimes of the files recorded in the OTHER checkout's dep-info. The worker's own
edited file is not in that list, so nothing looks stale and cargo reports
=Fresh=. The stale uplifted binary at =target/<profile>/<name>= is what
=CARGO_BIN_EXE_*= resolves to, so every integration test inherits it.

It is also NOT ordering-dependent: after making worktree B's file genuinely
dirty and rebuilding, rebuilding worktree A reported =Fresh= and left B's binary
in place. Either checkout can silently receive the other's program.

*Why this outweighs the problem it was chosen to solve.* The TCC prompt costs
the operator a click and leaves a dead db row; no run has ever failed on it.
This trades that for a class of false green — and this session already lost real
work to two of those (a re-cut =orgasmic verify= artifact that never reached its
assertions, and a shipped artifact that became a false green when a fix made its
defect unreproducible). The whole point of the gate is that green means
something. Sharing a target dir across checkouts makes green mean nothing, in a
way no test on this repo would detect.

*Option 5, which satisfies BOTH constraints — the recommendation.* The tension
is: TCC needs the EXECUTED BINARY PATH to be stable across dispatches, while
cargo needs ONE checkout path per target dir. A fixed pool of stable worktree
slots gives both:

: .orgasmic/tmp/dispatch/slot-1/worktree  (own target/)
: .orgasmic/tmp/dispatch/slot-2/worktree  (own target/)   ... N slots

A slot's binary path never changes, so the operator grants it ONCE and the grant
survives — the prompt stops for good and the TCC db stops filling with dead
rows. Each slot keeps its own target dir with exactly one checkout path in it,
so cargo's freshness logic is the ordinary, sound, single-checkout case. Two
concurrent workers never share a slot, so there is no lock contention and no
flag thrashing either. Disk is bounded at N targets instead of unbounded, which
is strictly better than today.

Cost: the dispatch directory is named by slot rather than by task, so the
task->worktree mapping has to be read from the dispatch record instead of the
path. Several gotchas and briefs name task-shaped worktree paths and would need
updating, and slot release has to be part of worktree teardown.

*Reopened for a decision.* The operator picked option 2 on 2026-08-03 with the
lock measurement in hand and without this one; that choice should not be
executed as recorded. Options 1 and 3 stand as previously described. Option 5 is
the recommendation.
** The cdhash measurement: option 5 withdrawn, and the general-vs-specific split
OPTION 5 (STABLE WORKTREE SLOTS) IS WITHDRAWN AS THE TCC FIX. The manager
recommended it on 2026-08-03 on the assumption that a STABLE BINARY PATH keeps a
TCC grant. Measured 2026-08-05: it does not. The premise was wrong.

*The measurement.* Decoding the =csreq= blob stored on each grant in the user
TCC db:

: worktree-built binary   -> cdhash H"b081c98ab06c6ab3611c357bdb900420b3e66d54"
: installed runtime binary -> identifier "com.theaspirational.orgasmic" and
:                            certificate root = H"a70531007cd128ff081597cb27bee0816613d86d"

A grant for a linker/ad-hoc-signed binary is pinned to that binary's exact
CONTENT HASH, not to its path. Rebuild the same file at the same path and the
cdhash changes, so the grant no longer matches and macOS prompts again.

*Consequence, and it inverts the recommendation.* Slots would make the prompt
fire ONCE PER BUILD instead of once per dispatch — strictly WORSE than today,
since a worker builds many times per dispatch. Any scheme whose mechanism is "a
stable path earns a durable grant" fails for the same reason.

*What actually makes a grant durable* is the second row: a stable SIGNING
IDENTITY. =identifier ... and certificate root= survives rebuilds, which is
exactly why the 2026-08-03 runtime reinstall kept its Documents access while
the gotcha warns that a linker-signed binary is "a stranger to TCC". That is a
real fix but not one orgasmic can apply generally — it cannot codesign arbitrary
projects' build outputs, and requiring a signing identity to run a dispatch is a
much heavier ask than the problem justifies.

=== The remaining general fix is option 3, and the framework owns it
Get dispatch worktrees OUT of TCC-guarded directories, rather than trying to
make grants survive. macOS guards =~/Documents=, =~/Desktop=, =~/Downloads= and
iCloud Drive; nothing outside them needs a grant at all, so there is no cdhash
to invalidate and no prompt to answer.

This is the right layer because ORGASMIC CREATES THE PROBLEM: it chooses to put
worktrees at =<project>/.orgasmic/tmp/dispatch/<task>/worktree=, which inherits
whatever guarded ancestor the project root has. A project rooted in
=~/Documents= gets it for free and cannot opt out.

*To verify before building* (do not assume, this task has already burned one
assumption): a worktree outside Documents still has a =.git= file pointing at
=<project>/.git/worktrees/<name>= INSIDE Documents. Confirm whether git
operations from the worker trip a prompt, and if so whether a separate git dir
or a shared clone avoids it.

=== Is this orgasmic-specific or general? — general, with a specific accent
GENERAL: any orgasmic project that (a) runs on macOS, (b) COMPILES BINARIES it
then executes, and (c) is rooted under a guarded folder. Interpreted projects
mostly escape it because the executed binary is the interpreter
(=/usr/bin/python3=, =node=), which is already known to TCC; it is freshly
COMPILED output that is a stranger. So Rust, Go, C/C++, CGo, Tauri and Electron
projects hit it; plain Python/Node/Ruby ones typically do not.

SPECIFIC to this repo: only the vocabulary of the disproved fix
(=CARGO_TARGET_DIR=) and the fact that this project happens to live in
=~/Documents=. Neither belongs in the framework.

=== So: bundle the behaviour, do not write a convention
- The FIX belongs in the daemon's worktree placement — one code path, applies to
  every dispatch in every project, nothing to remember.
- A repo-wide convention is the wrong instrument for the reason already recorded
  here ("a prose instruction is skippable and this needs to hold for every
  dispatch") and for a reason this session demonstrated three times: prohibitions
  in prose get missed. The ORGASMIC_HOME outages happened WITH a written
  prohibition in force.
- Do NOT bundle toolchain knowledge. Orgasmic should not know what cargo is. If
  a per-project environment is ever needed, it should be a generic
  project-declared env map, not a =CARGO_TARGET_DIR= special case.

=== What slots are still worth
Bounded disk (N targets instead of one per concurrent worktree, measured at
3.0 GB for a single worktree) and, if anyone ever revisits sharing a target dir,
they are the only shape that keeps cargo sound. Keep them on the table as a
disk/scale change; just not as the TCC answer.
** Verification and recommended placement (2026-08-05)
VERIFIED 2026-08-05, before building anything. Four measurements, one of which
closes the option-5 question for good and one of which corrects an earlier
assumption in this task.

1. *A rebuild at a STABLE PATH changes the cdhash.* Built a tiny ad-hoc-signed
   binary twice at one path:

   : build 1: CDHash=16237ab166c90df05f15b3f29b0f25c03d7616c9
   : build 2: CDHash=6cdc20d7c7e6574d879e6b72648f0c1922a79120

   The TCC grant is pinned to the cdhash, so it cannot survive a rebuild. Option
   5 (stable worktree slots) is CONFIRMED DEAD as the TCC fix — it would move
   the prompt from once-per-dispatch to once-per-BUILD.

2. *git is NOT exempt, but it is already satisfied and path-stable.* Earlier
   reasoning here assumed git would be covered as an Apple binary. Wrong:
   =/opt/homebrew/Cellar/git/2.52.0_1/bin/git= holds its own
   =kTCCServiceSystemPolicyDocumentsFolder= grant, as do =tmux= and =rustup=.
   They prompted once historically and stuck, because their paths are stable.
   Crucially, across the FIVE recorded dispatch prompts, every grant was for a
   freshly built =.../worktree/target/{debug,release}/orgasmic= — NEVER for git.
   So git reaching into a Documents-hosted =.git= was already a solved case
   during real dispatches. (A Homebrew git upgrade moves the path and re-prompts
   once; that is pre-existing and unrelated to worktree placement.)

3. *A worktree outside the repo works normally.* Created one at
   =~/.orgasmic/worktrees/orgasmic/tcc-probe=: its =.git= file resolves back to
   =<repo>/.git/worktrees/tcc-probe= inside Documents, =git status= and
   =git log= work, =.orgasmic/project.org= is present so project resolution
   still works from the worktree, and =git worktree remove= works from outside
   the repo. Same filesystem (=/dev/disk3s5=), so no cross-volume concerns.

4. *The toolchain builds and runs there.* cargo built and ran a fresh binary
   under =~/.orgasmic/worktrees/...= with no issue.

*LIMIT OF TEST 4, STATED SO NOBODY OVER-READS IT.* The probe binary also read
the Documents-hosted repo successfully and RAISED NO PROMPT — but that proves
nothing about dispatches. It ran under this shell, whose responsible process
already holds Documents access; TCC attributes to the responsible process, and a
dispatch's chain (daemon -> tmux -> harness -> cargo -> test binary) is
different. The argument for the fix does NOT rest on that result. It rests on
the fact that a worker whose worktree is outside Documents HAS NO REASON to read
Documents at all: its cwd, sources, target dir and =.orgasmic= snapshot are all
inside the worktree, and the only link back is the =.git= file, which git — a
separately granted, path-stable binary — follows.

=== Recommended placement
: ~/.orgasmic/worktrees/<project-id>/<task-id>/

Keyed on PROJECT ID, not display name: the id is what the CLI and daemon already
use everywhere (=--project <id>=), and names can collide or change.

Why a TOP-LEVEL =worktrees/= rather than the proposed
=~/.orgasmic/user/projects/<name>/worktrees=:
- =user/= currently means user-level CONFIG — it holds =auth/= and =board.org=,
  both small. Worktrees are multi-GB regenerable scratch; mixing them buries the
  largest thing on disk inside a config namespace.
- =user/= also carries a bad association: =user/workers/= was the retired
  residue that TASK-8ED6V was filed about. A new =user/projects/= invites the
  same "is this live or dead?" question that incident was about.
- Top-level makes the cost visible: =du -sh ~/.orgasmic/*= shows it at a glance.

Why NOT =~/Library/Caches/orgasmic/...=, the otherwise idiomatic macOS choice:
it is unguarded (confirmed: zero TCC rows for Cache services), but macOS may
PURGE it under disk pressure, which would destroy a worktree mid-dispatch.
Rejected on that alone.

=== What moves and what stays
Move only the SCRATCH. The dispatch RECORD — brief, report =last.txt=, stdout
log under =.orgasmic/tmp/dispatch/<task>/= — stays in the project, because it is
part of the durable record, it is small, and it is written by the daemon, which
is identity-signed and already granted. Splitting on "record stays, scratch
leaves" keeps every existing brief-path reference valid.

=== Already has an escape hatch
=orgasmic manager dispatch --worktree <PATH>= already exists ("omitted -> a
managed path under =.orgasmic/tmp/dispatch/="). So this change is to the MANAGED
DEFAULT only, and the override is precedent, not new surface.

=== Make it universal, not macOS-gated
Linux has no TCC, but the move is still correct there: the repo stops
accumulating multi-GB build trees, =git status= in the repo stays clean, and
disk is attributable per project. One code path beats a platform branch.

=== Known follow-on work
=.orgasmic/tmp/dispatch/<task>/worktree= is named in gotchas, briefs and
memories; those need updating with the change. =dispatch-close
--worktree-remove= must remove from the new root, and the salvage ref
=refs/orgasmic/salvage/<sha>= is git-level so it is unaffected.
** Implementation task
FILED [2026-08-05] as TASK-M47E5 — "Move dispatch worktrees out of TCC-guarded
directories, and give them a prune" (P1).

It carries both halves deliberately. Part 1 is the relocation to
=~/.orgasmic/worktrees/<project-id>/<task-id>/=. Part 2 is pruning, and it is a
REQUIREMENT of the move rather than a follow-up: today a stray worktree sits
inside the repo where =git status= and =git worktree list= surface it, so after
the move the same leak becomes SILENT multi-GB accumulation. Shipping the
relocation alone would trade visible garbage for invisible garbage.

This task (TASK-3X5AQ) stays open only as the measurement record — the cdhash
proof, the disproved shared target dir, and the disproved stable slots. The work
lives on TASK-M47E5.

* BACKLOG TASK-RQ270 Excise the architecture layer from orgasmic and the UI (dec_HBK6A, staged A-E)    :daemon:cli:ui:shipped:
:PROPERTIES:
:ID: TASK-RQ270
:MOTIVATED_BY: dec_HBK6A
:PRIORITY: P1
:WRITE_SCOPE: crates/**,ui/**,shipped/**,.orgasmic/**
:END:

** Description
Execute the excision decided in dec_HBK6A. The measuring is already done — do
NOT re-derive it. TASK-XYYBT produced =archive/TASK-XYYBT-architecture-excision-estimate.md=
(merged 84892aa), which carries the inventory actuals, answers to seven scoping
questions, the coupling map, and the staged plan with per-stage LOC and round
estimates. Read that report before touching anything; this task is the go.

Operator gate: PASSED [2026-08-03]. The report's own framing left excision
"not started; awaiting operator go/no-go". The operator gave the go, naming both
halves explicitly: remove it from orgasmic AND from the UI.

** The five stages, in order
Each lands independently with suites green and no dead routes. Expect one
implementer dispatch per stage; mint =TASK-<this>.N= subtasks per round the way
the FZB6T chain did.

A. STOP PRODUCING (~-450 LOC, ~14 files, reversible, 1 round)
   Retire =POST /architect=, =stage_spec("architect")=, =orgasmic architect=,
   the =architecture create= verb and its POST routes, and
   =DispatchEndpointKind::Architector=. Delete the architector prompt spec, its
   context pack, and the decomposition convention. KEEP "architector" PARSEABLE
   for ledger history.

B. RETIRE DRIFT AND THE =// arch:= BACK-EDGES (~-550 LOC, ~75 files, 1 round)
   Delete =crates/orgasmic-cli/src/architecture_drift.rs= (457 LOC, arch-only
   end to end) and the =Drift= verb; strip the 73 legacy =// arch:= lines. Can
   fold into A.

C. REMOVE READ MODEL, CLI READS AND UI (~-1,500 LOC, ~30 files, 1-2 rounds)
   =GET /architecture*=, the index projections and =load_architecture=,
   =architecture {list,get,schema}=, the =node.rs= arms, =schema_examples.rs=.
   UI: =ArchitectureView.tsx= (273 LOC), the NodeModal arch views, router/nav
   entries, =descriptor.ts=, api/types, capabilities — 14 files.
   *** ALSO LANDS THE LINT CARVE-OUT, and this is the ordering constraint that
   matters: =done.org='s 231 =:IMPLEMENTS: arch= tokens become dangling graph
   edges (=index.rs:729= -> =1188= -> lint at =1256=). Drop =arch_= from the
   structured prefix lists (~4 lines + 2 test updates). This MUST be on main
   BEFORE stage D deletes the file.

D. RETIRE THE NODE CLASS AND THE FILE (~-800 LOC, ~15 files + state, 1-2 rounds)
   =ArchitectureNode= / =parse_arch_edges= (=schema.rs:361-660=),
   =NodeIdClass::Architecture= + =is_arch_id= (=id.rs=), the identity-lint arch
   collectors, the =node_kind= variant, and full =WorkerKind::Architector=
   removal WITH a legacy-ledger parse fallback. State edits go through daemon
   verbs or git, never by hand: delete =.orgasmic/architecture.org=, repoint the
   two live task drawers (below), optionally the 4 glossary =:PRIMARY_LEAF:=
   refs, and the conventions/decision-graph wording.

E. SHIPPED DOCTRINE REWRITE (~-200 LOC, ~20 files, 1 round)
   =router.org=, =SKILL.md=, =recall-resume.md=, =init.md=, =tx.org=
   (architector rows become a historical note), the manager/planner/griller/
   base/general/implementer specs, =conventions.org=, =manager-dispatch.org=,
   and the project-scaffold seeds. Foldable into A+D.

** What must NOT be removed — the coupling map
- *The shared graph substrate stays.* =GraphLayer= / =NodeLayer= / =NodeKind= /
  =NodeIdClass= and the create/mutate/lint plumbing serve decisions and glossary
  too. Excision removes VARIANTS AND THEIR MATCH ARMS, never the enum
  machinery, =read_or_seed_graph_file=, writer authority, tx append, or the org
  substrate. =org.rs='s 33 =arch_= hits are sample ids in its own tests, not
  coupling; likewise =session.rs='s 8 prose comments.
- *The generic marker scanner stays* (=marker.rs=, =scan_project_markers=,
  =/graph/markers=) — it serves dec_/TASK-/term markers. Only the arch-specific
  drift comparator goes.
- *=GraphEdgeSummary= and task =implements= edges stay.* =:IMPLEMENTS:= remains
  a legal task drawer key; only the LINT's arch-prefix handling changes.
- *History parse paths stay untouched*: tx TYPE is free-string (=tx.rs:432=), so
  history parses forever; done.org indexing is unchanged.
- *Enum-variant retirement is ordered.* =WorkerKind::Architector= and the
  dispatch-kind strings are parsed from persisted ledger records
  (=manager.rs:3805-3817= maps historical "architector" rows). This is THE ONE
  PLACE naive deletion breaks history.

** Live fallout, already enumerated
- TASK-CQM2X and TASK-VBSG2 carry =:IMPLEMENTS: arch_RN73Z= (=backlog.org:38,:54=);
  repoint to =:MOTIVATED_BY: dec_PC6T2 dec_WDR5K=.
- 4 glossary =:PRIMARY_LEAF:= refs are unvalidated and inert.
- Load-bearing content: fold ~3 nodes, drop ~23, park 0.

** Acceptance
- No route, verb, worker kind, node class, UI surface or shipped instruction
  offers the architecture layer.
- Historical =arch_= references in tx, =done.org= and =decisions.org= still
  parse and still resolve; the dangling-edge lint does not fire on them.
- A dispatch ledger row recorded as "architector" still parses after stage D.
- =.orgasmic/architecture.org= is gone and nothing reads it.
- =cli_parity= passes — it resolves every =orgasmic ...= invocation and named
  flag in =shipped/= against the real binary, so it is what catches a doctrine
  rewrite that names a verb the excision just deleted.

* BACKLOG TASK-37WN7 the TASK-Q07Y5 signal-wait mitigation does not fully close the SIGTERM race under load, and the flake is unregistered    :test:cli:
:PROPERTIES:
:ID: TASK-37WN7
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-cli/**,verify/flake-registry.toml
:END:

** Description
=sigterm_exits_through_graceful_shutdown_rather_than_default_disposition=
(=crates/orgasmic-cli/tests/daemon_lifecycle.rs:319=) failed once under
parallel load during TASK-YN5FJ.1 and passed everywhere else.

WHAT MAKES IT WORTH A TASK RATHER THAN A SHRUG: the test ALREADY carries a
mitigation for exactly this race. =wait_until_serve_awaits_signals= was added by
TASK-Q07Y5 precisely because =serve= registers its SIGTERM handler AFTER
=Daemon::run= returns, so a SIGTERM sent on the strength of a successful connect
can land on the default disposition. Its own comment says the window is
"invisible on an idle machine" but not "under a loaded test binary".

So this is not an unmitigated race — it is a mitigation that does not hold. Either
=wait_until_serve_awaits_signals= does not actually prove the handler is
installed, or there is a second window it does not cover. That distinction is the
task.

** Evidence
Measured 2026-08-05 during TASK-YN5FJ.1, on =bb2fb24=:
- implementer full =scripts/run-tests.sh -p orgasmic-cli=: 1 failure, this test;
- same test rerun in isolation by the wrapper: PASS;
- whole =daemon_lifecycle= binary alone: 6 passed / 0 failed;
- second full =run-tests.sh -p orgasmic-cli=: GREEN;
- manager own gate run on the merged tree: GREEN, this test did not fail.

The change under test touched =dispatch-close= argument handling only and cannot
reach =orgasmic serve=, so it is not a regression from that work.

UPDATE 2026-08-05, and it CORRECTS this task own framing.

The panic this task asked someone to capture arrived on its own, as the sole REAL
failure of the =scripts/run-tests.sh -p orgasmic-cli= gate on the TASK-M47E5.2
merge (=b20fba5=). The failing assertion is =status.signal()= — the process really
did die on SIGTERM default disposition — NOT the 30s =try_wait= deadline. So the
handler genuinely was not installed when the signal landed, which is exactly the
window =wait_until_serve_awaits_signals= claims to close.

THE LOAD-ONLY READING IS WRONG. This task, and the TASK-Q07Y5 comment it quotes,
both say the window is "invisible on an idle machine". Measured immediately after
the failure, alone, against the already-built binary: 1 failure in the first 14
isolated runs, then 12/12 green. So it reproduces WITHOUT load at roughly 7%. The
"passes in isolation" folklore — which the wrapper own isolation retry reinforces
every time it goes green — has been hiding a defect that is not load-dependent at
all. Anyone measuring this by running it once in isolation will conclude wrongly.

REGISTERED, now that the bar this task set is met: =898c7e3= adds a
signature-bearing entry owned by this task, pinned to the =status.signal()=
assertion only, so a failure on the deadline branch or the exit-code branch still
reads REAL. The first acceptance item ("the failing assertion is identified from a
captured red, not inferred") is DONE. What remains is the second: strengthen
=wait_until_serve_awaits_signals= so the window is genuinely closed, or record why
it is not worth closing.

** Why it is NOT in verify/flake-registry.toml yet
Deliberate, and this is the part to not undo casually. No panic signature was
captured — the failure was observed through the wrapper verdict, not a saved log.
A registry entry with no =signature= makes EVERY future failure of that test name
read as this known flake, which is precisely the "trains everyone to discount
failures" failure the registry exists to prevent. Registering it blind would buy
a quieter suite at the cost of the signal.

So: capture the panic first (which assertion — the =status.signal()= one, or the
30s =try_wait= deadline), THEN either fix the mitigation or register the entry
with that signature and this task as owner.

** Acceptance
- The failing assertion is identified from a captured red, not inferred.
- Either =wait_until_serve_awaits_signals= is strengthened so the window is
  genuinely closed under load, or a signature-bearing =[[flake]]= entry is added
  with this task as owner and a rationale for why the race is not worth closing.
- =scripts/run-tests.sh --check= stays green (owner must be an open task).

* BACKLOG TASK-A1KJW the dispatch record directory is named from the brief filename, not the task, so it can collide and now disagrees with the worktree    :cli:
:PROPERTIES:
:ID: TASK-A1KJW
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-cli/**
:END:

** Description
Found by probing the merged TASK-M47E5 tree, and PRE-EXISTING — =git diff= over
that merge shows =dispatch_artifact_stem= was not touched. Do not treat it as an
M47E5 regression; M47E5 is what made it matter.

=dispatch_artifact_stem(brief_path)= (=crates/orgasmic-cli/src/manager.rs=, ~:5517)
derives the dispatch record directory and the =last=/=stdout= filenames from the
BRIEF FILENAME alone. The task id is never consulted. Measured on the real binary:

: --task TASK-STWVB --brief .../task-m47e5-brief.md
:   worktree: ~/.orgasmic/worktrees/orgasmic/task-stwvb
:   last:     <project>/.orgasmic/tmp/dispatch/task-m47e5/task-m47e5-<uuid>-last.txt

: --task TASK-STWVB --brief .../zzz-unrelated-name.md
:   last:     <project>/.orgasmic/tmp/dispatch/zzz-unrelated-name/zzz-unrelated-name-<uuid>-last.txt

So the record for a TASK-STWVB dispatch lands in a directory named after whatever
the brief file happened to be called.

** Why it matters more now than it did last week
Before TASK-M47E5 the worktree sat beside the record under the same
=.orgasmic/tmp/dispatch/<stem>/= tree, so the stem was at least internally
consistent. Now the WORKTREE is keyed on the task id
(=~/.orgasmic/worktrees/<project>/<task>/=) while the RECORD is keyed on the brief
filename. The two disagree, and the record — which is the part that stays in the
project and the part a recovering manager reads — is the one that no longer names
its task.

Concretely: a fresh manager doing recovery reads =.orgasmic/tmp/dispatch/= to find
what was in flight, and the directory names there do not reliably identify tasks.
Two tasks briefed with the same filename share one record directory.

** Not a data-loss bug
=DispatchArtifactReservation= (TASK-KE0JW) reserves the =last=/=stdout= pair with
retries, so a collision does not silently overwrite another run artifacts. The
defect is naming and discoverability, which is why this is P3 and not higher.

** Fix direction
Key the record stem on the task id the way the worktree now is, keeping the brief
filename only where it is genuinely about the brief. Migration is not needed —
existing directories are historical records and should be left alone — but the
change must not break the many gotchas, briefs and memories that name
=.orgasmic/tmp/dispatch/<stem>/= paths, so check those before renaming anything.

** Acceptance
- The record directory for a dispatch identifies its task without knowing what the
  brief file was called.
- Two tasks briefed from identically-named files do not share a record directory.
- Existing on-disk records are untouched.

* BACKLOG TASK-RMA18 redesign worktree reclamation: fd-anchored end to end, with the daemon owning worktree identity    :cli:daemon:
:PROPERTIES:
:DEPENDS_ON: TASK-M47E5.3
:ID: TASK-RMA18
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-cli/**,crates/orgasmic-daemon/**
:END:

** READ THIS FIRST — STATUS
The prune implementation is ON MAIN but GATED OFF by TASK-M47E5.3. Start from that
code and from the two review reports; do not start from scratch, and do not
un-gate until this task acceptance is met.

Two independent review rounds produced SIX BLOCK SHIP findings, every one on the
destructive path. That is the signal this task exists for: reclamation was
attempted as a CLI feature with safety bolted on, and it needs to be designed as a
safety problem with a feature attached.

** The six findings, which are the design input
ROUND 1 (tx =tx-20260805-orgasmic-4474=):
1. =read_dir= on the managed root followed a symlinked root; the direct-child
   fence could not fail because =path.parent()= IS =managed_root= by construction.
2. Ownership came from the tx ledger alone; =RunSummary= deserialized only
   =run_id=, so the CLI could not match a live run to a worktree at all.
3. =worktree_repo_gone= treated ANY =read_to_string= error as proof the repo was
   gone — the one disposition that skips salvage and deletes.

ROUND 2 (tx =tx-20260805-orgasmic-4495=), against the fixes for those:
4. =O_NOFOLLOW= GUARDS ONLY THE FINAL COMPONENT. =open_dir_nofollow= opens
   =<home>/worktrees/<project-id>= in one syscall, so replacing the ANCESTOR
   =<home>/worktrees= with a symlink is followed and the fd anchors a victim
   directory. The round-1 regression replaced only the final component, so it
   could not catch it. Confirmed by the manager against POSIX semantics and the
   code.
5. THE HANDLE WAS NOT THE REFERENCE POINT FOR THE WHOLE DECISION. Enumeration used
   the fd, but classification reconstructed =root.path().join(name)= and freshly
   resolved metadata, canonicalization, repo state, sizing AND the daemon
   reservation through that pathname. So the reservation can describe tree B while
   =unlinkat= deletes anchored tree A. The salvageable branch has the same gap:
   =git worktree remove= is itself the destructive remover and receives only a
   re-resolved pathname, with no child inode ever captured or passed.
6. The reservation task-id fallback to the directory name misses the window where
   an acquire is admitted but its =RunRecord= is not yet inserted —
   =blocking_run_for_close= detects that window ONLY by lease lookup on
   =params.task_id=, and =--worktree= may name any directory.

** Design directions from the reviewers, not yet ruled on
- Walk from a trusted anchored home fd and open =worktrees= and =<project-id>=
  with =openat(..., O_NOFOLLOW|O_DIRECTORY)= each, so no ancestor is ever resolved
  by pathname.
- Carry a child directory HANDLE plus a stable identity from enumeration through
  classification to removal; use fd-relative metadata and repo inspection. Either
  stop letting git own the recursive deletion through a rebindable pathname, or
  prove the exact child inode at the destructive syscall.
- Key the daemon fence on a stable worktree identity that survives rename, not a
  canonical pathname.
- Make pending admissions worktree-addressable in the supervisor: store the
  canonical/stable worktree identity when =admit_live_run= installs the lease, and
  have =reserve_dispatch_close= reject a matching pending admission regardless of
  task-name guessing. THIS IS A DAEMON CHANGE, which is why it did not fit a CLI
  fix round.
- Retain the deterministic pause-file race test under a =debug_assertions= or
  test-hooks compile gate; today =ORGASMIC_WORKTREE_PRUNE_PAUSE_FILE= is live in
  the release binary and can hang forever holding the global cleanup lock and the
  daemon reservation.

** Two things measured along the way, worth keeping
- FD accounting peaks near the anchored root plus 65 child fds against a macOS
  soft =RLIMIT_NOFILE= of 256; depth 65 and =EMFILE= propagate an error, but
  earlier siblings can already have been removed.
- A mid-stream =readdir= error is treated as EOF, so recursive removal can delete
  entries already returned, fail =rmdir= with =ENOTEMPTY=, and print KEPT. "Kept"
  currently does not mean "untouched", and it should.

** Acceptance
- No pathname is resolved for any ancestor of the managed root, and an
  ANCESTOR-symlink regression with surviving sentinels proves it.
- The identity classified, the identity reserved, and the identity deleted are
  provably the same, with a regression that substitutes a child and a root
  mid-operation.
- A live run is never reclaimed, including one admitted but not yet recorded, and
  including a dispatch whose =--worktree= names a nonconforming directory.
- "Kept" means untouched, or the report says precisely what it touched.
- No test-only hook is reachable in a release build.
- Only then is the TASK-M47E5.3 gate removed.
** Inherited from the M47E5.3 gate review
The gate review returned APPROVE with no findings. Three things it left for this task.

1. UN-IGNORE ALL NINE retained behaviour tests as part of this redesign, and re-run
   the destructive-path review BEFORE reconnecting either =*_ungated= function.
   They are listed on TASK-M47E5.3.

2. THE GATE OWN STRUCTURAL GUARANTEE WAS NEVER PINNED BY A FIXTURE. The gate tests
   boot a daemon and seed a valid project, so "refuses before project-root
   resolution and before daemon contact" is proved by the one-line body rather than
   by a test that would fail if the gate slipped later in the function. An
   empty-tempdir / no-daemon refusal case would make that executable evidence. Only
   worth adding if a gated state persists for any length of time.

3. A REVIEWER DESIGN OPINION WORTH WEIGHING, recorded because it is a real argument
   and not a defect: it would have REVERTED the unsafe implementation from compiled
   source rather than gating it, keeping the nine tests and pointing this task at
   the prior commits and review reports — on the grounds that git is already a
   durable archive, and dead code that once deleted the wrong things is a loaded gun
   that =#[allow(dead_code)]= keeps quiet. The manager ruled to RETAIN it so this
   task starts from working code. IF THIS REDESIGN STALLS OR IS DEFERRED, revisit
   that ruling rather than leaving the implementation sitting there indefinitely.

