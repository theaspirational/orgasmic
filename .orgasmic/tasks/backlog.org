#+title: Backlog
#+orgasmic_version: 1

* BACKLOG TASK-SQE9P Live rmux finalize prompt-spec smoke (DWJVH part-1, publish-gated)    :dispatch:hardening:followup:
:PROPERTIES:
:ID: TASK-SQE9P
:RELATES_TO: TASK-DWJVH
:END:

Spun out of TASK-DWJVH at its close: the DWJVH code items (A caller-identity ownership check, B stall-race resilience, C --status blocked test) are DONE (merged 14a79c0). This is the remaining part-1 verification only, deferred because it requires the finalize verb to be in a DEPLOYED daemon.

At runtime-publish time: dispatch a real rmux implementer against a freshly-built ISOLATED daemon (non-4848; isolated-smoke-daemon recipe — CLAUDE_CODE env, folder-trust, rmux SDK/CLI socket gotchas) and confirm the worker, following its prompt spec, actually CALLS 'orgasmic dispatch finalize' and the dispatch closes with the report intact (no transcript recovery). NEVER boot a prod daemon from a worktree (4848 LaunchAgent hijack). The mechanism itself is already covered by the dispatch.rs integration tests + the demoted-scrape daemon log line; this verifies prompt-spec ADHERENCE in practice. RELATES_TO TASK-DWJVH.
