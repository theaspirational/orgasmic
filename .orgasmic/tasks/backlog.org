#+title: Backlog
#+orgasmic_version: 1

* BACKLOG TASK-P8ZSP Native Windows PATH support for the CLI (PowerShell/cmd, %USERPROFILE%)    :install:path:windows:platform:
:PROPERTIES:
:ID: TASK-P8ZSP
:END:

Extends dec_CKMW3 and dec_YKFZX. The current PATH-hardening is a POSIX-shell mechanism (env file sourced from ~/.*rc); native Windows shells do not read it. Make the CLI reachable on native Windows.

Work required:
- Home resolution: orgasmic-core Home::from_env uses $HOME; add %USERPROFILE% (and/or %ORGASMIC_HOME%) fallback for Windows.
- Installer: install.sh is bash-only; provide a Windows install path (install.ps1 or equivalent) that downloads/places the runtime bundle.
- PATH wiring: instead of a sourced sh env file, set the user PATH via PowerShell $PROFILE and/or setx / the User-PATH registry value; make it idempotent and reversible.
- Binary linking: Windows symlinks need Developer Mode/privilege — fall back to copying the binary or a shim .cmd/.exe launcher into a bin dir on PATH.
- CLI surfaces: path_env::relink_source_binary currently bails on non-unix; add Windows variants for `orgasmic path ensure`, `orgasmic doctor --fix`, and the doctor 'cli on PATH' finding.
- Verification: confirm `orgasmic` resolves in a fresh PowerShell and cmd session.

Note: WSL and Git-Bash/MSYS2 already work via the unix path; this task is specifically native PowerShell/cmd.

* BACKLOG TASK-2DFTX.1 Core: generic path/tree helper + DecisionNode.parent + :PARENT: edge vocab    :decision-model:
:PROPERTIES:
:ID: TASK-2DFTX.1
:END:

Part of [[TASK-2DFTX]]. In orgasmic-core: (1) extract a generic, class-agnostic path/tree helper — validate :PARENT:, derive parent + ancestor chain, cycle/self-parent check — reusable by tasks later; (2) DecisionNode gains parent parsed from :PARENT: (schema.rs:328); (3) add :PARENT: to the canonical edge vocabulary ([[dec_HJENQ]]). Same-class + create-time-existence rules live here.

* BACKLOG TASK-2DFTX.2 Daemon index: decision tree read-model (parent/children/depth/path, orphan, ghost)    :decision-model:
:PROPERTIES:
:DEPENDS_ON: TASK-2DFTX.1
:ID: TASK-2DFTX.2
:END:

Part of [[TASK-2DFTX]]. In orgasmic-daemon/index.rs: build a decision tree index mirroring build_subtask_index (index.rs:1404) — parent, children ordered by document position, depth, derived full-numeric path (root = 1-based index among roots). Orphan :PARENT: -> ParseError (mirror task orphan, index.rs:1415). Superseded parent with live children kept as a ghost grouping node.

* BACKLOG TASK-2DFTX.3 Daemon API: :PARENT: contract validation + reparent tx tag    :decision-model:
:PROPERTIES:
:DEPENDS_ON: TASK-2DFTX.1
:ID: TASK-2DFTX.3
:END:

Part of [[TASK-2DFTX]]. In orgasmic-daemon/api.rs decision create/update/delete handlers: enforce the :PARENT: contract (cycle/self-parent, same-class, create-time existence, block deletion of a parent with children). Re-parent rides POST /decisions/:id updating :PARENT:; tag :PARENT:-changing updates as reparent in tx (api.rs:6751). No new endpoints.

* BACKLOG TASK-2DFTX.4 CLI: outline in decision list/get    :decision-model:
:PROPERTIES:
:DEPENDS_ON: TASK-2DFTX.2
:ID: TASK-2DFTX.4
:END:

Part of [[TASK-2DFTX]]. orgasmic-cli: decision list renders the indented numbered outline (1 / 3.1 / 3.2.1); decision get shows parent, children, path. CLI stays mutation-free for nesting (re-parent via API/UI).

* BACKLOG TASK-2DFTX.5 UI: tree-primary DecisionsView + breadcrumb/children/PARENT chip + add-sub-decision    :decision-model:
:PROPERTIES:
:DEPENDS_ON: TASK-2DFTX.2 TASK-2DFTX.3
:ID: TASK-2DFTX.5
:END:

Part of [[TASK-2DFTX]]. UI (ui/src): DecisionsView becomes tree-primary (collapsible, path numbers, ghost superseded, ancestor-context on tag-filter); NodeModal gains a parent breadcrumb, ordered children list, single-value PARENT link-chip; descriptor.ts gains a PARENT field; an 'Add sub-decision' action prefills the parent. Drag-and-drop and Graph-page nesting deferred. See [[dec_PA23Y]].

* BACKLOG TASK-KZ0VF Rewrite existing decision titles to problem-form (no-scars pass)    :decision-model:
:PROPERTIES:
:DEPENDS_ON: dec_2GQ7Q
:ID: TASK-KZ0VF
:END:

Separate from [[TASK-2DFTX]] per [[dec_2GQ7Q]] (consequence 9b). Rewrite existing decision titles from noun-phrase labels to problem/question form so title and body stop duplicating and the corpus stays consistent (no-migration-scars value). A per-decision judgment pass, best run as its own grilling/authoring session; not part of the nesting mechanism.

* BACKLOG TASK-KNENN Harden default instructions: route implementation through orgasmic workers, not raw provider CLIs    :conventions:agent-dx:
:PROPERTIES:
:ID: TASK-KNENN
:END:

Harden the agent-facing default instructions so the next agent dispatches implementation work through orgasmic workers, never a raw provider CLI.

Observed failure: asked to "implement all, use codex 5.5 xhigh on fast" inside this managed repo, an agent reached for the raw `codex` binary (`codex --help`, `codex exec ...`) instead of `orgasmic manager dispatch`. Shelling out to a provider CLI bypasses the daemon write authority, tx recording, worktree isolation, dispatch leases, and reattach/recovery — the entire reason work is managed here.

Fix: add an explicit rule to `conventions/orgasmic-tooling.org` (and/or `contributing.org`) stating that any request to implement/dispatch work — especially one naming a provider+model+effort like "codex 5.5 xhigh" — maps to `orgasmic manager dispatch --kind implementer --worker implementer-<harness>-<driver> --model <model> --effort <effort> --brief <path>`, resolving the worker from `orgasmic worker list` (e.g. codex 5.5 xhigh -> implementer-codex-tmux, model gpt-5.5, effort xhigh; service_tier fast comes from the codex harness config). Raw `codex`/`claude`/`cursor-agent` invocations are off-limits for doing project work. State the why (write authority, tx, leases, reattach) so it sticks.

Scope: docs/conventions only; a worded rule + a short mapping table from "provider model effort" to worker id. No code. Consider whether the daemon should also surface workers more discoverably to agents.

