#+title: Backlog
#+orgasmic_version: 1

* BACKLOG TASK-DWJVH dispatch finalize residuals: live rmux prompt-spec smoke + caller-identity auth + finalize/stall race    :dispatch:hardening:followup:
:PROPERTIES:
:ID: TASK-DWJVH
:END:

Filed from the WFW1N adversarial review + acceptance criterion 6. Do at runtime-publish time (finalize verb is not in the deployed daemon until then, so a live smoke before publish is moot). Three items: (1) Criterion-6 LIVE rmux smoke — dispatch a real rmux implementer against a freshly-built ISOLATED daemon (non-4848; per the isolated-smoke-daemon recipe: CLAUDE_CODE env, folder-trust, rmux SDK/CLI socket gotchas) and confirm the worker, following its prompt spec, actually CALLS 'orgasmic dispatch finalize' and the dispatch closes with the report intact (no transcript recovery). NEVER boot a prod daemon from a worktree (4848 LaunchAgent hijack). Mechanism itself is already covered by the dispatch.rs integration tests + the demoted-scrape daemon log line; this verifies prompt-spec ADHERENCE in practice. (2) Review #4 LOW: 'orgasmic dispatch finalize' has no caller-identity authorization — any local process can finalize a live run and forge implementer.done + commit + verbatim last.txt. Matches the pre-existing localhost/unauthenticated dispatch-close trust model (not a regression), but the new verb widens the surface to worker authority; consider binding finalize to the run's RuntimeIdentity/ownership check (supervisor check_ownership) like send_input does. (3) Review #5 LOW: narrow race — if the stall timeout releases the run in the window between resolve_finalize_run (live) and the finalize's own release, the worker's release fails after it already committed + wrote last.txt, while the watcher independently records orphan → done+orphan contradiction for one run. Also add a test for --status blocked → manager.dispatch_aborted (currently wired but untested).
