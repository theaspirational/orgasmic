#+title: Backlog
#+orgasmic_version: 1

* BACKLOG TASK-P19ZM Daemon boot follow-ups from TASK-35TK4 review    :daemon:hardening:
:PROPERTIES:
:ID: TASK-P19ZM
:PRIORITY: medium
:END:

Independent review of b18808c..bd3d7bd found additional non-blocking follow-ups outside the shipped slice: pre-bind project filesystem work remains unbounded; timed-out Git lookup should surface an actionable macOS Files-and-Folders hint; live reindex currently drops a project when its scan times out; daemon KeepAlive/restart semantics deserve an end-to-end regression; daemon-home comparison should avoid raw path-string equality; and repo_url should clear when a remote origin is removed.

Acceptance: split or address these findings with focused regressions, preserving the lock-first and no-pre-bind-Git-spawn invariants. Keep TASK-RRVX0 separate because it is the new CLI lock-probe overlap risk.

* BACKLOG TASK-4DTBZ rmux review transport must survive auth-shadow and daemon disconnects    :drivers:rmux:claude:hardening:
:PROPERTIES:
:ID: TASK-4DTBZ
:PRIORITY: high
:RELATES_TO: TASK-QSX56
:END:

Two consecutive TASK-QSX56 reviewer dispatches blocked the required Opus verdict for different transport reasons. Attempt 1 inherited a stale ANTHROPIC_AUTH_TOKEN from the foreground daemon and native Claude returned 401 even though the stored claude.ai login was healthy. After restarting the daemon with provider auth overrides removed, attempt 2 reached real Claude Opus 4.8/high and performed substantive analysis, then the rmux daemon closed the pane-output-cursor transport after 4m25s; the worker released protocol_end_without_finalize and no report was preserved. Define explicit provider-auth environment precedence/scrubbing for rmux launches, and make a daemon-side rmux disconnect preserve or reattach the native Claude session/report rather than losing the review. Add deterministic regressions for stale auth shadowing and rmux daemon disconnect during an active reviewer session.

* BACKLOG TASK-QSX56 Cursor ACP must authorize scoped project workflow commands and finalize :drivers:cursor:acp:hardening:
:PROPERTIES:
:ID: TASK-QSX56
:PRIORITY: high
:RELATES_TO: TASK-2YZDJ
:END:

A real non-simulated cursor-agent/acp-stdio TASK-2YZDJ run reached native grok-4.5[effort=high,fast=true], read project files, and emitted tool envelopes, but the adapter returned reject_once for the worker shell probe `which orgasmic; orgasmic entry; echo "EXIT_CODE=$?"`. The request payload had nested toolCall.kind=execute and only title/content, with no structured input; current shell_command_allowed accepts only a narrow git/grep/find grammar and cannot authorize the mandatory `orgasmic entry` / `orgasmic dispatch finalize` workflow. Implement a bounded, injection-resistant authorization path for the exact project workflow commands needed by a Cursor worker, using the nested ACP payload actually emitted. Preserve worktree/path confinement and fail closed for unsafe shell controls/unknown commands. Add fixture regressions for the captured payload, permitted entry/finalize commands, and unsafe compound/foreign commands. Prove a real Cursor canary can run entry and finalize. Do not widen into TASK-2YZDJ feature implementation.
** Evidence
Codex gpt-5.6-terra/high implemented the bounded title-only Cursor ACP permission path in 8e3854e. The captured nested payload remains rejected as a compound command; standalone `orgasmic entry` and worker-bound `orgasmic dispatch finalize` are authorized, while shell control, foreign/destructive commands, task/run mismatches, and path escapes fail closed. Worker gates: drivers lib 208 pass, driver clippy/fmt/diff-check green, live legacy Cursor bridge green. Manager independently reran both new focused regressions, fmt, and diff-check before landing. Merged as aa4bb55; rebuilt source daemon boot 43ee051a-2ee7-476c-86aa-98d0dcbb484b is serving the patch. Live ACP workflow proof and independent Claude review remain.
Independent review is not yet complete. First rmux/Opus attempt never reached code because a stale inherited ANTHROPIC_AUTH_TOKEN caused 401; stored claude.ai auth was healthy. After restarting without provider auth overrides, the single retry reached real Claude Opus 4.8/high and inspected the security-sensitive routing, but rmux closed the pane-output-cursor transport before a verdict. Partial transcript identified no bypass before interruption, but it is not a SHIP verdict. TASK-4DTBZ tracks the rmux reliability defect; TASK-QSX56 stays IN_REVIEW.

* BACKLOG TASK-CQM2X Crash-safe run inventory, bounded history, and manual retro foundation    :daemon:recovery:retro:plan:
:PROPERTIES:
:ID: TASK-CQM2X
:IMPLEMENTS: arch_RN73Z
:DEPENDS_ON: TASK-KWSTJ TASK-FZB6T TASK-VBSG2 TASK-5P60H TASK-3R3EZ TASK-TJKFC
:END:

Umbrella plan for the 2026-07-23/25 incident family. The live 0.0.14 daemon reproduces authenticated `GET /api/runs` with no headers or body before a 12-second timeout; unauthenticated 401 and other routes return immediately. The purported complete output file in the follow-up report is not evidence of response framing because curl reported zero downloaded bytes and a fresh output path remained absent. Source inspection shows inventory reconstructs classifications from every historical session JSONL, performs driver attach work, revalidates missing worktrees, and historically rebuilt a whole-board recovery-origin index per failed tombstone.

Measured production evidence: 197 orgasmic session files occupy 2,404,385,576 bytes (2.239 GiB); the largest is 114.7 MB. Legacy Claude/rmux files contain thousands of rendered TUI redraw chunks. `/api/runs` polling also causes repeated missing `ddd/.orgasmic/project.org` warnings. Current source already contains uncommitted TASK-JN5Q1 work for bounded attach probes and exact-run close/finalize; preserve and verify it rather than replacing it.

Execute the child tasks independently in priority order. P0 restores and proves the control plane. P1 separates current lifecycle inventory from historical evidence and bounds retained event volume. Service and registry hardening follow without blocking P0. Manual retro materialization is a separate explicit feature built on the bounded evidence architecture; it is not required for `/api/runs` to work and must not become an automatic startup/completion hook.

Done only when the production-shaped wire regression completes inside its deadline, exact-run close/finalize do not enumerate history, run enumeration cost is independent of historical transcript bytes, default storage cannot recreate rendered-TUI growth, stale/missing records stop producing recurring warnings, and an explicit multi-run retro can analyze selected Claude sessions through derived bounded evidence.

* BACKLOG TASK-FZB6T P1 compact run catalog and bounded semantic session retention    :storage:recovery:sessions:
:PROPERTIES:
:DEPENDS_ON: TASK-KWSTJ
:ID: TASK-FZB6T
:PRIORITY: P1
:END:

Separate operational lifecycle inventory from transcript/evidence storage so run enumeration and daemon recovery scale with record count, not transcript bytes. This task follows the P0 wire repair and owns the durable shape rather than adding more per-request scans.

Design and implementation:
1. Maintain a compact per-run catalog entry containing identity, project/task/kind, driver/harness, verified native metadata reference, worktree authority state, lifecycle classification, terminal outcome/time, and replacement/claim link. Update it through the existing session writer/supervisor boundary; rebuild legacy entries once with bounded prefix/tail parsing and cache them by file identity.
2. `/api/runs` serves actionable live/recoverable records plus a bounded recent terminal window from the catalog. Older terminal history is paged or queried explicitly; it is never reclassified on every poll. Missing/pruned worktrees become a stable tombstoned authority state, not an eternal ambiguous attach candidate.
3. Lock future storage: rendered tmux/rmux redraw or scrollback events remain forbidden; add a regression proving a long TUI session does not grow orgasmic JSONL from screen repaint traffic. For structured ACP/subprocess events, retain lifecycle and compact semantic tool/progress events needed for recovery and retrospective analysis, but cap individual text/tool-result/thinking payloads and replace overflow with digest, byte count, and source reference. Never copy harness-native JSONL automatically.
4. Add `run history inspect` and an explicit dry-run-first maintenance command to compact or archive legacy rendered-TUI payloads. It must report files/bytes by driver+harness+event class, preserve lifecycle/native correlation, write recoverably, and require explicit operator confirmation before deletion or irreversible compaction. No automatic cleanup of existing 2.239 GiB.
5. Define default retention by authority: lifecycle/catalog kept; compact semantic events retained within a documented budget; derived retro evidence cache is disposable; vendor-native history remains vendor-owned.

Acceptance: inventory latency and boot reconciliation are independent of payload bytes after the one-time legacy index; 197-file production-shaped history yields the same actionable classifications before/after catalog migration; current TUI generates zero persisted redraw chunks; ACP/tool evidence remains sufficient to count calls/results/retries/latencies while oversized outputs are bounded; dry run exactly accounts for reclaimable legacy bytes and changes nothing. Include migration rollback and corruption tests.
** Notes
Handoff from TASK-KWSTJ: the bounded legacy fallback this task's item 1 calls for already exists as `orgasmic_core::scan_session_lifecycle` (prefix/tail byte window, transcript lines rejected from raw bytes without parsing, `truncated` and `final_envelope_retained` flags for conservative classification). Measured on the real 198-file / 2.239 GiB board: 26.67 MiB inspected in 95.3 ms versus 3.08 s for a full read. The durable catalog can be built by caching that scan's result by file identity rather than by inventing a second reader. `InventoryStageMetrics` on `GET /api/runs` gives the before/after signal for the catalog work.

* BACKLOG TASK-VBSG2 Add explicit Claude evidence materialization and manual multi-run retro dispatch    :retro:claude:evidence:
:PROPERTIES:
:DEPENDS_ON: TASK-FZB6T
:ID: TASK-VBSG2
:IMPLEMENTS: arch_RN73Z
:PRIORITY: P1
:END:

Implement the smallest end-to-end slice of arch_RN73Z after operational run storage is bounded. This is an explicit diagnostic feature, not part of normal dispatch, completion, startup, recovery, or run listing.

Slice A — materializer:
- expose `orgasmic run evidence materialize --run <id>` and one equivalent daemon/agent-callable function
- reuse verified NativeRuntime provider/session/path metadata and the existing native-transcript resolver; fail closed when correlation is missing or ambiguous
- parse Claude native JSONL through a Claude-local adapter into the existing DriverEvent/activity vocabulary, including tool_use, tool_result, timestamps, token/context fields when present, and terminal signals
- write an idempotent, versioned `derived_native` cache keyed by source digest + converter version under `.orgasmic/tmp/retro/evidence`; enforce per-event/per-run limits and return a compact summary + reference, never the full transcript
- prove derived events cannot enter supervisor recovery/liveness/finalization paths

Slice B — manual retro:
- add `orgasmic manager retro` accepting repeated run ids, task ids, or an explicit ordered task sequence
- resolve those selectors once into an immutable scope manifest with run ordering, task/dispatch links, source digests, missing evidence, and requested questions
- dispatch a read-only retrospector that starts from catalog summaries, lazily calls the materializer for only needed runs, and reports evidence-linked bottlenecks: repeated equivalent tool calls, retries/failures, tool latency, idle/auth/permission waits, redundant reads, context resets, circular reasoning, output volume, token use, and progress per wall time/token
- restrict writes to the derived evidence directory and report artifact; no task transitions, cleanup, lease/recovery action, dispatch closure, source/vendor mutation, or code edits

Use a backlog-to-done task with multiple implementer/reviewer/recovery runs as the integration fixture. Assert a second materialization is a cache hit, a source change creates a new digest entry, an ambiguous transcript is refused, and a retro spanning several runs produces cross-run findings without injecting raw transcript volume. Routine scheduling is explicitly deferred: a future routine must call this same manager path and be user configured; do not add an automatic trigger here.
** Notes
Blocked in practice for the acp-stdio transport by TASK-VB9DQ. Measured 2026-07-25: the acp-stdio claude adapter passes `--no-session-persistence` and writes zero native transcripts (0 new files under `~/.claude/projects` versus 1 for an otherwise identical run), never passes `--session-id`, and never emits a `NativeRuntime` lifecycle event. Slice A's materializer resolves verified NativeRuntime metadata and parses Claude native JSONL, so for acp-stdio runs there is nothing to resolve and nothing to parse. The rmux/tmux transport is unaffected — it mints an explicit session id and records launch/resume argv. Land TASK-VB9DQ first, or scope this task to TUI-transport runs and say so explicitly.

* BACKLOG TASK-5P60H Harden launchd boot backoff and prove single-owner slow startup    :daemon:launchd:recovery:
:PROPERTIES:
:DEPENDS_ON: TASK-KWSTJ
:ID: TASK-5P60H
:PRIORITY: P1
:END:

Treat the crash-loop report as a regression audit of the now-present lock-first boot protocol, not as proof that all suggested redesigns are still needed. Current source already acquires the home lock before project scanning, publishes boot-state heartbeats, makes explicit start return `StillBooting` after 20 seconds, lets autostart follow live heartbeats up to a finite backstop, retries bind for 8 seconds, and the current 3,118-tx daemon reached listening in about 8.45 seconds. Preserve those semantics.

Work:
1. Add a deterministic service-level test with an artificially slow project scan longer than the old readiness timeout and concurrent `daemon start`/autostart attempts. Assert exactly one lock owner and one eventual listener, callers observe the same pid/boot phase, no competitor reaches bind, and no healthy boot is killed/restarted.
2. Add an explicit `ThrottleInterval` to the generated macOS LaunchAgent and verify install/update rewrites existing plists. Keep restart policy intentional and test repeated abnormal exits use launchd backoff.
3. Correlate `daemon.lock`, boot-state owner, pid liveness, and authenticated status in diagnostics. A lock-held pre-bind owner with fresh heartbeat is `starting`, not unhealthy; a dead owner is stale and may be replaced only after the OS lock is actually released. Never break a live file lock based only on HTTP failure.
4. Capture phase durations and tx/project counts for config, project scan, session migration/catalog load, watcher attach, reattach, and bind. Optimize or make replay incremental only if measurement still exceeds the accepted startup budget after the run-catalog work; do not expose normal API reads from a partially rebuilt index merely to make health green. A tiny early health responder is allowed only if it cannot serve normal routes and tests prove the handoff to the full router.
5. Make the no-progress/absolute readiness budgets configurable only if production measurement shows current heartbeat behavior insufficient; preserve a finite backstop.

Acceptance: the slow-start matrix produces no alternating Address-in-use/instance-lock errors, launchd does not hot-loop, one daemon becomes ready, and normal routes see a fully rebuilt index. Include a real generated-plist assertion and a macOS smoke or launchctl transcript where available.
** Notes
Handoff from TASK-KWSTJ: `GET /api/runs` now classifies from a bounded prefix/tail lifecycle scan, but `reattach_live_runs_on_boot` still calls `read_session_file` on every session JSONL — on the reported board that is 2.239 GiB read at boot before the daemon can serve. It was left alone deliberately: boot reattach takes the *last* Acquire/RunMeta rather than the first, so switching it to the bounded scan is a semantic change that belongs with the slow-start work, not with the request-path repair. `orgasmic_core::scan_session_lifecycle` is ready to reuse.

* BACKLOG TASK-3R3EZ Quarantine missing project/worktree records, warn once, and add project remove    :daemon:project:recovery:
:PROPERTIES:
:DEPENDS_ON: TASK-FZB6T
:ID: TASK-3R3EZ
:PRIORITY: P2
:END:

Fix the repeated missing `ddd/.orgasmic/project.org` warning at its actual source: run-inventory polling revalidates historical RunMeta worktrees and logs on every request. Also complete board lifecycle so an operator can intentionally deregister a project.

Behavior:
1. The run catalog stores worktree/project authority as verified, missing, invalid, or unavailable with the observation time and file identity. Missing/pruned worktrees for pid-gone terminal history become stable tombstoned metadata and are not attach candidates. Inventory reads do not touch or warn about the same missing path repeatedly.
2. A registered project whose root or `.orgasmic/project.org` disappears transitions to `unavailable`; emit one structured warning/event per transition, retain last-good read state where safe, stop project refresh/repo-url/session scans, and unwatch paths that still have a watch. Reappearance triggers revalidation and a recovery event. Do not silently deregister on one ENOENT because removable volumes and temporary renames are valid.
3. Add `orgasmic project remove <id>` through the daemon writer. It removes only the board registration/watch/index entry, never the repository or its `.orgasmic` files. Default output previews the target identity/path; support idempotency and require an explicit confirmation or force flag in noninteractive use. Refuse when live runs/leases still belong to the project unless an explicit safe cleanup flow resolves them first.
4. Board refresh must diff added and removed entries: start watches/load added projects, unwatch/drop removed projects, and never leave stale index entries.

Regression: poll `/api/runs` repeatedly with several records pointing at one pruned worktree and assert zero repeated filesystem reads/warnings after the first classification; remove a board project and assert its watch/index vanish while files remain; simulate temporary disappearance/reappearance and assert one unavailable and one recovered event. Measure warning volume, not only return values.

* BACKLOG TASK-CM5KV Evaluate real ACP as an orgasmic transport    :drivers:research:
:PROPERTIES:
:ID: TASK-CM5KV
:PRIORITY: P2
:DEPENDS_ON: TASK-XCJYC
:END:

Evaluate adding the Agent Client Protocol as a real orgasmic transport, once TASK-XCJYC frees the name.

ACP is an open standard from Zed Industries (August 2025, Apache-licensed): JSON-RPC 2.0 over stdio for local agents, HTTP/WebSocket for remote (work in progress). It standardises the editor-to-agent boundary; MCP standardises tool access beneath it. Adopted by JetBrains, Google and GitHub, with 25+ agents.

The draw is consolidation. Zed's ACP Registry already lists Claude Agent, Codex, Gemini CLI, OpenCode, Copilot, Cursor and Pi Coding Agent. Orgasmic currently maintains a bespoke adapter per harness — `adapters/claude.rs` (stream-json), `adapters/codex.rs` (app-server), `adapters/cursor_acp.rs`, `adapters/hermes.rs` — each with its own event mapping, session identity story and transcript-finder strategy. One ACP transport could in principle serve several of them through a single event mapping.

Open questions to answer before committing to it:
1. Does ACP expose what orgasmic's supervisor actually needs — terminal declaration, tool-call visibility, stall signals, and a resumable native session identity? A protocol built for editor UX may not carry the lifecycle facts recovery depends on.
2. Does an ACP-launched agent still write its vendor-native transcript, or does the adapter layer hide it? The retro contract depends on vendor JSONL existing and being resumable (see dec on transcript ownership).
3. Is the ACP adapter an extra process between orgasmic and the harness, and what does that mean for the startup preflight (TASK-TJKFC) and for credential handling (TASK-Z8WEJ)?
4. Would it replace the bespoke adapters or sit alongside them? A fifth transport that duplicates four existing ones is a cost, not a saving.

This is deliberately a research-and-decide task, not an implementation commitment. If the answer is that ACP does not carry orgasmic's lifecycle needs, recording that is a complete and useful result.

Depends on TASK-XCJYC only for the name; the evaluation can start earlier.

* BACKLOG TASK-7QM8M Bound the bytes boot auto-reattach reads per session file    :daemon:boot:performance:
:PROPERTIES:
:ID: TASK-7QM8M
:PRIORITY: P1
:END:

Boot auto-reattach reads every session file whole. It calls
=read_session_file= -> =std::fs::read_to_string=, so a project with large TUI
transcripts pays their full size at every daemon start. This is the same
unbounded read TASK-KWSTJ removed from =GET /api/runs=, still present on the
boot path; measured there, a 2.4 GB board cost 24.7 s and the bounded scanner
reduced it to 28 MB inspected.

TASK-KKGKM moved this scan after bind and onto a blocking thread, so it can no
longer stop the daemon serving, and it is no longer on the critical path. What
remains is cost and latency: reattachment of the operator's live manager
terminal is delayed by however long the read takes.

=boot_reattach_candidate= consumes only lifecycle-class events — acquire,
run_meta, release, the terminal driver event, and the external-registration
marker — which is exactly what =scan_session_lifecycle= retains, so the
substitution should be direct.

One correctness subtlety must be handled explicitly rather than assumed. The
function calls =latest_run_segment=, which picks the newest contiguous run
segment by run_id. On a truncated scan the prefix may hold one segment's
acquire while the tail holds a later segment's end, so the two windows can
describe different runs. =classify_session_files= already guards this with
=scan.final_envelope_retained= before trusting =release_outcome=; reattach needs
an equivalent rule. Getting it wrong means a live run is silently not
reattached, which looks like the run vanished.

Acceptance:
- boot reattach inspects bounded bytes per session file, not file size
- a truncated scan cannot combine two run segments into one candidate
- a still-live run in a multi-segment session file is still reattached
- measured against a production-shaped board, as TASK-KWSTJ was

* BACKLOG TASK-6HJYT Split the liveness question out of the recovery inventory    :daemon:api:architecture:
:PROPERTIES:
:ID: TASK-6HJYT
:PRIORITY: P1
:END:

Problem: =GET /api/runs= answers three unrelated questions with one full pass
over every session file on disk.

1. "Is anything live?" — the CLI's fence before restart, update, and
   dispatch-close (=daemon_lifecycle.rs::live_manager_runs=). It reads the
   response, keeps =live=, and filters to =task_id= starting =manager.launch:=.
2. "What should the UI badge show?" — =ui/src/hooks/useTaskRuns.ts=.
3. "What needs recovery?" — the actual crash-recovery inventory, which is the
   only one of the three that genuinely requires reading durable history.

Question 1 needs no disk at all. Live runs are supervisor-local: the daemon
already holds them in memory, with their identities. Answering it through the
recovery inventory means a restart fence pays for classifying every historical
run on the board.

The codebase already contains this reasoning, applied to the singular endpoint
and never to the plural one. =get_run= (api.rs, "Exact active-run lookups are
supervisor-local") checks =state.supervisor.snapshot()= first and only falls
back to =recovery_status= when the id is not live, with the comment: "Worker
finalize and manager close must not scan unrelated durable recovery history
before they can act on a live run whose id they already know." That is exactly
the argument for splitting the list endpoint too.

Why this is worth doing even though the scan is now cheap: the coupling, not the
duration, is the defect. Measured on the operator's board 2026-07-25 after
TASK-KWSTJ: 224 session files, 2.42 GB on disk, 31.4 MB inspected, 0.24 s to
first byte — down from 41.9 s. Before that fix the same fence failed outright:
=manager dispatch-close --status aborted= died after 10.03 s against the CLI's
10 s deadline, so a dispatch that failed at startup could not be closed by the
documented verb. Two defects multiplied — startup left the mess, enumeration
prevented cleaning it. A control-path check that cannot fail for reasons
belonging to unrelated historical runs removes that whole class.

Work:
1. Give the liveness question its own answer sourced from the supervisor
   snapshot, with no session-file reads. The CLI fence and any other
   "is something running right now" caller move onto it.
2. Keep =GET /api/runs= as the recovery inventory it actually is. Decide
   explicitly whether the UI badge path belongs on the live source, the
   inventory, or a third shape — it wants live state per task, not history.
3. Do not duplicate classification logic. If the live bucket is derived in two
   places it will drift; the supervisor snapshot should be the single source for
   "live" and the inventory should consume it rather than recompute it.
4. Naming should stop implying that =/api/runs= is a general run list. It is a
   recovery inventory and reads as one only after reading the handler.

Acceptance:
- the restart/update/dispatch-close fence performs no session-file reads
- that fence still refuses correctly while a manager terminal is live, and
  still permits when only historical runs exist
- =GET /api/runs= keeps its current classifications and bounded-scan metrics
- a regression proves the fence answers on a board whose session files are
  unreadable or absent, since its correctness no longer depends on them

* BACKLOG TASK-MRJRK A registered project can be missing from the index, and the 404 blames the wrong thing    :P1:
:PROPERTIES:
:ID: TASK-MRJRK
:PRIORITY: P1
:END:

Problem: a project can be registered on the board, listed by `orgasmic project
list`, and still be absent from the index snapshot, at which point every route
that addresses it by id fails with a 404 that names the project as though it
were unregistered. The operator's only signal is an error that points at the
wrong thing, and the only known remedy is `orgasmic reindex` - which nobody
tries, because nothing suggests the index is the problem.

Two things need fixing, and they are separable.

1. The diagnosis. `select_project` (api.rs) reports a missed lookup as
   `not_found("project {id}")`. That message is indistinguishable from a genuinely
   unknown project, so it sends the reader to check registration - the one thing
   that is definitely fine, since `project list` just showed it. The message
   should distinguish "no such project on this board" from "registered but not
   in the current index snapshot", and the latter should name the remedy
   (`orgasmic reindex`). This is cheap and is most of the operator-visible value.

2. The underlying gap. A project that is registered but missing from the
   snapshot is a state the daemon can sit in indefinitely: nothing retries, and
   nothing reports it. Whatever the cause, the daemon should either converge on
   its own or say plainly that it has not. A `status` field naming registered-
   but-unindexed projects would make the condition self-reporting rather than
   something an operator stumbles into through an unrelated 404.

Do not start by assuming a cause. What is actually established is thin, and one
plausible-sounding explanation has already been checked and ruled out.

EVIDENCE, 2026-07-25, live on the operator's daemon
Found incidentally while trying to record gotchas during TASK-TJKFC.

- Daemon booted 17:53:31Z (pid 10647, boot_id a6fdd006). It had restarted on its
  own during the session; the previous instance was pid 71878 from 16:49.
- At ~18:03Z - ten minutes after that boot, so this is NOT a boot race -
  `orgasmic node body append <anything> --project orgasmic` returned
  `404 {"error":"project orgasmic"}`. Same for ddd.
- The same command with `--project orsl`, `--project lynx-auth` and
  `--project rare` got PAST project resolution (they failed later, on the node
  id or the glossary file). So exactly two of five were missing.
- `orgasmic project list` showed all five with clean ids at that moment. Ids
  were checked through `cat -v` for hidden characters: clean.
- `orgasmic reindex` at ~18:04Z fixed it immediately. The retry of the same
  command then failed at the *next* stage instead, which is how we know project
  resolution had started succeeding.
- Corroborated in ~/.orgasmic/logs/daemon.out.log: the first `glossary file not
  found` warnings for ddd appear at 18:04:23Z - i.e. only after the reindex,
  because before it the request never got far enough to touch a file.

RULED OUT, so nobody re-runs it
- Not a boot race. Ten minutes elapsed, and the boot log shows a completed
  reattach pass at 17:53:35 with no scan-timeout or partial-load warnings.
- Not "`status` reads a different source". `status.projects` is
  `snap.projects.len()` (api.rs:5753) - the same map the lookup missed. So
  `status` should have reported 3, not 5, during the window. It was simply never
  run at that moment, which is the one piece of evidence this report is missing
  and the first thing to capture if it recurs.

STILL OPEN
Whether the two projects were dropped after a successful load, never loaded at
all, or present under a different key. Note `load_project` inserts under
`board_entry.id`, and the two affected projects are the two with the largest and
most actively written `.orgasmic/` trees - both were being written continuously
by this session while the others were idle. That is a correlation, not a cause.

REPRODUCTION NOTE
If this recurs, capture in this order before reindexing, because reindex
destroys the evidence: `orgasmic status` (the projects count is the decisive
missing datum), `orgasmic project list`, and the failing node call for each of
the five project ids.

ACCEPTANCE
- A lookup miss for an id that IS registered on the board reports that
  distinctly from an unknown id, and names `orgasmic reindex` as the remedy.
- The condition is visible without provoking it: an operator can see that a
  registered project is not in the current snapshot without first hitting an
  unrelated 404.
- A test drives a snapshot that is missing a registered project and asserts the
  error text distinguishes the two cases. The test must construct the state
  directly rather than wait for whatever produced it here, since the cause is
  not yet known - fixing the diagnosis must not be blocked on finding it.

* BACKLOG TASK-5M45A Hermes ignores the provider and model a dispatch asks for    :P1:
:PROPERTIES:
:ID: TASK-5M45A
:END:

Measured 2026-07-25 on hermes 0.16.0, driving `hermes acp` directly with the
same JSON-RPC handshake `acp_stdio` performs.

On the hermes stdio path orgasmic transmits the dispatch's provider and model
only inside `thread_start._meta.orgasmic` (`spawn_payload`, hermes.rs), and the
argv is a bare `hermes acp`. Hermes does not read them. Two `session/new`
calls, identical but for the provider:

  provider = "anthropic"             -> currentModelId ollama-cloud:glm-5.2
  provider = "nonexistent-provider"  -> currentModelId ollama-cloud:glm-5.2

Neither errored. Both landed on hermes's own configured default. An unknown
provider id is not even rejected, so there is no signal that the request was
dropped.

Consequences, in order of how much they cost:
1. A dispatch that specifies a provider silently runs on a different one. The
   operator is billed to the wrong account and gets a different model's output,
   with nothing in the session to say so.
2. It is worse than invisible, it is actively misreported: the `Ready` event's
   capabilities echo `cfg.provider` and `cfg.model` back
   (`on_ws_thread_started`), so the session JSONL records the provider that was
   ASKED FOR as though it were the one in use. Reading the session is the normal
   way to find out what a run did, and here it lies.
3. It blocks a hermes readiness probe. Checking a credential requires knowing
   which credential the launch consumes; while the provider is ignored, orgasmic
   cannot know. See the comment on `HermesAdapter` (TASK-TJKFC decided against a
   probe on exactly this ground).

Not yet established: whether hermes exposes any supported way to select a
provider per ACP session (a `session/set_config_option` equivalent, an argv
flag, or an env var). The adapter already builds a `/model ... --provider ...`
slash command in `transition_state`, so mid-run switching works; whether that
same path can be driven at startup was not tested. The fix might be as small as
issuing that command once after `session/new`.

Minimum acceptable outcome if selection turns out to be impossible: stop echoing
a provider and model the run is not using, so the session records what actually
happened rather than what was requested.

* BACKLOG TASK-5T575 dispatch_endpoint asserts worker names the daemon stopped minting    :testing:daemon:dispatch:
:PROPERTIES:
:ID: TASK-5T575
:PRIORITY: P1
:END:

Three tests in `crates/orgasmic-daemon/tests/dispatch_endpoint.rs` failed on
every run of the suite, on HEAD, unrelated to any change under test:

- dispatch_endpoint_routes_codex_through_supervisor_and_emits_run_created
- dispatch_with_task_worker_property_still_spawns_via_explicit_path
- dispatch_override_off_list_model_passes_through_with_warn

Two asserted `implementer-codex-appserver` / `pinned-implementer` and got
`implementer-codex-acp-ws`; the third timed out waiting for a run.created
carrying a name that is never written.

Root cause, two halves that hid each other:

1. `seed_worker` wrote worker templates to `$ORGASMIC_HOME/user/workers/`.
   Templates were retired — `lib.rs:454` logs "legacy user/workers directory
   ignored" at boot and reads nothing. 33 call sites were seeding a directory
   with no reader.
2. The tests sent `"worker_id"` in the dispatch body. `DispatchRequest`
   (api.rs:3787) has no such field and does not deny unknown fields, so serde
   dropped it silently. The daemon derives the label from the address alone,
   via `addressing::compatibility_worker_id(kind, mode, harness)`.

So the tests asked for a worker by a name with no reader, over a field with no
receiver, and asserted a result nothing produces. Each half looks deliberate on
its own; together they are inert.

Cost: the suite has been red on this crate on every full run, so it could not
answer "did my change break something?" — TASK-TJKFC's evidence had to carry
"dispatch_endpoint has 3 failures, identical on HEAD" as a manual caveat, and
every future change to this crate would have repeated that stash-and-compare.

Fix: delete `seed_worker`/`seed_worker_kind` and all 33 call sites; correct
the label literals to the ids the daemon actually mints; stop sending the
dropped `worker_id` field; assert on the pinned-worker test that a task's
stale `:WORKER:` property does NOT override the dispatch address (the real
current contract, and a stronger claim than the one it replaced); add
`asserted_worker_labels_match_the_daemons_naming_rule`, which pins all four
labels to `compatibility_worker_id` so the rule cannot drift silently again.

Related but distinct: TASK-STWVB (four daemon --lib tests that fail only under
full-suite parallelism). This one fails deterministically, alone or not.

* BACKLOG TASK-Y9BY6 live_rmux_exit_139_emits_a_fatal_terminal_event times out on every run    :testing:drivers:flake:
:PROPERTIES:
:ID: TASK-Y9BY6
:PRIORITY: P2
:END:

`modes::rmux::tests::live_rmux_exit_139_emits_a_fatal_terminal_event` fails
deterministically with `timed out waiting for rmux exit 139 event: Elapsed(())`
(rmux.rs:2871).

Measured 2026-07-26: 3/3 failures on a working tree carrying the TASK-SGRTX
changes, and 2/2 on stashed HEAD without them. So it is pre-existing and
unrelated to that work — recorded here so the next person does not re-derive
that, as happened this morning with the dispatch_endpoint three.

Not investigated. `rmux` IS installed (`~/.local/bin/rmux`) and an rmux-daemon
is running under the orgasmic LaunchAgent config, so "binary missing" is not the
explanation. Candidates worth checking first:
- the test drives the DEFAULT rmux socket, which the running LaunchAgent daemon
  may already own with different config (see the socket-split note in the
  isolated-smoke recipe)
- exit 139 is SIGSEGV; whether the stub actually segfaults on this machine and
  rmux reports it as 139 is unverified

Belongs with TASK-STWVB's theme: the drivers suite cannot answer "did my change
break something?" while one test fails every run for an unrelated reason.
CORRECTION [2026-07-26], same session. This task's opening claim that the test
"fails deterministically" is wrong.

It failed 5/5 when filed (3/3 on the working tree, 2/2 on stashed HEAD). Roughly
ninety minutes later the same test passes, and the full drivers lib suite is
231/231 green on 3 consecutive runs. Nothing in the test or in rmux changed
between those measurements.

What did change is machine state. Between the two, an isolated smoke daemon was
started and stopped twice on port 4899, rmux servers were killed during cleanup,
and a dispatched worker was running full test suites in a worktree. So the
trigger is environmental and shared — most likely the default rmux socket
ownership described in the isolated-smoke recipe — not the test's own logic.

Two other tests were also seen failing exactly once in that window and passing
on every run since:
- adapters::claude::tests::acp_stdio_accepts_a_dispatch_for_a_logged_in_claude
- adapters::claude::tests::acp_stdio_rejects_a_dispatch_for_a_logged_out_claude
- adapters::claude::tests::an_empty_endpoint_still_gets_a_verdict_because_it_still_spawns_claude

Those three manipulate process-global PATH and HOME to simulate a logged-out
harness, which is the documented shared-environment hazard in gotchas
("Tests that set PATH break every other test in the binary"). They are the
drivers-crate instance of TASK-STWVB's disease.

So re-scope: this is not one deterministic rmux failure. It is that the drivers
suite has environment-coupled tests whose result depends on what else is running
on the machine, which is the same reason the daemon suite could not answer "did
my change break something?". Reproduce by running the suite WHILE a second
daemon or an rmux server is live, not by running it alone.

* BACKLOG TASK-4YC8E hermes reasoning_effort alias collides with the daemon's dual-key driver_config    :drivers:bug:
:PROPERTIES:
:ID: TASK-4YC8E
:PRIORITY: P1
:END:

`adapters/hermes.rs` declares

    #[serde(default, alias = "effort")]
    reasoning_effort: Option<String>

while the daemon writes the value under BOTH keys at once (`api.rs`:
`"effort": effort` and `"reasoning_effort": reasoning_effort`, lines 3573-3574;
also 2329-2330). Serde then sees one field populated twice and fails
deserialization, so `validate_config` returns `InvalidConfig` and the dispatch
dies with

    400 Bad Request: {"error":"driver configuration is invalid"}

Measured on claude 2026-07-26: adding that exact alias to `ClaudeAcpConfig`
reproduced this immediately, and removing it fixed it. Hermes carries the alias
today, so a hermes dispatch that sets an effort should fail the same way.

NOT VERIFIED for hermes itself — inferred from identical code and a reproduced
failure in a sibling adapter, not from a hermes dispatch. Confirm before fixing:
run a hermes acp-stdio dispatch with `--effort high` and check for the 400.

If confirmed the fix is one line — drop `alias = "effort"`, since the daemon
already writes `reasoning_effort`. Check codex too; it also carries
`reasoning_effort` and needs the same read.

Worth fixing beyond the crash: the error text names neither the field nor the
adapter. `InvalidConfig(e.to_string())` has serde's message in hand and drops it
on the floor at the API boundary. Surfacing it would have made this a ten-second
diagnosis instead of a grep through api.rs.

The dual-key write is itself questionable — one value under two names is what
made an ordinary alias a landmine. Consider emitting only `reasoning_effort`
once every adapter reads that key.

* BACKLOG TASK-HAREX A worker that exits without finalize stays live for tens of minutes instead of being released    :daemon:supervisor:dispatch:recovery:
:PROPERTIES:
:ID: TASK-HAREX
:PRIORITY: P1
:END:

Observed 2026-07-26 on run `run-20260726T080801-79056fdb262348afb3e0eb6633d3cab2`
(TASK-STWVB, acp-stdio/claude, installed runtime 0.0.15-dev.1).

The worker wrote a complete report to `last.txt` and exited WITHOUT calling
`orgasmic dispatch finalize`. Roughly forty minutes later the run was still
`sub_state: implementer.working`, `live`, with its process long gone. It was
only released when a manager ran `manager dispatch-close`, which releases
explicitly (`release_dispatch_run`, crates/orgasmic-cli/src/manager.rs:627).

WHY THIS IS A DEFECT AND NOT A MISSING FEATURE

The rescue-policy convention is explicit that this is the daemon's job:

  "A failed agent run (protocol-end without terminal declaration, fatal driver
  error, stall / max-duration / idle timeout) ends after the daemon writes the
  authoritative release tombstone and, for dispatches, a
  manager.dispatch_orphaned tx."

So the intended sequence for a vanished worker is: daemon detects protocol-end
or stall, writes the Release tombstone with
`reason = protocol_end_without_finalize`, emits `manager.dispatch_orphaned`, and
only then does the manager choose a rescue action via `orgasmic run recover`.
None of that happened here. There was no orphan tx, and the manager had to
close the record for the run to go away.

Deliberately NOT the fix: adding an `orgasmic run release` verb. Release is not
a manager-authorized action in this model — `run recover` is — and a manual
release verb would paper over the daemon failing to write its own tombstone.

LIKELY ALREADY FIXED, NEEDS CONFIRMING

The running daemon is 0.0.15-dev.1 and predates f80b577,
"a declared failure ends the run instead of waiting for the harness" (TASK-TJKFC),
whose message says `terminal_event_releases_transport` allowlisted
`tmux | tmux-tui | rmux`, so on every OTHER transport a terminal event set the
outcome and released nothing, leaving release to stream end. acp-stdio is
exactly such an 'other transport'. That is a strong candidate for the whole
explanation.

So the first step is confirmation, not code: re-run this scenario on a daemon
built from current main — dispatch acp-stdio/claude, let the worker exit without
finalize, and check whether a Release tombstone and `manager.dispatch_orphaned`
appear on their own within the stall window. If they do, close this against
f80b577 with the evidence. If they do not, the stall detector is not reaching
this state at all and that is the bug.

Either way the observable acceptance is the same: a worker that exits without
finalize produces a daemon-written release tombstone and an orphan tx without
any manager action.

* BACKLOG TASK-099T6 driver_modes equivalence test spawns real claude twice on one session id and now collides    :drivers:test:claude:bug:
:PROPERTIES:
:ID: TASK-099T6
:priority: P1
:PRIORITY: P1
:END:

Found 2026-07-26 running the TASK-8W30B merge gates on main (433fef1 + merge).

SYMPTOM

`cargo test -p orgasmic-drivers --test driver_modes` fails:

    ---- legacy_drivers_and_explicit_pairs_emit_equivalent_start_events ----
    assertion `left == right` failed: legacy=claude-acp mode=acp-stdio harness=claude
      left:  Ready { protocol_version: "claude-code-stream-json/1", ... }
      right: TextChunk { stream: Stderr,
             chunk: "Error: Session ID 6fc90d23-2093-4957-9fa9-ccb9488e0bb1 is already in use." }

CAUSE

The test deliberately shares one identity across the two spawns it compares
(driver_modes.rs:1665-1684, comment: "Share one identity so harness-aware native
metadata (e.g. the tmux claude --session-id pinned to runtime_id) is identical
across the legacy and explicit drivers being compared"). That was harmless while
`--session-id` was computed and then dropped. TASK-SGRTX (f5c6913) and 0f65de5
made the flag actually reach the claude process, so the second spawn of each pair
now asks claude for a session id the first spawn just claimed.

This is fallout from making the claude driver real, not from TASK-8W30B. The
8W30B implementer saw this suite green (28/28) in its worktree and the manager
saw 27/1 on the merged tree an hour later, so it presents as intermittent — the
collision depends on whether the first spawn has registered the session by the
time the second starts. Treat it as a race that is always latent, not as a test
that sometimes breaks.

TWO SEPARATE PROBLEMS, BOTH WORTH FIXING

1. The equivalence assertion needs identical *metadata* across the pair, not an
   identical live session. Give each spawn its own session id and normalize the
   id out of the compared Ready event, the way `normalize_volatile_ready_event`
   already removes `session_id` at line 1724 — or make the case inert.

2. This test spawns a REAL `claude` process, twice per case. Per the gotcha
   "Asking a harness to prove it can work costs real money", a claude turn cost
   $0.0994 and 24.5k tokens when measured 2026-07-25. A default-suite test that
   bills the operator on every run is a problem independent of the collision, and
   it got worse the moment the driver stopped being inert. Decide whether this
   case belongs behind an opt-in gate.

Acceptance:
- `cargo test -p orgasmic-drivers --test driver_modes` is green on repeated
  consecutive runs, not once
- the claude case either does not spawn a billable real turn by default, or its
  cost is a deliberate, documented choice
- the equivalence property being asserted is still actually asserted
2026-07-27, TASK-RWCRN dispatch: this test is now actively costing money on ordinary gate runs, not just on deliberate invocation. The brief explicitly told the worker not to run it; the worker ran it once anyway and was right to flag why — `cargo test -p orgasmic-drivers` is a declared verification gate and this test is not #[ignore]d, so the gate as written cannot avoid it. It failed on a real claude spawn with "Error: Session ID 377794d3-... is already in use", the documented TASK-099T6 collision, made more likely because the suite ran inside a live claude dispatch. Until this is #[ignore]d, every brief must carry a --skip instruction and every gate run must use `-- --skip legacy_drivers_and_explicit_pairs_emit_equivalent_start_events`, which is exactly the kind of tribal knowledge that does not survive. The manager's own gate run used the skip and was green: 239/4/28/1.

* BACKLOG TASK-A55J7 Persist model and effort on the run record and the dispatch tx    :dispatch:runtime:observability:
:PROPERTIES:
:ID: TASK-A55J7
:priority: P1
:PRIORITY: P2
:END:

Filed 2026-07-26 alongside a manager prompt change requiring every dispatch to
state driver, harness, model and effort. That rule is currently unenforceable
after the fact, because two of the four are never written down.

WHAT IS RECORDED TODAY

`manager.dispatch_started` carries `:WORKER:`, `:DRIVER:`, `:HARNESS:` and no
model or effort (verified on tx-20260726-orgasmic-2861 and -2869).
`orgasmic run list` reports `driver`, `harness`, `worker_id`, `kind`, `role`,
`sub_state` — and no model or effort field on the live run object.

`orgasmic manager dispatch` accepts `--model` and `--effort` and passes them
through to the adapter, so the values exist at dispatch time. They are simply
dropped before anything durable is written. A manager who sets them cannot prove
it afterwards, and a manager who omits them cannot discover what the harness
chose instead.

WHY IT MATTERS

Two failures on 2026-07-26 came from exactly this blind spot. A reviewer was
dispatched over a P1 concurrency fix and the manager could not afterwards say
what model was reviewing it. Separately, a TASK-STWVB implementer ran on
claude-opus-5 when nothing had selected it, discovered only by reading argv on a
live process (TASK-SGRTX).

The cost is not tidiness. Worker output is treated as a claim to be weighed, and
weighing a claim requires knowing its author. An evidence section that says "the
implementer measured +5 sessions per run" means something different depending on
what produced it, and today that is unrecoverable the moment the process exits.

WORK

1. Thread the resolved model and effort — the values actually sent to the
   adapter, not the ones requested — onto RunMeta, so they survive boot reattach
   the way last_path/stdout_path/worktree do.
2. Emit them on `manager.dispatch_started` as `:MODEL:` / `:EFFORT:`, and on
   `run.created`.
3. Surface them in `orgasmic run list` and `orgasmic manager dispatch-status`.
4. Record what the harness actually resolved when the manager passed nothing.
   The default is the case most worth capturing — it is the one nobody chose.
   Where the harness does not report its own resolved model, record the request
   as absent rather than inventing a value, and say so in the field.
5. Show them wherever a run is displayed in the UI Runs surface.

Note the task node already carries `model` and `reasoning_effort` properties
(both null on most headings). Decide whether those are the requested values and
the new run fields are the resolved ones, or whether the task properties should
be retired in favour of run-level truth. Two half-populated surfaces for the same
fact is how this became invisible in the first place.

Acceptance:
- a completed dispatch can be asked what model and effort ran it, from state
  alone, after the process is gone
- a dispatch where the manager passed neither still records what was resolved,
  or records explicitly that nothing was requested and the harness default
  applied
- `orgasmic run list` and `dispatch-status` both show all four of driver,
  harness, model, effort

* BACKLOG TASK-VSWZT A run can appear live with no release surface able to clear it, blocking the update fence    :runtime:supervisor:recovery:bug:
:PROPERTIES:
:ID: TASK-VSWZT
:priority: P1
:PRIORITY: P1
:END:

Observed 2026-07-26 clearing run-20260726T080801-79056fdb262348afb3e0eb6633d3cab2
(TASK-STWVB implementer). The run showed as `implementer.working` in
`orgasmic run list` .live for ~80 minutes after its worker died, and NO surface
could clear it.

THE DEAD END

- `POST /api/runs/:id/release` returned 404 `{"error":"active run <id>"}` — even
  though the same run was present in `GET /runs` .live. Both the snapshot lookup
  (api.rs:6173) and `SupervisorError::RunNotFound` (api.rs:6197) return that same
  string, and `.live` is fed from `supervisor.snapshot()` (api.rs:7651), so the
  run was in the snapshot while `release_with_finalization` could not find it.
  Those two views of the supervisor disagreed.
- `orgasmic manager lease-release` refuses while a live run holds the lease.
- `orgasmic run recover` offers only reattach_tmux / resume_native_fork /
  start_recovery_run. The rescue-policy convention lists *abandon* as one of four
  manager judgments, but no verb implements it.
- `orgasmic manager dispatch-close` had already run; the dispatch record was
  closed and the run stayed live regardless.

WHY IT IS NOT COSMETIC

`refuse_if_live_runs` (crates/orgasmic-cli/src/update.rs:436-462) reads exactly
`GET /runs` .live and refuses the runtime update for any entry. One phantom run
therefore blocks `orgasmic update` and any publish behind that fence, with no
documented way out. The only route that worked was a full daemon restart.

WHY THE SESSION NEVER CLOSED

The session JSONL ends at seq 1657 `run_complete` ("worker finalize for
TASK-STWVB") and seq 1658 fatal `driver_error` ("claude exited with status exit
status: 143"), with no `Lifecycle::Release`. The supervisor kept its entry; the
tombstone was never written. On the next boot the classifier read the same file
and correctly returned terminal_noop ("session ended with terminal driver
event") via `session_driver_terminal_event` (api.rs:9104), so the durable
evidence was sufficient all along — only the live supervisor disagreed.

Note this is the second wrong answer recorded about this same run. The prior
handoff asserted it had been released and that `run list` reported 0 live; it had
not been, and did not.

WORK

1. Reconcile the two supervisor views, or make `POST /runs/:id/release` able to
   clear anything that `GET /runs` .live reports. A run the fence refuses to
   ignore must be releasable by the surface documented to release it.
2. Implement abandon as a first-class manager verb so the fourth branch of
   rescue-policy is executable, not just describable.
3. Write the release tombstone when a driver reports a fatal terminal event.
   `run_complete` followed by a fatal `driver_error` should end the run rather
   than leave it working; the boot classifier already treats that pair as
   terminal, so the two paths disagree about the same evidence.
4. Consider having `refuse_if_live_runs` name the escape hatch in its error text.
   It currently says "Close them or use the existing force path deliberately"
   without saying how, which is what sent this session looking for a verb that
   does not exist.

Acceptance:
- a run reported in `GET /runs` .live can always be cleared without restarting
  the daemon
- a worker whose harness dies after run_complete leaves a release tombstone
- `orgasmic run list` .live and the release path never disagree about whether a
  run exists

* BACKLOG TASK-S2KM0 No CI workflow runs the test suite; every green gate in this project is a local claim    :ci:process:test:
:PROPERTIES:
:ID: TASK-S2KM0
:priority: P1
:PRIORITY: P1
:END:

Verified 2026-07-26 while ruling on reviewer finding 5 on TASK-8W30B.

`.github/workflows/` contains six files — nightly-android, nightly-macos,
nightly-runtime, release-android, release-macos, runtime-bundles. `grep -rn
"cargo test\|cargo nextest" .github/workflows/` returns nothing. No workflow runs
the Rust test suite, clippy or fmt.

Every "gates green" claim in every task Evidence section in this repository is
therefore a local run by whoever happened to run it, on their machine, in
whatever state that machine was in. Nothing re-checks it on merge, and nothing
would catch a regression landed by a contributor who skipped the local run.

This session is a direct illustration. The full suite degrades as it runs
(TASK-8W30B: leaked rmux sessions), a driver test now bills real money and fails
intermittently (TASK-099T6), three daemon tests are known-red, and all of that
was discovered by hand, twice, because two different agents ran the suite in two
different local states and got two different answers.

The reviewer raised the narrow version — the new rmux regression test skips
rather than fails when rmux is absent, so adding `cargo test` to CI naively would
make it green forever without ever creating a session. That is real, but it is
downstream of the larger gap.

Work:
1. Add a CI lane that runs `cargo test --workspace`, `cargo clippy --workspace
   --all-targets -- -D warnings` and `cargo fmt --all --check`.
2. Decide what to do about the three known-red daemon tests before turning the
   lane required — a lane that is red on arrival teaches everyone to ignore it.
3. Decide which live-harness tests may run in CI at all. Some spawn real claude
   and cost money per run (see the gotcha "Asking a harness to prove it can work
   costs real money"); those need an opt-in gate, not a default lane.
4. For tests that skip when their runtime is unavailable, add a required-environment
   mode where a skip is a failure, and provision rmux in whatever lane is
   designated as the gate.

Acceptance:
- a pull request that breaks the workspace suite fails a check
- the suite CI runs is honest about what it did not run, rather than skipping
  silently
- no default CI lane bills the operator for a provider turn
SIBLINGS FILED [2026-07-28]: TASK-DJ1WV (flake registry + retry-in-isolation runner wrapper) is the concrete answer to work item 2 — the CI lane should consume its verdict block so known flakes cannot teach anyone to ignore the lane. TASK-WE0Q7 adds the scheduled minutes-scale daemon soak (the uptime blindspot class behind Q07Y5) as the nightly complement to this per-PR lane. TASK-TCTTD's `orgasmic verify` replay is a candidate per-merge CI step once it exists.

* BACKLOG TASK-HXSW0 Make the CLI self-documenting for agents: real help per verb and flag, no silent no-ops, a drivers listing verb    :cli:agent-ergonomics:docs:
:PROPERTIES:
:ID: TASK-HXSW0
:priority: P1
:write_scope: crates/orgasmic-cli/src/
:PRIORITY:    P1
:END:

Filed 2026-07-26 after a session in which the manager guessed wrong CLI verbs
four times, and one shipped convention was found instructing a hallucination.

The operator framing, which sets the constraint: fix this with better CLI help
that agents are told to consult, NOT by pasting a command cheat sheet into every
prompt. Prompt space is scarce and a stale cheat sheet is worse than none.

MEASURED FAILURES, all from one session

1. `orgasmic manager drivers` does not exist in any command group — and
   `shipped/prompt-studio/conventions/manager-dispatch.org` instructed the
   manager to run it to choose `--mode`/`--harness`. The valid pairs live only in
   `orgasmic_drivers::SUPPORTED` (crates/orgasmic-drivers/src/lib.rs:74) with no
   CLI surface at all. The convention was corrected the same day, but the
   underlying gap stands: the one fact needed on EVERY dispatch has no listing
   verb. The only discovery path is to submit a bogus pair and read the
   rejection, which does helpfully name all sixteen.
2. `orgasmic task show` — guessed repeatedly. The real verb is `task get`, and
   listing lives in a SEPARATE top-level group, `tasks list`. A singular/plural
   split where both exist and neither mentions the other is a hallucination
   generator.
3. `orgasmic run release` — a prior manager asserted it existed, corrected
   itself in the handoff, and the correction itself was then wrong. Two wrong
   records about one nonexistent verb.
4. `orgasmic worker list` — guessed, does not exist.

5. WORST CLASS — a silent no-op on a write verb.
   `orgasmic task update TASK-X --property "WRITE_SCOPE=..." --property
   "TEST_CMD=..."` returned `{"changed":{"STATE":"in_progress"}}` and dropped
   both properties without a word. The same call with lowercase `write_scope`
   and `test_cmd` worked. An unrecognised property key must be an error: a write
   verb that reports success while discarding two of three arguments is worse
   than a missing feature, because the caller has no reason to re-check.

6. Flags on the primary agent verb have NO help text. `orgasmic manager dispatch
   --help` renders `--task <TASK>`, `--kind <KIND>`, `--brief <BRIEF>`,
   `--from <FROM>`, `--model <MODEL>`, `--effort <EFFORT>`, `--worktree`,
   `--branch`, `--reason` with blank descriptions. An agent cannot learn from
   that whether `--from` is a git ref or a path, or that `--worktree-remove`
   on `dispatch-close` defaults to TRUE and force-deletes uncommitted work
   (TASK-2BPWM).

WHAT ALREADY WORKS, and should be the template

Two errors in the same session were excellent and cost zero follow-up:
- `manager dispatch` from the wrong lifecycle: "task TASK-X is in lifecycle
  stage in_progress; implementer dispatch is allowed only from BACKLOG or TODO"
  — names the rule and the allowed set.
- `node body append --section Evidence` on a node lacking it: "no section
  \"Evidence\"; sections: [] (use `set --section` to create one via add)" —
  names the fix verb.
Contrast `node body append gotchas ...` → "glossary file not found", which names
the wrong file entirely (already a recorded gotcha).

WORK

1. Give every command and every flag a one-line description that says what it
   does and what it returns. Blank `#[arg]` descriptions on agent-facing verbs
   should be treated as a bug, not a style nit.
2. State destructive defaults in the help text where the operator reads them,
   not only in the code.
3. Make unrecognised `--property` keys a hard error listing accepted keys.
   Audit for other silent-drop paths in the same shape.
4. Add a real listing verb for mode/harness pairs, sourced from
   `orgasmic_drivers::SUPPORTED` so it cannot drift.
5. Make near-miss verbs self-correcting: an unknown subcommand should suggest
   the real one (`task show` -> `task get`; `worker list` -> ?) and cross-
   reference the singular/plural split between `task` and `tasks`.
6. Audit shipped prompts and conventions for commands that do not exist. One was
   found by accident; nobody has checked the rest. A test asserting that every
   `orgasmic ...` invocation appearing in `shipped/**` parses against the real
   clap surface would make this class permanently impossible.

Item 6 is the highest-leverage item here — it converts "an agent hallucinated"
into a build failure.

Acceptance:
- an agent can discover any verb and flag it needs from `--help` alone, in at
  most three calls, without reading Rust source
- no write verb silently ignores an argument
- every command string in shipped prompts/conventions is checked against the
  real CLI surface by a test
- mode/harness pairs are listable
CONFIRMED INSTANCE, found while filing these very tasks [2026-07-26].

`orgasmic task create` has no `--priority` flag at all. Every task filed this
session used `--property "priority=P1"` on create, which was accepted silently
and discarded. ALL SEVEN tasks created today came out with priority None:
TASK-099T6, TASK-A55J7, TASK-VSWZT, TASK-S2KM0, TASK-6FNAY, TASK-2BPWM,
TASK-HXSW0. They were repaired afterwards with `task update --priority`, which
does have a real flag and works.

This sharpens item 3 beyond "unknown property keys should error". Two distinct
defects compose here:

1. The property allowlist differs between `create` and `update`. `update`
   accepts `--priority` as a first-class flag; `create` accepts neither the flag
   nor the property. The same field is settable, unsettable, or silently ignored
   depending on which verb you reach for.
2. The silent drop makes the divergence invisible. `create` returned a normal
   success object with `id` and `tx_id` and no indication that an argument had
   been thrown away.

The result is a whole session of P1 work filed as unprioritised, which is exactly
the kind of state error that later reads as "nobody triaged this" rather than
"the CLI dropped it".

Note the failure is silent in BOTH directions worth testing: an unknown key
(`WRITE_SCOPE` uppercase, dropped) and a known-but-unsupported-on-this-verb key
(`priority` on create, dropped). Fixing only the first would leave this case
intact.

TWO MORE CONFIRMED INSTANCES [2026-07-26], both found while filing a fix subtask.

4. `task create` accepts and silently discards property keys that `task update`
   accepts and persists. Reproduced on TASK-D0GA3: the same
   `--property "PARENT_TASK=TASK-2BPWM" --property "FIX_SUBTASK=t"` pair was
   dropped by `create` (drawer contained only ID, PRIORITY, TEST_CMD,
   WRITE_SCOPE) and then written verbatim by `update`, whose response echoed both
   keys under "changed". PRIORITY, TEST_CMD and WRITE_SCOPE survived `create` in
   the same invocation, so this is a per-key filter on create, not a general
   restriction. The user-visible effect is that the same command produces
   different state depending on which verb you happen to use.

5. `:PARENT_TASK:` is never read by anything. `parent_task` is derived purely
   from the id grammar — `derive_task_parent_id` (crates/orgasmic-core/src/id.rs:319)
   splits `TASK-<stem>.<digits>` and returns `TASK-<stem>`. So the property is
   accepted by `update`, stored in the drawer, echoed as changed, and ignored by
   every reader. `orgasmic task get TASK-D0GA3` still reports
   `parent_task: null` with `:PARENT_TASK: TASK-2BPWM` sitting in the file.

   This one had a downstream cost: the shipped `manager-dispatch` convention
   instructed managers to file fix subtasks with a `:PARENT_TASK:` back-edge, so
   the documented mechanism produced no edge at all. Corrected in the convention
   the same day — parentage must be minted into the id as `TASK-<parent>.N`.

The pattern across all five instances is one defect: `--property` is an untyped
passthrough with no schema, so every layer is free to accept, store, filter or
ignore a key independently, and every combination reports success. The fix is not
five special cases. It is (a) validate keys against the schema at write time and
reject unknown or miscased ones with a message naming the correct key, and
(b) make `create` and `update` accept the same set, or say why they differ.

INSTANCE 6 [2026-07-26]. `orgasmic tx record` is the only mutation verb that is
NOT project-scoped by default. Without `--project`, it writes to
$ORGASMIC_HOME/state/tx/YYYY-MM.org — the global home file — and reports success
with a differently-shaped tx id (tx-20260726192932-05e34dce) rather than the
project's tx-20260726-orgasmic-NNNN. Every sibling verb (`task update`, `node
body append`, `task create`) resolves the project from the cwd and needs no
flag. The help text for --tx-path says "defaults to $ORGASMIC_HOME/state/tx/
YYYY-MM.org", which is accurate and is the only warning you get; nothing in
`tx record --help` suggests --project is load-bearing.

Cost: one manager.action for TASK-D0GA3 was written to the home tx file and had
to be re-recorded with --project. The stray entry is still there. A manager
following the obvious invocation silently loses every tx it records off the
project ledger and cannot tell from the exit code.

Fix direction: default `tx record` to the cwd project like its siblings, and
keep --project / --tx-path as explicit overrides.

* BACKLOG TASK-K4G1D Run the mux-dependent daemon tests under tmux as well as rmux, so 'is rmux at fault' becomes a measurement    :testing:daemon:drivers:rmux:
:PROPERTIES:
:DEPENDS_ON: TASK-RRT4T
:ID: TASK-K4G1D
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/src/api.rs
:END:

Filed after the operator asked whether rmux, being far less mature than tmux,
might itself be the defect behind the recurring artifact-test failures.

It cannot currently be answered, and that is the problem worth fixing.

WHAT IS KNOWN
rmux 0.9.0 (crates.io, pinned `=0.9.0` in crates/orgasmic-drivers/Cargo.toml) is
a pre-1.0 third-party tmux clone with a tmux-compatible CLI. Measured 2026-07-26
against tmux on the same machine: 15 create+kill cycles cost 12.1 ms each against
tmux's 13.1 ms, 20 pane spawns of a warm script cost 12.8 ms against tmux's
6.9 ms, neither leaves a session behind, and neither costs syspolicyd anything.
So at the level that can be measured from outside, rmux is leak-free and merely
about 2x slower per pane spawn.

WHAT IS NOT KNOWN
Every artifact-regenerate test in crates/orgasmic-daemon/src/api.rs runs only
under rmux. There is no tmux counterpart, so when one fails there is no way to
tell whether the defect is in our driver, in rmux, or in the test's deadline.
Two of the three worker rounds on TASK-STWVB attributed that failure to a
different cause and both attributions were later withdrawn.

The one confirmed rmux-adjacent defect so far was OURS, not rmux's: TASK-8W30B,
where RmuxControl::release aborted its lifecycle task before Session::kill and
poisoned the shared ordered SDK transport. Worth noting the structural asymmetry
it exposed, though — the tmux driver drives tmux through its CLI, one process per
command and no shared state, while the rmux driver holds a persistent socket with
an ordered transport shared between Session and the lifecycle watcher. The second
design has failure modes the first cannot have, and every hang and leak this week
has been on that side. That is an argument about our integration, not about rmux's
correctness, and the two keep getting conflated.

WHAT TO DO
Parameterize the artifact-regenerate tests (and any other mux-dependent daemon
test) over both rmux and tmux, running the same assertions through each mode.

- red under rmux, green under tmux -> rmux is indicted, with a reproducer we can
  file upstream or route around by defaulting to tmux
- red under both -> the defect is ours, and we stop suspecting the dependency
- green under both -> the deadline is the defect

Depends on TASK-RRT4T: a mode that is unavailable must make the test fail or
report as ignored, never silently pass. Without that this change would just
double the number of tests that can quietly do nothing.

Acceptance:
- at least post_artifact_regenerate_hot_path_reuses_live_run_id runs under both
  modes
- a failure names which mode it failed under
- the answer to "is rmux at fault" is a test result, not a judgement call

* BACKLOG TASK-VZMZE Heartbeats reset the stall timer, so a wedged run that never starts a turn is immortal for four hours    :daemon:supervisor:dispatch:reliability:
:PROPERTIES:
:ID: TASK-VZMZE
:PRIORITY: P1
:TEST_CMD: cargo test -p orgasmic-daemon --lib
:WRITE_SCOPE: crates/orgasmic-daemon/src/supervisor.rs
:END:

Measured 2026-07-26 on a real dispatch that produced nothing for 60 minutes and
was never released by the daemon.

THE ARITHMETIC
- Heartbeats arrive every 30 s (observed: 118 of them, seq 0..115, exactly 30 s
  apart).
- `DEFAULT_STALL_TIMEOUT` is 600 s (supervisor.rs:65).
- Stall is computed from `last_activity_at = rec.last_input_at.max(rec.last_driver_event_at)`
  (supervisor.rs:3410) and the drain updates `last_driver_event_at` for EVERY
  drained event, variant-agnostically. The crate's own test says so:
  `heartbeat_is_non_terminal_so_drain_never_releases_on_it` — "The event drain
  resets last_driver_event_at for every drained event (variant-agnostic)".

So a harness that is alive and emitting heartbeats can never trip the 600 s stall
timeout, no matter how long it does no work. The only backstop is
`DEFAULT_MAX_RUN_DURATION` at 14_400 s — four hours of a billed session producing
nothing before the daemon acts.

THE OBSERVED RUN
run-20260726T144430-aa47b867840f42f282b30d3469949731 (TASK-RRT4T implementer,
acp-stdio / codex / gpt-5.6-sol / xhigh). Session event tally against the healthy
run that preceded it on the same harness and model:

| event       | TASK-2BPWM (healthy) | TASK-RRT4T (wedged) |
|-------------|----------------------|---------------------|
| ready       | 1                    | 1                   |
| tool_call   | 243                  | 0                   |
| tool_result | 135                  | 0                   |
| text_chunk  | 211                  | 12 (all stderr logs)|
| heartbeat   | 7                    | 118                 |
| lifecycle   | 3                    | 2 (no terminal)     |

It reached `ready`, never began a turn, wrote zero bytes into its worktree, and
consumed 6.77 s of CPU in 60 minutes. Its 12 text chunks are all harness stderr:
`codex_models_manager::cache: failed to load models cache: missing field
'supports_reasoning_summaries' at line 88 column 5`, repeated `failed to renew
cache TTL` with the same message, and three
`rmcp::transport::worker: worker quit with fatal: Transport channel closed` for
`http://localhost:8080/_agent-native/mcp`.

WHY THIS MATTERS MORE THAN THE HARNESS BUG
The harness fault may be transient — the reviewer run dispatched seven minutes
later on the same binary and model worked fine. What is not transient is that the
daemon could not tell the difference. A run with zero tool calls and 118
heartbeats is, to the supervisor, exactly as healthy as one with 243 tool calls.

The manager-dispatch convention already states the correct rule for humans:
"A live PID alone is not proof of progress, and heartbeat-only/session-token
activity must not reset the deadline." The supervisor does the opposite of what
we tell managers to do. The manager's shell watcher caught this in 60 minutes
because it was told to; the daemon would have taken four hours.

FIX DIRECTION
Distinguish liveness from progress. A heartbeat should keep the run from being
declared dead, and should NOT count as progress against the stall timer. Track a
separate `last_progress_at` advanced only by events that represent work —
tool_call, tool_result, text_chunk from stdout, turn boundaries — and evaluate
stall against that. Keep `last_driver_event_at` for liveness, since a harness
that stops heartbeating is a different failure.

Second, a run that reaches `ready` and never starts a turn deserves its own
shorter deadline. "Started but never began work" is a distinct and much cheaper
failure to detect than a mid-turn stall, and it is the one that keeps happening.

Related: the handoff already records a run that stayed live ~40 minutes after its
worker exited. Same family, and this is the mechanism.

Acceptance:
- a run emitting only heartbeats trips the stall timeout at the configured
  threshold, not at max_run_duration
- a heartbeat still prevents a "harness is gone" classification
- a test asserts a heartbeat-only run is released, using fake time rather than a
  600 s sleep
- the reason recorded distinguishes "no progress" from "no liveness"

CORRECTION, same session, before this task is worked. The harness stderr listed
above is NOT the cause and must not be treated as a lead.

The healthy TASK-2BPWM reviewer run, dispatched seven minutes after the wedged one
on the same binary, model and effort, contains the SAME errors: 6 occurrences of
`missing field 'supports_reasoning_summaries'` and the same 3
`_agent-native/mcp` transport fatals. It then made 41 tool calls and finalized
normally.

So those messages are ambient noise on every codex run here, and the wedged run's
cause is UNKNOWN. What is established is only this: it reached `ready`, never
emitted a single tool_call or a turn, and the supervisor could not tell.

That does not weaken this task. The defect filed here is the detection gap, which
is proven from the code and the arithmetic (30 s heartbeats against a 600 s
variant-agnostic stall timer), not from any theory about why the harness stalled.
If anything the correction strengthens it: the trigger is unidentified and may
recur at any time, so the daemon cannot rely on knowing the cause in advance.

Worth filing separately if it happens again: a run that reaches `ready` and never
starts a turn, with no explanation in its session events.
HARD COMPATIBILITY GATE added 2026-07-27 by the TASK-RWCRN reviewer (finding 2, MEDIUM). TASK-RWCRN landed DriverEvent::PaneActivity, a content-free per-30s event emitted while an rmux pane writes; it is the ONLY driver event an rmux worker produces after ready, and the rmux stall clock now depends on it. This task's recorded fix direction — move stall onto a progress-only clock advanced by tool/text/turn events — will kill every rmux dispatch at 600s again if implemented literally, because a pane produces none of those. The RWCRN author deliberately did NOT reuse DriverEvent::Heartbeat precisely so this task can classify the two independently: Heartbeat means the harness produced no substantive output, PaneActivity means the pane demonstrably wrote. That reasoning is sound, but the only warning it left was a doc comment in orgasmic-core/src/session.rs, which is outside this task's declared supervisor.rs write scope and outside this task record — so an implementer routed here would never see it. Hence this note. REQUIRED ACCEPTANCE, both regressions, cross-transport: (a) an acp-stdio run emitting only heartbeats and no substantive progress MUST time out, which is this task's whole point; and (b) a working rmux run emitting only pane_activity MUST continue to reset its stall deadline and MUST NOT be released. Treat (b) as a gate, not a note. Related: TASK-RWCRN's own reviewer also filed a HIGH that pane_activity is line-based and misses a redrawing TUI; if that lands as a raw-byte observation the event may change shape, so re-read the variant before building on it.

* BACKLOG TASK-NYF7Z rmux TUI workers are not MCP-isolated the way acp-stdio workers are    :drivers:isolation:rmux:
:PROPERTIES:
:ID: TASK-NYF7Z
:PRIORITY: P1
:TEST_CMD: cargo test -p orgasmic-drivers
:END:

The acp-stdio claude adapter isolates its worker from the operator's MCP
servers. The rmux TUI path does not. Same harness, same worker personas, two
different blast radii — and rmux is now the standing default for every dispatch
(manager-dispatch, standing worker matrix, 2026-07-26).

MEASURED ON A LIVE WORKER, not read from code alone. TASK-6AYEJ dispatched as
implementer-claude-rmux (rmux / claude / claude-opus-5 / high), pid 37956:

    claude --dangerously-skip-permissions --model claude-opus-5 --effort high
           --session-id a3c31a76-... -- <compiled prompt>

No --strict-mcp-config. Compare adapters/claude.rs:361-365, where the acp path
pushes it deliberately, with the comment "without it this machine loaded nine
MCP servers into a worker that wants none". `claude mcp list` on this machine
today returns exactly nine: Figma, Google Drive, Gmail, Google Calendar, Notion,
plugin:svelte, syncntn, ray-exomem-dev, ray-exomem. Six connect.

The rmux argv is built at modes/rmux.rs:550-575 and pushes only
--dangerously-skip-permissions, --model, --effort, --session-id.

DO NOT REACH FOR --bare. It skips hooks, LSP, plugin sync, auto-memory and
CLAUDE.md discovery, but it also makes Anthropic auth strictly ANTHROPIC_API_KEY
or apiKeyHelper — OAuth and keychain are never read. Our workers run on
subscription auth, so --bare would break them. That is precisely why the acp
adapter branches on ClaudeCredentialMode and, for NativeLogin, rebuilds "what
--bare would have given us, minus its credential policy" from
--strict-mcp-config alone. rmux is NativeLogin. Use the same narrow flag.

SIZE THE WIN HONESTLY — it is an isolation win, not a context win. Measured with
two `claude --print` runs in this repo, identical but for the flag: prefix
tokens 26528 without, 26284 with. A 244-token difference. Nine servers' worth of
tool schemas would cost far more than that if they were loaded eagerly, so the
harness appears to defer MCP tool schemas and charge only for names. Anyone
justifying this task on token savings is quoting a number that no longer holds.

The real argument is blast radius. A dispatched implementer working on a Rust
CLI has no business holding live, authenticated handles to the operator's Gmail,
Google Drive, Google Calendar and Notion. Nothing in the worker persona forbids
calling them, the run is --dangerously-skip-permissions, and the operator is not
watching every pane. TASK-6AYEJ's worker invoked no MCP tool, which is luck
rather than a control.

Scope:
- push --strict-mcp-config in the rmux claude path, guarded exactly as the
  existing pushes are (skip if operator-supplied harness_args already name it,
  so an operator can still opt a pane back in)
- check the equivalent for the reviewer stage: rmux/codex gets only --model
  today. Codex reads MCP from ~/.codex/config.toml. Determine whether it has a
  comparable flag and either use it or record plainly that it does not exist.
- one test per harness asserting the flag is present by default and absent when
  the operator supplied it

Out of scope: changing --dangerously-skip-permissions, touching the acp path,
and any CLAUDE.md / plugin / hook suppression. Those are separate arguments and
at least one of them (CLAUDE.md discovery) we actively want.

Acceptance:
- a live rmux/claude dispatch shows --strict-mcp-config in its argv
- an operator-supplied harness_args override still wins
- the codex question is answered in the report with evidence either way

* BACKLOG TASK-K5NDR ORGASMIC_RUN_ID leaks into test subprocesses, so the CLI suite fails inside a dispatch    :tests:defect:dispatch:
:PROPERTIES:
:ID: TASK-K5NDR
:PRIORITY: P1
:TEST_CMD: cargo test -p orgasmic-cli
:END:

`ORGASMIC_RUN_ID` leaks from the operator's (or a worker's) environment into
test subprocesses, so every `dispatch finalize` test in
`crates/orgasmic-cli/tests/dispatch.rs` resolves the AMBIENT run id instead of
the one its fixture created, and fails with `no live run ...`.

Reported by the TASK-6AYEJ implementer, and it reproduced the failure on
UNMODIFIED code before touching anything —
`dispatch_finalize_commit_flag_leaves_clean_committed_worktree` fails when run
from inside a live dispatch and passes outside one. All of that task's gates had
to be run as `env -u ORGASMIC_RUN_ID cargo test -p orgasmic-cli`.

This is the same class of defect as TASK-RRT4T (tests that silently skip when
tooling is absent) and TASK-STWVB (tests that pass alone and fail in-suite): the
suite's result depends on who is running it and from where, rather than on the
code. It is worse than either in one respect — it makes the CLI suite
UNRUNNABLE from inside a dispatch, which is exactly where every dispatched
worker runs it. A worker that does not know the incantation reports a red suite
it cannot explain, or worse, works around it.

The manager's own runs of this suite pass because a manager shell has no
ORGASMIC_RUN_ID set (verified: unset). So the manager and the worker see
different results from the same command on the same tree, and the manager's
green is the misleading one.

Fix direction: the test harness must not inherit ambient orgasmic run identity.
Clear `ORGASMIC_RUN_ID` (and audit sibling `ORGASMIC_*` variables that steer
resolution) in the fixture that builds the command, not in a wrapper the caller
has to remember. A test whose correctness depends on the operator's environment
is not a test.

Acceptance:
- `cargo test -p orgasmic-cli` passes with `ORGASMIC_RUN_ID` set to an arbitrary
  value in the environment
- the fixture, not the caller, is responsible for the isolation
- an audit of other `ORGASMIC_*` variables that can steer CLI resolution is in
  the report, with each one either cleared or explicitly justified as safe

* BACKLOG TASK-Z9C73 a released rmux run leaves one zombie harness child under the rmux daemon    :rmux:drivers:defect:
:PROPERTIES:
:ID: TASK-Z9C73
:PRIORITY: P2
:DEPENDS_ON: 
:END:

Split out of TASK-RWCRN, whose acceptance included "a released rmux run does not leave a zombie harness process or an orphaned rmux session behind". The session half is met — `rmux ls` is empty after release. The process half is NOT, and the TASK-RWCRN worker measured it with a proper baseline control rather than asserting it.

MEASURED. One live acquire + release leaves exactly one <defunct> child under the rmux-daemon (pid 8920 at time of measurement). Zombie count under that ppid went 3 -> 4 across one release_reaps_live_rmux_session run on the TASK-RWCRN tree, and 4 -> 5 across the same test on the stashed baseline tree (git stash -> run -> git stash pop). Equal deltas on both trees, so the leak is pre-existing and TASK-RWCRN neither caused nor fixed it.

MECHANISM, named at crates/orgasmic-drivers/src/modes/rmux.rs:1094: panes are spawned with keep_alive_on_exit(true) deliberately, because RMUX 0.9 retains a real exit code/signal only for a dead pane it keeps around. The exited child therefore stays unreaped by the rmux daemon, a process orgasmic does not own. So this is not a missing waitpid in orgasmic — any fix has to either negotiate reaping with the rmux daemon, drop keep_alive_on_exit and give up the exit code/signal fidelity that TASK-6FNAY and TASK-8W30B depend on, or accept and bound the leak.

WHY IT MATTERS. One zombie per dispatch is small, but it accumulates across the lifetime of a long-lived rmux daemon, and the standing worker matrix puts every dispatch in an rmux pane. Bounded growth needs to be either proven acceptable or fixed.

ACCEPTANCE:
- name which of the three options is taken, with the tradeoff argued
- if the leak is retained, bound it and prove the bound
- the exit code/signal fidelity relied on by TASK-6FNAY and TASK-8W30B is preserved or its loss is explicitly accepted

* BACKLOG TASK-XHP43 tmux pane runs have no liveness signal either, so they are unprotected past 600s    :tmux:drivers:defect:
:PROPERTIES:
:ID: TASK-XHP43
:PRIORITY: P2
:END:

Raised by the TASK-RWCRN reviewer as an open question. tmux.rs:1789-1824 has the same pane-is-not-an-event-source shape that TASK-RWCRN fixed for rmux, and only polls for session exit, so a tmux-mode TUI run emits no driver events after ready and is released by the 600s stall detector exactly as rmux was. The reviewer AGREED with leaving tmux out of the rmux-scoped RWCRN fix — tmux has no existing output subscription to reuse, so adding capture polling there would widen both risk and retention scope, and the standing worker matrix is rmux — but flagged that tmux/claude, tmux/codex, tmux/cursor-agent and tmux/hermes remain in orgasmic_drivers::SUPPORTED and are documented as usable. Either give tmux an equivalent liveness signal, or stop advertising tmux pairs as safe for runs longer than 600s. Depends on the shape TASK-RWCRN.1 settles: if pane observation moves from lines to raw bytes, tmux should adopt the same contract rather than inventing a second one.

* BACKLOG TASK-Y5M0H rmux pane-liveness stream source has no fast regression guard    :rmux:drivers:test-gap:
:PROPERTIES:
:ID: TASK-Y5M0H
:PRIORITY: P2
:END:

Reviewer residual on TASK-RWCRN.1 (run-20260728T045548-5e2ae042c2844e039a98edc26e05a739, verification note 6, SHIP with no blocking findings).

The throttle unit tests at crates/orgasmic-drivers/src/modes/rmux.rs:2788-2880 call PaneActivityThrottle directly, so they would all still pass if the production wiring regressed from PaneOutputStream back to PaneLineStream. The only test that catches that regression is the #[ignore]d live smoke live_rmux_pane_publishes_pane_activity_for_newline_free_redraws at rmux.rs:3381-3409, which does not run in CI.

So the exact defect TASK-RWCRN.1 fixed could silently return while every non-ignored test stays green.

WANTED: a fast, non-ignored test that pins the stream source itself - either asserting spawn_pane_exit_watch subscribes via output_stream_starting_at rather than line_stream_starting_at, or a seam that lets a fake pane transport feed newline-free bytes through the real wiring without a live rmux server.

Not a ship blocker and not urgent: it is insurance on a fix that is already validated live (the TASK-WGXKD dispatch ran claude-on-rmux 1675s with 55 PaneActivity bytes events).

* BACKLOG TASK-XYYBT Explore and estimate the cost of excising the architecture layer (arch_ nodes, architector agent, architecture.org)    :architecture:excision:estimate:plan:
:PROPERTIES:
:ID: TASK-XYYBT
:PRIORITY: P1
:END:

Operator decision [2026-07-28]: decisions.org is enough. The architecture layer makes orgasmic too heavy and adds redundant bureaucracy. This task does NOT excise anything — it is a scoped exploration that produces a cost estimate and a staged removal plan, so the operator can decide go/no-go with real numbers.

Deliverable: a written report (artifact or task Evidence block) covering the inventory, the coupling risks, a staged plan with per-stage effort estimate, and an explicit recommendation. Plus a decision draft for decisions.org if the recommendation is GO.

** Scope of what "architecture" means here
1. The =arch_XXXXX= node class and =.orgasmic/architecture.org= (177 headings, 26 top-level subsystems, IMPLEMENTED + TARGET ARCHITECTURE sections).
2. The =architector= worker kind / =architect= stage: =POST /architect=, =stage_spec("architect")=, =WorkerKind::Architector=, =DispatchEndpointKind::Architector= ("architector-watch-then-integrate"), the architector prompt spec, and =manager architecture= stage plumbing.
3. CLI surface: =orgasmic architecture {list,show,drift,create,schema,...}= and =crates/orgasmic-cli/src/architecture_drift.rs= (457 lines).
4. Read model: architecture projections in =orgasmic-daemon/src/index.rs=, =api.rs= routes, =writer.rs= authority, =orgasmic-core= =org.rs=/=schema.rs=/=id.rs=/=marker.rs=/=identity_lint.rs=/=schema_examples.rs=.
5. UI: =ArchitectureView.tsx= (273 lines), NodeModal arch node views, router/routes entries, =descriptor.ts=, capabilities.
6. Shipped instruction surfaces: =shipped/prompt-studio/context-packs/architecture.org=, =shipped/prompt-studio/prompt-specs/manager.org=, =shipped/entry/router.org=, =shipped/schema/tx.org=, =shipped/skills/orgasmic/SKILL.md=, =shipped/skills/orgasmic/references/recall-resume.md=, =shipped/project-scaffold/tasks/{goal,backlog}.org=.
7. The =// orgasmic:arch_NNNNN= source marker convention and its drift comparator.

** Measured inventory (manager scan 2026-07-28, non-target, non-node_modules)
- =architecture.org=: 177 org headings, 26 top-level =arch_= subsystems, ~2100+ lines.
- =arch_= identifier occurrences by file (source only): api.rs 21, index.rs 25, org.rs 31, id.rs 22, schema.rs 21, architecture_drift.rs 20, marker.rs 10, identity_lint.rs 8, session.rs 8, plus fixtures/tests (integration.rs 21, fixtures.rs 19, bootstrap_smoke.rs, id_mint.rs, identity_lint.rs).
- =architect= occurrences: api.rs 94, main.rs 29, manager.rs 26, dispatch.rs (test) 25, index.rs 24, NodeModal.tsx 22, integration.rs 21, ArchitectureView.tsx 15.
- Source files carrying an =orgasmic:= header marker that names an =arch_= id: 51. Of those, 21 marker lines are arch-ONLY (no =dec_=/=TASK-= companion) — those 21 files lose their entire back-edge if arch ids are dropped, while 30 degrade gracefully to their decision/task ids.
- State coupling: =.orgasmic/tasks/done.org= has 335 =arch_= references, tx logs 151 (2026-06: 58, 2026-07: 93), =decisions.org= 15, =tasks/goal.org= 6, =tasks/backlog.org= 2 (=:IMPLEMENTS: arch_RN73Z= on two live backlog tasks).

** Questions the exploration must answer with evidence
1. *Is the source-marker back-edge worth keeping without arch nodes?* The drift comparator is the only mechanical code↔model link in the product. If arch dies, does =// orgasmic:dec_XXXXX, TASK-XXXXX= keep enough value to retain a (renamed, simplified) drift check, or does drift die with it? Name the 21 arch-only marker files and say what each would carry instead.
2. *What happens to history?* tx records and done.org are append-only narrative. Excision must NOT rewrite them (dec on ID migration set the precedent that history keeps old ids and stays resolvable via git). Confirm that dangling =arch_= references in immutable surfaces do not trip the identity lint / malformed-ID lint, or specify the lint carve-out needed.
3. *What do the 15 decisions.org =arch_= references become?* Several decisions cite arch nodes as pairing context (dec on recovery cites arch_Z3Z3V/arch_A53QX; dec on graph-page excision says arch_M8JQT "sheds its claims at excision"). Do those decisions need amendment, or is a dangling-but-historical reference acceptable?
4. *What absorbs the content that is actually load-bearing?* Some arch nodes carry real constraints (INTERFACE/CONSTRAINTS claims, =:SOURCE_PATHS:=). Decide per top-level subsystem: fold into the owning decision, drop, or park in an archive file. Give a rough count of nodes in each bucket — do not do the folding.
5. *Live task fallout*: two backlog tasks carry =:IMPLEMENTS: arch_RN73Z=. Enumerate every open task with an arch edge and say what the drawer becomes.
6. *Does anything block on architecture at runtime?* Specifically: does dispatch, boot reconciliation, or the read model fail if =architecture.org= is absent, or is it already optional? Test it — this determines whether excision is subtractive or requires new fallbacks.
7. *Precedent*: the Graph page excision (dec_ZG9B9 / TASK-M6146) is the closest prior art for removing a whole surface. Read what that cost in practice and calibrate against it.

** Estimate format required
Break the removal into stages that can each land and gate independently, e.g.: (a) stop producing — retire the architector worker kind + architect stage + create verbs; (b) retire the drift comparator and the marker convention; (c) remove read model + CLI + UI surfaces; (d) retire the file and the node class from core schema/id/lint; (e) shipped instruction surface rewrite. For each stage give: files touched, LOC delta estimate, test suites affected, whether it is reversible, and a rough dispatch-round count. Flag which stages are cheap and independently valuable even if the operator later declines full excision.

** Non-goals
Do not remove anything. Do not edit architecture.org. Do not rewrite tx or done.org. No source changes beyond a throwaway experiment for question 6 (which must be reverted). The output is a report and a decision draft.

* BACKLOG TASK-37TAF An old CLI releasing with no terminal_tx still loses it on acp-stdio; only one skew direction is fail-closed    :dispatch:daemon:defect:skew:
:PROPERTIES:
:ID: TASK-37TAF
:PRIORITY: P1
:test_cmd: cargo test -p orgasmic-daemon
:END:

Found 2026-07-28 by the TASK-WGXKD.2 reviewer (reviewer-codex-rmux,
run-20260728T103240) as finding 2. Pre-existing from TASK-WGXKD.1, outside the
TASK-WGXKD.2 diff, so it did not block that round — but it means the parent
TASK-WGXKD **cannot claim both version-skew directions are safe**, and TASK-WGXKD
must not close until this is resolved.

** The defect

`crates/orgasmic-daemon/src/api.rs:6849,7118-7141`.

`terminal_tx` is optional on the release payload. A `finalized_by_worker` release
carrying `terminal_tx: None` releases the run and returns success with
`terminal_tx_id: None`. An old CLI then still owes the separate `POST /tx`.

On acp-stdio the release reaps the old finalize process's own setsid group
**before** that POST is made, leaving a released run with no terminal tx. That is
exactly the loss TASK-WGXKD exists to prevent, arriving through the skew
direction the capability handshake does not cover.

** What is already correct — do not redo it

The other direction is fail-closed and proven: new CLI against an old daemon is
refused by the TASK-WGXKD.1 capability handshake, and its regression passed under
review. Only old CLI → new daemon is open.

** Fix, per the reviewer

Two shapes, choose and record which:

A. Fail closed before releasing when `finalized_by_worker: true` arrives with no
   `terminal_tx`, and tell the operator to upgrade or restart the CLI. Keeps the
   lease held rather than releasing into a known-lossy path.
B. Reconstruct the terminal tx server-side from what the daemon already knows,
   so the old CLI's missing POST stops mattering.

A is the safe one and matches the handshake's existing posture; B removes the
skew concern entirely but has to be correct about what the daemon can reconstruct
without the worker's own report.

** Acceptance

- An old-payload / new-daemon acp-stdio regression that proves either the lease
  stays held, or the terminal tx lands atomically with the release.
- The chosen shape is recorded, not just implemented.
- TASK-WGXKD's version-skew claim is restated to match what is actually true.

** Non-goals

- Do not touch the capability handshake's new-CLI/old-daemon direction; it works.
- Do not fold this into the TASK-WGXKD.2 fix round (TASK-Q07Y5) — the reviewer
  scoped them apart deliberately, and Q07Y5's writer-budget work is unrelated.

* BACKLOG TASK-KPMFK A live stage loses its completion watcher across daemon restart, so no terminal tx is ever emitted    :daemon:stage:boot:defect:
:PROPERTIES:
:ID: TASK-KPMFK
:PRIORITY: P1
:test_cmd: cargo test -p orgasmic-daemon
:END:

Found 2026-07-28 by the TASK-C0XMR reviewer (reviewer-codex-rmux,
run-20260728T103249) as finding 2. Pre-existing and outside the C0XMR diff, so it
did not block that round — but it falsifies the cross-restart guarantee that
review asked C0XMR to prove, so it belongs to the same family.

** The defect

`crates/orgasmic-daemon/src/api.rs:3321-3330,8973-9020`.

`post_stage` keeps `stage`, `target` and completion ownership **only** in the
spawned in-process `StageCompletion`. Boot reattach reconstructs the run but
respawns only `DispatchCompletion`, and only when both `last_path` and
`stdout_path` exist.

Stage runs carry `last_path: Some` and `stdout_path: None`
(`api.rs:3239-3267`), so they land in the partial-artifact warning branch and **no
stage watcher is recreated**.

Consequence: if the daemon restarts while `grill`, `plan` or `architect` is live,
a later worker finalize is persisted but no `<stage>.completed` or
`<stage>.failed` tx is ever emitted. The C0XMR outcome rule is never called at
all. The stage silently has no terminal record.

No restart test exercises a stage watcher, which is why this survived.

** Fix, per the reviewer

Persist stage completion metadata — `stage`, project/task, and `target` — in
recoverable lifecycle state, and have boot reconciliation respawn or directly
reconcile `StageCompletion`.

Note the partial-artifact branch is doing real work for dispatch runs; the fix is
to stop treating `stdout_path: None` as "incomplete" for a run shape that legitimately
never has one, not to weaken the branch for everybody.

** Acceptance

- A daemon-restart test for each shared stage path (`grill`, `plan`, `architect`).
- The `architect` case must cover its report-only terminal tx specifically —
  `architect` is a STAGE kind whose worker finalize emits `architector.reported`
  rather than `architector.done` (recorded near `api.rs:22475`, TASK-6AYEJ.1), so
  a watcher keyed on `.done` would pass the other two and still miss this one.
- A stage live across a restart emits its terminal tx.

** Non-goals

- Do not fold this into the C0XMR fix round (TASK-QSSQH). That round owns the
  failure-stickiness rule; this one owns watcher recovery. They touch the same
  file and must not be dispatched concurrently.

* BACKLOG TASK-BCYMM Test fixtures leak whole process trees on the panic path; the Z3093 guard reaps sessions, not processes    :testing:leak:daemon:production:
:PROPERTIES:
:ID: TASK-BCYMM
:PRIORITY: P1
:test_cmd: cargo test -p orgasmic-daemon
:END:

A second leak class, measured 2026-07-28 11:35Z and **not** covered by
TASK-Z3093. That task made `LiveSessionGuard` reap rmux *sessions* on the panic
path. It does not own *spawned child processes*, so tests whose fixture forks a
process tree still leak it exactly as before.

** Measured

Three orphaned `cursor-worker-sibling` fixture trees, all reparented to PPID 1,
each holding a `/bin/sleep 3600` child:

| fixture pid | child pid | age    | executes from |
|-------------|-----------|--------|---------------|
| 5770        | 8260      | 10 min | `target/debug/deps/` (main checkout) |
| 13734       | 14957     | 42 min | `.orgasmic/tmp/dispatch/task-z3093/worktree/target/debug/deps/` |
| 24454       | 26030     | 39 min | `.orgasmic/tmp/dispatch/task-z3093/worktree/target/debug/deps/` |

Two of the three run from a worktree `dispatch-close` had **already removed** —
the binary keeps executing from an unlinked path, which is the same signature the
rmux leak showed (tx-20260728-orgasmic-3117) and is why neither is visible to a
`git worktree list` audit. Reaped by hand; recorded in
tx-20260728-orgasmic-3182.

** Mechanism

`crates/orgasmic-daemon/src/test_fixtures.rs`, the `cursor-worker-sibling` arm:

```
cursor-worker-sibling)
    ready=$2
    /bin/sleep 300 &
    (exec -a worker-server /bin/sleep 300) &
    /usr/bin/touch "$ready"
    /bin/sleep 3600
    ;;
```

It backgrounds two sleeps and then sleeps an hour in the foreground, so it never
exits on its own. The owning test,
`supervisor::tests::poll_direct_child_pid_prefers_worker_server_over_generic_sibling`,
kills it as a **trailing statement** — the same shape TASK-Z3093 fixed for
sessions, unfixed for processes.

And, once again, **the flake and the leak are the same event**: that test
panicked with `fake cursor-agent did not start children` in the manager's own
gate at 11:35Z, and the TASK-QSSQH worker independently recorded it failing on
the pristine tree under load. Every load-induced failure of this test leaks a
process tree that survives for an hour.

** Fix

Extend the same ownership idea from sessions to processes, in the one shared
guard TASK-Z3093 established (`orgasmic_drivers::modes::rmux::test_tooling`).

- A registration surface for spawned pids — `guard.owns_process(pid)` or a
  fixture handle that registers itself — reaped on `Drop`, before the flock
  releases, exactly as sessions are.
- Kill the **process group**, not the pid. The fixture's children are backgrounded
  subshells; killing only the parent orphans `sleep 3600` to init, which is
  precisely what two of the three observed cases show. Spawn the fixture in its
  own group so the group id is knowable.
- Audit every fixture arm that outlives its foreground command:
  `cursor-worker-sibling` (`sleep 3600`), `artifact-busy` (`sleep 120`),
  `claude-capture` (`exec sleep 600`), and `config` (`exec sleep 30`). Each is a
  candidate leak on its own test's panic path; say which ones are already covered
  by an existing kill and which are not.

** Acceptance

- A test that registers a fixture process and panics mid-body leaves no process
  behind — proven by injection the way TASK-Z3093 was: remove the reap, watch the
  new test fail naming the survivor, restore it, watch it pass.
- After a full `cargo test -p orgasmic-daemon`, no `fixture.sh` process and no
  `sleep` child of one remains.
- The fixture arms listed above are each classified as covered or fixed.

** Non-goals

- Do not change the rmux session reap; it is merged, verified and orthogonal.
- Do not fix `poll_direct_child_pid_prefers_worker_server_over_generic_sibling`'s
  flakiness itself. That belongs to TASK-STWVB. This task makes its cost bounded.
- This is **not** TASK-Z9C73. That one is a zombie harness child under the rmux
  daemon on the production release path; this is a test fixture's own process
  tree and involves no rmux at all.

* BACKLOG TASK-0RCRY Workers inherit the daemon's $TMUX, so tmux tests talk to the rmux server and six gated tests never run    :daemon:drivers:tmux:testing:
:PROPERTIES:
:ID: TASK-0RCRY
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-drivers/**,crates/orgasmic-daemon/**,.orgasmic/gotchas.org
:END:

**This task was filed on 2026-07-28 with a wrong diagnosis and rewritten the same
day.** The original claim — that workers inherit the daemon's `$TMUX` and so
reach the rmux server — is false and is recorded as such in
tx-20260728-orgasmic-3226. What survives is a narrower, measured problem.

=== What was measured, and what it ruled out

Filed after a TASK-1T3FZ dispatch reported six daemon failures with
`tmux new-session failed (exit 1): server exited unexpectedly` and
`required test tooling is missing: tmux (gates 6 tests)`.

Ruled out, each by direct measurement:

- *The daemon does not carry `TMUX`.* `ps eww 32814` shows none. (The earlier
  contrary reading came from `pgrep` matching a worker's prompt text instead of
  the daemon; the pid was a claude worker, not the daemon.)
- *A worker's tmux calls are not broken.* rmux creates the worker's pane, sets
  `TMUX=/private/tmp/rmux-501/default,...`, and prepends a shim directory in
  which `tmux` is a symlink to `rmux`. Under the exact worker `PATH` + `TMUX`,
  `tmux new-session -d -s … -- sleep 1` exits **0**.
- *It is not environmental at all.* `supervisor::tests::required_test_tooling_is_present`
  passes in isolation in both the manager environment and a replicated worker
  environment (0.04 s / 0.03 s).
- *It is not worker-specific.* A full `cargo test -p orgasmic-daemon --lib` in
  the **manager** environment — no shim, no `TMUX` — reproduced three of the
  worker's six failures identically: `production_resume_native_fork_uses_pinned_claude_not_path_shim`,
  `recovery_reattaches_tmux_session_when_handle_exists`, and
  `two_recovery_chain_second_resume_uses_first_fork_session_id`
  (528 passed, 5 failed, 451 s).

So these are **full-suite concurrency failures — the TASK-STWVB family** — and
this task is not about a worker/manager divergence. The RRT4T/R2HDN open question
is still open, and is now *more* open, because the leading candidate is gone.

=== The real, narrower defect

No tmux call site anywhere passes `-L` or `-S`
(`crates/orgasmic-drivers/src/modes/tmux.rs:1506`, `:2489`, `:3730`). Every
probe, every test session, and every production call therefore shares **one tmux
server** — whichever the environment selects. Concurrent tests create, query and
reap sessions on that single shared server, and on a developer machine it is the
same server the operator's own sessions live on.

`server exited unexpectedly` under a full-suite run, from a server that was
verified alive throughout (pid 32723, up since 13:32, still running at 15:1x), is
consistent with contention on that shared server rather than with a missing or
broken tmux.

=== Ask

- Give the test path an owned server: an explicit `-L <socket>` per test binary
  (or per test), so no test can reach a server it did not create, and so a
  developer's own tmux sessions are never touched by a test run.
- Decide separately, and state, whether production call sites should pin a
  socket too. An operator running the daemon inside tmux is the mirror image of
  this and would be a product bug, not a test bug.
- Make the diagnostic name the cause. `tmux test session should start` and
  `required test tooling is missing: tmux` are both true and both useless when
  tmux is installed, on PATH, and working — the message should name the server it
  tried and how that server was selected.

=== Acceptance

- The tmux-gated daemon tests use a server the test run owns, and a concurrent
  full-suite run does not produce `server exited unexpectedly`.
- Reproduce first: show the contention on the shared server before the fix.
  If it cannot be reproduced, say so plainly rather than shipping a speculative
  isolation change — that is precisely the mistake this task's first version made.
- Sessions created on the owned server are reaped. TASK-Z3093's guard covers
  rmux sessions, not a new tmux socket; say which mechanism reaps this one.
- The three failures that reproduce in the manager environment are re-run after
  the change and their status reported honestly, whether or not this fixes them.

=== Relationship to TASK-STWVB

STWVB is "four daemon tests fail on every full-suite run and pass alone". This
measurement says the set is at least five and includes the tmux-session-start
failures. If the socket isolation here does not fix them, that is a genuine
result and STWVB keeps them; do not widen this task into STWVB's investigation.

=== Non-goals

- Do not weaken the TASK-RRT4T sentinel to make the red go away. The red is
  correct — the tests really did not run.
- Do not use `ORGASMIC_ALLOW_MISSING_TOOLS` to paper over this. A contended
  server is not a missing tool, and the waiver would recreate the silent skip.

** Measured, one variable, 2026-07-28

The daemon runs inside an rmux pane and therefore carries rmux's `TMUX` in its
own environment:

```
$ ps eww 30834 | tr ' ' '\n' | grep ^TMUX=
TMUX=/private/tmp/rmux-501/default,32723,751
```

Every dispatched worker inherits it. A tmux client with `$TMUX` set and no
`-L`/`-S` connects to the socket named in `$TMUX` — which is the **rmux**
server's socket, and rmux is a tmux fork with an incompatible protocol version.

```
$ TMUX=/private/tmp/rmux-501/default,32723,751 tmux new-session -d -s p -- sleep 1
server exited unexpectedly
EXIT=1

$ env -u TMUX tmux new-session -d -s p -- sleep 1
EXIT=0
```

Same binary (tmux 3.6a), same host, same user, same TMPDIR. The only difference
is the inherited variable.

** Scope, measured rather than assumed

- `rmux` under the same inherited `TMUX` exits **0** — matching binary, matching
  socket. So the 7 rmux-gated tests do run in workers, and the rmux half of the
  RRT4T story was never the problem.
- The class is **tmux-only: 6 gated tests**
  (`crates/orgasmic-daemon/src/supervisor.rs:6941`,
  `ToolRequirement::new("tmux", 6, tmux_spawn_usable_for_test().await)`).
- Observed live in the TASK-1T3FZ dispatch: `required test tooling is missing:
  tmux (gates 6 tests); gated tests did not run`, with
  `recovery_reattaches_tmux_session_when_handle_exists`,
  `failed_terminal_with_live_tmux_never_offers_reattach`,
  `boot_reattach_respawns_dispatch_completion_watcher_without_artifacts`,
  `production_resume_native_fork_uses_pinned_claude_not_path_shim` and
  `two_recovery_chain_second_resume_uses_first_fork_session_id` all failing on
  `tmux test session should start`. Before TASK-RRT4T's sentinel these skipped
  **silently** — which is precisely the historical divergence.
- No probe and no production call site passes `-L` or `-S`
  (`crates/orgasmic-drivers/src/modes/tmux.rs:1506, 2489, 3730`), so all of them
  inherit whatever server `$TMUX` names.

** Ask

1. **Isolate the tmux server the tests use.** A test that means to exercise tmux
   should not be able to reach whatever server happens to host the daemon. Give
   the test path an explicit `-L <socket>` (or clear `TMUX` for the spawn), so
   the probe and the tests it gates talk to a server they own. Decide and state
   whether production tmux calls should do the same — an operator running the
   daemon inside tmux hits the mirror image of this, and that is a product bug,
   not a test bug.
2. **Make the diagnostic name the cause.** `tmux test session should start` and
   `required test tooling is missing: tmux` are both true and both useless here:
   tmux is installed, on PATH, and working. The failure text should say the
   server it tried, and that `$TMUX` selected it.
3. Reconcile `.orgasmic/gotchas.org`. R2HDN correctly demoted the PATH claim to a
   simulation and recorded the open question. Replace the open question with this
   measurement.

** Acceptance

- The 6 tmux-gated daemon tests pass inside a dispatched worker. Prove it by
  dispatching, not by reasoning — that is the whole point of this task.
- Reproduce first, the way it was found: set `TMUX` to a foreign server's socket,
  show the probe failing, apply the fix, show it passing.
- The manager path stays green; isolating the socket must not make tests reach a
  server they then leak. Whatever sessions the tests create are still reaped —
  TASK-Z3093's guard covers rmux sessions, not a new tmux socket.
- State whether production call sites were changed and why.

** Why this matters beyond the six tests

Every dispatched worker has been reporting a green tmux lane it never ran, for
as long as the daemon has been hosted in an rmux pane. The sentinel from
TASK-RRT4T is what made it visible; this is the first dispatch where a worker
reported the failure honestly instead of a silent skip. Any conclusion drawn from
a worker's daemon-suite count before 2026-07-28 should be treated as excluding
these six tests.

** Non-goals

- Do not weaken the sentinel to make the red go away. The red is correct.
- Do not change `ORGASMIC_ALLOW_MISSING_TOOLS` semantics; a foreign tmux server
  is not a missing tool, and papering over it with the waiver would recreate the
  silent skip this task exists to end.

* BACKLOG TASK-95SGV finish_dispatch_cleanup releases by a recomputed worktree key, so a removed directory leaks the reservation forever    :daemon:supervisor:hardening:
:PROPERTIES:
:ID: TASK-95SGV
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/**
:END:

Found by the TASK-1T3FZ worker while building the close-guard reservation, in a
path that round deliberately did not touch.

** The defect

`finish_dispatch_cleanup` — the TASK-NW4WV rollback path — looks its reservation
up by a **worktree key it recomputes after the directory has been removed**.
Path normalization on macOS resolves `/var` → `/private/var` only while the path
exists; once the directory is gone the recomputed key can differ from the one
installed, the lookup misses, and the reservation is never released.

A leaked reservation is not inert. `Supervisor::acquire_impl` blocks on
`cleanup_reservations`, so a stale entry silently refuses every future acquire on
that worktree path — and dispatch worktree paths are deterministic per task, so
the task becomes permanently undispatchable with no diagnostic naming why.

** Why the new path is immune, and why that is the fix to copy

TASK-1T3FZ's `reserve_dispatch_close` / `finish_dispatch_close` release by
`close_guard_id` — an opaque handle minted at install time — precisely so that
release never depends on recomputing a key from a path that may no longer exist.
Reservations also gained `owner_pid`, swept when an out-of-process holder dies.

`finish_dispatch_cleanup` should adopt the same shape: release by handle, and
carry an `owner_pid` backstop. This is a port of a verified fix, not a new
design.

** Ask

- Reproduce first. Install a reservation against a path under a symlinked
  prefix (`/var/...` on macOS), remove the directory, then call the existing
  release and show the lookup missing. If it does not reproduce on this
  platform, say so and say what would be required — that is a useful finding and
  should not be replaced by a speculative fix.
- Port the guard-id release and `owner_pid` sweep from the close path.
- Make a leaked reservation diagnosable: an acquire refused by a reservation
  should name the reservation, its owner pid, and whether that pid is alive.
  Today the operator sees a refusal with no way to tell a live cleanup from a
  stale entry.

** Acceptance

- A regression that removes the directory between install and release and shows
  the reservation released anyway; it must fail if the release reverts to the
  recomputed-key lookup.
- An acquire blocked by a reservation whose owner is dead is swept rather than
  refusing forever, and the refusal message names the owner either way.
- The TASK-1T3FZ close-guard tests still pass unmodified.

** Non-goals

- Do not restructure `cleanup_dispatch` or move it behind the daemon endpoint.
  TASK-1T3FZ considered that and recorded it as the larger follow-on if the
  daemon-state-only boundary is ever rejected; it is not this task.

* BACKLOG TASK-F9VEZ codex records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:codex:sessions:evidence:
:PROPERTIES:
:ID: TASK-F9VEZ
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. See that matrix for the measurements this rests on.

`CodexAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`). Verified 2026-07-28: `native_runtime` is implemented in exactly two places in the tree — `adapters/claude.rs:168` and the delegating `modes/acp_stdio.rs:111`.

Codex mints its own identity server-side: `thread.id` is read out of the `thread/start` response in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/codex.rs:198-206`) and surfaced only inside `Ready.capabilities` as `thread_id` (`:225`). `thread_start_params` (`:864-885`) has no id field, so orgasmic cannot pre-mint one the way `--session-id` lets it for claude.

Two consequences, both live:
1. `resolve_recovery_actions` cannot offer `resume_native_fork` for a codex run — there is no validated NativeRuntime session id to resume.
2. dec_Y5MPK item 6 (worker/operator attribution) is not computable for codex. 1457 rollout JSONL files sit under `~/.codex/sessions` on the operator machine (measured 2026-07-28) and none of them can be attributed to an orgasmic dispatch, because attribution is defined as "its session id appears in a NativeRuntime record".

Work:
1. Emit `NativeRuntimeMeta` from the handshake response rather than from launch: provider `codex`, session id = `thread.id`, `session_path` = the rollout file the transcript finder already knows how to locate, and a `resume_argv`/resume params that codex actually accepts. Establish what codex resume looks like before designing the shape — `thread_id` currently flows only into turn params (`:326-410`), and there is no resume call in the adapter today.
2. Confirm the mode carries a NativeRuntime that arrives *after* compose. `modes/acp_stdio.rs` reads `adapter.native_runtime()` immediately after composing the request, which is too early for a value the handshake produces. This is the real blocker and it is shared with TASK-B6D8W (cursor-agent) and TASK-2B215 (hermes).

See also TASK-ZY259: `thread_start_params` also sends `persistExtendedHistory: false`, which is a separate dec_Y5MPK item 4 question and should be settled first since it touches the same call.

Blocked on a contract decision recorded against TASK-SGRTX: whether the cross-harness session contract permits post-launch handshake capture followed by a `NativeRuntime` event, or requires pre-launch pinning. Do not answer it here with a codex-specific patch.

* BACKLOG TASK-B6D8W cursor-agent records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:cursor:sessions:evidence:
:PROPERTIES:
:ID: TASK-B6D8W
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX TASK-F9VEZ
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. Sibling of TASK-F9VEZ (codex); same shape, different protocol.

`CursorAcpAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`).

The session id is server-minted and post-launch: `sessionId` is read out of the `session/new` result in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/cursor_acp.rs:288-292`) and surfaced only inside `Ready.capabilities` (`:320`). Unlike codex this is not an implementation choice orgasmic could lobby to change: ACP defines `session/new` as the agent minting the session id, so a pre-launch pin in the claude `--session-id` sense has no protocol slot at all.

Consequences: no `resume_native_fork` for cursor-agent runs, and dec_Y5MPK item 6 attribution is not computable — 909 JSONL files under `~/.cursor` on the operator machine (measured 2026-07-28), none attributable to an orgasmic dispatch.

The transcript finder already handles the fallback: recorded path confined to `~/.cursor/projects`, else Ready `session_id` + worktree at **high** confidence, else cwd+launch-time at **medium** (`crates/orgasmic-drivers/src/transcript_finder.rs`). So locating a transcript mostly works; what is missing is the recorded NativeRuntime that makes it deterministic and makes attribution possible.

Work: emit `NativeRuntimeMeta` from the `session/new` result — provider `cursor-agent`, session id, resolved session path, and whatever ACP `session/load` (or the cursor equivalent) needs as resume argv. Verify against the binary rather than the spec; `session/new` already returns an undocumented structured models block (`:295`), so the shipped surface is wider than the published one.

Blocked on the same contract decision as TASK-F9VEZ, recorded against TASK-SGRTX: post-launch handshake capture versus mandatory pre-launch pinning. Cursor is the strongest evidence that pre-launch pinning cannot be made universal.

* BACKLOG TASK-2B215 hermes records no NativeRuntime, so its transcripts are neither resumable nor attributable    :drivers:hermes:sessions:evidence:
:PROPERTIES:
:ID: TASK-2B215
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-VB9DQ TASK-H0TYV TASK-SGRTX TASK-F9VEZ TASK-B6D8W
:END:

Filed by TASK-H0TYV (VB9DQ fix round) from the recorded cross-harness matrix. Sibling of TASK-F9VEZ (codex) and TASK-B6D8W (cursor-agent).

`HermesAdapter` never overrides `HarnessEventAdapter::native_runtime`, so it takes the trait default `None` (`crates/orgasmic-drivers/src/trait.rs:248`).

The session id is server-minted and post-launch: `sessionId` is read out of the `session/new` result in `on_ws_thread_started` (`crates/orgasmic-drivers/src/adapters/hermes.rs:287-292`, method name at `:327`) and surfaced only inside `Ready.capabilities`. As with cursor-agent, ACP gives no slot for a client-supplied session id.

Consequences: no `resume_native_fork` for hermes runs, and dec_Y5MPK item 6 attribution is not computable — 82 JSONL files under `~/.hermes/sessions` on the operator machine (measured 2026-07-28), none attributable to an orgasmic dispatch. The absolute number is small; the property is the same.

The transcript finder already resolves `~/.hermes/sessions/<id>.jsonl` from the Ready `session_id` at **high** confidence, and notes the paired `session_<id>.json` is metadata only (`crates/orgasmic-drivers/src/transcript_finder.rs`). Emitting NativeRuntime turns that into a recorded path rather than a recomputed one.

Work: emit `NativeRuntimeMeta` from the `session/new` result — provider `hermes`, session id, `~/.hermes/sessions/<id>.jsonl`, and a resume argv verified against the binary. Hermes is the cheapest of the three to prove because its path rule is the simplest.

Blocked on the same contract decision as its siblings, recorded against TASK-SGRTX.

* BACKLOG TASK-ZY259 codex thread/start sends persistExtendedHistory:false — decide whether that is a dec_Y5MPK item 4 violation    :drivers:codex:sessions:evidence:
:PROPERTIES:
:ID: TASK-ZY259
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-H0TYV TASK-F9VEZ
:END:

Filed by TASK-H0TYV (VB9DQ fix round). Small, bounded, and worth doing before TASK-F9VEZ.

dec_Y5MPK item 4: "Orgasmic never suppresses vendor persistence. A transport that disables the harness's own session recording is a defect, not a configuration choice." TASK-VB9DQ removed claude's `--no-session-persistence` on exactly that ground.

`thread_start_params` sends `persistExtendedHistory: false` to codex app-server on every run (`crates/orgasmic-drivers/src/adapters/codex.rs:883`). It is the only occurrence in the tree, it carries no comment, and no test asserts anything about it. It is set to `false` — the suppressing value — and nothing records why.

What is known: codex still writes rollout JSONL with this flag set. A codex run that died on a provider 503 wrote `~/.codex/sessions/2026/07/25/rollout-....jsonl` (TASK-VB9DQ evidence), and 1457 rollout files exist under `~/.codex/sessions` on the operator machine (measured 2026-07-28). So it is not the blanket kill switch `--no-session-persistence` was.

What is not known: what "extended history" actually is, and whether anything retro or resume would need is missing from those rollouts because of it.

Work:
1. Determine what codex app-server does with `persistExtendedHistory` — read the shipped app-server, do not infer from the name.
2. If it removes content a retro run or a resume would want, remove the flag and say so under dec_Y5MPK item 4, exactly as claude's flag was removed.
3. If it is genuinely orthogonal to session persistence, leave it and add the one-line comment that its absence is why this task had to be filed.

Either way the outcome is a recorded answer. The defect being fixed here is that a persistence-shaped flag sits in the launch path with no stated reason.

* BACKLOG TASK-E3K1B doctor reports no vendor-transcript inventory, so dec_Y5MPK's operator-owned retention has no numbers to act on    :doctor:sessions:evidence:
:PROPERTIES:
:ID: TASK-E3K1B
:PRIORITY: P2
:RELATES_TO: dec_Y5MPK TASK-H0TYV TASK-F9VEZ TASK-B6D8W TASK-2B215
:END:

Filed by TASK-H0TYV (VB9DQ fix round). dec_Y5MPK names `doctor` as the owner of two obligations and neither exists in code.

Item 5 (2026-07-25): orgasmic never prunes a vendor transcript, and "its obligation is visibility: report per-harness counts, total size, and age distribution through `doctor` so the operator can act with full information."

Item 6 (added 2026-07-28 by TASK-H0TYV): that per-harness report splits into orgasmic-attributed and unattributed, where a transcript is orgasmic-attributed exactly when its native session id appears in a `NativeRuntime` lifecycle record in orgasmic's own session JSONL.

Neither has been filed until now, and `doctor` reports nothing about vendor stores. The decision assigns retention to the operator on the explicit premise that orgasmic supplies the numbers; without this the operator owns a duty they have no instrument for. Measured on the operator machine 2026-07-28: 652 JSONL under `~/.claude/projects`, 1457 under `~/.codex/sessions`, 909 under `~/.cursor`, 82 under `~/.hermes/sessions`.

Work:
1. Per-harness inventory in `doctor`: file count, total size, age distribution. Roots are already defined and test-overrideable — reuse `TranscriptRoots::from_env_home` (`crates/orgasmic-drivers/src/transcript_finder.rs`) rather than hardcoding a second copy of the path rules.
2. Split each harness's numbers into orgasmic-attributed and unattributed by the item 6 rule. Read attribution from recorded `NativeRuntime` events; never from path shape, file age, or launch-time proximity — those are the discovery heuristics dec_Y5MPK item 3 forbids.
3. Report honestly where the split is not computable. Today only claude mints its own session id, so codex, cursor-agent and hermes are wholly unattributable until TASK-F9VEZ, TASK-B6D8W and TASK-2B215 land. `doctor` must say "attribution unavailable for this harness" and name the blocking task, not silently report 0 attributed — a zero that means "cannot tell" reads as "orgasmic wrote none of these", which is false and would misdirect a prune.
4. Report only. `doctor` never deletes, moves, or offers to delete a vendor file (item 5).

* BACKLOG TASK-4PPPE serve announces readiness before it can handle SIGTERM, so the graceful-shutdown test races itself
:PROPERTIES:
:ID: TASK-4PPPE
:PRIORITY: P2
:TAGS: cli,lifecycle,flake
:END:
** Description
Found by the TASK-JQARS worker while classifying its own gate failures, and
correctly excluded from that task's scope. It is a **test defect, not a
production defect** — the production path was probed directly and is sound.

=== Measured

`crates/orgasmic-cli/tests/daemon_lifecycle.rs`,
`sigterm_exits_through_graceful_shutdown...`.

- On the JQARS branch: **3 pass / 6 runs**.
- On the pristine baseline (`git stash -u`, same command): **2 pass / 3 runs**.

It fails on both, so it is pre-existing and unrelated to that change.

=== Mechanism

The test sends SIGTERM the instant `serve` prints "press Ctrl+C to stop".
`main.rs` prints that line **before** `wait_for_shutdown_signal()` registers the
handler, so a SIGTERM that arrives inside that window hits the default
disposition and kills the process instead of running the graceful path. The
test's own comment describes the window.

Direct probe of the built binary using the test's exact home, config and
process-group setup: `rc=0` in **3/3 with SIGTERM at 0 ms delay**, and **3/3
with a 300 ms delay**. The production sequence is fine; the test races its own
readiness signal.

=== Why it matters beyond the flake

The readiness line is a **lie about readiness** for one specific consumer: a
supervisor that treats "press Ctrl+C to stop" as "safe to signal" has the same
race. Nothing in the product does that today, but the plist and any future
service adapter are exactly the kind of consumer that would. Decide whether the
line should move after handler registration, or whether a separate, explicit
readiness signal is owed — and record which, because "the test is racy" and
"the readiness announcement precedes readiness" are different bugs with the same
symptom.

=== Ask

1. Make the readiness announcement follow handler registration, or introduce a
   readiness signal that does, and say which you chose and why.
2. Remove the race from the test rather than papering it with a sleep — a sleep
   restores green without restoring the guarantee.
3. Check the same ordering question for Ctrl+C and for the service-stop path
   ATAXN's marker protocol now depends on.

=== Acceptance

- The test passes 10/10 consecutive runs, and a run with SIGTERM at 0 ms delay
  still exits through graceful shutdown.
- Prove it by injection: restore the announcement to its current position, watch
  the test fail, move it back, watch it pass — with output.
- State whether any shipped consumer (plist, installer, docs) tells an operator
  or supervisor to signal on that line.

=== Non-goals

- Do not change the shutdown budgets or the ATAXN marker protocol.

* BACKLOG TASK-QCG6J the reattach fence regression asserts which refusal fired, so it goes red on a safe outcome under load
:PROPERTIES:
:ID: TASK-QCG6J
:PRIORITY: P2
:TAGS: daemon,tests,flake
:END:
** Description
Found by the manager's independent gate on TASK-AK6EM, 2026-07-28. **The fence
itself is correct** — this is about what its test asserts.

=== Observed

In a full `cargo test -p orgasmic-daemon --lib` under concurrent load:

```
---- api::tests::explicit_reattach_tmux_is_fenced_out_of_a_worktree_a_close_guard_holds ----
panicked at crates/orgasmic-daemon/src/api.rs:19229:
the refusal must name the cleanup reservation:
  recovery action reattach_tmux is not valid for run run-fence-d825d586...
```

The reattach **was refused**. It was refused by an earlier validity check rather
than by the cleanup fence, because under load the test's tmux session was not
live by the time the recovery action was evaluated. The test asserts *which*
refusal fired, so it goes red on a safe outcome.

Its sibling `boot_rehydration_is_fenced_out_of_a_worktree_a_close_guard_holds`
passed in the same run and its log shows the fence doing exactly its job:

```
boot reattach: skipped (mux session not live / not reattachable)
  run_id=run-fence-cc02bfb7... task_id=TASK-BOOT-FENCE
  error=dispatch cleanup in progress for task=TASK-BOOT-FENCE kind=Worker
```

So the P0 fix is sound and merged. This task is the test's precision.

=== Why it is worth fixing rather than tolerating

A P0 destructive-path fence whose regression goes red for a reason unrelated to
the fence trains the next reader to wave it through — which is precisely how a
real fence regression would get discounted. It also lands on the standing
known-flake list, which this project is actively trying to shrink (TASK-5HBST,
TASK-STWVB).

Note the same full run showed two more load-induced failures on the same axis —
`post_artifact_regenerate_hot_path_reuses_live_run_id` ("harness busy", 409) and
`poll_direct_child_pid_prefers_worker_server_over_generic_sibling` ("fake
cursor-agent did not start children"). AK6EM added three new tmux-shaped tests,
and the `LiveSessionGuard` flock serializes live sections across every test
binary, so the tmux contention window widened. That is context for TASK-STWVB,
not a separate defect.

=== Ask

1. Make the assertion robust to *which* refusal fires: the property under test
   is "a reattach into a guarded worktree does not become live". Assert that,
   and assert the reservation-named refusal only when the run is actually still
   reattachable — or make the fixture's tmux session reliably live for the
   duration, if that can be done without a sleep.
2. Do not weaken it into "some error happened". The test must still fail if the
   fence is removed; verify that by injection (`worktree: None` in the reattach
   admission was AK6EM's own injection and turned four tests red).
3. Say whether the sibling boot-rehydration and pending-plan fence tests have
   the same exposure and fix them together if so.

=== Acceptance

- 10 consecutive full `cargo test -p orgasmic-daemon --lib` runs with no failure
  from any of the three fence tests. Report the count.
- Removing the fence still turns them red, shown by injection with output.
- State plainly whether the fixture still depends on a live tmux session and, if
  so, what now guarantees it.

=== Non-goals

- Do not change the fence, the admission module, the close-guard persistence, or
  dec_NFZY2. All are merged and manager-verified.

* BACKLOG TASK-DTCBP orgasmic verify: --all, the verify_artifact drawer property, and the dispatch-close gating decision    :cli:verification:mechanism:
:PROPERTIES:
:ID: TASK-DTCBP
:PRIORITY: P2
:END:

Follow-ups from TASK-TCTTD (merged 539d2af), as listed by its implementer and
confirmed by the manager's replay. The verb works and is proven; these are the
integration steps that make it bind by default instead of on request.

=== 1. `orgasmic verify --all`

Replay every artifact under `verify/`, machine-readable summary, nonzero exit
if any artifact fails. This is the invocation items 2 and 3 actually want.
Expect ~2x a normal suite per artifact plus rebuild churn from patch cycling
(measured on TASK-R74E8: each arm recompiles orgasmic-daemon, 10-35s).

=== 2. `verify_artifact` drawer property (the task body's stated preference,
deferred by the implementer for write scope — correctly)

Add the schema field in orgasmic-core, let implementers set it, have
`orgasmic verify` prefer it over the path convention, and validate at
`dispatch-close` that a task claiming an artifact has one that loads.

=== 3. DECIDE: gate `manager dispatch-close --status done` on a passing replay

TASK-TCTTD's own non-goal said "once the verb has been used in anger". It now
has been (three replays by the manager on merge day, including a false-green
catch of a manager-authored no-op probe). Record the decision either way; if
gating, an escape hatch (--no-verify with a recorded reason) is required for
tasks whose defects cannot be patch-expressed.

=== Acceptance

- `verify --all` replays both shipped artifacts green and a deliberately broken
  one red, with one summary line each.
- The drawer property round-trips create -> get and `verify` resolves it.
- The gating decision is recorded as a dec_ node or in the task, with reasons.

=== Non-goals

- No CI wiring here (TASK-S2KM0 owns the lane; it should call `verify --all`).

* BACKLOG TASK-0DANB Billing-capable daemon tests are one lock from a charge and their ignore reasons hide the spend    :daemon:testing:mechanism:
:PROPERTIES:
:ID: TASK-0DANB
:PRIORITY: P2
:END:

Follow-up to TASK-MFJZ7 (merged), which gated the one ungated billing-capable
test and audited the rest. Its audit left four daemon-side items, all
default-safe today via bare `#[ignore]` but out of that dispatch's write scope:

- `real_cursor_agent_stream_json_bridge_emits_events_and_releases`
  (`crates/orgasmic-drivers/tests/cursor_agent.rs:136`) — wait: drivers crate,
  but was left as-is because already ignored; include for wording only.
- `dispatch_endpoint_routes_cursor_agent_through_supervisor_and_emits_run_created`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:994`)
- `dispatch_endpoint_routes_acp_stdio_codex_through_supervisor`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:1060`)
- `dispatch_grace_path_writes_artifacts_without_terminal_session_marker`
  (`crates/orgasmic-daemon/tests/dispatch_endpoint.rs:2129`)

Three carry ignore reasons phrased as availability ("requires X installed")
when the real hazard is spend: someone installing codex and running
`--include-ignored` to "check the codex path" gets a charge. `#[ignore]` is a
control, but a single-lock one, and the wording invites exactly the mistake.

=== Ask

1. Extend the MFJZ7 mechanism (`BILLED_TESTS` + `skip_unless_billing_allowed`
   + `report_billed_tests`, in `orgasmic_drivers::modes::rmux::test_tooling`)
   to the daemon test binaries above, so `--include-ignored` alone cannot bill
   without `ORGASMIC_ALLOW_BILLED_TESTS=1`.
2. Reword every billing-capable test's ignore reason to name the spend.
3. Re-run MFJZ7's audit method on `crates/orgasmic-daemon/**` (it audited
   drivers thoroughly; daemon got only the grep-obvious cases): every
   `.acquire(`/harness spawn in daemon tests, verdict per test.

=== Acceptance

- Collection proof both arms (never execution), per the MFJZ7 standard: without
  the env, `--include-ignored --list` collects but the guard's runtime skip is
  shown on a safe representative; with the env, collection shows it armed.
- Audit table recorded on this task.
- A `verify/TASK-<this>/` artifact per the TASK-TCTTD standard — with a patch
  that `git apply` ACCEPTS (MFJZ7's shipped patch was a documentation diff that
  git apply rejects; do not repeat that).

* BACKLOG TASK-15MJN RESTART_DRAIN_TIMEOUT carries 15s of narrative slack; name it as a constant on the restart-endpoint axis    :daemon:hardening:
:PROPERTIES:
:ID: TASK-15MJN
:END:

From TASK-G7E4R's axis sweep (its report, section 3): daemon_lifecycle::RESTART_DRAIN_TIMEOUT (40s) waits on the restart endpoint's own budgets (RELEASE_FINALIZATION_DRAIN_TIMEOUT 20s + RESTART_WRITER_DRAIN_TIMEOUT 5s = 25s) and is already guarded by the relationship test restart_drain_fuse_outlasts_the_endpoint_budgets_it_waits_on — so it is NOT the unguarded-literal shape G7E4R removed. But its remaining 15s of slack ('rest of the handler') is documented in prose rather than composed from a named margin constant. Ask: express it as endpoint_budgets_total() + RESTART_HANDLER_MARGIN with the existing relationship test asserting the composition, the G7E4R/QRB8S pattern. Small; ship with a verify artifact.

* BACKLOG TASK-A2YEM RESTART_DRAIN_TIMEOUT carries 15s of narrative slack; name it as a constant on the restart-endpoint axis    :daemon:hardening:
:PROPERTIES:
:ID: TASK-A2YEM
:PRIORITY: P2
:END:

From TASK-G7E4R's axis sweep (its report, section 3): daemon_lifecycle::RESTART_DRAIN_TIMEOUT (40s) waits on the restart endpoint's own budgets (RELEASE_FINALIZATION_DRAIN_TIMEOUT 20s + RESTART_WRITER_DRAIN_TIMEOUT 5s = 25s) and is already guarded by the relationship test restart_drain_fuse_outlasts_the_endpoint_budgets_it_waits_on — so it is NOT the unguarded-literal shape G7E4R removed. But its remaining 15s of slack (rest of the handler) is documented in prose rather than composed from a named margin constant. Ask: express it as endpoint_budgets_total() + RESTART_HANDLER_MARGIN with the existing relationship test asserting the composition, the G7E4R/QRB8S pattern. Small; ship with a verify artifact.

* BACKLOG TASK-EP3H1 dispatch-close tears under load: tx appended, lifecycle stranded, and a timed-out request can succeed invisibly    :cli:daemon:hardening:
:PROPERTIES:
:ID: TASK-EP3H1
:END:

Measured 2026-07-29 at load average ~190 (a worker deliberately inducing load).
`orgasmic manager dispatch-close --task TASK-G7E4R --status done` produced:

  warning: close tx appended but lifecycle update failed: daemon request
  failed ... is the daemon reachable?
  closed: TASK-G7E4R implementer.done tx=tx-20260728-orgasmic-3500

State afterwards: close tx recorded, dispatch record closed, task stranded at
`in_review`. The warning is loud (good — this is not a silent tear), but the
operator is left to repair the lifecycle by hand.

The repair then hit the second half of the defect: a follow-up
`task update --state done` returned `"changed": {}` with an EMPTY tx_id —
because the "failed" lifecycle request had actually landed server-side after
the client gave up. Client timeout is not server failure. Two consequences:

1. `dispatch-close` is not atomic across its two writes (tx append +
   lifecycle update) and does not retry or reconcile the second on failure.
2. A timed-out request can succeed invisibly, so naive retry logic can
   double-apply or mis-read state. The `{"changed":{}, "tx_id":""}` no-op
   response was correct here, but nothing documents that contract.

=== Ask

1. Make dispatch-close's task-state transition part of the same daemon
   operation as the close tx (single request, single commit), or make the CLI
   reconcile on startup of the next manager command ("close tx tx-N exists but
   task is in_review -> finish the transition").
2. Idempotency: closes and state updates should carry the started-tx (they
   already do) and the daemon should treat a re-submitted close/transition for
   an already-closed dispatch as a clean no-op with a response that SAYS it was
   already applied, distinguishable from "nothing to do".
3. While in the file: the CLI's timeout error text "is the daemon reachable?"
   misdiagnoses scheduler starvation as daemon death (three occurrences on
   2026-07-29, all with the daemon healthy and answering raw HTTP in 0.4s).
   Say "request timed out after Ns; the daemon may be healthy but the system
   is under load" and name the load if cheaply readable.

=== Acceptance

- A close whose lifecycle leg is injected to fail leaves a state the NEXT
  manager command repairs automatically, proven by test.
- A re-submitted close is a labelled no-op, proven by test.
- Injection per the TCTTD standard; verify artifact `git apply`-clean.
