Implement TASK-48SFW from committed main18d170d2. Risky: priority + daemon lifecycle blast radius. Selection implementer stdio/codex, gpt-5.5, high. Independent Claude review follows. P0 diagnostic runs independently in another worktree; no shared target directory.

Read actual task via CLI, project/gotchas and all callers before editing. Parent and separate planner verified these constraints:
- ensure_running sync/async Down paths converge at daemon_lifecycle::start_via_selected_adapter.
- unauthorized repair calls stop_inner, which can send /daemon/restart drain BEFORE daemon_service::stop. Ownership guard must precede that drain/probe.
- main::cmd_daemon_restart captures/prepares runtime overrides before calling lifecycle restart. Guard BEFORE RuntimeSnapshot/preparation so --from-source/--clear-runtime-override cannot mutate the binary/config before refusing (or enter rollback/restart).
- Service adapters use per-user fixed identities: macOS plist, systemd user unit, Windows scheduled-task name. A foreign ORGASMIC_HOME does not isolate them. No existing owner parser was found.

Implement the smallest shared installed-service-owner check reused at startup and shutdown mutation boundaries. Use native readers/existing helpers where possible; do not add a general service framework, parser framework or dependency for this. Discover actual installed owner rather than assuming ~/.orgasmic. macOS owns via plist EnvironmentVariables.ORGASMIC_HOME; systemd effective properties may include overrides; Windows query installed task action/referenced wrapper, not requested home's private wrapper cache. Compare canonical home paths; allow legitimate nondefault owner and symlink aliases. Existing unreadable/malformed/empty/ambiguous/unsupported service ownership must refuse before mutations. Missing file plus loaded service is unknown, not a fresh install. Genuinely absent service permits first installation. Detached adapter has no shared service ownership; preserve its existing test seam. --force does not override foreign/unknown ownership. Preserve active runtime-override/managed-binary selection.

Actual task acceptance also requires ORGASMIC_RUN_ID lifecycle refusal: reject explicit lifecycle calls from a worker and automatic start/auth-repair that would mutate lifecycle. Ordinary worker reads against an already running daemon still work. Preserve ensure_running[_async]'s earliest ORGASMIC_DAEMON_URL successful bypass; explicit local lifecycle still refuses externally-owned URL. Add safe worker verification recipe to existing relevant guidance, not a new doc tree; load writing-for-agents skill if editing agent guidance.

Focused regression evidence (red without fix, green with it): foreign installed service + unavailable requested daemon rejects both sync/async auto-start with service bytes unchanged/no adapter mutation; stop/restart/force/unauthorized repair rejects before drain; runtime override JSON/binary unchanged on --from-source or --clear refusal; matching nondefault owner and alias allowed; first-install absent vs unknown/malformed/duplicate/loaded-with-missing definition; worker lifecycle refusal and existing-daemon reads; explicit URL bypass. Prefer existing test_support env_guard/ScopedEnv/RecordingDaemon and temporary service definitions plus a narrow command-recording seam if necessary. Never execute real launchctl/systemctl/schtasks mutations in tests. Keep smallest meaningful checks; no per-method mirror suite.

No operator daemon start/stop/restart/update, no push/deployment, no real service mutation, no raw provider CLI, no provider/quota probes or billed tests. Never stage ORGASMIC_HOME for daemon-dependent CLI calls: real test daemons must be booted explicitly with a child ORGASMIC_DAEMON_URL. Never cargo test --workspace. Run focused checks via scripts/run-tests.sh, logs redirected, default billing skip/watchdog, CARGO_BUILD_JOBS=2 and worktree's own target. No release/UI build required. Commit scoped changes and finalize exact dispatch with summary file; include actual SHA, source scope, red/green logs, acceptance evidence and remaining platform limitations. Parent owns independent review/merge/deployment/closure.

Evidence directory /Users/aspirational/.codex/artifacts/orgasmic-recovery-20260906; prefix p1-. If complete platform owner discovery would require substantial new machinery, report the exact boundary and smallest safe alternative promptly rather than silently accepting unknown ownership or building a framework.
