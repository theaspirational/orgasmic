TASK-48SFW final small validation fix. Reuse retained worktree/target at101f7e18. Risky task remains declared; implementer stdio/codex gpt-5.5 high, independent Claude review follows. The two prior independent reviews approve the implementation. Do not reopen their broader scope.

Only edit daemon_service.rs and its existing tests:
1. systemd_unescape: when the next bytes begin backslash-x, require exactly two valid hex digits available; incomplete or invalid hex must bail, not fall through and drop the backslash. Preserve valid UTF-8 byte accumulation, single decoding and literal escaped backslashes. Add assertions for invalid/incomplete hex to the existing Unicode test. This fixes L1 in p1-fix-independent-review.md.
2. read_macos_plist_for_owner_scan: native conversion fallback is intended for binary plists. Restrict that fallback to the binary plist magic bplist00; unreadable/non-UTF8 XML must remain an error rather than going through a normalizer that could collapse duplicate keys. Preserve existing valid binary-plist test. Add one malformed/non-UTF8 XML refusal assertion using a temporary file. No general plist parser, dependency or broader UTF-16 support.

Do NOT implement reviewer M1's suggested disk fallback when loaded owner is unknown: original acceptance explicitly requires refusal. No policy change, force override, runtime/service mutation, P0 edits, rollback, deployment or push.

Run only the smallest relevant regression filters through scripts/run-tests.sh, redirected logs, default billed skip/watchdog, CARGO_BUILD_JOBS=2 and this worktree's own target. Observe the new malformed-hex assertion failing before correction if possible; the established tests are already green. No broad suite, raw/piped cargo tests, repeated builds/clippy, extra frameworks or reviewer spawning. Format/diff-check this small edit, commit, write a concise final summary to the artifact directory and finalize exact generation. Evidence prefix p1-validation- in /Users/aspirational/.codex/artifacts/orgasmic-recovery-20260906.
