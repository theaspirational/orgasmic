#+title: orgasmic task TASK-BX0K0
#+orgasmic_version: 2

* DONE TASK-BX0K0 CLI parity for stubbed graph/task/reconcile commands :cli:arch_C87Z9:
:PROPERTIES:
:ID:               TASK-BX0K0
:PRIORITY:         P1
:WORKER:   implementer-codex
:IMPLEMENTS:       arch_C87Z9
:PRODUCES:         crates/orgasmic-cli/src/main.rs crates/orgasmic-cli/tests/cli_parity.rs
:WRITE_SCOPE:      crates/orgasmic-cli/src/main.rs crates/orgasmic-cli/tests/cli_parity.rs
:READ_SCOPE:       .orgasmic/architecture.org .orgasmic/decisions.org docs/adr/*.org crates/orgasmic-daemon/src/api.rs crates/orgasmic-cli/src/**
:TEST_CMD:         cargo test -p orgasmic-cli
:DEPENDS_ON:       TASK-M1MZ4
:END:

** Description
Audit-surfaced arch_C87Z9 gap: =orgasmic tasks=, =orgasmic task=, =orgasmic glossary=, =orgasmic decision=, =orgasmic architecture=, =orgasmic adr=, =orgasmic snapshot=, =orgasmic reconcile= dispatch to =not_implemented= in =crates/orgasmic-cli/src/main.rs= despite the daemon exposing real routes. arch_C87Z9 promises "CLI mirrors daemon capabilities and is the complete tool surface".

** Acceptance Criteria
- [ ] Each of =tasks list / task get|update / glossary list|get|create / decision list|get / architecture list|get / adr list|get|regenerate / snapshot take|list|restore / reconcile run|status= subcommands invokes the matching daemon route via =DaemonClient= and renders the response.
- [ ] New parity test =crates/orgasmic-cli/tests/cli_parity.rs= asserts every declared CLI subcommand resolves to a real handler (not =not_implemented=) by walking clap's command tree and checking each leaf against an allow-list of subcommands whose v0.0.1 backing is intentionally deferred (today: none).
- [ ] =cargo test --workspace= green; clippy clean; fmt clean; diff check clean; MSRV 1.78.

** Worklog

