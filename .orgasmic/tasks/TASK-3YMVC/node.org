#+title: orgasmic task TASK-3YMVC
#+orgasmic_version: 2

* DONE TASK-3YMVC De-scaffold conventions; fold universal guidance into orgasmic entry :conventions:entry:
:PROPERTIES:
:DEPENDS_ON: dec_MCM6Y TASK-74TA6
:ID: TASK-3YMVC
:PRIORITY: P2
:WRITE_SCOPE: crates/** shipped/**
:END:

** Description
Implement dec_MCM6Y. Remove the four generic conventions (=contributing.org=, =manager-implementer.org=, =no-skill-installed.org=, =orgasmic-tooling.org=) from =SCAFFOLD_FILES= and the scaffold tree; delete =no-skill-installed.org= outright. Fold their universal content — write rules, contributing discipline, tooling usage, dispatch-through-workers rule — into the =orgasmic entry= output (shipped text, update-tracked). =.orgasmic/conventions/= remains a user-extension dir the manager consults when present; verify =prompt-specs/manager.org= wording needs no change for the empty/absent case. manager-implementer content moves to =workflows/default.org= (TASK dependency via dec_T949W slice), not into entry output.

** Acceptance Criteria
- [ ] Fresh init creates no =conventions/= files (dir may be absent).
- [ ] =orgasmic entry= output carries the universal guidance previously scaffolded.
- [ ] No shipped reference to =no-skill-installed.org= remains (entry.org router, doctor, tests).

