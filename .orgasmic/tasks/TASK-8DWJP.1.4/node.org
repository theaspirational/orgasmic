#+title: orgasmic task TASK-8DWJP.1.4
#+orgasmic_version: 2

* IN_REVIEW TASK-8DWJP.1.4 Round 6 for 8DWJP.1.3: run the rebase abort under the writer barrier; carry pending salvage; restore SALVAGE_REF assertion :e01mc:chain-review:
:PROPERTIES:
:ID: TASK-8DWJP.1.4
:PRIORITY: P1
:RELATES_TO: TASK-8DWJP.1.3
:FIX_SUBTASK: t
:END:

Follow-ups of the 8DWJP.1.3 review (claude-opus-5 high, tx-20260901-orgasmic-8bf79b46, APPROVE WITH FOLLOW-UPS; merged 1ff48c3a stays on local main). Round 6 of the conflict path; scope is deliberately tiny.

MEDIUM: abort_rebase_with_salvage (ledger_sync.rs ~:369-373, called at ~:107 and ~:211) runs outside the writer barrier; only park_conflict is wrapped in run_barrier (~:895-903). A writer rename onto machines/<id>/tx/<month>.org between salvage_worktree add and git rebase --abort is hard-reset away with no copy in any ref. Run salvage+abort under the same barrier at both sites.

LOWs: (a) empty unmerged set at entry (~:103-117) mints a salvage ref that is never reported and the later in-tick ref the status names lacks the outage writes - carry a pending_salvage ref into status/event (PENDING_SALVAGE_REF) or warn + status note; (b) salvage failure at ~:370 propagates before the abort = ledger wedged; decide the trade (preferred: warn, empty salvage_ref, still abort) and pin it with a ponytail comment; (c) restore the SALVAGE_REF event assertion in conflicting_two_writer_tick (~:1417). NOT in scope: salvage-skip noise / conflict-ref retention.

** Acceptance
- [ ] A writer append issued while the abort runs lands after the abort and survives (test).
- [ ] pending salvage ref from the empty-unmerged entry case is named in status/event or warned + noted.
- [ ] salvage-failure trade recorded in code; SALVAGE_REF event assertion restored.
- [ ] daemon ledger_sync/status/sync_conflict/barrier tests, cli daemon_lifecycle, clippy -D, fmt green.
** Notes
Implementer: opencode / zai-coding-plan/glm-5.3 variant max (first opencode dispatch in this ledger), commit 641ec6c8, merged 62f986e0. Deviation on LOW (c): the verbatim SALVAGE_REF assertion cannot hold in conflicting_two_writer_tick (its pull stops mid-rebase, source Worktree; abort-time salvage tree == orig-head tree, so salvage_ref is empty; verbatim restore fails - probe log cited in the report). Assertion relocated to mid_rebase_tick_aborts_and_recovers_instead_of_idling; the negative is pinned in conflicting_two_writer_tick. Salvage-failure trade chosen: degrade (warn, empty salvage_ref, still abort) - keeps the 1.2 unwedge guarantee. Residual: synced+pending-salvage note lives in LedgerSyncStatus.error with outcome synced, which CLI status/doctor do not render (daemon log warn + API only).
Review (opus-5 high, tx-20260901-orgasmic-bac902c5): APPROVE WITH FOLLOW-UPS. Accepted LOW residuals, not fixed in this round (operator may carry or drop): (1) ledger_sync.rs:123 pending_salvage is dropped on the tick Err paths (pull bail, push exhaustion, any ? in stage/commit) - ref exists in git but is named nowhere; fix = warn it in sync_ledger_at_with_park Err arm (~:811). (2) :730 synced+pending note rides LedgerSyncStatus.error under outcome synced, which orgasmic status (cli/main.rs:2791) and doctor (cli/doctor.rs:301) do not render, and it self-erases next tick; fix = one match arm in each. (3) :986 production barrier wiring (spawn) is unpinned by tests - pre-existing since the park barrier; fix = extract barrier_closures(writer, runtime) and test it. (4) :884 record_sync_conflict request_id ignores pending_salvage in the unrecoverable branch (parked_ref and salvage_ref both empty) - a later tick with the same remote_head+paths dedups away a different pending ref; fix = append the pending ref to the key (~:886-889). Also out of scope by brief: salvage-skip noise / conflict-ref retention (8DWJP.1.3 LOW d).
** Evidence
Worker commit 641ec6c8 (opencode zai-coding-plan/glm-5.3 variant max) merged to main as 62f986e0 (--no-ff, from 1ff48c3a). Manager gates on merged main 2026-09-01: cargo test -p orgasmic-daemon --lib -- ledger_sync status sync_conflict barrier = 32 passed 0 failed (/private/tmp/claude-501/-Users-aspirational-Documents-code-tools-orgasmic/25219e20-4403-4221-9eb2-cd596e21be27/scratchpad/gates-8DWJP.1.4-daemon.log); cargo test -p orgasmic-cli --bin orgasmic -- daemon_lifecycle = 22 passed 0 failed (/private/tmp/claude-501/-Users-aspirational-Documents-code-tools-orgasmic/25219e20-4403-4221-9eb2-cd596e21be27/scratchpad/gates-8DWJP.1.4-cli.log); clippy -p orgasmic-daemon -p orgasmic-cli --all-targets -D warnings rc=0; cargo fmt --all --check rc=0. Worker gates in its worktree matched (32/22/clippy/fmt; logs under /tmp/orgasmic-8dwjp14-logs/). Manager pre-check of production hunks: abort closure runs under run_barrier at both sites mirroring park; pending_salvage carried on Synced/Conflict + PENDING_SALVAGE_REF event extra; salvage-failure degrade pinned with a ponytail comment.
