#+title: orgasmic task TASK-TFXR2
#+orgasmic_version: 2

* IN_REVIEW TASK-TFXR2 Port remaining old-layout test fixtures: daemon api concurrency/watcher + cli manager :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-TFXR2
:PRIORITY: P1
:END:

** Description
Follow-up to TASK-IXPD4 (which covered index.rs only). Remaining red tests on the e01mc-integration branch still use the retired aggregate layout or pre-tx-split expectations:
- `orgasmic-daemon --lib`: api::tests::{concurrent_task_creates_preserve_every_heading, concurrent_task_property_updates_preserve_every_write, decision_parent_contract_and_reparent_tx_are_enforced, parse_errors_route_attributes_project_and_reindex_clears_it, project_task_mutation_is_immediately_visible_with_project_tx}; watcher::tests::{changed_board_path_moves_watch_to_the_new_project_root, fs_event_triggers_project_refresh_after_atomic_task_file_rename, fs_event_triggers_project_refresh_after_cli_task_file_rename, temp_only_atomic_rewrite_event_refreshes_project}
- `orgasmic-cli`: manager::tests::closes_architector_lifecycle_to_done
Two port patterns: (a) old aggregate fixture paths (`.orgasmic/tasks/backlog.org` etc.) -> node dirs `.orgasmic/tasks/<ID>/node.org` with v2 headers (recipe: index.rs tests just ported by IXPD4); (b) tx-count/tx-content assertions on node-scoped events -> journal assertions per the AP971.5 routing table (task.created = journal entry #1; see the ap971_ tests and the artifact api tests fixed at dd74ea50). Preserve intent; no assertion weakening, no test deletion.
** Acceptance
- [ ] `cargo test -p orgasmic-daemon --lib` fully green
- [ ] `cargo test -p orgasmic-cli` fully green
- [ ] no test deleted; routing-table semantics (journal vs tx) preserved per AP971.5
** Evidence
- Integrated on main =62dc3e8a= inside the chain merge =543782d7= (2026-08-26); shipped in runtime 0.0.23 stable. whole-chain review tx-20260901-orgasmic-1c6d2115 (claude-opus-5 high, 2026-09-01): APPROVE WITH FOLLOW-UPS; follow-ups filed as fix tasks tagged chain-review.

