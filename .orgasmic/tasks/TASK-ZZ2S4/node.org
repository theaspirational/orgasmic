#+title: orgasmic task TASK-ZZ2S4
#+orgasmic_version: 2

* IN_REVIEW TASK-ZZ2S4 Re-mint 3 hand-created task IDs with non-Crockford letters (CLM6W, IXPD4, LBRX7)
:PROPERTIES:
:ID: TASK-ZZ2S4
:PRIORITY: P3
:END:

TASK-CLM6W, TASK-IXPD4, TASK-LBRX7 (added in ledger commit 22b951f2, 2026-08-27) contain I/L, which the Crockford stem alphabet forbids — they were hand-minted, not daemon-minted. They are the whole remaining parse_errors=3 ("malformed identity id"). Referenced from TASK-AP971, TASK-AS0FS, TASK-D2DM3, TASK-MSYN4, TASK-TFXR2. Fix = re-mint each through the daemon and rewrite the references; needs a daemon-side rename path or a careful one-off migration, NOT hand edits.
** Status 2026-09-06
Source/ledger triage at main 0bf80eb0. BLOCKED on a missing supported migration operation. Checked installed CLI and current source: id mint only generates IDs; node/task ID is immutable; task deletion is deliberately unsupported; project migrate handles aggregate conversion/branch extraction; doctor --fix-id-collisions only handles duplicates and cannot move node directories/journals. All three malformed nodes are DONE and have historical journals; task get can read them although the listing index excludes them. Additional live structured references include TASK-8DWJP and dec_EWY0K, beyond the original list. Repair must preserve journals and immutable event history, atomically remap node identity/path and current references, and record an old-to-new mapping with crash-safe retry. No IDs minted, no duplicate replacement tasks, no ledger files hand-edited. This is a required daemon migration feature, not routine task metadata cleanup.
2026-09-06 implementation plan refinement: an explicit offline CLI maintenance migration fits the original task better than a general rename API. Reuse project_migrate Git/checkpoint patterns; hold the actual daemon instance lock, require clean ledger and no active claims/runs, build and validate a full source-to-target mapping before mutations, preserve existing journal/event bytes, rewrite current references only, retain durable old/new mapping. A persisted pending marker must prevent daemon startup after an interrupted apply until safe resume. Source implementation delegated to a native Codex subagent at high effort; this avoids provider quota probes. Independent review will use a separate reviewer; any same-family exception will be recorded explicitly. Live repair only after focused tests and reviewed diff.
Review selection: separate native Codex reviewer with explicit high effort. Same-family exception: use the available isolated native reviewer without a billed external provider invocation or quota probe. Implementation ran in a separate native subagent with inherited session effort; the earlier high-effort wording was intent, not verified dispatch metadata. No orgasmic dispatch generation was opened. Reviewer will inspect committed diff and focused evidence; parent owns deployment and live repair.
** Evidence
Committed candidate 7f6deeaf passed CLI fixtures (3), daemon startup-fence test (1), and a read-only live dry-run finding exactly the three malformed IDs. Independent review identified an additional crash window: SIGKILL before atomic-write persist can leave an unrecorded temporary file that blocks supported resume. Candidate not approved or deployed; implementer addressing owned staging-file recovery and a discriminating regression. Artifact logs: /Users/aspirational/.codex/artifacts/orgasmic-id-repair-20260906/.
