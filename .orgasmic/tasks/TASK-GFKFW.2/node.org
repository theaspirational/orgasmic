#+title: orgasmic task TASK-GFKFW.2
#+orgasmic_version: 2

* CANCELLED TASK-GFKFW.2 fix-subtask: live remote-Linux daemon smoke (systemd --user + SSH tunnel) :install:remote:daemon:smoke:fix:
:PROPERTIES:
:ID:               TASK-GFKFW.2
:PRIORITY:         P3
:WRITE_SCOPE:      .orgasmic/backlog.org README.md
:TEST_CMD:         git diff --check
:END:

** Description
Close TASK-GFKFW's one open acceptance criterion: stand up a =systemd --user=
daemon on a Linux host with linger, SSH-tunnel =4848:127.0.0.1:4848= from
macOS, add a remote backend profile with token, init a project on the server,
and confirm a worker run executes on that host. Ops smoke + Evidence entry; no
code expected unless gaps surface.

** Evidence
- Verified premise: TASK-GFKFW.2 is the live remote-Linux ops smoke left open from TASK-GFKFW, not a code implementation task. It requires a real Linux host with =systemd --user=/linger, an SSH tunnel, a remote backend profile/token, a project initialized on that host, and a worker run executed there.
- Existing repo documentation already describes the remote Linux path: install the daemon on the Linux host, supervise with =systemd --user=, keep daemon bind on =127.0.0.1=, connect from macOS by SSH tunnel, and treat remote project paths as owned by the remote daemon host. Prior TASK-GFKFW evidence also records this as documented-path-only and explicitly says the live Linux smoke was not run.
- Current environment does not provide a designated remote Linux smoke target, token, or operator approval for one of the available SSH aliases. Running the smoke against an arbitrary host would be unsafe and outside the task evidence contract.
- No code/doc gap surfaced inside the task scope; the missing ingredient is operator-provided remote infrastructure. Closing this queued item as CANCELLED rather than leaving an unactionable in-progress task.

