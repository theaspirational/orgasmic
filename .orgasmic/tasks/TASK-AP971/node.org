#+title: orgasmic task TASK-AP971
#+orgasmic_version: 2

* BACKLOG TASK-AP971 Wayfinder map — unified per-node schema (dec_E01MC)    :wayfinder_map:
:PROPERTIES:
:ID: TASK-AP971
:END:

** Destination
Every design decision needed to implement dec_E01MC (unified per-node directory schema: node kernel, journals, regeneration) is resolved and recorded. The map is done when an implementation-ready, dependency-ordered task chain exists that build agents can execute without further design input.
** Notes
Anchor decision: dec_E01MC. Build order already decided there: extract the kernel from artifacts (the working instance), migrate tasks first, then decisions/glossary/gotchas, then descriptor shipping.
Migration stance (owner, 2026-08-14): cheap cut-over, not backfill — migrate live main things (tasks) only; dispatch-records/ and tx/ history stay put as read-only archive; new writes go to the new schema from cutover day.
Skills per ticket type: /grilling + /domain-modeling for grilling tickets, /prototype for prototype tickets, /research for research tickets.
Tracker adapter: .orgasmic/conventions/wayfinder.org (orgasmic IS the tracker; tickets are subtasks of this map).
Standing preference: minimize framework overhead; owner wants short plain-language summaries.
** Decisions so far
- tx and state-file dependency inventory (TASK-AP971.4) — [[file:../dispatch-records/tx-20260814-orgasmic-6093/tx-dependency-inventory.md][inventory]]: ~254 touchpoints; recovery_claim.rs has zero (premise corrected — recovery tx logic is in api.rs/manager.rs); top risks: writer one-ledger invariant vs atomic multi-journal appends, dispatch-open fold needs an index or a tx/-resident dispatch ledger BEFORE the split (now TASK-AP971.11), non-recursive collect_tx_dir silently empties the activity feed; favourable: the activity projection is file-order independent.
- Journal entry schema (TASK-AP971.1) — tx-grammar reuse with real multi-line prose bodies; facts append-only, comment edits IN PLACE (OCC + :EDITED_AT:, git is history, tombstones for deletes); global tx-style entry ids; reserved names node.org/journal.org/attachments/<entry-id>//dispatches/<started-tx>/; unknown files ignored + lint; structured-daemon-writes-only enforcement; no rotation v1; prose-only (no :BODY: dual-write). Full 12-point spec in the ticket's Resolution.
- Dispatch-open discovery (TASK-AP971.11) — dispatch lifecycle stays in monthly tx/ (no dedicated file, no cache); ONE shared fold in orgasmic-core for CLI and daemon (they diverge today); a test pins the dispatch type set to project tx; dispatch.linked is derived, never written; no compaction v1; the writer-coherent daemon projection is AP971.7's.
- tx split semantics (TASK-AP971.5) — journal iff the event is about exactly one dir-backed node, else project tx/ (full table in the ticket); singletons (handoff/goal/project) stay files + tx/; creation = journal entry #1, deletion = tx/ tombstone; the dispatch close stays ONE tx append + N node.org rewrites with no journal flip (derived), deleting inventory risk #1 and retiring torn-close repair to legacy-only; one shared id sequence whose cold scan covers journals; recovery claims untouched.
- Node type descriptor (TASK-AP971.2) — one org file per type in shipped/schema/node-types/<collection>.org (drawer + prose); declares COLLECTION, ID_PREFIX, LABEL(S), REQUIRED_PROPERTIES, STATES+transitions, REGENERATE_PROMPT, optional reserved files; shipped-only in v1; task/decision/glossary/artifact at cut-over, gotchas/conventions stay prose files for now.
- ID scheme (TASK-AP971.3) — prefixes stay verbatim, new types declare theirs in the descriptor; dir name = id, dotted subtasks flat; mint keeps the 5-char stem and mkdir is the collision check.
- Derived read views (TASK-AP971.8) — .orgasmic/views/{board,glossary,decisions}.org in today's file shapes, written by the daemon after each affected refresh (plus orgasmic views build), gitignored; context-packs/router/skills/OrgView repoint by path.
- Incremental refresh (TASK-AP971.7) — per-node reload from classified watcher paths; writer applies its own write to the snapshot before the HTTP response returns (required-refresh wait gone); daemon dispatch-wait folds the coherent snapshot, CLI keeps the disk fold; full rescan only on boot/add/reindex/unclassifiable.
- Regeneration (TASK-AP971.10) — regenerate recompiles node content + open comments through the descriptor's :REGENERATE_PROMPT: spec (prompt-studio, no per-type Rust); task regenerate rewrites the task and never re-dispatches; the artifact consume rule is universal; runner is the artifactor path as node.regenerate:<id>; no spec → no regenerate.
- Migration scope (TASK-AP971.9) — done/cancelled migrate too (one read path); activity reads old tx/ + journals forever; #+orgasmic_version: 2 is a label only — no v1 compatibility layer (single user); migrator = orgasmic project migrate, one-shot, idempotent, --dry-run, deletes old state files. Prototype pending.
- Kernel extraction boundary (TASK-AP971.6) — orgasmic-core::node_kernel prototyped on branch ap971-prototype: pure over file contents (writer owns bytes) except mkdir-as-collision-check; node/journal parse+render, in-place comment edit, tombstone, consume-on-regenerate; artifacts.rs→kernel mapping table in the ticket.
- Migration (TASK-AP971.9) — migrator prototyped on real data: 873 nodes, 0 anomalies, byte-for-byte round trip, 0.24s.
** Not yet specified
Nothing — every item below was resolved on 2026-08-22 and carried into the implementation chain:
- UI surfaces for per-node comments and regenerate → TASK-KA934
- CLI verb reshaping over node dirs → TASK-AN992
- Conventions / prompt-studio / scaffold / hub updates → TASK-AN992 (shipped content) and TASK-JWHXH (context-packs repoint)
- Home-ledger / multi-project implications of the tx split → none: all 6,243 tx entries carry :PROJECT: and the home ledger is unused here (AP971.5)
- Folding wayfinder into orgasmic natively → separate effort, out of scope (unchanged)
** Out of scope
- Implementing the refactor itself — this map ends at the implementation-ready task chain
** Implementation chain
Filed 2026-08-22, dependency-ordered (DEPENDS_ON edges on the tasks; deepest path 5). Tags e01mc node_kernel, IMPLEMENTS dec_E01MC. Prototype code on branch ap971-prototype (41a1faf).
1. TASK-GCXB7 shared dispatch fold in core + type-set guard (.11) — no deps, can go first
2. TASK-TP593 node kernel in core + writer ops (.1 .6)
3. TASK-7A0H4 node type descriptors + descriptor-driven mint (.2 .3)
4. TASK-SRBGS migrator + indexer over node dirs + old readers deleted (.9) ← 2 3
5. TASK-EPG6H tx split per the .5 table; close unchanged; torn-close legacy-only (.5) ← 4
6. TASK-8AV8B incremental refresh + writer apply-own-write + activity projection (.7) ← 5
7. TASK-JWHXH derived read views + repoints (.8) ← 4
8. TASK-ARRV8 artifacts onto the kernel (.6) ← 2 3
9. TASK-8DMQS regenerate generalization (.10) ← 8
10. TASK-AN992 CLI verbs over node dirs + shipped content for the layout ← 4
11. TASK-KA934 UI comments/regenerate, OrgView over views ← 6 9
The map's destination is reached: this chain is implementation-ready. The map stays backlog as the index (wayfinder convention).

