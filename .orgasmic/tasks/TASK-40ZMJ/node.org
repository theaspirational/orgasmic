#+title: orgasmic task TASK-40ZMJ
#+orgasmic_version: 2

* DONE TASK-40ZMJ Provider/quota health invisible until a worker dies: manager drivers --health probe + dispatch pre-flight refusing provider_quota/provider_auth :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-40ZMJ
:PRIORITY: P2
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 11. Codex locked out five days; glm-5.2/5.3 share one z.ai quota so a 429 on one kills the other; `claude` harness reports "no catalog available" which reads as cannot-run but means cannot-enumerate.
** Expected
- `orgasmic manager drivers --health`: per configured provider auth ok / quota ok / retry-after.
- dispatch pre-flight refuses with `provider_quota: codex locked until …` instead of spawning a worker that dies in two seconds.
- Empty catalog message: catalog unavailable; --model passed through unvalidated.
Related: item 1 (reason classification) supplies the provider_quota class.
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit 85fde20d on sprint/PREFLIGHT, merged onto local sprint-20260902 as 0a4f348e. Built: 'orgasmic manager drivers --health' [--json] runs the same adapter preflight the dispatch path uses for every SUPPORTED harness and prints '<harness> auth=<ok|missing|unknown (<why>)> quota=unknown (no probe)' (smoke on this Mac: claude/codex/cursor-agent auth=ok, hermes/custom unknown). SKIPPED with reason: quota lockout memory + 'provider_quota: locked until' refusal + --force-preflight — no terminal-reason classification or 429/quota signal exists anywhere in the drivers (only codex's passive account.rate-limits.updated event); needs letter item 1 (TASK-XQCNA) first. Empty-catalog message already landed via XC9N4. TASK STAYS OPEN for the quota part. Gates as on AP298.
- 2026-09-02 dispatch (stdio/codex-chat, gpt-5.6-sol, effort high, started_tx tx-20260902-orgasmic-5d2b619d): QUOTA HALF DONE. Worker commit 1423d4a2 + manager fix f57ba638, merged as 29e61d71, close tx tx-20260902-orgasmic-d8776b3e. New crates/orgasmic-daemon/src/provider_quota.rs keeps a machine-local lockout memory at $ORGASMIC_HOME/state/provider-lockouts.json, populated ONLY from a classified provider_quota terminal reason carrying real evidence (a parseable Retry-After, or one unambiguous passive account.rate-limits.updated deadline). A dispatch into an active lockout is refused before acquire as "provider_quota: <provider> locked until <RFC3339>"; --force-preflight overrides exactly that refusal and records FORCE_PREFLIGHT=true on manager.dispatch_started. "drivers --health" overlays a remembered lockout.
- HONESTY HELD, which was the point of the task: a termination carrying neither signal is deliberately NOT cached, and a provider with no quota signal still prints "quota=unknown (no probe)" rather than inventing an expiry. Verified by a synthesised probe: codex printed "quota=locked until 2099-01-01T00:00:00Z" while every provider without memory stayed unknown.
- UNBLOCKED BY THIS SPRINT: the earlier skip said this needed "letter item 1 (TASK-XQCNA)" first. XQCNA shipped today (merge e796cb72) with the classified ExitReason, and this work builds on it rather than inventing a parallel mechanism.
- DEFECT FOUND BY THE MANAGER GATE, not the worker (fixed as f57ba638): the new guard returned ApiError::internal on a lockout-store READ failure, which refused EVERY dispatch for EVERY provider until someone deleted the file. That contradicted the store's own missing-file path, which already reads as "no lockout" - an unreadable file is the same epistemic state. It now logs loudly and proceeds. Worst case is one dispatch that dies on quota (visible, recoverable) instead of an unrecoverable machine-wide dispatch outage from one corrupt cache byte.
- Manager gate after the fix: clippy on 4 crates rc=0; daemon provider_quota + quota_ 4 passed; dispatch_quota_lockout 1 passed (real HTTP refusal creates no session, forced dispatch succeeds, tx carries the override); dispatch_preflight 1 passed (existing auth refusal intact); cli health_listing_ + dispatch_request_carries_force_preflight 3 passed; cli_parity 9; core exit_reason 3; fmt rc=0.

