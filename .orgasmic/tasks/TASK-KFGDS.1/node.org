#+title: orgasmic task TASK-KFGDS.1
#+orgasmic_version: 2

* DONE TASK-KFGDS.1 reviewer-findings fix-subtask: wire CLI dispatch babysitter spawn + allowlist threading + strict CSV parse :drivers:acp:stdio:codex:sandbox:babysitter:bug:fix:
:PROPERTIES:
:ID:               TASK-KFGDS.1
:PRIORITY:         P2
:WORKER:           implementer-cursor
:END:

** Evidence
- Salvaged from a cursor implementer dispatch (tx-20260526-org-0359) that implemented all 7 files cleanly (+381/-30, 164 LOC of new dispatch_endpoint integration test) but spun ~14min on a phantom test failure (cursor mis-read terminal output; tests actually pass). Manager-killed at 27min elapsed, ran gates manager-side: cargo build/test/clippy/fmt/diff-check all green (281 workspace tests).
- Salvage-commit =edba5af= (manager-salvage author). Merged no-ff as =37037fb=. =implementer.done= tx-20260526-org-0362; =:FIX_SUBTASK: t= auto-flipped IMPLEMENT → DONE.
- F-1 (CLI babysitter spawn): =spawn_worker_run= reads =worker.babysitter_worker=, loads that worker, builds =BabysitterAutoSpawn=, threads =Some(...)= into =AcquireRequest.babysitter=. New =dispatch_endpoint= integration test asserts =<run_id>.babysitter.jsonl= exists after =POST /dispatch=.
- F-2 (allowlist threading): =SandboxAllowlist::resolve(task, worker)= now flows from worker spec + task heading override into =DriverConfig.sandbox_permissions= via the dispatch spawn path. Drivers pick it up via =allowlist_from_driver_config=. Precedence task > worker > default verified.
- F-3 (strict CSV): =allowlist_from_driver_config= now returns =Result= instead of silently falling back to trust-all on malformed CSV. Errors surface at acquire/dispatch boundary.

