#+title: orgasmic task TASK-ZKZBF.1
#+orgasmic_version: 2

* DONE TASK-ZKZBF.1 Close the ZKZBF review findings: ID-immutability guard across all drawer writes, tx record --extra case rule, message polish :daemon:cli:
:PROPERTIES:
:ID: TASK-ZKZBF.1
:PRIORITY: P2
:WRITE_SCOPE: crates/orgasmic-daemon/src/api.rs,crates/orgasmic-core/src/tx.rs,crates/orgasmic-cli/src/manager.rs,crates/orgasmic-cli/tests
:FIX_SUBTASK: t
:END:

** Description
Fix round for the TASK-ZKZBF review (report promoted at .orgasmic/dispatch-records/tx-20260818-orgasmic-6148/last.txt; findings below verbatim-summarized). No regression to undo — build forward on merged =136418f=.

*MEDIUM-1 (correctness):* =node prop set= writes =:ID:= on every non-task layer — the =other =>= arm runs only =validate_drawer_property_key_case= (no refusal table), =ID= is uppercase so it passes, and the upsert re-keys the node (=:15637=; =find_by_id= matches =property("ID")=, org.rs:284), dangling every reference to the old id, no =--force=. Also =node_layer_schema_property_keys= (:16412) LISTS ="ID"= for Project and Goal/Handoff, so refusal text advertises it. Same divergence: =create_graph_heading= refuses =ID= (:14652) but =mutate_graph_heading= (:14837) got only the case check. FIX: hoist an ID-is-derived refusal into the shared check called from all three sites; drop ="ID"= from the Project/Goal/Handoff key lists. Open question 1 answered by manager: NO id-repair intent exists anywhere — task layer already refuses =ID= as immutable; extend that rule, do not route a repair verb.

*MEDIUM-2 (correctness, scope-adjacent):* =tx record --extra= writes the same tx ledger =dispatch-close --property= now guards, through =validate_property_key= (tx.rs:292) which checks empty/reserved/colon/whitespace/control but NOT case; =extra()= (manager.rs:9783) is byte-exact, so a miscased =--extra kind=implementer= makes a dispatch INVISIBLE to =dispatch-close= (dispatch_record_from_entry :9562 returns None). The task's own test stages a dispatch with the uppercase form (:519-533). FIX: apply the decided ZKZBF rule (open key vocabulary, closed case rule, refused by name at parse) to =tx record --extra=.

*LOW-1:* =PropertyNotFound= 400 leaks the absolute on-disk path (org.rs:176 Display via =OrgRewriter::new(&file, ..)=) and omits what the drawer has — contradicts org_parse_bad_request (:1658) and tx.rs:226 "Never a path". FIX: mirror =unknown_section_error= (:15169) — name the node, list the drawer's actual keys, no path.

*LOW-2:* unset refusals reuse task_property_key_refusal phrasing about a =--property KEY=…= flag =node prop unset= does not have (:15505). FIX: unset-shaped advice (same shape the set-path already has).

*LOW-3:* =glossary create --property id=x= takes two round trips (case check says "Use =ID=", then =ID= is hard-refused). FIX: check the =ID= guard case-insensitively or order it before the case check. Also remove dead =NodeLayer::Task | NodeLayer::Goal => "Task"= arm at :15521 (Task handled above, Goal rejected at :15479).

** Acceptance
- =node prop set term_/dec_/project/handoff ID <anything>= refused by name as identity-immutable on ALL layers; =node prop unset <id> ID= likewise; =mutate_graph_heading= refuses =ID= like create; ="ID"= gone from the advertised key lists.
- =tx record --extra kind=x= refused by name naming =KIND=; production-path test staging a dispatch with a miscased extra proves =dispatch-close= still finds the uppercase-staged record while the miscased form is refused.
- PropertyNotFound body carries no filesystem path and names the drawer's actual keys; unset advice has no =--property= phrasing; =id=x= on glossary create is one refusal naming =ID= immutability.
- Existing 12 ZKZBF+HXSW0 tests stay green (adapt where the contract legitimately changed); fmt, clippy -D, focused =run-tests.sh= gate green; extend the injection artifact or add a sibling for the ID guard.
- Independent review pass.

