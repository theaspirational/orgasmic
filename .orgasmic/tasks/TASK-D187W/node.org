#+title: orgasmic task TASK-D187W
#+orgasmic_version: 2

* DONE TASK-D187W Plugins P2: reconcile registry reload and descriptor authority
:PROPERTIES:
:ID: TASK-D187W
:IMPLEMENTS: dec_882B4
:PRIORITY: P2
:END:

** Description
Follow up on the P0 registry review after the UI registry slice. P0 commit: 453b8d9ba4f6689bef0ab4ef9c9bb72342a470b7 on plugins/p0-registry. These items are deliberately not fixed in P0.
** Acceptance Criteria
- [X] Registry reload and CLI/daemon descriptor agreement landed in P2 (83866bee); focused plugin/node route tests cover reconciliation and ownership.
- [X] Compiled descriptors are pinned in full; node_registry::tests::compiled_descriptor_is_pinned_in_full covers rejected overrides.
- [X] Audited node_id_class_by_prefix at main 341a03c5: only its definition and public re-export remain; no production caller treats None as not a node. Remaining UI inferNodeKind labels are separate deferred polish.
- [X] Split out 2026-09-11 to TASK-H85X7: emit an Org TODO separator for done states after defining descriptor terminal-state semantics; do not invent a terminal state from a reversible transition.
- [X] Retain the P0 member policy explicitly: project members can read project/handoff context, editors can delete decisions/glossary, viewers cannot delete, and foreign-project access is denied. HTTP coverage in node_registry_routes passed on 2026-09-08; committed as 8fee5fd9.
** Notes
P0 remains branch-only and is not deployed. This is one consolidated P2 task, not authorization to pull loader, sidecar, attachment, or chat work into P0/P1.
P1 review follow-ups (deferred, do not fold into the three P1 fixes): generic peeks still load the document twice; make NodeDocEditor the only loader for generic kinds. Replace remaining inferNodeKind prefix guessing and ArtifactsView subject labels with graph-provided titles. Revisit the residual ViewName union and old-kind-keyed descriptor record when compiled decision/glossary views are extracted. Show a toast when node-types fails instead of silently removing generic navigation. P2 is authorized in two halves: reloadable registry plus per-ledger activation and packaging CLI first, then plugin run with a scoped principal. Sidecars, UI bundles, attachments, and chat remain out of scope.
P2 scope boundary: supports core.nodes@1 and nodes.read/nodes.write. Commands run with full OS-user filesystem access, not a sandbox; daemon credentials are scoped and expire after 24 hours or on revoke/disable/removal/manifest change. Removal moves plugin code to recoverable user/plugins-removed storage and disables registered ledgers. Registry reconciliation checks manifest/config metadata once per second, and CLI activation/removal reconcile synchronously; no per-request plugin tree walk or file hashing. P1 double generic fetch, remaining inferNodeKind labels and unions, and metadata-error toast remain deferred, as do the other original follow-ups. Sidecars, UI bundles, attachments and chat were not added.
Pre-release reconciliation 2026-09-08: P0/P1/P2/P3/Slice A/Slice B are merged on main through 341a03c5; the older branch-only and uncommitted statements above are historical, not current status. UI bundles, links and external attachments are implemented; persistent sidecars and scoped chat remain deferred. Registry reload, full compiled pinning and the retired-prefix caller audit are checked off. Keep this task in_progress for the explicitly listed policy coverage and deferred Org done-state formatting. P1 metadata-failure toast, duplicate generic fetch and remaining inferNodeKind labels remain polish, not claimed complete.
** Evidence
P1 committed as 669d7993 after three review fixes: registry-prefix compiled peeks, explicit node collection, server-side graph layer filter. Typecheck, 382 UI tests and node_registry_routes passed before commit. P2 is uncommitted on plugins/p0-registry: cached reloadable per-ledger registries; plugin.org manifests; daemon-owned plugins.json activation and retained ownership; add/check/enable/disable/remove/list/scaffold CLI; revocable plugin-run principal with caller-capability intersection and collection write ownership. Compiled descriptors are pinned in full. Focused green checks: core node_registry/plugin tests, daemon cached-snapshot test, node_registry_routes and plugin_registry_routes HTTP integrations, CLI node_registry_cli/plugin_cli/cli_parity including child CLI wrong-collection refusal and debug_assertions-off leg, existing artifact-context/regenerate tests, UI typecheck and 383 UI tests. Final clippy has only the pre-existing transcript_stream warning. Logs: /tmp/orgasmic-p2-final-routes.log, /tmp/orgasmic-p2-cli-final.log, /tmp/orgasmic-p2-ui.log, /tmp/orgasmic-p2-final-clippy.log. Full CLI suite not rerun; previously documented dispatch failures remain outside scope. No merge, push, deployment or production daemon restart.
2026-09-08 pre-release check at main 341a03c5: serial node_registry_routes (1), plugin_registry_routes (2), node_services_routes (1) passed; the 2 GiB gate remains ignored in this invocation. UI typecheck and 13 focused tests passed across the editor, full-document edit contract, plugin runtime/events, player, backlinks and upload transport. Logs: /tmp/orgasmic-prerelease-routes.log, /tmp/orgasmic-prerelease-ui.log, /tmp/orgasmic-prerelease-node-contract.log. No candidate, publication or installed-build/device playback proof yet.
