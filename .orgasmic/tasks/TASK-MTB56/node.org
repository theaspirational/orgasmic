#+title: orgasmic task TASK-MTB56
#+orgasmic_version: 2

* DONE TASK-MTB56 Agent-ergonomics batch: guard word-boundaries, compact mutations, porcelain lists, goal-handoff sync :cli:daemon:hardening:
:PROPERTIES:
:ID: TASK-MTB56
:PRIORITY: medium
:END:

** Description
Batch of agent-ergonomics fixes from the 2026-07-06 battle-test (F3, F11, F12, F13, F14) — none blocking, all context-economy or dead-end costs on hot paths:

- F3 glossary marker guard false positives: =reject_glossary_implementation_detail= (crates/orgasmic-daemon/src/api.rs:7890) substring-matches "struct " so ordinary English ("construct holding…") is rejected. Match on word boundaries/tokens; add =--allow-marker= override. A glossary about a language legitimately contains construct/class/function.
- F12 mutation output volume: =task update= returns the full TaskDetail (api.rs:8804) ~1.5 KB per state flip; =node body set= echoes the whole node. Default to the compact =GraphMutationResponse=-style shape ({id, changed field → value, tx_id}); =--json= for the full node.
- F13 silent-empty listing: =tasks --project X= printed zero stdout in the battle-test despite 9+ tasks. Daemon returns ALL states unfiltered (api.rs:1254) and empty prints =[]= — so the observed zero-output is likely an unreproduced error path, not a filter. Repro + fix; listings never empty-and-silent (print "0 tasks" + hint).
- F14 machine-consumable lists: =--ids=/=--porcelain= mode (one id per line, stable) across list commands; related-term ids in =glossary list= currently break naive grep counting.
- F11 stale goal linkage: =goal set= mints the new goal but =handoff-current.GOAL_ID= keeps the old value; sync it on =goal set= (or derive from goal.org).

** Acceptance Criteria
- [ ] "construct"/"reconstruction" pass the glossary guard; real markers still rejected; override flag works.
- [ ] =task update= default output is compact; =--json= restores full node.
- [ ] F13 root-caused with a regression test; no list command can produce empty-and-silent output.
- [ ] =--ids= mode on task/glossary/decision/architecture list verbs.
- [ ] =goal set= leaves handoff GOAL_ID consistent.

