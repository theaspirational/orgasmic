#+title: orgasmic task TASK-M47E5.1.1.1.1
#+orgasmic_version: 2

* DONE TASK-M47E5.1.1.1.1 Cover the daemon's last_path to artifact-dir derivation with a mutation-proof test
:PROPERTIES:
:ID: TASK-M47E5.1.1.1.1
:PRIORITY: P1
:FIX_SUBTASK: t
:WRITE_SCOPE: crates/orgasmic-daemon/
:END:

** Description

Fix round for the BLOCK SHIP on TASK-M47E5.1.1.1's review. *Test-only: the
production fix is confirmed correct by that review.* The finding is on that task
under "Review round 1 — REJECT (test-only; production confirmed correct)". Read
it there.

In one line: the daemon test computes =last_path.parent()= itself and asserts
against =last_path.parent()=, so the production derivation at =api.rs:4770= is
covered by nothing.

** The mutation that must fail, and today does not

Replace =req.last_path.and_then(FsPath::parent)= at =api.rs:4770= with a
GRANDPARENT derivation. That yields the DISPATCH ROOT, which restores the
cross-stem finalize grant this whole chain exists to close — and every test in
the workspace stays green.

** Why this is the third round to fail the same way

Round 3's daemon test proved only that an already-correct value was serialized.
Round 4's brief demanded "a test that derives the config from a =last_path=,
rather than passing an expected directory straight to the config helper". Round 4
moved the =.parent()= call into the test body and passed the result straight to
the helper — the criterion's letter, not its substance.

So the criterion is being restated as *mutation-shaped* rather than
value-shaped, because naming the value a test should assert has now failed twice:

*The regression test MUST FAIL if the production derivation is changed to any of:
the grandparent, the =last_path= file itself, or =None=.*

State in the report that you performed those three mutations and observed the
test go red for each. That is the acceptance, not a suggested exercise.

** Two routes; either is acceptable

1. *Exercise =spawn_worker_run== with a capture or stub driver and assert the
   =dispatch_artifact_dir= in the config it actually builds. Strongest, because
   it covers the real call edge.
2. *Extract the derivation into a named helper* that =spawn_worker_run= calls and
   the test exercises through the same config-construction path. Weaker — a
   regression that stops calling the helper still passes — so if you choose this,
   say so plainly and describe what it does not cover.

Prefer 1 if it is reachable without disproportionate scaffolding. Do not build a
large harness for a small property; if 1 needs one, take 2 and be honest.

** Acceptance

- The three mutations above each turn the test red, verified by running them.
- No production behaviour change. This round is not licensed to alter the grant,
  the stem validation, or the derivation itself — only to cover them.
- Existing tests and all four proofs in this chain stay green:
  =TASK-M47E5.1=, =TASK-M47E5.1.1=, =TASK-M47E5.1.1.1=.

** Not in scope

Everything else. The review confirmed the production fix, the no-widening
comparison against =103ed7e=, the =resolve_policy_path= soundness, the =Option=
propagation, the CLI refusal ordering, and the transcript-probe fix. Do not
revisit them.
** Landed and verified by the manager
Merged =8e3b317= (worker =3732f37=, base =e0b31f3=, +63/-24 in one file).
*Test-only, confirmed by the manager rather than taken on report:* every changed
hunk is at line 26026+, inside a test module beginning at =api.rs:18052=, and
=req.last_path.and_then(FsPath::parent)= is intact at =:4770=.

=== Route 1, the real edge

The implementer exercised =spawn_worker_run= with the in-process stub driver and
read =run_meta.driver_config.dispatch_artifact_dir= as actually persisted for
launch — not the helper in isolation.

The detail that matters, and that the two previous rounds got backwards: the test
declares =expected_artifact_dir= FIRST and derives =last_path= FROM it. The
expectation is never computed by the path under test. That is what makes the
assertion non-tautological.

Declared limitation, accepted: the test stops at the stub/supervisor boundary, so
it does not launch a real external adapter or re-test the downstream finalize
predicate. The chain's own proofs cover that predicate. An accurate statement of
what a test does not cover is worth more than a stronger-sounding claim, which is
the lesson of this whole chain.

=== The manager verified the acceptance criterion independently

The acceptance was mutation-shaped, so it was checked by mutation rather than by
reading the report. The dangerous one, applied to =api.rs:4770= by hand:

: -        req.last_path.and_then(FsPath::parent),
: +        req.last_path.and_then(FsPath::parent).and_then(FsPath::parent),

Result: *exit 101*, and the assertion reads

: assertion `left == right` failed: spawn_worker_run must derive the driver artifact directory from last_path
:   left: ".../project/.orgasmic/tmp/dispatch"
:  right: ".../project/.orgasmic/tmp/dispatch/task-attempt-7"

*Left is the dispatch root* — precisely the value that restores the cross-stem
finalize grant. The test catches it. Tree reverted, =git status= clean.

The implementer reported all three required mutations red with their left/right
values; the manager reproduced the one that matters.

=== A false green the manager caught in its own gate, worth recording

The first attempt to run the new test returned *exit 0 with "0 passed; 710
filtered out"* — =--exact= had been given a bare function name instead of
=api::tests::<name>=. Exit 0, nothing run. Re-run with the module path: 1 passed.

That is the same failure shape this entire chain has been about: a check that
succeeds while proving nothing. It is recorded here because it happened on the
MANAGER's side of the gate, and reading "exit 0" without reading "0 passed" would
have entered a fabricated pass into the ledger.

=== Gates on the merged tree =8e3b317=

- Targeted: =api::tests::dispatch_spawn_derives_the_authoritative_artifact_dir_from_last_path=
  — 1 passed (after the false green above was corrected).
- Grandparent mutation — RED, as required, with the dispatch root as the left value.
- =cargo clippy --workspace --all-targets -- -D warnings= — exit 0.
- =cargo fmt --all --check= — exit 0.
- =orgasmic verify TASK-M47E5.1=, =TASK-M47E5.1.1=, =TASK-M47E5.1.1.1= — all PASS.
- =scripts/run-tests.sh -p orgasmic-daemon= — RED, 18 failures, *18/18
  isolation-green*, and NOT attributable: this was the third run of a controlled
  series. Three runs on effectively identical production code gave three
  different failure sets (19/19/18 names, 14 common to all three). The new test
  does NOT appear among the failures. Full series on TASK-STWVB.
** Review round 1 — APPROVE, no findings
Reviewer =reviewer-codex-tmux= (tmux/codex/gpt-5.6-sol/high) on merge =8e3b317=
vs base =e0b31f3=. *APPROVE, no findings*, and it states plainly that the
TASK-M47E5.1 chain is closed.

=== It did the thing the acceptance actually required

The brief granted a narrow, explicit exception to the read-only contract: the
acceptance criterion was a MUTATION, and a mutation cannot be checked read-only.
The reviewer used it correctly and completely — *all three mutations performed
independently at =api.rs:4770=*, each with the observed left/right values:

- grandparent -> left = the project dispatch ROOT, right = the attempt dir
- =req.last_path= itself -> left = the =-last.txt= file, right = the attempt dir
- =None= -> left = =Null=, right = the attempt dir

Then restored the tree and re-ran clean: =1 passed=, =git status --short= and
=git diff --check= both empty. That is the whole acceptance, discharged by
someone other than the author.

=== It answered the stub-boundary question rather than deferring to the author

The brief asked whether the stub route is load-bearing or decorative — whether
the config the test reads is really the one a real launch uses. The reviewer
traced it end to end instead of accepting the declaration: derivation at
=api.rs:4770=, staged as =dispatch_artifact_dir= at =:4393=, shared preflight pin
at =:4819=, handed to =Supervisor::acquire= at =:4850=, passed to
=driver.acquire= at =supervisor.rs:1685=, and persisted into =RunMeta= at
=:1719= / =:2262=. *No later shared-path rewrite exists that a real launch takes
and the stub skips*, and the only preflight plan key in the tree is
=credential_plan=, which cannot overwrite the artifact dir.

=== Hermeticity, checked rather than assumed

The stub is in-process, opens no subprocess or socket, and reaches =Ready=
through an in-memory channel. Home, project root, worktree, session file,
supervisor and lease all live under one =tempfile::TempDir=, and the test awaits
supervisor release before asserting. It cannot reach the operator daemon, takes
no host port, and does not depend on scheduling — wall-clock only contributes to
a filename inside the unique temp project. That matters more than usual here: a
new nondeterminism contributor in THIS suite would have been costly.

=== Non-tautological expectation, confirmed with line numbers

=expected_artifact_dir= declared at =:26055=, =last_path= derived FROM it at
=:26060=, sent through =SpawnWorkerRequest.last_path= at =:26072=, asserted
against the independently declared directory at =:26100=. The expectation never
calls =last_path.parent()= and never routes back through the production
derivation. That is the property the two previous rounds inverted.

=== Accepted residual, accurately declared

The test stops at the stub/supervisor boundary: no real external adapter, no
re-test of the downstream finalize predicate — which the chain's own proofs own.
Declared by the author, confirmed by the reviewer, and not a ship blocker for a
test-only round. An accurate statement of what a test does not cover is the habit
this chain was trying to build.

