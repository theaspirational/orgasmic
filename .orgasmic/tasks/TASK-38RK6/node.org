#+title: orgasmic task TASK-38RK6
#+orgasmic_version: 2

* DONE TASK-38RK6 dec_B4147 cleanup: delete v* tags and purge stale CI artifacts :release:ci:cleanup:
:PROPERTIES:
:ID: TASK-38RK6
:BLOCKED_BY: TASK-32R7Z
:END:

Implements dec_B4147 (post-implementation cleanup). Destructive / outward-facing — run only after explicit maintainer approval, and after TASK-32R7Z lands (the new flow no longer needs v* tags and sets artifact retention).

Work:
- Delete git tags v0.0.1, v0.0.2, v0.0.3, v0.0.4, v0.0.5 locally and their remote refs (origin). decisions.org keeps version provenance; runtime-manifest.json stamps commit+version.
- Purge the ~49 stale `runtime-*` Actions artifacts (gh api / UI) to reclaim ~300 MB.
- Confirm runtime-bundles.yml upload-artifact retention-days: 1 is in effect so the leak does not recur.

Note: the `stable` and `nightly` GitHub releases are NOT tied to v* tags and must be left intact (install.sh consumes `stable`).
** Evidence
Ran after TASK-32R7Z landed, with explicit operator pre-authorization. Deleted git tags v0.0.1 through v0.0.5 locally and on origin. Purged all 50 stale runtime-* Actions artifacts (~389 MB reclaimed); artifacts total_count now 0. retention-days: 1 is set on runtime-bundles.yml upload-artifact (commit 29723d5) so the leak does not recur. Verified stable + nightly releases left intact: stable runtime-latest.json + all five runtime tarballs (incl. windows-x86_64) remain and the manifest is reachable for install.sh. No GitHub release was tied to any v* tag, so tag deletion was non-destructive.

