#+title: orgasmic task TASK-A7JFG
#+orgasmic_version: 2

* DONE TASK-A7JFG Repair create idempotency domains, cached response identity, and delete safety :daemon:cli:fix:
:PROPERTIES:
:ID: TASK-A7JFG
:END:

Fix every actionable finding from the TASK-N4TGD Claude Opus 4.8 BLOCK review: domain-separate default create request ids by operation/layer and resolved project; return the original real task id on retry; carry mutation identity in the writer idempotency cache so recovery never depends on a lagging index; reject deletion with inbound references unless an explicit safe force policy is added; make task delete tx/event semantics accurate or refuse task deletion; correct glossary normalization claim/implementation; add cross-layer, cross-project, task retry identity, race-safe recovery, and inbound-reference delete regressions.
** Evidence
Codex GPT-5.6 Terra/high implemented the Opus BLOCK fixes in 1c07f17; manager merged as 07a5b5a. Default create IDs now include operation and resolved project, task and graph retries return writer-cached original identities without index recovery, cache-scope reuse fails closed, inbound references block deletion, and generic task deletion is refused. Manager independently verified duplicate_write 5/5, create_request_id_tests 4/4, daemon writer/unit library 496/496, fmt, diff-check, and CLI+daemon clippy with warnings denied. The broader daemon integration sweep also exposed three pre-existing dispatch tests coupled to the checkout worker configuration; they are outside this mutation patch.

