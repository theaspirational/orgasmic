#+title: orgasmic task TASK-RPRD0
#+orgasmic_version: 2

* BACKLOG TASK-RPRD0 effort-delivery truth is tmux-only: stdio and subprocess-stream-json still record an effort the adapter ignored    :drivers:dispatch:
:PROPERTIES:
:ID: TASK-RPRD0
:PRIORITY: P2
:RELATES_TO: TASK-C7NVH
:END:

Found 2026-09-02 by the manager sprint review of 7c85f177..b1c6ca5f (drivers/docs reviewer, MEDIUM, SUSPECTED).

TASK-C7NVH fixed "tmux dispatches silently drop --effort" so the tx record never claims an effort the process never saw. That honesty fix lives in tmux_effort_delivery, which means it is TMUX-ONLY.

On stdio and subprocess-stream-json, an adapter that ignores effort (cursor-agent is the named case) still drops it, and nothing marks the record as "unsupported". So the exact failure TASK-C7NVH exists to kill - a record asserting an effort the worker never received - is still reachable on the non-tmux transports.
