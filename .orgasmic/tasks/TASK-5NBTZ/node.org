#+title: orgasmic task TASK-5NBTZ
#+orgasmic_version: 2

* DONE TASK-5NBTZ api.rs real_tmux_on_path claims the owned server BEFORE the strictness check — worker suites start a keepalive through the rmux shim
:PROPERTIES:
:ID: TASK-5NBTZ
:PRIORITY: P2
:END:

From TASK-FJCE9 (2026-07-29), same class as FJCE9/JGHNC, one line above JGHNC's fix: in crates/orgasmic-daemon/src/api.rs tests, real_tmux_on_path calls own_tmux_server_for_tests() AHEAD of tmux_mode_availability_for(...), so inside a worker the daemon lib suite still claims an owned server THROUGH THE RMUX SHIM. Observed directly at 2026-07-29 03:01:49: /private/tmp/rmux-501/ gained orgasmic-test-56699 + .startup-lock (own_tmux_server_for_tests's name format, rmux's server), gone by 03:04. The claim-order attribution is a code reading, not a captured pid — verify at HEAD first. Nothing is killed (smaller than FJCE9's defect) but the class is identical: a tmux-labelled mechanism operating on the rmux server. Fix: check strictness first, claim second — supervisor.rs already has the correct order; recovery_fault_restart.rs (FJCE9) is the third example of the right shape. Injection per TCTTD standard (natural red: in a shim environment the claim happens — assert the rmux socket dir gains no orgasmic-test-* entry when availability fails).
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit f6633ade on sprint/DOCS, merged onto local sprint-20260902 as d41c6127. api.rs tests real_tmux_on_path: now checks orgasmic_drivers::modes::tmux::real_tmux_on_path() FIRST and claims own_tmux_server_for_tests() only on pass (same shape as tests/dispatch.rs and recovery_fault_restart.rs); the local 'tmux -V' probe is gone. At HEAD the shim strictness check no longer exists (010a32bc tmux-only), so the defect was the inverted order (keepalive claim even when tmux is absent). Grep: every other own_tmux_server_for_tests caller checks first; claim_owned_tmux_endpoint (13 callers) claims unconditionally by design (endpoint-isolation fixture) — left alone, flagged. No unit test (process-global OnceLock + PATH; would need a fork harness). Gates (private target): 2 tmux-gated daemon tests passed with a real tmux, clippy rc=0, fmt rc=0. Side note: /tmp/tmux-501 holds ~115 stale orgasmic-test-<pid> sockets (cosmetic).

