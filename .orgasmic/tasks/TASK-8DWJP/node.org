#+title: orgasmic task TASK-8DWJP
#+orgasmic_version: 2

* IN_REVIEW TASK-8DWJP CLM6W H6: claim gate only refuses CLAIMED nodes; every node is unclaimed between dispatches, so two daemons both land — the sync premise is false (fix round for TASK-CLM6W; id grammar refuses TASK-CLM6W.1) :e01mc:chain-review:
:PROPERTIES:
:ID: TASK-8DWJP
:PRIORITY: P1
:RELATES_TO: TASK-CLM6W TASK-AS0FS
:FIX_SUBTASK: t
:DEPENDS_ON:  TASK-MSYN4.2
:IMPLEMENTS:  dec_EWY0K
:END:

** Description
Source: whole-chain review tx-20260901-orgasmic-1c6d2115 (reviewer-claude-sdk-stdio, claude-opus-5 high, 2026-09-01), verdict APPROVE WITH FOLLOW-UPS; report promoted under tasks/<chain-task>/dispatches/tx-20260901-orgasmic-1c6d2115-188e-4db6-9ed1-ebb0a5415b07/report.md.
=guard_node_write= (crates/orgasmic-daemon/src/writer.rs:1766) returns Ok for any node with no claim row. Claims are per dispatch (live log: 62 task.claimed / 70 task.claim_released), so between dispatches a comment or task update from two daemons both land. ledger_sync.rs:52 states the opposite premise (a foreign node dir can only appear modified here if something wrote outside its pen, which the claim gate refuses); the consequence is the H5 rebase wedge. This is a DESIGN choice, overlapping TASK-AS0FS (singleton ownership): either claim on first write and hold until sync (make the pen real), or drop the two-machines-never-write-the-same-node claim and give the sync loop a conflict path. Half of each is shipped. Decide first (grill), then implement; record the decision.
** Acceptance
- [ ] A recorded decision (dec_) choosing pen-on-write vs conflict-path, folded with TASK-AS0FS.
- [ ] Implementation + a two-writer test proving the chosen property; ledger_sync.rs:52 comment matches reality.
- [ ] clippy -D; fmt.
** Notes
DECIDED 2026-09-01: dec_EWY0K — conflict path, not pen-on-write. Scope of this task is now implementation only: on a conflicting pull --rebase the sync loop aborts, parks the local side on refs/orgasmic/conflicts/<machine-id>/<ts>, follows the remote, records a ledger event (machine, parked ref, conflicting paths), and surfaces it via the MSYN4.2 status; ledger_sync.rs staging comment and references/ledger.md restate the premise; two-writer test proves both writes survive (one live, one parked), loop recovers, status shows it. Folds TASK-AS0FS (closes together).
** Evidence
Merged 200892f2 (worker fa8ef1f9, codex gpt-5.6-sol). Manager gates re-run on merged main 2026-09-01: cargo test -p orgasmic-daemon --lib -- ledger_sync status sync_conflict → 16 passed, 0 failed; cargo test -p orgasmic-cli --bin orgasmic -- daemon_lifecycle → 22 passed, 0 failed; cargo clippy -p orgasmic-daemon -p orgasmic-cli --all-targets -D warnings clean; cargo fmt --all --check clean. Logs: scratchpad/gates-8DWJP-{daemon,cli,clippy,fmt}.log. Implementer-named residual: write-loss window between the salvage commit and reset --hard origin/orgasmic; parked-ref push is best-effort. Review round (claude-opus-5 high) dispatched on the merged diff.
