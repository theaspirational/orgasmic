#+title: orgasmic task TASK-0Y363
#+orgasmic_version: 2

* DONE TASK-0Y363 Resolve health-probe starvation recovery after the shipped session-handle leak fix :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-0Y363
:PRIORITY: P0
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 2. Measured on the live daemon: 253 open fds / 256 soft limit, 223 session .jsonl handles held, 3 live runs. Root cause: writer.rs `session_handles` only evicted on LeaseSessions/shutdown.
** Shipped (commit 8bd8287)
- writer drops the handle on the `release` lifecycle append; `WriterStatus.open_session_handles` gauge + regression test.
- daemon raises RLIMIT_NOFILE at start (65536/10240/4096 fallback), logged at boot.
- `/status` carries `open_fds`/`fd_limit`; `orgasmic daemon status` prints `fds: N/limit (writer session handles: H)`.
** Leftover (d)
The exhausted daemon could not accept the health probe socket, so the supervisor logged `incumbent is not healthy … Refusing to start a competing daemon` and the sick daemon stayed up. Either answer health before the spawn path can starve it, or treat a held lock whose owner cannot answer health as dead.
** Status 2026-09-06
Source/ledger triage at main 0bf80eb0. P0 retained for the remaining health-starvation recovery question. The original session-handle leak is already fixed. Current crates/orgasmic-daemon/src/lib.rs:120-175 distinguishes NotDeparting and StuckPredecessor and still refuses to replace a holder that cannot answer health. A missed health response alone is not proof the process is dead; preserve single-daemon exclusion. No FD-exhaustion/live-daemon reproduction was run. Next: isolated exhaustion/health proof before choosing a recovery change.
2026-09-06 discriminator plan: current held-lock refusal must remain. The remaining mechanism is health transport starvation: /api/healthz and /api/daemon/status share the listener, so fresh accepts need FD capacity; incumbent classification uses full daemon/status with index snapshot/refresh dependencies. Investigate in an isolated child daemon: boot first, lower only child RLIMIT_NOFILE, hold pre-opened keepalive/control connections, exhaust child FDs, compare existing vs fresh health requests, release held FDs and verify recovery. Never exhaust or kill the operator daemon. This separates inability to accept from blocked handler work before selecting a fix; no stress probe executed yet.
** Evidence
2026-09-06 diagnostic e464ff13 (integration merge 751bf2d2, not yet main): real disposable Unix child daemon, RLIMIT_NOFILE lowered only after boot. Two cycles each filled 33 descriptors at soft limit47; warm health/status stayed200, fresh health/status timed out (client os error35), then recovered after release. Child stayed alive and daemon lock remained held; bounded shutdown/reap exited0. One focused scripts/run-tests.sh test GREEN; no production recovery code changed. Evidence: /Users/aspirational/.codex/artifacts/orgasmic-recovery-20260906/p0-diagnostic-report.md and p0-baseline-discriminator-observations.txt. Exact implementer generation tx-20260906-orgasmic-62b41264-7678-46b5-9f31-200930a954b0 closed after report, merge is on integration branch only. P0 production criterion remains open: automatic versus explicit restart policy. A missed fresh health probe is not authority to replace a live lock owner.
2026-09-06 closure evidence: b877d0e6 implements explicit-recovery guidance; c61543b3 extends only the disposable child fixture. End-to-end real CLI status/start preserved the exhausted child PID/boot/lock; explicit detached restart exited 0, child exited 0, fresh authenticated status reached a new PID/boot in the same home; cleanup passed. Original two-cycle exhaustion diagnostic passed again. Scope and logs: /Users/aspirational/.codex/artifacts/orgasmic-p0-restart-20260906/outcome.md. Prior diagnostic-only/open-policy notes are superseded by the user-confirmed preserve-runs/explicit-restart policy and this verified outcome. Source complete, not installed or pushed.
** 2026-09-06 outcome
Diagnostic commit e464ff13 is independently accepted and now merged to source main14561065. Two child-only EMFILE/release cycles showed warm health/status exchanges succeeding, fresh requests timing out, and the PID plus daemon lock remaining live. Fresh requests recovered after descriptor release. Actual EMFILE was observed on child filler opens; accept-loop starvation is an inference, not a measured accept() event.

The production recovery criterion remains unmet, so this P0 task stays IN_PROGRESS. No production FD reserve, lock takeover, automatic restart or recovery policy was added. A failed fresh health probe cannot prove a held lock owner is dead. Optional recovery-policy question remains unanswered; no automatic recovery authorization is inferred. Diagnostic report and observations are under ~/.codex/artifacts/orgasmic-recovery-20260906/. No open dispatch remains for this task.
** Recovery policy confirmed 2026-09-06
User explicitly selected: preserve active runs and require an explicit restart. Implement recovery guidance within that policy; do not automatically drain/restart or infer lock takeover authority from failed health probes. TASK-STWVB is the next selected work slice; production P0 implementation remains open.
** Explicit recovery diagnostic 2026-09-06
Local source commit b877d0e67ba36e8849864b0d78c0bfc345508210 implements the selected explicit-recovery diagnostic. Inspection confirmed the CLI already preserves the held instance lock after transport/HTTP failure through starting_fallback; it does not invoke service start for that state. But absent boot metadata was mislabeled as starting/stalled boot.

The readiness error plus daemon status/start now say the instance is not responding and boot progress is unavailable, suggest inspecting logs, and name explicit orgasmic daemon restart from the same home. No new automatic restart, reserve FD or lock takeover was added. Named boot phases retain their existing messages.

RED: the existing real-HTTP-500/held-lock regression failed on the misleading boot-stalled error after adding the recovery assertion. GREEN: all 28 lifecycle unit tests, including that regression; the new spawned-CLI integration test passed for both status and start with a held lock, checked output and preserved lock contents/ownership. cargo fmt --all -- --check and git diff --check passed. Logs: /Users/aspirational/.codex/artifacts/orgasmic-p0-explicit-recovery-20260906/.

P0 remains IN_PROGRESS: prior child-only EMFILE evidence and current CLI-held-lock behavior are verified separately. An end-to-end explicit restart under descriptor pressure has not yet been tested. No install/push or live daemon mutation. Next bounded slice: exercise the existing disposable FD-exhaustion child with the built CLI, verify status/start preserve it under pressure, then an explicit detached-adapter restart replaces only that owned child. Reuse the existing child protocol; do not duplicate the stress harness or address the operator service.
** Recovery verified 2026-09-06
P0 recovery resolution at source c61543b3f7777a6e5e2884c884f6fd9734b75341, following the user-selected policy: preserve the current instance/runs and require explicit restart. The original FD leak and RLIMIT fix remain in place. No automatic replacement, held-lock takeover or FD-reserve policy is introduced.

Confirmed end-to-end in a parent-owned temporary home, using the real built CLI and the existing daemon test child. EXHAUST opened 33 fillers to child soft limit 46 and observed EMFILE. A pre-existing authenticated connection remained usable. daemon status and daemon start both returned the explicit-recovery guidance while the original child PID 27895, boot 1972dc58-8d0b-4d6e-9dd1-b08cb2dc1ce5 and held instance lock were unchanged. No RELEASE command cleared the pressure before recovery.

The operator-equivalent explicit command used ORGASMIC_TEST_SERVICE_ADAPTER=detached and only that temporary home. daemon restart exited 0 in 1.434s; the exhausted child exited cleanly with code 0, and a fresh authenticated status request reached replacement PID 27907, boot 9994f036-b914-47e8-8df6-6051e7938df9 on the same home/port/token. Replacement lock held. Cleanup stopped only that replacement successfully. No active provider worker was launched; this proves daemon preservation/replacement boundaries, not provider-session resumption after an explicit restart.

The original two-cycle EMFILE/release diagnostic passed again in 10.19s. Prior b877d0e6 added truthful unresponsive-instance status/start/readiness guidance: 28 lifecycle unit checks and the real CLI held-lock output regression passed, with RED proof for the prior misleading boot message. Current fmt and diff checks passed. Current commit only extends the test child with parent-owned home and pre-armed SIGINT handling; runtime behavior comes from existing lock preservation plus b877d0e6 diagnostics.

Evidence and runnable exact probe: /Users/aspirational/.codex/artifacts/orgasmic-p0-restart-20260906/probe.py, observations.json, probe.log, per-command logs, original-diagnostic.log, build.jsonl. Operator daemon PID39114 / boot019f72b5-9e79-4536-905c-a95dff391ac7 remained unchanged. No push or installation. Source policy/diagnostic/recovery verification is complete; runtime activation remains a separate rollout task.

