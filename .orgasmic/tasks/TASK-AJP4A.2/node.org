#+title: orgasmic task TASK-AJP4A.2
#+orgasmic_version: 2

* BACKLOG TASK-AJP4A.2 Own the two LOW notes from the final lazy-index approval (renderer guard newtype/debug-assert; multi-project delayed-branch test)    :daemon:cli:testing:
:PROPERTIES:
:ID: TASK-AJP4A.2
:PRIORITY: P3
:WRITE_SCOPE: crates/orgasmic-cli/src/daemon_service.rs,crates/orgasmic-daemon/src/api.rs
:END:

** Description
Carries the two LOW, non-blocking notes from the final approving review of TASK-AJP4A.1.1.1.1.1 (=tx-20260811-orgasmic-5833=, VERDICT approve, report at =.orgasmic/dispatch-records/tx-20260811-orgasmic-5833/last.txt=):

1. *systemd renderer lost its own control-char guard* (=daemon_service.rs:950= dropped the =.filter(..is_control)= from the renderer; the check now lives once at the capture boundary =daemon_service.rs:386=). Fix direction: enforce the invariant rather than leave it conventional — newtype on =ServiceSpec.project_scan_timeout= whose only constructor is =captured_project_scan_timeout=, or re-add a cheap =debug_assert!= inside each renderer.
2. *race test asserts the degenerate case* (=api.rs:30203-30213= asserts the bare empty array for a single delayed project; no multi-project assertion exists for the delayed branch). Add a multi-project delayed-branch test.

** Acceptance
- Renderer invariant enforced structurally (newtype or debug_assert) with a test that fails loudly on bypass.
- A multi-project test covers the delayed branch of unscoped /tx aggregation.

