#+title: orgasmic task TASK-7A5GH
#+orgasmic_version: 2

* BACKLOG TASK-7A5GH Cross-process slow-start matrix: two real orgasmic serve processes racing one temp home
:PROPERTIES:
:ID: TASK-7A5GH
:PRIORITY: P2
:END:

From TASK-5P60H (2026-07-29): its in-process concurrent-start matrix proves one lock owner / one listener / classify-not-refuse, but its actors share one process, so pid divergence cannot be reproduced — single ownership is asserted where recorded (lock file, boot record, incumbent status, one bound socket) rather than by pid inequality. A spawned-binary version (two real orgasmic serve processes on a temp home, compressed budgets via env seams) closes that stated limitation. Bigger, slower harness — worth doing when the boot protocol is next touched, or as the integration half of the boot-audit program. Also fold in 5P60H candidate 4: a nightly macOS launchd smoke proving ThrottleInterval against real launchd (not its documentation) belongs with the WE0Q7/nightly infra once the operator pushes the workflows.

