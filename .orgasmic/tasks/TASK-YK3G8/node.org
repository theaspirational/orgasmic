#+title: orgasmic task TASK-YK3G8
#+orgasmic_version: 2

* DONE TASK-YK3G8 TASK-PMVC7 fix: bound Cursor ACP error logging and ambiguity coverage :fix:cursor:driver:
:PROPERTIES:
:ID: TASK-YK3G8
:PRIORITY: high
:END:

Claude Opus 4.8/high review of e4b6b7b..c1dccd1 returned SHIP with one medium gap: crates/orgasmic-drivers/src/modes/jsonrpc.rs renders the full server-controlled error.data into DriverError::Transport, which is copied into durable session JSONL and last artifacts without a size bound. The same review found a missing ambiguous-alias regression and low-risk subset semantics in legacy_model_alias_matches.

Acceptance: cap serialized error.data at a small deterministic byte/character budget with an explicit elision marker while preserving method, code, and message; keep output valid UTF-8 and deterministic for strings/objects/arrays; add regressions for below-limit and oversized/multibyte data. Add an ambiguity regression with two matching advertised model variants and prove InvalidConfig before prompting. Tighten legacy alias matching so under-specified aliases cannot silently select extra distinguishing attributes, while cursor-grok-4.5-high-fast still resolves to grok-4.5[effort=high,fast=true]. Keep scope to Cursor ACP/JSON-RPC tests and code.
** Notes
Implemented by Codex gpt-5.6-terra/high in 4a59743 and merged to main as 559add1. The delta bounds server-controlled JSON-RPC error.data at 3 KiB with UTF-8-safe deterministic elision, covers small/oversized/multibyte data, rejects under-specified legacy aliases, and proves ambiguous aliases fail before any config or prompt write. Manager independently reran 13 Cursor ACP tests and 8 JSON-RPC tests; all passed, with fmt and delta diff-check clean.

