#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-Z8WEJ Select the claude credential mode instead of hardcoding --bare :drivers:dispatch:bug:
:PROPERTIES:
:ID: TASK-Z8WEJ
:PRIORITY: P0
:END:

Problem: `crates/orgasmic-drivers/src/adapters/claude.rs:136` hardcodes `--bare` in the claude stdio argv. Per the CLI's own help, `--bare` means "Anthropic auth is strictly ANTHROPIC_API_KEY or apiKeyHelper via --settings (OAuth and keychain are never read)". An operator authenticated through a claude.ai subscription therefore cannot dispatch a claude worker at all: the credential exists, `claude auth status` reports `loggedIn: true, authMethod: claude.ai, subscriptionType: max`, and the dispatch still dies 1.2 s after acquire with "Not logged in - Please run /login" (observed TASK-KWSTJ reviewer dispatch, run-20260725T073841).

`--bare` is wanted and should stay available: it skips hooks, LSP, plugin sync, auto-memory, background prefetches and CLAUDE.md auto-discovery, which is the right shape for a worker. The defect is that it is unconditional, and it silently carries a credential policy that a subscription operator cannot satisfy.

Measured channel matrix, same binary and prompt, obviously-fake credential values (2026-07-25, claude 2.1.220):

Under `--bare`:
- no credential -> "Not logged in - Please run /login"
- `ANTHROPIC_API_KEY=<fake>` -> "Invalid API key - Fix external API key" (channel LIVE)
- `CLAUDE_CODE_OAUTH_TOKEN=<fake>` -> "Not logged in" (channel DEAD; the token is ignored)
- `apiKeyHelper` via `--settings` -> "Invalid API key" (channel LIVE)

Without `--bare`:
- keychain OAuth -> authenticates and completes
- `--strict-mcp-config` + empty `--mcp-config` + minimal `--settings` -> authenticates, `mcp_servers: []`, 30 tools, 2.0 s round trip

Critical negative result: `CLAUDE_CODE_SIMPLE=1` on its own, with no `--bare` flag, also fails with "Not logged in". The lightweight behaviour and the credential policy are the same switch, so there is no way to keep bare-mode lightness while reading the keychain. Any design that assumes they can be separated is wrong.

Both live `--bare` channels want an API key, not an OAuth credential, and a subscription's keychain credential is not an API key. So "`--bare` plus the user's keychain auth" is not achievable, by the flag's contract rather than by oversight.

Work:
1. Make the claude adapter select a credential mode instead of hardcoding one. At minimum: bare+API key (preferred, lightest) when an API key or apiKeyHelper is configured; non-bare with narrow isolation when auth is OAuth/subscription.
2. Build the fallback tier explicitly rather than by dropping flags: `--strict-mcp-config` with an empty `--mcp-config`, a minimal `--settings`, and the existing `--no-session-persistence`. Verify it suppresses MCP, hooks, and CLAUDE.md discovery — do not assume the narrow flags cover everything `--bare` did, and record what is not covered.
3. Detect, do not guess. `claude auth status` is non-interactive and returns structured JSON with `authMethod`; use it to choose the tier. Note it is necessary but not sufficient — see 5.
4. Let the manager override the tier per dispatch, and record which tier was used in the run's RunMeta so a failure is diagnosable from the session JSONL alone.
5. Feeds TASK-TJKFC: a generic auth probe is not enough. `claude auth status` reports `loggedIn: true` while the `--bare` invocation fails. The preflight must exercise the argv the worker will actually run.
6. Consider whether the same credential-mode question exists for codex, cursor-agent, and hermes, or whether this is claude-specific. Do not generalise without checking.

Acceptance:
- a subscription-authenticated operator can dispatch a claude worker with no API key
- an API-key operator still gets the light `--bare` path
- the selected credential mode is visible in RunMeta
- a mode that cannot authenticate is rejected before a worktree, lease, or dispatch record is created
** Evidence
Implemented 2026-07-25 in commit 61b9b7f on `task-kwstj-bounded-run-inventory`.

`adapters/claude.rs` now resolves a `ClaudeCredentialMode` in `compose_request` rather than hardcoding flags in `stdio_spawn`:
- `BareApiKey` when `api_key_env` resolves or `ANTHROPIC_API_KEY` is present — keeps the light `--bare` path, which is the one credential channel `--bare` accepts.
- `NativeLogin` otherwise — drops `--bare` so the harness reads its own keychain/OAuth login, and rebuilds isolation from `--strict-mcp-config` plus a minimal `--settings`.

`stdio_spawn` now returns the credential-agnostic wire flags only; mode flags and the pinned session id are added where the run context is available.

Item 2's instruction to verify rather than assume the narrow flags cover everything `--bare` did produced one negative result worth recording. Measured against claude 2.1.220:
- MCP is fully suppressed: `--strict-mcp-config` alone yields `mcp_servers: []`, against nine servers loaded without it on the measurement machine.
- Hooks are NOT suppressible. `--settings '{}'` and `--settings '{"hooks":{}}'` both still executed SessionStart hooks; `--include-hook-events` governs reporting, not execution; `--bare` is the only flag that skips them.

So native-login workers execute whatever hooks the operator has configured. This is accepted deliberately — the alternative is that subscription-authenticated operators cannot dispatch claude at all — and is documented on the `NativeLogin` variant rather than left for a later reader to rediscover. It surfaced through a real test failure (`legacy_drivers_and_explicit_pairs_emit_equivalent_start_events` began comparing nondeterministic hook uuids), not through inspection.

Item 4 remains open: the resolved mode is logged at debug but is not yet recorded in RunMeta, so a failure is not yet diagnosable from the session JSONL alone. Item 6 also remains open — codex, cursor-agent and hermes have not been checked for the same credential-mode question.

Gates: `cargo fmt`, `cargo clippy --workspace --all-targets`, `git diff --check` clean. orgasmic-drivers 213 lib tests pass; `driver_modes` returns to its pre-change baseline of 20 passed / 4 failed, those four being pre-existing codex failures verified identical on the clean branch by stashing. orgasmic-core 132 pass.

Not verified end to end: the running daemon is the installed 0.0.14 build, so a live dispatch still uses the old adapter. Proving this against a real dispatch needs a daemon rebuild and restart, which is an operator decision.
BLOCKER RESTATED [2026-07-26]. Stays in_review, but not for the reason the
evidence above gives.

"The running daemon is the installed 0.0.14 build" is stale twice over. The
daemon is now 0.0.15-dev.1, and TASK-SGRTX (b43fc70, f5c6913) established
something worse than "unverified": this code was INERT in production. claude
simulation_reason treated an empty endpoint as grounds to simulate, which is
every dispatch, so compose_request returned Simulated before building any argv.
The credential mode this task resolves was computed, recorded, and then dropped
on the floor. Read the evidence above as verified in test and inert in
production, not as verified-but-unproven.

So the verification this task needs is a live claude dispatch on a daemon
carrying todays main. The operators daemon is the installed 0.0.15-dev.1
bundle, which predates those commits. That publish is itself gated behind
TASK-8W30B -> TASK-6FNAY. This is a chain, and it is the reason the task cannot
simply be closed by re-reading the diff.

Two acceptance criteria are also still unmet, independently of verification:
- "the selected credential mode is visible in RunMeta" — work item 4 is open;
  the mode is logged at debug only. This is the same gap filed today as
  TASK-A55J7 (model and effort are likewise never persisted to RunMeta or the
  dispatch tx). Whoever does one should do both; it is one field set on one
  record.
- work item 6 — whether codex, cursor-agent and hermes have the same
  credential-mode question — has not been checked.

Closing this needs: the live dispatch, item 4, and item 6. Not a reviewer.
LIVE MEASUREMENT 2026-07-27 after the runtime rebuild (commit 4d1369a, daemon boot 306cf994), run run-20260727T131952-9138610466c24c469819ac27e3f6c504, worker implementer-claude-acp-stdio, acp-stdio/claude/claude-opus-5/high. The composed argv now reaches the spawned process. Observed live via ps and confirmed against the persisted launch_argv lifecycle event: claude --strict-mcp-config -p --input-format stream-json --output-format stream-json --include-partial-messages --verbose --session-id df92384b-fd23-423c-ab2c-131beb03db82 --model claude-opus-5 --effort high. Compare the SGRTX baseline, which was the bare stdio_spawn list with no session id, model, effort or mcp flag. Session pinning is real and end to end: orgasmic recorded runtime_id df92384b-fd23-423c-ab2c-131beb03db82 on every event, passed that same value as --session-id, and the vendor transcript landed at ~/.claude/projects/-Users-aspirational-Documents-code-tools-orgasmic--orgasmic-tmp-dispatch-task-RWCRN-worktree/df92384b-fd23-423c-ab2c-131beb03db82.jsonl, so the filename equals the runtime_id exactly and the live-only acceptance is met. Note for anyone re-checking: compare against runtime_id, not against the 32-hex suffix inside run_id, which is the dispatch attempt token and will never match. The driver also reports capabilities.simulated=false with endpoint="" — the shape dec_S18RH requires — so f5c6913 is confirmed live rather than merely merged.

* IN_REVIEW TASK-VB9DQ Persist native session evidence for acp-stdio claude runs :drivers:recovery:evidence:
:PROPERTIES:
:ID: TASK-VB9DQ
:PRIORITY: P1
:END:

Problem: `crates/orgasmic-drivers/src/adapters/claude.rs:144` passes `--no-session-persistence` on every acp-stdio claude run, and the adapter never passes `--session-id` or emits a `NativeRuntime` lifecycle event. The consequence is that acp-stdio claude runs leave no harness-native evidence of any kind.

Measured 2026-07-25 (claude 2.1.220, counting files under `~/.claude/projects`):
- run with `--no-session-persistence`: 0 new native transcripts
- identical run without it: 1 new native transcript

Confirmed by inspection:
- `NativeRuntimeMeta` appears only in `modes/rmux.rs` and `modes/tmux.rs`; `adapters/claude.rs` never emits it
- the acp-stdio reviewer run `run-20260725T073841` recorded lifecycle phases `acquire, run_meta, release` only — no `native_runtime`

Contrast the TUI path, which already does this correctly. A real rmux run recorded:
- `launch_argv: [claude, --dangerously-skip-permissions, --model, claude-sonnet-5, --session-id, a9fe25ff-...]`
- `resume_argv: [claude, --resume, a9fe25ff-..., --fork-session, --dangerously-skip-permissions]`
- `session_id: a9fe25ff-...`

It passes an orgasmic-minted session id up front, so correlation is deterministic rather than discovered. acp-stdio should follow that pattern.

Three consequences, all currently live:

1. TASK-VBSG2 is impossible for this transport. Its slice A resolves "verified NativeRuntime provider/session/path metadata" and parses "Claude native JSONL". Neither exists for acp-stdio runs, so retro over an arbitrary past claude run cannot work no matter how good the materializer is.

2. dec_WDR5K item 7 is violated in effect. TUI screen capture was removed on the premise that "session transcripts come from harness-native JSONL located by per-harness transcript finder adapters". For acp-stdio claude the finder adapter has nothing to find, so those runs have no transcript at all — not a lossy one, none. The decision's own sequencing constraint (finders must land before capture removal, or runs lose their session inputs) is satisfied in letter and broken in substance here.

3. Recovery is degraded. `resolve_recovery_actions` offers `resume_native_fork` only when `session_native_runtime` yields a validated claude session id. acp-stdio records none, so that action can never be offered for this transport; recovery falls through to a fresh run even when resuming was possible.

Work:
1. Drop `--no-session-persistence` from the acp-stdio claude argv, or make it a deliberate per-dispatch option rather than a hardcoded default.
2. Mint and pass an explicit `--session-id`, matching the TUI path, so correlation never depends on discovery heuristics.
3. Emit a `NativeRuntime` lifecycle event from the acp-stdio path with provider, session id, launch argv, and resume argv, so recovery and retro read one shape regardless of transport.
4. Weigh and record the retention tradeoff rather than assuming it away. Persisting worker sessions grows `~/.claude/projects` without bound (446 transcripts already present on this machine), and the retro contract holds vendor JSONL to be vendor-owned and immutable — so orgasmic may not prune it. Decide explicitly whether that is acceptable, whether worker sessions should be distinguishable from operator sessions, and record the answer.
5. Check the same three properties for codex, cursor-agent, and hermes under acp-stdio. Do not assume this is claude-specific.

Coordinates with TASK-Z8WEJ: both change `stdio_spawn` in the same adapter and should land together.

Acceptance:
- an acp-stdio claude run leaves a native transcript that the existing transcript finder resolves
- its session id is recorded in a NativeRuntime lifecycle event, not discovered after the fact
- `resume_native_fork` is offered for a recoverable acp-stdio claude run
- the retention tradeoff is a recorded decision, not an implicit one
** Notes
Item 4's retention tradeoff is now settled by dec_Y5MPK: retention of vendor transcripts belongs to the operator. Orgasmic never prunes, moves, rewrites or expires a vendor file; its obligation is visibility (per-harness counts, total size, age distribution through `doctor`). So this task does not need to solve growth — it needs to stop suppressing persistence, mint the session id, and emit NativeRuntime. The doctor reporting is separate work.
** Evidence
Implemented 2026-07-25 in commit 61b9b7f on `task-kwstj-bounded-run-inventory`.

- `--no-session-persistence` removed from the claude stdio argv. Verified beforehand that it was the sole cause: an otherwise identical run wrote one new file under `~/.claude/projects` without it and zero with it. Also verified that `--bare` does not suppress persistence, so both credential modes keep evidence.
- `--session-id` is now pinned to the run's `runtime_id`, matching `modes/tmux.rs`. Verified that `-p` mode honours it and that the vendor transcript lands at exactly `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl`, the path `claude_session_path` computes. Correlation is minted, not discovered (dec_Y5MPK item 3).
- `HarnessEventAdapter::native_runtime` added, defaulting to `None`. The claude adapter populates it while composing the request; `AcpStdioComposeAdapter` delegates it; `modes/acp_stdio.rs` reads it immediately after composing and carries it onto the `DriverSession` in place of the previous hardcoded `native_runtime: None`.
- `claude_session_path` promoted to `pub(crate)` in `modes/tmux.rs` rather than duplicating the cwd-encoding rule in a second place.

`resume_argv` is recorded as `[claude, --resume, <session-id>, --fork-session]`. Note it deliberately omits `--dangerously-skip-permissions`, which the TUI path includes: the stdio path never passed that flag at launch, and a resume argv should reproduce the original run's permission posture rather than widen it.

Regression `every_mode_persists_a_locatable_native_session` asserts no `--no-session-persistence`, a pinned session id, and NativeRuntime whose `session_id` and `resume_argv` both target it.

Still open from this task's work list: item 5, whether codex, cursor-agent and hermes need the same treatment. Codex is known to persist natively without orgasmic asking — a run that died on a provider 503 still wrote `~/.codex/sessions/2026/07/25/rollout-...jsonl` — so the question there is session-id correlation, not persistence.

Gates as recorded on TASK-Z8WEJ. Not verified end to end: the running daemon is the installed 0.0.14 build, so proving this against a live dispatch needs a daemon rebuild and restart.
BLOCKER RESTATED [2026-07-26]. Stays in_review; the closing line above is stale
in the opposite direction from how it reads.

"Not verified end to end: the running daemon is the installed 0.0.14 build"
suggests the work is done and merely unproven. TASK-SGRTX (b43fc70, f5c6913)
showed it was INERT in production: the claude adapter returned Simulated before
building argv, so the pinned --session-id and the NativeRuntime event this task
adds never reached a real process. After SGRTX it does work — so the honest
status is "verified only after a fix that landed later, on a runtime the
operator is not yet running".

The verification this needs is a live claude acp-stdio dispatch on a daemon
carrying todays main, proving the three acceptance criteria against a real run:
a native transcript the existing finder resolves, a NativeRuntime lifecycle
event carrying the minted session id, and resume_native_fork actually offered
for that run by resolve_recovery_actions. The operators daemon is
0.0.15-dev.1, which predates SGRTX; the publish is gated behind
TASK-8W30B -> TASK-6FNAY.

Work item 5 also remains open: whether codex, cursor-agent and hermes need the
same treatment. The body already notes codex persists natively without being
asked (a run that died on a 503 still wrote ~/.codex/sessions/...jsonl), so for
codex the open question is session-id correlation rather than persistence.

Lands together with TASK-Z8WEJ — same adapter, same live dispatch proves both.
LIVE MEASUREMENT 2026-07-27 after the runtime rebuild (commit 4d1369a, daemon boot 306cf994), run run-20260727T131952-9138610466c24c469819ac27e3f6c504, worker implementer-claude-acp-stdio, acp-stdio/claude/claude-opus-5/high. The composed argv now reaches the spawned process. Observed live via ps and confirmed against the persisted launch_argv lifecycle event: claude --strict-mcp-config -p --input-format stream-json --output-format stream-json --include-partial-messages --verbose --session-id df92384b-fd23-423c-ab2c-131beb03db82 --model claude-opus-5 --effort high. Compare the SGRTX baseline, which was the bare stdio_spawn list with no session id, model, effort or mcp flag. Session pinning is real and end to end: orgasmic recorded runtime_id df92384b-fd23-423c-ab2c-131beb03db82 on every event, passed that same value as --session-id, and the vendor transcript landed at ~/.claude/projects/-Users-aspirational-Documents-code-tools-orgasmic--orgasmic-tmp-dispatch-task-RWCRN-worktree/df92384b-fd23-423c-ab2c-131beb03db82.jsonl, so the filename equals the runtime_id exactly and the live-only acceptance is met. Note for anyone re-checking: compare against runtime_id, not against the 32-hex suffix inside run_id, which is the dispatch attempt token and will never match. The driver also reports capabilities.simulated=false with endpoint="" — the shape dec_S18RH requires — so f5c6913 is confirmed live rather than merely merged.

* IN_REVIEW TASK-8W30B RmuxControl::release aborts its tasks before kill, leaking a session, pty and harness process per run :drivers:rmux:bug:production:
:PROPERTIES:
:ID: TASK-8W30B
:PRIORITY: P1
:test_cmd: cargo test -p orgasmic-drivers
:write_scope: crates/orgasmic-drivers/src/modes/rmux.rs crates/orgasmic-drivers/tests/rmux_release_reaps_session.rs
:read_scope:  crates/orgasmic-drivers/src/ crates/orgasmic-daemon/src/supervisor.rs .orgasmic/gotchas.org
:END:

Production defect, root-caused by the TASK-STWVB implementer while diagnosing a
test flake. It is not a test problem.

THE DEFECT

`RmuxControl::release` (crates/orgasmic-drivers/src/modes/rmux.rs:1760) aborts
its lifecycle task and its startup task BEFORE calling `Session::kill()`. The
kill is then issued over a torn-down SDK transport and fails. Instrumented probe:

    PROBE rmux Session::kill -> Err(Transport { operation: "complete
      `kill-session` request/response exchange with rmux daemon",
      source: BrokenPipe: "rmux SDK request was cancelled while its ordered
      response was pending" })

The session survives. The only report is a `tracing::warn!`, invisible under
`cargo test` and easy to miss in a daemon log.

WHY IT MATTERS BEYOND TESTS

Every released rmux run leaks a session, a pty and a harness process into the
per-user rmux daemon — a singleton on `$TMPDIR/rmux-$uid`, shared across every
run and every test binary, started by launchd and therefore inheriting a 256-fd
soft limit (`launchctl limit maxfiles` → `maxfiles 256 unlimited`, confirmed
independently on this machine 2026-07-26).

Once saturated the daemon cannot create sessions, and the rmux driver degrades
to INERT — silently. A developer's machine reaches that state on its own, and
from then on every rmux dispatch quietly does nothing useful.

MEASURED

- 8 leaked sessions per `--lib artifact` run (109 → 189 across 12 runs; 7 still
  present 15 s after the process exited)
- 236 stale `orgasmic-rmux-run-*` sessions and 69 orphaned
  `artifactor-test-harness.sh` processes found accumulated, oldest predating the
  dispatch that found them
- at 236 sessions the daemon held 253 open fds against the 256 limit
- dose-response over 10 consecutive full-suite runs: green at 204/210/217/221/226
  leaked sessions, failing at 233/236/236/236/236. The count stopped growing
  exactly when failures began — the daemon could no longer create sessions.
- the resulting symptom is `409 capability_unsupported: send_input` on all 40
  retries, not a deadline that wanted lengthening

SUGGESTED FIX

`RmuxControl` already carries `rmux_bin` and `session_target`. Either kill the
session before aborting the tasks, or add a CLI `kill-session` fallback for when
the SDK transport is already gone. Prefer both: the CLI path also cleans up
sessions leaked by earlier builds.

Also make the failure visible. A kill that fails is currently a warn on a path
nobody watches; it should be an error the release path surfaces.

NOTE ON CURRENT MACHINE STATE: the accumulation was cleared incidentally during
this session (rmux servers were killed during unrelated cleanup), so a check
right now reports 0 sessions and no running daemon. That is not the defect being
fixed — it will re-accumulate from the next rmux run.

Downstream: this is the production root cause of
`api::post_artifact_regenerate_hot_path_reuses_live_run_id` flaking, which is why
TASK-STWVB deliberately did not patch that test.
INDEPENDENTLY CONFIRMED [2026-07-26] by the manager, on different runs than the
implementer's, from a known-empty daemon.

23 leaked `orgasmic-rmux-run-*` sessions before one
`cargo test -p orgasmic-daemon --lib` run, 31 after: **+8 per run**, matching the
implementer's 8/run exactly. rmux-daemon fds 47/256. The 23 had accumulated from
three earlier verification runs in the same hour (~7.7/run) starting from zero,
so this is a clean growth curve rather than an inherited pile.

CONSEQUENCE FOR MEASUREMENT, not only for users: at 8 sessions per run,
saturation (~236 sessions / 253 fds) arrives after roughly 29 more full-suite
runs. Any 'N consecutive green runs' gate on this crate therefore measures a
monotonically degrading environment — later runs are strictly likelier to fail
than earlier ones for reasons unrelated to the change under test. That is why
TASK-STWVB's implementer saw its only green streak in runs 1-2 of a 10-run gate
and none after, and why that gate should be re-run only once this lands.
** Evidence
Implemented by implementer-codex-acp-stdio, run-20260726T102724, commit d60c4ed, merged --no-ff to main.

THE FIX (both halves kept)
Ownership hypothesis confirmed by the implementer: Session and the lifecycle
watcher pane retain clones of ONE ordered SDK transport after the local Rmux
facade drops, so aborting a pending pane request poisons the later kill.
release() now reaps first, then aborts/joins. A bounded 5s `rmux kill-session -t`
CLI fallback covers a transport that is already gone. The Drop backstop shared
the same ordering defect and was fixed the same way; kill_on_drop == false still
takes the no-kill branch, so reattached and system-wide sessions are never
implicitly reaped.

IMPLEMENTER MEASUREMENT (controlled)
Unfixed: 31 -> 36 sessions across one `cargo test -p orgasmic-daemon --lib`
(+5). Fixed: 36 -> 36 (+0), before/after session-name set difference empty.
Regression test fails (exit 101, "rmux session survived explicit release") on
restored unfixed ordering and passes 1/1 with the fix.

MANAGER MEASUREMENT (independent, on the merged tree, different runs)
Before 37, after 38 (+1). The single new session is
orgasmic-rmux-run-20260726T105347-034ff6ca..., which is the exact run_id the
FAILING api::post_artifact_regenerate_hot_path_reuses_live_run_id asserted on
("left: run-20260726T105347-034ff6ca..."). It is a run abandoned by a panicking
test, not a leak on the release path. Production release growth is 0, down from
the +8/run recorded in the heading.

GATES ON THE MERGED TREE
- cargo test -p orgasmic-drivers: lib 231/231, cursor_agent 5/5,
  rmux_release_reaps_session 1/1 (ran for real, did not print SKIPPED)
- cargo clippy --workspace --all-targets -- -D warnings: PASS
- cargo fmt --all --check: PASS
- cargo test -p orgasmic-daemon --lib: 500 passed, 2 failed, both declared
  known-red and untouched

WHAT THIS DID NOT FIX, contrary to the hypothesis in the heading
api::post_artifact_regenerate_hot_path_reuses_live_run_id STILL FAILS on the
merged tree (two consecutive rounds got different run_ids). The implementer saw
it pass once in a fixed run and reported that; the manager then saw it fail. The
heading calls this defect the production root cause of that flake — that claim is
now disconfirmed, not confirmed. Do not close TASK-STWVB on the assumption the
flake is gone; it needs its own root cause.

UNRELATED DEFECT SURFACED BY THESE GATES
driver_modes::legacy_drivers_and_explicit_pairs_emit_equivalent_start_events now
fails on a claude session-id collision. Filed as TASK-099T6. It is fallout from
TASK-SGRTX making --session-id actually reach the process, not from this task.

RESIDUAL RISK (manager review of the diff)
1. supervisor.rs:3988 wraps release in its own 5s timeout and discards the
   result (`let _ = tokio::time::timeout(...)`). The new Err return therefore
   reaches no caller on the production path, so "make the failure visible" is
   only half met — the value is returned but nothing reads it.
2. Nested 5s budgets: the outer timeout equals the CLI fallback timeout, so a
   slow SDK kill leaves the fallback no room to run.
3. release() now latches terminal_emitted BEFORE the reap. If the outer timeout
   cancels release mid-reap, RunComplete is never emitted by anyone. The drain
   still writes Lifecycle::Release (supervisor.rs:3976-3981), so the run is not
   stranded, but its summary event is lost.
These three are for the reviewer to rule on; none blocked the merge.

* IN_REVIEW TASK-Z3093 LiveSessionGuard serializes but never reaps, so every failing live-session test leaks an rmux session :testing:rmux:leak:production:
:PROPERTIES:
:ID: TASK-Z3093
:PRIORITY: P1
:test_cmd: cargo test -p orgasmic-daemon
:END:

`LiveSessionGuard` serializes live-session tests and reaps nothing. Every one of
its 9 copies is the same flock-only type whose `Drop` calls `fs2::FileExt::unlock`
and returns. Cleanup of the rmux session the test created is a *trailing
statement* in the test body, so a panic anywhere above it skips the reap and the
session, its pty and its harness process survive the test binary.

This is the open source of the TASK-8W30B leak that a production-path fix cannot
close. TASK-6FNAY hardened `RmuxControl::release` (merged 83ea420) and the leak
still reproduced afterwards: two orphan sessions found 2026-07-28 at 13:20,
ages 3h18m and 2h46m, both running
`orgasmic_daemon-e5f5d4f31537ab72.fixture.sh artifact-ready`, both 404 to the
daemon and absent from every ledger, i.e. from test fixtures rather than from a
dispatch (tx-20260728-orgasmic-3117). Earlier the same day three more were found
at 09:57 (tx-20260728-orgasmic-3103, refiled onto 8W30B by
tx-20260728-orgasmic-3110).

** Measured inventory

- 9 definitions of `struct LiveSessionGuard(std::fs::File)`, byte-identical in
  behaviour, across 3 crates:
  - `crates/orgasmic-daemon/src/api.rs:15772`
  - `crates/orgasmic-daemon/src/supervisor.rs:5478`
  - `crates/orgasmic-drivers/src/modes/rmux.rs:2329`
  - `crates/orgasmic-drivers/src/modes/tmux.rs:2481`
  - `crates/orgasmic-drivers/tests/rmux_release_reaps_session.rs:23`
  - `crates/orgasmic-cli/src/test_support.rs:37`
  - `crates/orgasmic-cli/tests/dispatch.rs:38`
  - `crates/orgasmic-cli/tests/list_output.rs:33`
  - `crates/orgasmic-cli/tests/bootstrap_smoke.rs:46`
- 85 `live_session_guard()` call sites. Heaviest: `cli/tests/dispatch.rs` 30,
  `daemon/src/api.rs` 19, `drivers/src/modes/rmux.rs` 12,
  `drivers/src/modes/tmux.rs` 12.
- All 12 artifactor tests in `api.rs` that call `seed_test_artifactor_harness` /
  `seed_busy_artifactor_harness` spawn a real rmux session. 11 end with a
  trailing `supervisor.release(..)` or `release_with_finalization(..)`; one,
  `launch_artifact_generation_persistence_failure_releases_run`
  (api.rs:27530), has no release in the test body at all and relies on the
  production path releasing for it — confirm that it actually does.
- The fixture never exits on its own. `artifact-ready` loops
  `echo '> ready'; read -r line || exit 0`, and inside an rmux pane stdin never
  closes, so an unreaped session runs until something kills it
  (`crates/orgasmic-daemon/src/test_fixtures.rs:20`).

** Why this compounds

TASK-STWVB records four daemon tests that fail on every full-suite run and pass
alone, and the TASK-C0XMR merge note (`0860861`) says that family gained two more
members under load. A load-induced failure is a panic, and a panic is exactly the
path that skips the trailing release — so **the flake and the leak are the same
event**. Every dispatched worker runs the full suites, so leak rate scales with
dispatch volume, which is what the two-per-day observation shows.

A leaked session also outlives its worktree: pid 42520 still named
`.orgasmic/tmp/dispatch/task-C0XMR/worktree`, a directory `dispatch-close` had
already removed.

** Fix

One shared guard that owns cleanup, not nine that own a file lock.

- Give the guard a registration surface (`guard.owns(run_id)` /
  `guard.owns_session(name, endpoint)`) and reap on `Drop`, unconditionally,
  before releasing the flock. Drop runs on the panic path, which is the whole
  point.
- The reap must not itself depend on a live SDK transport — reuse the CLI
  fallback shape TASK-6FNAY established (`rmux_session_reap_args(endpoint,
  name)`, rmux.rs:1505), addressed at the endpoint the handle owned. A `Drop`
  cannot await, so the reap has to be a blocking call or a spawned-and-joined
  helper; pick one and say why.
- De-duplicate: one definition in a shared test-support location that all three
  crates use. Nine copies is how a fix to one of them silently misses the other
  eight.
- Keep the flock behaviour exactly as it is. Serialization is load-bearing and
  independent of the leak.

** Acceptance

- A live-session test that panics mid-body leaves no session behind — proven by
  a test that deliberately panics inside the guard's scope and then asserts the
  session is gone.
- `rmux ls` is empty after a full `cargo test -p orgasmic-daemon` and after
  `cargo test -p orgasmic-cli` on a machine that started with no sessions.
- One `LiveSessionGuard` definition remains in the tree.
- `launch_artifact_generation_persistence_failure_releases_run` either releases
  through the production path (demonstrated) or gains a guard registration.

** Non-goals

- Do not change `RmuxControl::release`; TASK-6FNAY owns the production path and
  is in review.
- Do not fix the TASK-STWVB flakes themselves. This task makes their cost
  bounded, not zero.
- Do not run `legacy_drivers_and_explicit_pairs_emit_equivalent_start_events`
  (TASK-099T6) — it bills real money. Skip it explicitly on the drivers suite.

* IN_REVIEW TASK-QSSQH C0XMR fix round: a genuine RunFail must survive a later worker finalize :daemon:stage:fix:
:PROPERTIES:
:ID: TASK-QSSQH
:PRIORITY: P0
:test_cmd: cargo test -p orgasmic-daemon
:write_scope: crates/orgasmic-daemon/src/api.rs crates/orgasmic-daemon/src/supervisor.rs
:END:

Fix round for the reviewer BLOCK SHIP on TASK-C0XMR (merged `0533735`).
Reviewer: reviewer-codex-rmux (rmux / codex / gpt-5.6-sol / high),
run-20260728T103249, closed tx-20260728-orgasmic-3139.

The merged change is NOT reverted. What it got right, confirmed by review:
session ordering on the normal path is persisted JSONL order and the
implementation does **not** compare wall-clock timestamps; the same outcome
function serves grill, architect and plan, so the fix is not architect-specific
and does not depend on `architector.done`; duplicate CLI finalize is already
convergent at the release/tx boundary (release removes the live run, retries
verify the durable worker-finalize tombstone, the terminal tx has a deterministic
request id) — no idempotence defect exists. All gates passed, including the real
production-path test `stage_architect_finalize_from_orgasmic_run_id_on_main`.

The defect is the one the brief named as worse than the original bug, and it is
in this diff.

** HIGH — a genuine terminal failure followed by a worker finalize is turned green

`crates/orgasmic-daemon/src/api.rs:6098,6141-6149`.

`DriverEvent::RunFail` is a real failure: the production drain maps it to
`ReleaseOutcome::Failed` and stores it on the run
(`supervisor.rs:3429-3463`). Explicit release deliberately waits for that drain
and persists the stored terminal outcome, so a finalize racing a queued `RunFail`
produces the authoritative shape
`Release { outcome: Failed, finalized_by_worker: true }`
(`supervisor.rs:2181-2228`).

The new scan sets `pending_failure` and then clears it **unconditionally** on
`finalized_by_worker`, inspecting neither the failure kind nor the release
outcome, and returns `StageOutcome::Completed`. The stage watcher then writes
`<stage>.completed` for a run the supervisor classified as failed.

The added negative test does not cover the claimed case:
`stage_outcome_fails_when_a_fatal_driver_error_has_no_worker_finalize_after_it`
uses `finalized_by_worker: false`, while the adversarial case is failure **plus**
finalize.

Required (reviewer's direction 1): make semantic failure sticky across worker
finalize. At minimum `RunFail` must survive a later finalize. For fatal driver
errors, persist enough release-boundary or classification data to suppress only
the errors *caused by* the finalize teardown, rather than clearing every earlier
failure. Add a regression for
`RunFail -> Release { outcome: Failed, finalized_by_worker: true }`, plus a
production-path race test.

** Open question the reviewer raised, and it is the crux

What durable session event should identify the finalize-admission boundary? The
trailing release cannot distinguish a queued genuine `RunFail` from a fatal error
caused by teardown, because **both are drained before that release**. Answer this
explicitly before writing the fix; a suppression rule that cannot name its
boundary will get one of the two cases wrong. Record the answer in the task or a
decision, not only in a code comment.
The reviewer is right that the trailing release cannot carry the boundary, and
the evidence is stronger than "cannot distinguish": the two cases produce a
byte-identical release. `terminal_outcome_for_event` maps `RunFail` and fatal
`DriverError` to the same `ReleaseOutcome::Failed`
(`supervisor.rs:3429-3436`), both are drained before `release_one` freezes
`rec.terminal_outcome.unwrap_or(outcome)`, so a genuine queued `RunFail` and a
teardown-induced fatal error both write
`Release { outcome: Failed, finalized_by_worker: true }`. The release is not a
weak discriminator; it is not a discriminator at all. Asserted directly by the
production-path test pair
`worker_finalize_does_not_clear_a_run_fail_drained_during_teardown` and
`worker_finalize_still_suppresses_the_error_its_own_teardown_caused`, which
assert the identical release shape and opposite stage outcomes.

The boundary is two rules, not one, because no single durable event answers
both halves.

1. KIND, for semantic failure. `RunFail` is only ever emitted from a harness
   protocol result — Claude `is_error`, a failed codex turn, a cursor stop
   reason. Teardown reaps the process group, and every transport classifies
   that as a fatal `DriverError` (`classify_pane_exit`,
   `orgasmic-drivers/src/modes/rmux.rs:1901-1959`); nothing synthesizes a
   `RunFail`. So `RunFail` is sticky across a later worker finalize,
   unconditionally and regardless of position. Position must not matter: a
   `RunFail` still queued when the finalize is admitted is drained AFTER the
   admission marker, so a marker-only rule would clear exactly the case this
   task exists to fix.

2. A durable ADMISSION MARKER, for driver errors. The supervisor appends a
   `SessionEventKind::Note` carrying `{"note": "worker_finalize_admitted"}` at
   the instant it grants finalize admission — after
   `explicit_release_in_progress = true`, before
   `stop_and_join_driver_producer`, awaited so it is on disk first
   (`supervisor.rs`, `release_one`). Teardown cannot produce anything before
   it. A fatal driver error after the marker is one this finalize caused and
   stays suppressed; one before it was already there and is sticky.

Why a new event rather than an existing one: the release is the only other
durable session event on this path, and it is appended after the drain, so
every candidate discriminator is already behind it. This is a session-schema
addition, but not a `Lifecycle` variant change — `Note` is the existing
free-form supervisor channel, so `orgasmic-core` is untouched and every reader
that filters by kind ignores it.

Backward compatibility: sessions with no marker (written before this change, or
where the append failed and logged) keep the TASK-C0XMR reading for driver
errors — dominance by the following finalize. Rule 1 still applies to them, so
the reported HIGH is fixed for old sessions too.

If a third case appears later, the question to ask is which of the two rules it
belongs to: is the failure semantically declared by the harness (rule 1, sticky
by kind), or is it a transport/process artifact whose blame depends on whether
this finalize caused it (rule 2, sticky unless after the marker)? A signal that
fits neither means a new durable event is needed, not a new heuristic on the
existing ones.

** Acceptance

- A run with a queued `RunFail` that is then finalized by its worker is recorded
  as failed, not completed.
- A teardown-induced error after a clean finalize is still suppressed — the
  original C0XMR symptom must not regress.
- Both cases have regressions, and the failure case exercises
  `finalized_by_worker: true`.
- The finalize-admission boundary is named and durable, not inferred from
  ordering alone.

** Non-goals

- Do not revert `0533735`.
- Reviewer finding 2 (a live stage loses its completion watcher across daemon
  restart) is **out of scope** and filed separately. It is pre-existing and
  outside this diff.

* IN_REVIEW TASK-Q07Y5 WGXKD.2 fix round: bound writer shutdown and derive ExitTimeOut from the real budget :dispatch:daemon:cli:fix:
:PROPERTIES:
:ID: TASK-Q07Y5
:PRIORITY: P0
:test_cmd: cargo test -p orgasmic-cli
:write_scope: crates/orgasmic-cli/src/daemon_service.rs crates/orgasmic-cli/src/daemon_lifecycle.rs crates/orgasmic-cli/tests/daemon_lifecycle.rs crates/orgasmic-daemon/src/lib.rs crates/orgasmic-daemon/src/writer.rs
:END:

Fix round for the reviewer BLOCK SHIP on TASK-WGXKD.2 (merged `6a518f3`).
Reviewer: reviewer-codex-rmux (rmux / codex / gpt-5.6-sol / high),
run-20260728T103240, reported tx-20260728-orgasmic-3136, closed
tx-20260728-orgasmic-3137.

The merged change is NOT reverted. Its central claim held under review: `try_admit`
checks `closed` and inserts the admission under the same `watch::Sender` write
lock, `post_run_release` acquires it before its first await, the admission moves
into the detached task so cancellation retains ownership through `Drop`, and no
sole handle is taken before a cancellable await. `daemon restart` does parse the
real `/daemon/restart` response with a 40s client ceiling, SIGTERM and Ctrl+C both
route through `RunningDaemon.shutdown`, and the macOS plist is rewritten on every
service start (`start_macos_launch_agent` writes it before bootstrap), not only on
install. All independent gates exited 0 at `ca3f9df`.

What follows is what the round claimed and did not establish.

** 1. HIGH — `ExitTimeOut 60` is not larger than the worst-case shutdown, because
   half of that shutdown has no budget at all

`crates/orgasmic-cli/src/daemon_service.rs:707-727`,
`crates/orgasmic-daemon/src/lib.rs:665-688`,
`crates/orgasmic-daemon/src/writer.rs:513-522`.

After SIGTERM, `RunningDaemon` waits up to 20s for release finalizations and then
calls `WriterHandle::shutdown().await`. That call queues behind prior writer
commands and awaits its reply **with no timeout**. `RESTART_WRITER_DRAIN_TIMEOUT`
bounds only the pre-stop marker, not `WriterHandle::shutdown`.

Concrete failure: a terminal-tx append is still blocked in writer I/O or fsync
when the 20s release drain expires; shutdown then waits behind that append, and
launchd sends SIGKILL at 60s while the append is still non-durable. That
recreates the released-but-unreported loss the whole WGXKD family exists to
prevent.

The comment at daemon_service.rs:707 reasons "drain (20s) + writer shutdown < 60",
but the second term is unbounded, so the inequality is unprovable rather than
generous. The budget test added in this round checks 40 > 20 + 5 and 35 > 20 — it
never proves the plist timeout covers writer shutdown.

**This is live on the operator's machine right now.** The plist was rewritten with
`ExitTimeOut 60` on 2026-07-28 (tx-20260728-orgasmic-3118); the window is wider
than it was, and still not closed.

Required: give writer shutdown an explicit finite budget and a durable timeout
outcome, then **derive** the service-manager timeout from release drain + writer
shutdown + margin rather than hard-coding 60. If the derived number differs from
60, the plist must carry the derived one, and a test must assert the plist value
is the derived value — not a literal.

** 2. LOW — the production lifecycle composition still has no end-to-end regression

`crates/orgasmic-cli/src/daemon_lifecycle.rs:1498-1563`,
`crates/orgasmic-cli/tests/daemon_lifecycle.rs:99-181`.

The >5s test uses a stand-in HTTP server and stops after parsing its response; the
SIGTERM test has no in-flight release. Both pass even if the real `daemon restart`
sequence parses the drain response and then has launchd cut off the old daemon
during writer shutdown — which is exactly finding 1.

Required: drive the actual lifecycle restart path with a release lasting beyond
five seconds, including service stop and replacement bind. Testing the HTTP wait
and the SIGTERM routing in isolation is what let finding 1 through.

** Acceptance

- `WriterHandle::shutdown` has a finite budget and a defined outcome on timeout;
  the outcome is durable or recoverable, not silent.
- The macOS `ExitTimeOut` is derived from the drain and writer budgets, and a test
  asserts the rendered plist carries the derived value.
- A SIGTERM test holds a terminal-tx append in flight through the release timeout
  and proves either durability before exit, or a durable run-specific recovery
  record written before the watchdog can kill the daemon.
- An end-to-end restart regression covers service stop and replacement bind with a
  release that outlasts five seconds.

** Non-goals

- Do not revert `6a518f3`. Its atomic-admission fix is sound and reviewed.
- Reviewer finding 2 (old CLI → new daemon terminal-tx race) is **out of scope**
  and filed separately. It is pre-existing from TASK-WGXKD.1 and the reviewer
  states it does not block this round.

* IN_REVIEW TASK-1T3FZ 6AYEJ.3 fix round: destructive close must hold a daemon-owned worktree reservation across the liveness decision :dispatch:cli:daemon:fix:race:
:PROPERTIES:
:ID: TASK-1T3FZ
:PRIORITY: P0
:test_cmd: cargo test -p orgasmic-cli
:write_scope: crates/orgasmic-cli/src/manager.rs crates/orgasmic-cli/tests/dispatch.rs crates/orgasmic-cli/tests/shipped_conventions.rs crates/orgasmic-daemon/src/supervisor.rs crates/orgasmic-daemon/src/api.rs
:END:

Fix round for the reviewer BLOCK SHIP on TASK-6AYEJ.3 (merged `edc68b4`).
Reviewer: reviewer-codex-rmux (rmux / codex / gpt-5.6-sol / high),
run-20260728T103245, closed tx-20260728-orgasmic-3146.

The merged change is NOT reverted, and its enumeration work held under review.
Confirmed: every reachable replacement constructor associates —
`reattach_tmux` preserves the origin id (`api.rs:8085-8135`), and fresh
`resume_native_fork` / `start_recovery_run`, pending-plan reattach,
committed-claim replay and pending-to-committed replay all call the replacement
association helper (`api.rs:7664-7711,8377-8508`). No missing path was found.
Generation binding from `.1` and mandatory close from `.2` both still hold: an
exact `--started-tx` selects one generation, a tokenless close refuses every open
candidate (`manager.rs:3650-3705`), and a present-but-unmatched report `RUN_ID`
still returns without task fallback (`manager.rs:3951-3960`). The real-daemon
crash matrix passed 3/3 including the exactly-one-durable-association assertion.

The refusal-to-clean-up-beneath-a-live-run half is what did not hold.

** 1. HIGH — destructive close has a recovery/acquire TOCTOU across a process
   boundary, and can still remove a live worker's worktree (in this diff)

`crates/orgasmic-cli/src/manager.rs:723-791`,
`crates/orgasmic-daemon/src/supervisor.rs:893-905,2763-2885`.

`dispatch-close` releases the recorded run (or takes its 404), optionally
snapshots `/runs` at lines 747-750, and only then runs
`cleanup_dispatch` / `git worktree remove` at line 791 — **in the CLI process**.
Nothing reserves the normalized worktree in daemon state across that gap.

A concurrent `POST /runs/:origin/recover` can acquire a replacement after the
release/snapshot and before local removal. `Supervisor::acquire_impl` blocks only
daemon-owned `cleanup_reservations`, and this close path installs none. The close
then removes the directory beneath a live replacement — precisely the TASK-8W30B
symptom this round was filed to prevent, and precisely the shape observed on
2026-07-28 when a leaked session still named
`.orgasmic/tmp/dispatch/task-C0XMR/worktree` after `dispatch-close` had removed
it (tx-20260728-orgasmic-3117).

Critically: **this is not cured by removing `await` points.** The competing
recovery runs in another request, in another process. An in-process check-then-act
audit will conclude the code is safe and be wrong.

The daemon already demonstrates the required authority boundary:
`prepare_dispatch_cleanup` installs a worktree reservation **under the supervisor
lock, before any await**, and retains it through filesystem mutation. Follow that.

The new integration tests (`crates/orgasmic-cli/tests/dispatch.rs:2887-3098`)
cover a replacement already live before close, and a static unassociated run
before the snapshot. Neither interleaves a recovery **between the liveness
decision and the removal**, which is the only ordering that reproduces this.

Required (reviewer direction 1): move destructive generation-close into a
daemon-owned operation, or extend the existing dispatch cleanup endpoint, that
installs a normalized-worktree cleanup reservation under the supervisor lock
before releasing or declaring absent any run, and holds it until worktree and
branch cleanup completes. Recovery and acquire must observe that reservation. Add
a deterministic hook test that starts recovery **after** the liveness decision and
**before** removal.

** 2. HIGH — a dispatch with no recorded RUN_ID fetches authoritative live state,
   discards it, and cleans up anyway (pre-existing)

`crates/orgasmic-cli/src/manager.rs:770-791`.

Real shape: `manager.dispatch_started` is durable, the daemon acquire succeeded,
and the CLI died before appending `run.created`. The open record then has
`run_id=None` while a worker is live in its worktree. The legacy branch calls
`fetch_live_runs` **only to prove the daemon is reachable**, ignores the returned
runs, and proceeds to `cleanup_dispatch`. The worker stays live while its worktree
is removed.

This violates the rule the brief stated explicitly: undetermined liveness must
refuse, not permit. It predates `edc68b4` so it would not independently block the
round — but finding 1 already does, and the fix is the same reservation path.

Required (reviewer direction 2): route no-`RUN_ID` records through the same
reservation and ownership check. Never treat daemon reachability as proof that an
unidentified live worker is absent.

** 3. LOW — the semantic dangerous-command guard reads only the first `git` token
   per segment (in this diff)

`crates/orgasmic-cli/tests/shipped_conventions.rs:168-200`.

`tokens.iter().position(...)` selects one `git`, classifies only what follows it,
and never continues to a later invocation. Compiled probes of the production
helper returned:

- `git worktree remove $WT --force` => 1 hit
- `run git status, then git worktree remove $WT --force` => 0 hits
- `git \` + continued `worktree remove $WT --force` => 0 hits
- `/usr/bin/git worktree remove $WT --force` => 0 hits

So any ordinary safe `git` mention before a forced removal makes the guard
vacuous. In the current convention the marked forced removal is **not** what keeps
the guard green — the later `git branch -D` after `&&` is what creates the hit.
The guard is passing by accident.

Required (reviewer direction 3): iterate every candidate `git` invocation in each
segment, or use a quote/continuation-aware tokenizer; normalize path-qualified
`git`; ignore structural continuation tokens. Add the three zero-hit probes above
as regressions.

** Acceptance

- A recovery interleaved between the close's liveness decision and its removal
  cannot remove the worktree — proven by a deterministic hook test, not by
  reasoning about `await` points.
- A no-`RUN_ID` open record with a live worker refuses cleanup.
- The three zero-hit probes above become hits.

** Non-goals

- Do not revert `edc68b4`; its replacement-association work is sound and reviewed.
- Do not weaken generation binding or the mandatory-close rule from `.1` and `.2`.

