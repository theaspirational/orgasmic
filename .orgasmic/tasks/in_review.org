#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-FZB6T P1 compact run catalog and bounded semantic session retention :storage:recovery:sessions:
:PROPERTIES:
:DEPENDS_ON: TASK-KWSTJ
:ID: TASK-FZB6T
:PRIORITY: P1
:END:

Separate operational lifecycle inventory from transcript/evidence storage so run enumeration and daemon recovery scale with record count, not transcript bytes. This task follows the P0 wire repair and owns the durable shape rather than adding more per-request scans.

Design and implementation:
1. Maintain a compact per-run catalog entry containing identity, project/task/kind, driver/harness, verified native metadata reference, worktree authority state, lifecycle classification, terminal outcome/time, and replacement/claim link. Update it through the existing session writer/supervisor boundary; rebuild legacy entries once with bounded prefix/tail parsing and cache them by file identity.
2. `/api/runs` serves actionable live/recoverable records plus a bounded recent terminal window from the catalog. Older terminal history is paged or queried explicitly; it is never reclassified on every poll. Missing/pruned worktrees become a stable tombstoned authority state, not an eternal ambiguous attach candidate.
3. Lock future storage: rendered tmux/rmux redraw or scrollback events remain forbidden; add a regression proving a long TUI session does not grow orgasmic JSONL from screen repaint traffic. For structured ACP/subprocess events, retain lifecycle and compact semantic tool/progress events needed for recovery and retrospective analysis, but cap individual text/tool-result/thinking payloads and replace overflow with digest, byte count, and source reference. Never copy harness-native JSONL automatically.
4. Add `run history inspect` and an explicit dry-run-first maintenance command to compact or archive legacy rendered-TUI payloads. It must report files/bytes by driver+harness+event class, preserve lifecycle/native correlation, write recoverably, and require explicit operator confirmation before deletion or irreversible compaction. No automatic cleanup of existing 2.239 GiB.
5. Define default retention by authority: lifecycle/catalog kept; compact semantic events retained within a documented budget; derived retro evidence cache is disposable; vendor-native history remains vendor-owned.

Acceptance: inventory latency and boot reconciliation are independent of payload bytes after the one-time legacy index; 197-file production-shaped history yields the same actionable classifications before/after catalog migration; current TUI generates zero persisted redraw chunks; ACP/tool evidence remains sufficient to count calls/results/retries/latencies while oversized outputs are bounded; dry run exactly accounts for reclaimable legacy bytes and changes nothing. Include migration rollback and corruption tests.

=== Evidence (manager, 2026-07-29)
Merged 1b726f6. The hardest of the three rescues: the reboot left two commits
on the branch plus a substantial UNCOMMITTED refactor in the worktree
(api.rs, run_catalog.rs, writer.rs — 136 insertions / 166 deletions) making
=RunCatalog= synchronous and removing a =Handle::current().block_on= re-entry
from inside =spawn_blocking=, which is a deadlock hazard on a blocking-pool
thread.

That uncommitted work was salvaged rather than discarded. It compiled clean on
first check (=cargo check -p orgasmic-daemon --all-targets=, exit 0), so the
manager committed it on the task branch (bf1f8cb) purely to get a tree clean
enough to replay the proof — and the proof, not the compile, is what accepted
it.

Manager-replayed proof, all three shipped artifacts, each PASS with red at exit
101 matching its pinned signature, tree byte-identical after revert, green at
exit 0:
- =orgasmic verify TASK-FZB6T= — inventory cost independent of transcript
  bytes, identical classification before/after the catalog, boot reconciliation
  reading no session bytes
- =orgasmic verify TASK-FZB6T-corruption=
- =orgasmic verify TASK-FZB6T-redraw=
All three replayed against fully-merged main and PASSED there too.

Gates on merged main: fmt clean; clippy -D warnings clean; daemon lib suite 617
passed / 7 load flakes, all green in isolation.

NOT independently reviewed: manager integration review + injection replay only.
The rescued refactor in particular reached main without its author ever
reporting on it, which is worth weighing if anything in the run-catalog path
misbehaves.
** Notes
Handoff from TASK-KWSTJ: the bounded legacy fallback this task's item 1 calls for already exists as `orgasmic_core::scan_session_lifecycle` (prefix/tail byte window, transcript lines rejected from raw bytes without parsing, `truncated` and `final_envelope_retained` flags for conservative classification). Measured on the real 198-file / 2.239 GiB board: 26.67 MiB inspected in 95.3 ms versus 3.08 s for a full read. The durable catalog can be built by caching that scan's result by file identity rather than by inventing a second reader. `InventoryStageMetrics` on `GET /api/runs` gives the before/after signal for the catalog work.

* IN_REVIEW TASK-4CSMY The pane open-turn evidence channel is rmux-only, so tmux dispatches stay exposed to the 600s stall-kill :dispatch:daemon:hardening:
:PROPERTIES:
:ID: TASK-4CSMY
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-daemon/src/supervisor.rs, crates/orgasmic-drivers/src/modes/tmux.rs, crates/orgasmic-drivers/src/modes/rmux.rs, verify/TASK-4CSMY/, verify/TASK-JQ8AV/
:END:


=== WIDENED at filing (manager, 2026-07-29): the primary channel is missing too

Further measurement while scoping the tmux default switch. The gap is not only
JQ8AV's rescue channel — the PRIMARY pane-liveness channel is absent on tmux:

- =pane_activity= is emitted by =crates/orgasmic-drivers/src/modes/rmux.rs=
  and by nothing else. A grep for the event across =crates/orgasmic-drivers/src/=
  returns rmux hits only.
- =modes/tmux.rs= (4668 lines) sends =DriverEvent::Ready=,
  =TransitionState=, =ToolCall= and the fatal-error paths. There is no
  continuous pane-output event of any kind.

So on a tmux transport the stall clock sees driver events only when the worker
happens to call a tool or change state. Between those, evidence falls back to
the subtree CPU probe alone. A CPU-hot stretch (a long =cargo test= under the
pane) is still classified Working, so the common long-tool case survives. The
fatal case is the provider-bound think: low CPU, no tool calls, no pane event,
and — because JQ8AV's consult is gated on =transport == "rmux"= — no rescue
either. That is a 600s kill of a healthy worker with every channel blind.

This is why TASK-RWCRN's history matters: rmux only became safe when it grew
=pane_activity=. tmux has never had that, so it is where rmux was BEFORE the
fix, not merely missing the JQ8AV refinement.

ORDERING CONSEQUENCE. The operator has directed that tmux become the shipped
default driver, on the sound reasoning that a first-time user may have only
tmux. That makes this task a SHIP BLOCKER rather than a hardening nicety: with
the default flipped and this unfixed, the users least equipped to diagnose it
get healthy workers killed at ten minutes. Land this before the default flips;
dogfood on tmux in the meantime with
=--governance-json '{"stall_timeout_secs":3600}'= on every long-thinking
dispatch.

Work item 0, ahead of the others: give the tmux driver a coalesced
=pane_activity= equivalent to rmux's, with rmux's live tests ported
(=live_rmux_pane_publishes_pane_activity_while_it_writes= and the
newline-free-redraw variant) onto a test-owned tmux server. Only then does the
open-turn-statusline consult of items 1-3 become a refinement rather than the
sole channel.

** Description
TASK-JQ8AV landed a third work-evidence channel: at the stall deadline, when
subtree CPU is below the work threshold, the probe captures the pane and looks
for the TUI's open-turn statusline. A provider-bound turn is then classified
=Working= instead of being stall-killed.

That channel is gated on =target.transport == "rmux"= (supervisor.rs, in
=ProcessSubtreeCpuProbe=). On a =tmux= transport the consult never runs, so the
classification falls back to CPU alone — which is exactly the state that killed
FZB6T, RB1ZN and SZJ2B at 600.1s during long claude-opus-5 high-effort thinks.

This matters now because the operator has directed that orgasmic ship =tmux= as
the default driver (first users may have only tmux installed, so tmux is what
must be hardened). Dispatching opus-5 high-effort work over tmux before this
lands reproduces the original incident.

=== Work
1. Generalise the pane consult from rmux to any pane transport. =rmux= and
   =tmux= share the =capture-pane -p -t <session>= surface, so the marker
   classifier (=pane_open_turn_marker=) is reusable verbatim; what differs is
   the binary, the session-name derivation, and the socket/endpoint flag.
2. Keep the fail-closed ordering JK66P set and JQ8AV preserved: pane unreadable
   must never rescue a run, only report =pane capture unavailable=. The channel
   may flip Idle->Working and nothing else.
3. Extend the acceptance test to the tmux transport. JQ8AV's
   =a_network_waiting_provider_turn_survives_the_stall_window_and_a_wedge_dies=
   drives a REAL pane on a test-owned server; the tmux half needs the same
   shape on a test-owned tmux server, not a stubbed capture.
4. Do NOT widen the rescue to non-pane transports. acp-* transports already
   stream turn events into the stall clock and need no pane channel.

=== Acceptance
- A network-waiting provider-bound turn on a =tmux= transport survives repeated
  expired stall budgets, proven with a real tmux pane.
- An at-rest, connectionless wedge on =tmux= still dies on schedule, with a
  reason naming both empty channels.
- The rmux behaviour JQ8AV shipped is unchanged (its verify artifact still
  replays red-then-green).
- A shipped injection proof under verify/, per the project bar.
** Evidence
Merged =7d9c60c= (impl =75255e5=, branch =task-4csmy-impl=, base =9017514=).
Implementer =implementer-claude-tmux=: driver tmux, harness claude, model
claude-opus-5, effort high. Dispatch =tx-20260802-orgasmic-4044=.

MANAGER GATES on the merge, run independently of the worker's report:

| gate | result |
| =cargo fmt --all --check= | PASS |
| =cargo clippy --workspace --all-targets -- -D warnings= | PASS |
| =orgasmic verify TASK-4CSMY= | PASS — red matches pinned signature, then green, tree byte-identical |
| =orgasmic verify TASK-JQ8AV= | PASS — the re-cut artifact still replays red-then-green |
| =scripts/run-tests.sh= | RED, and NOT attributable to this change — see below |

THE SUITE RED, MEASURED RATHER THAN ASSUMED. The project bar is a green suite,
and this environment does not currently produce one. Three runs, because a
single red proves nothing about its own cause:

| # | site | tree | real failures |
| 1 | repo root | =7d9c60c= (the merge) | 8 |
| 2 | repo root | =9017514= (BASE, pre-merge) | 4 |
| 3 | dispatch worktree | =75255e5= (tree-identical to the merge) | 2 |

Run 2 is the control the flake convention requires: the SAME worktree at the
BASE commit. It is red too, so "this change broke the suite" is refuted — four
failures predate the diff entirely
(=explicit_reattach_rmux_is_fenced_out_of_a_worktree_a_close_guard_holds=,
=recovery_reattaches_rmux_session_when_handle_exists=,
=direct_child_pid_finds_wrapper_child_process=,
=process_subtree_cpu_probe_sees_a_real_cpu_burning_child=).

Run 3 settles the remaining four, which failed ONLY on the merge in run 1
(=recovery_inventory_waits_for_atomic_claim_commit=,
=live_rebuild_preserves_repo_url_and_schedules_post_bind_refresh=,
=dead_pid_aborts_joins_hung_producer_then_receiver_releases=, and this task's
own new =a_network_waiting_tmux_turn_outlives_the_stall_window_and_a_wedge_dies=).
On a byte-identical tree at another site, NONE of the four reproduced — the new
test passed. They also each passed in isolation in run 1. So they are
site/load-dependent, not diff-caused.

Every failure across all three runs also passed in isolation under
=run-tests.sh='s own re-run.

WHAT THE MATRIX SAYS ABOUT THE FLAKE SET ITSELF (routed to TASK-STWVB): the
worker measured 0 real failures on tree =75255e5= in the dispatch worktree; run
3 measured 2 on the SAME tree in the SAME worktree. So the axis is not only
load and not only worktree — identical tree at an identical site varies BY RUN.
Any future "N tests green" claim needs a run count, not just a worktree name.

NOT CLAIMED: that the suite is green. It is not, here. What is claimed is that
its red is independent of this change, on the evidence above.

RESIDUALS the implementer named and I am carrying forward rather than closing
over: =pipe-pane= backpressure needs a wedged daemon to reach; the tmux probe
returns Unknown (fail-closed, today's behaviour) if a daemon's PATH resolves
=tmux= to the rmux shim; and the production claim — a real dispatched tmux
worker surviving a real 600s provider-bound think — is NOT proven, because it
needs the runtime reinstalled at =7d9c60c= or later. Until that reinstall,
=--governance-json '{"stall_timeout_secs":3600}'= stays mandatory on every long
tmux dispatch.

SCOPE: the implementer wrote outside the declared WRITE_SCOPE and flagged it
rather than asking. Correct call — the heading's own WIDENED section made
=modes/tmux.rs= "work item 0", and the scope field had simply never been updated
when the task widened. WRITE_SCOPE corrected to match at integration.

INDEPENDENT REVIEW [2026-08-02]: NEEDS FIXES — BLOCKS SHIP.
=reviewer-codex-tmux= (driver tmux, harness codex, model gpt-5.6-sol, effort
high), run =run-20260802T133215-ec3730d4717a4ca595ee0f9a9bd715a4=, dispatch
=tx-20260802-orgasmic-4053=, closed =tx-20260802-orgasmic-4060=. Finding:
=tx-20260802-orgasmic-4055=.

THE BLOCKER is not in what this task built — it is what this task EXPOSED.
=pane_open_turn_marker= (supervisor.rs ~4902, loose spinner branch ~4913-4919)
accepts any visible line containing =Esc to interrupt=, anywhere in the capture.
This task routed tmux through that classifier, so an at-rest tmux pane whose
prompt, transcript or tool output contains those bytes is classified =Working=
on every expired stall sweep and survives to the 14,400s max-run ceiling instead
of dying at 600s. The new wedge arm cannot catch it: it drives a blank
=sleep 300= pane, so a marker-bearing at-rest pane is untested by construction.

It is also an ORDERING VIOLATION, recorded in TASK-JQ8AV.1 before this task ran:
transport generalization was to follow the classifier fix, not precede it. That
ordering inverted. TASK-JQ8AV.1 is therefore the unblocking work for this task
too; its Acceptance has been updated to require the marker regressions on tmux
as well as rmux, and it is dispatched (=tx-20260802-orgasmic-4064=).

WHAT THE REVIEWER INDEPENDENTLY CONFIRMED as sound, and should be preserved
through the fix round: =PaneMux= transport scoping; tmux pane-PID resolution;
the raw-byte =pipe-pane= activity channel; fail-closed behaviour on capture
errors; release/Drop cleanup. It ran the live smoke on a real pane
(=live_tmux_pane_publishes_pane_activity_for_newline_free_redraws=, PASS, 30.3s)
and watched =cat= pid 61791 live as a child of the test-owned tmux server and
disappear on release.

CRITICALLY, the re-cut proof was checked and NOT weakened:
=orgasmic verify TASK-JQ8AV= PASS — "the re-cut patch still removes the same
pane-content channel, reproduces the pinned incident red, restores a
byte-identical tree, and passes green. I found no weakening of this artifact."
That was the sharpest risk in the integration and it is closed.

OPEN, not attributed: 112 inactive =orgasmic-pane-*= FIFO directories were
already in the temp root in five clusters spanning 15:33-16:28, with no open
handles and no surviving =cat= processes. The reviewer's own acquire/release
smoke and explicit Drop-path test each added ZERO, so this is test-teardown
residue rather than a per-run daemon leak — but it accumulates. Filed as
TASK-8XPRR.

STATE: stays IN_REVIEW. Per the active goal, a BLOCK SHIP is not closed until
its fix round lands and passes a fresh review. Do not flip the tmux default
driver until TASK-JQ8AV.1 lands, and keep
=--governance-json '{"stall_timeout_secs":3600}'= on every long tmux dispatch.

* IN_REVIEW TASK-JQ8AV The evidence clock cannot see a provider-bound turn: long server-side thinking reads as the VZMZE wedge (three false-positive kills)
:PROPERTIES:
:ID: TASK-JQ8AV
:PRIORITY: P1
:END:

Measured 2026-07-29 06:23-06:27, first hours of TASK-JK66P's clock in production: THREE healthy workers (FZB6T, RB1ZN, SZJ2B, dispatched together) were stall-released with 'no work evidence for 600s; 1 process(es) at 0.0-0.3% cpu' — and the RB1ZN retry's pane, captured live, showed the truth: 'Quantumizing… (3m41s · ↓13.1k tokens · thinking with high effort)'. A claude-opus-5 high-effort turn thinks server-side for many minutes: ~0% local cpu (network wait), zero pane repaints (the TUI spinner does not repaint without token arrival), no protocol events on the rmux transport. Under JK66P's two signals (pane bytes, subtree cpu) that is BYTE-IDENTICAL to VZMZE's wedge. The single-dispatch WKJX4 worked earlier only because its turns' evidence landed inside each 600s window.

JK66P's design anticipated adjacent shapes (its Unknown-fails-closed rule is right); what is missing is a third evidence channel that can see 'waiting on the provider'. Candidates, to be MEASURED not assumed: (a) established TCP connection to the provider with recent traffic on the harness pid's sockets; (b) the harness's own native session/log file mtime advancing (claude writes turn state under ~/.claude — check what a thinking turn touches); (c) the TUI spinner does repaint on some cadence in some modes — measure exactly when pane bytes flow during thinking; (d) provider-turn-in-flight marker from the driver where a protocol exists (acp has heartbeats — VZMZE forbids counting them as WORK, but 'turn open' is a different, bounded claim: a turn that stays open past max-turn-duration is its own timeout class).

MITIGATION until fixed (in use now): dispatch with --governance-json '{"stall_timeout_secs":3600}' — the per-run governance override; max_run_duration still bounds the run. Recorded in gotchas.org.

=== Acceptance
- A worker in a measured multi-minute server-side think survives the stall window with the production default budget, proven by test (compressed budgets, a stub that is network-waiting rather than sleeping — the distinction IS the task).
- VZMZE's wedge (idle process, no provider connection/no native-file progress) still dies on schedule.
- The reason string keeps naming which evidence was consulted (JK66P's ask 4).
- Injection per TCTTD, self-tested.

=== Evidence (manager, 2026-07-29)
Merged 4fc57fe. Rescued after a host reboot: four commits were already on the
branch; the implementer's report and a gotchas entry were uncommitted in the
worktree and were preserved by hand before the tree was cleaned for the replay.
The report stops mid-sentence at "results below" — the reboot cut it off while
it was writing its per-crate gate results, so the manager re-ran every gate.

Manager-replayed proof: =orgasmic verify TASK-JQ8AV= PASS — red at exit 101
matching the pinned incident tombstone verbatim (a provider-bound
network-waiting turn stall-released at 0.0% cpu), tree byte-identical after
revert, green at exit 0. Replayed again on fully-merged main and PASSED.

Gates on merged main: fmt clean; clippy -D warnings clean; daemon lib suite 617
passed / 7 load flakes, all green in isolation.

LIMIT FOUND AT INTEGRATION, recorded in gotchas.org and filed as TASK-4CSMY
(P1): the pane consult is gated on =target.transport == "rmux"=, so this fix
does not reach tmux. Compounding it, =pane_activity= is emitted by the rmux
driver and by nothing else, so on tmux BOTH pane channels are blind and the
600s kill stands unchanged. This became load-bearing the same day, because the
operator directed tmux to become the shipped default pane mode.

The worker's scratch report.md was committed into the branch and removed from
main in 7e0ca50; it is dispatch scratch, not repo content.

NOT independently reviewed: manager integration review + injection replay only.

