#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-RQ270.3.1 Excision stage C part 1 — remove the architecture read surface from the UI
:PROPERTIES:
:ID: TASK-RQ270.3.1
:PRIORITY: P1
:WRITE_SCOPE: ui/
:END:

** Description

Stage C, part 1 of 2: remove the architecture READ SURFACE FROM THE UI. Read
=TASK-RQ270= for the staged plan and the coupling map. Stages A and B are done
and reviewed.

*Split rationale, and the ORDER matters.* The parent estimates stage C at ~1,500
LOC across daemon, CLI and UI. This session's repeated evidence is that large
changes hide their gaps from review. So stage C is two dispatches, and *the UI
goes FIRST* so main never contains a UI calling a route the daemon has removed.

TASK-RQ270.3.2 removes the daemon read model, the CLI reads and lands the lint
carve-out.

** Measured surface, at HEAD

*18 files* under =ui/src/= reference architecture — the parent estimated 14.
Measure again yourself before trusting either number; three rounds of this
excision have been rejected over an inventory someone trusted too early.

Known members: =ArchitectureView.tsx=, =app/routes.ts=, =app/router.tsx=,
=components/ProjectTabs.tsx=, =components/AppShell.tsx=,
=components/node-views/NodeModal.tsx=, =components/node-views/orgNodes.ts=,
=components/orgdoc/descriptor.ts=, =lib/capabilities.ts=, =lib/api.ts=,
=lib/types.ts=, =lib/tabsStore.ts=, =lib/orgdoc/types.ts=, =lib/richText.tsx=,
=components/OrgView.tsx=, =components/ArtifactsView.tsx=, plus the
=lib/__tests__/= files.

*Named explicitly because a previous round misclassified it*:
=ui/src/lib/api.ts:370= — =postStage= accepts ='architect'= and constructs a live
=POST /architect=. It has no callsite and the daemon rejects the stage, but it is
a client WRITE surface, not a read. *Drop ='architect'= from that union.*

** In scope

Every UI surface that lists, routes to, renders, types or advertises the
architecture read model: the view, its route and nav entry, the node-modal arch
rendering, the org-doc descriptor, capabilities, the API wrappers and types.

** Explicitly NOT in this stage

- =GET /architecture*=, =load_architecture=, the index projections, the CLI
  =architecture {list,get,schema}= verbs — TASK-RQ270.3.2.
- The dangling-edge lint carve-out — TASK-RQ270.3.2, and it MUST be on main
  before stage D.
- The node class, =schema.rs=/=id.rs=, =.orgasmic/architecture.org= — stage D.
- The README hero image — TASK-ZFNE1, deferred by operator decision.

** Acceptance

- No UI route, nav entry, view, type, API wrapper or capability offers the
  architecture read model.
- ='architect'= is gone from =postStage='s union.
- The UI builds and typechecks with no unused-import or dead-type residue.
- =npm test= passes.
- *The audit is claim-shaped and includes the RENDERED result*, not only source
  text. Five audit failure shapes have been paid for on this excision, ending
  with a claim rasterized into an image no grep could see. Confirm the running UI
  offers no architecture surface — nav, route, search, command palette — and say
  how you confirmed it.

** Not to be broken

The generic org-node viewer, the graph view, decisions, glossary and tasks all
share the machinery architecture used. Excision removes ARCHITECTURE-SPECIFIC
arms and surfaces, never the shared org-doc, node-modal or graph plumbing.
** Landed and verified by the manager
Merged =18739ea= (worker =f4f2264=, base =6f64b6d=). *20 files, +24/-526*,
=ArchitectureView.tsx= deleted.

=== It audited the CLAIM, including the rendered result — the first round to do all five

The brief listed the five audit failure shapes this excision has paid for and
demanded a claim-shaped audit including rendered output. The implementer did all
of it:

- *Source*: exact searches for =fetchArchitecture=, response/summary types,
  =/architecture= route strings, node kind, descriptor suggestion, nav page,
  capability entry — none. Plus SYNONYM sweeps (=system design=,
  =technical design=, =component map=, =module map=, =blueprint=) — no
  replacement surface.
- *Built bundle*: no built file contains =ArchitectureView=,
  =Loading architecture=, the project architecture route, or =/architecture/nodes=.
  It correctly identified the one architecture-named chunk as Mermaid's generic
  =architectureDiagram= renderer — shared artifact machinery, not our read model.
- *Rendered*: served the production =ui/dist= build in a browser. Nav showed
  Project, Decisions, Tasks, Glossary, Artifacts, Prompts, Activity. Direct
  navigation to =/projects/orgasmic/architecture= rendered the Decisions
  fallback. DOM inspection found no global command root and no search control,
  and it verified from the import graph that the command primitive is used only
  by the Decisions tag filter. *It inspected the rendered screenshot*, not only
  the DOM — the exact instrument that caught the hero image.

=== It found two surfaces the manager's task list did not name

- =ui/vite.config.ts= — the SPA fallback route regex still matched
  =architecture=, so the dev/preview server would have served the app shell for a
  dead route.
- =ui/README.md= — the screens table still listed the Architecture row with
  =GET /architecture=, =GET /architecture/nodes=.

Both are exactly the class this excision keeps producing: a surface that no
=ui/src= grep reaches. The manager's inventory was source-directory shaped again.

=== And it corrected the count with a reason

19 files matched, not 18. The extra is =TaskAgentBadges.tsx=, whose =architector=
regex recognises historical dispatch FILENAMES — retained deliberately, because
it reads history and is not a read surface. That is the distinction the stage-A
round got wrong on =api.ts=, applied correctly here unprompted.

=== Manager verification on the merged tree

- Claim-shaped source audit: no route, view, nav entry, API wrapper or type. The
  only two =architect= hits left in =ui/src= are =TaskAgentBadges.tsx='s
  historical filename regex and a test asserting legacy architecture ids render
  as PLAIN TEXT — the desired post-excision behaviour, correctly kept.
- =postStage= union is now ='grill' | 'plan'=; ='architect'= is gone. That is the
  write surface the stage-A round misclassified as read-only residue, carried by
  name into this task and discharged.
- *Built-bundle audit re-run by the manager*: zero files under =ui/dist/= contain
  =ArchitectureView=, =/architecture/nodes= or =Loading architecture=.
- =npm run typecheck= — exit 0. =npm run build= — exit 0, 5,695 modules
  transformed. =npm test= — *38 files, 264 tests, all passed*.
- No Rust gate run, correctly: the diff is =ui/**= only.

=== Declared honestly, and accepted

- The rendered preview was unauthenticated, so a daemon-connect dialog overlaid
  route content; the sidebar and the direct-route fallback still rendered and
  were inspected, and the palette/search absence was independently established
  from the bundle and the import graph. Stating the limitation is worth more than
  a cleaner-sounding claim.
- One =npm test= run timed out on an unrelated Mermaid render test while
  executing concurrently with the build; the file passed 12/12 alone and two
  subsequent sequential runs passed 264/264. Correctly classified as
  concurrency-induced. The manager's own sequential run also passed 264/264.
- =npm ci= reports 17 pre-existing dependency vulnerabilities. Out of scope, and
  right to name rather than silently carry.

