#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-QGWK7.1.1.1.1.1 follow-up: correct the false revert-abort claim in shipped content and drop the latching sequencer entry :dispatch:evidence:manager:
:PROPERTIES:
:ID: TASK-QGWK7.1.1.1.1.1
:PRIORITY: P2
:WRITE_SCOPE: crates/**,shipped/**,verify/**
:FIX_SUBTASK: t
:DEPENDS_ON:  TASK-QGWK7.1.1.1.1.1.1
:END:

=== READ THIS FIRST — STATUS ===
Follow-up round for the TASK-QGWK7.1.1.1.1 review (merged =8e56b86=, verdict
*approve-with-follow-ups*, NO BLOCK SHIP). *THE BEHAVIOUR IS CORRECT AND STAYS.*
This round corrects a FALSE MEASURED CLAIM that is now in shipped content, and
removes one entry whose only reachable effect is a trap.

*THE COMPLETENESS QUESTION IS CLOSED — DO NOT REOPEN IT.* The reviewer
enumerated 12 git states from the OPERATIONS side (=merge --squash= conflicted
and clean, =stash pop=, =apply -3=, =checkout -m= on a branch and on a commit,
=am --3way=, =rebase -i= at a break, both rebase backends, stopped ranges) and
found NO harmful cell lacking a marker. Every state with a dedicated =--abort= is
marked; every state without one is either safe under its documented recovery or
already caught by the detached-HEAD refusal. The single destroyed cell
(=git reset --hard ORIG_HEAD= after a squash merge) is a generic rewind that
destroys the manager's own work equally — no marker can distinguish it.

*ALSO SETTLED, DO NOT RE-DERIVE:* B-2's premise (nothing in =crates/= or
=shipped/= reads a record's branch — all four consumers read a repo-relative
path); the B-3 gate is sound (the only other =Ok(())=-without-commit routes are
an empty dir and a dir of only subdirectories, both of which hit the warning
arm); =write_failing_git_shim= is leak-free and its coverage claim is accurate.
=== END STATUS ===

** C-1 — MEDIUM — the shipped convention states a measured symptom that is FALSE
*MANAGER-RE-MEASURED IN THE PRODUCTION SHAPE.* The claim that a record commit
written inside a revert makes =git revert --abort= fail with
=Untracked working tree file … would be overwritten by merge= reproduces ONLY
when the commit is written WITHOUT the real-index =git add= at
=manager.rs:5712=. Production always runs that add first — it is the load-bearing
line settled two rounds ago — so production is the other shape.

Manager measurement, conflicted revert, production shape (real-index add, then
throwaway index, =commit-tree=, =update-ref=):
=git revert --abort= -> *exit 0*, record survives ON DISK and IN =HEAD=,
=REVERT_HEAD= cleared, index =UU f.txt= before the abort.

The reviewer's wider table, same git: =revert --abort=, =merge --abort=,
=cherry-pick --abort= and =am --abort= ALL exit 0 and the record SURVIVES.
*=rebase --abort= is the only guarded operation whose abort destroys it* — and a
rebase detaches HEAD, so the detached-HEAD refusal already catches that before
any marker does.

*The refusal itself is still right* — it is consistent, costs only a promote plus
a re-run, and closes a class rather than a case. Only the JUSTIFICATION comes
down. Correct it in FOUR places:
- =shipped/prompt-studio/conventions/manager-dispatch.org:344-347= — shipped to
  managers, the one that matters most
- =crates/orgasmic-cli/src/manager.rs:5759-5767= (the guard's doc comment)
- =crates/orgasmic-cli/src/manager.rs:9888-9893= (the test's doc comment)
- =verify/TASK-QGWK7.1.1.1.1/README.md:8-14=

*LEAVE =verify/TASK-QGWK7.1.1.1.1/expect-red= BYTE-UNCHANGED.* Its directives are
correct regardless; only its comment repeats the claim, and the artifact's
immutability is worth more than the comment's accuracy. *MANAGER RULING on the
reviewer's open question 2: treat the whole file as immutable.*

=shipped_conventions.rs= pins six phrases and NONE is this sentence, so the prose
corrects with no assertion change. KEEP the six-name operation list and the
staged-=-n= sentence exactly as they are — both are pinned and both are TRUE.

The replacement should say what is actually true: a record commit written inside
a revert or cherry-pick is NOT destroyed (git declines to rewind a moved HEAD),
but it lands in the middle of an operation the manager has not finished, so the
close stands down and the record enters history once, cleanly, at a point they
chose.

** C-2 — MEDIUM — DROP the =("sequencer", …)= entry
=manager.rs:5782=. *MANAGER RULING on the reviewer's open question 1: REMOVE IT.*

It provides NO coverage: every stopped revert/cherry-pick range carries its own
=*_HEAD= marker — the implementer and the reviewer measured this independently
and agree — so the entry catches nothing the array does not already catch.

Its only reachable effect is a LATCH. Measured by the reviewer: after a stopped
range, =git reset --hard= (the ordinary way to abandon one) clears
=CHERRY_PICK_HEAD= but LEAVES =.git/sequencer=. From then on *every close refuses
to persist any record, indefinitely*, and the refusal says "re-run this close
once the revert or cherry-pick sequence finishes" — which will never happen. Same
for =git checkout= away and back, and for a =reset --hard= followed by an
ordinary commit.

I am choosing removal over a better message because an entry that adds no
coverage and one trap is not worth carrying. Also correct the "cannot latch on"
sentence in the doc comment at =:5769-5773=, which is false as written.

Update =an_interrupted_pick_sequence_is_refused_and_a_finished_one_is_not=
accordingly: a real interrupted range must STILL be refused (via its =*_HEAD=
marker), a completed one must not be, and the fabricated no-marker state should
be dropped along with the entry that made it meaningful.

** C-3 — LOW — the conflicted-revert test's abort assertion does not discriminate
=manager.rs:9899=, assertion at =:9946-9952=. It asserts =git revert --abort=
exits 0 after a REFUSED close — but per C-1 it exits 0 after an UNREFUSED close
too, so the assertion holds with or without =REVERT_HEAD= in the array. The
discriminating assertion is the =CleanupStatus::Partial= one, and =expect-red=
says so honestly; the test's NAME and doc claim coverage the assertion does not
deliver. *Fix:* rename to =…_keeps_the_record_and_persists_nothing= (the twin
naming the parent round used) and soften the doc to call the abort assertion a
regression fence rather than the discriminator. The test is sound — keep it.

** C-4 — LOW — the empty-directory warning DOES self-heal and does not say how
=manager.rs:5865-5871= vs =:5830=. The prior report said the warning "never
self-heals". It does: =repersist_dispatch_record_best_effort= returns silently
when =dest_dir.is_dir()= is false, so one =rmdir= of the empty directory ends it
permanently. *Fix:* one clause on the warning naming that remedy.

Related, very low reachability but it undermines the line B-3 exists to make
trustworthy: =dispatch_record_is_in_history= (=:5902-5917=) is =unwrap_or(false)=,
so a =git ls-tree= that ERRORS after a successful commit routes to the warning
arm, which then asserts "the directory holds no files" — a statement that would
be false. Optionally distinguish =false= from =errored=.

** Acceptance
- No site in =crates/=, =shipped/= or =verify/= claims a =git revert --abort=
  wedge. What replaces it is true of the production shape and does not overstate.
- =verify/TASK-QGWK7.1.1.1.1/expect-red= is BYTE-UNCHANGED.
- The six-name operation list and the staged-=-n= sentence are unchanged and
  still pinned.
- The =("sequencer", …)= entry is gone; a stopped range is still refused via its
  =*_HEAD= marker; an abandoned range no longer latches. Pinned by a test that
  exercises =reset --hard= after a stopped range and expects a close to PERSIST.
- The conflicted-revert test is renamed and its doc no longer overclaims.
- The empty-directory warning names its remedy.
- All four existing verify artifacts still PASS.

* IN_REVIEW TASK-QGWK7.1.1.1.1.1.1 fix-round: restore the sequencer guard the manager wrongly ordered dropped, and make the convention pin bidirectional :dispatch:evidence:manager:
:PROPERTIES:
:ID: TASK-QGWK7.1.1.1.1.1.1
:PRIORITY: P1
:WRITE_SCOPE: crates/**,shipped/**,verify/**
:FIX_SUBTASK: t
:END:

=== READ THIS FIRST — STATUS ===
Fix round for the TASK-QGWK7.1.1.1.1.1 review (merged =091d62a=, REJECT, ONE
BLOCK SHIP). *THE BLOCK SHIP IS AGAINST A MANAGER RULING, NOT A WORKER ERROR.*
The previous round did exactly what it was told; the instruction was wrong.

*THE C-1 CORRECTION IS RIGHT AND STAYS.* The reviewer independently re-measured
every row: =revert=, =cherry-pick=, =merge= and =am= aborts all exit 0 with the
record alive on disk and in =HEAD=; =rebase --abort= is genuinely the only
destroyer and genuinely detaches HEAD. =expect-red= is byte-unchanged and the
re-authored injection restores the identical five-marker set. C-3 (the test name
and its doc) and C-4 (the empty-directory warning) are CORRECT — do not revisit.

Full review at =.orgasmic/dispatch-records/tx-20260807-orgasmic-5239/last.txt=.
=== END STATUS ===

** D-1 — BLOCK SHIP — restore the =sequencer= entry, WITH A BETTER MESSAGE
=crates/orgasmic-cli/src/manager.rs:5793-5801= (array), =:5808-5816= (doc).
*MANAGER RULING REVERSED, AND MANAGER-REPRODUCED.*

The previous round dropped =("sequencer", "revert or cherry-pick sequence")= on
my instruction, justified by "every stopped revert/cherry-pick range ALWAYS
carries its own =*_HEAD= marker, so the entry adds no coverage". *That universal
is FALSE.* Counterexample by an ordinary, documented git workflow — resolve the
conflict and =git commit= instead of =git revert --continue=:

#+begin_src bash
git revert --no-edit HEAD~1 HEAD~2      # stops between picks
#   .git -> REVERT_HEAD, sequencer          <- refused, correctly
echo resolved > f.txt && git add f.txt && git commit -qm "resolved by hand"
#   .git -> sequencer ONLY                  <- NO *_HEAD marker
#   .git/sequencer/todo still lists two pending reverts
#+end_src

Manager reproduced this verbatim. The range is NOT abandoned — =git revert
--continue= resumes it from there. In that state the guard now returns =None= and
the close COMMITS the record mid-range, at a point the manager did not choose; if
they later abandon with =git reset --hard <pre-range>= the record dies with it.
*Before that round this state was refused, so the removal is a coverage
regression*, and the acceptance item "a stopped range is still refused via its
=*_HEAD= marker" is falsified.

*THE LATCH IS STILL REAL* — after =git reset --hard=, =.git/sequencer= survives,
=git status= says nothing, an ordinary commit does not clear it, and only
=git revert --quit= does. Reviewer reproduced my measurement. *But the failure
mode was never the ENTRY — it was the MESSAGE*, which told the manager to wait
for a sequence that cannot finish.

*Fix (exit (a), the reviewer's lean and mine):* restore the entry AND special-case
the advice. When the marker that fired is =sequencer= with NO =*_HEAD= beside it,
say: "finish it with =git revert --continue=, or — if you already abandoned this
range with =git reset --hard= — clear the leftover todo list with
=git revert --quit=". That turns a permanent unexplained refusal into a one-command
self-serve fix AND keeps the F-1 state covered. *Pin BOTH:* the abandoned-range
close must name =--quit=, and the manual-commit state must be REFUSED.

** D-2 — MEDIUM — the convention pin is UNIDIRECTIONAL, so this exact drift went through it
=crates/orgasmic-cli/tests/shipped_conventions.rs:436-457=. Its comment claims
"dropping one from EITHER side fails here". It does not: the assertions are
=text.contains(phrase)= over the =.org= prose ONLY. The previous round dropped the
code entry and the pinned phrase "revert/cherry-pick sequence" (=:444=) still
passed, because the convention prose still promises it.

*That is verbatim the B-2 defect* — prose promising a guarantee the code does not
provide — recreated in the round convened for prose accuracy, with the guard
against it inert.

*Fix:* make it bidirectional. Export the marker array from
=sequencer_operation_in_progress= as a =const= and have =shipped_conventions=
assert each operation name is nameable off the prose, instead of hard-coding a
phrase list. Until that exists the comment at =:436-438= must not claim it.

** D-3 — MEDIUM — the replacement MECHANISM is false for 3 of 4 operations, and stamped "(measured)"
Three sites: =manager-dispatch.org:346-349=, =manager.rs:5765-5767=,
=verify/TASK-QGWK7.1.1.1.1/README.md:75-78=.

The prose explains the true conclusion with "git declines to rewind a HEAD that
moved". Reviewer measured: *only =am= declines.* For a single-commit
revert/cherry-pick git's =rollback_single_pick= does =reset --merge= TO THE
CURRENT HEAD — it REWINDS, silently, and the record survives only because it is
already AT HEAD. =merge --abort= is likewise =reset --merge= to HEAD. The
"declines to rewind" wording belongs to the MULTI-pick path, a different code
path from the one the correction measured.

Nothing behavioural rests on it, but it is an explanation stamped "(measured)"
that the measurement does not support, shipped to managers, in the round convened
because exactly that happened. *A manager reasoning forward from it will predict
wrongly about their own conflict resolution surviving an abort — it does not.*

*Fix:* state the OUTCOME, not the mechanism — "the abort exits 0 and the record
survives in =HEAD= and on disk; for revert and cherry-pick because the abort
rewinds to the current HEAD, which IS the record commit; for =am= because git
refuses to rewind past a moved HEAD" — or drop the mechanism clause entirely.
Worth adding: *the abort discards the manager's conflict resolution either way.*

** D-4 — MEDIUM — the runtime =CLEANUP_ERROR= still carries the disproven claim
=manager.rs:5696=: "committed (an --abort would discard the commit and the
promoted file with it)". True for =rebase= ONLY; false for =revert=,
=cherry-pick=, =merge= and =am=.

*MANAGER RULING REVERSED.* The previous round left this deliberately and I let it,
because editing the string makes =verify/TASK-QGWK7.1/injection.patch:23= and
=verify/TASK-QGWK7.1.1/injection.patch:24= stale (they quote it as deleted
context). The reviewer overruled that trade and is right: *this is the ONLY one of
the five sites a manager meets at RUNTIME*, with no README beside it to correct
it. My acceptance bar ("no site claims a =revert --abort= WEDGE") let it through
by the letter while failing the intent — "discard" is a weaker claim than "wedge",
and still false.

*Fix:* rewrite the string to promise only what is true, and refresh the
deleted-context lines in both artifacts IN THE SAME COMMIT. Re-verify both
afterwards; =expect-red= and =cmd= stay byte-unchanged in both.

** NOT a defect — recorded so it is not re-filed
The review's F-5 said the previous implementer's report was unrecoverable
(=tx-20260807-orgasmic-5232/last.txt= absent from =dispatch-records/=, never
committed). *FALSE ALARM.* It was absent because that dispatch was still OPEN —
promotion happens at close. The manager closed it and the record promoted
normally. The reviewer's inference was reasonable from where it sat (every
neighbour it compared against was a closed dispatch) but the conclusion was wrong.

** Acceptance
- The manual-commit stopped-range state (=sequencer=, no =*_HEAD=) is REFUSED
  again, pinned by a test that builds it the ordinary way.
- An abandoned range (=reset --hard=) is still refused, but the refusal NAMES
  =git revert --quit= as the remedy. Pinned.
- No claim anywhere says the =sequencer= entry adds no coverage, or that a
  stopped range always carries a =*_HEAD= marker.
- =shipped_conventions= fails if an operation is dropped from EITHER the code
  array or the prose, and its comment claims only what it does.
- No site explains the abort outcome with a mechanism that is false for the
  operation it is asserted over.
- =manager.rs:5696= promises only what is true; both affected artifacts are
  re-authored in the same commit and re-verified, =expect-red= and =cmd=
  byte-unchanged.
- All four passing verify artifacts still PASS. (=verify/TASK-QGWK7= is stale and
  is TASK-8Q92K's, not this round's.)
