#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-TP593 Node kernel in orgasmic-core plus the writer ops that go with it :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-TP593
:IMPLEMENTS: dec_E01MC
:PRIORITY: P1
:DEPENDS_ON: 
:END:

** Description
AP971.1/.6: land crates/orgasmic-core/src/node_kernel.rs from the ap971-prototype branch (41a1faf): node_dir, create_node_dir (mkdir = collision check), parse_node, parse_journal/append_entry, edit_comment_body, tombstone_comment, consume_open_comments. Add the daemon writer ops over it: append journal entry, OCC-guarded in-place comment edit stamping :EDITED_AT:, tombstone; journal.org joins the ledger-rewrite denylist; the writer refuses column-0 '* ' in journal prose; size lint past ~500KB. shipped/schema/journal.org v1 documents the grammar (AP971.1 items 1–12).
** Acceptance
- [ ] kernel unit tests + the real_data parse test pass
- [ ] writer ops exist with OCC and denylist enforcement, covered by tests
- [ ] schema doc shipped

* IN_REVIEW TASK-7A0H4 Node type descriptors: shipped org files, loader, descriptor-driven mint :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-7A0H4
:IMPLEMENTS: dec_E01MC
:PRIORITY: P1
:DEPENDS_ON: 
:END:

** Description
AP971.2/.3: shipped/schema/node-types/{task,decision,glossary,artifact}.org — one heading per type with :COLLECTION:, :ID_PREFIX:, :LABEL:/:LABEL_PLURAL:, :REQUIRED_PROPERTIES:, :STATES: + transitions (task only), :REGENERATE_PROMPT:, optional reserved files. Daemon loader; a collection dir with no descriptor gets generic indexing/comments/journals and a low-severity lint. mint_node_id(descriptor) keeps the 5-char Crockford stem; prefixes stay TASK-/dec_/term_/ART-; dir name = id verbatim, dotted subtasks flat. Shipped-only in v1.
** Acceptance
- [ ] four descriptors ship and load; unknown collection path tested
- [ ] mint is descriptor-driven and create_node_dir retries on AlreadyExists
- [ ] task states/transitions come from the descriptor (state-machine.org folded in or pointed at)

* IN_REVIEW TASK-SRBGS Migrator orgasmic project migrate; indexer reads node dirs; old state-file readers deleted :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON:  TASK-TP593 TASK-7A0H4
:ID: TASK-SRBGS
:IMPLEMENTS: dec_E01MC
:PRIORITY: P1
:END:

** Description
AP971.9: port scripts/ap971-migrate-proto.py (branch ap971-prototype) to orgasmic project migrate — one-shot, idempotent, refuses on a dirty git tree, --dry-run, writes <collection>/<ID>/{node.org,journal.org} for tasks (all six state files incl. done/cancelled), decisions, glossary; deletes the old files; bumps #+orgasmic_version to 2 on project.org (label only — no v1 compat layer, no v1 read path). The indexer reads collection dirs; iter_task_file_paths and every by-id full scan (index.rs, api.rs inbound-reference check, manager.rs read_task_lifecycle, identity_lint entry points, run-tests.sh globs) are rewritten or deleted. Singletons (handoff.org, goal.org, project.org) stay files. dispatch-records/ and tx/ untouched.
** Acceptance
- [ ] migrate runs on this repo: 873 nodes, 0 anomalies, byte-for-byte heading round trip
- [ ] parse_errors 0 and dangling-edge lint green after migration (old ids resolve)
- [ ] no reader of tasks/<state>.org, decisions.org, glossary.org remains in crates/ scripts/ verify/

* IN_REVIEW TASK-GCXB7 Shared dispatch fold in orgasmic-core with a type-set guard :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-GCXB7
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:DEPENDS_ON: 
:END:

** Description
AP971.11: one dispatch fold in orgasmic-core, called by both manager.rs scan_dispatches (8 call sites) and api.rs dispatch_generation_ledgers (they diverge today: the daemon ignores *.done/aborted). Add one test pinning the dispatch type set (manager.dispatch_started, run.created, implementer/reviewer/architector.reported, implementer/reviewer/architector.done, manager.dispatch_aborted, manager.dispatch_orphaned) to tx_destination → project tx, and list the set in shipped/schema/tx.org.
** Acceptance
- [ ] CLI and daemon call the same core fold; the daemon's open/closed answer matches the CLI's on this repo's tx/
- [ ] the type-set guard test exists and fails if any listed type routes anywhere but project tx/
- [ ] no behaviour change for dispatch-status / dispatch-wait / worktree-prune

* IN_REVIEW TASK-JWHXH Derived read views: views/{board,glossary,decisions}.org :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON: TASK-SRBGS
:ID: TASK-JWHXH
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:END:

** Description
AP971.8: daemon writes .orgasmic/views/board.org (tasks by state), glossary.org, decisions.org after each affected refresh (debounced) in today's file shapes; orgasmic views build for cold clones; .orgasmic/.gitignore gains views/. Repoint the four prompt-studio context-packs, shipped/entry/router.org, the two skill refs and the UI OrgView to views/.
** Acceptance
- [ ] views regenerate on node change; byte-stable when nothing changed
- [ ] no shipped or UI path still names glossary.org/decisions.org/tasks/<state>.org at the old location

* IN_REVIEW TASK-EPG6H tx split: route node-scoped events to journals per the AP971.5 table :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON: TASK-SRBGS
:ID: TASK-EPG6H
:IMPLEMENTS: dec_E01MC
:PRIORITY: P1
:END:

** Description
AP971.5: writer routing — journal iff the event is about exactly one dir-backed node, else project tx/ (comment, worker.comment, non-dispatch task.state_transitioned, task.property_updated, task.edited, graph.*.edited, graph.decision.reparent, reviewer.finding, review.verdict → journal; the .11 dispatch set + commit_pending, all manager.*, goal events, graph.handoff.edited, project.*, release.*, daemon.*, benchmark.*, question.*, legacy untyped, node deletions → tx/). Creation = journal entry #1 created; deletion = tx/ tombstone + dir removal. The dispatch close stays ONE tx append + N node.org rewrites with no journal flip (derived); torn-close repair becomes legacy-only. Journal ids share the project sequence; scan_project_tx_max_seq covers journals.
** Acceptance
- [ ] routing table pinned by a test over every known type
- [ ] dispatch close still atomic via transaction_multi with no multi-ledger append
- [ ] id sequence cold-start scan covers journals (test)

* IN_REVIEW TASK-ARRV8 Artifacts onto the node kernel :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON:  TASK-TP593 TASK-7A0H4
:ID: TASK-ARRV8
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:END:

** Description
AP971.6 mapping table: artifacts.rs and its six routes move onto node_kernel — reviews.org becomes journal.org (CID→TX_ID, ANCHOR/RESOLUTION_TARGET/REPLY_TO→extras, CONSUMED kernel-owned, RESOLVED stays artifact's axis); artifact.org becomes node.org; artifact descriptor ships. Keeps artifact-only: BLOCK_TYPES, validate_mdx, launch address, versions_dir, artifact.mdx. Migrate existing ART-* dirs in place.
** Acceptance
- [ ] all artifact API tests pass on the kernel
- [ ] existing artifacts under .orgasmic/artifacts/ migrated and readable

* IN_REVIEW TASK-IXPD4 Port orgasmic-daemon index tests to node-dir fixtures (left unported by TASK-SRBGS) :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-IXPD4
:PRIORITY: P1
:END:

** Description
TASK-SRBGS removed the aggregate-file readers but ported only 2 of the daemon index test fixtures; 21 tests in `crates/orgasmic-daemon/src/index.rs` still write `.orgasmic/tasks/backlog.org` / `decisions.org` / `glossary.org` aggregate fixtures the indexer no longer reads, so they fail. Port each red test's fixture to the node-dir layout: `.orgasmic/<collection>/<id>/node.org` with the `#+title: orgasmic <label> <id>` + `#+orgasmic_version: 2` header (see the already-ported tests at index.rs ~3728 and ~4935, and the ported core tests in crates/orgasmic-core/tests/fixtures.rs / identity_lint.rs for the recipe). Preserve each test's intent — port the fixture, do not weaken the assertions. Red list = `cargo test -p orgasmic-daemon --lib index::` failures at base.
** Acceptance
- [ ] `cargo test -p orgasmic-daemon --lib index::` fully green
- [ ] no test deleted, no assertion weakened

* IN_REVIEW TASK-RGRD5 Repair paths bypass the descriptor transition guard; fix last 11 dispatch.rs tests :e01mc:node_kernel:
:PROPERTIES:
:ID: TASK-RGRD5
:PRIORITY: P1
:END:

** Description
On e01mc-integration, 11 tests in `crates/orgasmic-cli/tests/dispatch.rs` still fail after the fixture port (8132b74a). Two causes:
1. PRODUCTION: TASK-7A0H4's shipped task descriptor now guards state transitions, and the torn-close REPAIR path hits it ("task transition backlog -> in_review is not allowed by the shipped task descriptor"). Repair/recovery replays a transition that was already recorded and accepted — it must bypass the descriptor transition guard. Live, newly-requested transitions keep the guard. Find where torn-close reconciliation applies the transition and route it through a guard-free (or guard-suppressed) writer path; add one focused test pinning "repair bypasses guard, live transition does not".
2. TESTS: remaining fixture/path issues (NotFound on record paths, e.g. dispatch.rs:12002) from the node-dir port. Red list: a_re_persist_lands_on_the_branch_the_re_run_is_standing_on, a_record_commit_cannot_land_on_a_branch_the_close_never_resolved, a_record_commit_rollback_reports_both_the_clean_and_the_failed_arm, a_record_persist_that_failed_is_recovered_by_re_running_the_close, a_torn_close_is_reconciled_before_an_absolute_report_path_is_refused, an_empty_promoted_record_directory_is_never_reported_as_committed, dispatch_close_commits_terminal_and_lifecycle_txs_in_one_request, dispatch_close_prunes_stem_dir_leaving_brief, dispatch_timeout_requests_daemon_cleanup, legacy_torn_dispatch_close_is_still_repaired_by_next_manager_command, task_update_from_dispatch_worktree_lands_in_the_live_ledger.
If a test's LIVE transition starts from an illegal state, fix the fixture to a legal from-state instead of weakening the guard.
** Acceptance
- [ ] `cargo test -p orgasmic-cli --test dispatch` fully green
- [ ] focused test: repair path bypasses the descriptor guard; a live illegal transition is still refused
- [ ] no test deleted, no assertion weakened

* IN_REVIEW TASK-LBRX7 Ledger branch extraction: orphan `orgasmic` branch, hidden worktree, .orgasmic leaves the main tree :twcp9:distributed:
:PROPERTIES:
:ID: TASK-LBRX7
:PRIORITY: P1
:DEPENDS_ON: TASK-SRBGS
:END:

** Description
TWCP9 decision, step 1. Create the orphan `orgasmic` branch (no shared history with main). The daemon owns a hidden worktree for it at ~/.orgasmic/<project>/ledger/ and repoints every ledger read/write there. `.orgasmic/` is removed from the main tree; worker worktrees are ledger-free by construction. Single combined cutover: extend the AP971.9 migrator (`orgasmic project migrate`) with a target-branch mode so old-format state migrates directly onto the branch — never migrate twice. CLI/daemon project discovery must work from a worktree that contains no `.orgasmic` (resolve via the git common dir).
** Acceptance
- [ ] `orgasmic` branch exists as an orphan; daemon reads/writes only via the hidden worktree
- [ ] main tree and fresh worker worktrees contain no `.orgasmic` (test)
- [ ] `orgasmic project migrate --to-branch` is one-shot, idempotent, --dry-run capable
- [ ] CLI verbs work from a worker worktree with no `.orgasmic` present (test)
- [ ] CI config excludes the `orgasmic` branch from triggers

* IN_REVIEW TASK-AN992 CLI verbs over node dirs and shipped content for the new layout :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON: TASK-SRBGS
:ID: TASK-AN992
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:END:

** Description
task/decision/glossary verbs and orgasmic node body operate on node dirs; comment edit/delete verbs over the journal; conventions, prompt-studio, project-scaffold and hub content updated for the new layout (scaffold ships empty collection dirs; gotchas.org and conventions/ stay prose files). Evidence for new dispatches lands in tasks/<ID>/dispatches/<started-tx>/ (brief, compiled prompt, report, stdout) instead of dispatch-records/.
** Acceptance
- [ ] every verb in orgasmic --help that read a state file works on dirs
- [ ] a fresh orgasmic project init scaffolds the v2 layout

* IN_REVIEW TASK-8AV8B Incremental refresh: per-node reload and writer apply-own-write :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON: TASK-EPG6H
:ID: TASK-8AV8B
:IMPLEMENTS: dec_E01MC
:PRIORITY: P1
:END:

** Description
AP971.7: watcher path → (project, collection, node id) → reload only that node dir; a tx/ file change re-collects that file; views/ and tmp/ ignored. After a successful structured write the writer applies the one node dir it wrote to the snapshot and publishes before the HTTP response returns; the required-refresh wait is removed; the watcher's echo is a no-op on content equality. Activity feed = projection over tx/ + journals (both sources, forever). dispatch-wait folds the coherent snapshot (CLI keeps the disk fold). Full rescan only on boot, project add, orgasmic reindex, unclassifiable events.
** Acceptance
- [ ] a comment POST returns without a project-wide reparse (measured)
- [ ] activity feed shows journal comments and tx run events interleaved
- [ ] dispatch-wait no longer calls project_tx_entries on the poll path

* IN_REVIEW TASK-MSYN4 Machine identity + ledger sync loop over the git remote :twcp9:distributed:
:PROPERTIES:
:ID: TASK-MSYN4
:PRIORITY: P1
:DEPENDS_ON: TASK-LBRX7 TASK-EPG6H
:END:

** Description
TWCP9 decision, step 2. Mint a stable machine-id per daemon. Project tx/ files become per-machine (`machines/<machine-id>/tx/YYYY-MM.org`); the activity fold reads the union (it is file-order independent). Daemon sync loop on the ledger worktree: coalesced auto-commit, `pull --rebase`, push, retry on race — all machine writes stay inside `machines/<machine-id>/` plus claim-gated node dirs, so rebases can never hit content conflicts. Event hygiene: every event carries a globally unique id + actor + machine-id + timestamp; ingest deduplicates retries; rejected events are preserved for inspection, never silently dropped.
** Acceptance
- [ ] two daemons (test harness) sync through a bare remote with zero conflicts under concurrent writes
- [ ] push race resolves by rebase+retry (test forces a non-fast-forward)
- [ ] duplicate event submission is deduplicated by event id (test)
- [ ] rejected events land in an inspectable location with a reason
- [ ] single-machine projects behave unchanged (no remote configured → sync loop idles)

* IN_REVIEW TASK-8DMQS Regenerate generalization: node.regenerate:<id> over descriptors :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON: TASK-ARRV8
:ID: TASK-8DMQS
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:END:

** Description
AP971.10: one regenerate path — recompile node content + open comments + extra prompt through the descriptor's :REGENERATE_PROMPT: prompt-studio spec (the hardcoded assemble_regen_context becomes the artifact spec), run task id node.regenerate:<id> on the artifactor supervisor path, one live regenerate per node; close-out consumes open comments and writes a regenerated journal entry with CONSUMED_CIDS; version bumps; old content in git. Task regenerate rewrites the task, never re-dispatches. No spec → no regenerate.
** Acceptance
- [ ] regenerate works for artifact (unchanged behaviour), task, decision, glossary
- [ ] the consume rule is tested once, type-agnostically

* IN_REVIEW TASK-CLM6W Cross-machine task claims + multi-machine fold :twcp9:distributed:
:PROPERTIES:
:ID: TASK-CLM6W
:PRIORITY: P1
:DEPENDS_ON: TASK-MSYN4 TASK-8AV8B
:END:

** Description
TWCP9 decision, step 3. Claims live in per-machine append-only logs (`machines/<machine-id>/claims.org`) with an advisory :WRITE_SCOPE: glob. Node dirs are claim-gated: the daemon refuses to write a node dir whose current claim belongs to another machine. First push wins; on pull, a losing claimant backs off; tie-break when both events are visible: earliest claim timestamp, then machine id. Double-claims are surfaced in views/status, not prevented. Projections (board/backlog/activity) fold the union of all machines' logs.
** Acceptance
- [ ] concurrent claim of the same task by two daemons converges to one holder deterministically (test)
- [ ] write to a node dir claimed by another machine is refused with a clear error (test)
- [ ] double-claim interval is visible in dispatch-status/views
- [ ] projections fold multi-machine logs identically regardless of ingest order (test)

* IN_REVIEW TASK-KA934 UI: per-node comments and regenerate; OrgView over views :e01mc:node_kernel:
:PROPERTIES:
:DEPENDS_ON:  TASK-8AV8B TASK-8DMQS
:ID: TASK-KA934
:IMPLEMENTS: dec_E01MC
:PRIORITY: P2
:END:

** Description
Per-node comment threads (reply, edit, delete) and a regenerate button wherever the descriptor declares a prompt; OrgView reads views/; task dialog activity rail reads the node journal. Labels come from descriptors.
** Acceptance
- [ ] comment on a task from the UI, edit it, delete it, regenerate the task
- [ ] no UI code path reads tasks/<state>.org / decisions.org / glossary.org

