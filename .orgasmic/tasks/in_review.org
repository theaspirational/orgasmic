#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-AJP4A.1.1.1.1 Fix blocking-scan lease and delayed-state truthfulness
:PROPERTIES:
:ID: TASK-AJP4A.1.1.1.1
:PRIORITY: P1
:READ_SCOPE: crates/orgasmic-daemon/src/index.rs,crates/orgasmic-daemon/src/api.rs,crates/orgasmic-cli/src/daemon_service.rs,crates/orgasmic-cli/tests,ui/src
:TEST_CMD: cargo test -p orgasmic-daemon index:: --lib -- --test-threads=1 && cargo test -p orgasmic-cli daemon_service && npm --prefix ui test -- --run
:WRITE_SCOPE: crates/orgasmic-daemon/src/index.rs,crates/orgasmic-daemon/src/api.rs,crates/orgasmic-cli/src/daemon_service.rs,crates/orgasmic-cli/tests,ui/src
:FIX_SUBTASK: t
:END:

** Description
Fix the deterministic rejection from the final Opus review without changing the lazy catalog-plus-on-demand projection architecture.

** Acceptance Criteria
- [ ] A successful begin_blocking_scan registration is owned by a completion guard before seed capture, so every Project/Markers/Artifacts early return releases the target immediately; add deterministic production-path regression coverage.
- [ ] Unscoped /tx reports coordinator-only delay separately from genuine failed project loads using an additive conditional delayed coverage segment; dismissal keys include genuine failures only.
- [ ] NotificationBell and BoardView render delayed/non-path diagnostics as non-destructive and do not let delayed rows suppress later genuine failures.
- [ ] The late-waiter test has a robust timeout margin and dismissal normalization is tested across reordered project ids.
- [ ] systemd service environment rendering rejects or safely handles control characters in the captured scan-timeout value, with a renderer test.
- [ ] Run focused Rust/CLI/UI gates plus format, clippy, build, and report the existing aggregate classification; do not run a redundant broad daemon suite and do not install or restart any live daemon/service.
