#+title: In Review
#+orgasmic_version: 1

* IN_REVIEW TASK-STWVB.1.1.1 fix-round: round 3 fixed exit-4-on-a-calm-host and broke exit-0-on-a-flake :testing:daemon:flake:
:PROPERTIES:
:ID: TASK-STWVB.1.1.1
:PRIORITY: P1
:WRITE_SCOPE: crates/**,scripts/**,verify/**,.github/**,.orgasmic/project.org
:FIX_SUBTASK: t
:DEPENDS_ON:  TASK-STWVB.1.1.1.1
:END:

=== READ THIS FIRST — STATUS ===
Round 4 of the TASK-STWVB chain. Fix round for the TASK-STWVB.1.1 review (merged
=b92199a=, REJECT). TWO BLOCK SHIP, two MEDIUM, two LOW. All four verified by the
manager in source before filing.

*B1 IS FIXED AND SETTLED. The rate is the right shape* — the reviewer attacked it
empirically and could not break it, and the acceptance was demonstrated twice
(worker's =--workspace= run: exit 1 not 4, =host : calm=, rate 0.6045 s/s; the
manager's independent scoped run: =verdict: GREEN — no failures=, exit 0). Do not
re-open the rate-vs-absolute question.

*PART A IS SETTLED AND CLOSED* (mechanism-justified, effect size unmeasured). Do
not probe it. Three rounds have now been spent adjacent to that question.

The full review is preserved at
=.orgasmic/dispatch-records/tx-20260806-orgasmic-5063/last.txt=.
=== END STATUS ===

** The headline: round 3 fixed one half and broke the other
The chain has been chasing "the gate returns no verdict". Round 3 fixed the
exit-4-on-a-calm-host half and broke the exit-0-on-a-flake half.

*Both of round 3's demonstrations dodged this by construction*, which is why it
shipped: the =--workspace= run had a REAL failure (so the earlier =REAL_COUNT=
branch won), and the scoped run had ZERO failures (so cargo exited 0 and the
branch was never reached). Neither exercised the ordinary flake path. When the
next round demonstrates its fix, *demonstrate the FLAKE case explicitly* — it is
the case both prior demonstrations missed.

** M-1 — HIGH — BLOCK SHIP — a registered flake now reddens the live gate
=scripts/run-tests.sh:1048=. MANAGER-VERIFIED IN SOURCE.

B2's move put

: elif [ "$SUITE_EXIT" != "?" ] && [ "$SUITE_EXIT" != 0 ]; then

above the =HOST_DEGRADED= arm *and above the =FAIL_COUNT > 0= arm*. In the
pre-change ladder (=4788e1f:scripts/run-tests.sh=) it sat AFTER =FAIL_COUNT > 0=,
which is exactly what made its message TRUE — it could only fire when there was
no per-test failure list. There is now NO =FAIL_COUNT= guard, and =cargo test=
exits 101 whenever any test fails.

Reviewer reproduction, stub cargo emitting one failing test whose signature
matches a registry entry, isolation-green:

#+begin_example
  failures : 1
  host     : calm (threshold syspolicyd_rate>=1.50; ...)
FLAKE (1) — green in isolation, registered signature matched:
  tests::recovery_inventory_waits_for_atomic_claim_commit
verdict: RED — cargo exited 101 with no per-test failure list.   exit 1
#+end_example

The same log through =--classify= (where =SUITE_EXIT= is =?=):

#+begin_example
verdict: GREEN modulo 1 registered flake(s). No unexplained red.   exit 0
#+end_example

*Two modes, one log, opposite verdicts.* And the sentence is false on its face —
the failure list is printed immediately above it.

Second reproduction, degraded host + unregistered isolation-green failure: the
=LOAD-SENSITIVE (1)= block prints and the verdict is still
=RED — cargo exited 101...= at exit 1. *So =EXIT_INCONCLUSIVE= is DEAD on the live
path, and every artifact this chain exists to build — LOAD-SENSITIVE, the C
interlock, exit 4 — is now =--classify=-only.*

WHY THE SUITE CANNOT SEE IT: all thirteen flake/load/host cases drive
=--classify=, where =SUITE_EXIT="?"= short-circuits the branch, and the two new
live-path cases use a stub cargo that either exits 0 or exits 101 WITH NO FAILURE
LIST.

*Fix (reviewer's, manager concurs):* keep B2's position, restore the guard its own
message states —

: elif [ "$FAIL_COUNT" -eq 0 ] && [ "$SUITE_EXIT" != "?" ] && [ "$SUITE_EXIT" != 0 ]; then

— and pin it with LIVE-PATH cases: stub cargo emitting a registered flake and
exiting 101 -> =GREEN modulo 1 registered flake=, exit 0; and the degraded
variant -> =INCONCLUSIVE=, exit 4. Neither case exists today.

** M-2 — HIGH — BLOCK SHIP — =host : calm= when the primary signal was never measured
=scripts/run-tests.sh:496-507= and =:988=. MANAGER-VERIFIED: =host_judgment_
measured= ORs =num(load) || num(cpu) || num(wall)=, and load essentially always
parses — so the word is =calm= whenever the GATING signal (the rate) is
unmeasurable.

Reviewer probes on the live path:

| injected sample | printed |
| =load=4.0,syspolicyd_cpu=?,wall_s=300= | =host : calm=, =rate ?=, exit 0 |
| =load=4.0,syspolicyd_cpu=250.0= (no =wall_s=) | =host : calm=, =rate ?=, exit 0 |
| =load=4.0,syspolicyd_cpu=250.0,wall_s=0= | =host : calm=, =rate ?=, exit 0 |
| =load=4.0,syspolicyd_cpu=250.0,wall_s=-5= | =host : calm=, =rate ?=, exit 0 |

*Row 2 is a REGRESSION against STWVB.1's F3, not merely an F-E residual*: the
stamp format gained =wall_s= in this commit with no compat path, so EVERY suite
log written by any earlier version now reclassifies as =calm= where F3
deliberately made it =unknown=. Rows 1, 3 and 4 cover Linux, a
=pgrep -x syspolicyd= miss, and a sub-second window.

THE REACH THAT MATTERS IS CI. =ci.yml:200-206= fails the lane on exit 4 on the
authority of the rate threshold. If =syspolicyd= is not visible on the
=macos-15= runner, the gate is INERT and the summary still prints
=host : calm (threshold syspolicyd_rate>=1.50; ...)= — asserting a threshold was
applied to a measurement that never happened. Same class as the original F2
phantom =0.0=, one level up, which is what F-E was filed about.

The two new F-E cases pass because they set EVERY field to =?=/junk. They pin the
all-unknown corner and miss the common one.

*Fix:* derive the word from the RATE, not from any-field. =calm= only when the
rate parsed as a number and is below threshold; =unknown= otherwise, with the
reason named (=no syspolicyd signal= / =window unknown=). Keep load as display.

** M-3 — MEDIUM — the rate has no minimum window
=scripts/run-tests.sh:465-493=, =:636-638=. =local_wall= is =date +%s=
arithmetic — integer, +/-1s quantisation — clamped to a floor of 1, and
=host_is_degraded= gates at ANY wall. Reviewer probes:
- =wall_s=1, syspolicyd_cpu=2.0= -> rate 2.0000 -> DEGRADED, exit 4
- =wall_s=3, syspolicyd_cpu=4.5= -> rate 1.5000 -> DEGRADED, exit 4

So a 1-3s scoped run judges the whole host on 1-3 samples of a bursty quantity.
This is the short-window mirror risk the review was asked to attack, and it is
real — it simply did not fire in the two runs anyone happened to make.

*Fix:* require =wall_s >== a floor (10-30s) for the rate to gate at all; below the
floor report =host : unknown (window too short to judge)= — neither calm nor
DEGRADED. That also disposes of =wall_s=0= and the sub-second clamp, which
currently UNDER-report (a 0.3s window's delta divided by 1).

** M-4 — MEDIUM — the summary advertises a load threshold that does not exist
=scripts/run-tests.sh:88=, =:986-990=. MANAGER-VERIFIED BY GREP:
=LOAD_DEGRADED_THRESHOLD= returns exactly three lines — the assignment and two
=printf= arguments. *Nothing compares load to it.* Load is printed
unconditionally at any value, while the summary tells the operator
=load corroborating only, display>=8.0=.

*Fix:* either annotate the load field when it clears 8.0 (which is what
"corroborating" would mean in the output), or drop the constant and the clause.

** M-5 — LOW — F-F's replacement assertion is narrower than its headline
=scripts/run-tests-selftest.sh:396=. =grep -Eq 'syspolicyd_time=0(\.0)?($| )'=
cannot match the format the field actually carries — verified:
=syspolicyd_time=0:00.00= does NOT match; only a bare =0= or =0.0= would. No
longer vacuous, but the realistic phantom-zero shape (the F2 defect re-expressed
as a cumulative =ps= time) escapes. *Fix:* also reject
=syspolicyd_time=0:00(\.0+)?=.

** M-6 — LOW — note, not a defect: the stamp format is still duplicated
=scripts/run-tests-selftest.sh:110-116=. =stamp_host= still hand-writes the
stamp, so STWVB.1.1's acceptance clause "the stamp format is not duplicated in
the test" is not literally met. In practice the duplication now works in the
RIGHT direction — the hand-written fixtures pin the READER while the new e2e case
pins the WRITER, and the reviewer mutation-tested three field renames and all
three were caught. Recorded so the clause is not silently dropped; no change
requested.

** Acceptance
- A registered flake on the LIVE path returns =GREEN modulo N registered
  flake(s)= at exit 0, and the degraded variant returns INCONCLUSIVE at exit 4.
  Both pinned by live-path cases (stub cargo exiting 101 WITH a failure list) —
  neither exists today, and their absence is why M-1 shipped.
- =--classify= and the live path return the SAME verdict for the same log.
- The host word is derived from the RATE: =calm= only when the rate parsed and is
  below threshold; =unknown= otherwise with the reason named. A stamp with no
  =wall_s= (any pre-=b92199a= log) reads =unknown=, not =calm=.
- The rate does not gate below a stated minimum window; below it the host is
  =unknown (window too short to judge)=.
- Every constant the summary names is either enforced or removed from the
  summary.
- =verify/flake-registry.toml= byte-unchanged, as in all three prior rounds.
- THE DEMONSTRATION MUST INCLUDE THE FLAKE CASE. Both prior rounds demonstrated
  on inputs that skipped the branch they broke.
** Evidence
IMPLEMENTED 2026-08-07. Worker implementer-cursor-agent-tmux; driver tmux,
harness cursor-agent, model cursor-grok-4.5-high-fast, effort omitted
deliberately. Merged 1add6fd. Report preserved at
=.orgasmic/dispatch-records/tx-20260806-orgasmic-5079/last.txt=.

ALL FOUR VERIFIED BY THE MANAGER IN THE MERGED SOURCE:
- M-1. The guard is restored at =scripts/run-tests.sh:1078=:
  =elif [ "$FAIL_COUNT" -eq 0 ] && [ "$SUITE_EXIT" != "?" ] && [ "$SUITE_EXIT" != 0 ]=,
  keeping B2's position above =HOST_DEGRADED=.
- M-2. A stamp with no =wall_s= now reads =host : unknown (window unknown)=,
  restoring the STWVB.1/F3 fail-safe for every pre-=b92199a= log.
- M-3. =SYSPOLICYD_RATE_MIN_WALL_S=10= (=:111=), gating both awk comparisons
  (=:502=, =:518=). The sub-second clamp-to-1 is removed so a fractional window
  is not under-reported.
- M-4. =LOAD_DEGRADED_THRESHOLD= is now actually COMPARED (=:1004=) and annotates
  =load=<n> elevated (>=8.0), corroborating only=. It is no longer a constant the
  summary advertises and nothing enforces.

THE FLAKE CASE — the one both prior rounds missed — IS NOW DEMONSTRATED AND
PINNED. Live path, stub cargo with a registry-matching signature:
- registered flake + calm host -> =GREEN modulo 1 registered flake(s)=, exit 0;
  =--classify= on the same log: IDENTICAL.
- registered flake + degraded host -> =INCONCLUSIVE — re-run when calm=, exit 4;
  =--classify=: IDENTICAL. *=EXIT_INCONCLUSIVE= is alive on the live path again*,
  so LOAD-SENSITIVE and the C interlock are no longer =--classify=-only.

SEVEN NEW LIVE-PATH SELFTEST CASES (44 total, up from 33), covering exactly the
population M-1/M-2/M-3 describe — registered flake calm and degraded,
unregistered iso-green degraded, cpu unknown, no =wall_s=, =wall_s=0=,
=wall_s=1=. The =--classify=-only blindness that let M-1 ship is closed.

GATES at merge, manager-run: selftest 44 passed / 0 failed; =--check= OK; clippy
=-D warnings= clean; fmt clean; =orgasmic verify TASK-STWVB.1.1.1= PASS. The
proof was inspected rather than trusted: the injection removes EXACTLY the
=FAIL_COUNT= guard, and the FIRST FAIL is
"live path: registered flake on calm host -> GREEN modulo flake, exit 0", with
the two other live flake cases failing behind it. That is the M-1 population, so
the artifact is aimed at the defect and not adjacent to it.
=verify/flake-registry.toml= byte-unchanged for the fourth consecutive round.

REAL SCOPED LIVE RUN: =scripts/run-tests.sh -p orgasmic-core --lib= ->
=host : calm=, =delta syspolicyd_cpu=1.2 wall_s=11=, =rate 0.1091=,
=verdict: GREEN — no failures=, exit 0. Note =wall_s=11= clears the new 10s
floor, so a scoped crate run still judges rather than falling to =unknown=.

RESIDUAL, NAMED HONESTLY BY THE WORKER AND CARRIED TO THE REVIEW: live and
=--classify= still DISAGREE on one input — a crashed binary with no per-test
failure list (live =RED ... cargo exited 101=, exit 1; =--classify= =GREEN — no
failures=, exit 0). Cause: the cargo exit is not stamped into the log and
=--classify= sets =SUITE_EXIT="?"=, so that arm cannot fire on reclassify. The
worker scoped it out rather than widening. That is a defensible call, but it
means the "same verdict for the same log" invariant this round established holds
for the =FAIL_COUNT>0= population only — and the obvious fix (stamp the suite
exit into the log, exactly as host state is already stamped) is small enough that
the reviewer should rule on whether it belongs here.

* IN_REVIEW TASK-STWVB.1.1.1.1 fix-round: a crashed test binary reads as GREEN whenever a registered flake fires :testing:daemon:flake:
:PROPERTIES:
:ID: TASK-STWVB.1.1.1.1
:PRIORITY: P1
:WRITE_SCOPE: crates/**,scripts/**,verify/**,.github/**,.orgasmic/project.org
:FIX_SUBTASK: t
:DEPENDS_ON:  TASK-STWVB.1.1.1.1.1
:END:

=== READ THIS FIRST — STATUS ===
Round 5 of the TASK-STWVB chain. Fix round for the TASK-STWVB.1.1.1 review
(merged =1add6fd=, REJECT). *ONE ROOT CAUSE IN TWO FORMS (R-1, R-2), ONE FIX*,
plus two LOW.

*ROUND 4's FOUR FIXES ARE ALL CORRECT AND CORRECTLY PINNED — DO NOT RE-OPEN
THEM.* The reviewer attacked the rate arithmetic, the window floor and the load
annotation empirically and could not break any of them. In particular:
*DO NOT RE-DROP THE =FAIL_COUNT= GUARD* — that recreates M-1 exactly, and this
chain has already spent one round on that mistake.

Also settled: part A (mechanism-justified, effect size unmeasured), and B1's
rate shape. Do not probe either.

The full review is preserved at
=.orgasmic/dispatch-records/tx-20260806-orgasmic-5084/last.txt=.
=== END STATUS ===

** The headline: a demonstrated FALSE GREEN, and it is the worst category yet
Every prior round of this chain produced a false RED or a missing verdict. This
one is a *false GREEN on the live path*, and CI gates on the exit code alone.

=FAIL_COUNT= counts *LISTED* per-test failures. A test binary that aborts or
segfaults contributes ZERO listed failures — libtest dies with the process, so
there is no =---- name ----= block. So a run with BOTH (a) a crashed binary and
(b) any classified failure has =FAIL_COUNT > 0=, the crash arm is skipped, and
the run falls through to =GREEN modulo N registered flake(s)= at *exit 0*.

MANAGER-VERIFIED, and it is worse than the guard alone: =BUILD_BROKE= matches
only =^error: could not compile= and =^error\[E[0-9]+\]= (=:724=), so it does NOT
catch =error: N targets failed=; and =grep -c= for =signal:= /
=didn't exit successfully= / =targets failed= across the whole script returns
*ZERO*. There is no crash detection anywhere.

=FAIL_COUNT -eq 0= is exactly the condition that FAILS on this board whenever a
registered flake fires — and this chain exists because registered flakes fire.
Round 3 traded a live-dead =EXIT_INCONCLUSIVE= for a working one; round 4 traded
a live-dead crash detector back. Round 3's demonstrations missed the flake path;
round 4's missed the crash-plus-flake path.

*CLASSIFICATION: PRE-EXISTING, NOT A REGRESSION.* At =4788e1f= the arm sat BELOW
=FAIL_COUNT > 0= and was equally unreachable. Round 3's reorder plus its dropped
guard accidentally closed it; restoring the guard reopened it. The reviewer filed
it BLOCK SHIP anyway because it is a demonstrated false green on the live path,
and the manager concurs.

** R-1 — HIGH — BLOCK SHIP — a crashed binary is swallowed whenever any failure classifies
=scripts/run-tests.sh:1078=.

REVIEWER REPRODUCTION WITH VERBATIM CARGO OUTPUT. A two-target crate — one
=assert!= failure whose panic matches a registry signature, one
=std::process::abort()= — real =cargo test --no-fail-fast=, log replayed through
the live path:

#+begin_example
  failures : 1
  host     : calm (threshold syspolicyd_rate>=1.50; load corroborating only)
FLAKE (1) — green in isolation, registered signature matched:
verdict: GREEN modulo 1 registered flake(s). No unexplained red.      exit 0
#+end_example

What the SAME log says, verbatim, from cargo:

#+begin_example
error: test failed, to rerun pass `--bin crashdemo`
Caused by:
  process didn't exit successfully: `.../crashdemo-...` (signal: 6, SIGABRT: process abort signal)
error: 2 targets failed:
    `--lib`
    `--bin crashdemo`
#+end_example

WHY THIS REACHES CI: =ci.yml:197= runs the live path and gates on the exit code
alone. *Exit 0 merges.* And the verdict block a human might read does not mention
the crash AT ALL — not in =failures :=, not in any REAL/LOAD/FLAKE section, not
in the verdict line.

*FIX DIRECTION — detect the crash FROM THE LOG*, independent of both
=SUITE_EXIT= and =FAIL_COUNT=:
- =error: test failed, to rerun pass `<target>`= is printed once per failing
  target. Diff that target set against the set of binaries that produced a parsed
  failure block. A target in the first set and not the second is an UNACCOUNTED
  CRASH.
- Corroborate with =process didn't exit successfully:= / =(signal: N, SIG...)=,
  which is crash-specific — an ordinary red does not emit it.
- Report it as its own section (=CRASHED (n)=) and its own RED arm, placed ABOVE
  =FAIL_COUNT > 0= and ABOVE =HOST_DEGRADED=. A crashed binary is a code fact
  whatever else the run found — the same argument F4 made for =REAL_COUNT=.

Pin it with a live-path case whose stub cargo emits *a failure list AND an
unaccounted crashed target* and exits 101 -> RED, exit 1. No such case exists
today, and its absence is why R-1 shipped.

** R-2 — HIGH — BLOCK SHIP — "same verdict for the same log" is unmet
=scripts/run-tests.sh:1078= (reads =SUITE_EXIT=) vs =:604= (=SUITE_EXIT="?"=).

Byte-identical log, both modes:
| mode | verdict | exit |
| live | =RED — cargo exited 101 with no per-test failure list.= | 1 |
| =--classify= | =GREEN — no failures.= | 0 |

The log the human is holding contains =error: test failed, to rerun pass ...=,
and =--classify= prints =GREEN — no failures= over it.

*RULING ON "is =--classify= inherently a weaker mode?" — NO, not on this point.*
The information exists at write time and is simply not written down. That is the
IDENTICAL situation this chain already resolved once: STWVB.1's F3 found
=--classify= sampling host state at reclassify time, and the answer was not
"accept a weaker mode", it was =write_host_stamp=. The suite exit deserves the
same treatment, by the same mechanism.

*Fix:* append =# orgasmic-suite-exit: <n>= alongside the existing
=# orgasmic-host-state:= stamp; under =--classify=, read it and use it instead of
=?=, falling back to =?= only when the stamp is absent (pre-stamp logs). R-1's
log-derived detector covers those legacy logs anyway.

** R-3 — LOW — docs — =project.org= overstates round 4's coverage
=.orgasmic/project.org:23=, added by =deffb1e=: "A non-zero cargo exit with no
per-test failure list is also a code fact... on the live path and under
=--classify= alike." The first half is FALSE under =--classify= (R-2); the second
is true only of failures that were LISTED — a crashed target contributes nothing
to classify and is dropped (R-1). Resolves with R-1/R-2; otherwise qualify the
sentence.

** R-4 — LOW — design note, NO CHANGE REQUESTED — the floor leaves scoped runs unjudged
=scripts/run-tests.sh:111=. The floor itself is RIGHT. But the reviewer measured
the everyday warm scoped invocation at *=wall_s=3=, not the 11 the round-4 report
recorded* (that was a colder run). So the host machinery is inert for scoped runs
ALWAYS, not marginally.

The reviewer traced what =unknown= does to the VERDICT and it is sound:
LOAD-SENSITIVE is correctly suppressed (=classify_one:811= requires
=HOST_DEGRADED -eq 1=, so an unregistered iso-green failure stays REAL at exit 1
— unknown does not mint an excuse); the exit code for an otherwise-green short
run is unchanged at 0. The accepted cost is that INCONCLUSIVE is unreachable
below the floor, which is M-3's own stated fix direction, the reason is named in
the output, and CI's workspace run is never near the floor.

*If host judgment on scoped runs is ever wanted, widen the MEASUREMENT window —
sample syspolicyd over a fixed >=10s window bracketing the run — do NOT lower the
floor.* Lowering it walks straight back into M-3.

** R-5 — LOW — test — two selftest cases can silently not run
=scripts/run-tests-selftest.sh:768,793=. Both =--classify=-agreement companion
cases sit behind =if [ -f "$live_log" ]; then ... else rm -rf "$TMP/work"; fi=.
If the live run ever fails to write =suite.log=, the case VANISHES with no
failure and no message, and nothing asserts the expected total of 44. Low risk
today. *Fix:* fail when the log is absent, or pin the case count.

** Recorded, not a defect — M-5's regex covers the realistic shape
The reviewer verified the old and new assertions against the live sampler and
synthetic phantoms; together they cover the shapes =ps -o time== actually emits
on macOS (=0:00.00=, =0:00=, bare =0.0=). No change requested.

** Acceptance
- A run containing a crashed/aborted target returns RED at exit 1, *whatever
  else the run classified*, and the crash is NAMED in the verdict block (its own
  =CRASHED (n)= section). Pinned by a live-path case emitting a failure list AND
  an unaccounted crashed target.
- The crash is detected from the LOG, not from =SUITE_EXIT= or =FAIL_COUNT=, so
  it survives both modes and legacy logs.
- =--classify= and the live path return the SAME verdict for the same log, for
  the crashed-binary population too. The suite exit is stamped into the log,
  mirroring =write_host_stamp=, with a documented fallback for unstamped logs.
- The =FAIL_COUNT= guard is STILL THERE and a registered flake still returns
  =GREEN modulo N=, exit 0. Do not regress M-1 while fixing R-1 — pin both in the
  same run.
- =.orgasmic/project.org:23= says what is actually true.
- The two conditional selftest cases cannot silently vanish.
- =verify/flake-registry.toml= byte-unchanged, as in all four prior rounds.

* IN_REVIEW TASK-STWVB.1.1.1.1.1 fix-round: the B2 guard is a leaky proxy for exit 101, so a killed cargo still reads as GREEN :testing:daemon:flake:
:PROPERTIES:
:ID: TASK-STWVB.1.1.1.1.1
:PRIORITY: P1
:WRITE_SCOPE: scripts/**,verify/**,.orgasmic/project.org
:FIX_SUBTASK: t
:END:

=== READ THIS FIRST — STATUS ===
Round 6, and the defect is the SAME GUARD FOR THE THIRD TIME. Fix round for the
TASK-STWVB.1.1.1.1 review (merged =060b84d=, REJECT, one BLOCK SHIP).

*ROUND 5'S OWN ACCEPTANCE IS FULLY MET AND CORRECTLY PINNED — DO NOT RE-OPEN IT.*
The reviewer reproduced the crash fix independently: crash+flake is RED exit 1 on
both paths, the suite-exit stamp works, =project.org:23= says what is true, the
R-5 tripwires are live, and the ordinary cases keep their verdict and exit. R-1
is genuinely fixed. Likewise settled and NOT to be re-probed: round 4's four
fixes, part A, B1's rate shape, and =open_owner()= — the reviewer PROVED the
resolver's predicate strictly implies the gate's, so there is no board state
where it picks an owner the gate then rejects. *KEEP =open_owner()=.*

*DO NOT RE-DROP THE =FAIL_COUNT= GUARD.* That recreates M-1 and this chain has
already spent a round on it. F-1's fix below is written specifically so that 101
stays exempt, which is the whole of M-1.

The full review is at =.orgasmic/dispatch-records/tx-20260807-orgasmic-5157/last.txt=
— 9 synthetic-log probes plus one live-path run, with verbatim output. Read it.
=== END STATUS ===

** F-1 — HIGH — BLOCK SHIP — a non-101 non-zero cargo exit is swallowed whenever any failure classifies
=scripts/run-tests.sh:1254=. MANAGER-VERIFIED IN SOURCE.

*THE ANSWER TO "WHICH ADJACENT BRANCH DID THE LAST ROUND LEAVE WRONG".* The B2
arm's =FAIL_COUNT -eq 0= guard is a PROXY for "the exit is 101", and a leaky one.
The arm's own comment states the real justification — "cargo exits 101 whenever
any test fails" — which justifies exempting *101*, not every non-zero code.

REVIEWER REPRODUCTION, LIVE PATH, stub cargo emitting one registered-flake
failure and exiting *137*:

#+begin_example
  failures : 1
  crashed  : none — every failing target reported a failure list
  host     : calm
verdict: GREEN modulo 1 registered flake(s). No unexplained red.      exit 0
#+end_example

...over a log the same live run wrote carrying =# orgasmic-suite-exit: 137=. Drop
the flake from that same log and the same exit IS caught (=RED — cargo exited 137=,
exit 1). *Identical exit code, identical arm, opposite verdict — decided entirely
by whether a registered flake happened to fire.*

WHY THIS IS NOT R-1 RE-FILED: R-1 was a crashed TARGET — cargo survived, printed
=error: test failed, to rerun pass=, and the log named the casualty. This is a
crashed SUITE: cargo itself died, so there is no failing target to diff and no
cause line, =CRASH_COUNT= is 0 BY CONSTRUCTION, and round 5's log-derived
detector cannot see it. The only witness is the exit code, and that is exactly
the witness the guard gags. Reachable via jetsam/OOM under the memory pressure
this chain documents, or an operator =^C= mid-run. =ci.yml:197= gates on the exit
code alone, so exit 0 merges.

CLASSIFICATION: PRE-EXISTING, not a regression — the guard predates round 5 and
round 5 did not widen it. Filed BLOCK SHIP under the same standard round 4's R-1
was: a demonstrated false green on the live path.

*Fix:* replace the proxy with the fact the comment already states. Guard on the
CODE, not on =FAIL_COUNT=:
- =SUITE_EXIT= in {0, 101} -> ordinary; classify exactly as today. *This is what
  preserves M-1* — 101 is the flake path and it stays =GREEN modulo N=.
- =SUITE_EXIT= anything else, and not =?= -> its own RED arm with its own summary
  line, firing WHATEVER ELSE CLASSIFIED, placed beside the crash arm.
- =?= (pre-stamp =--classify=) -> skip, as B2 already does.
Pin with a live-path case: registered flake + stub exit 137 -> RED exit 1, AND
the existing flake+101 case still =GREEN modulo 1= in the same run.

** F-2 — MEDIUM — NEW IN =0e2ba6a= — a target that DID report is called a crash
=scripts/run-tests.sh:841=. The NAMED exclusion is =(exit status: 101)= ONLY, and
NAMED never asks whether the target was accounted for. So any other exit status
on a target that printed its full failure list is counted as a crash.

REVIEWER REPRODUCTION: one registered flake with a full =failures:= list, cause
changed from =(exit status: 101)= to =(exit status: 1)= and nothing else —
verdict flips from =GREEN modulo 1= exit 0 to =RED — 1 crashed test target(s)=
exit 1. The CRASHED section then prints "this target reported NO failures" about
a target whose failures are listed three lines above it. *A false RED on the
flake path — M-1's shape by a third route*, and the printed statement is false on
its face.

Not currently live here: no =harness = false= targets in the workspace
(reviewer-grepped), so every target exits 101 through libtest. Latent — and
latent on the one branch whose whole job is not to invent reds.

*Fix:* require a NAMED target to ALSO be unaccounted, so the cause line only
EXPLAINS a crash rather than DECIDING one. One condition, and it closes F-3 too.

** F-3 — MEDIUM — NEW IN =0e2ba6a= — =max()= unions two DISJOINT sets by cardinality
=scripts/run-tests.sh:858=. =max= is safe as a GATE (=max(N,U)=0= iff both are 0,
so it can never produce a false green) — but it does not merely gate a boolean:
=CRASH_COUNT= is printed in the summary line, the section header and the verdict
line, and it decides whether the unnamed-remainder block prints at all (=:1199=).

NAMED and UNACCOUNTED can describe DIFFERENT targets. Reviewer reproduction with
a two-target log — =--lib= reported its failures and exited 1 (NAMED, accounted),
=--bin crashdemo= genuinely vanished (unaccounted, no cause line) — output names
=--lib=, the target that DID report, and =--bin crashdemo= *appears nowhere*,
because the remainder block is suppressed when =UNACCOUNTED > NAMED= is false.
The operator is handed the wrong binary to re-run, told a falsehood about it, and
never told about the real one.

*Fix:* union by TARGET IDENTITY, not by cardinality — keep the NAMED
target->cause map, mark unaccounted targets, report the union. F-2's fix
collapses this case on its own.

** F-4 — MEDIUM — test — the =(exit status: 101)= exclusion has ZERO coverage
=grep -n "exit status" scripts/run-tests-selftest.sh= returns NOTHING across all
50 cases. The fixtures model cargo as emitting =Caused by:= for the crashed
target and nothing at all for the ordinary red. That single regex at =:841= is
the only thing standing between every ordinary red on the real suite and a false
CRASH verdict, and it is exercised by nothing. Verified correct by hand for the
canonical shape, so this is COVERAGE, not a live defect — but four rounds running
have shipped a defect in an unexercised adjacent branch.

*Fix:* two fixture cases — an ordinary registered-flake red WITH the
=(exit status: 101)= cause block (must stay =GREEN modulo 1=, exit 0), and the
same with a non-101 status (asserting whatever F-2's fix decides).

** F-5 — LOW — docs — the registry guard offers two routes and the manager took a third
=scripts/run-tests.sh:318= says "delete the entry, or reopen the task." The
manager's =a8edf93= TRANSFERRED ownership to an open task instead — a third route
the message does not sanction. The reviewer attacked that call and it HOLDS (the
new owner's acceptance forbids the graveyard outcome). But the message is the
only instruction the next agent reads at 2am with CI red, and following it
literally means deleting live entries and converting measured flakes into
unexplained REAL failures.

*Fix:* name the third route — "delete the entry, transfer it to an open owner
that will fix it, or reopen the task."

** F-6 — LOW — the case-count constant invites bumping
=scripts/run-tests-selftest.sh:35=. =EXPECTED_CASES= does NOT fail open — the
reviewer proved the tripwire live by bumping it to 51 in a scratch copy. But
adding a case means editing a constant ~900 lines away, and the failure message
("a case was added or vanished") literally offers bumping the number as one of
two readings. *Fix:* derive the count from the =start= calls instead of declaring
it.

** Recorded, NO CHANGE REQUESTED
- *The crash arm outranking =HOST_DEGRADED= is RIGHT — ship it.* SIGABRT/SIGSEGV/
  SIGILL/SIGFPE cannot plausibly be host-caused; SIGBUS is a code fact far more
  often than not. SIGKILL(9) is the only misfiled signal, and even there RED is
  the conservative direction and exit 4 fails CI identically, so only the
  diagnosis differs. Revisit ONLY if a measured false red appears.
- The implementer's report says the ordinary cases are "byte-identical". They are
  not: one summary line was added (=crashed : none…=). Substance holds, wording
  does not — worth correcting in a chain where report precision is load-bearing.
  That line also reads oddly on a clean run where no target failed at all.

** Open questions worth one cheap answer
1. *Does real cargo emit =Caused by: … (exit status: 101)= for an ordinary
   failing target?* It decides whether =:841= is the load-bearing guard on every
   red or dead code, and it also settles the doctest string. ONE throwaway
   two-target crate answers it and the round-4 reproduction already built one.
2. Does anything in this workspace abort today? Decides whether R-1 was theory or
   a live outage. The fix does not depend on it.

** Acceptance
- A non-zero, non-101 cargo exit returns RED at exit 1 WHATEVER ELSE classified,
  named in its own summary line, on the live path AND under =--classify=.
- The flake+101 path still returns =GREEN modulo N= at exit 0, pinned IN THE SAME
  RUN as the case above. The =FAIL_COUNT= guard is not dropped.
- A target that reported its failures is never counted or printed as a crash,
  whatever its exit status (F-2).
- When NAMED and UNACCOUNTED describe different targets, EVERY crashed target is
  named and no reporting target is named (F-3).
- The =(exit status: …)= cause shape is exercised by fixtures in both the 101 and
  non-101 forms (F-4).
- =scripts/run-tests.sh:318= names the transfer route (F-5).
- The case count is derived, not declared (F-6).
- =verify/flake-registry.toml= byte-unchanged, as in all five prior rounds.
- =open_owner()= is KEPT.
