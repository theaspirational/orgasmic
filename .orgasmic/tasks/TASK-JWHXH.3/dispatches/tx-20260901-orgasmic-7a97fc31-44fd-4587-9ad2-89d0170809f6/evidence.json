{
  "event_count": 26719,
  "tool_call_count": 130,
  "unparsed_events": 32,
  "bounded_events": 38,
  "session": {
    "status": "found",
    "filename": "dispatch-TASK-JWHXH.3-implementer-20260901T220401.jsonl",
    "size_bytes": 24723413
  },
  "native_transcript": {
    "status": "unsupported",
    "harness": "opencode",
    "reason": "no transcript finder adapter for harness 'opencode'"
  },
  "narrative": [
    {
      "stream": "assistant",
      "text": "Let me start by understanding the task. I'm the orgasmic implementer for TASK-JWHXH.3. The task is to:\n\n1. Stop writing `.orgasmic/views/*.org` files; render on demand\n2. Add `project migrate` + `doctor` for stragglers\n\nLet me first read the task record and the relevant files.\n\nKey steps from the brief:\n1. `crates/orgasmic-core/src/views.rs`: keep `render_collection` as pure renderer; add `pub fn render_view(project_root, name)`; delete `build_views` + `write_if_changed` and `views/` dir creation. Delete scaffold of `views/` + `board.org` in `projects.rs`.\n2. `crates/orgasmic-daemon/src/index.rs`: delete three `build_views` call sites and debounce machinery only for them.\n3. `crates/orgasmic-daemon/src/api.rs` `get_org_file`: render on demand for `.orgasmic/views/<name>.org`.\n4. CLI `orgasmic views build`: delete subcommand (or make it print to stdout if a test depends).\n5. `orgasmic project migrate`: untrack + delete `.orgasmic/views/`; idempotent. `orgasmic doctor`: warn while tracked/present.\n6. Prompt-studio + docs cleanup.\n7. Tests.\n\nWait — but the assignment says something narrower:\n\n\"Assignment: Implement dec_AF61D. Reuse the sync-loop code from TASK-JWHXH.1 (ledger_sync.rs ~:140-160: ensure views/ in .orgasmic/.gitignore, git rm --cached tracked views/*) behind an explicit path: orgasmic project migrate (project_migrate.rs) applies it idempotently to a git-repo project; orgasmic doctor reports 'views/* tracked in git' with the exact command while it is not applied. init_project must not skip the ignore rule when .gitignore already exists (projects.rs:188).\"\n\nAnd the acceptance criteria:\n- [ ] Plain-branch fixture with tracked .orgasmic/views/board.org: doctor warns; project migrate untracks + ignores; second run is a no-op; doctor is quiet.\n- [ ] Ledger-without-remote fixture behaves the same.\n- [ ] Daemon never runs git rm --cached outside the synced-ledger loop.\n- [ ] clippy -D; fmt; targeted cli/daemon tests green.\n\nHmm, so the assignment is narrower than the dispatch brief. The assignment focuses on:\n1. `orgasmic project migrate` (project_migrate.rs) — apply views untracking idempotently\n2. `orgasmic doctor` — report 'views/* tracked in git' with exact command while not applied\n3. `init_project` must not skip the ignore rule when .gitignore already exists (projects.rs:188)\n\nBut the dispatch brief (manager handoff) describes the full TASK-JWHXH.3 including views.rs refactor, index.rs changes, api.rs get_org_file, CLI views build deletion, prompt-studio/docs, tests.\n\nHmm, which one do I follow? The assignment says \"Implement dec_AF61D\" and describes a narrower scope. The dispatch brief is the \"Manager handoff content supplied at dispatch time\" which is the full task. Let me first read the task record via `orgasmic task get --project orgasmic TASK-JWHXH.3` and dec_AF61D to understand.\n\nActually wait — the assignment says \"Implement dec_AF61D\" — I need to find that decision. Let me look at the repo. The working directory is /Users/aspirational/.orgasmic/worktrees/orgasmic/task-jwhxh.3. This is a worktree of the orgasmic repo. The live ledger is at ~/.orgasmic/ledgers/orgasmic which I must never touch (well, read-only commands like `orgasmic task get` are fine — \"never touch the live ledger\" means don't modify it).\n\nLet me start by reading the task and exploring the codebase.\n\nThe scope question: The assignment (which is the authoritative task assignment) says:\n\n\"Reuse the sync-loop code from TASK-JWHXH.1 (ledger_sync.rs ~:140-160: ensure views/ in .orgasmic/.gitignore, git rm --cached tracked views/*) behind an explicit path: orgasmic project migrate (project_migrate.rs) applies it idempotently to a git-repo project; orgasmic doctor reports 'views/* tracked in git' with the exact command while it is not applied. init_project must not skip the ignore rule when .gitignore already exists (projects.rs:188).\"\n\nThis looks like just the migration part (part 5 of the brief) plus the projects.rs:188 fix. The dispatch brief includes the full thing. Hmm.\n\nActually, wait. Let me re-read. The task title: \"Stop writing .orgasmic/views/*.org; render on demand; project migrate + doctor for stragglers.\" The assignment is a subset focusing on the migrate + doctor part. The acceptance criteria only mention doctor/migrate/daemon-git-rm/clippy/fmt/tests.\n\nHmm, but the dispatch brief says the full change. This is confusing. The instructions say \"Read the task record, then `project.org` and `gotchas.org`, then only the files the assignment references.\"\n\nLet me read the task record first — `orgasmic task get --project orgasmic TASK-JWHXH.3`. That should clarify. Also dec_AF61D.\n\nActually, maybe the task was split: TASK-JWHXH.3 might have been narrowed to just the migrate+doctor part, with the rest in other tasks. The acceptance criteria in the assignment strongly suggest the narrower scope:\n\n- Plain-branch fixture with tracked .orgasmic/views/board.org: doctor warns; project migrate untracks + ignores; second run is a no-op; doctor is quiet.\n- Ledger-without-remote fixture behaves the same.\n- Daemon never runs git rm --cached outside the synced-ledger loop.\n- clippy -D; fmt; targeted cli/daemon tests green.\n\nThis is clearly just the migrate/doctor work. The dispatch brief may be stale (from the original task creation before splitting). The instruction says \"Implement TASK-JWHXH.3 without widening the task\" and \"Code is authoritative once written\" and \"Treat any prior agent result in the assignment or dispatch brief as a claim. Reproduce or inspect before relying on it for completion.\"\n\nAlso: \"If the assignment's premise is false or already satisfied, stop and return a blocker with evidence instead of manufacturing the requested output.\"\n\nSo the plan: read the task record to see the current state of the task (it may have been updated/narrowed). Let me do that first.\n\nLet me start exploring. I'll run:\n1. `orgasmic task get --project orgasmic TASK-JWHXH.3` (read-only)\n2. Look at the worktree structure.\n\nThen read the referenced files:\n- crates/orgasmic-daemon/src/ledger_sync.rs (~140-160)\n- crates/orgasmic-cli/src/project_migrate.rs\n- crates/orgasmic-cli/src/doctor.rs\n- crates/orgasmic-core/src/projects.rs (~188)\n- crates/orgasmic-core/src/views.rs\n- crates/orgasmic-daemon/src/index.rs\n\nLet me check whether the views-on-demand refactor has already landed (maybe TASK-JWHXH.2 or .1 did it). The worktree is task-jwhxh.3 — maybe it already has the earlier sibling tasks landed, or maybe not.\n\nLet me go.The task record confirms the scope: the notes say \"Scope rewritten 2026-09-02 per dec_XH2XY: DELETE the on-disk views...\" — so the full dispatch brief is the current scope. The assignment text in my prompt is the older description; the notes rewrite it. The task implements dec_AF61D AND dec_XH2XY.\n\nLet me read the two decisions, the brief at scratchpad/brief-TASK-JWHXH.3.md, project.org and gotchas.org. The brief is in the ledger at /Users/aspirational/.orgasmic/ledgers/orgasmic/scratchpad/brief-TASK-JWHXH.3.md.The task record confirms the full rewritten scope (dec_XH2XY: delete on-disk views + migrate/doctor). Let me read the decisions and brief.The brief file doesn't exist at that path. Let me check for it elsewhere. The notes say \"Brief: scratchpad/brief-TASK-JWHXH.3.md\". Maybe it's relative to the ledger — /Users/aspirational/.orgasmic/ledgers/orgasmic/scratchpad/. Let me check.The brief matches the dispatch brief in my prompt. Now I understand the full scope. Let me set up a todo list and explore the relevant files:\n\n1. `crates/orgasmic-core/src/views.rs`\n2. `crates/orgasmic-core/src/projects.rs`\n3. `crates/orgasmic-daemon/src/index.rs`\n4. `crates/orgasmic-daemon/src/api.rs` (get_org_file)\n5. `crates/orgasmic-cli/src/main.rs` (views build)\n6. `crates/orgasmic-cli/src/project_migrate.rs`\n7. `crates/orgasmic-cli/src/doctor.rs`\n8. `crates/orgasmic-daemon/src/ledger_sync.rs`\n9. prompt-studio context packs + prompt-parts + prompt-specs + skill docs\n10. Tests\n\nLet me read the core files first. Start with views.rs, projects.rs, and the relevant line ranges in the daemon files. These are large files, so I'll read targeted ranges. Let me check file sizes first.The brief is confirmed. Setting up the work plan and reading the code.Interesting — the current projects.rs doesn't match the brief's line numbers exactly. Let me look at the scaffold constants at the top of projects.rs — SCAFFOLD_DIRS, SCAFFOLD_FILES, and the .gitignore template at line 399 (`\"tmp/\\nviews/\\n\"`). The brief says: \"Delete the scaffold of `views/` + `board.org` in `crates/orgasmic-core/src/projects.rs` (~:188 skip-existing, ~:339 \"board.org failed to parse after write\", ~:399/:593 fixtures); keep `tmp/` in the scaffold .gitignore.\"\n\nWait — the scaffold .gitignore has \"tmp/\\nviews/\\n\" (line 399 in the test fixture). The real shipped scaffold is at shipped/scaffold/.gitignore or similar. Let me check the top of projects.rs and the shipped scaffold dir.\n\nAlso the assignment says \"init_project must not skip the ignore rule when .gitignore already exists (projects.rs:188)\" — line 188 is `if dest.exists() { continue; }`. Hmm, but wait — the brief says delete the scaffold of views/ + board.org. Let me look at the actual scaffold files.Interesting. The shipped dir contains `entry`, `project-scaffold`, `prompt-studio`, `schema`, `skills`, `workflows`. Note `ls -R shipped/scaffold` returned nothing because it's `shipped/project-scaffold`. Let me check the shipped scaffold contents.Good. The scaffold:\n- `shipped/project-scaffold/.gitignore` contains `tmp/\\nviews/\\n` — the brief says \"keep `tmp/` in the scaffold .gitignore\". Since views/ directory will no longer exist, the `views/` line in the scaffold .gitignore... hmm, the brief says \"keep `tmp/` in the scaffold .gitignore\" — implying remove `views/` from it? Actually wait — re-reading: \"Delete the scaffold of `views/` + `board.org` in `crates/orgasmic-core/src/projects.rs` (~:188 skip-existing, ~:339 ... ~:399/:593 fixtures); keep `tmp/` in the scaffold .gitignore.\"\n\nHmm — \"scaffold of views/ + board.org\" — where is views/ scaffolded? Looking at SCAFFOLD_DIRS — no \"views\" dir. SCAFFOLD_FILES — no views/board.org. Hmm wait, maybe the current code doesn't scaffold views at all. Let me check where `.orgasmic/views` gets created elsewhere. In views.rs `build_views` creates it. And in projects.rs? Let me grep for \"views\" in projects.rs.\n\nActually the ~:188 reference in the assignment (\"init_project must not skip the ignore rule when .gitignore already exists (projects.rs:188)\") refers to line 188 `if dest.exists() { continue; }` — i.e., when `.orgasmic/.gitignore` already exists, init skips writing it, so the ignore rule isn't applied. But wait — under the new decision (delete views), do we still need the ignore rule? Hmm.\n\nActually wait. Let me re-read the current state. The assignment description is from BEFORE the scope rewrite (dec_XH2XY). The notes say the scope was rewritten. Under the new scope:\n\n- No more .orgasmic/views/ directory at all.\n- project migrate untracks (git rm -r --cached) and deletes .orgasmic/views/; doctor warns while present.\n- Scaffold no longer creates views/ (notes).\n\nLet me check whether the scaffold currently creates views/. Grep projects.rs for \"views\":Now let me look at the index.rs areas in detail: ~830-1000 (debounced rebuild + claim views), ~1180-1200 (VIEWS usage), ~3000-3040 (board entry), and the tests ~5790-5860. Also check where VIEWS is used at line 1190.Now let me see where `schedule_view_rebuild` is called, where `view_dirty_roots`, `view_drain_scheduled`, `VIEW_REBUILD_DEBOUNCE` are declared/used, and line ~1190 VIEWS usage and ~3029 board entry.Now the third call site ~3029 and the surrounding context, plus the VIEWS const usage. Let me view ~2990-3060.Now let me look at api.rs `get_org_file` (~14512), `post_org_file` (~14632), and the tests around ~21318/21326. Also `writer.rs:38` DAEMON_OWNED_SURFACES, `home.rs` for context. And ledger_sync.rs ~140-160 and tests ~2418/2443.Ledger sync loop (TASK-JWHXH.1 code): lines 136-165. Per the brief: \"For the synced-ledger loop (`ledger_sync.rs` ~:140-160, TASK-JWHXH.1): keep the untrack, and also remove the now-dead `views/` directory there (daemon owns it).\"\n\nSo in ledger_sync, I need to:\n- keep `git rm -r -q --cached --ignore-unmatch -- .orgasmic/views`\n- ALSO remove the `.orgasmic/views` directory (since daemon owns this ledger, it can delete the now-dead dir)\n- What about the .gitignore ensure rule? Since views/ no longer exists, should we still write `views/` into .gitignore? The decision dec_XH2XY says \"No more .orgasmic/views/ directory.\" The ignore rule would prevent accidental re-adding. Hmm — \"keep the untrack, and also remove the now-dead views/ directory there (daemon owns it)\". The brief says keep the untrack. For the ignore-rule writing... if the directory is deleted, keeping the `views/` line in .gitignore is harmless but also now moot. But actually, keeping the ignore rule would be good defense. Hmm, but simplest reading: keep the untrack (git rm --cached), and add removal of the dir. The .gitignore ensure — I could keep it too; it's idempotent and harmless. But wait — is it needed? If we delete the dir and never write views again, nothing will create .orgasmic/views again. The ignore line is dead weight but harmless. The brief says \"keep the untrack, and also remove the now-dead `views/` directory there\". It doesn't say to remove the ignore-rule ensure. I'll keep the ignore ensure (still useful as belt-and-braces for old clients that might write views files) — actually hmm. Let me think about minimizing: the scaffold .gitignore still gets written by init_project with tmp/. Should scaffold gitignore still contain views/? The brief says \"keep `tmp/` in the scaffold .gitignore\" — that phrasing suggests the scaffold .gitignore may lose `views/`... but actually it might mean \"when deleting the views scaffold bits, keep tmp/\". The shipped scaffold .gitignore currently is \"tmp/\\nviews/\\n\". Since views/ dir no longer exists, the `views/` line is obsolete. But for existing projects that still carry an old daemon... no, decision is delete.\n\nHmm, actually — there's a subtlety: `project migrate` untracks + deletes. If the .gitignore doesn't ignore views/, and some old daemon binary writes views again, they'd get committed again. But we're deleting all writers, so that can't happen. I'll remove `views/` from the shipped scaffold .gitignore (leaving `tmp/`) per \"keep tmp/ in the scaffold .gitignore\", and in ledger_sync keep the gitignore ensure? Hmm wait — if scaffold gitignore drops views/, but ledger_sync keeps adding views/ to .gitignore... inconsistent.\n\nLet me re-read ledger_sync code purpose: the sync loop ensures views/ in .orgasmic/.gitignore and untracks. Under dec_XH2XY: \"Synced ledgers clean their own dir inside the sync loop.\" So the sync loop should: untrack (git rm -r --cached) AND delete the dir. The gitignore ensure — with the dir deleted, keeping the ignore line is optional. But \"keep the untrack, and also remove the now-dead views/ directory\" — the minimal reading is: keep lines 154-165 (the git rm for views), add dir removal. The .gitignore block (144-153) — hmm.\n\nActually, thinking about it more: the .gitignore ensure protects against any stale tracked views files in history being re-added... no, gitignore doesn't affect tracked files. Its purpose was to prevent build_views output from being committed. With no more writers, it's dead. But it's also the mechanism that makes `git rm --cached` \"stick\" — without the ignore, the deleted-from-index files would show as untracked files in the worktree until the dir is removed. Since we remove the dir in the same breath, that's moot.\n\nDecision: in ledger_sync, replace the gitignore-ensure + untrack block with: untrack + remove dir. Actually wait — order matters: `git rm -r --cached -- .orgasmic/views` requires the path to exist in the index (with --ignore-unmatch it's fine either way). Then remove the dir from disk. If I remove the dir first, git rm --cached still works from the index. Simplest: keep git rm as-is, then `std::fs::remove_dir_all(dotorg.join(\"views\"))` ignoring NotFound.\n\nShould I keep the .gitignore ensure? Let me think about what the \"views/\" ignore line does post-migration: nothing writes views/, so nothing to ignore. But the tmp ignore... note the .gitignore in .orgasmic comes from the scaffold (\"tmp/\\nviews/\\n\"). If I remove views/ from the scaffold template but keep the ensure in ledger_sync, ledgers would get views/ re-added to their .gitignore forever. That's weird. I'll remove the ensure from ledger_sync too — replaced by dir removal. Hmm, but wait: \"keep the untrack\" — keep the git rm --cached. And the dir removal. The ignore-ensure becomes dead code; removing it is consistent with \"delete the machinery\".\n\nHmm, actually let me reconsider once more. dec_AF61D (pre-rewrite) said \"Every registered project gets views/ in .orgasmic/.gitignore\". dec_XH2XY refines: \"instead of keeping and gitignoring .orgasmic/views/*.org, the daemon stops writing them at all... Migration stays explicit...: orgasmic project migrate untracks (git rm -r --cached) and deletes .orgasmic/views/\". So the ignore rule is gone from the decision. The scaffold .gitignore keeps tmp/ only.\n\nOK so plan for ledger_sync:\n```rust\ngit(ledger, &[\"rm\",\"-r\",\"-q\",\"--cached\",\"--ignore-unmatch\",\"--\",\".orgasmic/views\"])?;\n// delete the now-dead views dir (daemon owns the synced ledger)\nmatch std::fs::remove_dir_all(dotorg.join(\"views\")) {\n    Ok(()) => {}\n    Err(error) if error.kind() == std::io::ErrorKind::NotFound => {}\n    Err(error) => return Err(error).context(\"remove .orgasmic/views\"),\n}\n```\nAnd remove the .gitignore read/ensure block (lines 137-153), keeping the tmp untrack git rm below.\n\nWait, but the `dotorg.exists()` check wraps everything including the tmp untrack. Keep that structure.\n\nAlso note: after removing the gitignore-ensure, does anything else in ledger_sync reference views? Let me check tests at 2418/2443 later.\n\nNow, `project_migrate.rs` — let me read it. And doctor.rs. Let me also read api.rs get_org_file/post_org_file.Now the CLI side — project_migrate.rs, doctor.rs, main.rs views command, and api.rs get_org_file.Now let me look at:\n1. `read_artifact` — used by get_org_file to read contents.\n2. main.rs views command (~498, ~1984).\n3. doctor.rs structure.\n4. writer.rs DAEMON_OWNED_SURFACES context.\n5. home.rs — check whether home has views references (brief says don't touch home.rs user/board.org).\n\nAlso, `VIEWS` const in views.rs is used at index.rs:1190. If I delete `build_views`, keep `VIEWS` for render_view mapping.\n\nLet me look at main.rs views subcommand and doctor.Now I understand doctor.rs. I need to add a per-registered-project check: for each registered project that is a git repo, warn while `.orgasmic/views/` is tracked or present. The doctor has access to `home`; I need `projects::read_board(home)` — but note doctor.rs uses `crate::home::Home` (the CLI's Home?). Let me check: `use crate::home::Home;` — there's a CLI-level home module. And orgasmic_core has Home too. Let me check what `crate::home::Home` is — probably a re-export.\n\nAlso for doctor to check \"tracked in git\", I need `git ls-files --error-unmatch .orgasmic/views` or `git ls-files .orgasmic/views` non-empty. Simple: run `git -C <root> ls-files -- .orgasmic/views` and check for output; plus check dir existence.\n\nNow the migration path in project_migrate.rs: the existing `project migrate` is the v1→v2 node migration. I need to add the views untrack+delete to it, idempotently. Note `run_at` currently:\n- plan(root)\n- if to_branch && no old_files && is_ledger_root → \"already migrated\" early return\n- refuse_dirty_tree\n- if to_branch && !dry_run && !is_ledger_root → migrate_to_branch\n- else if old_files.is_empty() → \"already migrated\" early return\n- else if !dry_run → apply_with_recovery\n\nThe early returns (\"already migrated\") would skip a views cleanup that's still pending. So I should handle views cleanup BEFORE those early returns, or fold it into the flow. Care: `refuse_dirty_tree` — if views files are tracked but unmodified, tree is clean. But after `git rm --cached`, the tree becomes \"dirty\" (staged deletions + untracked files until dir removed... actually git rm --cached removes from index; the files remain on disk as untracked → `--untracked-files=all` would show them). So: run git rm -r --cached --quiet -- .orgasmic/views, then remove the dir. After both, `git status --porcelain` shows staged deletions (D .orgasmic/views/...). That's fine — operator commits it (like the to_branch flow which tells the operator to commit).\n\nOrder in run_at: the views migration should happen even when the node migration is already applied. Let me restructure:\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    ...existing early return logic but with views check folded in...\n}\n```\n\nHmm, let me think about the desired behavior per acceptance:\n- fixture with tracked .orgasmic/views/board.org: `project migrate` untracks + deletes; second run is a no-op (prints \"already migrated\"?).\n- doctor warns before, quiet after.\n\nDesign: a helper `views_migrate(root, dry_run) -> Result<bool>` (returns whether it did anything) in project_migrate.rs:\n\n```rust\n/// dec_AF61D/dec_XH2XY: derived views are never committed. Untrack and delete\n/// `.orgasmic/views/` in a git-repo project; idempotent.\nfn migrate_views(root: &Path, dry_run: bool) -> Result<bool> {\n    let views = root.join(\".orgasmic/views\");\n    let tracked = !git_capture(root, &[\"ls-files\", \"--\", \".orgasmic/views\"])\n        .unwrap_or_default()  // hmm, need to distinguish not-a-repo\n        .is_empty();\n    ...\n}\n```\n\nWait — should this only apply to git repos? \"applies it idempotently to a git-repo project\". If root isn't a git repo, git commands fail. ls-files in a non-repo errors. I should check `git rev-parse --is-inside-work-tree` first, or handle error → treat as not tracked. But \"delete the directory\" — if not a git repo, should we still delete the views dir? The daemon no longer writes views, so a leftover dir in a non-git project is dead but harmless. The brief says \"if `.orgasmic/views/` is tracked, run `git rm -r --cached --quiet -- .orgasmic/views`; then delete the directory\". So the whole operation is gated on tracked. Hmm, but doctor: \"warn `.orgasmic/views/ still tracked/present — run: orgasmic project migrate` while it is [present]\". \"tracked/present\" — so doctor warns if either tracked or present. But then for a non-git project with a present-but-untracked views dir, doctor would warn \"run project migrate\" but migrate (gated on tracked) wouldn't do anything... that leaves the operator stuck with an eternal warning. So migrate should also delete the dir when present-but-untracked? Let's make migrate: if tracked → git rm -r --cached; if dir present → delete. That makes doctor's warning always actionable. And idempotent: when neither tracked nor present → no-op.\n\nBut careful about the `to_branch` ledger-root early return: `run_at` with to_branch on a ledger root returns \"already migrated\" before doing anything. Should views cleanup run there too? The ledger is daemon-synced; the sync loop now untracks + removes the dir itself. But project migrate on a ledger root should probably also clean it (operator explicit verb). Simplest: run views cleanup in run_at before/independently of the early returns, but respect dry_run and refuse_dirty_tree ordering.\n\nCurrent flow has refuse_dirty_tree AFTER the early returns. For the views path: a tracked, committed views file leaves a clean tree, so refuse_dirty_tree passes. Good. But if I do views cleanup before refuse_dirty_tree, a dirty tree from a prior partial run would... hmm. Let me design:\n\n```rust\nfn run_at(home, root, dry_run, to_branch) -> Result<()> {\n    let migration = plan(root)?;\n    let views_pending = views_cleanup_pending(root)?; // tracked or dir present\n    if to_branch && migration.old_files.is_empty() && !views_pending && is_ledger_root(...)? {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;\n    let mut did_views = false;\n    if !dry_run {\n        did_views = migrate_views(root)?;   // untrack + delete dir\n    }\n    if to_branch && !is_ledger_root(...)? {\n        migrate_to_branch(...)  // this removes .orgasmic entirely from source tree\n    } else if migration.old_files.is_empty() && !did_views {\n        println!(\"already migrated\");\n        return Ok(());\n    } else if !migration.old_files.is_empty() {\n        apply_with_recovery(root, &migration)?;\n    }\n    ...\n}\n```\n\nHmm wait, there's a subtlety with the existing second early return: `else if migration.old_files.is_empty() { println!(\"already migrated\"); return Ok(()); }` — that's for !to_branch runs where node migration is done. If views cleanup pending, we don't want the early return. And \"second run is a no-op\" — the acceptance says second run is a no-op; printing \"already migrated\" is fine (it's the existing no-op output).\n\nWait, actually there's an ordering problem: `refuse_dirty_tree` before views cleanup. On the FIRST run, tree is clean (views tracked + committed). git rm --cached + dir delete → staged deletions. Second run: `refuse_dirty_tree` would FAIL because the tree has staged deletions from run 1 that the operator hasn't committed yet! The acceptance says \"second run is a no-op\". So on second run, either (a) the test commits between runs (unlikely — \"second run is a no-op; doctor is quiet\" — but doctor quiet requires the file gone from disk; doctor checks tracked via ls-files which after git rm --cached + commit... hmm).\n\nLet me think about what \"doctor is quiet\" means after migrate: doctor warns while views tracked OR present. After migrate (without commit): views dir deleted, untracked from index → `git ls-files -- .orgasmic/views` returns empty → not tracked; dir gone → not present. Doctor quiet. The staged deletion is still pending commit, but doctor doesn't check that.\n\nFor second run no-op: after run 1 (no commit), the tree is dirty (staged D .orgasmic/views/board.org). If run 2 calls refuse_dirty_tree first, it bails. So I must handle views cleanup BEFORE refuse_dirty_tree, or make refuse_dirty_tree tolerate the exact staged-deletion state. Cleaner: do the views cleanup before refuse_dirty_tree (it only makes the tree dirty in a way it itself created, idempotently). On second run: tracked? no (ls-files empty after rm --cached). dir present? no. → views_pending = false, no-op. Then the rest of the flow proceeds as before.\n\nBut wait — refuse_dirty_tree is also protecting the node migration. Doing views cleanup before it means we mutate the index before checking cleanliness. The views mutation itself doesn't conflict with anything in the node migration (paths under .orgasmic/views, which the node migration doesn't touch... the v1→v2 migration moves tasks/*.org etc.). Acceptable — and the test acceptance demands idempotent second run.\n\nHmm, but actually, hold on. What if the tree is dirty with UNRELATED changes? Doing git rm --cached views first, then refuse_dirty_tree bails → we've left the repo with a staged deletion even though the command failed. Slightly rude but recoverable (git reset). Alternatively, run refuse_dirty_tree but allow the specific post-views state. Simplest correct order that satisfies the acceptance AND keeps the dirty-refusal meaningful:\n\n1. Check views pending (tracked or present).\n2. If dry_run → just report? The existing dry-run path: plan + prints DRY RUN without applying (apply_with_recovery only when !dry_run). For views in dry-run: report pending but don't act.\n3. refuse_dirty_tree — but if views cleanup itself ran in a previous uncommitted run, the tree shows staged deletions of .orgasmic/views/*. Hmm — the acceptance \"second run is a no-op\" — if the test does NOT commit between runs, refuse_dirty_tree fails. Unless... the test commits. Let me write the test to commit between runs? The acceptance says \"second run is a no-op; doctor is quiet\". I control the test. But a real operator might run migrate twice without committing. The task says idempotent (second run is a no-op). I should make it actually idempotent without requiring an intermediate commit.\n\nOption: order as (a) views cleanup first (before refuse_dirty_tree), (b) then refuse_dirty_tree for the node/branch migration. Second run without commit: views_pending=false → skip; then refuse_dirty_tree bails on the staged deletion from run 1 → NOT a no-op; it errors!\n\nHmm. So for true idempotency, refuse_dirty_tree must not count the staged views deletions as dirty. I could filter: run `git status --porcelain=v1 -z --untracked-files=all`, parse, ignore entries whose path is under .orgasmic/views/ with staged deletion (X= 'D', or any status). Actually simpler: after views cleanup, the only dirt we introduced is staged deletions under .orgasmic/views. If I filter out all status entries for paths under .orgasmic/views/, then:\n- run 1: tree clean → proceed.\n- run 2 (uncommitted): status shows only `.orgasmic/views/...` staged D entries → filtered → clean → proceed → views_pending=false → \"already migrated\" no-op.\n\nThat works and is defensible: the refuse-dirty guard protects the node migration's recovery story; views deletions are exactly the thing this command creates.\n\nAlternatively, git rm --cached + delete dir leaves NO untracked files (dir deleted), only staged D. Filtering by path prefix is easy with porcelain -z parsing. The -z format: entries `XY path\\0` (and for renames `XY new\\0old\\0`). Staged deletion of .orgasmic/views/board.org → `D  .orgasmic/views/board.org`. I'll parse: split by NUL; each record starts with 2 status chars + space. Check if the path starts with \".orgasmic/views/\". Good.\n\nHmm wait, but there's another wrinkle: what about the `to_branch` full-flow test (branch_cutover_is_orphan_dry_run_idempotent_and_worker_discoverable)? It registers a project with `.gitignore` \"tmp/\\n\" (no views), commits .orgasmic, runs run_at(dry), then run_at(false, true). With my changes: views_pending — .orgasmic/views doesn't exist there → false. Flow unchanged. \n\nNow — what about \"Ledger-without-remote fixture behaves the same\"? That's for DOCTOR + migrate on a ledger project that has no git remote (so the daemon sync loop is Idle — git remote get-url origin fails → SyncOutcome::Idle, the untrack code at line 104-106 returns Idle BEFORE the views cleanup! Wait: `if git_optional(ledger, &[\"remote\", \"get-url\", \"origin\"])?.is_none() { return Ok(SyncOutcome::Idle); }` — yes, a ledger with no remote skips the whole dotorg cleanup block. So a no-remote ledger with tracked views needs doctor + project migrate. That's the second acceptance fixture. Fine — same migrate code path; is_ledger_root is true for a ledger root; with to_branch... hmm.\n\nWait — when would someone run `orgasmic project migrate` on a ledger root? The CLI command — let me check main.rs for how project migrate is invoked (flags). `Cmd::Project { ... }`? Let me grep. Also check `run_at` signature usage: `run(home, dry_run, to_branch)` uses find_project_root. For a ledger-without-remote, find_project_root presumably resolves to the ledger path. Then to_branch=true, is_ledger_root=true, old_files empty → \"already migrated\" early return → views cleanup must run BEFORE that early return. OK so my ordering: views cleanup before the early returns. But note run 1 on that ledger: refuse_dirty_tree... where exactly?\n\nLet me restructure run_at carefully:\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    let views = ViewsCleanup::plan(root)?;   // tracked files + dir presence\n    if to_branch\n        && migration.old_files.is_empty()\n        && views.is_clean()\n        && is_ledger_root(home, root, &migration.project_source)?\n    {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;   // ignores .orgasmic/views paths\n    if !dry_run {\n        views.apply(root)?;     // git rm -r --cached --quiet --ignore-unmatch -- .orgasmic/views ; remove_dir_all\n    }\n    if to_branch && !dry_run && !is_ledger_root(home, root, &migration.project_source)? {\n        migrate_to_branch(home, root, &migration)?;\n    } else if migration.old_files.is_empty() {\n        if views.is_clean() || dry_run && views.is_clean() ... hmm\n```\n\nThe tricky bit: the `else if migration.old_files.is_empty() { println!(\"already migrated\"); return Ok(()); }` path. After applying views cleanup (run 1, plain branch, old_files empty): we want output \"MIGRATED ... views untracked\" not \"already migrated\". Run 2: views clean → \"already migrated\". In dry-run with pending views: print DRY RUN + mention views.\n\nLet me now think about what apply does when views pending but old_files empty: it must print something informative. I'll add to the summary output: `  views untracked` / count of untracked files. Keep it simple:\n\n- `Migration` gets a field or run_at computes `views_tracked: Vec<String>` (ls-files output) and `views_dir_present: bool` before acting.\n- After the flow, print `  views {} untracked` hmm.\n\nLet me write:\n\n```rust\nstruct ViewsState {\n    tracked: Vec<String>,   // git ls-files -- .orgasmic/views\n    dir_present: bool,\n}\nimpl ViewsState {\n    fn plan(root: &Path) -> Result<Self> { ... }  // non-repo → tracked empty\n    fn pending(&self) -> bool { !self.tracked.is_empty() || self.dir_present }\n}\n```\n\nFor plan in a non-git dir: `git ls-files` errors (not a repo). Treat error as \"not tracked\" — but still dir_present check. And `git rm` would fail — only run when !tracked.is_empty() (which requires a working repo). Deleting the dir when present-but-untracked in a non-repo: fine, plain fs op. Should doctor also warn for non-repo projects with views present? Brief: \"for each registered project that is a git repo, warn ... while it is [tracked/present]\". So doctor only checks git-repo projects. But migrate deleting a present dir in a non-repo is harmless and matches \"then delete the directory\". Actually — careful: if a project is NOT a git repo and has views/, migrate deletes them. That's consistent with \"no more views on disk\". OK.\n\nHmm, wait. Should `project migrate` even run for non-git-repo roots? The existing migrate already runs git commands unconditionally (refuse_dirty_tree would fail in a non-repo: `git status` fails → bail \"git status failed\"). So in practice root must be a git repo already. In the git-repo case: tracked = ls-files output; if ls-files fails for some reason, bail? For robustness: if `git rev-parse --is-inside-work-tree` says not a repo... existing code would already have failed at refuse_dirty_tree before reaching views apply. But my new order puts views BEFORE refuse_dirty_tree (to get idempotency). Hmm, wait — do I actually need views before refuse_dirty_tree? I decided refuse_dirty_tree will FILTER .orgasmic/views paths. Then order can stay: refuse_dirty_tree first (filtered), then views.apply. Run 2 without commit: staged D .orgasmic/views entries filtered → passes → views pending=false → \"already migrated\". \n\nThat keeps the original order intact — less churn. But one more case: untracked views files on disk (present but not tracked, e.g. after an old daemon wrote them without commit). `--untracked-files=all` shows `?? .orgasmic/views/board.org` → my filter ignores paths under .orgasmic/views/ → passes. views.apply: tracked empty → skip git rm; dir present → delete. Good.\n\nNow, where does apply happen relative to migrate_to_branch? migrate_to_branch removes the whole `.orgasmic` from the source tree via remove_dir_all, and copies .orgasmic (minus?) to the ledger... wait, copy_tree copies the whole .orgasmic including views/ if present! Let me check: `copy_tree(&root.join(\".orgasmic\"), &stage.path().join(\".orgasmic\"))` — yes, it would copy views/ into the staged ledger, then `git add --all -- .orgasmic` in create_orphan_branch would track views in the new ledger branch! So views cleanup MUST happen BEFORE migrate_to_branch. My order: views.apply(root) before the to_branch branch — good. Also `source_tmp` copy: only tmp. OK.\n\nAlso `git add --all -- .orgasmic` — after views cleanup, staged deletions of views files are in the index; `git add --all -- .orgasmic` operates on a SEPARATE index (GIT_INDEX_FILE) — create_orphan_branch uses git_env with its own index read-tree --empty. Fine.\n\nAnd in migrate_to_branch's failure-recovery messages — views deletions staged... whatever, edge.\n\nNow output format. Current output after apply:\n```\nMIGRATED\n  decisions.nodes 1\n  glossary.nodes 1\n  tasks.nodes 1\n  nodes 3\n  bytes 123\n  heading_round_trip byte-for-byte\n```\nFor a views-only migration, headings empty, nodes 0, bytes 0. I'll add a line when views were cleaned: `  views untracked (N files) and directory removed` — printed when the run cleaned views. In dry-run with pending views, the DRY RUN summary should mention it: `  views N tracked files` or so. Let me keep it minimal:\n\n- After the \"DRY RUN\"/\"MIGRATED\" line and headings, print `  views {}` where {} is like \"3 untracked + dir removed\" (applied) or \"3 tracked, dir present\" (dry run). Hmm. Simplest: \n  - applied: `  views untracked 3, directory removed` — but only if it did something or pending in dry-run.\n\nLet me look at how tests will assert. I'll write tests asserting on FS/git state, not stdout text, mostly. And doctor message contains \"orgasmic project migrate\".\n\nNow the doctor side. `diagnose(home)` — add `push_tracked_views_findings(&mut out, home)`:\n\n```rust\nfn push_tracked_views_findings(out: &mut Vec<Finding>, home: &Home) {\n    let Ok(entries) = orgasmic_core::projects::read_board(home) else { return; };\n    for entry in entries {\n        let root = &entry.path;\n        if !root.join(\".git\").exists() && git rev-parse check ... \n    }\n}\n```\n\n\"for each registered project that is a git repo\" — check with `git -C root rev-parse --is-inside-work-tree` == \"true\" (worktrees! a ledger worktree has .git as a FILE, so `root.join(\".git\").is_dir()` is wrong for worktrees — the ledger IS a worktree of the user repo, per migrate_to_branch. So use rev-parse). Then tracked check: `git -C root ls-files -- .orgasmic/views` non-empty OR dir present → warn:\n\n`.orgasmic/views/ still tracked/present — run: orgasmic project migrate`\n\nHmm, exact wording from brief: \"warn `.orgasmic/views/ still tracked/present — run: orgasmic project migrate` while it is\". The assignment (older) says: doctor reports 'views/* tracked in git' with the exact command. I'll craft: \n\n`Finding::Warn(format!(\"{}: .orgasmic/views/ still tracked/present — run: orgasmic project migrate\", root.display()))`\n\nAlso — wait, should doctor skip projects whose root IS a daemon-synced ledger (remote present)? The daemon cleans those itself in the sync loop. But \"while it is [present]\" — the daemon will clean it within a tick. Brief says plainly \"for each registered project that is a git repo\". Keep it simple: warn for all registered git-repo projects with views tracked/present. The daemon will clean its own; transient warning until then. Fine.\n\nWhich Home does CLI doctor use — `crate::home::Home`. Let me check what that is and whether read_board is reachable from CLI doctor (orgasmic_core::projects::read_board(home: &Home) — orgasmic_core::Home). Is crate::home::Home the same type? Let me check crates/orgasmic-cli/src/home.rs.\n\nAlso main.rs: cmd_views_build deletion, Cmd::Views enum variant, ViewsCmd, help fixture cli-help/views/build.txt. Check for tests referencing \"views build\".\n\nNow the api.rs get_org_file render-on-demand. Current: reads via read_artifact(&path, label). New: if rel is `.orgasmic/views/<name>.org` where name in {board, decisions, glossary}: return orgasmic_core::views::render_view(&project.root, name). Note: validate_org_edit_path requires .org path under .orgasmic — ok. render_view returns Result<String, anyhow::Error>; map to ApiError (500? or 404 if unknown view name → not_found). The OrgView.tsx ORG_FILES — check names used by UI. Let me grep OrgView.tsx.\n\nNote post_org_file refuses writes to views (keep). DAEMON_OWNED_SURFACES keep \"views\".\n\nNow index.rs deletions:\n1. `schedule_view_rebuild` fn + view_dirty_roots/view_drain_scheduled fields + VIEW_REBUILD_DEBOUNCE const + struct init lines.\n2. Call site 1190-1195 (the `if VIEWS.iter().any(...)` block in reload_node_dir presumably).\n3. Call site 972 (claims.org → build_views).\n4. Call site 3029-3035 (board entry load → build_views + push_parse_error).\n5. Check `warn!` import still used elsewhere in index.rs (certainly).\n6. Check `AtomicBool` import still needed (repo_url_refresh_enabled uses it — yes, line 834).\n7. Ordering import at 827-886 region: `Ordering::SeqCst` used elsewhere? grep.\n\nThen tests in index.rs ~5792-5860: refresh_rebuilds_byte_stable_derived_views, incremental_node_write_rebuilds_views_without_claim_churn — delete or rewrite to assert views are NOT written. I'll rewrite the first into \"refresh no longer writes views files\" or just delete both. Brief says \"fix/delete the ones that read the files\". I'll replace with a test asserting refresh does not create .orgasmic/views and that claims.org reload path works (the second test's core was: incremental node write rebuilds views without claim churn). Simpler: delete both, add one test that a node write + claims write leaves no views/ dir. Let me read those tests first.\n\nAlso integration.rs ~312 — check.\n\nviews.rs new API:\n```rust\npub fn render_view(project_root: &Path, name: &str) -> Result<String> {\n    let (_, file, header) = VIEWS.iter().find(|(_, file, _)| *file == name) ... \n```\nBrief: `pub fn render_view(project_root, name)` for board.org/decisions.org/glossary.org. name = file name presumably (\"board.org\"). read_claims + render_collection. render_collection stays private? \"keep `render_collection` as a pure renderer\" — it already is; keep private fn. render_view:\n\n```rust\npub fn render_view(project_root: &Path, file: &str) -> Result<String> {\n    let (_, _, header) = VIEWS\n        .iter()\n        .find(|(_, view_file, _)| *view_file == file)\n        .with_context(|| format!(\"unknown view {file}\"))?;\n    let claims = read_claims(project_root)?;\n    render_collection(project_root, collection, header, &claims)\n}\n```\nWait render_collection takes collection. VIEWS entries: (collection, file, header). Find by file, get collection. Good.\n\nDelete build_views + write_if_changed + TMP_COUNTER. Update tests: keep the ingest-order-independence test but using render_view (compare two renders, check CLAIM_HOLDER/DOUBLE_CLAIM). Delete concurrent_builds test (it tested write_if_changed atomicity — gone). Also check orgasmic-core lib.rs exports `build_views` — grep for re-export.\n\nprojects.rs: the brief mentions deleting \"scaffold of views/ + board.org\" — but I found NO views scaffolding in projects.rs (SCAFFOLD_DIRS/FILES have no views). Only the .gitignore template contents include \"views/\" line (test fixture at :399 writes \"tmp/\\nviews/\\n\" mimicking shipped). And test at :477 asserts gitignore has \"views/\". So: update shipped/project-scaffold/.gitignore → \"tmp/\\n\" and the test fixture + assertion accordingly. Line 188 skip-existing + :339 \"board.org failed to parse after write\" are about the USER board (home board.org) — different file; the brief's line numbers were approximate/mixed. projects.rs:339 \"board.org failed to parse after write\" — that's register_project_locked writing the home board. NOT related to views. Leave it. The brief says \"~:188 skip-existing\" — that's `if dest.exists() { continue; }` for scaffold files. The assignment's \"init_project must not skip the ignore rule when .gitignore already exists (projects.rs:188)\" was the OLD scope (ensure views/ in .gitignore even when file exists). Under dec_XH2XY there's no views/ ignore rule to ensure — the scaffold .gitignore just has tmp/. So nothing to do at :188. Correct per rewritten scope: \"scaffold no longer creates views/\" (notes) — scaffold never created views dirs; it only had the ignore line. Update template + tests.\n\nWait — let me double check SCAFFOLD_DIRS: tasks, tasks/TASK-*, decisions, glossary, artifacts, conventions, tmp, tmp/sessions, tmp/dispatch. No views. Good — so scaffold changes = .gitignore template only (shipped/project-scaffold/.gitignore: remove \"views/\" line; test fixture at :399 and assertion at :477).\n\nLet me read projects.rs tests 420-624 to see the assertions.\n\nThen ledger_sync tests ~2418/2443 — read them.\n\nThen integration.rs ~312.\n\nThen api tests ~21318-21410 (post_org_file refusal fixtures at 21366/21399 + get_org_file tests?). Brief: \"api.rs ~:21318/~:21326 keep the refusal fixtures\". Let me read.\n\nAlso check ui/src/components/OrgView.tsx ORG_FILES to confirm names.\n\nAlso prompt-studio: context-packs sprint_tasks/decisions/glossary.org — grep for references; prompt-parts grill_domain_policy.org ~9-10, graph_authoring_policy.org ~9-10; prompt-specs/manager.org ~45-46; skill docs references/ledger.md ~21, references/recall-resume.md ~44,~:71, operations/core-project.md ~46.\n\nAlso — daemon index.rs:946 `if matches!(parts.get(1).copied(), Some(\"tmp\" | \"views\"))` — keep? views dir no longer written; but stale events... keep harmless. Actually if a user hand-creates .orgasmic/views/foo.org, watcher event → returns Ok(false) — fine, keep (it also guards render reads). Hmm, but clippy/dead code? It's a runtime string match, no dead-code warning. Keep — cheap.\n\nindex.rs:3712 `Some(\"tx\" | \"tmp\" | \"views\" | \"machines\")` — similar, keep.\n\nwriter.rs DAEMON_OWNED_SURFACES keep \"views\" per brief.\n\nLet me now read all the remaining pieces. Start with projects.rs tests (420-624), index.rs tests (5780-5870), ledger_sync tests (2380-2480), integration.rs (~280-340), api tests (21280-21420), main.rs views parts (180-200, 490-510, 1515-1530, 1975-1990), cli-help fixture, OrgView.tsx, prompt-studio files.Now let me read the remaining test/reference sites in parallel.The zsh interpreted `===API-TESTS===` weirdly. `echo ===API-TESTS===` — zsh treats `=foo` as command position expansion. Let me quote it. Also integration.rs line ~312 — I saw assert_invalid_scaffold_request; let me grep integration.rs for views specifically.integration.rs has no views references (grep returned nothing). Good — brief's ~:312 may be stale or about scaffold; skip.\n\napi.rs refusal fixtures exist around 21318-21366 (org_file_rewrite_refuses_ledger_paths). Keep.\n\nNow let me look at:\n- main.rs Views command (~185-200, ~495-505, ~1520-1530, ~1980-1990)\n- cli-help fixtures dir\n- OrgView.tsx ORG_FILES\n- get_org_file existing tests (search for get_org_file tests with views)\n- read_claims signature in core (used by render_view)\n- CLI home module: crate::home::Home — is it a wrapper? doctor.rs `use crate::home::Home;` — check.Key facts:\n- CLI `Home` is a re-export of `orgasmic_core::home::Home` — so doctor can call `orgasmic_core::projects::read_board(home)`.\n- No cli-help fixtures dir found under tests/cli-help — let me check where cli-help fixtures live: `find crates/orgasmic-cli -path '*cli-help*'` returned nothing. Maybe tests/cli_parity.rs references help output. Let me grep for \"views build\" across the repo, and for cli_parity.\n- OrgView.tsx ORG_FILES includes .orgasmic/views/board.org, decisions.org, glossary.org + (line 27 something else, maybe .orgasmic/gotchas.org?). Let me check lines 25-30.\n\nAlso grep the whole repo for \"build_views\", \"views build\", \"sprint_tasks\" etc.Good. Now check:\n1. What includes the context packs (grep for their names in slots/prompt-specs): `grep -rn \"sprint_tasks\\|context-packs\"` more broadly.\n2. Skill docs: shipped/skills/orgasmic/references/ledger.md ~21, references/recall-resume.md ~44,71, operations/core-project.md ~46.\n3. Check `cli_parity.rs` for views references (CLI surface snapshot test).\n4. Check `slot` references like `{{context.sprint_tasks}}`? Let me grep \"sprint_tasks\" everywhere including shipped/skills.Interesting:\n- `sprint_tasks` referenced only by its own file — nothing includes it. But wait, does any prompt-spec *reference* context_packs by id like `context_packs = [...]`? Let me check prompt-specs for `context_packs` fields — the compiler reads `resolved.view.context_packs`. Which specs list context_packs? Grep in shipped/prompt-studio/prompt-specs for \"context\" or \"+context_packs\" property.\n- core-project.md:46 references `cli-help/views/build.txt` — a fixture doc reference. Where are cli-help fixtures? Search whole repo for \"cli-help\".The cli-help fixtures referenced by core-project.md — those `cli-help/*.txt` sources are in some other location (the ledger's artifacts?). The `find ~/.orgasmic/ledgers/orgasmic -path '*cli-help*' -name '*views*'` found nothing (no output before `---`). Let me search for cli-help dirs more broadly. Actually the skill's `sources:` may refer to files that are generated during skill install. Since core-project.md is a shipped skill doc listing `cli-help/views/build.txt` and alias `orgasmic views build`, I must update it (remove the views alias, sources lines, and any prose about views build). Also `shipped/skills` — check for other references to \"views build\" in skills and elsewhere.\n\nLet me check where cli-help fixtures actually live: `find . -type d -name \"cli-help\"`. And check `shipped_conventions.rs` test or `ui.rs` test for ORG_FILES/views usage. Also the corpus.md mentions cli-help — the skill corpus. Let me grep for \"views\" in shipped/skills.Interesting — there's a `corpus-manifest.json` in shipped/skills/orgasmic/meta/ that has hashes for `cli-help/views.txt` and `cli-help/views/build.txt`. Where do these cli-help files live? They're part of the skill corpus — maybe generated at install. If I remove the views subcommand, the manifest entries become stale. Let me look at how the corpus manifest is generated/validated — grep for corpus-manifest in crates.The corpus-manifest.json maps corpus source files to hashes, but nothing in crates/scripts references it (no code reads it in this repo — maybe the orgasmic skill loader elsewhere). The cli-help files themselves aren't in this repo (probably generated from the live CLI into ~/.orgasmic or the skill is installed from elsewhere). Since no code validates it here, I'll update the manifest by removing the two views entries (cli-help/views.txt, cli-help/views/build.txt)? Hmm — careful. The manifest lists hashes of corpus sources; cli-help files are captured CLI help output. If the views subcommand is deleted, future regenerations would drop those files. Removing the entries keeps the manifest consistent with a regenerated corpus. But is the manifest validated by any test? grep found nothing in crates/scripts. There might be a validation in the skill install flow in another repo (the runtime). The safest minimal action consistent with the brief (\"delete the subcommand and its cli-help fixture if present; skill docs updates listed\") — the brief lists only three skill docs (ledger.md, recall-resume.md, core-project.md). core-project.md includes the aliases + sources list which mention views. The corpus-manifest isn't mentioned. Hmm. But leaving stale manifest entries that reference now-nonexistent CLI help... The manifest also contains \"shipped/prompt-studio/...\" entries — wait, it hashes shipped files too! Including prompt-specs/manager.org etc. If I edit manager.org, its hash changes → manifest stale. Is there a check? Let me grep the whole repo for corpus-manifest usage more thoroughly, including ui/.The corpus-manifest.json isn't validated by any code in this repo (no references in crates/scripts/ui). It's metadata for the installed skill corpus; it references cli-help files that live outside this repo (captured from the CLI) plus shipped files. Since it hashes shipped/prompt-studio files, ANY edit to those (which the brief orders) would go stale anyway, and there's no checker here. The brief doesn't list it. I'll remove the two `cli-help/views*` entries to keep it consistent with the deleted subcommand — actually wait, minimal-change principle: the brief says delete cli-help fixture \"if present\" (not present in repo) and update named skill docs. The manifest already goes stale with any shipped edit and nothing validates it. Editing it partially (removing views entries but not updating hashes of edited org files) is inconsistent either way. Decision: leave corpus-manifest.json alone (not in scope, no checker, partial edits make it neither more nor less consistent). Name it in residual risk.\n\nHmm, wait — actually, let me reconsider. corpus-manifest hashes `shipped/prompt-studio/...` files. Does some regeneration script live elsewhere (the orgasmic runtime repo = THIS repo?). scripts/ has no corpus script. OK leave it; mention in report.\n\nNow — check the ui.rs test and entry.rs test etc. for ORG_FILES or views references: none found. Good.\n\nNext, look at prompt_compiler list_context_packs test at integration.rs:2783 — if I delete context-pack files (sprint_tasks, decisions, glossary), does that integration test break? It probably lists packs from the daemon's home source... in tests the daemon home is a temp home whose source() points where? Let me check that test. Also `shipped/prompt-studio/context-packs/decisions.org` — wait, the brief says delete {sprint_tasks,decisions,glossary}.org. But do any PROMPT-SPECS include packs via `:CONTEXT_PACKS:` properties? My earlier grep of prompt-specs for context_packs found nothing. Let me check how ContextPackView ids work and check integration test 2783.The integration test task_007 just checks the route returns OK — deleting three pack files won't break it (the daemon's home in tests symlinks repo source — `symlink_repo_source(&home)` — so it lists the REAL shipped context-packs; still fine with fewer files).\n\nNo existing get_org_file unit tests. I'll add one.\n\nNow let me check `read_claims` signature and `collection_node_file_paths` in core lib (exports), plus `Home::project_ledger` (used by doctor? no). And check the CLI main.rs `Cmd` enum area and where `project` subcommand doctor is wired (cmd_doctor). Let me read main.rs doctor invocation + see doctor module usage: grep \"doctor::\" in main.rs.All the pieces are mapped. Plan of edits:\n\n**crates/orgasmic-core/src/views.rs**\n- Delete `build_views`, `write_if_changed`, `TMP_COUNTER`.\n- Add `pub fn render_view(project_root: &Path, file: &str) -> Result<String>`.\n- Keep `VIEWS`, `render_collection`.\n- Tests: rewrite `multi_machine_views_are_ingest_order_independent_and_show_double_claims` to use render_view; delete `concurrent_builds_publish_well_formed_views`; add a test for unknown name error + no disk writes.\n\n**crates/orgasmic-core/src/lib.rs**\n- Replace `pub use views::build_views;` with `pub use views::render_view;`.\n\n**crates/orgasmic-core/src/projects.rs**\n- Test fixture: `std::fs::write(dst.join(\".gitignore\"), \"tmp/\\nviews/\\n\")` → \"tmp/\\n\" (line 399).\n- Assertion line 477 `assert!(gitignore.lines().any(|line| line == \"views/\"));` → assert NOT present: `assert!(!gitignore.lines().any(|line| line == \"views/\"));` — actually with fixture \"tmp/\\n\" just delete the assertion; but shipped template also changes. Better: assert gitignore has tmp/ and NOT views/.\n- Add scaffold no-views assertion? The scaffold never created views. Maybe assert `!proj.join(\".orgasmic/views\").exists()` after init. Good idea.\n\n**shipped/project-scaffold/.gitignore**\n- \"tmp/\\nviews/\\n\" → \"tmp/\\n\".\n\n**crates/orgasmic-daemon/src/index.rs**\n- Remove fields view_dirty_roots/view_drain_scheduled (403-404), VIEW_REBUILD_DEBOUNCE (436), init lines (836-837), schedule_view_rebuild (849-886), claims.org build_views block (971-974 — keep the reload_tx_file + return), the VIEWS block (1190-1195), board-entry build_views (3029-3035).\n- Check `warn!` macro still used. Check `Ordering` import still used elsewhere (repo_url_refresh_enabled swap?). grep after edit.\n- Tests: replace refresh_rebuilds_byte_stable_derived_views + incremental_node_write_rebuilds_views_without_claim_churn with: `refresh_leaves_no_views_directory` (rebuild + refresh + node write via apply_written_path → .orgasmic/views never created). Note apply_written_path for a node write → reload_node_dir → the VIEWS block deleted. Also need `write` helper & seed_project — existing helpers.\n\n**crates/orgasmic-daemon/src/api.rs get_org_file**\n- After computing `rel`, before reading disk: if rel is `.orgasmic/views/<file>.org` with file in {board,decisions,glossary}.org → contents = orgasmic_core::views::render_view(&project.root, file). Return response with rel_str. Unknown file under views (e.g. .orgasmic/views/other.org) → fall through to disk read (which will 404 if missing). Hmm — or not_found? render_view errors with \"unknown view\". For simplicity: match exact three names; else read disk (404 via read_artifact). Implementation:\n\n```rust\nlet contents = if let Some(view) = rendered_view_name(&rel) {\n    orgasmic_core::views::render_view(&project.root, view)\n        .map_err(|error| ApiError::internal(&format!(\"render {rel_str}: {error:#}\")))?\n} else {\n    read_artifact(&path, label)?\n};\n```\n\nNeed an ApiError constructor — check what's available (ApiError::not_found, bad_request...). Look for `internal` or similar. render happens AFTER project selected (needs project.root). \n\nfn to detect: \n```rust\nfn rendered_org_view(rel: &Path) -> Option<&'static str> {\n    let mut components = rel.components();\n    if components.next()? != Component::Normal(\".orgasmic\".as_ref()) { return None; }\n    if components.next()? != Component::Normal(\"views\".as_ref()) { return None; }\n    let name = components.next()?.as_os_str().to_str()?;\n    components.next().is_none().then_some(name).filter(|name| VIEWS-file check)\n}\n```\nActually render_view itself validates name via VIEWS lookup. So `rendered_org_view` can just return the file name; render_view errors for unknown names — but then a bogus `.orgasmic/views/nope.org` GET would 500 instead of 404. Better to validate against the three names here and fall through to disk otherwise (which 404s). Use orgasmic_core::views::VIEWS for the check — VIEWS is pub. \n\n**crates/orgasmic-cli/src/main.rs**\n- Delete Cmd::Views variant, ViewsCmd enum, match arm, cmd_views_build.\n- Check the \"command hint\" machinery at 1370 mentions doctor distance... fine.\n\n**crates/orgasmic-cli/src/project_migrate.rs**\n- Add views migration (plan + apply), refuse_dirty_tree filtering .orgasmic/views paths, summary output, is-noop integration, tests:\n  a. plain-branch fixture: tracked views/board.org → migrate untracks + deletes dir; second run no-op; doctor quiet.\n  b. ledger-without-remote fixture: same.\n- Where do doctor tests live? crates/orgasmic-cli/tests/doctor.rs (integration) + doctor.rs unit tests. The acceptance wants doctor warns → migrate → no-op → doctor quiet. I'll write CLI integration test in tests/doctor.rs? That test file — let me check its shape. Actually the gate says `cargo test -p orgasmic-cli --bin orgasmic -- doctor migrate views` (bin tests, filtered). So tests must be IN the bin (unit tests in src). doctor.rs unit mod and project_migrate.rs unit mod are both in the bin. Good — put the fixture test in project_migrate.rs tests (using run_at + doctor's new fn) or doctor.rs tests. I'll put the end-to-end fixture test in doctor.rs? It needs run_at (pub(crate) in project_migrate). Both are modules of the bin crate — accessible via crate::project_migrate. Let me structure:\n  - project_migrate.rs: `fn migrate_views(...)` internal + tests for untrack/delete/idempotent.\n  - doctor.rs: `fn push_project_views_findings(out, home)` (pub(crate) for test) + test fixture covering doctor-warn → migrate (crate::project_migrate::run_at) → doctor quiet.\n\n- Ledger-without-remote fixture: a project registered whose root IS a ledger (home.project_ledger path) with .orgasmic/views tracked, no git remote. run_at(home, ledger_root, false, false)? or to_branch=true? `orgasmic project migrate` CLI default: Migrate { dry_run, to_branch } — check defaults: `#[arg(long)] to_branch` → default false. So plain `orgasmic project migrate` = dry_run=false, to_branch=false. For the ledger root with old_files empty: current flow `else if migration.old_files.is_empty() → \"already migrated\" early return` — my views work must run before that return. With to_branch=false the is_ledger_root checks don't gate. Wait the first early return requires to_branch. Second early return: `else if migration.old_files.is_empty() { println!(\"already migrated\"); return Ok(()); }` — happens when !(to_branch && !dry_run && !is_ledger_root) i.e. for to_branch=false. So gate it: `else if migration.old_files.is_empty() && views clean`. \n\nLet me now write the new run_at:\n\n```rust\nfn run_at(home: &Home, root: &Path, dry_run: bool, to_branch: bool) -> Result<()> {\n    let migration = plan(root)?;\n    let views = ViewsMigration::plan(root)?;\n    if to_branch\n        && migration.old_files.is_empty()\n        && views.is_clean()\n        && is_ledger_root(home, root, &migration.project_source)?\n    {\n        println!(\"already migrated\");\n        return Ok(());\n    }\n    refuse_dirty_tree(root)?;\n    let mut views_applied = false;\n    if !dry_run {\n        views_applied = views.apply(root)?;\n    }\n    if to_branch && !dry_run && !is_ledger_root(home, root, &migration.project_source)? {\n        migrate_to_branch(home, root, &migration)?;\n    } else if migration.old_files.is_empty() {\n        if !views_applied && views.is_clean() {\n            println!(\"already migrated\");\n            return Ok(());\n        }\n    } else if !dry_run {\n        apply_with_recovery(root, &migration)?;\n    }\n    println!(\"{}\", if dry_run { \"DRY RUN\" } else { \"MIGRATED\" });\n    ... headings/nodes/bytes ...\n    if !views.is_clean() {\n        println!(\n            \"  views {} tracked file(s){}\",\n            views.tracked.len(),\n            if views.dir_present { \" + directory on disk\" } else { \"\" }\n        );\n    } else if views_applied {\n        println!(\"  views untracked and directory removed\");\n    }\n    ...\n}\n```\n\nHmm wait, dry_run + views pending: currently dry-run flow for old_files: plan → not early-return (old_files nonempty) → refuse_dirty_tree → skip apply (dry) → prints \"DRY RUN\" + stats. For views dry-run: views.apply skipped, print pending summary. Good.\n\nWait, there's an issue with `else if migration.old_files.is_empty()` branch when dry_run && views pending && to_branch=false: prints DRY RUN + views pending. OK.\n\nAnd when views_applied && old_files.is_empty() (the plain fixture): prints MIGRATED + \"  views untracked...\". Second run: views clean, old_files empty → \"already migrated\". \n\nViewsMigration:\n\n```rust\n/// dec_AF61D / dec_XH2XY: `.orgasmic/views/` is never tracked or kept on\n/// disk. The migration untracks it from the repo index and removes the\n/// directory; both steps are no-ops once applied.\nstruct ViewsMigration {\n    tracked: Vec<String>,\n    dir_present: bool,\n}\n\nimpl ViewsMigration {\n    fn plan(root: &Path) -> Result<Self> {\n        let tracked = if git_ok(root, &[\"rev-parse\", \"--is-inside-work-tree\"]) {\n            git_capture(root, &[\"ls-files\", \"--\", \".orgasmic/views\"])\n                .unwrap_or_default()\n                .lines()\n                .map(str::to_string)\n                .collect()\n        } else { Vec::new() };\n        Ok(Self { tracked, dir_present: root.join(\".orgasmic/views\").is_dir() })\n    }\n    fn is_clean(&self) -> bool { self.tracked.is_empty() && !self.dir_present }\n    fn pending(&self) -> bool { !self.is_clean() }\n    fn apply(&self, root: &Path) -> Result<bool> {\n        if self.is_clean() { return Ok(false); }\n        if !self.tracked.is_empty() {\n            git(root, &[\"rm\", \"-r\", \"-q\", \"--cached\", \"--ignore-unmatch\", \"--\", \".orgasmic/views\"])?;\n        }\n        match std::fs::remove_dir_all(root.join(\".orgasmic/views\")) {\n            Ok(()) => {}\n            Err(error) if error.kind() == std::io::ErrorKind::NotFound => {}\n            Err(error) => return Err(error).context(\"remove .orgasmic/views\"),\n        }\n        Ok(true)\n    }\n}\n```\n\nNote: brief says `git rm -r --cached --quiet -- .orgasmic/views`. ledger_sync uses `-q` + `--ignore-unmatch`. I'll match ledger_sync's exact form (proven) — `-q` == `--quiet`. Keep same flags.\n\nEdge: dir present but not a dir (file)? is_dir() false → skip; fine.\n\nrefuse_dirty_tree filter: parse porcelain -z and skip records whose (post-rename) path is under .orgasmic/views/. Implementation:\n\n```rust\nfn refuse_dirty_tree(root: &Path) -> Result<()> {\n    ... output ...\n    if !output.status.success() { bail }\n    let mut dirty = false;\n    let mut records = output.stdout.split(|b| *b == 0);\n    while let Some(record) = records.next() {\n        if record.is_empty() { continue; }\n        // \"XY <path>\" (renames: \"XY <new>\\0<old>\")\n        let bytes ="
    }
  ],
  "narrative_truncated": true
}
