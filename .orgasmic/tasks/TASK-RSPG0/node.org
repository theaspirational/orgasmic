#+title: orgasmic task TASK-RSPG0
#+orgasmic_version: 2

* BACKLOG TASK-RSPG0 Worktree provisioning ignores submodules: hydrate scoped submodules with --reference, avoid per-worker LFS fetch, route or refuse submodule-scoped tasks    :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-RSPG0
:PRIORITY: P2
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 10. Fresh worktrees have un-hydrated submodules → 62 spurious test failures; five parallel workers serialised behind one git-lfs filter-process on an unneeded LFS docs submodule (one died in setup); a submodule inside a worktree is read-only/uninitialised so a worker commit there dies with the worktree — forced the operator to split the server into its own project (dec_00M3W).
** Expected
- `project init` records the submodule graph; dispatch hydrates only the submodules a task's write_scope touches, once per worktree, via `git submodule update --reference <main checkout>`; never LFS over the network per worker.
- A task whose write_scope is inside a submodule is routed to that submodule's project or refused with the reason.

