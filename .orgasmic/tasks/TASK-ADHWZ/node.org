#+title: orgasmic task TASK-ADHWZ
#+orgasmic_version: 2

* DONE TASK-ADHWZ Verify remaining concurrent-write acceptance after node-directory and writer fixes
:PROPERTIES:
:ID: TASK-ADHWZ
:PRIORITY: P1
:WRITE_SCOPE: crates/orgasmic-core/**, crates/orgasmic-daemon/**, crates/orgasmic-cli/**
:END:

** Description

Three distinct =orgasmic task create= calls were issued concurrently against
the same project and =backlog.org= on 2026-07-29. All three returned success:

- TASK-95SGV.1 → =tx-20260729-orgasmic-3946=
- TASK-JQ8AV.1 → =tx-20260729-orgasmic-3948=
- TASK-FZB6T.1 → =tx-20260729-orgasmic-3947=

Immediately afterwards, only TASK-JQ8AV.1 existed in =backlog.org= and the
daemon returned 404 for updates to the other two. The missing headings had to
be recreated sequentially with new idempotency keys. This is silent successful
write loss at the daemon's structured state authority boundary.

** Acceptance

- Reproduce with deterministic concurrent creates of distinct task ids that
  target the same state file.
- Every successful response has exactly one durable heading and matching tx
  after the requests finish and after reindex/restart; no last-writer-wins loss.
- Serialize or compare-and-swap the entire read/transform/write transaction at
  the central writer boundary, not at individual CLI callers.
- Cover concurrent create/create, create/update and update/update operations
  against the same Org file, including response loss and idempotent retry.
- A stale transform must retry from the new file version or fail explicitly;
  it must never report success for content absent from the committed file.
- Preserve the existing sanctioned git/human concurrent-writer rules and tx
  inode-swap protection.
- Add an injection proof and run focused writer/API tests, fmt, and clippy with
  warnings denied. Do not run any billed harness test.

** Incident Evidence

The manager observed the three successful CLI responses directly, then verified
the raw task files with =rg=. Sequential restore txs are
=tx-20260729-orgasmic-3950= and =tx-20260729-orgasmic-3952=.
** Status 2026-09-06
Source/ledger triage at main 0bf80eb0. Original sibling-heading loss targets aggregate backlog.org, which is retired. Current tasks have separate node directories, writer FileMutate supports central read/transform/write, and api.rs has concurrent_task_creates_preserve_every_heading (16 creates, durable journals, idempotent replay) plus concurrent_task_property_updates_preserve_every_write (16 updates/replay). The named mechanism and regressions already exist; do not reimplement them. Leave open pending a focused acceptance audit of create/update interleavings and restart durability; these Rust tests were inspected, not executed in this status pass.
** Evidence
Source-complete acceptance audit, 2026-09-07. Local commits: 300358b1f2874559f768137ac13146c8636320ea (fresh lifecycle transforms and concurrency/restart proof), 4740d3b20c8ea72337e09ba1fb0e3dbc50406f2f (single-transaction sync-uncertain contract), d3ae78638a795cd74317b1d4c6675186c3132ea6 (pending-create recovery before caller guards), f7481eb9810568bdf6695ea172e0314d63c9c1a2 (durable replay recovery).

The original incident used retired aggregate backlog.org. Current tasks have separate node directories. The remaining shared-node loss was reproduced deterministically: priority P1 queued before a lifecycle update disappeared although both succeeded (mixed-red.log). Lifecycle changes now transform fresh bytes inside the existing serialized FileMutate transaction, check the authorized starting state, and recheck required Evidence using the existing body parser. Stale state or removed evidence returns 409 without appending a transaction. Concurrent create/create, create/update and update/update tests preserve every successful node/property and exact journal acknowledgements; create/update response bodies are discarded and retried. Disposable-daemon restart checks retain byte-identical nodes/journals and successful GETs.

A cross-restart create probe originally minted another ID (restart-retry-probe.log). Single transactions now include versioned request replay metadata in the same ledger append: semantic operation/project, SHA-256 payload fingerprint, original mutation ID, and paths relative to registered roots. Recovery scans registered home/project history before accepting semantic transactions, including unloaded projects and projects registered later. It reads home history through Home::tx(), checks copies before EVENT_ID projection deduplication, validates the complete batch before caching, and resyncs the same unchanged file bytes/descriptors it relies on. Malformed, conflicting, unsupported, escaping, or unreadable evidence fails closed; repairing the evidence allows retry without another restart. Callers cannot supply the reserved replay property through tx/transaction writes.

The rejected initial replay draft is preserved as rejected-journal-replay-draft.patch. Its failure injection showed a journal claiming a node mutation that had been rolled back after sync failure (replay-sync-failure-probe.log). The prerequisite fix preserves pre-append rollback, retains nodes after a completed append with uncertain sync, and reports typed 503 with committed=true/durability=uncertain. Identical retry resyncs without transforming or appending again; payload/API collisions fail. Original paths and transaction event metadata are retained, so auto-minted IDs project correctly and successful recovery emits the original event once. A replacement journal inode cannot be synced as proof of an unrelated earlier transaction.

Final verified source: 27 writer library tests, 25 focused API/index tests, and 12 durability integration tests (64 total). Coverage includes the deterministic stale-transform cases, required-evidence guard, same-boot response-loss retries, exact bytes across daemon restart, original create IDs/txs after restart, cross-project and cross-operation collisions, changed-payload conflicts, late project registration, home-ledger conflicting copies, repair after five kinds of invalid history, and reserved metadata rejection. Real HTTP tests restart disposable daemons after a sync-uncertain task create and after decision/glossary creates; replays return original IDs/transactions with no new node or append. Existing retained-inode, other-writer, rollback, projection repair, and event tests pass. cargo fmt --check, git diff --check, and cargo clippy -p orgasmic-daemon --lib --tests -- -D warnings pass.

Artifacts: /Users/aspirational/.codex/artifacts/orgasmic-adhwz-20260907. Final logs: durable-writer.log, durable-api.log, durable-durability-verified.log, durable-clippy.log, durable-fmt.log, durable-diff-check.log. Earlier red/negative-control logs and slice outcomes remain there. The older integration assertion expecting obsolete collision wording was verified to fail on unchanged source (single-sync-baseline-collision.log); it now checks the existing typed RequestIdReuseConflict.

Lifecycle is source completion, not runtime adoption. Replay persistence applies to requests recorded by the updated code; older entries have no request metadata to reconstruct. This adds daemon-local restart recovery, not distributed exactly-once coordination. No full workspace suite, billed/provider call, push, install, release, or operator restart occurred. The operator daemon stayed PID 39114 / boot 019f72b5-9e79-4536-905c-a95dff391ac7. P0 restart remains explicit.

