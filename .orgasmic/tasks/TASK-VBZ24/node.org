#+title: orgasmic task TASK-VBZ24
#+orgasmic_version: 2

* DONE TASK-VBZ24 Runtime-mandatory entry, workflows v1, config.org removal :entry:workflows:scaffold:
:PROPERTIES:
:DEPENDS_ON: dec_YTJ0G dec_N7BM4 dec_MCM6Y dec_T949W dec_S0CPP
:ID: TASK-VBZ24
:PRIORITY: P1
:END:

** Description
Umbrella for the 2026-07-09 entry/workflows redesign (dec_YTJ0G dec_N7BM4 dec_MCM6Y dec_T949W dec_S0CPP). Kills the frozen-scaffold-surface problem: root pointers route to a stub that defers to a runtime-generated =orgasmic entry= router; generic conventions and the manager-implementer discipline move behind the runtime (shipped =workflows/default.org=); =config.org= and =:PIPELINE:= are deleted; dispatch always names its worker. Subtasks carry the implementation slices; this heading tracks overall completion.

** Acceptance Criteria
- [X] All six implementation slices are done: TASK-74TA6, TASK-CZTWD, TASK-3YMVC, TASK-A8559, TASK-PCDA2, and TASK-KWD80.
- [X] A fresh source-built `orgasmic project init` emits the slim scaffold with stub `entry.org`, no `conventions/`, and no `config.org`.
- [X] This repository is migrated to the runtime-served entry and workflow shape.
** Evidence
- [2026-07-11 Sat] Confirmed all six redesign slices are in `done`; landed implementation chain: `8fe0c98`, `0b20f6e`/`11086bd`, `ef3bd8c`/`ee6bd6b`, `2c38471`/`b6ca44b`, `4b5fb6a`/`3823a91`, and repo migration `c47a855`.
- Real fresh-init probe using the source-built 0.0.12 CLI scaffolded 14 files and asserted stub `entry.org` present, `config.org` absent, and `conventions/` absent.
- Current gates: entry integration tests 3/3; greenfield bootstrap smoke 1/1; shipped scaffold parser 1/1; workspace clippy passed.
- Current repository route is live: root pointers require `orgasmic entry`, `.orgasmic/entry.org` is the stub, `config.org` and generic conventions are absent, and the runtime router resolves `shipped/workflows/default.org`.

