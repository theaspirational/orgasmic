#+title: orgasmic task TASK-DHYXQ
#+orgasmic_version: 2

* DONE TASK-DHYXQ Greenfield bootstrap e2e smoke: project init through first artifact, CLI-only :tests:hardening:
:PROPERTIES:
:DEPENDS_ON: TASK-JJ9RD TASK-GERBB
:ID: TASK-DHYXQ
:PRIORITY: high
:END:

** Description
The regression net for the whole battle-test class: no end-to-end bootstrap smoke exists today — the closest are the crates/orgasmic-cli/tests/init.rs registration tests and the live dispatch tests. The 2026-07-06 orsl battle-test hit F1/F2/F7/F8/F13 precisely on the greenfield path no unit test exercises: an agent discovering contracts from --help, error strings, and shipped files.

Add one integration test that does what the battle-tester did, CLI-only, against a running isolated test daemon (existing harness: crates/orgasmic-cli/tests/common/mod.rs seeding + the cross-process live_session_guard flock; port-isolation gotcha applies):

=project init= in a temp git repo → immediately drive the shipped bootstrap backlog's own instructions: create decisions (minted ids, per the corrected scaffold text), glossary terms with =--relates-to= refs, architecture nodes with typed edges + MOTIVATED_BY, fill config.org verification commands via CLI, create+update a task, mint + submit a minimal artifact.

Assert at the end: =parse_errors == 0=; every scaffolded .orgasmic file was writable through the CLI (zero hand-edits); no silent-empty listing.

This turns the scaffold prose into a tested contract — scaffold text and daemon policy can no longer drift apart (the F2 class), and every future regression in the bootstrap path fails a named test instead of a downstream user session.

** Acceptance Criteria
- [ ] Integration test boots an isolated daemon, inits a fresh project, drives the full bootstrap flow through the CLI only.
- [ ] Asserts parse_errors == 0 and a lawful CLI write path for every scaffolded file.
- [ ] Red on reintroduction of: post-boot registration 404 (F1), config write-path gap (F7), scaffold/policy id contradiction (F2).
- [ ] Serialized under the shared live-session flock; workspace-concurrency safe.

