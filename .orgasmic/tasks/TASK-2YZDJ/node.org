#+title: orgasmic task TASK-2YZDJ
#+orgasmic_version: 2

* DONE TASK-2YZDJ CLI readiness: boot-phase heartbeat instead of the flat 20s spawn timeout :cli:daemon:hardening:
:PROPERTIES:
:ID: TASK-2YZDJ
:PRIORITY: medium
:RELATES_TO: TASK-35TK4
:END:

Evidence (2026-07-23): daemon_lifecycle.rs:513 gives up with "daemon process exited before becoming ready after 20s" while a daemon can legitimately spend minutes in pre-bind boot on a large board — the operator-facing effect was repeated spawn attempts, racing instances, and a misleading error (the process had not exited; it was booting). Fix: the daemon writes a phase heartbeat next to the lockfile (phase, started_at, pid, refreshed periodically — e.g. "scanning projects"); the CLI autostart/status path reads it and distinguishes "booting since <ts>, phase <p>, be patient or inspect" from "dead, check logs". The 20s budget then applies only to a pid with no heartbeat progress. Depends on the daemon.lock introduced by TASK-35TK4. Regressions: slow-boot fixture where the CLI reports phase instead of timing out; stale-heartbeat (killed -9) detection.
** Notes
Blocked before implementation by the requested Cursor ACP transport. Attempt 1 run-20260723T160534-79aa3fe66ca3469eaa17adec7ca07c68 used generic grok-4.5/high: Cursor session/new succeeded (simulated false), then prompt failed with Invalid params. The single retry run-20260723T160811-3cfa31b5bddb44328dd28862e3f0e88b used the proven harness-native cursor-grok-4.5-high-fast/high identifier: session/new again succeeded, then prompt failed identically. Both isolated worktrees are clean at e4b6b7b and produced no work envelopes or edits. No third retry under manager policy. Resume after the Cursor ACP session/prompt parameter contract is repaired.
Transport blocker repaired and independently reviewed: c1dccd1 + 4a59743 landed as 559add1. Source runtime is active under an interactive foreground daemon (boot 35358fe3-22bb-4292-96b8-ccf904480f4f) because launchd still hangs in pre-bind project scanning. Resuming as a fresh post-repair Cursor canary/implementation run.
Post-repair run run-20260723T165635-a8f0a0d8196d4dd48ff137411fa7ce2e proved the real Cursor ACP canary: session/new resolved cursor-grok-4.5-high-fast/high to native grok-4.5[effort=high,fast=true] and produced real tool calls. TASK-2YZDJ itself remains unimplemented: Cursor ended its work turn after an invalid task:__subagent_task request and later could not run the required project workflow because the approval policy rejected its shell command. Clean worktree at 559add1; route the permission-policy defect separately, then resume with a brief that forbids Cursor subagent tools.

