#+title: orgasmic task TASK-EXN3N
#+orgasmic_version: 2

* IN_PROGRESS TASK-EXN3N Read-only reviewer is a request, not a guarantee: pin cwd to the worktree and report main_checkout_dirty in dispatch-status :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-EXN3N
:PRIORITY: P2
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 13. A reviewer mutated the main checkout (ran an in-place script after cd'ing there to read implementer logs). Found only by `git status` on main; every reviewer brief now carries a STAY OUT paragraph.
** Expected
- Reviewer run pinned to its worktree (cwd fixed; main checkout not writable from the worker process — sandbox profile), or at minimum a post-run `git status` diff on main reported in dispatch-status as `main_checkout_dirty: <files>`.
Related: orphaned-acp-reverts-tx memory (same class: workers touching main).
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit 80ea02aa on sprint/DIRTY, merged onto local sprint-20260902 as 3ec20e11. Verified from code: the reviewer ALREADY runs in its <slug>-review worktree (worktree_stem(task, Reviewer); daemon stage_driver_config sets cwd=worktree for every kind) — pinned by daemon test reviewer_dispatch_cwd_is_its_review_worktree_not_the_project_root and a CLI stem test. dispatch-status now prints 'main_checkout_dirty=N CHECKOUT=<path> PATHS=<≤3>' as the first line while any dispatch is open (asserted 0 -> 1 in the stub-codex integration test). No sandboxing of main for the reviewer process (the 'at minimum' branch shipped). Gates: clippy rc=0, fmt rc=0, tests as on GCTMA.
Correction: the merge commit on sprint-20260902 is daa3486b (not 3ec20e11); manager.rs dispatch_inner conflict with XC9N4 resolved by keeping both the unvalidated-model warning and guard_dirty_main_checkout.

