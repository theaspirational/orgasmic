#+title: orgasmic task TASK-2QK4P.1.1.1
#+orgasmic_version: 2

* DONE TASK-2QK4P.1.1.1 every bool on the recovery-authority path that conflates 'verified false' with 'could not check'
:PROPERTIES:
:ID: TASK-2QK4P.1.1.1
:PRIORITY: P0
:IMPLEMENTS: TASK-2QK4P.1.1
:FIX_SUBTASK: TASK-2QK4P.1.1.1.1
:END:

** READ THIS FIRST — STATUS
FIX ROUND for TASK-2QK4P.1.1, opened by a =REJECT= with =BLOCK SHIP=
(=tx-20260808-orgasmic-5446=, codex/gpt-5.6-sol cross-family).

*ROUND FOUR ON ONE DEFECT, AND THE PREVIOUS ROUND'S FIX WAS APPLIED TO THE TWO
PLACES THE FINDINGS NAMED RATHER THAN TO THE CLASS.*

: round 1  an EMPTY catalog index            read as proof of non-contradiction
: round 2  a ONE-ELEMENT catalog index       read as proof of uniqueness
: round 3  a FAILED per-file scan            read as an empty-but-complete file
: round 4  a FAILED AUTHORITY VERIFICATION   read as "not authentic" / "invalid"

Round 3 introduced =IndexedRecoveryOrigins::{Complete,Unobserved}= and
=AuthoritativeOriginLinks= so that "I did not observe" could not be spelled "I
observed nothing". *It did that, and then handed those types a =bool= computed by
a predicate that already had the same defect.*

*THE GENERATING SHAPE, STATED ONCE: A =bool= THAT MEANS "I VERIFIED AND IT IS
FALSE" AND ALSO MEANS "I COULD NOT CHECK".* Every round has been one instance of
it. The round-3 acceptance said "make the collapse unrepresentable" and the work
made it unrepresentable *in the two types it changed*. This round's acceptance is
a COMPLETENESS ARGUMENT over the whole authority path, not another type change at
one point.

** Findings to close
*** F1 (HIGH, BLOCK SHIP) — the completeness types stop before authority verification
=crates/orgasmic-daemon/src/recovery_claim.rs:304-371=, =:1297-1301=, =:1542=,
=:1635-1642=, =:1644=, =:1699=, =:2157-2179=, =:2262-2269=;
=crates/orgasmic-daemon/src/api.rs:9264-9364=

*Two collapses, and they fail in OPPOSITE directions, which is why one type
change could not catch both.*

*(a) An observation failure becomes ABSENCE.* =authority_key= is fallible, but
=claim_has_valid_authority= collapses every =authority_tag= error to =false=. At
=:1297-1301= =index_recovery_origins_in_session= uses that boolean to =continue=
— *dropping the link* — and the enumerator then returns
=AuthoritativeOriginLinks::Complete= at =:1542=. So a transient key-file read
failure while indexing one of TWO authenticated links removes that link from a
set *labelled complete*. If the surviving link matches the loaded claim,
=:1635-1642= returns =Valid=. **The exact state rounds 1–3 exist to prevent, now
reached through the authenticator instead of the index.**

*(b) An observation failure becomes INVALID EVIDENCE.*
=verify_committed_claim_against_session= returns a bare =bool= and maps
replacement/origin session open, read and parse failures to =false= at
=:2157-2179= and =:2262-2269=. The resolver reads that as *invalid*, quarantines
at =:1644=, and can reach =Missing= at =:1699= — after which
=POST /runs/:id/recover= plans a NEW replacement at =api.rs:9264-9364=. A
transient read failure therefore destroys a valid live rescue AND mints a
competitor. That is the "unresolved must stay distinct from invalid" ruling from
round 3 violated one call deeper than round 3 looked.

*THE STRUCTURAL PIN IS BLIND TO BOTH*, and the reviewer says why: it inspects
only the two new enum declarations. Worse, it *prefix-matches* — it checks that
the =Unobserved { reason: ...= prefix exists (=:4634-4643=), so *adding a =links=
field after the checked prefix keeps the pin green.* No regression injects an
authority-key failure, or a read failure between the cached complete enumeration
and claim verification.

*NOT REPRODUCED.* The reviewer derived this from the exact production
error-collapse path and said plainly it did not build a running reproduction.
Build it first (see the brief).

*** F2 (MEDIUM) — one recover POST performs TWO complete enumerations; acceptance 5 is unmet on the production endpoint
=api.rs:9028=, =:10087=, =:10092-10093=, =:9045=, =:9055-9059=

=post_run_recover= calls =recovery_status_inner= at =:9028=. That function
creates *its own* =ProjectOriginAuthority= at =:10087= and resolves every
failed-recoverable run at =:10092-10093=, enumerating the project. The handler
then *discards that authority*, creates another at =:9045=, and calls =links_for=
at =:9055-9059= — enumerating the same files again *in the same decision*.

=one_authority_serves_every_claim_in_a_project= (=:4487-4556=) only proves reuse
when callers ALREADY SHARE one authority object; it never exercises the
endpoint's inventory-then-recover composition. Against the round-3 measurement of
1.15 GiB / 340 ms per pass, a POST pays roughly *two full passes* before action
execution, and the second is not represented in the inventory metrics at all.

*** F3 (MEDIUM) — the up-front 503 preempts request validation that needs no authority
=api.rs:9054-9070= runs before explicit action validation at =:9163-9171=, the
"multiple actions; specify action" 409 at =:9209-9215=, and the failed-record
=reattach_tmux= 400 at =:9233-9236=. So an *invalid action* against an otherwise
valid failed record returns *503 whenever any unrelated session file is
unreadable*, and silently becomes the actionable 400/409 once that file is
repaired. The round-3 symlinked-origin ordering fix covered
=origin_authority.envelopes()= only. *The safety refusal must precede minting or
accepting authority — not input validation.*

*** F4 (MEDIUM) — the torn-final inference is true of today's writer but is not enforced at the scanner boundary
=crates/orgasmic-core/src/session.rs:1477-1510=, regression at =:2420-2425=;
writer at =:257-270= and =crates/orgasmic-daemon/src/writer.rs:1593-1626=

*The reviewer CONFIRMED the invariant holds today* — every production append
routes through =append_session_inner= to =SessionWriter::append=, which
serializes one valid envelope, writes the line, writes =\n=, flushes and
=sync_all=s. That answers round 3's C2 in the affirmative.

*But the scanner does not check the thing the argument rests on.*
=scan_session_lifecycle_complete_reader= defers any parse error at EOF *without
checking whether the malformed line was newline-terminated*, and its regression
supplies a *newline-terminated* malformed final line — **a shape the current
writer cannot produce as a torn append**, because the newline is written only
after the serialized JSON write succeeds. So the test does not fail if a future
writer starts persisting complete malformed lines. The liveness exception should
survive only for a *non-terminated* EOF fragment.

** Acceptance
1. *THE COMPLETENESS ARGUMENT, AND IT COMES FIRST.* Enumerate EVERY predicate on
   the recovery-authority path that returns =bool= or =Option= and, for each,
   state in the report what it does when it *cannot observe*. Any that conflates
   "verified false" with "could not check" becomes a typed three-way result:
   valid / invalid evidence / unobserved. =claim_has_valid_authority= and
   =verify_committed_claim_against_session= are the two the review found; *the
   deliverable is the enumeration, not those two.*
2. Unobserved propagates through =IndexedRecoveryOrigins=,
   =AuthoritativeOriginLinks=, =ResolvedRecoveryClaim= and out to the API's
   retryable 503 — *without quarantining.*
3. *Fix the pin the review defeated.* It must not prefix-match, and it must cover
   more than two declarations. If a source-text pin cannot express the property,
   say so and pin the behaviour instead — a pin that passes on the defect is
   worse than no pin, because it reads as coverage.
4. F2: share the authority built during POST's inventory classification with the
   later claim/mint decision. Add an *endpoint-level scan-count test*; the
   existing one cannot see this because it hands both callers the same object.
   Report the full cost of every pass.
5. F3: move authority-independent action validation ahead of the 503 while
   preserving committed-claim idempotency. Keep the completeness check
   immediately before any branch that can mint or accept authority.
6. F4: record whether EOF's malformed line ended with =\n=; tolerate only a
   non-terminated final fragment. Update the regression to the *production tear
   shape* and add a newline-terminated-corruption case that stays unresolved.
7. Running regressions for: an nth authority-key read failure; a
   replacement/origin read failure after a cached complete enumeration. Each must
   prove *no =Valid=, no quarantine, no new replacement* — then prove normal
   resolution after repair.
8. =recovery_faults_kill_real_daemon_and_preserve_original_pane= still fixed, by
   repetition with a stated pass count. =scripts/run-tests.sh -p orgasmic-daemon=
   GREEN with the VERDICT block quoted.

** Open questions the reviewer answered, so nobody re-litigates them
- *C3, the 340 ms full scan:* "the filesystem fallback is the correct safety
  source in the current tree; I would not replace it with a cache lacking a
  completeness generation merely to land this round." It is nevertheless *an
  unbounded polling-path regression* and wants a completeness-generation design
  before release. *F2's duplicate POST scan is NOT part of that redesign and
  should be fixed in this round.*
- *C6, =remove_recovery_claim=:* ruling it out of scope was reasonable, and it
  still has no caller. *But the reviewer corrected the manager's model:* claim
  files are NOT what selects the slow path — any failed-recoverable tombstone
  reaches =resolve_authoritative_recovery_claim=, which enumerates *before*
  loading the claim. So removing committed claim files would not remove the
  repeated scan. Cleanup is still worth a follow-up; it is not a performance fix.

** What the review did NOT run
=recovery_claim::= 27 passed, =orgasmic-core session::= 19 passed, and the exact
=api::tests::recover_refuses_to_mint_while_a_session_file_is_unreadable= passed —
all under a watchdog, peaks 1.1% / 0.1% / 6.5%. All three injection patches
=git apply --check= clean, and the TASK-2QK4P.1 re-authoring keeps both original
=Valid(= signatures with no dropped =git add=.

*Not run:* the full daemon wrapper suite, the recovery-fault repetition, clippy,
fmt, workspace tests, MSRV 1.87, non-Unix, the real-board latency measurement, and
all three =orgasmic verify= red/green replays — *those remain manager evidence.*
No running reproduction was built for F1.

Minor, recorded and explicitly not classified as a product finding:
=git diff --check 408b838^..0658e18 -- crates verify= reports one =+ = blank line
at line 48 of each copied TASK-2QK4P.1.1 injection patch.

