#+title: orgasmic task TASK-X0ZVE
#+orgasmic_version: 2

* DONE TASK-X0ZVE Live rmux tests flake under parallel load (render_stream sentinel timing) :tests:runtime:
:PROPERTIES:
:ID: TASK-X0ZVE
:END:

live_rmux_render_path_streams_screen_and_completes (modes/rmux.rs) failed once
during the full orgasmic-drivers suite ("expected the rendered screen to carry
the sentinel over render_stream", rmux.rs:2232), then passed 5/5 on re-run (3
isolated + 2 full-suite at 122/122). Not a code regression — RCWFF did not touch
the render path. The failure is real-rmux-daemon render_stream timing starved
under concurrent live tests; RCWFF added two more live rmux tests
(live_rmux_send_input_*) which raise concurrent daemon load and likely bump the
trigger rate. Same CLASS as TASK-SJQ9V (parallel-only flake).

Fix direction: serialize the live rmux/tmux tests (a shared serial guard / mutex,
or a #[serial] attribute) so real-daemon render/paste tests don't contend, or
raise the render sentinel wait deadline. Low priority; gate green single-threaded
and on re-run.
** Evidence
Merged d70af6f (worker e4bd43f). Cross-process advisory flock (fs2::FileExt::lock_exclusive on $TMPDIR/orgasmic-live-session-tests.lock) added as first stmt of all live tmux/rmux tests across 4 modules: orgasmic-drivers modes/tmux.rs (7 real_tmux_*), modes/rmux.rs (9 live_rmux_*), orgasmic-daemon api.rs (boot_reattach/recovery/post_artifact_regenerate x3/artifact_release_watcher/tmux_ws_member_denied) + supervisor.rs (tmux_terminal_event_releases). fs2 added to drivers dev-deps; .truncate(false) for clippy. Verify: cargo test -p orgasmic-drivers 125+5+25 pass; clippy --workspace --all-targets -D warnings clean; fmt clean. Worker ran cargo test --workspace 14x: X0ZVE flake class (real-tmux/rmux contention) GONE — zero failures across all 24 guarded tests every run. Residual workspace redness is 3 pre-existing out-of-scope orgasmic-cli flakes (dispatch_close_prunes_stem_dir_leaving_brief reproduces at baseline 88c0e94; doctor daemon_staleness_warns_for_git_commits_since_boot; content_lifecycle hub_install_list_remove) — tracked separately.

