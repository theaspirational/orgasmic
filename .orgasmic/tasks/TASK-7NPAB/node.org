#+title: orgasmic task TASK-7NPAB
#+orgasmic_version: 2

* BACKLOG TASK-7NPAB Task-level exclusivity for dispatch: --exclusive / resources tag so the daemon queues a quiet-host task behind live runs    :vscode-orsl-letter:
:PROPERTIES:
:ID: TASK-7NPAB
:PRIORITY: P3
:END:

** Description
Source: vscode-orsl manager letter 2026-08-22 (operator-forwarded, 40 dispatches over 4 days, orgasmic 0.0.18). Item 15. Two measurement tasks each carried a refuse-while-sibling-live rule; dispatched together, the second always lost. Nothing lets a task say it needs a quiet host.
** Expected
`--exclusive` on dispatch (or a `resources:` property on the task) so the daemon queues it behind live runs instead of the worker discovering the conflict and aborting.

