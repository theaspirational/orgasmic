#+title: orgasmic task TASK-AP298
#+orgasmic_version: 2

* IN_PROGRESS TASK-AP298 An admitted-but-unchecked preflight leaves no trace in the dispatch's own evidence (RunMeta/tx)
:PROPERTIES:
:ID: TASK-AP298
:PRIORITY: P2
:END:

From TASK-GEZHQ (2026-07-29): when the credential probe cannot answer (timeout/spawn failure), the dispatch is admitted per dec_7P79C and GEZHQ's WARNs reach the daemon log — but RunMeta and the tx record still say only that the plan was pinned, not that the probe never answered. A run that dies at startup should be able to say 'admitted without a credential check' from its own record, without the operator correlating daemon-log timestamps. Add the unchecked-admission fact to the dispatch's own evidence surface (RunMeta field or tx annotation), so post-mortems read it where they already look. Keep dec_7P79C intact: this is observability, not a new rejection path. Injection per TCTTD standard: natural red = a probe-timeout dispatch whose run record contains no trace of the unchecked admission.
** Evidence
Manager sprint 2026-09-02 (no dispatch): subagent commit 1bf94b83 on sprint/PREFLIGHT, merged onto local sprint-20260902 as 0a4f348e (3 both-sides-added conflicts with A55J7 resolved by keeping both). PreflightOutcome::label() = ok | unchecked:<timeout|spawn_failed|inconclusive> | refused:<reason>; read_status_output returns Result<_, Unasked> so timeout vs spawn failure stay distinct; spawn_worker_run writes 'preflight' onto the pinned driver config; supervisor lifts it into Lifecycle::RunMeta{preflight} and RunSummary.preflight (serde default); dispatch_started tx carries :PREFLIGHT:; dispatch-status appends PREFLIGHT=<v> only when not ok. New integration binary tests/dispatch_preflight_unchecked.rs drives spawn_failed and timeout through the real endpoint. Gates (private target): daemon lib 11, daemon integration 3/3, drivers 21, cli 9, clippy rc=0, fmt rc=0. Note: refused:<class> never lands on a surface (no run/tx is created). Operator review/test after the sprint.

