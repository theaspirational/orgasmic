## Changed

- Removed project/home refresh seeding clones for `tx` and `parse_errors`; project refreshes retain only the board entry, prior project projection for last-good behavior, and `rebuilt_at` before scanning.
- Kept in-flight Git probe deduplication while removing a failed non-forced probe key so a later registration/watch request retries. Added deterministic failure-then-success coverage; existing concurrent dedup and live-URL publication merge coverage remains green.
- Bounded one captured mutation batch at five consecutive stale projection discards. Its older covered waiters receive the existing refresh error/committed-503 mapping; later arrivals remain queued and the detached coordinator converges. Slow-scan diagnostics now include `stale_discards_for_batch`.
- Documented the 50 ms uncontended acknowledgement floor and the five-discard contract.
- Added a writer-level cached `Transaction` replay regression that proves a subsequent writer command succeeds.
- Added `verify/TASK-K9WWM/hardening-round.md` with measurements and reproducible gates.

## Verification Gates

- `cargo test -p orgasmic-daemon --lib index::tests:: -- --test-threads=4` — 53 passed, including last-good/parse-error, Git retry/dedup/live merge, stale bounds, ordinary coalescing, and watcher convergence.
- `cargo test -p orgasmic-daemon --lib index::tests::sustained_stale_generations_fail_older_batch_but_newer_waiters_converge -- --exact --nocapture` — passed; 6 requests, 6 scans, 5 coalesced, 5 stale discards, then zero pending targets. Older error arrived after the fifth discard; all five newer waiters survived and converged on scan 6.
- `cargo test -p orgasmic-daemon --lib writer::tests::writer_accepts_a_command_after_cached_transaction_replay -- --exact --nocapture` — passed.
- `cargo test -p orgasmic-daemon --lib committed_refresh_failure_returns_structured_503 -- --nocapture` — passed.
- `cargo test -p orgasmic-daemon --lib project_task_mutation_is_immediately_visible -- --nocapture` — 2 passed.
- `cargo test -p orgasmic-daemon --lib cached_task_retry_after_committed_503_repairs_projection -- --nocapture` — passed.
- `cargo test -p orgasmic-daemon --lib cached_graph_retry_after_committed_503_repairs_projection -- --nocapture` — passed.
- `cargo test -p orgasmic-daemon --lib release_terminal_tx_committed_refresh_failure_is_truthful_and_durable -- --nocapture` — passed.
- `cargo test -p orgasmic-daemon --lib sixteen_concurrent_api_writes_finish_within_budget_without_refresh_amplification -- --nocapture --test-threads=1` — passed in 147 ms; 16 requests -> 1 scan, 15 coalesced, 0 discarded, 4 ms scan.
- Three repetitions of `env -u ORGASMIC_RUN_ID -u ORGASMIC_HOME cargo test -p orgasmic-cli --test dispatch reviewer_close_ -- --test-threads=4` — 5/5 passed each time (15/15 total).
- `cargo fmt --all --check` — passed after final edits.
- `cargo clippy --workspace --all-targets -- -D warnings` — passed after final edits, zero warnings.
- `cargo build` — passed after final edits.

## Unmet Criteria

- None. The manager-reserved full `scripts/run-tests.sh` gate was deliberately not run, per dispatch brief.

## Residual Risk

- The five-discard bound is deterministic per captured batch, but wall-clock latency still includes the actual scan duration; a separately stalled filesystem scan remains governed by its existing scan/client bounds.
- The first detached clippy wrapper was interrupted before producing output or status; the single retry and the final post-edit clippy run both passed.
- No live daemon was installed, replaced, restarted, or probed. Independent review and the manager-owned full gate remain intentionally downstream.
