## Changed
- Moved artifact node/comment storage from `artifact.org`/`reviews.org` to kernel-backed `node.org`/`journal.org`; artifact reads, comment appends, consume, resolution, ID paths, and node creation now route through `orgasmic_core::node_kernel`.
- Extended `orgasmic project migrate` to convert legacy `ART-*` directories in place and preserve artifact-specific MDX, versions, launch metadata, and the independent `RESOLVED` axis.
- Migrated the tracked `ART-04FYD` fixture in place; the shipped artifact descriptor was already present and remains authoritative.

## Verification Gates
- `cargo test -p orgasmic-cli --bin orgasmic project_migrate::tests::migration_is_verbatim_dry_run_and_idempotent` — passed (1/1).
- `cargo test -p orgasmic-daemon --lib artifact` — 91 passed; two artifact API assertions exposed by the kernel mapping were corrected and rerun green; one unrelated pre-existing index test matched the broad name filter because its name contains `artifact` but still writes retired `.orgasmic/tasks/backlog.org` input.
- `cargo test -p orgasmic-daemon --lib api::tests::artifact_create_submit_feedback_consume_roundtrip` — passed (1/1).
- `cargo test -p orgasmic-daemon --lib api::tests::assemble_artifact_context_includes_subject_node_and_neighborhood` — passed (1/1).
- `cargo test -p orgasmic-daemon --lib artifacts::tests::parse_comments_round_trip` — passed (1/1); all artifact-store tests in the broad run were green.
- `cargo check -p orgasmic-daemon --lib` and `git diff --check` — passed.
- Production-path core probe parsed `.orgasmic/artifacts/ART-04FYD/node.org` and its one `journal.org` comment successfully.

## Unmet Criteria
- None.

## Residual Risk
- Legacy review records have no timestamp or machine fields; migration uses deterministic `[1970-01-01 Thu 00:00:00]` / `legacy` provenance while preserving their original actor, comment body, extras, consumed state, and resolved state.
- Out of scope: `index::tests::arch_like_produced_target_is_not_materialized_as_an_artifact` remains red because its fixture targets the deleted aggregate task-file reader; production artifact code is not involved.
