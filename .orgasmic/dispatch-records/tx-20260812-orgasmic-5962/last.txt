## Changed
- Rendered `UnsafeTraversal` details with anyhow's alternate display so the SKIP refusal preserves the complete cause chain, including the OS permission error.
- Reworded the refusal and `worktree-prune --help` contract to make the whole worktree the subject: it is skipped whole, nothing within it is deleted, and the operator must make the descendant readable (for example with chmod) or remove it by hand and rerun because there is no `--force` override.
- Tightened the production chmod-000 regression to pin the errno, whole-worktree guarantee, and remedy; replaced the privileged PARTIAL/KEPT fixture's silent return with a loud assertion guarded by reliable permission restoration.
- Changed only `crates/orgasmic-cli/src/manager.rs`, `crates/orgasmic-cli/tests/cli_parity.rs`, and `crates/orgasmic-cli/tests/dispatch.rs`; traversal and removal mechanics are unchanged and no force flag was added.

## Verification Gates
- `cargo test -p orgasmic-cli --bin orgasmic manager::tests::` — green: 72 passed, including the unreadable/depth parity fixtures and anchored-root regressions.
- `cargo test -p orgasmic-cli --test cli_parity worktree_prune_help_describes_the_mechanism_it_actually_uses` — green: 1 passed; phrase-sensitive whole-worktree help contract exercised.
- Production dispatch regressions — all green individually: unreadable descendant, over-depth tree, PARTIAL/KEPT accounting, ancestor symlink, and symlinked managed root.
- `cargo clippy -p orgasmic-cli --all-targets -- -D warnings` — green.
- `cargo fmt --all -- --check` — green.
- `git diff --check` — green.
- Durable logs: `/tmp/TASK-GRCWC.1-focused-20260813T0100.log` (status 0) and `/tmp/TASK-GRCWC.1-clippy-20260813T0103.log` (status 0).

## Unmet Criteria
- None within implementer scope. Independent review remains the manager-owned next stage.

## Residual Risk
- The chmod fixtures intentionally fail loudly on a privileged filesystem context that does not enforce the permission refusal; they cannot false-green there. No live operator worktree prune, daemon restart, service mutation, or runtime installation was performed.
