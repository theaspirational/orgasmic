# TASK-ZKZBF.1 — fix round report

Base: 136418f (round-1 merge). Head: this branch (fix commit + verify artifact).

## Per-finding outcome

### MEDIUM-1 — ID-immutability on all drawer writes: FIXED
- Hoisted the identity guard INTO the shared `validate_drawer_property_key_case`
  (api.rs): any casing of `ID` (`eq_ignore_ascii_case`) is refused by name as
  "identity is immutable and the drawer :ID: is derived from it … would re-key
  the node and dangle every reference to the old id", BEFORE the case check.
- Call sites that inherit it (all now one guard, per the review's fix shape):
  1. `post_org_node_edit` set/unset, Task arm (`validate_task_property_keys`)
  2. `post_org_node_edit` set/unset, other arm (Decision/Glossary/Project/Handoff)
  3. `create_graph_heading` — the old exact-match special case is DELETED; it
     collapses into the shared check
  4. `mutate_graph_heading` (graph revise) — inherits via its loop
- Dropped `"ID"` from the Project and Goal/Handoff lists in
  `node_layer_schema_property_keys`; the lists' doc now states ID is
  deliberately absent.
- Open question 1 honored as ruled: no repair verb invented, the task layer's
  refusal is extended, not rerouted.
- Message contract note: the task-layer ID refusal sentence moved from the
  `task_property_key_refusal` table (arm deleted — now unreachable) to the
  shared guard, so `task create/update --property ID=…` and `node prop … ID`
  all get the same layer-labeled sentence. No existing test asserted the old
  task-specific text; all 12 ZKZBF+HXSW0 tests stayed green UNADAPTED.

### MEDIUM-2 — `tx record --extra` case rule: FIXED
- Applied inside `validate_property_key` (core/tx.rs), the parse every ledger
  writer already passes (`TxEntry::validate` → `TxWriter::append_many`, and
  `POST /tx` → `tx_entry_write_error`): open key vocabulary
  (`[A-Z][A-Z0-9_]*`), closed case rule, miscased keys refused by name with
  the canonical spelling — mirroring `parse_close_property`. Reserved check
  now runs on the canonicalized key, so `end` is refused as reserved, not
  steered into "use END". Keys outside uppercase-snake (e.g. `MY-KEY`), legal
  to the old check, are now refused by pattern — same rule dispatch-close
  already enforces; all in-repo writers use uppercase-snake keys (verified by
  grep over every `extra.push` in daemon/cli/core src).
- Production-path test stages a dispatch with the uppercase `KIND=implementer`
  and proves `dispatch-close` still resolves it (aborted close records
  `manager.dispatch_aborted`), while `--extra kind=implementer` is refused
  naming `KIND` — the invisible-dispatch reproduction, closed.

### LOW-1 — PropertyNotFound disclosure: FIXED
- `unknown_property_error` (api.rs), twin of `unknown_section_error`: names the
  node, lists the drawer's actual keys ("its drawer has: ID, PRIORITY"), no
  path. The path stays in the `tracing::warn!` only, exactly the
  `org_parse_bad_request` convention.

### LOW-2 — unset advice shape: FIXED
- `task_property_key_refusal` split into `task_property_key_refusal_reason`
  (sentence only). Set path wraps it byte-identically
  ("`--property KEY=…` is refused: …"); the unset path now says
  "unsetting `KEY` is refused: …" — no `--property` phrasing.

### LOW-3 — one-refusal `id=` + dead arm: FIXED
- The guard lives before and case-insensitively to the case check, so
  `glossary create --property id=x` is ONE refusal naming ID immutability
  (tested for `id`/`Id`/`ID`).
- Dead `NodeLayer::Task | NodeLayer::Goal => "Task"` arm removed by replacing
  the inline match with a total `node_layer_label` helper.

## Changed
- crates/orgasmic-core/src/tx.rs — case rule + reserved-on-canonical + 3 unit tests
- crates/orgasmic-daemon/src/api.rs — shared ID guard, key lists, create special-case
  collapse, `node_layer_label`, refusal-reason split, `unknown_property_error`
- crates/orgasmic-cli/tests/node_property_silent_drop_cli.rs — 7 new tests (13 total
  in the file), zero existing tests adapted
- verify/TASK-ZKZBF.1/ — cmd, injection.patch (reverse of the source diff), expect-red
  pinned from a real injected-red run

## Gates (all run by me on this worktree)
- `cargo fmt --all` / `--check`: clean.
- `cargo clippy --workspace --all-targets -- -D warnings`: exit 0, zero warnings.
- `scripts/run-tests.sh -p orgasmic-cli --test node_property_silent_drop_cli
  --test task_property_silent_drop_cli` → **verdict: GREEN — no failures.**
  (13 + 6 = 19 passed; host calm, rate 0.0176)
- daemon-lib leg (daemon src touched beyond compilation):
  `scripts/run-tests.sh -p orgasmic-daemon --lib` → **verdict: GREEN — no
  failures.** (rate 0.9134, calm)
- core leg: `scripts/run-tests.sh -p orgasmic-core --lib` → **verdict: GREEN —
  no failures.**
- `orgasmic verify --artifact verify/TASK-ZKZBF.1 TASK-ZKZBF.1` →
  **PASS — red-then-green replay reproduced** (clean tree → inject → red exit
  101 signature-matched → revert byte-identical → green exit 0).

## Surprises measured
- Under the injected pre-fix tree, `node prop set term_… ID …` re-keys so
  completely that the same test's follow-up `glossary get` 404s on the old id,
  and `node prop unset … ID` removes the identity line and the post-edit
  re-read 404s — the dangling-reference outcome from the finding, reproduced
  live and pinned in expect-red.
- No existing test needed adaptation: the round-1 six stay green even under
  this round's injection (pinned: "6 passed; 7 failed").

## Boundaries / residual risk
- `parse_key_values` (prompt compile/lint `--value`) left untouched — template
  values, not drawers (review noted it out of scope).
- tx key vocabulary is now closed to `[A-Z][A-Z0-9_]*` for ALL writers through
  core (incl. daemon-internal ones); in-repo writers verified uppercase-only.
  An out-of-repo caller with hyphenated/lowercase tx keys would now be refused
  — that is the decided rule applied at the ledger, not a regression.
- Independent review pass: not performed here (next stage's assignment).
