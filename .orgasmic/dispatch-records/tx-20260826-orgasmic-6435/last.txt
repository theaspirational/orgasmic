## Changed
- Mint and persist a UUID machine-id per daemon home; daemon status and every new ledger event use it.
- Route project tx to `.orgasmic/machines/<machine-id>/tx/YYYY-MM.org`; read/deduplicate the legacy plus per-machine union.
- Add the hidden-ledger sync loop: machine-only coalesced commit, pull/rebase, push, and bounded push-race retry; no remote returns idle.
- Add UUID EVENT_ID hygiene, persistent writer/index deduplication, and synced JSON rejection envelopes with reasons.
- Leave the claimed-node staging hook empty for TASK-CLM6W as required.

## Verification Gates
- `cargo check -p orgasmic-daemon` — pass.
- `cargo test -p orgasmic-core home::tests::machine_id_is_stable_for_one_home_and_distinct_between_homes` — pass.
- `cargo test -p orgasmic-daemon --lib ledger_sync::tests -- --test-threads=1` — 3 pass, including two real daemon loops over a bare remote and a forced concurrent push retry.
- `cargo test -p orgasmic-daemon writer::tests::duplicate_event_id_is_a_persistent_no_op` — pass.
- `cargo test -p orgasmic-daemon index::tests::activity_is_the_deduplicated_union_of_machine_ledgers` — pass.
- `cargo test -p orgasmic-daemon api::tests::rejected_event_is_preserved_with_its_reason` — pass.
- `cargo test -p orgasmic-daemon --test integration project_tx_record_routes_to_its_machine_ledger_with_global_ids -- --exact` — pass.
- `cargo fmt --all -- --check`, `git diff --cached --check` — pass.

## Unmet Criteria
- None.

## Residual Risk
- Claim-gated node-directory staging remains deliberately inactive until TASK-CLM6W supplies claim ownership.
- Focused gates passed; no repo-wide test suite was run.
