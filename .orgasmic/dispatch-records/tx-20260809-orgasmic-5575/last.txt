# TASK-D6N77 implementation summary

Changed:
- Added `orgasmic manager dispatch-wait`, with `reported` / `died` / `timeout`
  outcomes and exit codes 0 / 2 / 3; `manager dispatch` now prints its
  backgroundable wait command.
- Added provider-neutral claimed-terminal registration and `orgasmic manager
  wake`; tmux and rmux re-check provider foreground evidence and composer
  readiness before automated input.
- Updated the shipped manager dispatch convention for both the wait and claim/
  wake flows.
- Commit: `585dfb6 feat(manager): wake claimed terminal sessions`.

Verification:
- `cargo fmt --check`
- `cargo check -p orgasmic-cli -p orgasmic-daemon -p orgasmic-drivers`
- Direct prebuilt unit tests:
  `manager::tests::dispatch_wait_timeout_parser_is_explicit_and_nonzero` and
  `modes::tmux::tests::automated_wake_composer_gate_is_provider_specific_and_rejects_menus`
- `target/debug/orgasmic manager --help`
- `git diff --check`

Unmet criterion:
- No real daemon dispatch was created. The task explicitly prohibited touching
  or restarting the live daemon/runtime, so there is no recorded live proof
  that `dispatch-wait` returns on a real report and ignores `[pid-gone]`.

Residual risk:
- The live terminal matrix (Claude/Codex × tmux/rmux), singleton races with an
  app-launched manager, and the exact foreground-process evidence on this macOS
  host still need an independent verifier against an isolated daemon.
