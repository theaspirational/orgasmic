YES — I would reinstall the operator runtime from this tree: the F3 operator path and recovery behavior are sound; the reject verdict below is confined to the P2b derivation acceptance.

## Verdict

**reject**

The branch closes P1a, P1b, and P2a. I found no remaining behavior path where the F3 refusal is invisible, where the F2 boot route reattaches an unobserved claim-owned prefix, or where the platform-independent raw-dirent regression lacks teeth. The task still fails acceptance 4 in two independently reproducible ways: the derivation self-check can green after silently losing valid Rust, and its claimed `cfg(test)` exclusion still admits test-only nodes.

## Findings

### P1 HIGH test/acceptance — the lexer self-check can green after silently dropping valid production functions

`scripts/derive-observation-surface.py:150-219`, especially `:183-187`, closes every block comment at the first `*/`. Rust block comments nest. With this valid Rust-shaped input:

```rust
fn kept() {}
/* outer /* inner */ " */
fn missed() {}
```

the exact `strip_noise` helper removes `fn missed`, while all five advertised checks pass: output length is preserved, braces are balanced, `seen == definitions + declarations + bodyless`, `bodyless == 0`, and indexed definitions equal parsed definitions. Reproduced directly against the branch helper. This is the exact under-approximation P2b says the self-check prevents, so acceptance 4's “lexer consumed to EOF” proof is not established. Recorded as `tx-20260809-orgasmic-5542`; corrected anchors are in `tx-20260809-orgasmic-5545`.

Impact: a future valid nested comment can hide every later authority leaf while `--self-check` and the derived surface stay green.

Fix direction: use a real Rust parser, or at minimum implement nested block-comment depth and pin this counterexample as a test that must retain both functions. A length-preserving blank-to-EOF path is not an EOF-consumption assertion.

### P2 HIGH test/acceptance — `cfg(test)` exclusion handles modules only, not items or blocks

`scripts/derive-observation-surface.py:308-320` builds only test-module ranges; `:326-369` marks a definition as test only by file path or membership in one of those modules. It does not exclude individual `#[cfg(test)]` functions or `#[cfg(test)]` blocks inside production functions.

The exact before/after seen-set comparison proves this is live, not hypothetical. Baseline `b8cd3d2` reports 935 reachable names and the implementation reports 929, but that net -6 is eight removals plus two additions. The current graph adds:

- `recovery_claim.rs:359 readdir_entry_name`, an individual `#[cfg(test)]` function;
- `collect`, a ghost free-function edge created when the call scanner interprets the local closure call in the test-containing `ClaimDirectory::names` body.

The eight removals are all safe noise: API/stub/test-tooling definitions reached through the previously included test modules, or bodyless `kill`/`setsid` extern declarations. The 96 leaf rows / 93 unique leaf keys are unchanged. Nevertheless, the production graph remains test/macro-inflated in the precise way acceptance 4 required removing. Recorded as `tx-20260809-orgasmic-5546`.

Impact: over-approximation can continue masking under-approximation, and the reported 929 count is not a checked production call graph.

Fix direction: track `cfg(test)` on functions, impls, and blocks, distinguish local callable bindings from free functions, and assert that neither `readdir_entry_name` nor the ghost `collect` key appears in the production graph. A `syn`-backed pass is the cleaner fix.

## Open Questions

No unresolved question blocks this verdict. Rulings on the brief's residuals:

- C6: at least two of the 48 dynamic rows do reach authority decisions: `Supervisor::reattach -> WorkerDriver::attach` and `spawn_driver_attach_probe -> WorkerDriver::attach` decide whether runs become reattached or interrupted. “A list, not a verdict” is acceptable only as the explicitly retained manual residual for this narrow round; it is not evidence that the 96-leaf output is a complete production graph. A type-aware follow-up should resolve these edges.
- C7: keep `failed_recoverable` out of `RunningAgentsMenu` Recent. That list explicitly contains terminal/no-op plus ambiguous runs and already omits interrupted and reattached; `RunsView` is the diagnostic inventory surface and now carries the refused recovery correctly.
- C8: file a new task for the unowned supervisor probe-child family, not an immediate unowned registry entry and not a fix in this round. Capture exact panic signatures, then either fix the probe deadlines/coordination or add short-lived registry entries owned by that task. `TASK-X0PV1` owns four specifically enumerated modes and should not silently absorb a different family.

## Verification Notes

- Reviewed `main...task-2qk4p.1.1.1.1.1-impl` at `a71f09d`: 8 files, 821 insertions, 37 deletions. `git diff --check` passed. No `verify/` artifact changed, and no injection patch body was read.
- P1a: after `npm ci` installed the lockfile-pinned local toolchain, `npm test -- RunsView` passed 4/4 and `npm run typecheck` exited 0. The rendering test places a `failed_recoverable` row in the table and verifies the reason cell contains the sanitized subject plus the remediation sentence. `npm test -- RunningAgentsMenu` also passed 4/4. The initial pre-install invocations failed as expected (`vitest` absent; ambient TypeScript 6 emitted TS5101); the local versions are the reported results.
- P1b/C1: `api::tests::boot_reattach_refuses_a_candidate_whose_claim_store_could_not_be_listed` logged 1 passed. The faulted phase goes through `reattach_live_runs_on_boot`, asserts no live supervisor row and byte-identical JSONL; the healthy phase removes the claim, reaches `Supervisor::reattach`, installs the same run in the supervisor, and appends the reattach marker. The sticky fault is keyed to `ClaimDirectory::names`; it cannot mask run-catalog/session-directory scanning. The failed origin record is terminal, so the replacement is the real boot candidate.
- Boot-test command handling: the first wrapper attempt failed before Cargo launched because an unset redirection variable expanded to an empty path. The one allowed corrected Cargo retry completed and its durable log proves the exact test green, but the launching shell orphaned before preserving Cargo's exit code. An attached watchdog observed the final three 10-second samples at 0% syspolicyd; the first part of compilation was not sampled. I did not start a third attempt.
- C2: `stub_config()` remains `{}` and the explicit `StubDriver::attach` default remains `NotReattachable`. `stub_reattachable: true` occurs only in the new API fixture's persisted claim/session metadata. A future fixture must opt in with that literal key; no existing caller silently becomes live.
- P2a/C3: watched Cargo invocation exited 0; both `recovery_claim::tests::f2_an_*` tests passed (2/2), watchdog peak 0%. The injected branch constructs a real `libc::dirent`, reads `d_name` through the production `CStr::from_ptr(...).to_bytes()`, and shares the single `collect` closure with real `readdir`. The second test continues through `pending_recovery_claim_owns_session`, covering the caller's `.json` filter. Static red-proof check: the old `if let Ok(name) = name.to_str()` implementation cannot push the injected invalid bytes, so the new `names.len() == 2` assertion necessarily fails under that defect; lossy conversion fails the exact-byte/U+FFFD assertions.
- P2b/C5: baseline script against baseline source reports 935 reachable / 96 leaves; implementation reports 929 / 96. Exact leaf sets are identical at 93 unique keys. Exact seen-set diff is eight removed and two added, as described in P2.
- `python3 scripts/derive-observation-surface.py --self-check` exited 0 and printed 6174 seen, 6158 definitions, 16 declarations, 0 bodyless, 5040 indexed names. `--report-dynamic` exited 0 with 48 rows and includes `Supervisor::acquire_impl [signature] -> WorkerDriver::{acquire,harness,transport}` plus the attach rows noted above.
- Acceptance 5 was not rerun. I verified the live manager evidence `tx-20260808-orgasmic-5536` and accepted its calm-host run as instructed:

  ```text
    failures : 0
    cargo    : exited 0 (0 all green, 101 libtest reported failures)
    host     : calm (threshold syspolicyd_rate>=1.50)
               delta   load=1.69 syspolicyd_cpu=12.2 wall_s=114
               rate    0.1070
  verdict: GREEN — no failures.
  ```

- The worktree is source-clean after review. `npm ci` changed only ignored `ui/node_modules`.

Not checked: the full daemon wrapper independently, recovery-fault repetition, clippy, fmt, workspace suite, MSRV 1.87, non-unix, real-board latency, any `orgasmic verify` replay, or any `verify/*/injection.patch` body. I did not mutate source to replay P1a/P1b/P2a red injections because the reviewer contract is strictly read-only; P1a's manager replay is `tx-20260808-orgasmic-5537`, while P1b/P2a were checked from the exercised assertions and the old defect bodies.

## Fix Directions

1. Repair P1 and P2 together with a real Rust parser if feasible. If retaining the lexer, add nested block-comment depth, item/block `cfg(test)` ownership, local-callable handling, and executable counterexamples for both findings.
2. Preserve the P1a/P1b/P2a implementation and tests; I found no fix needed on those paths.
3. File the C8 probe-child follow-up as a new owned task and only then decide whether measured signatures merit temporary flake-registry entries.
