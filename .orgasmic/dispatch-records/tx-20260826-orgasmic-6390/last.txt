# Changed
- Added shared deterministic `.orgasmic/views/{board,glossary,decisions}.org` generation from node directories, with lifecycle-grouped task ordering, atomic replace, and no rewrite when bytes are unchanged.
- Wired view generation into daemon project refreshes, ignored `views/` watcher echoes, and added `orgasmic views build` for cold clones.
- Added `views/` to current and scaffold `.orgasmic/.gitignore` files.
- Repointed shipped context packs, router/prompt/skill references, scaffold guidance, schema prose, and UI OrgView/test source paths away from removed aggregate locations.

# Verification Gates
- PASS: `cargo test -p orgasmic-daemon --lib refresh_rebuilds_byte_stable_derived_views` (1 passed; daemon rebuild, unchanged-byte stability, and node-change regeneration).
- PASS: `cargo check -p orgasmic-core`.
- PASS: `cargo fmt --all -- --check` and `git diff --check`.
- PASS: production CLI probe: first `cargo run --quiet -p orgasmic-cli -- views build` reported `3 changed`; second reported `0 changed`; SHA-256 values for all three views were identical across builds.
- PASS: exact shipped/UI grep found no `.orgasmic/glossary.org`, `.orgasmic/decisions.org`, `.orgasmic/tasks/<state>.org`, or concrete removed task-state path.
- ENVIRONMENT-BLOCKED: `npm run typecheck` under `ui/` could not start because `tsc` is absent (`ui/node_modules` is not installed).

# Unmet Criteria
- None.

# Residual Risk
- UI TypeScript compilation was not rerun locally because UI dependencies are absent; the UI changes are path-string replacements only.
