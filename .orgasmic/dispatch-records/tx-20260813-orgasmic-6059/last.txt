## Changed

- Commit: `63e662e` (`fix(dispatch-close): guard legacy partial replay`).
- `crates/orgasmic-cli/src/manager.rs`: legacy partial-close replay now uses parsed tx entries, matches exact `CLOSED_TX` plus task, excludes every close carrying lifecycle metadata, and only replays while the task remains at the dispatch kind's active stage.
- `crates/orgasmic-cli/tests/dispatch.rs`: retained the benign old-format partial-close recovery test and added a deterministic two-task regression proving an advanced already-closed sibling stays `done` while the open sibling converges to `in_review`.
- `crates/orgasmic-cli/src/daemon_client.rs`: committed-but-durability-uncertain HTTP 503 responses now tell the operator the mutation committed, durability confirmation is uncertain, and exact state must be re-read before retry; focused text test added.
- `crates/orgasmic-daemon/src/writer.rs`, `crates/orgasmic-daemon/src/api.rs`: replaced API string matching with `CommittedSyncUncertainError`; retained the structured `503` JSON contract and added a test proving a lookalike untyped string is not classified as committed.

## Verification Gates

- PASS: both CLI partial-close integration tests:
  - `bundled_partial_close_retry_is_idempotent_and_visible`
  - `legacy_partial_close_retry_does_not_move_an_advanced_sibling_backwards`
- PASS: CLI production atomic-close and old-ledger reconciler tests:
  - `dispatch_close_commits_terminal_and_lifecycle_txs_in_one_request`
  - `legacy_torn_dispatch_close_is_still_repaired_by_next_manager_command`
- PASS: writer durability tests for one flock/one sync/order, injected pre-commit failure, concurrent non-interleaving, and post-append sync-loss retry without duplicate append.
- PASS: daemon API tests `dispatch_close_uncertain_durability_uses_typed_writer_error` and `dispatch_close_home_ledger_refreshes_project_for_read_after_write`.
- PASS: CLI transport test `committed_uncertain_durability_requires_reconciliation_not_success`.
- PASS: `cargo clippy -p orgasmic-cli -p orgasmic-daemon --all-targets -- -D warnings`.
- PASS: `cargo fmt --all --check` and `git diff --check`.
- PASS: `orgasmic verify TASK-P9T4N`; injected run exited 101 with the pinned atomic-close signature, patch reverted byte-identically, and uninjected run passed.

## Unmet Criteria

- None.

## Residual Risk

- Per the correction brief, the reviewer's low-probability observation about replay from a noncanonical task source file was intentionally not addressed.
- No daemon install or restart and no live-daemon mutation were performed. The full workspace suite was not run; the required and directly affected gates above were run, with no deterministic failures or host-load flakes.
