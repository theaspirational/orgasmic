# Changed

- Restored immediate project read-after-write for every project-file mutation under both `tx.commit_to_project=true` and `false`; home-routed mutations now publish both the project and home-ledger projections through one API-level orchestration.
- Reworked refresh coordination so publication never holds the coordinator mutex, trailing coalescing has a 200 ms first-request bound, home tx scans do not consume the two-project permit, captured scan failures do not drain newer mutation/explicit/watcher arrivals, and per-target scans remain single-flight.
- Built bounded target-only seeds on the blocking pool instead of cloning the whole multi-project snapshot on an async worker; preserved last-good, parse-error, activity-index, and live Git URL publication behavior.
- Routed cached task and graph creation replays through reconciliation, removed mutation-acknowledgement Git actor lookup, de-duplicated registration/full-reindex Git probes, and removed the unused home refresh helper.
- Kept structured committed-refresh 503 semantics on run release while classifying a durable terminal tx truthfully and excluding it from lost-finalization accounting.
- Corrected deferred writer queue/completion telemetry and removed the hardcoded slow-log coalescing count.
- Updated `verify/TASK-K9WWM/README.md` with the measured root cause, simultaneous and staggered measurements, the explicit classifier decision, and exact reproduction commands.

# Verification Gates

- `cargo fmt --all --check` — green (final and post-README checks; `/tmp/k9-fmt-final.log`, `/tmp/k9-fmt-post-readme.log`).
- `cargo clippy --workspace --all-targets -- -D warnings` — green to completion, including `orgasmic-cli` (`/tmp/k9-clippy-final.log`).
- `cargo build` — green (`/tmp/k9-build-final.log`).
- All new/fixed coordinator cases passed individually: simultaneous single-flight, bounded stagger, stale generation after actual build, failed-scan arrival survival, responsive reads, two-project cap, home permit independence, cancellation convergence, Git stall independence, Git mid-build merge, one registration probe, and one full-reindex probe (`/tmp/k9-coordinator-focused.log`, `/tmp/k9-coordinator-remaining.log`, `/tmp/task-k9wwm-fix1-repetitions/staggered.log`).
- API/writer/release regressions passed: immediate task visibility for both tx routing modes, successful idempotent replay without a scan, committed-503 task and graph repair, 16-write HTTP concurrency, durable truthful release terminal tx failure, deferred writer accounting, and last-good parse-error preservation (`/tmp/k9-visibility.log`, `/tmp/k9-api-writer-focused2.log`, `/tmp/k9-last-good.log`).
- CLI reconciliation regression passed (`/tmp/k9-cli-reconcile.log`).
- `required_test_tooling_is_present` passed with no waiver (`/tmp/k9-cli-tooling.log`).
- Five `reviewer_close_*` repetitions at `--test-threads=4`: 25/25 green; test bodies 6.18-7.04 s (`/tmp/task-k9wwm-fix1-repetitions/reviewer-close-{1..5}.log`).
- Five production HTTP 16-write repetitions at `--test-threads=4`: 5/5 green, every write exactly once, 132-182 ms, 16 requests -> 1 scan / 15 coalesced (`/tmp/task-k9wwm-fix1-repetitions/api-concurrent-{1..5}.log`).
- Honest staggered measurement: 30 requests -> 4 scans / 27 coalesced / 1 stale discard in 766 ms (`/tmp/task-k9wwm-fix1-repetitions/staggered.log`).
- Full `scripts/run-tests.sh -p orgasmic-cli` classified gate: GREEN, 0 failures, no tooling waiver, `host: calm`, syspolicyd rate 0.0688 (`/tmp/k9-cli-classified-final.log`, status 0).

# Unmet Criteria

- Independent review and installation are intentionally not part of this implementer dispatch. The accepted plan requires a fresh independent review before any installation.

# Residual Risk

- The live installed daemon and LaunchAgent were not modified, restarted, or used for a production mutation probe. The isolated production-HTTP regression exercises the same daemon/router path; installed-candidate counters remain the independent reviewer/manager checkpoint.
- The exact all-`index::tests::` aggregate command had two pre-execution/background-wrapper interruptions with empty logs; per command-session policy it was not launched a third time. Every new or changed coordinator case and the relevant last-good cases were then run directly and passed, while workspace clippy, build, and the full classified CLI crate gate were green.
- The timeout classifier remains unchanged by explicit decision: the production path was fixed, `scripts/run-tests.sh` is outside write scope, and a future timeout remains a real reconciliation-required failure rather than a registered flake.
