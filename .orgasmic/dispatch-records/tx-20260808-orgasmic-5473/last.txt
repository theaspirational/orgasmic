# TASK-RMA18.1.1.1.1.1 — implementer summary

Four findings closed, all text or artifact; **no behaviour changed**. Branch
`task-rma18.1.1.1.1.1-impl`, two commits (`2b9bdb9` fix, `26e2370` artifact).

## Changed

- `crates/orgasmic-cli/src/manager.rs:386` (R1) — the bare universal is DELETED,
  per the manager ruling: `refuses any NESTED \`.git\` entry` →
  `refuses a NESTED \`.git\` entry`. Nothing was added; the "as deep and as far
  as it can read" clause was NOT written. Verified on the production path:
  `./target/debug/orgasmic manager worktree-prune --help` now ends the repo-gone
  sentence with "the anchored walk of the tree's own contents refuses a NESTED
  `.git` entry, of any type".
- `crates/orgasmic-cli/tests/cli_parity.rs:112-125, 228-256` (R2) — the doc
  comment now says both clauses ban a WORDING (the asymmetry the reviewer filed
  is gone) and names the escape no per-sentence pin of this shape can see: the
  claim split ACROSS two sentences, neither carrying both halves. Ban list grew
  from 2 literals to 5: `"both branches"`, `"either branch"`,
  `"whatever the branch"`. The class is deliberately NOT closed.
- `crates/orgasmic-cli/src/manager.rs:4155-4176` (R4) — both arms hedged to what
  was checked. `nonempty_dir_under` (`:4635-4657`) returns `true` for non-empty
  AND for a listing that FAILED (`Err(_) => true`), so neither arm may say it
  observed an initialized submodule. Repo-gone arm: "names {module}, which this
  verb found to be a directory it could not list or listed as NON-EMPTY (it does
  not distinguish the two, and neither is a licence to delete)". Sibling arm
  (`:4169`, the pre-existing copy of the same over-statement) got the same
  treatment. The `--force`-is-unavailable remedy and the module path it names are
  unchanged.
- `verify/TASK-RMA18.1.1.1.1/{cmd,expect-red,injection.patch}` (R3) — new.
  Injection deletes the repo-gone leg's `if repo_gone { … }` message so that
  branch falls through to the ordinary arm; `let _ = repo_gone;` keeps the
  injected tree warning-free. Pinned signature: `exit: 101`, the `.gitmodules`
  arm assertion text, `while the repository is still there`, and
  `test result: FAILED. 0 passed; 1 failed`.

## Verification gates — all green, all run in this worktree

**RED MEASURED HERE, not transcribed.** With the injection applied, exactly one
test failed at `dispatch.rs:9477`:

```
thread '...gitmodules_names_a_populated_directory' panicked at crates/orgasmic-cli/tests/dispatch.rs:9477:5:
the report must name the record it refused over (`.gitmodules`) and the submodule path, got:
KEPT PATH=<tmp>/home/worktrees/orgasmic/task-gonemodules WHY=refusing to remove it: <tmp>/... records a
submodule at vendor/sub, ... — no salvage can capture what is inside a submodule ... or — while the
repository is still there — remove the worktree with `git worktree remove --force`
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 85 filtered out
```

- **14 `worktree_prune_*`**: `test result: ok. 14 passed; 0 failed`.
- **`cli_parity`**: `test result: ok. 7 passed; 0 failed`.
- **R2's new literal MEASURED to fire**, not assumed: appending "It reads that
  INDEX on both branches." to `after_help` — the shape that PASSED the
  two-literal version — now panics at `cli_parity.rs:255` with
  `worktree-prune help quantifies index-reading over "both branches"`. Reverted.
- **All six verify artifacts PASS** (`orgasmic verify` each, real red-then-green,
  tree byte-identical between):
  `TASK-RMA18`, `.1`, `.1.1`, `.1.1-placement`, `.1.1.1`, `.1.1.1.1` — six
  `PASS — red-then-green replay reproduced`, exit 0 each. No patch context moved:
  the five existing artifacts applied unchanged.
- **`scripts/run-tests.sh -p orgasmic-cli`** — VERDICT block verbatim:

```
  suite    : cargo test -p orgasmic-cli --no-fail-fast -- --skip legacy_drivers_and_explicit_pairs_emit_equivalent_start_events
  registry : verify/flake-registry.toml (7 entries, every owner open)
  billed   : legacy_drivers_and_explicit_pairs_emit_equivalent_start_events — NOT RUN (--skip applied to every invocation)
  failures : 0
  crashed  : none — every failing target reported a failure list
  cargo    : exited 0 (0 all green, 101 libtest reported failures)
  ignored  : 1 test(s) carrying #[ignore]
  cfg-off  : passed — the pause rendezvous hooks are compiled out without debug_assertions
  environ  : complete — no tool requirement was waived
  host     : calm (threshold syspolicyd_rate>=1.50; load=26.25 elevated (>=8.0), corroborating only)
  rate     0.0816
verdict: GREEN — no failures.
```

- **`cargo clippy --workspace --all-targets -- -D warnings`** — exit 0.
- **`cargo fmt --all --check`** — exit 0.
- **Production-path probe of the hedged R4 text** (a unit assertion cannot show
  the operator's line, so it was dumped from the real verb over the real
  fixture, then reverted):

```
WHY=refusing to remove it: <tmp>/home/worktrees/orgasmic/task-gonemodules has no repository behind it,
so no index and no `modules` directory survive to say whether it contains a submodule — but its own
`.gitmodules` does survive inside the tree, and names vendor/sub, which this verb found to be a directory
it could not list or listed as NON-EMPTY (it does not distinguish the two, and neither is a licence to
delete). ... `git worktree remove --force` CANNOT run here ... then remove vendor/sub yourself and re-run
```

- Every cargo invocation ran detached under the syspolicyd watchdog (sample
  `ps -o pcpu=` every 10s, kill the run's pgid at ≥400% held 60s). Never tripped;
  peak observed 45.5%, and `run-tests.sh` independently rated the host **calm**
  (rate 0.0816 against a 1.50 threshold). `ORGASMIC_HOME` was never set; the
  runtime was not reinstalled; `~/.orgasmic/bin/` untouched;
  `legacy_drivers_and_explicit_pairs_emit_equivalent_start_events` never run.
  `git rev-parse --show-toplevel` confirmed the dispatch worktree before both
  commits.

## Unmet criteria

None. All five acceptance items closed.

## Residual risk

- **R1's safety still rests on unrun source symmetry.** Neither fail-open of
  `walk_worktree` (`:3833` depth, `:3836`/`:3855` unreadable) has ever been
  exercised by a fixture — no 65-deep tree, no `chmod 000`. Two reviewers derived
  the `remove_contents` symmetry from source; nobody has run it. The deletion
  removes the over-claim without asserting anything new, so it cannot be a fourth
  false statement, but the *protection* the sentence still implies is unproven.
  The reviewer asked for a `chmod 000` fixture before the next destructive change
  to this verb; that remains a separate task.
- **R2 closes wordings, not the class, by ruling.** A sixth quantifier still
  passes, and the split-across-two-sentences escape cannot be caught by any pin
  of this shape — both are now stated in the doc comment rather than left for the
  next reader to rediscover.
- **The help still under-states one thing, unchanged by this round and not in
  scope**: `RemovalFailure` carries `touched` and `remove_contents` is
  readdir-ordered, so a fail-open shape yields a *reported partial removal*, not
  an untouched worktree. The nested repository itself survives (it is inside what
  blocked the walk), which is why this is docs and not data loss.
- **`nonempty_dir_under`'s fail-closed `Err(_) => true` is now described but
  still not tested.** The hedge is accurate for both paths; only the non-empty
  path has a fixture.
