## Changed
- Added `orgasmic_core::fold_dispatches` as the single dispatch lifecycle fold, including recovery lineage, worker reports, partial closes, cleanup state, done, and aborted records.
- Replaced the CLI manager scan and daemon dispatch-wait ledger fold with that core function; closed daemon generations now terminate `dispatch-wait` successfully.
- Added the dispatch type routing guard and documented the complete project-tx type set in `shipped/schema/tx.org`.

## Verification Gates
- `cargo fmt --all -- --check` — pass.
- `cargo test -p orgasmic-core dispatch_fold_shares_recovery_reporting_and_close_state` — pass.
- `cargo test -p orgasmic-daemon every_dispatch_type_routes_to_project_tx` — pass.
- `cargo test -p orgasmic-daemon dispatch_wait_folds_the_complete_recovery_lineage_before_reports` — pass.
- `cargo test -p orgasmic-cli tx_scan_returns_only_unclosed_dispatches` — pass.
- `cargo test -p orgasmic-cli worker_reported_tx_keeps_dispatch_open_until_manager_done` — pass.
- `cargo test -p orgasmic-cli dispatch_wait_response_classifier_preserves_the_documented_exit_contract` — pass.
- Final core production-path probe over this repo's 6,343 tx entries — 586 dispatches: 8 open, 578 closed, 261 reported; both CLI and daemon call this fold.

## Unmet Criteria
- None.

## Residual Risk
- No full workspace suite was run, per the dispatch's strict focused-test budget.
