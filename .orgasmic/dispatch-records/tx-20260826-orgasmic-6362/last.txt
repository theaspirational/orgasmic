## Changed
- Ported and hardened `orgasmic_core::node_kernel`: node directory creation, node/journal parsing and rendering, structured comment edit/tombstone/consume, drawer validation, nested Org prose, and the 500 KiB size lint threshold.
- Added serialized daemon writer operations for journal append, OCC comment edit with `:EDITED_AT:`, and OCC tombstone; column-0 `* ` prose is refused and oversized journals warn.
- Added `journal.org` to the generic Org rewrite denylist.
- Shipped and parser-gated `shipped/schema/journal.org` v1 covering AP971.1 decisions 1-12.

## Verification Gates
- `cargo test -p orgasmic-core node_kernel` — PASS (4 tests).
- Real-data prototype migration plus `ORGASMIC_MIGRATED_DIR=<fixture> cargo test -p orgasmic-core real_data::every_migrated_node_parses -- --nocapture` — PASS (898 nodes, 0 migration anomalies).
- `cargo test -p orgasmic-daemon journal_ops_append_edit_with_occ_and_tombstone` — PASS.
- `cargo test -p orgasmic-daemon org_file_rewrite_refuses_ledger_paths` — PASS.
- `cargo test -p orgasmic-core parses_shipped_schema_files` — PASS.
- `cargo fmt --all -- --check` and `git diff --check` — PASS.

## Unmet Criteria
- None.

## Residual Risk
- Node indexing and watcher-origin hand-edit lint publication belong to later AP971 chain tasks; this task supplies the reusable size predicate and warns on structured writer mutations.
