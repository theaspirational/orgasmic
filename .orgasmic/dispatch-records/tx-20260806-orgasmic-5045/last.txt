# TASK-STWVB.1.1 — implementer summary

## Workspace measurement and chosen number

One `--workspace` run of `scripts/run-tests.sh` (no args), BEFORE load 6.10:

| field | value |
| --- | --- |
| wall_s | 356 |
| syspolicyd_cpu delta | +215.2 s |
| rate | 0.6045 s/s |
| host word | calm |
| exit | 1 (unrelated REAL in `dispatch.rs`; not exit 4) |

Under the old absolute bound of 100 s this run would have been DEGRADED on a
calm host. Ambient rate is 0.0737–0.0753 s/s; scoped in-run 0.35–0.96 s/s;
historical thrash ~5.6 s/s at ~50 s wall. Chose `SYSPOLICYD_RATE_DEGRADED=1.50`
(~2.5× measured workspace rate, above scoped peak, below thrash).

## No-arg `scripts/run-tests.sh` on a calm host

BEFORE load 6.10 (< 8). Did **not** return exit 4. Exit 1 from an unrelated
alone-red in `reviewer_dispatches_before_reported_implementer_close_and_unlocks_main_merge`.
Verbatim verdict host stamp:

```
  host     : calm (threshold syspolicyd_rate>=1.50; load corroborating only, display>=8.0)
             before  load=6.10 syspolicyd_time=496:25.62
             after   load=6.67 syspolicyd_time=500:00.79
             delta   load=6.10 syspolicyd_cpu=215.2 wall_s=356
             rate    0.6045

verdict: RED — 1 real failure(s). This red means something.
```

(Full block in `/tmp/stwvb11/calib.log`. That one workspace run served as both
the B1 calibration measurement and the acceptance demonstration — a second
workspace loop was skipped to avoid another syspolicyd storm on this host.)

## Crashed binary on a degraded host (B2)

With stub cargo emitting SIGABRT / exit 101 and
`ORGASMIC_HOST_STATE_SAMPLE=load=11.41,syspolicyd_cpu=250.0,wall_s=100`:

```
  host     : DEGRADED (...)
verdict: RED — cargo exited 101 with no per-test failure list.
```

Exit 1. Never "suite looked green".

## Gates

| gate | result |
| --- | --- |
| `scripts/run-tests-selftest.sh` | 33 passed, 0 failed |
| `scripts/run-tests.sh --check` | exit 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | exit 0 |
| `cargo fmt --all --check` | exit 0 |
| `orgasmic verify TASK-STWVB.1.1` | PASS — red-then-green; first FAIL under injection is the B1 rate case |
| `verify/flake-registry.toml` | byte-unchanged |

## Could not do / residual

- Did not run a second full-workspace acceptance after the calibration run
  (budget + host reboot risk). The calibration run itself demonstrated
  host:calm and non-4 exit on a calm BEFORE.
- The workspace suite currently has one unrelated REAL
  (`reviewer_dispatches_before_reported_implementer_close_and_unlocks_main_merge`);
  out of scope for this task.
- Part A untouched; no A-effect probe run.

