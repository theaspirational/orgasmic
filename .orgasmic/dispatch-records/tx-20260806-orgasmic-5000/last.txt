# TASK-STWVB.1 — implementer summary

## Verdict ladder (what it returns now)

| Case | Host state | Verdict | Exit |
| --- | --- | --- | --- |
| alone-red (fails in isolation) | calm or degraded | RED — this red means something (host stamp alongside) | 1 |
| load-sensitive (unregistered, isolation-green) | degraded (BEFORE load or syspolicyd delta) | INCONCLUSIVE — load-sensitive failure(s) | 4 |
| load-sensitive shape | calm / unknown | REAL (not LOAD-SENSITIVE; unknown cannot mint an excuse) | 1 |
| registered flake | calm | GREEN modulo flake | 0 |
| registered flake | degraded | INCONCLUSIVE — registered flakes; still not a trusted green | 4 |
| clean suite | calm | GREEN — no failures | 0 |
| clean suite | degraded | INCONCLUSIVE — suite looked green; that green is not trusted | 4 |
| `--classify` with no host stamp | unknown | LOAD-SENSITIVE unavailable; alone-red/unregistered stay REAL | as above |

Sanity: worker-busy BEFORE load=12.95 on a green suite → exit 4 INCONCLUSIVE (not over-corrected).

## Calm-host load baseline and threshold

Measured 2026-08-06 on this 10-core Mac during already-built `orgasmic-daemon --lib`:
- mid-run 1-min load ~7.35 (BEFORE was already 8.94 from other workers)
- post-run after calm BEFORE 4.13 → 7.87

`LOAD_DEGRADED_THRESHOLD=8.0` applies to the **BEFORE** load only. The suite's own mid/post contribution sits near 7–8, which is why during/after load must not trip the gate. Quiet BEFORE ~4–6.5 stays calm; worker-busy BEFORE 12.95 still yields INCONCLUSIVE.

`SYSPOLICYD_CPU_DEGRADED=100.0` (seconds of cumulative CPU-time delta). Historical +32 / +281; F9 deltas below.

## F9 probe (part A settling)

Ran two executions of the already-built test binary (`orgasmic_daemon-5f50721007a8fd8c`):

| Run | Mode | BEFORE load | syspolicyd Δ | wall | result | fixture stats |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | consolidated (default) | 8.94 | **+53.89 s** | 155.7 s | 735 pass / 1 fail (hung mux) | fresh=4 links=136 |
| 2 | `ORGASMIC_DISABLE_SHARED_FIXTURE_LINKS=1` | 4.13 | **+49.33 s** | 51.4 s | 736 pass / 0 fail | fresh=140 links=0 |

**Verdict on A:** causal effect on this suite is **not established** by this probe. Deltas were similar; run order confounds (run2 cache-warmed after run1; run1 started busier and spent wall time on a hung-mux failure). Mechanism itself remains previously measured (fresh 196 ms / link 4 ms / copy 162 ms). **Part A kept.**

## CI decision (F5)

Stated at `.github/workflows/ci.yml` Suite step: **exit 4 fails the lane deliberately**. After F4, exit 4 means pre-run host degradation or genuine syspolicyd delta — not the suite tripping itself via after-load. An untrusted result must not merge as green.

`.orgasmic/project.org`: INCONCLUSIVE is neither green nor a code defect — re-run when calm; alone-red on a degraded host is still exit 1.

## Final `run-tests.sh` verdict block (green suite, measured calm stamp)

```
  failures : 0
  ignored  : 1 test(s) carrying #[ignore]
  environ  : complete — no tool requirement was waived
  host     : calm (thresholds before_load>=8.0 syspolicyd_cpu_delta_s>=100.0)
             before  load=4.13 syspolicyd_time=490:03.40
             after   load=7.87 syspolicyd_time=490:52.73
             delta   load=4.13 syspolicyd_cpu=49.3

verdict: GREEN — no failures.
```

Returns **GREEN / exit 0** because BEFORE load 4.13 < 8.0 and syspolicyd delta 49.3 s < 100.0, with zero failures. (Classified from the F9 green log plus the measured stamp — third full suite run skipped per execution budget.)

## Gates

| Gate | Result |
| --- | --- |
| `scripts/run-tests-selftest.sh` | 27 passed, 0 failed |
| `scripts/run-tests.sh --check` | OK — 7 entries |
| `cargo test -p orgasmic-daemon --lib -- test_fixtures` | 3 passed |
| `cargo clippy --workspace --all-targets -- -D warnings` | OK |
| `cargo fmt --all --check` | OK |
| `orgasmic verify TASK-STWVB.1` | PASS — first FAIL under injection is the alone-red case |

`verify/flake-registry.toml` byte-unchanged.

## Unmet / residual

- F9 did not prove A's causal contribution on this suite (stated above).
- No third full `-p orgasmic-daemon --lib` via `run-tests.sh` live path (budget); live sampler covered by `--sample-host` selftest + stamp write path in code.
- CI whether macos-15 clears BEFORE load 8.0: answered free on first CI run via `host :` stamp; with BEFORE-based judgment it should usually be calm.
