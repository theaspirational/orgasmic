<RichText>
## Multi-model extraction — final artifact

**Run** mm_MOCK1 · **Prompt:** "Can I put a SQLite database on a network filesystem (NFS/SMB) and have several machines use it?"

**3 participants · 7 dispatches · 24 min · 11 knowledge items (3 shared, 4 unique, 1 contradiction, 3 to verify)**
</RichText>

<Callout tone="warning">
**Everything below is simulated.** The model answers, tx ids, timings, and knowledge items are invented. This artifact exists so we can agree on the *shape* of an extraction-mode result before building the pipeline.
</Callout>

<Section title="Final answer">
<RichText>
**Short version: don't.** All three models independently agree that SQLite over NFS or SMB with multiple machines is unsafe: POSIX advisory locking is broken or unreliable on most network-filesystem implementations, so two writers can corrupt the database. WAL mode is ruled out entirely — it coordinates through shared memory (the `-shm` file, via mmap), which cannot work across machines.

What to do instead, in order of preference:

- **Replicate instead of sharing the file.** Run SQLite locally and replicate with Litestream or LiteFS, or step up to a client-server database if you truly need concurrent writers. *(E3)*
- **Single client only:** if exactly one machine ever touches the file, `PRAGMA locking_mode=EXCLUSIVE` plus rollback-journal mode is workable. *(E2)*
- **Last resort:** the `unix-dotfile` VFS replaces POSIX locks with lock files, dodging broken NFS locking at a real performance and robustness cost. *(E1)*

**One claim stayed unresolved:** E2 says NFSv4 with a correctly configured lock daemon is acceptable for low-concurrency use; E1 says no network filesystem is ever safe. Both positions are shown; verify against SQLite's own documentation before relying on either (see "To verify").

*Bracketed tags name the contributing extractor. The raw answers live in the dispatch records listed below.*
</RichText>
</Section>

<Section title="The chain, walked backwards">
<RichText>
Read top to bottom to walk from the answer back to its origin. Every card below was one dispatch; each names its record, where the full raw text lives.
</RichText>

<Callout tone="success">
**Curator · claude / fable-5** — read the six cards below, deduplicated, kept the contradiction explicit, wrote the Final answer at the top of this page. `TASK-MM1.7 · dispatches/tx_9F44X/report.md`
</Callout>

<RichText>
**▲ synthesized from three blind cross-reviews:**
</RichText>

<Columns>
<Column label="A1 · E1 reviewed E2 + E3">
<RichText>
**Delta found:**

- Challenged E2's "NFSv4 + lock daemon is fine" — marked it a verification target, did not accept it.
- Added the exact SQLite docs section to check against.
- Confirmed E3's replication framing as the strongest path.

`TASK-MM1.4 · dispatches/tx_9F31A/report.md`
</RichText>
</Column>
<Column label="A2 · E2 reviewed E1 + E3">
<RichText>
**Delta found:**

- Added the EXCLUSIVE-locking escape hatch E1 omitted.
- Flagged E3's LiteFS maturity claim as unverified.
- Agreed WAL is impossible across machines — no dissent.

`TASK-MM1.5 · dispatches/tx_9F31B/report.md`
</RichText>
</Column>
<Column label="A3 · E3 reviewed E1 + E2">
<RichText>
**Delta found:**

- Added: SMB fails differently (oplock / lease breaks) — do not lump it with NFS.
- Softened E1's absolute "never" with the single-host loopback caveat.
- Endorsed E2's narrow EXCLUSIVE-mode case.

`TASK-MM1.6 · dispatches/tx_9F31C/report.md`
</RichText>
</Column>
</Columns>

<RichText>
**▲ each review read the two other answers anonymized — never its own:**
</RichText>

<Columns>
<Column label="E1 · claude / fable-5">
<RichText>
**Original answer (excerpt):**

"Don't share SQLite over NFS or SMB. POSIX advisory locking is broken on most network-filesystem implementations, and WAL is impossible across machines — it needs the shared-memory file. If truly forced, the unix-dotfile VFS avoids POSIX locks at a performance cost."

`TASK-MM1.1 · dispatches/tx_9F2KQ/report.md`
</RichText>
</Column>
<Column label="E2 · codex / gpt-5.2">
<RichText>
**Original answer (excerpt):**

"Narrowly workable: with PRAGMA locking_mode=EXCLUSIVE and a rollback journal, a single client is fine. NFSv4 with a correctly configured lock daemon can be acceptable at low concurrency." *(the disputed claim starts here)*

`TASK-MM1.2 · dispatches/tx_9F2KR/report.md`
</RichText>
</Column>
<Column label="E3 · opencode / gemini-3">
<RichText>
**Original answer (excerpt):**

"Wrong question — don't share the file at all. Run SQLite locally and replicate with Litestream or LiteFS, or move to a client-server database if several machines truly write."

`TASK-MM1.3 · dispatches/tx_9F2KS/report.md`
</RichText>
</Column>
</Columns>

<RichText>
**▲ all three answered the same prompt, independently and in parallel:**
</RichText>

<Callout tone="info">
**ROOT — the user's question:** "Can I put a SQLite database on a network filesystem (NFS/SMB) and have several machines use it?"
</Callout>
</Section>

<Section title="Knowledge map">
<Tabs>
<Tab label="Shared core (3/3)">
<RichText>
Claims all three extractors made independently — repetition, not proof:

- POSIX advisory locking (`fcntl`) is unreliable on many NFS/SMB implementations → concurrent writers can corrupt the file.
- WAL mode needs the shared-memory `-shm` file (mmap) → impossible across machines, full stop.
- The common symptom of trying anyway is spurious `database is locked` errors, then silent corruption later.
</RichText>
</Tab>
<Tab label="Unique finds">
<RichText>
Kept even with a support count of one — consensus is repetition, not truth:

- **E2 only:** `PRAGMA locking_mode=EXCLUSIVE` makes single-client use on a network mount workable.
- **E3 only:** Litestream / LiteFS replication sidesteps the problem entirely; usually the right architecture.
- **E1 only:** the `unix-dotfile` VFS swaps POSIX locks for lock files; slow but immune to broken NFS locking.
- **E3 only (via augment round):** SMB behaves differently from NFS — oplocks/lease breaks add their own failure mode; the two should not be lumped together.
</RichText>
</Tab>
<Tab label="Contradictions (1)">
<Callout tone="risk">
**E2 vs E1, unresolved.** E2: NFSv4 with a correctly configured lock daemon is acceptable for low-concurrency workloads. E1: no network filesystem is ever safe for shared SQLite. The curator did not pick a side — both positions appear in the final answer, flagged, with a verification target.
</Callout>
</Tab>
<Tab label="To verify (3)">
<Checklist items={[
  { label: "SQLite official docs: exact wording on WAL + network filesystems", done: false, note: "settles the E1/E2 dispute" },
  { label: "NFSv4 fcntl lock correctness on current Linux kernels", done: false },
  { label: "LiteFS maturity / production status as of today", done: false, note: "E3's claim may be stale" }
]} />
</Tab>
</Tabs>
</Section>

<Section title="Shape feedback — your call">
<Callout tone="info">
Meta section — this part would NOT appear in a real run. Answer here or drop anchored comments anywhere in the artifact; both land as comment threads I can read and fold into the design.
</Callout>

<QuestionForm title="How should the real artifact look?" questions={[
  {
    type: "single",
    prompt: "How much process detail belongs in the final artifact?",
    options: [
      { label: "Summary, like this one", detail: "Final answer + graph + provenance table + knowledge map. Raw answers stay in dispatch records.", recommended: true },
      { label: "Full audit", detail: "Also inline the three raw answers and three augment reports as tabs. Big artifact." },
      { label: "Final answer only", detail: "Just the answer and the contradiction flags. Graph and map only on request." }
    ],
    allowOther: true
  },
  {
    type: "single",
    prompt: "How much of each answer should a chain card show?",
    options: [
      { label: "Excerpt + record link, like now", detail: "A few lines per card; the full raw text stays in the dispatch record.", recommended: true },
      { label: "Full text inline", detail: "Whole answers and reviews expanded inside the chain. Complete, but a very long page." }
    ],
    allowOther: true
  },
  {
    type: "single",
    prompt: "What happens when you comment and hit regenerate?",
    options: [
      { label: "Re-run the curator only", detail: "Cheap and fast: curator re-reads the 6 existing reports plus your comments, writes v2. Fan-out is not repeated.", recommended: true },
      { label: "Comments can trigger a new round", detail: "A comment like 'dig deeper on X' re-dispatches extractors with X in the brief, then re-curates. More machinery." }
    ],
    allowOther: true
  },
  {
    type: "freeform",
    prompt: "What is missing from this mock, or what should be cut?"
  }
]} />
</Section>
